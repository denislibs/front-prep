// Блок тестов B: React/Next.js (qb1–qb45) и CSS/вёрстка (qb46–qb75).
// Формат описан в docs/quiz-format.md.
const QUIZ_B = [

  // ── React и Next.js ───────────────────────────────────────────────

  { id: 'qb1',
    deck: 'react',
    q: 'Приложение обёрнуто в StrictMode, сборка dev. Что попадёт в консоль при первом монтировании Widget?',
    snippet: `function Widget() {
  useEffect(() => {
    console.log('setup');
    return () => console.log('cleanup');
  }, []);
  return <div />;
}`,
    options: [
      'setup',
      'setup, cleanup, setup',
      'setup, setup',
      'setup, setup, cleanup',
    ],
    correct: 1,
    why: `<p>StrictMode в dev симулирует размонтирование и повторное монтирование сразу после первого: <strong>setup → cleanup → setup</strong>. Так проверяется, что cleanup симметричен setup-у — ловятся неотписанные слушатели, незакрытые сокеты, неотменённые запросы.</p>
    <p>Самый привлекательный неверный вариант — <code>setup, setup</code>: люди помнят «эффекты вызываются дважды», но забывают, что между вызовами обязательно проходит cleanup. Если бы React просто вызывал setup дважды подряд, проверка утечек была бы бессмысленной — она держится именно на промежуточной очистке.</p>
    <p>В проде ничего этого нет. Гасить симптом флагом <code>useRef(false)</code> — значит спрятать утечку, а не починить её.</p>`,
    cardId: 'rx21' },

  { id: 'qb2',
    deck: 'react',
    q: 'В каком порядке выведутся строки при первом монтировании дерева Parent > Child?',
    snippet: `function Child() {
  useLayoutEffect(() => console.log('child layout'));
  useEffect(() => console.log('child effect'));
  return null;
}

function Parent() {
  useLayoutEffect(() => console.log('parent layout'));
  useEffect(() => console.log('parent effect'));
  return <Child />;
}`,
    options: [
      'parent layout, child layout, parent effect, child effect',
      'child layout, parent layout, child effect, parent effect',
      'child layout, child effect, parent layout, parent effect',
      'parent layout, parent effect, child layout, child effect',
    ],
    correct: 1,
    why: `<p>Работают два независимых правила. Первое: обход дерева идёт <strong>снизу вверх</strong> — работа над фибером завершается только после всех потомков, поэтому ребёнок получает свой эффект раньше родителя. Второе: <strong>все layout-эффекты дерева</strong> отрабатывают синхронно в commit до отрисовки, и только потом планируются пассивные эффекты.</p>
    <p>Вариант <code>child layout, child effect, parent layout, parent effect</code> выглядит логично, если считать, что React обрабатывает компоненты покомпонентно целиком. Он не обрабатывает: фазы разведены по всему дереву, иначе часть экрана уже была бы отрисована, пока другая часть ещё измеряется.</p>
    <p>Практическое следствие: порядок между <strong>братьями</strong> определяется порядком в JSX и не гарантирован — строить на нём логику нельзя.</p>`,
    cardId: 'rx25' },

  { id: 'qb3',
    deck: 'react',
    q: 'Два эффекта без массива зависимостей в одном компоненте. Что выведется при повторном рендере?',
    snippet: `useEffect(() => {
  console.log('A setup');
  return () => console.log('A cleanup');
});

useEffect(() => {
  console.log('B setup');
  return () => console.log('B cleanup');
});`,
    options: [
      'A cleanup, A setup, B cleanup, B setup',
      'B cleanup, A cleanup, A setup, B setup',
      'A cleanup, B cleanup, A setup, B setup',
      'A setup, B setup, A cleanup, B cleanup',
    ],
    correct: 2,
    why: `<p>При обновлении React делает <strong>сначала все cleanup, потом все setup</strong> — и по дереву, и внутри одного компонента. Это даёт согласованность: в момент любого setup всё уже находится в новом состоянии.</p>
    <p>Внутри компонента порядок <strong>прямой</strong>, в порядке объявления хуков. Отсюда самая частая ошибка — вариант <code>B cleanup, A cleanup, ...</code>: по аналогии со стеком или с <code>defer</code> ждут обратного порядка размонтирования. React такой семантики не даёт.</p>
    <p>Вариант с чередованием (<code>A cleanup, A setup, B cleanup, B setup</code>) — это модель «покомпонентно», которой в React тоже нет.</p>`,
    cardId: 'rx25' },

  { id: 'qb4',
    deck: 'react',
    q: 'Row обёрнут в memo, но перерисовывается на каждый клик по кнопке. Почему?',
    snippet: `const Row = memo(function Row({ style, onPick }) {
  return <li style={style} onClick={onPick}>строка</li>;
});

function List() {
  const [n, setN] = useState(0);
  return (
    <ul onClick={() => setN(n + 1)}>
      <Row style={{ padding: 8 }} onPick={() => {}} />
    </ul>
  );
}`,
    options: [
      'memo без второго аргумента не сравнивает пропсы — нужен явный компаратор',
      'memo не спасает от рендера родителя: изменение состояния родителя всегда рендерит поддерево',
      'memo сравнивает пропсы глубоко, но функции сравнить не умеет и возвращает false',
      'объект-литерал и стрелка пересоздаются каждый рендер, поверхностное сравнение даёт false',
    ],
    correct: 3,
    why: `<p><code>memo</code> сравнивает пропсы <strong>поверхностно</strong>, через <code>Object.is</code> по каждому ключу. Литерал <code>{ padding: 8 }</code> и стрелка <code>() =&gt; {}</code> — новые ссылки на каждом рендере <code>List</code>, поэтому сравнение всегда даёт false и мемоизация не срабатывает ни разу.</p>
    <p>Самый привлекательный неверный вариант — «memo не спасает от рендера родителя». Он звучит правдоподобно, но неверен: именно от этого <code>memo</code> и защищает, если пропсы стабильны по ссылке. Проблема не в родителе, а в том, что пропсы каждый раз новые.</p>
    <p>Лечится выносом стиля в константу модуля и <code>useCallback</code> на обработчике — либо тем, что <code>memo</code> здесь вообще не нужен, если рендер строки дешёвый.</p>`,
    cardId: 'rx36' },

  { id: 'qb5',
    deck: 'react',
    q: 'Что покажет счётчик примерно через пять секунд после монтирования?',
    snippet: `function Counter() {
  const [n, setN] = useState(0);

  useEffect(() => {
    const id = setInterval(() => setN(n + 1), 1000);
    return () => clearInterval(id);
  }, []);

  return <b>{n}</b>;
}`,
    options: [
      '5 — интервал вызывает setN каждую секунду',
      '1 — колбэк интервала навсегда замкнул n === 0',
      '0 — setN внутри интервала не вызывает перерисовку',
      '4 или 5 в зависимости от таймингов планировщика',
    ],
    correct: 1,
    why: `<p>Эффект с пустым массивом зависимостей выполнился один раз и замкнул <code>n</code> первого рендера — ноль. Колбэк интервала на каждом тике вычисляет <code>0 + 1</code> и вызывает <code>setN(1)</code>. Первый раз это меняет состояние, дальше React видит то же значение через <code>Object.is</code> и рендер не планирует.</p>
    <p>Вариант «5» — то, чего ждёт человек, читающий код как императивный: интервал живой, сеттер вызывается, значит растёт. Живой он действительно, но значение он берёт из мёртвого замыкания.</p>
    <p>Правильно — функциональный апдейт <code>setN(v =&gt; v + 1)</code>: он не зависит от значения из замыкания и позволяет оставить зависимости пустыми честно.</p>`,
    cardId: 'r12' },

  { id: 'qb6',
    deck: 'react',
    q: 'Список неконтролируемых полей отрисован с key={index}. Пользователь заполнил три поля, затем удалили первый элемент. Что произойдёт с введённым текстом?',
    snippet: `{items.map((item, index) => (
  <input key={index} defaultValue="" placeholder={item.title} />
))}`,
    options: [
      'значения сдвинутся: React переиспользует поля по позиции, текст останется не у тех элементов',
      'все поля очистятся: React пересоздаст DOM-узлы целиком',
      'ничего не изменится: значение живёт в DOM и привязано к своему элементу',
      'React выведет предупреждение о дубликате ключей и не применит удаление',
    ],
    correct: 0,
    why: `<p><code>key</code> — это идентификатор элемента для реконсилятора. Индекс описывает <strong>позицию</strong>, а не сущность: после удаления первого элемента бывший второй получает key 0, и React считает, что это «тот же» элемент, что был раньше на нулевой позиции. DOM-узел переиспользуется вместе со всем, что в нём живёт вне React — введённым текстом, позицией курсора, состоянием прокрутки.</p>
    <p>Вариант «ничего не изменится» кажется верным именно потому, что поля неконтролируемые: значение действительно в DOM. Но проблема ровно в этом — React переиспользовал не тот DOM-узел, и значение «переехало» к чужим данным.</p>
    <p>Ключ должен быть стабильным идентификатором данных. Индекс допустим только для списка, который никогда не переупорядочивается и не редактируется.</p>`,
    cardId: 'r3' },

  { id: 'qb7',
    deck: 'react',
    q: 'Коллега поставил key={Math.random()} элементам списка, чтобы «гарантированно избежать конфликтов ключей». Что получится?',
    options: [
      'ключи станут уникальными, поведение списка не изменится',
      'React перестанет переиспользовать DOM только при изменении данных — это безопасная оптимизация',
      'на каждом рендере элементы будут пересоздаваться: слетит фокус, состояние и позиция прокрутки',
      'React выбросит ошибку: ключ обязан быть строкой или числом из данных',
    ],
    correct: 2,
    why: `<p>Ключ вычисляется заново на каждом рендере, поэтому реконсилятор никогда не находит совпадения со старым деревом: все элементы размонтируются и монтируются заново. Практические последствия — потеря фокуса прямо во время ввода, сброс локального состояния строк, перезапуск запросов и анимаций, полная перестройка DOM вместо точечных мутаций.</p>
    <p>Привлекательный неверный вариант — «ключи станут уникальными, поведение не изменится». Уникальность действительно достигается, но задача ключа не в уникальности как таковой, а в <strong>стабильном отождествлении</strong> элемента между рендерами. Случайный ключ уникален и абсолютно бесполезен.</p>
    <p>Тот же эффект даёт <code>key={Date.now()}</code> и любой ключ, вычисляемый в рендере.</p>`,
    cardId: 'rx29' },

  { id: 'qb8',
    deck: 'react',
    q: 'Серверный компонент передаёт клиентскому проп-функцию. Что произойдёт?',
    snippet: `// app/page.tsx — серверный компонент
import Chart from './chart'; // в файле chart.tsx стоит 'use client'

export default async function Page() {
  const data = await getData();
  return <Chart data={data} format={(v) => v.toFixed(2)} />;
}`,
    options: [
      'всё работает: Next сериализует функцию в строку и восстановит её на клиенте',
      'ошибка сериализации: функции нельзя передавать через границу, кроме ссылок на Server Actions',
      'всё работает, но format выполнится на сервере при рендере и на клиент уедет уже результат',
      'Chart потеряет директиву и станет серверным компонентом',
    ],
    correct: 1,
    why: `<p>Пропсы через границу сервер → клиент проходят через RSC-протокол и обязаны быть сериализуемыми. Можно: примитивы, массивы, простые объекты, <code>Date</code>, <code>Map</code>, <code>Set</code>, промисы, JSX-элементы и ссылки на Server Actions. Нельзя: обычные функции, классы и их инстансы, символы, объекты с методами и прототипами.</p>
    <p>Вариант с «сериализацией в строку» звучит правдоподобно, потому что у функции действительно есть <code>toString()</code>. Но восстановить замыкание из текста невозможно — форматтер потерял бы весь контекст, в котором был создан. Поэтому рантайм честно падает, причём именно в рантайме конкретного маршрута, а не на сборке.</p>
    <p>Решение — определить <code>format</code> внутри клиентского компонента или передать вместо функции сериализуемое описание: локаль, число знаков, имя пресета.</p>`,
    cardId: 'rx64' },

  { id: 'qb9',
    deck: 'react',
    q: 'Клиентский провайдер темы должен обернуть тяжёлый серверный список, не утаскивая его в бандл. Как это сделать?',
    options: [
      'импортировать серверный список внутрь клиентского провайдера — граница вниз не распространяется',
      'отрендерить провайдер в серверном компоненте и передать серверный список как children',
      'пометить серверный список директивой use server — тогда его безопасно импортировать в клиентский файл',
      'обернуть серверный список в dynamic() с ssr: false',
    ],
    correct: 1,
    why: `<p>Директива <code>'use client'</code> распространяется <strong>транзитивно вниз по импортам</strong>: всё, что клиентский модуль импортирует, тоже становится клиентским. Но она <strong>не распространяется через children</strong> — элемент, созданный в серверном компоненте и переданный клиентскому пропсом, остаётся серверным и приезжает уже отрендеренным.</p>
    <p>Первый вариант — самая частая ошибка новичка и потому самый привлекательный: кажется, что импорт внутрь обёртки ничего не меняет. Меняет: список и все его зависимости (парсеры, форматтеры, доступ к данным) уезжают в клиентский бандл.</p>
    <p><code>'use server'</code> помечает не компоненты, а Server Actions — это про другое. <code>dynamic(..., { ssr: false })</code> вообще относится к клиентским компонентам и только откладывает загрузку.</p>`,
    cardId: 'rx64' },

  { id: 'qb10',
    deck: 'react',
    q: 'Что на самом деле означает директива use client в начале файла?',
    options: [
      'компонент рендерится только в браузере, серверного HTML для него не будет',
      'компонент можно импортировать в серверные файлы, но не наоборот',
      'React выключит для этого поддерева стриминг и отдаст его отдельным запросом',
      'это точка входа в клиентский граф: модуль и все его импорты попадут в бандл, а SSR всё равно отрендерит HTML',
    ],
    correct: 3,
    why: `<p>Директива объявляет <strong>границу</strong> между серверным и клиентским графом модулей, а не «место исполнения». Клиентский компонент при SSR всё так же рендерится на сервере в HTML, а затем гидратируется в браузере. Отличие от серверного в том, что его код уезжает в бандл и он умеет иметь состояние, эффекты и обработчики.</p>
    <p>Самый привлекательный неверный вариант — «рендерится только в браузере». Так работает <code>dynamic(..., { ssr: false })</code>, но не <code>'use client'</code>. Из-за этой путаницы люди удивляются, что в клиентском компоненте всё равно падает обращение к <code>window</code> на первом рендере.</p>
    <p>Практическое следствие: директива нужна один раз на границе, а не в каждом файле поддерева.</p>`,
    cardId: 'r15' },

  { id: 'qb11',
    deck: 'react',
    q: 'Сколько рендеров вызовет один клик в React 18 или новее?',
    snippet: `function App() {
  const [n, setN] = useState(0);
  const [m, setM] = useState(0);

  async function onClick() {
    setN(n + 1);
    setM(m + 1);
    await Promise.resolve();
    setN(v => v + 1);
    setM(v => v + 1);
  }

  return <button onClick={onClick}>{n}:{m}</button>;
}`,
    options: [
      'четыре — каждый сеттер даёт свой рендер',
      'один — все обновления одного обработчика попадают в один батч',
      'два — батч до await и батч после него',
      'три — синхронная пара батчится, асинхронные обновления идут по одному',
    ],
    correct: 2,
    why: `<p>Автоматический батчинг в React 18 работает не только в обработчиках событий, но и в промисах, таймаутах и нативных колбэках. Поэтому пара сеттеров до <code>await</code> даёт один рендер, и пара после <code>await</code> — тоже один. Итого два.</p>
    <p>Вариант «три» — это ровно поведение React 17: там батчились только обновления внутри синтетических событий, а всё, что после <code>await</code>, вызывало по рендеру на сеттер. Поэтому он и самый привлекательный: человек помнит правило, но не помнит, что оно изменилось.</p>
    <p>Границу батча задаёт микрозадача, а не обработчик: разорвать её может любое ожидание, а объединить два таких блока в один коммит нельзя.</p>`,
    cardId: 'rx26' },

  { id: 'qb12',
    deck: 'react',
    q: 'В каком случае flushSync действительно оправдан?',
    options: [
      'чтобы прочитать новое значение состояния из переменной сразу после вызова сеттера',
      'чтобы измерить обновлённый DOM и доскроллить к новому элементу в том же обработчике',
      'чтобы гарантировать порядок нескольких setState внутри одного обработчика',
      'чтобы отключить конкурентный рендеринг на время тяжёлого обновления списка',
    ],
    correct: 1,
    why: `<p><code>flushSync</code> нужен там, где следующая строка кода обязана видеть <strong>уже обновлённый DOM</strong>: добавили сообщение в чат и сразу скроллим вниз, вставили строку и сразу ставим в неё фокус, снимаем размер для позиционирования. Синхронный коммит здесь — единственный способ не показать пользователю промежуточный кадр.</p>
    <p>Самый привлекательный неверный вариант — «прочитать новое состояние». Он выглядит логично, но состояние в замыкании текущего рендера не изменится даже после синхронного коммита: переменная <code>n</code> связана с этим рендером навсегда. Нужное значение получают функциональным апдейтом или простым вычислением, а не насильственным рендером.</p>
    <p>Цена <code>flushSync</code> высока: он выключает батчинг с внешними обновлениями, а в цикле по элементам гарантированно даёт фриз.</p>`,
    cardId: 'rx26' },

  { id: 'qb13',
    deck: 'react',
    q: 'Пользователь выбирает в списке другого пользователя, проп user меняется. Что покажет поле ввода?',
    snippet: `function Editor({ user }) {
  const [name, setName] = useState(user.name);
  return (
    <input value={name} onChange={e => setName(e.target.value)} />
  );
}`,
    options: [
      'старое имя — инициализатор useState отработал только при монтировании',
      'новое имя — React переинициализирует состояние при изменении пропсов',
      'пустую строку — React сбросит состояние, обнаружив смену пропа',
      'новое имя после первого ввода, до этого старое',
    ],
    correct: 0,
    why: `<p>Аргумент <code>useState</code> — это <strong>начальное</strong> значение, и оно используется ровно один раз, при монтировании фибера. Дальше состояние живёт самостоятельно и на пропсы не смотрит. Компонент не размонтировался — значит, в поле останется имя первого пользователя.</p>
    <p>Вариант «новое имя» — классическое заблуждение, и оно живучее потому, что в списке из одного элемента ошибка не видна: до первого переключения всё выглядит правильно. Это типичный баг формы редактирования, который находят на проде.</p>
    <p>Лечится не эффектом-синхронизатором, а <code>key={user.id}</code> на компоненте: смена ключа пересоздаёт состояние целиком и делает намерение явным.</p>`,
    cardId: 'rx28' },

  { id: 'qb14',
    deck: 'react',
    q: 'Какой способ сбросить всё локальное состояние формы при смене редактируемой сущности правильный?',
    options: [
      'useEffect, который при смене id вызывает набор сеттеров с новыми значениями',
      'useMemo по id, возвращающий свежий объект начальных значений',
      'key с идентификатором сущности на самом компоненте формы',
      'ref-флаг, который сравнивает предыдущий id с текущим прямо в рендере',
    ],
    correct: 2,
    why: `<p><code>key</code> — штатный механизм React для «это другой экземпляр». Смена ключа размонтирует старый компонент и монтирует новый: обнуляются все хуки состояния разом, без перечисления полей. Ничего не забудешь, потому что перечислять нечего.</p>
    <p>Вариант с <code>useEffect</code> и набором сеттеров — самый распространённый и самый привлекательный: он «работает». Цена — лишний рендер со старыми данными перед сбросом (пользователь видит мелькание), забытое поле при добавлении нового и каскад эффектов, порядок которых потом никто не восстановит.</p>
    <p><code>useMemo</code> не хранит состояние вообще, а сравнение в рендере через ref — нечистый рендер, который выбивает компонент из оптимизаций компилятора.</p>`,
    cardId: 'rx29' },

  { id: 'qb15',
    deck: 'react',
    q: 'Что не так с этой строкой?',
    snippet: `const [store] = useState(createStore());`,
    options: [
      'ничего: React вызовет createStore один раз при монтировании',
      'состояние без сеттера — React выведет предупреждение о неиспользуемом хуке',
      'стор попадёт в состояние как функция, а не как объект',
      'createStore вызывается на каждом рендере: объекты создаются и молча остаются жить',
    ],
    correct: 3,
    why: `<p>Здесь передан <strong>результат вызова</strong>, а не сама функция. JS вычисляет аргумент до вызова <code>useState</code>, поэтому <code>createStore()</code> отрабатывает на каждом рендере. React отбрасывает всё, кроме первого значения, но объект-то уже создан — вместе с подписками, таймерами и соединениями внутри. Утечка тихая: приложение работает, память растёт.</p>
    <p>Вариант «ничего страшного» самый привлекательный именно потому, что видимого бага нет: <code>store</code> действительно стабилен и указывает на первый экземпляр. Проблема в брошенных копиях, а не в возвращённом значении.</p>
    <p>Правильно — ленивый инициализатор: <code>useState(createStore)</code> или <code>useState(() =&gt; createStore(config))</code>. Помни, что в StrictMode он вызовется дважды, поэтому побочных эффектов в нём быть не должно.</p>`,
    cardId: 'rx27' },

  { id: 'qb16',
    deck: 'react',
    q: 'Проп withSearch меняется с true на false. Что произойдёт?',
    snippet: `function Panel({ withSearch }) {
  if (withSearch) {
    const [query, setQuery] = useState('');
  }
  const [open, setOpen] = useState(false);
  // ...
}`,
    options: [
      'ничего особенного: React просто забудет состояние query',
      'порядок хуков съедет — open получит значение, которое хранилось для query',
      'React бросит ошибку ещё при первом рендере, независимо от значения пропа',
      'query сохранится в фибере навсегда и будет занимать память',
    ],
    correct: 1,
    why: `<p>React не знает имён хуков. Состояние хранится <strong>списком по порядку вызова</strong>: первый <code>useState</code> в этом рендере читает первую ячейку, второй — вторую. Пока <code>withSearch</code> истинен, ячейка 0 — это query, ячейка 1 — open. Как только условие стало ложным, <code>useState(false)</code> для open оказывается первым вызовом и читает ячейку 0, то есть строку от query.</p>
    <p>Вариант «React бросит ошибку сразу» привлекателен, потому что линтер ругается на такой код всегда. Но линтер — статическая проверка; рантайм молчит ровно до момента, когда число хуков между рендерами изменится. Именно поэтому баг доживает до прода.</p>
    <p>Правило одно: хуки вызываются безусловно на верхнем уровне, а условной делают логику <strong>внутри</strong> хука.</p>`,
    cardId: 'r7' },

  { id: 'qb17',
    deck: 'react',
    q: 'В поиске стоит useDeferredValue, но интерфейс всё равно фризит при вводе. Что проверить первым делом?',
    options: [
      'мемоизирован ли тяжёлый список: без memo он пересобирается на каждом рендере, и отложенное значение ничего не даёт',
      'обёрнут ли setQuery инпута в startTransition — без этого хук не активируется',
      'не стоит ли рядом useTransition: два приоритетных API конфликтуют и отменяют друг друга',
      'достаточно ли большой таймаут передан вторым аргументом useDeferredValue',
    ],
    correct: 0,
    why: `<p><code>useDeferredValue</code> лишь возвращает <strong>отставшую копию</strong> значения и помечает рендер с новым значением как низкоприоритетный. Если потребитель не обёрнут в <code>memo</code>, он всё равно перерисовывается на срочном рендере вместе с инпутом — вся работа выполняется в том же кадре, и отставание не помогает. Это ошибка номер один при внедрении хука.</p>
    <p>Вариант с <code>startTransition</code> вокруг <code>setQuery</code> привлекателен и при этом активно вреден: обернув обновление самого поля, ты заставишь инпут отставать от клавиатуры. Транзишен применяют к производной части, а не к вводу.</p>
    <p>Второго аргумента-таймаута у хука нет — API времени не принимает.</p>`,
    cardId: 'rx7' },

  { id: 'qb18',
    deck: 'react',
    q: 'Что произойдёт, если обернуть в startTransition обновление состояния самого текстового поля?',
    snippet: `<input
  value={query}
  onChange={e => startTransition(() => setQuery(e.target.value))}
/>`,
    options: [
      'ввод станет плавнее: React отдаст приоритет отрисовке символов',
      'ничего не изменится: React и так обрабатывает ввод с высоким приоритетом',
      'поле начнёт отставать от клавиатуры — обновление value стало прерываемым',
      'React выбросит предупреждение: контролируемое поле нельзя обновлять в транзишене',
    ],
    correct: 2,
    why: `<p>Транзишен помечает обновление как <strong>прерываемое</strong>: React вправе отложить его, выбросить и пересчитать заново, пока приходят более срочные события. Для контролируемого поля это означает, что <code>value</code> обновляется с задержкой, а браузер тем временем показывает старый символ — классический «резиновый» ввод.</p>
    <p>Самый привлекательный неверный вариант — «станет плавнее». Он опирается на верную интуицию (транзишены про отзывчивость), но перепутывает роли: срочным должно оставаться то, что пользователь печатает, а откладывать надо результат — фильтрацию, список, график.</p>
    <p>Правильный паттерн: <code>setQuery</code> вне транзишена, а тяжёлое производное состояние — внутри, либо через <code>useDeferredValue</code> плюс <code>memo</code> на потребителе.</p>`,
    cardId: 'rx6' },

  { id: 'qb19',
    deck: 'react',
    q: 'Profile загружает данные в useEffect и кладёт их в состояние. Когда пользователь увидит Skeleton?',
    snippet: `<Suspense fallback={<Skeleton />}>
  <Profile />
</Suspense>`,
    options: [
      'на время первого запроса, пока данные не пришли',
      'никогда: эффект не приостанавливает рендер, границе нечего ловить',
      'только при первой навигации на маршрут, при повторных — нет',
      'на время запроса и при каждом обновлении данных',
    ],
    correct: 1,
    why: `<p>Suspense реагирует на одно: компонент во время <strong>рендера</strong> бросил промис — через <code>use()</code>, <code>React.lazy</code>, RSC-await или интеграцию библиотеки данных. Загрузка в <code>useEffect</code> происходит уже после коммита, рендер к этому моменту успешно завершился, приостанавливать нечего.</p>
    <p>Вариант «на время первого запроса» — самое частое ожидание: раз есть fallback и есть загрузка, значит связаны. Не связаны. Пользователь увидит компонент с пустым состоянием, поэтому спиннер приходится рисовать руками внутри <code>Profile</code>.</p>
    <p>Второй нюанс той же темы: граница должна быть <strong>выше</strong> приостанавливающегося компонента. Suspense, обёрнутый вокруг самого себя, ничего не поймает.</p>`,
    cardId: 'rx8' },

  { id: 'qb20',
    deck: 'react',
    q: 'Серверный компонент делает два независимых запроса подряд через await, страница отдаётся вдвое медленнее. Что применить?',
    options: [
      'обернуть каждый запрос в отдельную границу Suspense',
      'вынести запросы в useEffect клиентского компонента',
      'закешировать оба запроса через cache(), чтобы второй взялся из кеша',
      'запустить оба промиса сразу и дождаться их через Promise.all',
    ],
    correct: 3,
    why: `<p>Подряд идущие <code>await</code> в одном компоненте — это последовательный водопад: второй запрос физически не стартует, пока не разрешился первый. Если данные независимы, промисы создают одновременно и ждут вместе: <code>const [a, b] = await Promise.all([getA(), getB()])</code>. Время становится равным максимуму, а не сумме.</p>
    <p>Отдельные границы Suspense — самый привлекательный неверный ответ, потому что стриминг действительно связан с водопадами. Но границы меняют только <strong>порядок доставки</strong> уже полученных кусков; внутри одного компонента два <code>await</code> так и останутся последовательными.</p>
    <p><code>cache()</code> решает другую задачу — дедупликацию одинаковых запросов из соседних компонентов. Разные запросы она не ускорит.</p>`,
    cardId: 'rx9' },

  { id: 'qb21',
    deck: 'react',
    q: 'Клиентский компонент читает данные так. Что произойдёт?',
    snippet: `'use client';

function Prices({ url }) {
  const data = use(fetch(url).then(r => r.json()));
  return <List items={data} />;
}`,
    options: [
      'бесконечная приостановка: каждый рендер создаёт новый промис',
      'данные загрузятся один раз, use закеширует промис по url',
      'ошибка: use нельзя вызывать в клиентском компоненте',
      'данные загрузятся, но React предупредит о запросе в рендере',
    ],
    correct: 0,
    why: `<p><code>use()</code> приостанавливает компонент до разрешения промиса. Промис создаётся <strong>в теле рендера</strong>, поэтому после разрешения React рендерит компонент заново, тот вызывает <code>fetch</code> ещё раз, получает новый промис — и снова приостанавливается. Цикл не заканчивается, а в Network видно бесконечную ленту запросов.</p>
    <p>Вариант «use закеширует промис по url» звучит логично для человека, который переносит на хук привычки React Query. Кеша у <code>use()</code> нет вообще: ни дедупликации, ни инвалидации, ни ретраев. Это примитив чтения, а не слой данных.</p>
    <p>Правильно: промис создаёт серверный компонент и передаёт пропсом, либо клиент берёт данные из библиотеки с кешем. Внутри клиента <code>use()</code> уместен для контекста и для уже стабильного промиса.</p>`,
    cardId: 'rx10' },

  { id: 'qb22',
    deck: 'react',
    q: 'Server Action удаляет проект. Откуда брать идентификатор текущего пользователя для проверки прав?',
    options: [
      'из скрытого поля формы: значение подставляется на сервере при рендере',
      'из пропа, переданного серверным компонентом в bind экшена',
      'из серверной сессии или токена, прочитанного внутри самого экшена',
      'из middleware: оно уже проверило доступ к странице до рендера',
    ],
    correct: 2,
    why: `<p>Server Action — это <strong>публичный HTTP-эндпоинт</strong> с автогенерированным идентификатором. Его вызывают напрямую, минуя вашу страницу, форму и любой клиентский код. Значит, всё, что приходит снаружи — поля формы, аргументы, привязанные через <code>bind</code>, — это ввод пользователя, который можно подделать. Личность и права берутся только из того, что сервер выводит сам: сессии, cookie, токена.</p>
    <p>Скрытое поле — самый привлекательный неверный вариант: оно и правда заполняется на сервере при рендере, поэтому кажется доверенным. Но до отправки оно живёт в DOM пользователя и правится в DevTools за секунду.</p>
    <p>Middleware тоже не защита: его матчер покрывает маршруты страниц, а экшены и route handlers дёргаются напрямую. Авторизация обязана быть внутри экшена.</p>`,
    cardId: 'rx11' },

  { id: 'qb23',
    deck: 'react',
    q: 'Форму переписали с action={formAction} на onSubmit с ручным вызовом экшена. Что потеряли?',
    options: [
      'типизацию аргументов экшена — она выводится только из атрибута action',
      'работу формы до гидратации и без JS: нативная отправка больше не происходит',
      'возможность использовать useActionState — хук требует именно onSubmit',
      'автоматическую валидацию полей по атрибутам required и type',
    ],
    correct: 1,
    why: `<p>Функция в атрибуте <code>action</code> — это прогрессивное улучшение: React ставит форме настоящий обработчик отправки, а Next дополнительно генерирует эндпоинт. Пока JS не загрузился или не выполнился, браузер отправляет форму сам, обычным POST. С <code>onSubmit</code> и <code>preventDefault</code> этот путь исчезает: до гидратации кнопка не делает ничего.</p>
    <p>Вариант с валидацией по атрибутам самый привлекательный, потому что интуитивно связан с «нативностью формы». Но <code>required</code> и <code>type="email"</code> проверяет браузер независимо от того, как вы обрабатываете отправку — они не теряются.</p>
    <p>Проверять это выключением JS в DevTools недостаточно: реальный сценарий — JS включён, но ещё не выполнился. Тестируй троттлингом CPU и сети.</p>`,
    cardId: 'rx13' },

  { id: 'qb24',
    deck: 'react',
    q: 'Кнопка никогда не становится disabled. Почему?',
    snippet: `function CommentForm({ action }) {
  const { pending } = useFormStatus();
  return (
    <form action={action}>
      <textarea name="text" />
      <button disabled={pending}>Отправить</button>
    </form>
  );
}`,
    options: [
      'useFormStatus работает только с формами, у которых есть useActionState',
      'нужно импортировать хук из react, а не из react-dom',
      'pending обновляется асинхронно и не успевает попасть в первый рендер',
      'хук вызван в компоненте, который сам рендерит form — он читает статус родительской формы',
    ],
    correct: 3,
    why: `<p><code>useFormStatus</code> читает статус <strong>ближайшей формы-предка в React-дереве</strong>. Компонент, который сам рендерит <code>&lt;form&gt;</code>, находится не внутри неё, а снаружи, поэтому предка-формы у него нет и <code>pending</code> навсегда остаётся false. Ошибок при этом нет — просто тишина.</p>
    <p>Вариант про импорт привлекателен, потому что путаница <code>react</code> и <code>react-dom</code> для этого хука реальна: он действительно живёт в <code>react-dom</code>. Но неверный импорт дал бы ошибку сборки или undefined, а не молча ложный статус.</p>
    <p>Лечится выносом кнопки в отдельный компонент: <code>&lt;SubmitButton /&gt;</code> внутри формы — там хук найдёт предка и заработает.</p>`,
    cardId: 'rx15' },

  { id: 'qb25',
    deck: 'react',
    q: 'Разработчик вызывает addOptimistic прямо в обработчике клика, вне транзишена и вне экшена. Что произойдёт?',
    options: [
      'оптимистичное значение молча не применится — состояние не изменится вовсе',
      'значение применится, но не откатится после ответа сервера',
      'React бросит ошибку: useOptimistic требует транзишена',
      'значение применится и останется до следующей перерисовки родителя',
    ],
    correct: 0,
    why: `<p><code>useOptimistic</code> держит наложение поверх реального состояния и живёт ровно столько, сколько длится транзишен или Action. Вызов <code>addOptimistic</code> вне транзишена не с чем связать: React не запускает оптимистичный рендер, и на экране ничего не меняется. Это самая частая жалоба «хук не работает».</p>
    <p>Вариант «применится, но не откатится» привлекателен, потому что описывает знакомую механику ручной оптимистичности через <code>useState</code>. У этого хука откат не пишут руками — он происходит автоматически, когда транзишен завершается и приходит настоящее состояние. Нет транзишена — нет и наложения.</p>
    <p>Решение: вызывать <code>addOptimistic</code> первым делом внутри функции, переданной в <code>action</code> формы или в <code>startTransition</code>.</p>`,
    cardId: 'rx14' },

  { id: 'qb26',
    deck: 'react',
    q: 'Модалка отрендерена порталом в document.body. Сработает ли onClick на обёртке из React-дерева при клике по кнопке в модалке?',
    snippet: `function Card() {
  return (
    <div onClick={() => console.log('card')}>
      {createPortal(
        <button onClick={() => console.log('btn')}>Ок</button>,
        document.body
      )}
    </div>
  );
}`,
    options: [
      'нет: в DOM кнопка вне .card, событие туда не дойдёт',
      'нет: React специально изолирует события порталов',
      'да: выведется btn, затем card — синтетическое событие идёт по React-дереву',
      'да, но только если на портале явно включён проброс через свойство bubbles',
    ],
    correct: 2,
    why: `<p>Портал меняет место в <strong>DOM</strong>, но не в <strong>React-дереве</strong>. Синтетические события всплывают по дереву компонентов, поэтому обработчик родителя-обёртки сработает, хотя физически узлы лежат в разных ветках документа. Выведется <code>btn</code>, затем <code>card</code>.</p>
    <p>Самый привлекательный неверный вариант — первый: он опирается на верное наблюдение (в DOM кнопки внутри карточки действительно нет) и делает из него неверный вывод. Именно на этом расхождении ломаются попытки закрыть дропдаун нативным слушателем на <code>document</code>: клик внутри портала для него выглядит как клик снаружи.</p>
    <p>Отсюда правило: не смешивай нативные и синтетические обработчики в одном сценарии — выбери одну модель.</p>`,
    cardId: 'rx22' },

  { id: 'qb27',
    deck: 'react',
    q: 'Какому нативному событию соответствует onChange у текстового поля в React?',
    options: [
      'change — React лишь переименовал его для единообразия',
      'input — поэтому обработчик срабатывает на каждый символ',
      'keyup — React слушает клавиатуру и вычисляет значение',
      'change с полифилом, который добавляет срабатывание по таймеру',
    ],
    correct: 1,
    why: `<p>React сознательно отказался от нативной семантики: <code>onChange</code> подписан на <code>input</code> и стреляет на каждое изменение значения, включая вставку мышью и автозаполнение. Нативный <code>change</code> у текстового поля срабатывает только при потере фокуса — в модели контролируемых компонентов это было бы бесполезно.</p>
    <p>Первый вариант привлекателен, потому что имя совпадает и «переименовали» звучит безобидно. Разница принципиальна: код, ожидающий поведения нативного <code>change</code>, будет отправлять запрос на каждую букву.</p>
    <p>Если действительно нужен нативный <code>change</code> — есть <code>onBlur</code> или подписка через ref на реальный DOM-узел.</p>`,
    cardId: 'rx23' },

  { id: 'qb28',
    deck: 'react',
    q: 'В React-обработчике вызывается e.stopPropagation(), но сторонний нативный слушатель на document всё равно срабатывает. Как остановить его?',
    options: [
      'вернуть false из React-обработчика',
      'повесить обработчик в фазе перехвата через onClickCapture',
      'вызвать e.preventDefault() дополнительно к stopPropagation',
      'вызвать e.nativeEvent.stopImmediatePropagation()',
    ],
    correct: 3,
    why: `<p>React вешает свои слушатели на корневой контейнер приложения и разыгрывает всплытие по дереву компонентов сам. <code>e.stopPropagation()</code> останавливает это синтетическое распространение, но нативное событие к моменту вызова обработчика уже дошло до корня и продолжит путь дальше по DOM. Чтобы прервать его, нужен доступ к настоящему событию: <code>e.nativeEvent.stopImmediatePropagation()</code>.</p>
    <p>Самый привлекательный неверный вариант — <code>preventDefault</code>: его постоянно путают со <code>stopPropagation</code>. Он отменяет действие браузера по умолчанию (переход по ссылке, отправку формы) и к распространению события отношения не имеет.</p>
    <p>Если слушатель на <code>document</code> ваш собственный, чаще правильнее перевесить его на корень React-приложения — тогда порядок станет предсказуемым без хаков.</p>`,
    cardId: 'rx23' },

  { id: 'qb29',
    deck: 'react',
    q: 'Компонент часто перерисовывается. Что не так с этим ref-колбэком?',
    snippet: `<div
  ref={(node) => {
    if (node) new ResizeObserver(onResize).observe(node);
  }}
/>`,
    options: [
      'ResizeObserver нельзя создавать в ref-колбэке, только в useEffect',
      'колбэк вызовется до вставки узла в документ, и observe промахнётся',
      'колбэк новый на каждом рендере: React переприсоединяет его и наблюдатели накапливаются',
      'node здесь всегда null, потому что div не является компонентом',
    ],
    correct: 2,
    why: `<p>Инлайновая стрелка — новая функция на каждом рендере. React видит, что значение <code>ref</code> изменилось, отвязывает старый колбэк (вызывая его с <code>null</code> или его cleanup) и вызывает новый. Каждый такой цикл создаёт ещё один <code>ResizeObserver</code>, а старые никто не отключает — они держат узел и продолжают работать.</p>
    <p>Вариант «колбэк вызовется до вставки узла» привлекателен, но неверен: React вызывает ref-колбэки в commit-фазе, узел уже в документе. Проблема не в моменте, а в количестве.</p>
    <p>Лечится двумя способами: обернуть колбэк в <code>useCallback</code> со стабильными зависимостями и вернуть из него функцию очистки — в React 19 возвращённая функция вызывается при отвязке. Важно: со стилем «возвращаем cleanup» нельзя смешивать ветку с проверкой на <code>null</code>.</p>`,
    cardId: 'rx24' },

  { id: 'qb30',
    deck: 'react',
    q: 'Проект обновили до React 19, и эта строка стала источником ошибки. Почему?',
    snippet: `<input ref={(el) => (inputRef.current = el)} />`,
    options: [
      'стрелка с неявным возвратом вернула узел, а React 19 трактует возвращённое значение как cleanup',
      'в React 19 ref-колбэки запрещены, остались только объектные рефы из useRef',
      'присваивание в ref.current теперь разрешено только внутри useLayoutEffect',
      'React 19 передаёт в колбэк не узел, а объект с полем current',
    ],
    correct: 0,
    why: `<p>React 19 ввёл функции очистки для ref-колбэков: то, что колбэк вернул, считается cleanup и будет вызвано при отвязке. Стрелка с неявным возвратом возвращает результат присваивания — сам DOM-узел. React пытается вызвать его как функцию и падает. Достаточно фигурных скобок: <code>ref={(el) =&gt; { inputRef.current = el; }}</code>.</p>
    <p>Вариант «ref-колбэки запрещены» привлекателен на волне разговоров о депрекейте <code>forwardRef</code>. Убрали не колбэки, а необходимость оборачивать компонент: <code>ref</code> стал обычным пропом функциональных компонентов.</p>
    <p>Это самая частая реальная поломка при апгрейде — она не ловится типами и проявляется только в рантайме конкретного экрана.</p>`,
    cardId: 'rx4' },

  { id: 'qb31',
    deck: 'react',
    q: 'Все потребители контекста перерисовываются при любом рендере провайдера, хотя state не менялся. В чём причина?',
    snippet: `function StoreProvider({ children }) {
  const [state, dispatch] = useReducer(reducer, init);
  return (
    <Ctx.Provider value={{ state, dispatch }}>
      {children}
    </Ctx.Provider>
  );
}`,
    options: [
      'dispatch пересоздаётся на каждом рендере и тянет за собой обновление',
      'объектный литерал в value — новая ссылка на каждом рендере провайдера',
      'useReducer всегда уведомляет потребителей, даже если состояние то же самое',
      'children не мемоизированы, поэтому контекст считает дерево изменившимся',
    ],
    correct: 1,
    why: `<p>Контекст сравнивает значение по ссылке через <code>Object.is</code>. Литерал <code>{ state, dispatch }</code> создаётся заново при каждом рендере провайдера, поэтому все потребители получают уведомление независимо от того, изменилось ли что-то по существу. Лечится <code>useMemo</code> по <code>[state]</code> либо разделением на два контекста — состояния и диспатча.</p>
    <p>Самый привлекательный неверный вариант — про <code>dispatch</code>: кажется логичным, что функция «новая». На деле <code>dispatch</code> из <code>useReducer</code> стабилен всё время жизни компонента, его безопасно класть в зависимости и отдавать через контекст. Виноват именно объект-обёртка.</p>
    <p>Обрати внимание и на второй приём: провайдер должен рендерить <code>children</code>, а не строить дерево сам, иначе его рендер перерисует поддерево мимо всяких контекстов.</p>`,
    cardId: 'rx37' },

  { id: 'qb32',
    deck: 'react',
    q: 'Потребитель контекста обёрнут в memo, но всё равно перерисовывается при каждом обновлении контекста. Почему memo не помогает?',
    options: [
      'memo сравнивает пропсы поверхностно, а значение контекста лежит глубоко в объекте',
      'memo работает только для компонентов без хуков состояния',
      'memo сравнивает пропсы, а контекст обновляет фибер напрямую, минуя пропсы',
      'memo кеширует результат только на один рендер и сбрасывается любым обновлением сверху',
    ],
    correct: 2,
    why: `<p><code>memo</code> — это проверка «пришли ли новые пропсы». Контекст к пропсам не относится: React ведёт список потребителей и при смене значения помечает их фиберы как требующие обновления напрямую. Сравнение пропсов при этом даже не спасает — компонент всё равно попадёт в рендер.</p>
    <p>Первый вариант привлекателен своей полуправдой: <code>memo</code> действительно сравнивает поверхностно. Но здесь дело не в глубине сравнения, а в том, что сравнивать нечего — канал доставки другой.</p>
    <p>Работающие приёмы: мемоизировать <code>value</code>, разделить контексты по частоте изменения, или, если нужны селекторы по полям, взять внешний стор с <code>useSyncExternalStore</code>.</p>`,
    cardId: 'rx37' },

  { id: 'qb33',
    deck: 'react',
    q: 'Почему Context не считают полноценным стейт-менеджером?',
    options: [
      'он не переживает перезагрузку страницы и не умеет сохраняться в localStorage',
      'значение контекста нельзя изменить после создания провайдера',
      'он не работает в серверных компонентах и требует use client',
      'у него нет подписки на часть значения: любое изменение будит всех потребителей',
    ],
    correct: 3,
    why: `<p>Контекст — механизм <strong>доставки</strong> значения вниз по дереву, а не хранилище с подписками. У него нет селекторов: потребитель подписан на значение целиком, поэтому смена одного поля перерисовывает всех, кто читает контекст. На god-контексте это выливается в перерисовку таблицы от клика по чекбоксу в сайдбаре.</p>
    <p>Вариант про <code>'use client'</code> привлекателен и отчасти верен фактически (контекст действительно нужен в клиентских компонентах), но к вопросу отношения не имеет: это ограничение среды, а не свойство контекста как модели состояния.</p>
    <p>Плюс к этому: нет devtools, middleware, отложенных обновлений и сравнения по значению. Всё это добирают либо разделением контекстов, либо внешним стором.</p>`,
    cardId: 'r13' },

  { id: 'qb34',
    deck: 'react',
    q: 'Зачем библиотеки состояния (Redux, Zustand, Jotai) перешли на useSyncExternalStore вместо подписки в useEffect?',
    options: [
      'чтобы исключить tearing: React читает снимок синхронно и перерендерит дерево целиком при расхождении',
      'чтобы уменьшить размер бандла: хук заменяет собственную реализацию подписки',
      'чтобы состояние стора автоматически попадало в React DevTools',
      'чтобы работать в серверных компонентах без директивы use client',
    ],
    correct: 0,
    why: `<p>Конкурентный рендеринг вправе прервать работу над деревом и продолжить позже. Если внешний стор изменится в этот промежуток, часть дерева окажется отрисованной со старым значением, часть — с новым: пользователь увидит противоречивые данные в одном кадре. Это и есть <strong>tearing</strong>, и до конкурентного режима он был невозможен, потому что рендер был синхронным.</p>
    <p><code>useSyncExternalStore</code> даёт React контракт: функция подписки и функция получения снимка. React читает снимок синхронно в нужный момент и, обнаружив расхождение, откатывает работу и рендерит всё дерево с одним значением.</p>
    <p>Вариант про размер бандла привлекателен, потому что хук действительно позволил выкинуть код. Но это следствие, а не причина: подписку через <code>useEffect</code> написать несложно — она просто некорректна в конкурентном режиме.</p>`,
    cardId: 'r20' },

  { id: 'qb35',
    deck: 'react',
    q: 'Что здесь нарушает требование чистоты рендера сильнее всего?',
    snippet: `function Badge({ items }) {
  items.sort((a, b) => a.n - b.n);
  return <span>{items[0].n}</span>;
}`,
    options: [
      'обращение к items[0] без проверки на пустой массив',
      'sort мутирует массив, пришедший в пропсах, — рендер меняет чужие данные',
      'компаратор объявлен инлайн и пересоздаётся на каждом рендере',
      'вычисление в теле компонента вместо useMemo',
    ],
    correct: 1,
    why: `<p><code>Array.prototype.sort</code> сортирует <strong>на месте</strong> и возвращает тот же массив. Значит, рендер молча переупорядочивает данные, которыми владеет родитель. React вправе вызвать рендер дважды, прервать его или выбросить результат — при мутации это даёт разный порядок в разных прогонах и расхождения между тем, что отрисовано, и тем, что лежит в состоянии. Именно такие вещи и выявляет двойной рендер StrictMode.</p>
    <p>Вариант про <code>useMemo</code> привлекателен, потому что кажется «правильным React-советом». Но мемоизация здесь ничего не чинит: мутация просто будет происходить реже. Правильно — копия: <code>[...items].sort(...)</code>.</p>
    <p>Пересоздание компаратора — микрооптимизация, к чистоте отношения не имеющая. Отсутствие проверки на пустой массив — обычный баг, но не нарушение модели рендера.</p>`,
    cardId: 'rx65' },

  { id: 'qb36',
    deck: 'react',
    q: 'Тултип измеряет свои размеры и позиционируется после монтирования. Пользователь замечает вспышку в левом верхнем углу. Что применить?',
    options: [
      'обернуть позиционирование в startTransition, чтобы React отдал приоритет отрисовке',
      'перенести измерение в requestAnimationFrame внутри useEffect',
      'использовать useLayoutEffect: он выполняется после мутаций DOM и до отрисовки кадра',
      'вызвать flushSync вокруг setState с координатами внутри useEffect',
    ],
    correct: 2,
    why: `<p><code>useEffect</code> планируется на время <strong>после</strong> отрисовки: браузер успевает показать кадр с неспозиционированным тултипом, и пользователь видит вспышку. <code>useLayoutEffect</code> вызывается синхронно в commit-фазе — DOM уже обновлён, кадр ещё не показан, поэтому измерение и коррекция попадают в тот же кадр.</p>
    <p>Вариант с <code>requestAnimationFrame</code> самый привлекательный: он тоже «про кадры» и часто встречается в коде. Но rAF-колбэк выполняется перед <strong>следующей</strong> отрисовкой, то есть текущий неправильный кадр всё равно будет показан — вспышка останется, просто станет короче.</p>
    <p><code>startTransition</code> сделает только хуже, откладывая обновление. <code>flushSync</code> в эффекте — избыточная тяжёлая артиллерия там, где хватает правильной фазы.</p>`,
    cardId: 'r10' },

  { id: 'qb37',
    deck: 'react',
    q: 'Server Action успешно сохранил запись, ошибок нет, но список на странице показывает старые данные. Чего не хватает?',
    options: [
      'revalidatePath или revalidateTag в экшене: без инвалидации кеша новое дерево не придёт',
      'router.refresh() на клиенте — Server Actions не умеют обновлять серверные данные',
      'useOptimistic на списке: без него результат экшена не попадает в UI',
      'отключить кеширование на уровне fetch через cache: no-store на всех запросах',
    ],
    correct: 0,
    why: `<p>Server Action возвращает не только своё значение, но и обновлённое серверное дерево — если серверу есть что пересчитывать. Пока кеш соответствующего пути или тега валиден, Next честно отдаёт старый результат. Явная инвалидация <code>revalidatePath('/projects')</code> или <code>revalidateTag('projects')</code> в конце экшена — штатный способ сказать, что данные протухли.</p>
    <p>Вариант с <code>router.refresh()</code> привлекателен, потому что тоже даёт видимый эффект. Но это клиентский костыль вокруг проблемы кеша: он перезапрашивает маршрут целиком, теряя выгоду частичной инвалидации, и не поможет другим пользователям и другим маршрутам, где висит тот же тег.</p>
    <p>Глобальный <code>no-store</code> «на всякий случай» лечит симптом ценой всего кеша — так пропадает и статика, и дедупликация.</p>`,
    cardId: 'rx11' },

  { id: 'qb38',
    deck: 'react',
    q: 'Ошибка выброшена в app/dashboard/layout.tsx. Какая граница её поймает?',
    options: [
      'app/dashboard/error.tsx — он относится к тому же сегменту',
      'ближайший Error Boundary внутри самого layout, если он там объявлен',
      'никакая: ошибки в layout не перехватываются и всегда дают белый экран',
      'error.tsx родительского сегмента — свой error.tsx находится внутри этого layout',
    ],
    correct: 3,
    why: `<p><code>error.tsx</code> сегмента оборачивает его <strong>содержимое</strong>, но сам рендерится внутри <code>layout.tsx</code> этого же сегмента. Значит, layout находится выше границы, и его ошибка пролетает мимо — поймает её граница уровнем выше, то есть <code>error.tsx</code> родительского сегмента. Для корневого layout эту роль играет <code>global-error.tsx</code>.</p>
    <p>Первый вариант привлекателен своей симметрией: файлы лежат рядом, логично ждать, что один защищает другого. Это ровно то место, где интуиция расходится с деревом рендера.</p>
    <p>Отдельно помни, что <code>global-error.tsx</code> заменяет корневую разметку целиком и обязан сам отрисовать <code>&lt;html&gt;</code> и <code>&lt;body&gt;</code>, иначе получишь белый экран уже по своей вине.</p>`,
    cardId: 'rx52' },

  { id: 'qb39',
    deck: 'react',
    q: 'Что из перечисленного Error Boundary не перехватит?',
    options: [
      'ошибку в рендере вложенного компонента',
      'ошибку, выброшенную внутри обработчика onClick',
      'ошибку в конструкторе классового потомка',
      'ошибку в useLayoutEffect дочернего компонента',
    ],
    correct: 1,
    why: `<p>Error Boundary ловит ошибки на фазах, которыми управляет React: рендер, конструкторы и методы жизненного цикла, включая эффекты. Обработчик события выполняется вне этого цикла — это обычный колбэк браузера, и исключение из него уходит в глобальный <code>error</code>. То же касается кода в <code>setTimeout</code>, в <code>.then</code> и в асинхронных функциях после первого <code>await</code>.</p>
    <p>Вариант с <code>useLayoutEffect</code> самый привлекательный: кажется, что раз эффект — это «после рендера», то он уже вне зоны ответственности. Ошибки в эффектах границы как раз ловят.</p>
    <p>В обработчиках приходится писать <code>try/catch</code> самому и переводить ошибку в состояние, которое покажет UI.</p>`,
    cardId: 'r19' },

  { id: 'qb40',
    deck: 'react',
    q: 'Страница обвешана loading.tsx и границами Suspense, но пользователь всё равно ждёт весь ответ целиком. Что проверить в первую очередь?',
    options: [
      'не отключён ли стриминг флагом в next.config — по умолчанию он выключен',
      'не превышают ли границы Suspense лимит вложенности в четыре уровня',
      'нет ли await в корневом layout: shell не отдаётся, пока он не выполнится',
      'не используются ли клиентские компоненты — они блокируют стриминг',
    ],
    correct: 2,
    why: `<p>Стриминг устроен так: сервер сразу отдаёт shell — разметку до первой приостановки — и дописывает куски по мере готовности. Корневой <code>layout.tsx</code> входит в shell, поэтому <code>await</code> в нём откладывает <strong>первый байт</strong>. Все границы ниже становятся бессмысленными: вся страница снова ждёт самый медленный запрос.</p>
    <p>Вариант про клиентские компоненты привлекателен, потому что они действительно требуют гидратации. Но стриминг они не блокируют — их HTML приезжает в том же потоке.</p>
    <p>Вторая частая причина того же симптома — буферизующий прокси перед приложением: локально стриминг работает, а на проде ответ собирается целиком. Это проверяют curl-ом до и после прокси.</p>`,
    cardId: 'rx53' },

  { id: 'qb41',
    deck: 'react',
    q: 'Достаточно ли middleware с проверкой сессии, чтобы закрыть приватные данные приложения?',
    options: [
      'нет: Server Actions и route handlers вызываются напрямую и могут не попасть под матчер',
      'да: middleware выполняется до любого серверного кода Next',
      'да, если в matcher указать все маршруты через шаблон со звёздочкой',
      'нет, но достаточно продублировать проверку в корневом layout',
    ],
    correct: 0,
    why: `<p>Middleware — это фильтр запросов к маршрутам, а не единая точка авторизации. Server Actions публикуются как отдельные эндпоинты со своими идентификаторами, route handlers — как обычные HTTP-обработчики. Их дёргают напрямую, и матчер, написанный под страницы, их не покрывает. Плюс сам матчер легко разъезжается с реальным деревом маршрутов при рефакторинге.</p>
    <p>Вариант «достаточно продублировать в корневом layout» привлекателен, потому что layout действительно выполняется на сервере. Но он рендерит страницу — а экшен вызывают без страницы. Проверка должна стоять там, где происходит доступ к данным: в самом экшене, в обработчике, в слое доступа к БД.</p>
    <p>Роль middleware — дешёвые вещи на краю: редиректы, локали, A/B, отсечение явно анонимных запросов до рендера.</p>`,
    cardId: 'rx56' },

  { id: 'qb42',
    deck: 'react',
    q: 'Главное изображение лендинга сделано через next/image, но LCP стабильно плохой: картинка начинает грузиться поздно. Что исправить?',
    options: [
      'заменить next/image на обычный img с прямой ссылкой на файл',
      'поставить priority всем изображениям страницы, чтобы браузер начал раньше',
      'увеличить качество через quality — сжатие занимает время на сервере',
      'добавить priority конкретно этому изображению',
    ],
    correct: 3,
    why: `<p><code>next/image</code> по умолчанию ставит <code>loading="lazy"</code>: браузер откладывает загрузку до приближения к вьюпорту и не даёт картинке высокий приоритет. Для изображения первого экрана это прямая потеря LCP. Проп <code>priority</code> снимает ленивую загрузку и добавляет <code>fetchpriority="high"</code> с предзагрузкой — это самая частая правка на лендингах.</p>
    <p>Вариант «поставить priority всем» привлекателен логикой «если помогает одному, поможет всем». На деле приоритет перестаёт быть приоритетом: десяток картинок конкурируют за канал, и LCP-кандидат приезжает не раньше, а позже.</p>
    <p>Рядом стоит проверить <code>sizes</code>: у изображения с <code>fill</code> без <code>sizes</code> браузер выберет самый крупный вариант из <code>srcset</code>.</p>`,
    cardId: 'rx58' },

  { id: 'qb43',
    deck: 'react',
    q: 'Чем next/font борется с сдвигом макета при загрузке шрифта?',
    options: [
      'загружает шрифт синхронно и блокирует рендер до его готовности',
      'применяет font-display: block, пока файл не приедет',
      'встраивает шрифт в HTML как data-URI и убирает сетевой запрос',
      'подбирает метрики системного фолбэка под метрики целевого шрифта',
    ],
    correct: 3,
    why: `<p>Основной приём — <code>adjustFontFallback</code>: сборка вычисляет метрики целевого шрифта и генерирует локальный фолбэк с подогнанными <code>size-adjust</code>, <code>ascent-override</code> и <code>descent-override</code>. Текст, отрисованный фолбэком, занимает практически ту же площадь, поэтому подмена шрифта не двигает макет. Дополнительно файлы self-hosted и предзагружаются, что убирает обращение к стороннему домену.</p>
    <p>Вариант с <code>font-display: block</code> привлекателен, потому что действительно прячет подмену. Но цена — невидимый текст на время загрузки: CLS формально исчезает, а реальный опыт становится хуже, и страдает FCP.</p>
    <p>Частая ошибка на русскоязычных проектах — забыть <code>cyrillic</code> в <code>subsets</code>: кириллица уезжает в фолбэк, и никто не понимает почему.</p>`,
    cardId: 'rx59' },

  { id: 'qb44',
    deck: 'react',
    q: 'Компонент отрендерен на сервере и гидратируется в браузере. Что произойдёт?',
    snippet: `function Clock() {
  return <time>{new Date().toLocaleTimeString()}</time>;
}`,
    options: [
      'ничего: React допускает расхождение текстовых узлов и просто оставит серверный вариант',
      'React выбросит ошибку и отрендерит ближайший Error Boundary',
      'предупреждение о несовпадении гидратации, содержимое заменится клиентским',
      'на экране будет серверное время до первого обновления состояния',
    ],
    correct: 2,
    why: `<p>Сервер и клиент выполняют рендер в разные моменты и в разных часовых поясах, поэтому строка времени не совпадёт. React сверяет разметку при гидратации, ругается в консоль и приводит содержимое к клиентскому варианту. В React 19 сообщение стало подробным — с диффом того, что ожидалось и что получено.</p>
    <p>Первый вариант привлекателен, потому что визуально всё «как будто работает»: страница не падает, время в итоге верное. Но расхождение обходится дорого — React может отбросить и перерисовать поддерево, а на больших блоках это заметная работа плюс мигание.</p>
    <p>Лечится переносом зависящего от клиента значения в эффект (первый рендер — плейсхолдер) либо рендером на сервере в фиксированной таймзоне с форматированием на клиенте. Тот же класс багов дают <code>Math.random()</code>, <code>window</code> и <code>localStorage</code> в рендере.</p>`,
    cardId: 'r17' },

  { id: 'qb45',
    deck: 'react',
    q: 'Что React Compiler не делает, даже когда успешно оптимизировал компонент?',
    options: [
      'не трогает эффекты и их массивы зависимостей',
      'не мемоизирует значения, вычисленные из пропсов',
      'не работает с компонентами, использующими контекст',
      'не заменяет ручные useMemo и useCallback своими',
    ],
    correct: 0,
    why: `<p>Компилятор автоматизирует мемоизацию рендера: он анализирует поток данных и сам расставляет кеширование вычислений и JSX. <code>useEffect</code> при этом остаётся полностью на разработчике — компилятор не переписывает эффекты и не правит их зависимости. Более того, именно эффекты с объектом или массивом в зависимостях чаще всего дают «исчезнувшие обновления» после включения компилятора.</p>
    <p>Самый привлекательный неверный вариант — про ручные <code>useMemo</code>: кажется логичным, что компилятор их вычистит. Он их сохраняет, потому что не может доказать, что они лишние. Вреда нет, но код читается хуже, а иногда ручная мемоизация ещё и мешает анализу.</p>
    <p>И главное правило внедрения: смотреть отчёт сборки. Компонент, нарушающий правила React (мутация пропсов, чтение <code>ref.current</code> в рендере), молча пропускается целиком.</p>`,
    cardId: 'rx19' },

  // ── CSS и вёрстка ─────────────────────────────────────────────────

  { id: 'qb46',
    deck: 'css',
    q: 'Какого цвета будет текст абзаца?',
    snippet: `<div id="app">
  <div class="card"><p class="text">Привет</p></div>
</div>

#app .card p { color: red; }
.card p.text { color: blue; }`,
    options: [
      'красный: (1,1,1) весомее (0,2,1) — id перевешивает лишний класс',
      'синий: правило идёт ниже и содержит больше селекторов',
      'синий: p.text точнее описывает элемент, чем просто p',
      'красный, но только если #app объявлен в том же файле',
    ],
    correct: 0,
    why: `<p>Специфичность — это кортеж, который сравнивается <strong>по разрядам слева направо</strong>, а не суммой. У первого селектора один id, один класс и один тег: (1,1,1). У второго — два класса и один тег: (0,2,1). Разряд id решает всё сразу, дальше сравнение не идёт. Побеждает красный.</p>
    <p>Вариант «синий, правило ниже» — самая живучая ошибка: порядок в файле решает <strong>только при равной</strong> специфичности. Здесь она не равна, поэтому позиция не играет роли.</p>
    <p>Отсюда практическое правило: не повышать вес добавлением id. Честнее продублировать класс — <code>.text.text</code> даёт (0,2,1) без вовлечения id и легко отменяется потом.</p>`,
    cardId: 'css4' },

  { id: 'qb47',
    deck: 'css',
    q: 'Ссылка находится внутри .nav. Какой цвет она получит?',
    snippet: `:is(#header, .nav) a { color: red; }
.nav a { color: blue; }`,
    options: [
      'синий: селектор совпал по .nav, значит вес считается по .nav',
      'красный: :is() берёт вес самого тяжёлого аргумента, то есть #header',
      'синий: :is() всегда имеет нулевую специфичность',
      'зависит от порядка подключения файлов',
    ],
    correct: 1,
    why: `<p><code>:is()</code> берёт специфичность <strong>самого весомого аргумента</strong> из списка — независимо от того, какой из них реально совпал. Здесь это <code>#header</code>, поэтому селектор весит (1,0,1) против (0,1,1) у <code>.nav a</code>. Ссылка станет красной.</p>
    <p>Первый вариант выглядит абсолютно естественно: раз совпал <code>.nav</code>, логично считать по нему. Спецификация решила иначе — ради предсказуемости результата, который иначе зависел бы от конкретного элемента. Ровно так же ведёт себя <code>:not()</code>.</p>
    <p>Нулевую специфичность даёт <code>:where()</code> — им и пользуются для базовых стилей библиотеки, чтобы прикладной код переопределял их одним классом.</p>`,
    cardId: 'css4' },

  { id: 'qb48',
    deck: 'css',
    q: 'Дизайн-систему завернули в @layer components, а старые стили проекта остались вне слоёв. Кто победит при конфликте?',
    options: [
      'дизайн-система: явно объявленный слой сильнее неявного',
      'зависит от порядка подключения файлов в бандле',
      'старые стили: правила вне слоёв сильнее любого слоя',
      'победит правило с большей специфичностью, слои на это не влияют',
    ],
    correct: 2,
    why: `<p>Незаслоённые стили в каскаде стоят <strong>после всех слоёв</strong> — то есть выигрывают у любого из них при любой специфичности. Это ключевое и контринтуитивное правило <code>@layer</code>, и именно оно ломает первые попытки внедрения: команда заворачивает новый код в слои и обнаруживает, что легаси стал сильнее, чем был.</p>
    <p>Вариант «победит большая специфичность» привлекателен, потому что описывает привычный мир без слоёв. Слои сравниваются <strong>до</strong> специфичности: сначала происхождение и слой, и только внутри одного слоя — вес селектора.</p>
    <p>Порядок внедрения поэтому обратный ожидаемому: сначала в самый ранний слой заворачивают старый код, и только потом добавляют свои слои поверх.</p>`,
    cardId: 'css5' },

  { id: 'qb49',
    deck: 'css',
    q: 'Порядок объявлен как @layer reset, components, utilities. В reset и в utilities одно и то же свойство помечено !important. Кто выиграет?',
    options: [
      'utilities: слой объявлен позже, important лишь усиливает правило',
      'ни один: два !important в разных слоях взаимно гасятся',
      'тот, у кого выше специфичность селектора',
      'reset: для important-объявлений порядок слоёв инвертируется',
    ],
    correct: 3,
    why: `<p>Для <code>!important</code> каскад разворачивает порядок слоёв: побеждает important из <strong>самого раннего</strong> слоя. Сделано это, чтобы низкоуровневый слой мог что-то гарантировать жёстко — сброс или базовая доступность не должны отменяться утилитой, написанной кем-то позже.</p>
    <p>Первый вариант — прямое перенесение обычного правила «позже значит сильнее» на important, и это самая частая ошибка. Она обнаруживается в проде, когда утилита с <code>!important</code> внезапно не работает.</p>
    <p>Та же инверсия действует и между происхождениями: important автора сильнее important браузера, но important пользователя сильнее их обоих.</p>`,
    cardId: 'css5' },

  { id: 'qb50',
    deck: 'css',
    q: 'У .parent нет ни padding, ни border, ни overflow. Что произойдёт с отступом?',
    snippet: `.parent { background: #eee; }
.child  { margin-top: 40px; }

<div class="parent"><div class="child">текст</div></div>`,
    options: [
      'margin выйдет наружу: вниз сдвинется сам .parent, серый фон начнётся прямо над текстом',
      'внутри .parent появится отступ 40px, фон начнётся выше текста',
      'margin схлопнется в ноль: у родителя нет собственных отступов',
      'отступ применится, но только после явного указания display: block',
    ],
    correct: 0,
    why: `<p>Верхний margin первого потомка схлопывается с верхним margin родителя, если между ними нет ничего разделяющего — ни border, ни padding, ни собственного BFC. Итоговый общий отступ выносится <strong>наружу</strong> родителя: <code>.parent</code> отодвигается от предыдущего элемента, а серый фон начинается вплотную к тексту.</p>
    <p>Второй вариант — то, чего ждут все: «поставил ребёнку отступ, значит внутри стало просторнее». Именно из-за этого расхождения появляется наблюдение «поставил рамку — и отступ вдруг возник»: <code>border-top: 1px</code> или <code>padding-top: 1px</code> разрывает схлопывание.</p>
    <p>Схлопывание работает только по блочной оси и только в block layout. Горизонтальные margin не схлопываются никогда.</p>`,
    cardId: 'css2' },

  { id: 'qb51',
    deck: 'css',
    q: 'Нужно разорвать схлопывание маргинов у контейнера, не обрезая тени потомков и не превращая его в скролл-контейнер. Что поставить?',
    options: [
      'overflow: hidden — классический приём, побочных эффектов у него нет',
      'display: flow-root — он создаёт BFC и больше ничего не меняет',
      'position: relative — позиционирование само по себе создаёт BFC',
      'contain: strict — минимальная изоляция без последствий для раскладки',
    ],
    correct: 1,
    why: `<p><code>display: flow-root</code> был добавлен ровно для этого: он создаёт block formatting context и не тянет за собой ничего лишнего. Внутри BFC маргины детей не схлопываются с родителем, а плавающие потомки перестают вываливаться.</p>
    <p><code>overflow: hidden</code> — самый привлекательный неверный ответ, потому что действительно работает и живёт в проектах годами. Побочные эффекты серьёзные: обрезаются тени, тултипы и выпадающие меню, ломается <code>position: sticky</code> у потомков, а сам элемент становится скролл-контейнером и прокручивается при фокусе внутри.</p>
    <p><code>position: relative</code> BFC не создаёт вовсе. <code>contain: strict</code> создаёт, но заодно делает элемент containing block для fixed-потомков — модалка внутри перестанет прилипать к экрану.</p>`,
    cardId: 'css3' },

  { id: 'qb52',
    deck: 'css',
    q: 'Каким будет расстояние между двумя соседними li?',
    snippet: `.list { display: flex; flex-direction: column; }
.list > li { margin-block: 20px; }`,
    options: [
      '20px — маргины схлопнутся, как в обычном потоке',
      '20px — flex-контейнер нормализует отступы между элементами',
      '40px — внутри flex-контейнера схлопывания маргинов нет',
      '0 — flex игнорирует margin-block, нужен gap',
    ],
    correct: 2,
    why: `<p>Схлопывание маргинов — свойство <strong>block layout</strong>. Дети flex- и grid-контейнера выкладываются по другим правилам, и их отступы просто складываются: 20px снизу первого плюс 20px сверху второго дают 40px.</p>
    <p>Вариант «20px, как в обычном потоке» самый привлекательный: визуально это тот же вертикальный список, и глаз не подсказывает, что модель раскладки сменилась. Отсюда классический баг — вертикальные ритмы едут ровно после того, как контейнеру поставили <code>display: flex</code>.</p>
    <p>Практический вывод: во flex и grid отступы задают через <code>gap</code>. Он предсказуем, не удваивается и не оставляет лишнего отступа по краям.</p>`,
    cardId: 'css2' },

  { id: 'qb53',
    deck: 'css',
    q: 'Тултип с z-index: 9999 уходит под шапку с z-index: 1. Почему?',
    snippet: `.header  { position: relative; z-index: 1; }
.card    { position: relative; opacity: .99; }
.tooltip { position: absolute; z-index: 9999; }
/* .tooltip лежит внутри .card */`,
    options: [
      'z-index не работает на absolute внутри relative-предка',
      'значения больше 1000 браузер обрезает до максимума слоя',
      '.header объявлен позже и потому выигрывает при равном контексте',
      'opacity меньше единицы создала stacking context: 9999 считается внутри .card',
    ],
    correct: 3,
    why: `<p>Любое значение <code>opacity</code> меньше единицы создаёт новый stacking context. Внутри него <code>z-index: 9999</code> сортирует тултип только среди братьев в <code>.card</code>. Наружу карточка выходит как единое целое со своим z-index — а он у неё <code>auto</code>, то есть ниже шапки с явной единицей. Никакое число внутри контекста этого не изменит.</p>
    <p>Вариант «браузер обрезает большие значения» привлекателен, потому что объясняет наблюдаемое «увеличиваю число, а не помогает». Обрезки нет: диапазон целых огромен, дело исключительно в границах контекста.</p>
    <p>Тот же баг дают <code>transform</code>, <code>filter</code>, <code>will-change</code>, <code>contain</code>, <code>container-type</code> и <code>position: sticky</code>. Лечится не числами, а токенами уровней и <code>isolation: isolate</code> на корне компонента.</p>`,
    cardId: 'css8' },

  { id: 'qb54',
    deck: 'css',
    q: 'Какое объявление создаст новый stacking context на элементе без position?',
    options: [
      'transform: translateZ(0)',
      'z-index: 10',
      'overflow: hidden',
      'float: left',
    ],
    correct: 0,
    why: `<p><code>transform</code> с любым значением, кроме <code>none</code>, создаёт stacking context независимо от позиционирования. Это же верно для <code>filter</code>, <code>opacity</code> меньше единицы, <code>will-change</code> с перечислением таких свойств, <code>isolation: isolate</code>, <code>mix-blend-mode</code>, <code>contain: paint</code> и <code>container-type</code>.</p>
    <p>Самый привлекательный неверный вариант — <code>z-index: 10</code>. Он создаёт контекст только вместе с позиционированием (<code>relative</code>, <code>absolute</code>, <code>fixed</code>, <code>sticky</code>) или на flex- и grid-элементе. На обычном статичном блоке <code>z-index</code> просто игнорируется.</p>
    <p><code>overflow: hidden</code> создаёт block formatting context, но не stacking context — это разные механизмы, и их постоянно путают. <code>float</code> тоже даёт BFC для самого элемента, а не контекст наложения.</p>`,
    cardId: 'css8' },

  { id: 'qb55',
    deck: 'css',
    q: 'Модалка с position: fixed внезапно перестала растягиваться на весь экран. Она лежит внутри карточки, которой на hover ставят transform. Что происходит?',
    snippet: `.card:hover { transform: translateY(-2px); }
.modal { position: fixed; inset: 0; }`,
    options: [
      'transform у предка отключает fixed, и модалка становится absolute',
      'карточка с transform стала containing block: inset: 0 считается от неё',
      'inset: 0 не работает вместе с fixed, нужны отдельные top/right/bottom/left',
      'hover перерисовывает поддерево, и модалка теряет вычисленные координаты',
    ],
    correct: 1,
    why: `<p>Обычно containing block для <code>fixed</code> — вьюпорт. Но предок с <code>transform</code>, <code>filter</code>, <code>perspective</code>, <code>backdrop-filter</code>, <code>will-change</code> на этих свойствах, <code>contain: paint</code> или <code>container-type</code> становится containing block для fixed-потомков. <code>inset: 0</code> начинает считаться от карточки — модалка схлопывается до её размеров.</p>
    <p>Первый вариант близок к истине и потому опаснее всего: <code>position</code> остаётся <code>fixed</code>, элемент по-прежнему не прокручивается вместе со страницей относительно своего контейнера, меняется только точка отсчёта. Разница важна при отладке — в DevTools вы увидите именно <code>fixed</code> и решите, что дело не в этом.</p>
    <p>Коварство в том, что при <code>transform</code> на <code>:hover</code> баг воспроизводится только при наведении. Лечится порталом: модалка рендерится в <code>body</code>, а не внутри карточки.</p>`,
    cardId: 'css7' },

  { id: 'qb56',
    deck: 'css',
    q: 'Ширина 200px не применяется — все элементы получаются одинаковыми. Почему?',
    snippet: `.row  { display: flex; }
.item { width: 200px; flex: 1; }`,
    options: [
      'width игнорируется у всех flex-элементов, работает только min-width',
      'flex: 1 растягивает элемент, но width осталась минимальной шириной',
      'flex: 1 разворачивается в 1 1 0%, и базис 0% перекрывает width',
      'width конфликтует с flex и обе декларации отбрасываются',
    ],
    correct: 2,
    why: `<p><code>flex: 1</code> — это шорткат для <code>flex-grow: 1; flex-shrink: 1; flex-basis: 0%</code>. А <code>flex-basis</code> с любым значением, кроме <code>auto</code>, <strong>перекрывает</strong> <code>width</code> по главной оси. Элемент стартует от нулевой ширины и делит свободное место поровну — заявленные 200px не участвуют вообще.</p>
    <p>Вариант «width осталась минимальной шириной» привлекателен, потому что визуально элементы часто и правда не уже какого-то предела. Но этот предел задаёт авто-минимум (<code>min-width: auto</code> равен min-content), а не <code>width</code>.</p>
    <p>Если нужны именно 200px как стартовая ширина с возможностью роста — пиши <code>flex: 1 1 200px</code>. Полезно помнить и остальные шорткаты: <code>flex: auto</code> это <code>1 1 auto</code>, <code>flex: none</code> — <code>0 0 auto</code>, а умолчание — <code>0 1 auto</code>.</p>`,
    cardId: 'css9' },

  { id: 'qb57',
    deck: 'css',
    q: 'Длинный текст без пробелов растягивает ячейку и ломает строку, хотя многоточие настроено. Чего не хватает?',
    snippet: `.row  { display: flex; }
.cell {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}`,
    options: [
      'width: 100% на .cell — без явной ширины многоточие не считается',
      'display: block на .cell — text-overflow не работает у flex-элементов',
      'max-width в пикселях — процентные размеры не ограничивают flex-элемент',
      'min-width: 0 на .cell — авто-минимум флекс-элемента равен min-content',
    ],
    correct: 3,
    why: `<p>У flex-элемента <code>min-width</code> по умолчанию равен <code>auto</code>, что в главной оси означает min-content — ширину самого длинного неразрывного фрагмента. Сжатие останавливается на этой границе, элемент не может стать уже, и <code>overflow: hidden</code> просто нечего обрезать. Явный <code>min-width: 0</code> снимает пол и включает всю цепочку с многоточием.</p>
    <p>Вариант с <code>max-width</code> в пикселях привлекателен, потому что «работает»: жёсткая граница действительно заставит текст обрезаться. Но она ломает адаптивность и не масштабируется на реальную сетку, где ширина колонки неизвестна заранее.</p>
    <p>Важный нюанс: минимум просачивается снизу вверх, поэтому <code>min-width: 0</code> нужен на <strong>каждом уровне</strong> вложенных flex-контейнеров. Для колонки то же самое, но свойство другое — <code>min-height: 0</code>.</p>`,
    cardId: 'css10' },

  { id: 'qb58',
    deck: 'css',
    q: 'Чему эквивалентен шорткат flex: auto?',
    options: [
      '1 1 auto',
      '0 1 auto',
      '1 0 auto',
      '1 1 0%',
    ],
    correct: 0,
    why: `<p><code>flex: auto</code> разворачивается в <code>flex-grow: 1; flex-shrink: 1; flex-basis: auto</code>. Элемент стартует от своего естественного размера (или от <code>width</code>) и умеет и расти, и сжиматься. Именно поэтому колонки на <code>flex: auto</code> получаются <strong>пропорциональными содержимому</strong>, а не равными.</p>
    <p>Самый привлекательный неверный вариант — <code>1 1 0%</code>: это <code>flex: 1</code>, и разницу между двумя шорткатами замечают редко. Она принципиальна: при базисе 0% свободным считается всё место, и элементы становятся равными; при <code>auto</code> делится только остаток сверх контента.</p>
    <p>Умолчание для flex-элемента — <code>0 1 auto</code>: не растёт, но сжимается. И правило чтения шортката с одним значением: число без единиц это grow, число с единицами — basis.</p>`,
    cardId: 'css9' },

  { id: 'qb59',
    deck: 'css',
    q: 'Что произойдёт с этой сеткой?',
    snippet: `.grid {
  display: grid;
  grid-template-columns: repeat(3, 33.333%);
  gap: 16px;
}`,
    options: [
      'колонки станут ровно по трети, gap займёт место внутри них',
      'браузер пропорционально уменьшит колонки, чтобы уместить gap',
      'сетка переполнит контейнер на величину суммарных зазоров',
      'третья колонка перенесётся на новую строку',
    ],
    correct: 2,
    why: `<p>Проценты в <code>grid-template-columns</code> считаются от ширины контейнера и о зазорах ничего не знают. Три колонки по 33.333% дают почти 100%, а два <code>gap</code> по 16px добавляются <strong>сверху</strong> — контейнер переполняется на 32px, и появляется горизонтальный скролл.</p>
    <p>Вариант «браузер уменьшит колонки» привлекателен, потому что именно так ведёт себя <code>fr</code>: свободное место вычисляется <strong>после</strong> вычитания зазоров, поэтому <code>repeat(3, 1fr)</code> с любым gap помещается идеально. Но <code>fr</code> и проценты — разные механизмы, и подстановка одного вместо другого здесь ключевая ошибка.</p>
    <p>Практическое правило: проценты в grid почти всегда ошибка. Нужны доли — <code>fr</code>; нужен минимум — <code>minmax()</code>.</p>`,
    cardId: 'css11' },

  { id: 'qb60',
    deck: 'css',
    q: 'Контейнер шириной 1200px, внутри всего две карточки. Сетка задана как repeat(auto-fit, minmax(240px, 1fr)). Что получится?',
    options: [
      'две карточки по 240px, остальное место останется пустым справа',
      'две карточки и три пустых трека, которые займут место в раскладке',
      'две карточки растянутся на всю ширину — пустые треки схлопнутся',
      'сетка выстроит карточки в колонку: треков меньше, чем нужно',
    ],
    correct: 2,
    why: `<p><code>auto-fit</code> и <code>auto-fill</code> одинаково считают, сколько треков помещается (здесь пять), но дальше расходятся. <code>auto-fill</code> оставляет пустые треки в раскладке — карточки останутся по 240px в левой части. <code>auto-fit</code> <strong>схлопывает</strong> пустые треки до нуля, и <code>1fr</code> у оставшихся распределяет между ними всё место: две карточки станут по 592px.</p>
    <p>Вариант с пустыми треками привлекателен именно потому, что описывает поведение <code>auto-fill</code> — эти два ключевых слова путают чаще всего.</p>
    <p>Из этого следует и практический побочный эффект: с <code>auto-fit</code> и малым числом элементов карточки растягиваются непредсказуемо. Добавляют потолок: <code>minmax(min(240px, 100%), 400px)</code> плюс <code>justify-content: start</code>.</p>`,
    cardId: 'css12' },

  { id: 'qb61',
    deck: 'css',
    q: 'Сетка с minmax(240px, 1fr) переполняет экран на телефоне шириной 360px с боковыми паддингами. Как починить, сохранив адаптивность?',
    options: [
      'заменить 1fr на auto — тогда трек сожмётся по контенту',
      'добавить overflow-x: hidden контейнеру сетки',
      'перевести минимум в проценты: minmax(30%, 1fr)',
      'сделать минимум мягким: minmax(min(240px, 100%), 1fr)',
    ],
    correct: 3,
    why: `<p>Минимум в <code>minmax()</code> жёсткий: трек не станет уже 240px никогда, поэтому при доступной ширине меньше 240px сетка честно переполняет контейнер. Конструкция <code>min(240px, 100%)</code> делает минимум условным — на широких экранах это 240px, на узких схлопывается до доступной ширины.</p>
    <p><code>overflow-x: hidden</code> — самый привлекательный неверный ответ: скролл исчезает, и кажется, что проблема решена. Контент при этом всё так же выходит за пределы и оказывается физически недоступен, а сам приём ломает <code>position: sticky</code> у потомков.</p>
    <p>Проценты в качестве минимума проблему не решают: они считаются от контейнера и на узком экране дадут слишком маленький трек, а на широком — слишком большой.</p>`,
    cardId: 'css12' },

  { id: 'qb62',
    deck: 'css',
    q: 'При какой ширине вьюпорта сработает медиазапрос?',
    snippet: `html { font-size: 20px; }

@media (min-width: 40em) {
  .side { display: block; }
}`,
    options: [
      'при 640px: em в медиазапросе всегда считается от начального размера 16px',
      'при 800px: em берётся от font-size корневого элемента',
      'при 640px, но только пока пользователь не менял масштаб',
      'при 800px в Chrome и 640px в Safari — поведение не унифицировано',
    ],
    correct: 0,
    why: `<p>Медиазапросы вычисляются <strong>вне контекста документа</strong>: на момент их проверки нет элемента, чей <code>font-size</code> можно было бы взять. Поэтому <code>em</code> и <code>rem</code> в них всегда считаются от начального размера шрифта — обычно 16px, а точнее от того значения, которое пользователь выставил в настройках браузера. 40em это 640px.</p>
    <p>Второй вариант звучит логично: раз <code>html</code> задан 20px, значит база 20px. Но именно независимость от авторского CSS делает медиазапросы в <code>em</code> устойчивыми: точки перелома не поедут оттого, что кто-то поменял базовый кегль.</p>
    <p>Полезное следствие: при увеличении шрифта в настройках браузера точки перелома в <code>em</code> сдвигаются вместе с текстом, и макет переключается на более просторный раньше. В пикселях этого не происходит.</p>`,
    cardId: 'css14' },

  { id: 'qb63',
    deck: 'css',
    q: 'В проекте стоит html { font-size: 62.5% } ради удобного «1rem = 10px». Чем это плохо?',
    options: [
      'значение в процентах не наследуется дочерними элементами',
      'база завязана на настройку пользователя: при системных 24px 1rem станет 15px, и весь текст окажется мельче задуманного',
      'проценты у font-size считаются от ширины контейнера, а не от размера шрифта',
      'rem перестанет работать в медиазапросах и вернётся к пиксельным значениям',
    ],
    correct: 1,
    why: `<p>62.5% берутся не от абстрактных 16px, а от <strong>текущей пользовательской настройки</strong>. Человек, который увеличил шрифт браузера до 24px именно потому, что плохо видит, получит базу 15px и текст мельче, чем на дефолтных настройках у всех остальных. Приём, задуманный как удобство разработчика, бьёт по тем, кому доступность нужнее всего.</p>
    <p>Вариант про «проценты от ширины» привлекателен тем, что путает с процентными padding, которые действительно считаются от inline-размера. Для <code>font-size</code> процент всегда относится к размеру шрифта родителя.</p>
    <p>Альтернатива — честные 16px в базе и <code>calc</code> или переменные для удобных шагов. Считать в голове чуть тяжелее, зато масштабирование остаётся рабочим.</p>`,
    cardId: 'css14' },

  { id: 'qb64',
    deck: 'css',
    q: 'Секция с width: 100vw даёт горизонтальный скролл на десктопе, хотя ничего не выходит за края. Почему?',
    options: [
      'vw считается от размера окна ОС, а не от окна браузера',
      'к 100vw добавляются маргины body, которые не сброшены',
      'vw включает ширину вертикального скроллбара, а доступная ширина меньше на его размер',
      '100vw округляется вверх до целого пикселя и почти всегда превышает контейнер',
    ],
    correct: 2,
    why: `<p>Единица <code>vw</code> отсчитывается от ширины вьюпорта <strong>вместе с полосой прокрутки</strong>. Когда на странице есть вертикальный скроллбар (на десктопе он занимает место, а не накладывается), контенту доступно на 15–17px меньше. Блок в 100vw оказывается шире доступной области, и появляется горизонтальный скролл — который часто и создаёт этот самый вертикальный.</p>
    <p>Вариант про маргины <code>body</code> привлекателен, потому что тоже даёт горизонтальный скролл и встречается часто. Но здесь проблема воспроизводится и при полностью сброшенных отступах.</p>
    <p>Решения: использовать <code>width: 100%</code> вместо <code>100vw</code>, зарезервировать место через <code>scrollbar-gutter: stable</code> или взять <code>100dvw</code> с оглядкой на поддержку.</p>`,
    cardId: 'css14' },

  { id: 'qb65',
    deck: 'css',
    q: 'Шапка таблицы не прилипает. Порог задан, высоты хватает. В чём дело?',
    snippet: `.table-wrap { overflow-x: auto; }
.table-head { position: sticky; top: 0; }`,
    options: [
      'sticky не работает внутри элементов с display: table',
      'нужен ещё z-index — без него прилипший элемент не отображается',
      'top: 0 конфликтует с горизонтальным скроллом, нужен inset-block-start',
      'overflow-x: auto делает обёртку скролл-контейнером, и sticky привязывается к ней',
    ],
    correct: 3,
    why: `<p>Указание <code>overflow</code> по одной оси делает элемент скролл-контейнером по <strong>обеим</strong>: вторая ось получает вычисленное значение <code>auto</code>, а не <code>visible</code>. Липкий элемент всегда привязывается к ближайшему скролл-контейнеру-предку, поэтому шапка «прилипает» к обёртке, которая по вертикали не прокручивается вовсе. Внешне это выглядит как «sticky не работает».</p>
    <p>Вариант с <code>z-index</code> привлекателен, потому что рядом живёт реальная проблема: прилипшая шапка часто уезжает под соседний контент. Но это уже следующий шаг — сначала элемент должен прилипнуть хоть как-то.</p>
    <p>Искать причину приходится вверх по всему дереву: достаточно одного предка с <code>overflow: hidden</code>, поставленного ради обрезки, чтобы sticky сломался «на расстоянии». Часто помогает <code>overflow: clip</code>, который обрезает, но скролл-контейнером не делает.</p>`,
    cardId: 'css26' },

  { id: 'qb66',
    deck: 'css',
    q: 'Элементу задан position: sticky, но ни один из порогов (top, bottom, left, right) не указан. Что будет?',
    options: [
      'элемент будет вести себя как relative — прилипать не к чему',
      'браузер подставит top: 0 по умолчанию',
      'элемент прилипнет к верхней границе ближайшего скролл-контейнера',
      'объявление будет отброшено как невалидное',
    ],
    correct: 0,
    why: `<p>Порог — это и есть определение того, где элемент должен зафиксироваться. Без него sticky-элемент остаётся в потоке и ведёт себя ровно как <code>relative</code>: смещение равно нулю, прокрутка ни на что не влияет. Никакого умолчания браузер не подставляет — это самая частая и самая простая причина «sticky не работает».</p>
    <p>Вариант с автоматическим <code>top: 0</code> привлекателен, потому что в 90% случаев именно это и имеют в виду. Но CSS не угадывает намерение: у sticky четыре независимых порога, и элемент может прилипать хоть снизу, хоть сбоку в горизонтальном скролле.</p>
    <p>Проверять причины стоит по порядку: порог, затем <code>overflow</code> у предков, затем достаточная высота родителя, затем растягивание по кросс-оси во flex или grid (лечится <code>align-self: start</code>).</p>`,
    cardId: 'css26' },

  { id: 'qb67',
    deck: 'css',
    q: 'Какой padding получит .box?',
    snippet: `:root { --gap: ; }
.box { padding: var(--gap, 8px); }`,
    options: [
      '8px — сработает фолбэк, ведь значение переменной пустое',
      '0 — пустая строка валидна, фолбэк не подставится, а свойство станет невалидным',
      '8px — браузер отбросит объявление --gap как синтаксически неверное',
      'унаследованный от родителя padding',
    ],
    correct: 1,
    why: `<p>Пустое значение кастомного свойства <strong>валидно</strong>: <code>--gap</code> объявлена и содержит пустой список токенов. Фолбэк в <code>var()</code> срабатывает только тогда, когда переменная не определена или помечена как guaranteed-invalid. Здесь подстановка даёт <code>padding: </code> — объявление становится невалидным на этапе вычисления значения, свойство сбрасывается в unset, и для ненаследуемого <code>padding</code> это ноль.</p>
    <p>Первый вариант — почти всеобщее ожидание: «пусто значит нет значения, бери запасной». На этом ломаются темы, где переменная приходит из шаблонизатора и иногда рендерится пустой.</p>
    <p>Похожая ловушка рядом: невалидное значение переменной тоже даёт не «предыдущее правило», а unset — и выглядит это так, будто одна переменная сломала всё вокруг.</p>`,
    cardId: 'css18' },

  { id: 'qb68',
    deck: 'css',
    q: 'Переменная объявлена как --n: 4. Как получить ширину 4px?',
    snippet: `:root { --n: 4; }`,
    options: [
      'width: var(--n)px',
      'width: calc(var(--n)px)',
      'width: calc(var(--n) * 1px)',
      'width: var(--n, px)',
    ],
    correct: 2,
    why: `<p>Подстановка <code>var()</code> происходит на уровне токенов, а не текста: результат не склеивается с соседними символами. <code>var(--n)px</code> даёт два отдельных токена — число и идентификатор <code>px</code>, что не является валидной длиной. Единицу нужно «примножить»: <code>calc(var(--n) * 1px)</code>.</p>
    <p>Вариант <code>calc(var(--n)px)</code> самый привлекательный: кажется, что внутри <code>calc</code> склейка разрешена. Не разрешена — правило про токены действует везде одинаково.</p>
    <p>Отсюда практический выбор при проектировании токенов: либо хранить безразмерные числа и всегда умножать в <code>calc</code>, либо хранить сразу с единицами и не иметь возможности умножать. Смешивать оба подхода в одной системе — гарантированная путаница.</p>`,
    cardId: 'css18' },

  { id: 'qb69',
    deck: 'css',
    q: 'Панель раскрывается мгновенно, без анимации. Почему?',
    snippet: `.panel {
  height: 0;
  overflow: hidden;
  transition: height .3s ease;
}
.panel.open { height: auto; }`,
    options: [
      'transition не работает вместе с overflow: hidden',
      'длительности .3s недостаточно, браузер округляет её до нуля',
      'класс добавляется в том же кадре, что и элемент, — нужен reflow',
      'auto не интерполируется: у ключевого слова нет числового значения для промежуточных кадров',
    ],
    correct: 3,
    why: `<p>Переход строится на вычислении промежуточных значений между началом и концом. <code>auto</code> — не длина, а инструкция «посчитай по содержимому», поэтому интерполировать нечего, и браузер просто применяет конечное состояние сразу.</p>
    <p>Вариант про reflow привлекателен, потому что описывает реальную соседнюю проблему: если поменять стиль в том же кадре, в котором элемент вставлен, перехода тоже не будет. Но здесь элемент существует давно, а класс добавляется по клику — этот механизм ни при чём.</p>
    <p>Рабочие подходы: анимировать <code>grid-template-rows</code> от <code>0fr</code> к <code>1fr</code> (самый чистый), анимировать <code>max-height</code> с заведомо большим значением (даёт неравномерную скорость) или использовать новые <code>interpolate-size: allow-keywords</code> и <code>calc-size(auto)</code>.</p>`,
    cardId: 'css21' },

  { id: 'qb70',
    deck: 'css',
    q: 'Разработчик поставил will-change: transform всем карточкам списка «на всякий случай». Чем это плохо?',
    options: [
      'браузер держит композитные слои постоянно: растёт память, а элементы получают stacking context и containing block для fixed',
      'will-change работает только внутри @keyframes и в остальных местах игнорируется',
      'браузер отключит аппаратное ускорение для всей страницы как защиту от злоупотребления',
      'свойство конфликтует с transform и отменяет уже применённые трансформации',
    ],
    correct: 0,
    why: `<p><code>will-change</code> — это обещание браузеру «скоро изменится», по которому он заранее выносит элемент на отдельный композитный слой. Постоянное объявление на списке из сотен карточек означает сотни слоёв: расход памяти на GPU, дороже композитинг, а на слабых устройствах — деградация вместо ускорения.</p>
    <p>Вторая, менее очевидная часть беды: новый слой создаёт stacking context и containing block для fixed-потомков. Модалка или тултип внутри такой карточки внезапно позиционируется не относительно экрана — и багрепорт приходит совсем из другого места.</p>
    <p>Привлекательный неверный вариант про «отключит ускорение» звучит как разумная защита, но браузер так не поступает — он честно выполняет просьбу, и потому просить надо аккуратно: включать перед анимацией и снимать после <code>transitionend</code>.</p>`,
    cardId: 'css22' },

  { id: 'qb71',
    deck: 'css',
    q: 'Блок с border-box переполняет родителя. На сколько?',
    snippet: `.box {
  box-sizing: border-box;
  width: 100%;
  padding: 16px;
  margin: 0 12px;
}`,
    options: [
      'не переполняет: border-box учитывает и padding, и margin',
      'на 24px: margin в бокс не входит никогда, даже при border-box',
      'на 32px: padding при 100% ширины прибавляется снаружи',
      'на 56px: складываются и padding, и margin',
    ],
    correct: 1,
    why: `<p><code>border-box</code> включает в объявленную ширину content, padding и border — но <strong>не margin</strong>. Внешние отступы находятся вне бокса при любом значении <code>box-sizing</code>. Ширина 100% плюс по 12px с каждой стороны даёт переполнение ровно на 24px.</p>
    <p>Первый вариант — самое распространённое заблуждение про border-box: раз «всё внутри», значит и margin тоже. Именно с этого начинается охота за таинственным горизонтальным скроллом.</p>
    <p>Правильные приёмы: задать отступы родителю через padding, использовать <code>gap</code> во flex или grid, либо <code>width: calc(100% - 24px)</code>. Часто лучше всего просто не задавать ширину — блочный элемент и так займёт доступное место с учётом маргинов.</p>`,
    cardId: 'css1' },

  { id: 'qb72',
    deck: 'css',
    q: 'От чего считается padding-top: 50% у блочного элемента?',
    options: [
      'от собственной высоты элемента',
      'от высоты containing block',
      'от inline-размера containing block, то есть от ширины',
      'от высоты вьюпорта, если у родителя не задана высота',
    ],
    correct: 2,
    why: `<p>Процентные <code>padding</code> и <code>margin</code> — <strong>все четыре стороны, включая вертикальные</strong> — считаются от inline-размера containing block, то есть в обычном горизонтальном письме от его ширины. Сделано это, чтобы избежать циклической зависимости: высота блока зависит от содержимого, а содержимое сдвигается отступом.</p>
    <p>Вариант «от высоты containing block» самый привлекательный: интуиция подсказывает симметрию — горизонтальные от ширины, вертикальные от высоты. Этой симметрии в CSS нет.</p>
    <p>На этом правиле держался классический хак <code>padding-top: 56.25%</code> для соотношения 16:9 у встроенных видео. Сегодня его заменяет <code>aspect-ratio</code>, но понимать механику всё ещё нужно — старый код с ним встречается постоянно.</p>`,
    cardId: 'css1' },

  { id: 'qb73',
    deck: 'css',
    q: 'Что зарезервирует место под изображение до его загрузки и уберёт сдвиг макета?',
    snippet: `<img src="/hero.jpg" alt="Обложка">

img { max-width: 100%; height: auto; }`,
    options: [
      'object-fit: cover — он задаёт пропорции контейнера',
      'loading="lazy" — браузер узнаёт размеры до вставки в поток',
      'min-height в пикселях на самом img',
      'атрибуты width и height: браузер выведет из них aspect-ratio',
    ],
    correct: 3,
    why: `<p>Современные браузеры выводят из атрибутов <code>width</code> и <code>height</code> внутренний <code>aspect-ratio</code>. В связке с <code>max-width: 100%; height: auto</code> это даёт корректный резерв места при любой ширине контейнера: высота вычисляется сразу, ещё до загрузки файла, и сдвига не происходит.</p>
    <p><code>object-fit: cover</code> — самый привлекательный неверный ответ: он действительно про пропорции, но управляет тем, как содержимое вписывается в <strong>уже известный</strong> бокс, и сам никакого места не резервирует.</p>
    <p>Важные детали: числа в атрибутах должны отражать реальное соотношение (произвольные дадут резерв неверной высоты), а в <code>&lt;picture&gt;</code> с разными пропорциями под разные экраны размеры надо указывать на каждом <code>&lt;source&gt;</code>. И не ставь <code>loading="lazy"</code> изображению первого экрана — оно откладывает LCP-кандидата.</p>`,
    cardId: 'css28' },

  { id: 'qb74',
    deck: 'css',
    q: 'Прокрутка внутри модалки доходит до конца списка и начинает крутить страницу под ней. Что применить?',
    options: [
      'overscroll-behavior: contain на прокручиваемой области',
      'position: fixed на body, пока модалка открыта',
      'preventDefault на событии wheel внутри модалки',
      'overflow: hidden на html и body',
    ],
    correct: 0,
    why: `<p><code>overscroll-behavior: contain</code> запирает цепочку прокрутки внутри элемента: докрутив до края, пользователь не передаёт импульс родителю. Заодно на мобильных гасится pull-to-refresh. Это одно свойство, без JS и без побочных эффектов для позиции страницы.</p>
    <p><code>position: fixed</code> на <code>body</code> — самый привлекательный неверный вариант, потому что это распространённый рецепт блокировки фона. Цена: страница теряет позицию прокрутки и после закрытия модалки прыгает наверх, что приходится компенсировать сохранением и восстановлением <code>scrollY</code>.</p>
    <p><code>overflow: hidden</code> на <code>body</code> на iOS просто не работает. А <code>preventDefault</code> на <code>wheel</code> не покрывает тач и клавиатуру и требует неактивного слушателя. Самый надёжный путь для модалки целиком — нативный <code>dialog.showModal()</code>: браузер сам блокирует фон.</p>`,
    cardId: 'css27' },

  { id: 'qb75',
    deck: 'css',
    q: 'После включения container queries карточка схлопнулась в полоску. Что не так?',
    snippet: `.card { container-type: size; }`,
    options: [
      'container-type нужно объявлять вместе с container-name, иначе значение игнорируется',
      'size запрашивает обе оси и требует явной высоты — иначе высота считается от нуля',
      'container-type применим только к grid- и flex-контейнерам',
      'запрашивать можно только сам контейнер, а его потомки теряют размеры',
    ],
    correct: 1,
    why: `<p><code>container-type: size</code> включает containment по обеим осям: браузер перестаёт учитывать содержимое при расчёте размеров контейнера. Без явно заданной высоты она становится нулевой — блок схлопывается. Это самая частая причина «всё поехало после включения CQ».</p>
    <p>Вариант про <code>container-name</code> привлекателен, потому что имя действительно нужно, когда контейнеров несколько и надо адресовать конкретный. Но без имени контейнер работает как безымянный и находится обычным поиском вверх по дереву.</p>
    <p>В подавляющем большинстве случаев нужен <code>container-type: inline-size</code>: containment только по ширине, высота остаётся от контента. И помни побочный эффект — <code>container-type</code> создаёт stacking context и containing block для fixed-потомков, то есть те же баги, что и <code>transform</code>.</p>`,
    cardId: 'css15' },

];
