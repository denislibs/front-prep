const DECK_SD_EXTRA = [
  { id: 'sdx1',
    q: 'Спроектируй совместное редактирование документа как в Google Docs. Как решаешь конфликты?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что именно редактируем: плоский текст, rich text с форматированием или структурированный документ с таблицами, комментариями и встроенными объектами — от этого зависит модель данных сильнее, чем от алгоритма. Сколько одновременных редакторов в одном документе: 2-3 или 50+ (второе требует фанаута через pub/sub и троттлинга presence). Нужен ли настоящий офлайн с последующим мержем или достаточно работы при живом соединении. Есть ли требование к истории версий, к attribution («кто написал эту строку») и к юридическому аудиту правок. Можем ли мы держать авторитетный сервер (тогда OT реален) или нужна работа p2p и на edge. Каков размер документа: 10 страниц или книга на 500 — от этого зависит стратегия снапшотов.</p>

<h4>Ключевые решения</h4>
<p><strong>Модель конфликтов: OT vs CRDT.</strong> OT (как в Google Docs) требует авторитетного сервера, который трансформирует координаты операций относительно уже применённых: память экономная, интент пользователя сохраняется точнее, но сервер обязателен и алгоритм трансформаций почти невозможно написать без багов на rich text. CRDT (Yjs, Automerge) сходится без центрального арбитра, даёт local-first и офлайн-мерж, но платит метаданными на каждую вставку — нужны tombstone GC и сжатие структуры.</p>
<p><strong>Практический выбор:</strong> Yjs + y-websocket. Сервер — тонкий relay плюс persistence: документ хранится как бинарный лог апдейтов со снапшотом каждые N апдейтов, чтобы новый клиент не проигрывал всю историю. <strong>Транспорт</strong> — WebSocket с fallback на long-polling; при реконнекте клиент отправляет свой state vector, сервер отвечает диффом, а не полным документом. <strong>Рендеринг</strong> — редактор поверх CRDT-биндинга (y-prosemirror, y-codemirror), локальные правки применяются мгновенно, сеть асинхронна: UI никогда не ждёт сервер. <strong>Кеш</strong> — y-indexeddb, он же очередь офлайн-правок. <strong>Эфемерное состояние</strong> (курсоры, выделения, «печатает») идёт отдельным awareness-каналом и не попадает в CRDT.</p>

<h4>Компромиссы</h4>
<p>Взяли CRDT — получили офлайн и отсутствие единой точки отказа, отдали размер документа и невозможность точного «серверного» контроля порядка операций. Если бы требования были: строгая модерация каждой правки, экономия памяти на документах-книгах и нулевой офлайн — выбор перевернулся бы в сторону OT с авторитетным сервером. Снапшоты дают быстрый старт, но ломают полную историю — если нужен построчный blame, храним лог целиком и платим местом. Awareness через отдельный канал экономит трафик, но presence при этом не переживает реконнект: это осознанная потеря.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Права и валидацию: апдейт от клиента нельзя применять без серверной проверки, иначе любой участник с read-only правит документ через devtools.</li>
<li>Undo: глобальный undo-стек отменяет чужие правки. Нужен per-user UndoManager, отслеживающий только свои origin-ы.</li>
<li>Состояния загрузки: между открытием документа и синхронизацией есть окно, где редактор пустой — надо показывать skeleton и блокировать ввод, иначе первые символы улетят в «не тот» документ.</li>
<li>Гонку «два таба одного пользователя»: без BroadcastChannel получите два независимых клиента, конкурирующих за один awareness-слот.</li>
<li>Доступность: курсоры соавторов должны объявляться скринридером как live region, а не только рисоваться цветными полосками.</li>
<li>Масштаб: одна комната на один процесс не переживёт 200 участников — нужен шардинг по документу и фанаут через Redis.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать историю версий?»</strong> — периодические именованные снапшоты плюс дифф между state vector-ами; полноценный time travel в Yjs делается через snapshot API. <strong>«Что если документ 100 МБ?»</strong> — субдокументы: каждый раздел отдельный Y.Doc, грузим лениво по мере скролла. <strong>«Как гарантировать доставку?»</strong> — CRDT идемпотентен и коммутативен, повторная доставка апдейта безопасна, поэтому ретрай — это просто повторная отправка буфера.</p>`,
    code: `// протокол синхронизации: клиент присылает state vector, сервер отдаёт только дифф
socket.onopen = () => {
  const sv = Y.encodeStateVector(doc);        // что у меня уже есть
  socket.send(encode({ type: 'sync-step-1', sv }));
};

socket.onmessage = (e) => {
  const msg = decode(e.data);
  if (msg.type === 'sync-step-2' || msg.type === 'update') {
    Y.applyUpdate(doc, msg.update, 'remote'); // origin помечаем как remote
  }
};

// локальные правки уходят наружу, чужие обратно не отражаем
doc.on('update', (update, origin) => {
  if (origin === 'remote') return;
  socket.send(encode({ type: 'update', update }));
});

// undo только своих правок: origin-фильтр обязателен
const undo = new Y.UndoManager(doc.getText('body'), {
  trackedOrigins: new Set([null, 'local']),
});

// presence живёт отдельно и не пишется в документ
awareness.setLocalStateField('cursor', { anchor, head, userId, color });`,
    tip: 'Сильный ход — сузить задачу: для структурированных данных (форма, строка таблицы) хватает last-write-wins на уровне поля с версией, а дорогой CRDT нужен только там, где два человека печатают в одну строку текста.' },

  { id: 'sdx2',
    q: 'Спроектируй Kanban-доску с drag-n-drop. Что делать с конкурентными перемещениями карточек?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько карточек в колонке: 20 или 5000 (второе означает виртуализацию, которая конфликтует с drag-n-drop). Сколько людей одновременно двигают карточки на одной доске — от этого зависит, нужен ли реалтайм вообще или хватит polling при фокусе вкладки. Нужны ли swimlane-ы и перетаскивание между досками. Есть ли мобильная версия: тач-DnD требует long-press для старта, иначе конфликтует со скроллом. Нужен ли офлайн. И главное: что считается конфликтом — если двое поставили карточку в одно место, это ошибка, которую надо показать, или просто два допустимых порядка, из которых любой сойдёт.</p>

<h4>Ключевые решения</h4>
<p><strong>Модель данных.</strong> Порядок хранится не как индекс в массиве, а как <strong>fractional index</strong>: у каждой карточки есть <code>rank</code> — строка или дробь, и при вставке между A и B считаем значение посередине. Перемещение меняет ровно один объект, а не переиндексирует всю колонку. Именно это делает конкурентные перемещения безопасными: два клиента, двигающие разные карточки, не пишут в одни и те же строки. Строковые ранги (base62 lexicographic) лучше чисел с плавающей точкой: у double кончается мантисса примерно после 50 вставок в одну точку.</p>
<p><strong>Рендеринг и жесты.</strong> DnD на @dnd-kit или собственных Pointer Events поверх <code>transform</code>: во время перетаскивания двигается только один элемент, соседи разъезжаются CSS-транзишеном. Никаких layout-свойств в анимации, иначе на длинной доске кадр не укладывается в 16 мс.</p>
<p><strong>Транспорт и синхронизация.</strong> Оптимистичный апдейт: локально меняем rank, отправляем PATCH с новым rank и <code>version</code> карточки. Реалтайм — WebSocket с узкими событиями <code>card.moved</code> / <code>card.updated</code>, свои echo-события фильтруем по clientId, иначе карточка дёрнется дважды. Конкурентный move разрешаем last-write-wins по полю rank, коллизия рангов допустима, тай-брейк по id — это даёт детерминированный одинаковый порядок у всех клиентов.</p>

<h4>Компромиссы</h4>
<p>Fractional indexing даёт дешёвые перемещения, но ранги деградируют: при многократной вставке в одну точку строка растёт. Лечится фоновой перенумерацией колонки в тихий момент — платим редкой массовой записью ради быстрых обычных операций. LWW прост и не требует диалогов разрешения конфликта, но при одновременном перемещении одной карточки двумя людьми один из них увидит, как его действие «отменилось». Если бы это было недопустимо (например, доска — производственный процесс с ответственностью), решение поменялось бы на серверную блокировку карточки на время перетаскивания. Виртуализация экономит DOM, но ломает автоскролл и dropzone за пределами окна — на досках до нескольких сотен карточек её лучше не включать.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Откат: при 409 карточку нельзя телепортировать назад — нужна обратная анимация, иначе пользователь не понимает, что произошло.</li>
<li>Автоскролл при перетаскивании к краю экрана и к краю колонки — без него доска непригодна на ноутбуке.</li>
<li>Доступность: DnD обязан иметь клавиатурный режим (Space — взять, стрелки — двигать, Space — положить) с объявлением позиции через live region. Это требование WCAG, а не украшение.</li>
<li>Гонка «переместил и сразу переместил снова»: запросы приходят на сервер не по порядку. Нужна очередь по id карточки или монотонный счётчик операций клиента.</li>
<li>Офлайн: очередь move-операций должна схлопываться — десять перемещений одной карточки офлайн это одно финальное положение, а не десять PATCH.</li>
<li>Масштаб: событие «доска изменилась» без указания что именно заставляет всех клиентов перезапрашивать доску целиком — на 50 участниках это лавина.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Что если ранги кончились?»</strong> — детект длины строки, фоновый ребаланс колонки одной транзакцией с рассылкой полного нового порядка. <strong>«Как показать, что карточку двигает другой человек?»</strong> — эфемерное presence-событие <code>card.dragging</code> с TTL, не сохраняем в БД. <strong>«Как сделать WIP-лимиты?»</strong> — проверка на сервере при применении move, на клиенте только предупреждение; блокировать дроп на клиенте недостаточно.</p>`,
    code: `// fractional index: вставка между двумя соседями меняет ровно одну строку
function rankBetween(prev, next) {
  // prev/next — строки в base62 или null на краях списка
  if (!prev && !next) return 'a0';
  if (!prev) return decrement(next);
  if (!next) return increment(prev);
  return midpoint(prev, next);       // 'a0' + 'a1' -> 'a0V'
}

// оптимистичное перемещение с версией и очередью по карточке
async function moveCard(card, prevId, nextId) {
  const rank = rankBetween(rankOf(prevId), rankOf(nextId));
  store.applyLocal({ id: card.id, rank });          // UI обновился мгновенно
  try {
    const res = await api.patch('/cards/' + card.id, {
      rank,
      version: card.version,
      clientId,                                     // чтобы отфильтровать свой echo
    });
    store.commit(res.card);
  } catch (e) {
    if (e.status === 409) store.rollbackAnimated(card.id, e.body.card);
    else store.enqueueRetry({ id: card.id, rank }); // схлопывается по id
  }
}`,
    tip: 'Назови конкретную поломку, которую чинит fractional indexing: при «порядок = index в массиве» два одновременных перемещения дают либо дубль позиции, либо перезапись всей колонки — и вторая перезапись затирает чужое перемещение целиком.' },

  { id: 'sdx3',
    q: 'Спроектируй дашборд аналитики с графиками и реалтаймом.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько виджетов на экране и сколько точек в серии: 6 графиков по 200 точек и 40 графиков по 100 тысяч — это разные системы. Что значит «реалтайм»: обновление раз в секунду, раз в 5 секунд или «не старше минуты» (последнее снимает WebSocket с повестки). Дашборд фиксированный или пользователь сам собирает layout и сохраняет его. Нужен ли drill-down и кросс-фильтрация между виджетами — это самое дорогое требование. Кто пользователь: аналитик за монитором 27" или дежурный с телефона. Нужен ли экспорт в CSV/PNG и шаринг ссылкой. Какая задержка данных допустима на стороне хранилища — если бэкенд считает агрегаты минуту, спорить о фронтовом кеше бессмысленно.</p>

<h4>Ключевые решения</h4>
<p><strong>Агрегация — на бэке, всегда.</strong> Клиент запрашивает уже свёрнутые серии по параметрам (метрика, granularity, диапазон, фильтры), а не сырые события. Даунсемплинг делаем алгоритмом LTTB до 500-2000 точек на серию: он сохраняет визуальную форму (пики и провалы), в отличие от простого «каждая n-я точка», которая теряет выбросы — а именно ради выбросов дашборд и смотрят.</p>
<p><strong>Архитектура — независимые виджеты.</strong> Каждый виджет владеет своим запросом, своими состояниями loading/error/empty/partial и своим интервалом обновления. Глобальный стор держит только фильтры и диапазон, виджеты на них подписаны. Это даёт частичную деградацию: упавший виджет показывает заглушку и кнопку «повторить», а не роняет страницу.</p>
<p><strong>Кеш.</strong> Ключ — нормализованный кортеж (метрика, фильтры, granularity, границы бакета). Исторические бакеты иммутабельны и кешируются надолго, текущий (незакрытый) бакет — секунды. Политика stale-while-revalidate: при смене диапазона сразу показываем старые данные приглушённо и дорисовываем новые.</p>
<p><strong>Транспорт реалтайма.</strong> Не пушим каждое событие: сервер шлёт агрегаты раз в 1-5 секунд через SSE (проще WebSocket, работает через прокси, сам переподключается), клиент дописывает точку в хвост серии и сдвигает окно.</p>
<p><strong>Рендер.</strong> SVG до 1-2 тысяч элементов на график — он дешевле в разработке и доступен; выше — Canvas, а при десятках тысяч точек WebGL. Тултипы всегда через один делегированный слой и бинарный поиск по x, а не обработчик на каждой точке.</p>

<h4>Компромиссы</h4>
<p>Серверная агрегация делает клиент простым и быстрым, но убивает мгновенную кросс-фильтрацию: каждое изменение фильтра — раунд-трип. Если продукту нужен именно исследовательский режим с моментальным откликом, решение переворачивается: тянем колоночный срез данных на клиент (Arrow/Parquet через DuckDB-WASM) и считаем в Web Worker, платя мегабайтами трафика и памятью. SSE проще сокета, но односторонний — если появится интерактивная подписка на конкретные метрики, придётся переезжать на WebSocket. Canvas быстрее SVG, но теряет доступность и выделение текста — для маленьких графиков это плохая сделка.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Таймзоны: бакет «день» должен считаться в таймзоне пользователя или явно в UTC. Иначе «вчера» у аналитика и у графика разное, и это выясняется на демо перед руководством.</li>
<li>Гонки: при быстром переключении диапазона ответы приходят не по порядку. Нужна отмена через AbortController и проверка, что пришедший ответ соответствует текущим параметрам.</li>
<li>Пустое и частичное состояние: «нет данных за период» и «часть источников недоступна» — разные экраны, второй обязан быть честным, иначе график врёт нулями.</li>
<li>Разрывы в данных: отсутствующие бакеты нельзя интерполировать линией, иначе падение сбора выглядит как нормальная метрика.</li>
<li>Вкладка в фоне: таймеры обновления надо останавливать по <code>visibilitychange</code>, иначе 20 вкладок дашборда устроят DDoS собственному API.</li>
<li>Доступность: график без таблицы-альтернативы и без <code>aria-label</code> с описанием тренда недоступен полностью.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Пользователь открыл диапазон в год с гранулярностью минута»</strong> — сервер обязан отклонить запрос или принудительно огрубить granularity, вернув реально применённое значение; клиент показывает это явно. <strong>«Как сделать кросс-фильтр по клику на графике?»</strong> — клик пишет фильтр в общий стор и в URL, остальные виджеты перезапрашиваются; чтобы не было шторма, батчим изменения фильтров в один тик. <strong>«Как шарить дашборд?»</strong> — всё состояние в URL, длинные наборы фильтров — через сохранённый на сервере view id.</p>`,
    code: `// нормализованный ключ кеша: порядок фильтров не должен влиять на попадание
function seriesKey(q) {
  const filters = Object.entries(q.filters)
    .sort(([a], [b]) => (a < b ? -1 : 1))
    .map(([k, v]) => k + '=' + [].concat(v).sort().join(','))
    .join('&');
  return [q.metric, q.granularity, q.from, q.to, filters].join('|');
}

// ответ API: сервер сообщает, что реально применил
// {
//   "metric": "orders.count",
//   "granularity": "1h",          // клиент просил "1m", сервер огрубил
//   "downsampled": true,
//   "points": [[1735689600000, 412], [1735693200000, null], ...],
//   "partial": ["eu-west-1"]      // источник недоступен -> честная плашка
// }

// защита от гонок: игнорируем ответ, если параметры уже сменились
const ctrl = new AbortController();
const key = seriesKey(query);
const data = await fetchSeries(query, { signal: ctrl.signal });
if (seriesKey(currentQuery) !== key) return;   // ответ устарел
store.setSeries(key, data);`,
    tip: 'Проговори, что null в точке и 0 в точке — разные вещи: null означает «данных нет», рисуется разрывом, 0 означает «событий не было». Дашборды, которые их путают, врут ровно в момент инцидента, когда сбор метрик и отвалился.' },

  { id: 'sdx4',
    q: 'Спроектируй каталог e-commerce с фасетными фильтрами. Где живёт состояние фильтров?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько товаров и сколько фасетов: 5 тысяч SKU с 6 фильтрами и 5 миллионов с 40 динамическими характеристиками — принципиально разные задачи. Важен ли органический трафик: если да, SSR и индексируемые URL становятся жёстким требованием, а не оптимизацией. Персонализирована ли выдача (цены по региону, наличие по складу) — это определяет, можно ли вообще кешировать HTML на CDN. Есть ли мультиселект в фасетах и диапазонные фильтры. Нужны ли counts у значений фасета — их подсчёт стоит бэкенду дорого. Какая доля мобильного трафика: на мобильном фасеты — это отдельный полноэкранный шит с отложенным применением, а не сайдбар.</p>

<h4>Ключевые решения</h4>
<p><strong>Состояние фильтров живёт в URL, и это единственный источник правды:</strong> <code>/catalog/shoes?brand=nike,puma&amp;price=100-500&amp;sort=popular&amp;page=2</code>. URL даёт шаринг, работающую кнопку «назад», серверный рендер и индексацию. Стор — производная от URL, а не параллельная копия: как только появляются две копии состояния, начинается рассинхрон при навигации назад.</p>
<p><strong>Рендеринг.</strong> Первая страница — SSR (или ISR) по параметрам из URL: карточкам нужен SEO и быстрый LCP, а первый экран не должен ждать клиентского бандла. Дальнейшие изменения фильтров — клиентские запросы с debounce 200-300 мс и отменой предыдущего через AbortController.</p>
<p><strong>Фасеты приходят вместе с выдачей</strong> и содержат counts: пользователь не должен выбирать комбинацию, дающую ноль. Важная деталь: counts для уже выбранного измерения считаются <strong>без учёта этого измерения</strong>, иначе список брендов схлопнется до одного выбранного и мультиселект станет бесполезен.</p>
<p><strong>Кеш.</strong> На CDN кешируем по нормализованному URL: параметры сортируются, значения внутри параметра сортируются, дефолты выкидываются — чтобы <code>brand=a,b</code> и <code>brand=b,a</code> били в один ключ. Это напрямую поднимает hit rate, а значит и p75 TTFB.</p>
<p><strong>Пагинация — страницами</strong>, а не бесконечной лентой: лучше индексируется, даёт возврат к товару и предсказуемый футер.</p>

<h4>Компромиссы</h4>
<p>URL как источник правды даёт шаринг и историю, но делает каждое переключение чекбокса записью в history — поэтому промежуточные изменения пишем через <code>replaceState</code>, а «применить» — через <code>pushState</code>. SSR даёт SEO и LCP, но требует, чтобы бэкенд поиска держал латентность в бюджете рендера; при персонализированных ценах кешировать HTML нельзя — тогда переходим на кеш разметки без цен и дозагрузку цен отдельным запросом. Counts точные — дорого; в больших каталогах берут приблизительные (HyperLogLog) и показывают «более 1000», это осознанный размен точности на латентность.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Гонки: быстрое переключение фильтров даёт ответы вразнобой. Без отмены и проверки соответствия ответа текущему URL список «отматывается» на предыдущее состояние.</li>
<li>Мигание: между запросами нельзя показывать пустой экран — старый список под приглушённым слоем плюс скелетон, иначе воспринимается как поломка.</li>
<li>Нулевая выдача: нужен отдельный сценарий с предложением снять самый ограничивающий фильтр, а не пустая страница.</li>
<li>Индексация мусора: комбинации фильтров плодят миллионы URL. Нужен whitelist SEO-значимых комбинаций, <code>noindex, follow</code> для остальных и канонический URL на базовую категорию.</li>
<li>CLS: карточки без зарезервированного aspect-ratio под картинку дают скачок макета ровно там, где мерится Core Web Vitals.</li>
<li>Доступность: группа фильтров — это <code>fieldset</code> с легендой, изменение выдачи объявляется через live region «найдено 128 товаров», иначе для скринридера ничего не произошло.</li>
<li>Мобильный сценарий: применять фильтр мгновенно на мобильном — значит перезагружать выдачу под пальцем; там нужен отложенный «Показать 128 товаров».</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать так, чтобы фильтр не сбрасывал страницу?»</strong> — любое изменение фильтра обязано сбрасывать <code>page</code> в 1, иначе пользователь попадает на пустую страницу 7. <strong>«Как кешировать при персональных ценах?»</strong> — разделение: кешируемый каркас выдачи плюс некешируемый батч-запрос цен и наличия по списку id. <strong>«Что с бесконечной лентой для мобильного?»</strong> — гибрид: подгрузка кнопкой «показать ещё» с корректным обновлением <code>page</code> в URL, чтобы возврат работал.</p>`,
    code: `// нормализация URL ради попадания в CDN-кеш
function canonicalQuery(params) {
  const out = new URLSearchParams();
  for (const key of Object.keys(params).sort()) {
    const value = params[key];
    if (value == null || value === '' || value === DEFAULTS[key]) continue;
    out.set(key, [].concat(value).sort().join(','));
  }
  return out.toString();  // brand=nike,puma&page=2&price=100-500&sort=popular
}

// ответ API: выдача и фасеты одним запросом, counts без учёта своего измерения
// {
//   "total": 128,
//   "items": [{ "id": "sku-1", "price": 4990, "inStock": true }],
//   "facets": {
//     "brand": { "multi": true, "values": [
//       { "value": "nike", "count": 74, "selected": true },
//       { "value": "puma", "count": 54, "selected": true },
//       { "value": "asics", "count": 31, "selected": false }
//     ]},
//     "price": { "type": "range", "min": 990, "max": 24990 }
//   },
//   "appliedFilters": ["brand", "price"]
// }

// смена фильтра всегда сбрасывает страницу и не засоряет history
function setFilter(next) {
  const url = '?' + canonicalQuery({ ...current, ...next, page: 1 });
  router.replace(url, { scroll: false });
}`,
    tip: 'Редко вспоминают связку «нормализация query-параметров -> hit rate на CDN -> TTFB»: назови её и добавь, что дефолтные значения (sort=popular, page=1) надо выкидывать из URL, иначе одна и та же выдача живёт под четырьмя разными ключами кеша.' },

  { id: 'sdx5',
    q: 'Спроектируй корзину и чекаут. Как гарантировать, что заказ не создастся дважды?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Есть ли гостевой чекаут или логин обязателен — от этого зависит вся модель хранения корзины. Кто держит платёж: встроенная форма провайдера (Stripe Elements) или редирект на внешний шлюз с 3-D Secure — второе означает, что приложение теряет управление посреди сценария. Резервируется ли товар на время оформления и на сколько минут. Есть ли промокоды, бонусы, частичная оплата — каждая такая сущность добавляет пересчёт итоговой суммы на сервере. Сколько шагов и можно ли их пропускать. Нужна ли работа корзины офлайн. Какие требования комплаенса: PCI DSS означает, что реквизитов карты не должно быть в нашем DOM вообще.</p>

<h4>Ключевые решения</h4>
<p><strong>Идемпотентность как основа.</strong> Фронт генерирует <code>Idempotency-Key</code> (UUID) один раз в момент входа в чекаут, кладёт его в sessionStorage и отправляет в заголовке при создании заказа. Любой ретрай — после таймаута, после reconnect, после нажатия «назад» — идёт с тем же ключом, и сервер возвращает тот же заказ вместо создания второго. Блокировка кнопки «Оплатить» — это UX, а не гарантия: ретраить может сама сеть, прокси или пользователь через историю браузера.</p>
<p><strong>Модель корзины.</strong> Локально храним только id и количество; цены, скидки, доступность и итог считает сервер и возвращает целиком. Для гостя — серверная корзина по cookie плюс локальная копия для мгновенного рендера. При логине корзины мержим по явному правилу (обычно суммируем количества, конфликты по остатку показываем пользователю) — это продуктовое решение, и его надо проговорить вслух.</p>
<p><strong>Чекаут — машина состояний</strong> (адрес → доставка → оплата → подтверждение) с серверным черновиком заказа. Текущий шаг в URL, данные в draft на сервере: тогда возврат из 3-D Secure или закрытие вкладки не теряет прогресс.</p>
<p><strong>Валидация двухуровневая:</strong> мгновенная клиентская ради UX и серверная как источник истины. Ошибки бэка приходят с указанием поля и машинным кодом и подсвечивают конкретный инпут, а не выводятся общим алертом.</p>

<h4>Компромиссы</h4>
<p>Серверный расчёт итога добавляет раунд-трип на каждое изменение корзины, зато исключает расхождение «на экране 4900, списали 5400» — для денег это единственно верный размен. Резерв товара на 15 минут улучшает конверсию, но снижает доступный сток и требует фонового возврата резерва; без резерва получаем отказы на последнем шаге. Гостевой чекаут поднимает конверсию и усложняет всё: мерж корзин, дедупликация пользователей, восстановление заказа по email. Встроенная форма провайдера даёт лучший UX, редирект — меньше ответственности по PCI; если у команды нет ресурса на аудит, выбираем редирект осознанно.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Ресинхронизацию перед оплатой: цена или наличие могли измениться за время оформления. Это надо показать явным диффом «было / стало» с подтверждением, а не молча провести платёж по новой цене.</li>
<li>Двойной сабмит через историю браузера: пользователь жмёт «назад» с экрана успеха и повторяет POST. Ключ идемпотентности спасает, состояние «заказ уже создан» в draft — тоже.</li>
<li>Возврат из внешнего шлюза: платёж прошёл, а webhook ещё не доехал. Нужен экран «проверяем оплату» с polling и таймаутом, а не «ошибка».</li>
<li>Гонка «изменил количество и сразу оформил»: запрос на изменение корзины ещё в полёте. Оформление должно ждать квиесценции корзины или отправлять её версию.</li>
<li>Восстановление после потери сети посреди оплаты: outbox не подходит, платёж офлайн не делается — честно блокируем шаг и показываем состояние сети.</li>
<li>Доступность: сообщения об ошибках должны попадать в <code>aria-live</code>, фокус — переезжать на первое невалидное поле; иначе форма непроходима с клавиатуры.</li>
<li>Аналитика воронки теряется на переходах, если слать обычным fetch — только <code>sendBeacon</code> или <code>keepalive</code>.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Что если ответ на создание заказа потерялся?»</strong> — повтор с тем же ключом; сервер отвечает 200 с тем же order id и признаком, что это повтор. <strong>«Как понять, что заказ точно оплачен?»</strong> — источник истины webhook от провайдера на сервере, фронт только опрашивает статус заказа. <strong>«Как не потерять корзину при смене устройства?»</strong> — серверная корзина, привязанная к пользователю, с мержем при входе.</p>`,
    code: `// один ключ на попытку оформления, а не на клик
function getIdempotencyKey() {
  let key = sessionStorage.getItem('checkout:key');
  if (!key) {
    key = crypto.randomUUID();
    sessionStorage.setItem('checkout:key', key);
  }
  return key;
}

async function placeOrder(draftId) {
  const res = await fetch('/api/orders', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Idempotency-Key': getIdempotencyKey(),
    },
    body: JSON.stringify({ draftId, pricingVersion: draft.pricingVersion }),
  });

  if (res.status === 409) {
    // сервер пересчитал: цена или наличие изменились
    const diff = await res.json();
    return showPriceDiff(diff);   // явный экран «было / стало»
  }
  const order = await res.json();
  sessionStorage.removeItem('checkout:key');  // ключ израсходован
  return order;
}

// ошибки бэка адресуются полям, а не общему алерту
// { "type": "validation", "errors": [
//     { "field": "shipping.postalCode", "code": "unsupported_region" } ] }`,
    tip: 'Ключ идемпотентности генерируется один раз на попытку оформления и удаляется только после успеха — если генерировать его на каждый клик, защита исчезает; если не удалять после успеха, следующий заказ вернёт предыдущий.' },

  { id: 'sdx6',
    q: 'Спроектируй видеоплеер со стримингом. Как работает адаптивный битрейт?',
    a: `<h4>Уточняющие вопросы</h4>
<p>VOD или живой эфир — у эфира требование по задержке (2-5 секунд для интерактива) меняет всё: понадобится LL-HLS с частичными сегментами или WebRTC. Нужен ли DRM: Widevine/FairPlay добавляют лицензионный запрос в критический путь старта и запрещают часть отладки. Какие платформы: Safari и iOS играют HLS нативно и не дают MSE на iPhone, что ограничивает кастомный ABR. Сколько плееров одновременно на странице (лента с автовоспроизведением — это отдельная задача бюджета трафика). Нужны ли субтитры, несколько аудиодорожек, офлайн-скачивание. Какой целевой показатель: минимальное время до первого кадра или максимальное качество картинки — это конфликтующие цели.</p>

<h4>Ключевые решения</h4>
<p><strong>Формат и транспорт.</strong> HLS или MPEG-DASH поверх Media Source Extensions. Видео заранее нарезано на сегменты по 2-6 секунд в нескольких качествах (ladder), мастер-манифест описывает варианты. Плеер сам выбирает следующий сегмент и складывает его в <code>SourceBuffer</code>. В Safari HLS играется нативно через <code>video src</code>, в остальных браузерах — через hls.js. Трансмуксинг TS в fMP4 держим в Web Worker, иначе на слабом устройстве парсинг сегмента съедает кадры.</p>
<p><strong>ABR — гибридная логика.</strong> Оценка пропускной способности по времени загрузки последних сегментов (EWMA по быстрому и медленному окну) плюс буферное правило BOLA-типа. Правило асимметричное: при падении буфера ниже порога роняем качество агрессивно и сразу, при здоровом буфере поднимаем на одну ступень и с задержкой. Асимметрия нужна, чтобы не «пилить» качество вверх-вниз — переключения раздражают сильнее, чем стабильно среднее качество.</p>
<p><strong>Старт.</strong> Первый сегмент берём заведомо низкого качества ради быстрого time-to-first-frame, апгрейдим через 1-2 сегмента. Параллельно префетчим манифест на hover, если это лента.</p>
<p><strong>Буфер.</strong> Держим около 30 секунд вперёд, при seek чистим <code>SourceBuffer</code> вокруг новой позиции: иначе упираемся в квоту памяти MSE и получаем <code>QuotaExceededError</code> посреди просмотра.</p>

<h4>Компромиссы</h4>
<p>Короткие сегменты (2 с) дают низкую задержку и быструю реакцию ABR, но увеличивают число запросов и накладные расходы; длинные (6-10 с) эффективнее по сети, но плеер медленнее реагирует на падение канала. Агрессивный ABR держит высокое качество и рискует ребуферами; консервативный — наоборот. Что важнее, решается метрикой: rebuffer ratio почти всегда бьёт по удержанию сильнее, чем битрейт, поэтому по умолчанию выбираем консервативный. Нативный HLS в Safari даёт энергоэффективность и AirPlay, но отбирает контроль над ABR и метриками — приходится вести две ветки кода.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Ошибки: сегмент не скачался — это не фатально, нужен ретрай с backoff и переход на соседний вариант качества, а не «видео недоступно».</li>
<li>Autoplay-политику: без <code>muted</code> браузер не даст автозапуск, и промис от <code>play()</code> отклонится. Его обязательно надо обработать и показать кнопку.</li>
<li>Метрики: startup time, rebuffer ratio, средний битрейт, число переключений и exit-before-start — это и есть SLI плеера, без них спор об ABR беспредметен.</li>
<li>Память: десяток плееров в ленте с preload держат десятки мегабайт буферов; нужно уничтожать инстанс при уходе из вьюпорта, а не просто ставить на паузу.</li>
<li>Доступность: субтитры, управление с клавиатуры, фокус-ловушка в полноэкранном режиме, объявление состояния. Плеер без клавиатуры недоступен полностью.</li>
<li>Смена вкладки: браузер тротлит таймеры, ABR на <code>setInterval</code> начинает врать — считаем по событиям загрузки сегментов.</li>
<li>Экономия трафика: <code>navigator.connection.saveData</code> и мобильная сеть должны ограничивать верхнюю ступень ladder.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать задержку 3 секунды на эфире?»</strong> — LL-HLS с partial segments и blocking playlist reload, либо WebRTC, если нужна субсекундная задержка и мало зрителей. <strong>«Как измерить, что ABR стал лучше?»</strong> — A/B на реальном трафике по rebuffer ratio и времени старта, лабораторные тесты на эмуляции сети только как sanity check. <strong>«Что с превью в ленте?»</strong> — грузим только манифест и первый сегмент, максимум пару секунд, и отменяем при уходе из вьюпорта.</p>`,
    code: `// асимметричное правило ABR: вниз резко, вверх осторожно
function chooseLevel(levels, { bandwidthEwma, bufferSec }) {
  const safe = levels
    .filter((l) => l.bitrate < bandwidthEwma * 0.8)   // запас 20%
    .sort((a, b) => b.bitrate - a.bitrate);

  const target = safe[0] ?? levels[0];                // худший как fallback

  if (bufferSec < 5) return levels[0];                // паника: минимум сразу
  if (target.index > current.index && bufferSec < 15) return current;
  if (target.index > current.index) return levels[current.index + 1]; // +1 ступень
  return target;                                      // вниз можно сразу
}

// обязательная обработка autoplay-политики
try {
  await video.play();
} catch (err) {
  if (err.name === 'NotAllowedError') showPlayButton();
  else reportPlaybackError(err);
}

// SLI плеера, которые считаем всегда
// startupMs, rebufferRatio = rebufferMs / watchedMs,
// avgBitrate, levelSwitches, exitBeforeStart`,
    tip: 'Сформулируй целевой SLI вслух: rebuffer ratio ниже 0.5% и время до первого кадра ниже секунды. Тогда все решения об ABR и длине сегмента становятся расчётом, а не вкусом.' },

  { id: 'sdx7',
    q: 'Спроектируй загрузку больших файлов (несколько ГБ) из браузера.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Какой максимальный размер и сколько файлов за раз: один файл на 5 ГБ и папка из 20 тысяч мелких — противоположные оптимизации. Куда физически кладём: в собственный бэкенд или прямо в объектное хранилище через presigned URL. Нужна ли обработка после загрузки (транскодирование, антивирус, извлечение метаданных) — тогда загрузка асинхронна и нужен статус job. Требуется ли шифрование на клиенте — это меняет возможность возобновления. Нужно ли переживать закрытие вкладки. Какие сети у пользователей: на мобильном обрывы норма, и возобновляемость перестаёт быть опцией. Есть ли лимиты и квоты, которые надо проверить до начала передачи, а не после.</p>

<h4>Ключевые решения</h4>
<p><strong>Никакого одиночного POST.</strong> Тело на 5 ГБ упирается в таймауты прокси и лимиты body size, не даёт прогресса и теряет всё при обрыве. Режем файл через <code>File.slice()</code> на части по 5-10 МБ и льём в 3-4 параллельных потока.</p>
<p><strong>Протокол — один из двух.</strong> tus: сервер хранит offset, клиент делает <code>HEAD</code> и продолжает с <code>PATCH</code> — удобно, когда файл всё равно проходит через наш бэкенд. Или S3 multipart через presigned URL на каждую часть: браузер льёт напрямую в хранилище, минуя API. Второй вариант обычно дешевле, снимает трафик с приложения и масштабируется сам, но требует выдачи и ротации presigned URL и сборки (<code>CompleteMultipartUpload</code>) на бэке.</p>
<p><strong>Возобновление.</strong> В IndexedDB держим id загрузки, список подтверждённых частей с их ETag и отпечаток файла (имя + размер + lastModified). После перезагрузки страницы предлагаем продолжить и проверяем, что пользователь выбрал тот же файл.</p>
<p><strong>Прогресс.</strong> Через <code>XMLHttpRequest.upload.onprogress</code> (у fetch до сих пор нет надёжного upload-прогресса) или через <code>ReadableStream</code> в теле запроса там, где он поддерживается. Агрегируем по частям и сглаживаем ETA скользящим средним, иначе оценка скачет.</p>
<p><strong>Целостность.</strong> Контрольная сумма на часть, финальная сборка и проверка на сервере. Хеш большого файла считаем инкрементально в Web Worker потоково, не загружая файл в память целиком.</p>

<h4>Компромиссы</h4>
<p>Больше параллельных потоков — не всегда быстрее: на мобильной сети 6 потоков уменьшают throughput из-за конкуренции и потерь, оптимум обычно 3-4 и его стоит адаптировать по измеренной скорости. Крупные чанки экономят запросы, но дороже при обрыве (теряется весь чанк) и хуже показывают прогресс. Прямая загрузка в S3 дешевле, но лишает бэкенд возможности валидировать содержимое на лету — проверку приходится делать после сборки, а значит нужен статус «загружено, но ещё не готово». Клиентское шифрование даёт приватность и ломает дедупликацию и докачку по content hash.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Память: чтение файла целиком через <code>FileReader</code> убивает вкладку на 5 ГБ. Работаем только слайсами и потоками.</li>
<li>Истечение presigned URL посреди долгой загрузки: URL живёт час, загрузка идёт три. Нужен перезапрос ссылки на часть при 403.</li>
<li>Ретраи на уровне всей загрузки вместо чанка — классическая ошибка, превращающая один сбойный кусок в перезапуск с нуля.</li>
<li>Ошибка «файл изменился на диске во время загрузки»: <code>File</code> — это ссылка, а не копия. При чтении слайса можно получить <code>NotReadableError</code>, это надо ловить.</li>
<li>Уход со страницы: <code>beforeunload</code> с предупреждением обязателен, но не гарантирован — поэтому и нужна возобновляемость.</li>
<li>Квоты и лимиты проверяем до начала: спрашиваем у сервера разрешение на загрузку файла такого типа и размера, а не узнаём об отказе на 4-м гигабайте.</li>
<li>Доступность и обратная связь: прогресс должен быть в <code>role="progressbar"</code> с текстовым эквивалентом, ошибка конкретной части — в списке файлов, а не одним общим тостом.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как пережить закрытие вкладки?»</strong> — Background Fetch API через Service Worker; поддержка неполная, поэтому это прогрессивное улучшение поверх возобновляемости. <strong>«Как ускорить повторную загрузку того же файла?»</strong> — content-addressable: считаем хеш, спрашиваем сервер, есть ли такой объект, при попадании загрузка мгновенна. <strong>«Как грузить папку на 20 тысяч файлов?»</strong> — обратная задача: не чанки, а батчи и жёсткая очередь с ограничением параллельности, плюс агрегированный прогресс.</p>
<p>Отдельно спросят про порядок: части можно грузить в любом порядке, но сборка требует полного набора — поэтому список частей и их ETag хранит клиент, а подтверждает сервер.</p>`,
    code: `// очередь чанков с ограниченным параллелизмом и ретраем на уровне части
const CHUNK = 8 * 1024 * 1024;
const CONCURRENCY = 4;

async function uploadPart(file, index, getUrl) {
  const start = index * CHUNK;
  const blob = file.slice(start, Math.min(start + CHUNK, file.size));

  for (let attempt = 0; attempt < 5; attempt++) {
    try {
      let url = await getUrl(index);            // presigned URL на эту часть
      const res = await fetch(url, { method: 'PUT', body: blob });
      if (res.status === 403) { getUrl.invalidate(index); continue; } // URL протух
      if (!res.ok) throw new Error('HTTP ' + res.status);
      return { index, etag: res.headers.get('ETag') };
    } catch (e) {
      const backoff = Math.min(1000 * 2 ** attempt, 30000);
      await sleep(backoff + Math.random() * 500);  // джиттер
    }
  }
  throw new Error('part ' + index + ' failed');
}

// состояние для возобновления после перезагрузки страницы
await db.put('uploads', {
  uploadId,
  fingerprint: file.name + ':' + file.size + ':' + file.lastModified,
  parts: completedParts,          // [{ index, etag }]
});`,
    tip: 'Отпечаток файла (имя + размер + lastModified) обязателен: без него пользователь после перезагрузки выберет другой файл, докачка склеит два разных файла в один, и это обнаружится только по битой контрольной сумме.' },

  { id: 'sdx8',
    q: 'Спроектируй веб-клиент почты с офлайн-режимом.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что именно должно работать офлайн: только чтение уже загруженного, или полноценная отправка и поиск. Какой объём почтового ящика — 2 тысячи писем или 400 тысяч с вложениями на 30 ГБ (второе означает, что полный кеш невозможен в принципе). Нужен ли поиск офлайн: это отдельный локальный индекс, а не побочный эффект кеша. Сколько устройств у пользователя и насколько быстро должны сходиться состояния. Есть ли требования комплаенса к хранению переписки на устройстве — в корпоративной почте локальный кеш иногда запрещён политикой. Треды или плоский список писем. Нужны ли метки/папки с произвольной вложенностью и правила фильтрации на клиенте.</p>

<h4>Ключевые решения</h4>
<p><strong>Локальная база — первичный источник для UI.</strong> IndexedDB (через Dexie) хранит нормализованные сущности: письма, треды, папки, метки, вложения-метаданные. Компоненты читают только из неё, сеть работает как фоновая синхронизация. Это переворачивает обычную архитектуру: спиннера при открытии папки нет вообще, есть индикатор «синхронизируется».</p>
<p><strong>Синхронизация инкрементальная по курсору.</strong> Сервер отдаёт дельту от <code>historyId</code>: что добавилось, что изменилось, что удалено (tombstones обязательны, иначе удаления не доедут). Клиент применяет дельту в одной транзакции и сохраняет новый курсор. Полный ресинк только при разрыве истории — сервер обязан уметь сказать «твой курсор слишком старый».</p>
<p><strong>Мутации через outbox.</strong> Прочитано, архив, метка, удаление — оптимистичные операции с клиентским UUID, попадающие в очередь. Очередь проигрывается при появлении сети, сервер дедуплицирует по UUID. Операции схлопываются: пометить прочитанным пять раз — это одна операция.</p>
<p><strong>Рендеринг списка.</strong> Виртуализация с кешем измеренных высот: в папке легко 50 тысяч тредов. Тело письма и вложения — лениво по открытию, но следующее письмо в списке префетчим.</p>
<p><strong>Отправка.</strong> Черновик автосохраняется локально и на сервере, письмо ставится в очередь и уходит через Background Sync. «Отменить отправку» — не отзыв письма, а искусственная задержка 5-10 секунд перед реальным запросом.</p>

<h4>Компромиссы</h4>
<p>Local-first даёт мгновенный интерфейс и офлайн, но платит сложностью: два источника изменений, конфликты, миграции схемы локальной базы у пользователей, которые не заходили полгода. Полный кеш невозможен — держим последние N дней и папку «Входящие» целиком, остальное подгружаем; значит поиск офлайн будет неполным, и это надо честно показать в UI («поиск по локальным письмам»). Оптимистичные действия ускоряют работу, но при отказе сервера приходится откатывать уже увиденное пользователем изменение. Альтернатива — кеш-с-сетью (обычный SWR): проще на порядок, но каждое открытие ждёт сеть, и офлайн деградирует до «ничего не работает».</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Квоты: <code>QuotaExceededError</code> надо ловить и вытеснять старое по LRU, а также запрашивать <code>navigator.storage.persist()</code>, иначе браузер вычистит кеш без предупреждения.</li>
<li>Миграции локальной схемы: версия базы меняется, а у пользователя лежат данные старого формата. Нужен путь миграции или контролируемый сброс с ресинком.</li>
<li>Многовкладочность: две вкладки пишут в одну IndexedDB. Нужен лидер-выбор через Web Locks и рассылка изменений через BroadcastChannel.</li>
<li>Конфликт «письмо удалено на другом устройстве, а здесь его открыли» — побеждает сервер, но пользователю показываем аккуратное уведомление, а не тихое исчезновение экрана.</li>
<li>Безопасность рендера HTML: только sandboxed iframe с блокировкой внешних картинок по умолчанию (это и приватность — трекинг-пиксели, и защита от XSS).</li>
<li>Гонка порядка: дельта пришла раньше, чем применилась локальная мутация. Нужен порядок применения и версия на сущность.</li>
<li>Доступность: виртуализированный список ломает Ctrl+F и навигацию скринридера — нужны правильные aria-атрибуты списка и серверный поиск как альтернатива.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать поиск офлайн?»</strong> — локальный инвертированный индекс по заголовкам и отправителям (MiniSearch/FlexSearch) для быстрых подсказок, полнотекст по телу — на сервере, с явным разделением в UI. <strong>«Что если пользователь не заходил год?»</strong> — курсор протух, делаем полный ресинк заголовков и не тянем тела. <strong>«Как сделать так, чтобы вкладки не дублировали синк?»</strong> — Web Locks: одна вкладка держит лок синхронизации и вещает результат остальным.</p>`,
    code: `// outbox: идемпотентные операции со схлопыванием
async function enqueue(op) {
  await db.transaction('rw', db.outbox, async () => {
    const dup = await db.outbox
      .where({ entityId: op.entityId, type: op.type })
      .first();
    if (dup) return db.outbox.update(dup.id, { payload: op.payload });
    return db.outbox.add({ ...op, opId: crypto.randomUUID(), ts: Date.now() });
  });
  scheduleFlush();
}

// один синк на все вкладки: лидер держит Web Lock
navigator.locks.request('mail-sync', async () => {
  for (;;) {
    const { changes, nextCursor, expired } = await api.sync(cursor);
    if (expired) return fullResync();          // курсор слишком старый
    await db.transaction('rw', db.messages, db.threads, async () => {
      await applyChanges(changes);             // включая tombstones
      await db.meta.put({ key: 'cursor', value: nextCursor });
    });
    channel.postMessage({ type: 'synced', nextCursor });
    await waitForPushOrTimeout(30000);
  }
});

// вытеснение по квоте, а не по ошибке в проде
const { usage, quota } = await navigator.storage.estimate();
if (usage / quota > 0.8) await evictOlderThan(90);   // дней`,
    tip: 'Проговори границу локального кеша числом: «полные тела за 90 дней, заголовки за всё время, вложения только открытые». Без такой политики offline-first упирается в квоту и начинает молча терять данные пользователя.' },

  { id: 'sdx9',
    q: 'Спроектируй календарь с событиями. Что самое сложное?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Личный календарь или командный с бронированием переговорок и проверкой занятости — второе добавляет конфликты и права. Нужны ли повторяющиеся события (почти всегда да, и это главный источник сложности). Работают ли пользователи в разных таймзонах и путешествуют ли — если да, то что должно произойти с событием при перелёте. Нужна ли синхронизация с внешними календарями по CalDAV/ICS: это навязывает формат RFC 5545. Нужны ли приглашения с ответами участников (RSVP) — отдельная модель состояния на участника. Сколько событий на экране в худшем случае. Нужен ли офлайн и работает ли календарь на телефоне, где недельная сетка физически не помещается.</p>

<h4>Ключевые решения</h4>
<p><strong>Самое сложное здесь — не сетка, а модель времени.</strong> Точечное событие (созвон в конкретный момент) хранится как UTC-инстант. Но событие, привязанное к локальному времени («планёрка в 10:00 по Москве»), нужно хранить как локальное время плюс IANA zone id: <code>2026-03-12T10:00, Europe/Moscow</code>. Если сохранить его как UTC, то отмена или введение перехода на летнее время сдвинет встречу на час — и это реально происходит, правила таймзон меняются несколько раз в год. Отдельный третий тип — событие на весь день: это календарная дата без времени и без зоны, её нельзя превращать в инстант.</p>
<p><strong>Повторы описываем RRULE</strong> (RFC 5545): правило плюс <code>EXDATE</code> для исключений плюс переопределения конкретных вхождений (<code>RECURRENCE-ID</code>). Разворачивать серию в список нельзя — она бесконечна. Раскрываем только на видимый диапазон и кешируем по окну (месяц) с ключом «серия + окно + версия серии».</p>
<p><strong>Раскладка.</strong> Дневная и недельная сетка — интервальная задача: сортируем по началу, кластеризуем пересекающиеся, внутри кластера считаем колонки жадным алгоритмом и делим ширину. Месяц/неделя/день — три проекции одной модели, а не три компонента с копипастой.</p>
<p><strong>Транспорт и кеш.</strong> Запрос всегда по окну <code>from/to</code> с префетчем соседних окон при навигации; сервер возвращает и обычные события, и развёрнутые вхождения серий за это окно.</p>

<h4>Компромиссы</h4>
<p>Разворачивать серии на сервере — единообразно и дешевле для клиента, но каждый скролл по календарю становится запросом; разворачивать на клиенте — мгновенная навигация ценой сложной библиотеки RRULE в бандле и риска расхождения с сервером. Обычно берут гибрид: сервер отдаёт правило и вхождения на запрошенное окно, клиент умеет достроить соседние. Хранить локальное время безопаснее для регулярных встреч, но ломает сортировку «что ближайшее» без пересчёта в инстант — приходится держать денормализованное поле <code>startUtc</code> для запросов и пересчитывать его при обновлении tzdata.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Редактирование серии — это три разные операции: «только это вхождение», «это и последующие» (сплит серии на две), «вся серия». Кандидаты делают одну и ломают чужие исключения.</li>
<li>Несуществующее и удвоенное время: 2:30 в ночь перехода на летнее время не существует, а осенью бывает дважды. Библиотека обязана иметь явную политику (<code>disambiguation</code>), а не падать.</li>
<li>Конфликт бронирования: клиентская проверка — только подсказка, реальная проверка на сервере в транзакции, иначе двое забронируют одну переговорку.</li>
<li>Ручная арифметика с миллисекундами: «плюс сутки» это не <code>+86400000</code>, в дни перехода сутки длятся 23 или 25 часов. Только <code>Temporal</code> или date-fns-tz.</li>
<li>Обновление tzdata: правила меняются, и закешированные UTC-значения устаревают. Нужен фоновый пересчёт.</li>
<li>Доступность: сетка календаря должна быть навигабельна стрелками с ролью grid, событие — фокусируемым элементом с внятным accessible name, включая дату и время.</li>
<li>Часовой пояс отображения: показываем в зоне пользователя, но при разнице поясов явно подписываем («10:00 ваше время, 18:00 у участника»).</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Что будет с событием, если в стране отменят перевод часов?»</strong> — если хранили zone id и локальное время, встреча останется в 10:00 утра; если хранили UTC, она уедет. <strong>«Как сделать поиск свободного слота на пятерых?»</strong> — сервер считает объединение занятости (free/busy) и возвращает свободные интервалы; на клиенте это только визуализация. <strong>«Как синхронизировать с Google Calendar?»</strong> — инкрементальный sync token, вебхуки на изменения, дедупликация по iCalUID.</p>`,
    code: `// три типа времени в одной модели — их нельзя смешивать
type EventTime =
  | { kind: 'instant'; startUtc: string; endUtc: string }        // созвон
  | { kind: 'zoned'; start: string; end: string; tz: string }    // 'Europe/Moscow'
  | { kind: 'allDay'; date: string; days: number };              // '2026-03-12'

// повтор: правило + исключения + переопределения отдельных вхождений
const series = {
  id: 'ev-42',
  time: { kind: 'zoned', start: '2026-03-02T10:00', end: '2026-03-02T10:30', tz: 'Europe/Moscow' },
  rrule: 'FREQ=WEEKLY;BYDAY=MO,WE;UNTIL=20261231T000000Z',
  exdate: ['2026-05-11T10:00'],                     // отменённое вхождение
  overrides: { '2026-05-13T10:00': { start: '2026-05-13T11:00' } },
};

// раскрытие только на видимое окно, никогда не «всю серию»
const occurrences = expand(series, { from: viewStart, to: viewEnd });

// корректная арифметика: сутки в день перехода не равны 24 часам
const next = Temporal.ZonedDateTime
  .from({ ...parts, timeZone: series.time.tz })
  .add({ days: 1 });                                 // а не +86400000 мс`,
    tip: 'Заготовь ответ на классическую ловушку: событие создали в зоне, где потом отменили переход на летнее время. Правильный ответ — «поэтому мы храним zone id и локальное время, а не смещение; смещение вычисляется на момент отображения».' },

  { id: 'sdx10',
    q: 'Спроектируй карту с сотнями тысяч маркеров.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Точки статичные или движутся в реальном времени (такси, доставка) — это разные архитектуры: во втором случае кластеризация каждый кадр невозможна. Сколько точек в видимой области в худшем случае, а не всего в базе: 300 тысяч по стране и 300 тысяч в одном квартале — разные задачи. Нужны ли фильтры по атрибутам с мгновенным откликом. Нужны ли произвольные полигоны и геометрия помимо точек. Обязателен ли клик по конкретному объекту с попапом. Нужен ли офлайн или работа на слабых Android. И организационное: своя карта или сторонний провайдер с лимитом на тайлы и на MAU — это влияет на стоимость архитектуры не меньше, чем производительность.</p>

<h4>Ключевые решения</h4>
<p><strong>Подложка — тайлы.</strong> Растровые или векторные тайлы по схеме z/x/y, которые браузер и CDN кешируют сами. Векторные (MapLibre) дороже по CPU, но дают ретину, поворот, смену стиля без перезагрузки и локализованные подписи.</p>
<p><strong>Свои данные — тоже тайлы.</strong> Отдаём MVT (Mapbox Vector Tiles) по тем же z/x/y либо запросами по bbox с квантованием границ по сетке. Квантование критично: без него каждое микродвижение карты даёт уникальный bbox и нулевой hit rate кеша.</p>
<p><strong>Кластеризация.</strong> До сотни тысяч точек хорошо работает клиентский supercluster: строит иерархию кластеров по всем зумам один раз, в Web Worker, потом отдаёт готовый набор за миллисекунды. Выше объёма — кластеризация на сервере или предрасчёт тайлов агрегатов, тогда клиент вообще не видит сырых точек на далёких зумах.</p>
<p><strong>Рендер — WebGL-слой</strong> (deck.gl или symbol layer MapLibre), а не DOM-маркеры. Тысяча абсолютно позиционированных div-ов означает тысячу пересчётов позиции на каждый кадр pan — это гарантированные фризы.</p>
<p><strong>Детализация по зуму:</strong> на далёком зуме только агрегаты и счётчики, на близком — реальные объекты с атрибутами. Атрибуты для попапа догружаем по клику, а не кладём в тайл.</p>

<h4>Компромиссы</h4>
<p>Клиентская кластеризация даёт мгновенную реакцию на фильтры (пересчёт локальный, без сети), но требует загрузить весь набор точек — это мегабайты и секунды на старте. Серверная кластеризация стартует мгновенно и масштабируется на миллионы, но каждый фильтр — раунд-трип, и «поиграть» с данными уже не получится. Граница проходит примерно на 100-200 тысячах точек и на требовании к отклику фильтра. WebGL быстрый, но теряет доступность, выделение и простую стилизацию через CSS — часть UI (подписи, попапы) всё равно остаётся в DOM, и их синхронизация с камерой стоит отдельных усилий.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Бюджет кадра: 60 fps при pan означает не более 8-10 мс на всю работу за кадр. Отсюда и воркер, и предрасчёт — это не украшение, а следствие бюджета.</li>
<li>Гонки при быстром движении карты: ответы по bbox приходят вразнобой и рисуют устаревшие точки. Нужна отмена и версия вьюпорта в запросе.</li>
<li>Совпадающие координаты: десять объектов в одной точке не разворачиваются кластером — нужен spiderfy, иначе часть данных недоступна навсегда.</li>
<li>Антимеридиан и полюса: bbox через 180-й меридиан ломает наивные запросы; проекция Web Mercator не работает выше 85 градусов.</li>
<li>Доступность: карта обязана иметь текстовую альтернативу — список объектов в вьюпорте с клавиатурной навигацией. Без неё раздел недоступен целиком.</li>
<li>Мобильные жесты: одним пальцем страница должна скроллиться, карта — двумя; иначе пользователь застревает в карте на весь экран.</li>
<li>Память: тайлы и буферы WebGL не освобождаются сами — нужен лимит кеша тайлов и уничтожение слоя при уходе со страницы.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«А если точки движутся раз в секунду?»</strong> — отказ от перекластеризации: фиксируем кластеры по геосетке (H3/geohash) и обновляем только счётчики и позиции внутри; сами объекты рисуем одним буфером с обновлением атрибутов, а не пересозданием слоя. <strong>«Как сделать фильтр по атрибуту мгновенным?»</strong> — держим атрибуты в типизированных массивах и фильтруем на GPU через data-driven styling, а не пересобираем набор в JS. <strong>«Как выбрать между сервером и клиентом?»</strong> — по числу точек в вьюпорте и требуемому отклику фильтра, и это надо назвать числом.</p>`,
    code: `// кластеризация в воркере: главный поток только рисует
// worker.js
import Supercluster from 'supercluster';
const index = new Supercluster({ radius: 60, maxZoom: 16 });
index.load(points);                       // один раз, ~100k точек

onmessage = ({ data: { bbox, zoom, viewportId } }) => {
  postMessage({ viewportId, clusters: index.getClusters(bbox, zoom) });
};

// главный поток: квантуем bbox и отбрасываем устаревшие ответы
let viewportId = 0;

map.on('moveend', () => {
  const b = map.getBounds();
  worker.postMessage({
    bbox: quantize(b, map.getZoom()),     // сетка -> попадание в кеш
    zoom: Math.floor(map.getZoom()),
    viewportId: ++viewportId,
  });
});

worker.onmessage = ({ data }) => {
  if (data.viewportId !== viewportId) return;   // ответ устарел
  layer.setProps({ data: data.clusters });
};`,
    tip: 'Назови бюджет вслух: 8 мс на кадр при pan. Из него автоматически следуют и воркер, и WebGL, и предрасчёт кластеров — это превращает ответ из перечня технологий в инженерный расчёт.' },

  { id: 'sdx11',
    q: 'Спроектируй поиск с подсказками и фасетами.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что ищем: товары, документы, людей, всё сразу — от этого зависит, нужна ли федеративная выдача с несколькими источниками. Персонализирована ли выдача и подсказки — это определяет, можно ли кешировать их на edge. Какой бюджет латентности: подсказки в 100 мс и выдача в 500 мс — это разные требования к бэкенду. Нужны ли опечатки, синонимы, морфология (для русского языка это обязательно, и делается на стороне поискового движка). Сколько фасетов и есть ли среди них мультиселект и диапазоны. Важен ли SEO у страницы результатов. И самое практичное: есть ли уже поисковый движок (Elasticsearch, Meilisearch, Algolia) или мы его тоже выбираем.</p>

<h4>Ключевые решения</h4>
<p><strong>Два разных пути, а не один эндпоинт.</strong> <code>suggest</code> — префиксные подсказки, дешёвый индекс, бюджет 50-100 мс, почти без персонализации, а значит кешируется на CDN и на edge. <code>search</code> — полноценная выдача с ранжированием, фасетами и пагинацией, бюджет сотни миллисекунд. Попытка обслужить оба одним запросом заканчивается тем, что подсказки тормозят до скорости выдачи.</p>
<p><strong>Клиентская часть suggest.</strong> Debounce 150-250 мс, отмена устаревших запросов через AbortController и обязательная защита от гонок по монотонному номеру запроса: сеть возвращает ответы вразнобой, и без проверки номера пользователь увидит подсказки к предыдущему префиксу. Плюс локальный LRU-кеш по префиксу (пользователь стирает символ — ответ мгновенный из кеша) и показ истории запросов, пока сеть думает.</p>
<p><strong>Фасеты приходят вместе с выдачей</strong> и содержат counts; для мультиселект-фасета counts считаются без учёта собственного измерения, иначе список схлопывается до выбранного значения.</p>
<p><strong>Состояние — в URL.</strong> Запрос, фильтры, сортировка, страница. Переход по ссылке рендерим на сервере: и для SEO, и ради LCP.</p>
<p><strong>Zero-results — полноценный сценарий,</strong> а не пустой экран: «возможно, вы имели в виду», предложение снять самый узкий фильтр, популярные запросы категории.</p>

<h4>Компромиссы</h4>
<p>Debounce уменьшает нагрузку, но добавляет задержку восприятия. Компромисс: короткий debounce для коротких префиксов (пользователь ещё печатает) и мгновенный запрос при вставке из буфера или нажатии Enter. Кеширование suggest на edge даёт десятки миллисекунд, но исключает персонализацию — если продукт требует «мои недавние» в подсказках, их подмешивают на клиенте из локальной истории, оставляя серверную часть кешируемой. Поиск на клиенте (MiniSearch по загруженному индексу) даёт нулевую латентность, но применим только к небольшим и статичным наборам — для каталога на миллион позиций это не вариант.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Гонки: главный практический баг поиска. Отмена запроса не гарантирует, что ответ не придёт — нужна проверка номера.</li>
<li>Пустой запрос: при очистке инпута надо отменить полёт и показать историю, а не оставить подсказки от предыдущего слова.</li>
<li>Композиционный ввод (IME, японский/китайский, диктовка): запросы во время <code>compositionstart</code>/<code>compositionend</code> шлют мусор. Ждём завершения композиции.</li>
<li>Доступность: это паттерн combobox по WAI-ARIA — <code>role="combobox"</code>, <code>aria-expanded</code>, <code>aria-activedescendant</code>, список с <code>role="listbox"</code>, навигация стрелками, Escape закрывает, количество результатов объявляется в live region.</li>
<li>Мобильная клавиатура: подсказки перекрываются клавиатурой, если не учитывать visualViewport.</li>
<li>Аналитика поиска (запрос, позиция клика, zero-results, отказ) — это сырьё для улучшения ранжирования. Если её не заложить сразу, качество поиска потом невозможно улучшать осмысленно.</li>
<li>Подсветка совпадений должна быть на санитизированном тексте: наивная вставка <code>&lt;mark&gt;</code> в HTML из индекса — это XSS.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как ускорить подсказки до 30 мс?»</strong> — префиксный индекс на edge (Cloudflare Workers KV или аналог) с обновлением по расписанию, плюс локальный кеш и оптимистичное дополнение по уже введённому префиксу. <strong>«Как измерить качество поиска?»</strong> — CTR на первой позиции, доля zero-results, доля сессий с переформулировкой запроса, конверсия из поиска. <strong>«Как сделать поиск по нескольким источникам?»</strong> — параллельные запросы с общим таймаутом и отрисовкой по мере готовности, а не ожидание самого медленного.</p>`,
    code: `// защита от гонок: отмена недостаточна, нужен номер запроса
let seq = 0;
let ctrl = null;

async function onInput(value) {
  ctrl?.abort();
  ctrl = new AbortController();
  const my = ++seq;

  const cached = lru.get(value);
  if (cached) return render(cached);          // стёрли символ -> мгновенно

  try {
    const res = await fetch('/api/suggest?q=' + encodeURIComponent(value), {
      signal: ctrl.signal,
    });
    const data = await res.json();
    if (my !== seq) return;                   // пришёл ответ на старый префикс
    lru.set(value, data);
    render(data);
  } catch (e) {
    if (e.name !== 'AbortError') renderFallback();
  }
}

// не шлём запросы посреди композиции IME
input.addEventListener('compositionstart', () => (composing = true));
input.addEventListener('compositionend', (e) => {
  composing = false;
  onInput(e.target.value);
});
input.addEventListener('input', (e) => {
  if (!composing) debounced(e.target.value);
});`,
    tip: 'Сильная фраза: «подсказки почти не персонализированы, поэтому их можно отдавать с edge и кешировать, а выдачу — нет». Она показывает, что ты делишь систему по свойствам данных, а не по слоям кода.' },

  { id: 'sdx12',
    q: 'Спроектируй ленту сторис (Stories).',
    a: `<h4>Уточняющие вопросы</h4>
<p>Только картинки или видео тоже — видео меняет и префетч, и работу с памятью, и autoplay-политику. Сколько авторов в ленте и сколько слайдов у автора в среднем и в максимуме. Нужны ли интерактивные элементы (опросы, реакции, ответы) — это добавляет мутации прямо во время воспроизведения. Синхронизируется ли прогресс просмотра между устройствами. Есть ли эфемерность (24 часа) и что показывать, когда сторис протухла посреди просмотра. Основная платформа — мобильный веб (тогда жесты и экономия трафика первичны) или десктоп. Какая доля пользователей на 3G и на слабых устройствах: от этого зависит агрессивность префетча.</p>

<h4>Ключевые решения</h4>
<p><strong>Суть задачи — баланс между агрессивным префетчем и жёстким контролем памяти.</strong> Модель: список авторов, у каждого упорядоченный набор слайдов и указатель на последний просмотренный; прогресс синхронизируется с сервером, чтобы на другом устройстве лента продолжилась с нужного места.</p>
<p><strong>Префетч.</strong> Грузим следующие 1-2 слайда текущего автора и первый слайд следующего автора — этого достаточно, чтобы переход был мгновенным, и не настолько много, чтобы съесть трафик на историях, которые пользователь пролистает. Уже просмотренное выгружаем: держим в DOM не больше 3-5 слайдов и переиспользуем элементы, как recycler view.</p>
<p><strong>Медиа.</strong> Картинки — заранее заданный размер и <code>decode()</code> перед показом, чтобы декодирование не попало в кадр перехода. Видео — короткие MP4, <code>preload="metadata"</code> по умолчанию и переключение на <code>auto</code> только для ближайших слайдов; обязательно <code>muted</code> и <code>playsInline</code>, иначе на iOS будет полноэкранный нативный плеер.</p>
<p><strong>Плеер — явный конечный автомат:</strong> loading → playing → paused (удержание пальца) → ended, плюс error. Таймер прогресса на <code>requestAnimationFrame</code> с расчётом по <code>performance.now()</code>, а не <code>setInterval</code>: интервалы тротлятся в фоне и накапливают дрейф, из-за чего полоска рассинхронится с видео.</p>
<p><strong>Жесты</strong> на Pointer Events с порогами: тап в левую/правую треть — слайд, свайп вниз — закрыть, горизонтальный свайп — следующий автор, удержание — пауза. Переходы между авторами — только GPU-трансформы.</p>

<h4>Компромиссы</h4>
<p>Глубокий префетч даёт идеальную плавность и жжёт трафик пользователю, который смотрит одну сторис из десяти; мелкий — экономит трафик ценой видимой загрузки. Разумный компромисс — адаптивная глубина по <code>navigator.connection</code> и по поведению (быстро листает — префетчим первые слайды авторов, а не следующие слайды текущего). Переиспользование DOM-элементов экономит память, но усложняет анимации и обнуление состояния видео — надо явно сбрасывать <code>currentTime</code> и снимать обработчики, иначе получите чужой звук на новом слайде.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Медленную сеть: слайд не загрузился за секунду — нужен спиннер поверх, пауза таймера и продолжение после загрузки, а не проматывание пустого экрана.</li>
<li>Ошибку загрузки: битый слайд надо пропускать со сдвигом прогресса, а не вешать просмотр.</li>
<li>Autoplay-политику: промис от <code>play()</code> отклоняется, если браузер не считает жест достаточным. Обязательна обработка и кнопка.</li>
<li>Фоновую вкладку: по <code>visibilitychange</code> ставим на паузу и не засчитываем просмотр — иначе аналитика показывает просмотры, которых не было.</li>
<li>Гонки: быстрое пролистывание порождает отменённые загрузки и запоздалые <code>play()</code> на уже покинутом слайде. Нужен токен активного слайда.</li>
<li>Память: несколько десятков декодированных картинок на мобильном приводят к перезагрузке вкладки без единой ошибки в консоли.</li>
<li>Доступность: сторис — таймерный контент, обязательны пауза, управление с клавиатуры и текстовая альтернатива; для видео — субтитры.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как посчитать просмотр честно?»</strong> — событие шлём после N секунд активного показа при видимой вкладке, батчим и отправляем через <code>sendBeacon</code>; дедупликация по (userId, slideId) на сервере. <strong>«Как сделать плавный переход между авторами?»</strong> — держим смонтированным первый слайд соседнего автора и анимируем контейнеры трансформом; никакой перемонтировки во время анимации. <strong>«Что делать при 200 сторис в ленте?»</strong> — виртуализируем не только слайды, но и самих авторов, храня только окно вокруг текущего.</p>`,
    code: `// таймер прогресса на rAF: setInterval даёт дрейф и тротлится в фоне
function runSlide(durationMs, onDone) {
  let start = performance.now();
  let paused = false;
  let elapsed = 0;
  let raf = 0;

  const tick = (now) => {
    if (!paused) {
      elapsed += now - start;
      setProgress(Math.min(elapsed / durationMs, 1));
      if (elapsed >= durationMs) return onDone();
    }
    start = now;
    raf = requestAnimationFrame(tick);
  };
  raf = requestAnimationFrame(tick);

  return {
    pause: () => (paused = true),               // палец на экране
    resume: () => (paused = false),
    dispose: () => cancelAnimationFrame(raf),
  };
}

// токен активного слайда: защита от запоздалых загрузок при быстром листании
let activeToken = 0;
async function show(slide) {
  const my = ++activeToken;
  const img = new Image();
  img.src = slide.url;
  await img.decode();                            // декодируем до вставки
  if (my !== activeToken) return;                // пользователь уже ушёл дальше
  mount(img);
}

// адаптивная глубина префетча
const conn = navigator.connection || {};
const depth = conn.saveData || conn.effectiveType === '2g' ? 0 : 2;`,
    tip: 'Назови конкретный лимит: не больше 3-5 слайдов в DOM и не больше 2 предзагруженных вперёд, с переиспользованием элементов. Числа показывают, что ты думал про память мобильного устройства, а не только про красивую анимацию.' },

  { id: 'sdx13',
    q: 'Спроектируй систему уведомлений: push, in-app и realtime.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Какие типы уведомлений и какие из них критичны: «вам ответили» можно потерять, «платёж отклонён» — нет, и это определяет уровень гарантий. Нужны ли уведомления при закрытом браузере (тогда обязателен Web Push с Service Worker) или достаточно активной вкладки. Сколько уведомлений в пике на пользователя — если это соцсеть с лайками, нужна серверная группировка. Нужны ли настройки каналов, тихие часы, дайджесты — это влияет на схему данных, а не только на экран настроек. Есть ли мобильные приложения, с которыми надо согласовать состояние прочитанности. Какие требования к приватности: текст уведомления на заблокированном экране может раскрыть переписку.</p>

<h4>Ключевые решения</h4>
<p><strong>Три канала с разными гарантиями, и это надо развести явно.</strong> <strong>In-app</strong> (колокольчик, лента) — источник истины: REST с курсорной пагинацией и счётчиком непрочитанных. <strong>Realtime</strong> (WebSocket или SSE) — транспорт сигнала «есть новое», по которому клиент инвалидирует кеш; тело уведомления по сокету слать можно, но полагаться на доставку нельзя. <strong>Web Push</strong> — для закрытой вкладки: подписка с VAPID-ключом, доставка через push-сервис браузера, обработка в Service Worker.</p>
<p><strong>Главный инвариант:</strong> сокет ничего не гарантирует. При каждом реконнекте клиент запрашивает дельту по <code>lastSeenId</code> или курсору и не надеется, что за время разрыва ничего не произошло. Это же решает проблему пропущенных событий при переходе устройства в сон.</p>
<p><strong>Дедупликация по id уведомления</strong> обязательна: одно событие может прийти и пушем, и по сокету, и в дельте после реконнекта.</p>
<p><strong>Счётчик непрочитанных</strong> считает сервер, клиент только отображает. Синхронизация между вкладками — BroadcastChannel, иначе в одной вкладке прочитали, а в другой значок висит.</p>
<p><strong>Разрешение на пуш</strong> просим не при входе, а после ценностного действия и с предварительным объяснением: отказ в браузере необратим без ручных действий пользователя в настройках сайта.</p>

<h4>Компромиссы</h4>
<p>WebSocket даёт двунаправленность и низкую задержку, но требует держать соединение, переживать прокси и масштабировать stateful-слой. SSE проще, переподключается сам, работает через любой HTTP-прокси, но односторонний и ограничен числом соединений на домен в HTTP/1.1. Для «сигнала о новом» SSE обычно достаточно, и это правильный дефолт. Полное тело уведомления в push удобно, но означает, что содержимое проходит через push-сервис третьей стороны — для чувствительных данных шлём только id и подтягиваем текст в Service Worker уже с нашего API. Агрессивная группировка на сервере снижает шум и задерживает доставку — для критичных типов группировку выключаем.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Реконнект без backoff: после падения бэкенда все клиенты возвращаются одновременно и добивают его. Нужен экспоненциальный backoff с джиттером.</li>
<li>Гонку «прочитал локально, а сервер ещё не знает»: оптимистичное обнуление счётчика с последующей дельтой возвращает его обратно. Нужен номер версии счётчика.</li>
<li>Ошибку подписки на push: подписка протухает (браузер её отзывает), и сервер копит мёртвые эндпоинты. Нужна обработка 410 Gone и <code>pushsubscriptionchange</code>.</li>
<li>Офлайн: уведомления, пришедшие офлайн, должны появиться после ресинка, а не потеряться.</li>
<li>Доступность: появление уведомления объявляется через <code>aria-live="polite"</code>, а не только тостом, который скринридер не заметит.</li>
<li>Шторм на масштабе: у популярного автора миллион подписчиков — фанаут делает бэкенд, а клиент обязан выдержать «100 новых» без перерисовки всего списка.</li>
<li>iOS Web Push исторически был ограничен и требует установленного PWA — деградацию надо планировать заранее.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как не показать уведомление дважды, если открыто три вкладки?»</strong> — Service Worker показывает системное уведомление только если ни один клиент не виден (<code>clients.matchAll</code> с проверкой <code>visibilityState</code>). <strong>«Как сделать бейдж на иконке?»</strong> — Badging API как прогрессивное улучшение с фоллбэком на title вкладки. <strong>«Как гарантировать доставку критичного уведомления?»</strong> — никак на одном канале: комбинируем push, email и in-app с подтверждением прочтения.</p>`,
    code: `// Service Worker: не дублируем уведомление, если вкладка на экране
self.addEventListener('push', (event) => {
  event.waitUntil((async () => {
    const payload = event.data.json();           // { id, type, preview }
    const clients = await self.clients.matchAll({ type: 'window' });
    const visible = clients.some((c) => c.visibilityState === 'visible');

    if (visible) {
      clients.forEach((c) => c.postMessage({ type: 'notification', payload }));
      return;                                     // покажет сама страница
    }
    await self.registration.showNotification(payload.title, {
      body: payload.preview,
      tag: payload.id,                            // тот же tag -> замена, не дубль
      data: { url: '/n/' + payload.id },
    });
  })());
});

// подписка протухла — чистим её на сервере
self.addEventListener('pushsubscriptionchange', (event) => {
  event.waitUntil(resubscribeAndSync());
});

// после реконнекта всегда добираем пропущенное
socket.onopen = async () => {
  const { items, unread } = await api.get('/notifications/delta', {
    after: lastSeenId,
  });
  store.mergeById(items);                         // дедуп по id
  store.setUnread(unread);                        // счётчик считает сервер
};`,
    tip: 'Ключевая формулировка: «сокет — это оптимизация, а не гарантия доставки». Любой realtime-дизайн, где клиент верит, что не пропустил событие, разваливается на первом же разрыве связи в метро.' },

  { id: 'sdx14',
    q: 'Спроектируй realtime-мультиплеер в браузере. Как бороться с задержкой сети?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Какой жанр и какая чувствительность к задержке: пошаговая игра терпит 500 мс, шутер — нет. Сколько игроков в комнате: 2, 8 или 100 (последнее меняет всё — нужен interest management, чтобы не рассылать всем состояние всего мира). Соревновательная игра с рейтингом или кооперативная — от этого зависит, нужен ли античит и авторитетный сервер вообще. Целевые платформы и сети: мобильный LTE даёт 60-150 мс RTT и джиттер. Нужна ли запись и реплей матчей. Есть ли требования к стоимости серверов — авторитетная симуляция для тысяч комнат стоит денег, и иногда правильный ответ «host-authoritative у одного из игроков».</p>

<h4>Ключевые решения</h4>
<p><strong>Сервер — авторитетный симулятор, клиент — предсказатель.</strong> Три классические техники работают вместе. <strong>Client-side prediction:</strong> свой ввод применяем локально мгновенно, не дожидаясь сервера, иначе управление ощущается «резиновым». <strong>Server reconciliation:</strong> сервер шлёт состояние с номером последнего обработанного ввода, клиент откатывает своё состояние к нему и переигрывает все ещё не подтверждённые инпуты из буфера. <strong>Entity interpolation:</strong> чужие объекты рисуем в прошлом, с задержкой около 100 мс, интерполируя между двумя полученными снапшотами — это убирает дёрганье при джиттере.</p>
<p><strong>Транспорт.</strong> WebRTC DataChannel в режиме unreliable/unordered (<code>maxRetransmits: 0</code>, <code>ordered: false</code>) для тиков состояния: потерять один тик нормально, следующий придёт через 33 мс, а ретрансмиссия TCP только добавит задержку и head-of-line blocking. WebSocket оставляем для матчмейкинга, чата и надёжных событий (старт раунда, результат).</p>
<p><strong>Формат — бинарный:</strong> типизированные массивы с дельта-кодированием от последнего подтверждённого снапшота и квантованием координат. JSON на 30 Гц для 16 игроков — это мегабайты в минуту на клиента.</p>
<p><strong>Разводим петли:</strong> симуляция с фиксированным шагом (20-30 Гц) и рендер на 60 fps с интерполяцией между шагами. Смешивать их — гарантированно получить разное поведение физики на разных мониторах.</p>

<h4>Компромиссы</h4>
<p>Предсказание убирает ощущение задержки, но порождает рассинхрон и видимые коррекции («рывки»), когда предсказание не совпало. Чем агрессивнее предсказание, тем заметнее откаты. Интерполяция даёт плавность ценой того, что вы видите противников на 100 мс в прошлом; экстраполяция убирает эту задержку, но при ошибке телепортирует персонажей — поэтому её ограничивают 2-3 тиками. Lag compensation на сервере делает стрельбу честной для стреляющего и нечестной для убегающего («меня убили за углом») — это фундаментальный размен, у которого нет правильного ответа, только настройка. P2P дешевле по серверам, но отдаёт авторитет клиенту и открывает читы.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Античит: клиент никогда не сообщает «я попал» — он сообщает ввод, попадания считает сервер. Любая архитектура, где результат приходит от клиента, ломается в первый же день.</li>
<li>Валидацию ввода: сервер должен проверять, что игрок физически мог так двигаться, иначе speed hack тривиален.</li>
<li>Обработку плохой сети: индикатор качества соединения, деградация до интерполяции без предсказания, корректный выход при разрыве вместо зависшего персонажа.</li>
<li>Вкладку в фоне: rAF останавливается, симуляция копит долг. Нужен clamp на максимальный шаг и ресинк при возврате.</li>
<li>Переподключение в середине матча: state snapshot на входе, а не «начни сначала».</li>
<li>Interest management: рассылать всем состояние всех — квадратичный рост трафика; шлём только то, что в зоне видимости игрока.</li>
<li>Часы: клиентское время нельзя использовать для логики, нужна синхронизация с серверным временем и оценка RTT.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать честную стрельбу при 150 мс?»</strong> — lag compensation: сервер хранит историю позиций и перематывает мир на момент выстрела с учётом RTT и интерполяционной задержки конкретного игрока. <strong>«Как уменьшить трафик?»</strong> — дельта-кодирование, квантование, приоритизация сущностей по расстоянию и отправка дальних реже. <strong>«Почему не WebSocket для всего?»</strong> — TCP гарантирует порядок, а значит потеря одного пакета задерживает все последующие; для устаревающих данных это худший вариант.</p>`,
    code: `// клиент: предсказание + буфер неподтверждённых вводов
const pending = [];
let seq = 0;

function onInput(input) {
  const cmd = { seq: ++seq, dt: FIXED_DT, input };
  pending.push(cmd);
  applyInput(localState, cmd);          // предсказали сразу
  channel.send(encode(cmd));            // unreliable: потеря допустима
}

// реконсиляция: откат к серверному состоянию и переигрывание своих вводов
function onServerSnapshot(snap) {
  localState = snap.state;              // авторитетно
  while (pending.length && pending[0].seq <= snap.lastProcessedSeq) {
    pending.shift();                    // сервер это уже учёл
  }
  for (const cmd of pending) applyInput(localState, cmd);  // replay
}

// чужие сущности рисуем в прошлом: плавность важнее свежести
const renderTime = serverClock.now() - INTERPOLATION_DELAY_MS; // ~100
const [a, b] = snapshotBuffer.around(renderTime);
draw(lerpEntities(a, b, (renderTime - a.t) / (b.t - a.t)));

// транспорт для тиков состояния
const channel = pc.createDataChannel('state', {
  ordered: false,
  maxRetransmits: 0,
});`,
    tip: 'Формулировка, которая закрывает сразу латентность и античит: «клиент отправляет намерение, сервер решает результат». Добавь про lag compensation честно: она делает игру справедливой для стреляющего и несправедливой для убегающего, и это осознанный выбор, а не баг.' },

  { id: 'sdx15',
    q: 'Спроектируй форму из 50+ полей с автосохранением.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Форма статичная или её состав приходит с сервера (анкета, конструктор) — во втором случае нужна схема, а не JSX. Есть ли зависимости между полями: видимость, вычисляемые значения, каскадные справочники — это главный источник сложности. Один пользователь или несколько правят одну заявку. Что означает автосохранение для бизнеса: черновик, который можно бросить, или юридически значимый документ с версиями. Нужна ли работа офлайн. Насколько тяжёлая валидация: локальная по схеме или с серверными проверками (ИНН, адрес, доступность имени). Кто заполняет форму: оператор с клавиатурой сотни раз в день (тогда клавиатурная эргономика важнее всего) или клиент один раз.</p>

<h4>Ключевые решения</h4>
<p><strong>Изоляция ререндеров — первое и главное.</strong> Контролируемая форма с одним объектом состояния перерисовывает все 50 полей на каждое нажатие клавиши; на слабом ноутбуке это заметное залипание ввода. Решение: неконтролируемые поля с подписочной моделью (react-hook-form с регистрацией через ref) либо стор с подпиской по конкретному полю. Валидация схемой (zod) — на blur и на submit, на каждый символ валидируем только там, где это действительно нужно (длина пароля).</p>
<p><strong>Структура — декларативная схема.</strong> Поля, типы, видимость, зависимости и валидаторы описываются данными, а рендерер разворачивает их в компоненты. Это то, что превращает 50 захардкоженных блоков в поддерживаемую систему и открывает серверную конфигурацию формы.</p>
<p><strong>Автосохранение.</strong> Debounce 1-2 секунды плюс принудительное сохранение на blur поля, на <code>visibilitychange</code> и на <code>pagehide</code>. Шлём патч изменённых полей с версией черновика, а не всю форму — иначе два открытых таба затирают правки друг друга целиком.</p>
<p><strong>Локальный черновик в IndexedDB</strong> как страховка от закрытия вкладки и офлайна; при возврате предлагаем восстановить с явным списком расхождений, а не молча подменяем данные.</p>
<p><strong>Длинная форма — шаги</strong> с прогрессом, номером шага в URL и валидацией по шагу; «всё сразу в конце» гарантирует, что пользователь бросит форму на экране с двадцатью красными полями.</p>

<h4>Компромиссы</h4>
<p>Неконтролируемые поля решают производительность, но усложняют вычисляемые значения и кросс-полевые зависимости: приходится подписываться на конкретные поля через watch, и легко случайно подписаться на всю форму. Схема-драйвен форма даёт гибкость и отнимает типобезопасность и простоту отладки — стек-трейс идёт через рендерер, а не через ваш JSX. Автосохранение по патчам эффективно, но требует серверной поддержки merge и версий; сохранять целиком проще, но конфликтует на нескольких вкладках. Если полей 50, но они простые и форма одна — контролируемая форма с мемоизацией полей может оказаться дешевле по стоимости владения, и это честный ответ.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Измеримый критерий: input latency не выше 50 мс на символ. Без него разговор скатывается в «сделаем оптимизацию».</li>
<li>Гонки автосохранения: два патча в полёте, второй приходит первым. Нужна очередь по форме и версия черновика.</li>
<li>Автозаполнение браузера и менеджеры паролей: правильные <code>name</code> и <code>autocomplete</code> обязательны, иначе оператор потеряет часы на ручном вводе.</li>
<li>Восстановление после ошибки сети: «не сохранено» должно быть видно постоянно, а не одним исчезнувшим тостом.</li>
<li>Доступность: <code>aria-invalid</code>, <code>aria-describedby</code> на сообщение об ошибке, сводка ошибок сверху со ссылками на поля и перевод фокуса на первое невалидное. Это же требование и для скринридера, и для удобства обычного пользователя.</li>
<li>Динамические поля: при скрытии поля его значение надо либо удалять, либо явно сохранять — иначе на сервер уедут данные из скрытой ветки анкеты.</li>
<li>Большие селекты: справочник на 10 тысяч значений нужен виртуализированный и с серверным поиском, иначе он и есть источник тормозов, а не 50 инпутов.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как проверить, что не тормозит?»</strong> — профиль в React DevTools на печать в поле в середине формы: должен перерисовываться один компонент; плюс замер INP в RUM. <strong>«Как сделать зависимые поля без лишних рендеров?»</strong> — точечная подписка на конкретное поле (<code>useWatch</code> с именем), а не на весь объект формы. <strong>«Как быть с конкурентной правкой?»</strong> — версия черновика и показ, какие именно поля разошлись, с выбором пользователя.</p>`,
    code: `// схема как данные: поля, зависимости и валидация в одном месте
const schema = [
  { name: 'company.inn', type: 'text', label: 'ИНН', validate: z.string().length(10) },
  { name: 'company.kpp', type: 'text', label: 'КПП',
    visibleIf: (v) => v['company.type'] === 'legal' },
  { name: 'contact.email', type: 'email', validate: z.string().email() },
];

// автосохранение патчем с версией и очередью
const queue = new SerialQueue();          // по одному патчу за раз

const save = debounce((dirtyFields, values) => {
  const patch = pick(values, Object.keys(dirtyFields));
  queue.push(async () => {
    const res = await api.patch('/drafts/' + draftId, {
      patch,
      version: draft.version,
    });
    if (res.status === 409) return showFieldConflicts(await res.json());
    draft.version = (await res.json()).version;
  });
}, 1500);

// принудительный флаш там, где страница может исчезнуть
addEventListener('pagehide', () => {
  save.flush();
  navigator.sendBeacon('/drafts/' + draftId + '/flush', JSON.stringify(pendingPatch));
});`,
    tip: 'Назови бюджет числом: INP не выше 200 мс и отклик на ввод не выше 50 мс. После этого «изолировать ререндеры» перестаёт быть лозунгом и становится проверяемым требованием, под которое подбирается библиотека форм.' },

  { id: 'sdx16',
    q: 'Спроектируй большую таблицу с инлайн-редактированием ячеек.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько строк и колонок в худшем случае: 5 тысяч на 12 или 2 миллиона на 200 — второе означает виртуализацию по обеим осям и серверное всё. Сколько людей одновременно правят одни и те же строки. Нужны ли групповые операции: вставка диапазона из Excel, заполнение вниз, массовое изменение — это отдельная подсистема, а не «ещё одна кнопка». Нужны ли формулы и вычисляемые колонки. Есть ли экспорт и печать. Кто пользователь: если это операторы, работающие в таблице весь день, клавиатурная модель важнее визуального дизайна. И обязательный вопрос: точно ли нужна таблица, а не редактор с массовым импортом — иногда задача решается загрузкой CSV.</p>

<h4>Ключевые решения</h4>
<p><strong>Три независимых слоя.</strong> <strong>Данные:</strong> серверная пагинация, сортировка и фильтрация; клиент не грузит весь набор. <strong>Рендер:</strong> виртуализация строк, а при широкой таблице — и колонок; строки одинаковой высоты сильно упрощают расчёты, переменные требуют кеша измерений. <strong>Редактирование:</strong> тяжёлый edit-компонент монтируется только на активной ячейке, остальные ячейки — максимально дешёвый вывод текста, часто вообще без компонента-обёртки.</p>
<p><strong>Инлайн-правка оптимистичная на уровне ячейки:</strong> локально применяем значение, помечаем ячейку как pending, отправляем PATCH с id строки, именем поля и <code>version</code> строки. При ошибке откатываем значение и подсвечиваем ячейку с тултипом причины — модальное окно на каждую ошибку в таблице неприемлемо.</p>
<p><strong>Конкурентность — optimistic concurrency:</strong> сервер сравнивает версию и при расхождении возвращает 409 с текущим значением. Показываем оба и даём выбрать; для нечастых конфликтов это дешевле любых блокировок.</p>
<p><strong>Батчинг:</strong> правки группируем окном около 300 мс в один запрос, но сохраняем порядок операций по каждой ячейке, иначе быстрое исправление опечатки применится в обратном порядке.</p>
<p><strong>Клавиатура — не опция.</strong> Tab/Enter/Escape, стрелки между ячейками, F2 на вход в редактирование, Ctrl+C/Ctrl+V для диапазона, Shift+стрелки для выделения. Именно ради этого пользователи выбирают таблицу вместо формы.</p>

<h4>Компромиссы</h4>
<p>Виртуализация даёт работу на миллионах строк и ломает Ctrl+F браузера, печать, выделение текста через весь документ и часть возможностей скринридера. Компенсируем серверным поиском и режимом «показать всё» для небольших выборок. Оптимистичное редактирование ускоряет работу и создаёт момент, когда экран показывает то, чего нет на сервере, — для финансовых данных это может быть недопустимо, и тогда переходим на пессимистичную запись со спиннером в ячейке. Серверная сортировка правильна, но после правки строка может выпасть из текущей выборки: удалять её на глазах нельзя, помечаем и убираем при следующем обновлении.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Гонки: несколько PATCH на одну ячейку и ответы вразнобой. Нужна очередь по ключу (rowId + field) и отбрасывание устаревших ответов.</li>
<li>Undo: массовое редактирование без Ctrl+Z опасно. Нужен стек обратных операций, работающий и по сети (компенсирующий PATCH).</li>
<li>Вставку из Excel: пользователи будут это делать в первый же день. Парсинг TSV из буфера, проверка размерности диапазона, предпросмотр перед применением.</li>
<li>Sticky-заголовки и фиксированные колонки — через <code>position: sticky</code>, а не три синхронизированных скролл-контейнера: второй вариант всегда рассыпается на границе.</li>
<li>Доступность: <code>role="grid"</code>, <code>aria-rowcount</code>/<code>aria-rowindex</code> с реальными индексами (виртуализация врёт про количество строк в DOM), объявление режима редактирования.</li>
<li>Офлайн и потерю сети посреди массовой правки: очередь должна пережить обновление страницы или честно предупредить.</li>
<li>Производительность ячейки: 50 колонок на 40 видимых строк — это 2000 компонентов на кадр; любая мемоизация должна быть проверена профайлером, а не предположена.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать выделение диапазона и заполнение вниз?»</strong> — модель выделения как прямоугольник (anchor + focus) в координатах данных, а не DOM; операция применяется как один батч с одним undo-шагом. <strong>«Что если 200 колонок?»</strong> — горизонтальная виртуализация и заморозка первых колонок; кроме того, стоит спросить, реально ли пользователь смотрит 200 колонок или ему нужен конструктор видов. <strong>«Как показать, что строку правит другой?»</strong> — эфемерное presence по строке, а не блокировка.</p>`,
    code: `// очередь по ячейке: порядок правок одной ячейки обязан сохраняться
const queues = new Map();                       // key -> Promise chain

function editCell(rowId, field, value, version) {
  const key = rowId + ':' + field;
  store.setPending(key, value);                 // оптимистично

  const prev = queues.get(key) ?? Promise.resolve();
  const next = prev.then(async () => {
    const res = await api.patch('/rows/' + rowId, { [field]: value, version });
    if (res.status === 409) {
      const server = await res.json();
      return store.markConflict(key, { mine: value, theirs: server[field] });
    }
    store.commit(key, await res.json());
  });
  queues.set(key, next.catch(() => store.rollback(key)));
  return next;
}

// вставка из Excel: буфер приходит как TSV
grid.addEventListener('paste', (e) => {
  const rows = e.clipboardData.getData('text/plain')
    .split(/\\r?\\n/).filter(Boolean).map((r) => r.split('\\t'));
  if (rows.length * rows[0].length > 5000) return confirmLargePaste(rows);
  applyRangeAsSingleUndoStep(selection.anchor, rows);
});

// виртуализация обязана честно сообщать размеры скринридеру
// <div role="grid" aria-rowcount="248391"> ... <div role="row" aria-rowindex="1042">`,
    tip: 'Проговори, что виртуализация ломает Ctrl+F, печать и часть доступности — и назови компенсацию: серверный поиск с подсветкой и переходом к строке плюс режим экспорта. Кандидаты, которые называют цену виртуализации, выглядят сильно опытнее тех, кто называет её решением.' },

  { id: 'sdx17',
    q: 'Спроектируй редактор кода в браузере (как VS Code Web).',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что за сценарий: подсветка сниппета в документации, полноценная IDE или онлайн-курс с проверкой заданий — это три разных продукта. Нужен ли запуск кода и если да, то где: в браузере (WASM, WebContainer) или в удалённом контейнере. Какие языки и нужен ли полноценный IntelliSense с переходом к определению — это тянет за собой языковой сервер. Какие размеры файлов в худшем случае. Нужна ли работа офлайн. Нужно ли совместное редактирование. Мобильные устройства в списке платформ — если да, Monaco практически отпадает. И организационный вопрос: сколько людей будет это поддерживать, потому что редактор — это надолго.</p>

<h4>Ключевые решения</h4>
<p><strong>Не пишем редактор с нуля.</strong> Берём CodeMirror 6 или Monaco. CodeMirror легче (десятки килобайт против сотен), модульный, нормально работает на мобильных и на тач-вводе. Monaco тяжелее и требовательнее к окружению, но даёт опыт, близкий к VS Code, и готовый IntelliSense. Оба виртуализируют строки: в DOM только видимая область плюс запас, иначе файл на 100 тысяч строк не откроется в принципе.</p>
<p><strong>Тяжёлое — за пределы главного потока.</strong> Инкрементальный парсинг и подсветка (Lezer в CodeMirror, Tree-sitter как альтернатива), линтинг, форматирование и автодополнение уходят в Web Worker. Инкрементальность здесь ключевая: перепарсивать файл целиком на каждое нажатие невозможно, парсер должен переиспользовать неизменённые поддеревья.</p>
<p><strong>Протокол общения с воркером — в стиле LSP.</strong> Это не педантизм: тот же интерфейс потом позволяет подменить локальный воркер на удалённый языковой сервер по WebSocket без переписывания UI.</p>
<p><strong>Файловая система — виртуальная:</strong> OPFS (быстрее, синхронный доступ из воркера) или IndexedDB, с ленивой загрузкой содержимого и отдельным индексом дерева.</p>
<p><strong>Выполнение кода — только в изоляции:</strong> WASM-песочница, WebContainer или удалённый контейнер с лимитами CPU и времени. Никакого <code>eval</code> в контексте приложения.</p>

<h4>Компромиссы</h4>
<p>Готовый редактор экономит годы, но навязывает свою модель расширений и свой вес в бандле; кастомизация за пределами API оборачивается форком. CodeMirror легче и гибче, Monaco мощнее из коробки — выбор определяется тем, что дороже: килобайты и мобильные пользователи или скорость получения IntelliSense. Локальный воркер даёт офлайн и нулевую задержку, но не знает про весь проект и зависимости; удалённый языковой сервер знает всё, но добавляет 50-200 мс и требует инфраструктуры на пользователя. Разумный компромисс — гибрид: локальные символы мгновенно, проектные подсказки асинхронно с дописыванием списка.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Аргументацию «взять готовое»: её надо подкреплять конкретикой — IME для китайского и японского, bidi-текст, составные эмодзи, автоматические отступы, undo с группировкой, доступность. Это годы работы, а не «пара недель на contenteditable».</li>
<li>Большие файлы: выше порога (например, 2 МБ) отключаем подсветку, линт и автодополнение и честно сообщаем об этом, а не отдаём зависший редактор.</li>
<li>Гонки автодополнения: пользователь печатает быстрее, чем отвечает воркер. Нужны отмена, версия документа в запросе и отбрасывание устаревших ответов.</li>
<li>Восстановление после падения воркера: он падает на битом синтаксисе, и редактор должен продолжать работать без подсветки, а не белым экраном.</li>
<li>Несохранённые изменения: автосохранение в OPFS и предупреждение при уходе — иначе теряется работа.</li>
<li>Доступность: <code>role="textbox"</code> с <code>aria-multiline</code>, объявление ошибок линтера, screen-reader mode с отключённой виртуализацией; в обоих редакторах он есть, но его надо включить.</li>
<li>Совместное редактирование прикручивается поверх через CRDT-биндинг (y-codemirror), а не переписыванием редактора — попытка сделать наоборот убивает проект.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Почему инкрементальный парсер, а не регулярки?»</strong> — регулярная подсветка ломается на вложенных конструкциях и шаблонных строках и не даёт дерева для навигации и рефакторинга. <strong>«Как сделать поиск по проекту?»</strong> — индекс в воркере поверх OPFS, а при больших проектах — серверный поиск, потому что тянуть весь репозиторий в браузер бессмысленно. <strong>«Как запускать npm-пакеты в браузере?»</strong> — WebContainer с виртуальной ФС и Node-совместимым рантаймом на WASM; ограничения — нет нативных модулей и сети без прокси.</p>`,
    code: `// протокол в стиле LSP: тот же интерфейс для воркера и для удалённого сервера
interface LanguageService {
  completion(uri: string, pos: Pos, version: number): Promise<Item[]>;
  diagnostics(uri: string, version: number): Promise<Diagnostic[]>;
  format(uri: string, version: number): Promise<TextEdit[]>;
}

// версия документа отсекает устаревшие ответы
let docVersion = 0;
editor.onChange(() => { docVersion++; scheduleDiagnostics(); });

async function requestCompletion(pos) {
  const my = docVersion;
  const items = await service.completion(uri, pos, my);
  if (my !== docVersion) return;          // документ уже изменился
  showCompletions(rank(items));           // локальные символы выше сетевых
}

// деградация вместо белого экрана
worker.addEventListener('error', () => {
  setEditorMode({ highlight: false, lint: false });
  notify('Подсветка временно отключена, редактирование работает');
});

// порог, выше которого тяжёлые фичи выключаются осознанно
const HEAVY_LIMIT = 2 * 1024 * 1024;
if (file.size > HEAVY_LIMIT) setEditorMode({ highlight: false, lint: false });`,
    tip: 'Обосновывай «берём готовый редактор» списком того, что придётся написать самому: IME, bidi, составные эмодзи, undo-группировка, доступность, виртуализация строк. Это переводит решение из «лень» в оценку стоимости, а именно её и проверяют.' },

  { id: 'sdx18',
    q: 'Спроектируй систему комментариев с вложенными ветками.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько комментариев под популярной записью в худшем случае: 50 или 100 тысяч — второе меняет и хранение, и загрузку. Какая глубина вложенности допустима продуктово: две (как в Instagram) или неограниченная (как в Reddit). Нужен ли реалтайм или достаточно обновления по кнопке. Нужны ли SEO и индексация комментариев — тогда нужен серверный рендер верхних веток. Какая сортировка: по времени, по рейтингу, «лучшие» — сортировка по рейтингу с реалтаймом означает, что порядок меняется под пользователем. Есть ли модерация и как показывать удалённое. Нужны ли упоминания, вложения, реакции — каждое добавляет свою подсистему.</p>

<h4>Ключевые решения</h4>
<p><strong>Хранение — плоский список с <code>parent_id</code> плюс materialized path</strong> (<code>path = "1.14.87"</code>). Path даёт возможность одним индексным запросом достать всю ветку по префиксу и сортировать по нему естественным порядком обхода. Рекурсивные запросы по parent_id на глубоких тредах превращаются в CTE, которое база выполняет тем хуже, чем популярнее пост.</p>
<p><strong>Ограничение глубины в UI.</strong> Показываем 3-5 уровней, дальше — «показать ответы» со ссылкой на подтред как на отдельную страницу. Причина не эстетическая: на мобильном каждый уровень отступа съедает ширину, и на седьмом уровне остаётся колонка в два слова.</p>
<p><strong>Загрузка.</strong> Верхний уровень — курсорная пагинация; ответы — лениво по кнопке с заранее показанным счётчиком («12 ответов»). Счётчик — денормализованное поле, обновляемое фоново: <code>COUNT</code> по поддереву на каждый рендер не выдержит нагрузки.</p>
<p><strong>Стор нормализованный:</strong> <code>byId</code> плюс массивы <code>childIds</code>. Тогда правка одного комментария перерисовывает один узел, а не всё дерево.</p>
<p><strong>Отправка оптимистичная</strong> с временным id: комментарий появляется мгновенно, после ответа сервера подменяем id и связи. При ошибке комментарий остаётся на месте с кнопкой «повторить» — терять набранный текст недопустимо.</p>

<h4>Компромиссы</h4>
<p>Materialized path даёт быстрые выборки веток и дорогое перемещение поддерева (нужно переписать path у всех потомков) — но комментарии почти никогда не переносят, так что это выгодный размен. Ограничение глубины упрощает UI и теряет структуру длинных дискуссий; альтернатива — «схлопывание» с сохранением вложенности и переходом в подтред, что и берут в большинстве продуктов. Оптимистичная отправка ускоряет ощущение и создаёт риск дублей при ретрае — лечится клиентским UUID и дедупликацией на сервере. Реалтайм-вставка полезна в живых обсуждениях и вредна при чтении: поэтому вставляем не сразу, а через плашку.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Скачок скролла: новые комментарии, вставленные выше вьюпорта, сдвигают контент под пальцем. Нужна плашка «3 новых» или компенсация скролла через <code>overflow-anchor</code>.</li>
<li>Soft delete: физическое удаление ломает дерево ответов. Удалённый комментарий остаётся плейсхолдером «комментарий удалён».</li>
<li>Безопасность: рендерим только санитизированный markdown-подмножество, ссылки с <code>rel="noopener nofollow ugc"</code>, никакого сырого HTML от пользователя.</li>
<li>Deep link на комментарий из уведомления: сервер должен уметь отдать конкретную ветку по path с контекстом родителей, а не «догрузи 20 страниц».</li>
<li>Гонка «ответил на комментарий, который в этот момент удалили»: нужен внятный ответ сервера и сохранение текста пользователя.</li>
<li>Спам и флуд: rate limit на клиенте — это UX, реальный лимит на сервере; для UI нужно состояние «слишком часто, подождите».</li>
<li>Доступность: дерево комментариев должно быть навигабельно, вложенность выражена структурой (<code>article</code> с <code>aria-label</code> и уровнем), а не только отступами.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как отсортировать по рейтингу и не сойти с ума с реалтаймом?»</strong> — фиксируем порядок на момент загрузки страницы (snapshot ranking) и обновляем его только по явному действию пользователя. <strong>«Как показать 100 тысяч комментариев?»</strong> — виртуализация плюс подгрузка веток по требованию; полное дерево на клиент не грузится никогда. <strong>«Как считать счётчик ответов точно?»</strong> — денормализованное поле, инкремент в той же транзакции, что и вставка, плюс периодическая сверка фоновой задачей.</p>`,
    code: `// materialized path: вся ветка одним индексным запросом по префиксу
// SELECT * FROM comments WHERE path <@ '1.14' ORDER BY path LIMIT 100;

// нормализованный стор: правка узла не трогает дерево
const state = {
  byId: {
    'c14': { id: 'c14', parentId: 'c1', path: '1.14', text: '...', replyCount: 12 },
  },
  childIds: { 'c1': ['c14', 'c15'] },
  rootIds: ['c1', 'c7'],
  pendingByTempId: {},
};

// оптимистичная отправка с клиентским id: ретрай не создаёт дубль
async function submit(parentId, text) {
  const clientId = crypto.randomUUID();
  store.insertOptimistic({ id: clientId, parentId, text, status: 'sending' });
  try {
    const saved = await api.post('/comments', { parentId, text, clientId });
    store.replaceId(clientId, saved);        // подменяем id и связи
  } catch (e) {
    store.markFailed(clientId);              // текст остаётся, есть «повторить»
  }
}

// новые комментарии не вставляем в поток молча
socket.on('comment.created', (c) => {
  if (isNearBottom() && c.parentId === null) store.append(c);
  else store.stashIncoming(c);               // плашка «3 новых комментария»
});`,
    tip: 'Про счётчик ответов отвечай конкретно: денормализованное поле, инкремент в одной транзакции со вставкой, фоновая сверка раз в сутки. Ответ «сделаю COUNT» показывает, что кандидат не думал про популярный пост с сотней тысяч комментариев.' },

  { id: 'sdx19',
    q: 'Спроектируй дизайн-систему как продукт. Что входит в её архитектуру?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько команд-потребителей и на каких стеках: React-only и «React плюс Vue плюс мобильные» — принципиально разные архитектуры (во втором случае общими могут быть только токены). Есть ли выделенная команда на дизайн-систему или это работа по остаточному принципу — от этого зависит, что реалистично обещать. Есть ли легаси-приложения, которые надо поддерживать одновременно с новыми. Нужна ли темизация: одна тема, светлая/тёмная или white-label под клиентов — последнее меняет архитектуру токенов целиком. Кто владеет дизайном и как часто меняется визуальный язык. И главное: какую проблему решаем — скорость разработки, консистентность, доступность или ребрендинг. Ответ определяет приоритеты.</p>

<h4>Ключевые решения</h4>
<p><strong>Дизайн-система — продукт с пользователями, релизами и SLA,</strong> а не папка с компонентами. Отсюда следует всё остальное.</p>
<p><strong>Слои:</strong> design tokens (JSON в формате DTCG) → примитивы → составные паттерны → шаблоны. Токены собираются Style Dictionary в CSS-переменные, TS-типы и нативные форматы для iOS/Android — один источник правды для всех платформ. Внутри токенов обязательны два уровня: примитивные (<code>blue-500</code>) и семантические (<code>color-action-primary</code>). Продукты используют только семантические, иначе ребрендинг превращается в поиск по всему коду.</p>
<p><strong>Публикация:</strong> версионируемые npm-пакеты (tokens, icons, react) с semver и changelog, генерируемым changesets. Разработчик прикладывает changeset к PR, бот собирает release PR.</p>
<p><strong>API компонентов:</strong> контролируемый и неконтролируемый режимы, слоты и <code>asChild</code> вместо двадцати булевых пропов, обязательный <code>forwardRef</code>, вариант-API вместо произвольных стилей.</p>
<p><strong>Доступность — часть контракта,</strong> а не «сделаем потом»: клавиатура, фокус-ловушки, ARIA. Базу разумно брать на headless-примитивах (Radix, Ark), чтобы не переписывать поведение диалогов и комбобоксов с нуля.</p>
<p><strong>Документация живая:</strong> Storybook с примерами, таблицей пропсов, заметками по a11y и разделом do/don't — последнее экономит больше времени, чем весь остальной сайт.</p>

<h4>Компромиссы</h4>
<p>Жёсткий запрет на произвольный <code>className</code> сохраняет систему целостной и злит продуктовые команды, которым «нужно чуть-чуть подвинуть». Компромисс — расширяемые слоты и явные точки кастомизации, но не сквозная передача стилей. Быстрый выпуск компонентов повышает adoption и накапливает долг в API, который потом ломать дорого; медленный и продуманный API отталкивает команды к своим решениям. Практически это решается уровнями зрелости: экспериментальные компоненты помечены как unstable и не дают гарантий обратной совместимости. Монопакет проще в разработке, набор пакетов лучше для веса бандла — но требует дисциплины версий.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Метрики adoption: доля экранов на компонентах ДС, число локальных форков и оверрайдов, время апгрейда до новой мажорки, время до первого использования новым разработчиком. Без них дизайн-система не может доказать свою ценность и первой попадает под сокращение.</li>
<li>Миграции: breaking change без codemod перекладывает работу на десять команд. Депрекация с предупреждением в рантайме, codemod, окно поддержки старой мажорки — обязательный набор.</li>
<li>Визуальные регрессии: без скриншотных тестов (Chromatic, Playwright) любое изменение токена — рулетка.</li>
<li>Вес бандла: библиотека без tree-shaking и с барелем на всё тянет 300 КБ ради одной кнопки.</li>
<li>SSR: компоненты с обращением к <code>window</code> на импорте ломают серверный рендер у всех потребителей сразу.</li>
<li>Поддержка как процесс: канал, SLA на ответ, дежурство. Без этого команды перестают спрашивать и начинают форкать.</li>
<li>Вклад извне: если продуктовая команда не может внести компонент, система не масштабируется — нужен contribution-процесс с шаблоном и ревью.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как продать дизайн-систему бизнесу?»</strong> — не «консистентность», а деньги и время: сколько человеко-дней уходит на повторную реализацию одних и тех же компонентов в трёх командах, сколько стоил последний ребрендинг и сколько он будет стоить на токенах, сколько багов доступности приходит из самописных компонентов и чем это грозит юридически. Обещание должно быть проверяемым: «ребрендинг за спринт вместо квартала». <strong>«Как заставить команды перейти?»</strong> — заставлять не нужно, нужно сделать переход дешевле, чем неиспользование: codemod, миграционный гайд, помощь руками на первом внедрении и обязательность только для нового кода. <strong>«Что делать с компонентом, который нужен одной команде?»</strong> — он остаётся у них, попадает в ДС только после второго-третьего потребителя.</p>`,
    code: `// два уровня токенов: продукты используют только семантические
{
  "color": {
    "blue": { "500": { "$value": "#2563eb", "$type": "color" } },
    "action": {
      "primary": { "$value": "{color.blue.500}", "$type": "color" },
      "primary-hover": { "$value": "{color.blue.600}", "$type": "color" }
    }
  }
}

// API компонента: слоты и варианты вместо булевых пропов
<Button variant="primary" size="md" asChild>
  <Link href="/checkout">Оформить</Link>
</Button>

// депрекация с рантайм-предупреждением и codemod в релизе
/** @deprecated Используйте variant="primary". Удалено в v5. Codemod: npx @ds/codemod button-primary */
export function PrimaryButton(props) {
  warnOnce('PrimaryButton устарел, см. миграцию на Button variant="primary"');
  return <Button variant="primary" {...props} />;
}

// метрики adoption собираются автоматически из кода продуктов
// scan: доля импортов из @company/ds против локальных компонентов,
// число styled(...) поверх компонентов ДС, версии пакета по репозиториям`,
    tip: 'Сеньорность здесь читается по двум вещам: метрики adoption вместо списка сделанных компонентов и codemod вместо «мы объявили в чате». Добавь фразу про деньги: стоимость следующего ребрендинга — самый убедительный аргумент за токены.' },

  { id: 'sdx20',
    q: 'Спроектируй систему A/B-тестов на фронтенде.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что тестируем: изменения выше первого экрана (тогда flicker — главная проблема) или в глубине флоу. Кто субъект эксперимента: авторизованный пользователь, анонимный посетитель или аккаунт целиком (в B2B часто нужен именно аккаунт, иначе внутри одной компании люди видят разное). Сколько экспериментов одновременно и могут ли они пересекаться. Есть ли SSR и CDN — от этого зависит вся схема назначения. Кто анализирует результаты: своя платформа или готовая (Optimizely, GrowthBook, Statsig). Какие метрики считаются успехом и кто останавливает эксперимент. Нужны ли эксперименты на мобильных приложениях с общей когортой — тогда назначение обязано быть общим для платформ.</p>

<h4>Ключевые решения</h4>
<p><strong>Главная фронтовая проблема — flicker (flash of original content).</strong> Страница отрисовалась в контроле, затем дёрнулась в вариант: пользователь видит мигание, а эксперимент получает загрязнённые данные. Лечится тем, что решение о варианте принимается <strong>до</strong> рендера: назначение на edge или сервере (middleware вычисляет вариант, пишет его в cookie и отдаёт уже готовый HTML) либо синхронный бутстрап конфигурации в <code>head</code>. Асинхронный клиентский SDK допустим только для изменений ниже первого экрана.</p>
<p><strong>Назначение детерминированное:</strong> <code>hash(experimentId + subjectId) % 100</code>. Это даёт стабильность между сессиями, устройствами, SSR и CSR без сетевого запроса «какой у меня вариант» — и делает назначение воспроизводимым при разборе результатов. Соль из experimentId обязательна, иначе один и тот же пользователь всегда попадает в одну и ту же половину во всех экспериментах.</p>
<p><strong>Экспозиция логируется в момент фактического показа</strong> варианта, а не в момент назначения. Иначе в выборку попадают пользователи, которые до тестируемого экрана вообще не дошли, и эффект размывается до статистической незначимости.</p>
<p><strong>Кеширование:</strong> вариант обязан входить в ключ CDN-кеша (<code>Vary</code> по cookie или отдельный ключ), иначе половина пользователей получит чужой HTML — это самый разрушительный баг в A/B на фронте.</p>
<p><strong>Kill switch:</strong> любой эксперимент выключается мгновенно флагом, без релиза.</p>

<h4>Компромиссы</h4>
<p>Назначение на edge убирает flicker и требует инфраструктуры (middleware, конфигурация на границе, кеш с учётом варианта). Клиентский SDK ставится за час и портит верхние экраны. Разделение кеша по вариантам удваивает число ключей и снижает hit rate — при пяти одновременных экспериментах на одной странице это уже заметно, поэтому эксперименты, влияющие на HTML, приходится ограничивать по числу. Тестирование на 50/50 даёт быстрый результат и максимальный риск; постепенный раскат безопаснее, но требует больше времени до значимости.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Разницу между фича-флагом и экспериментом: первый — операционный переключатель, живёт вечно; второй — статистически измеряемая гипотеза с датой окончания. Смешивание даёт «эксперименты», которые никто не выключает.</li>
<li>Взаимное влияние: два эксперимента на одном экране перекрываются. Решается слоями (mutually exclusive layers), где пользователь попадает только в один эксперимент слоя.</li>
<li>Влияние варианта на производительность: вариант с лишними 200 КБ проиграет из-за скорости загрузки, а не из-за идеи. Перф-метрики надо смотреть по вариантам всегда.</li>
<li>Подглядывание в результаты (peeking): проверять значимость каждый день и останавливать на первом «выиграли» — это способ получить ложный результат. Нужен заранее рассчитанный размер выборки или последовательный тест.</li>
<li>SRM (sample ratio mismatch): если в вариантах 48/52 вместо 50/50, эксперимент сломан на уровне назначения, и результаты недействительны. Проверка должна быть автоматической.</li>
<li>Доступность и деградация: вариант обязан работать при отключённом JS и не ломать скринридер.</li>
<li>Тесты и Storybook должны уметь принудительно фиксировать вариант через query-параметр, иначе прод-конфигурация никогда не покрыта тестами.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как объяснить бизнесу, зачем инфраструктура на edge?»</strong> — через цену ошибки: flicker искажает результаты, а решения по искажённым данным стоят дороже, чем инфраструктура; плюс каждый эксперимент, который пришлось перезапускать, — это потерянные недели. <strong>«Что делать с пользователем, который сменил устройство?»</strong> — для анонимов идентификатор в cookie, при логине переезжаем на userId и логируем смену; часть трафика неизбежно теряет когорту, это надо учитывать. <strong>«Как понять, что эксперимент можно останавливать?»</strong> — заранее зафиксированный размер выборки и срок, а не ежедневный просмотр графика.</p>`,
    code: `// edge middleware: вариант вычислен до рендера, flicker невозможен
export function middleware(req) {
  const res = NextResponse.next();
  const subjectId = req.cookies.get('uid')?.value ?? crypto.randomUUID();

  for (const exp of activeExperiments) {
    const bucket = hash32(exp.id + ':' + subjectId) % 100;   // соль из id опыта
    const variant = bucket < exp.rollout ? pickVariant(exp, bucket) : 'control';
    res.cookies.set('exp_' + exp.id, variant, { maxAge: exp.durationSec });
  }
  res.headers.set('Vary', 'Cookie');       // вариант в ключе CDN-кеша
  return res;
}

// экспозиция логируется в момент показа, а не назначения
function useExperiment(id) {
  const variant = readVariantCookie(id);
  useEffect(() => {
    analytics.track('experiment_exposure', {
      experimentId: id, variant, ts: Date.now(),
    });
  }, [id, variant]);
  return variant;
}

// принудительная фиксация для тестов и Storybook
// /checkout?__exp=new_cta:variant_b`,
    tip: 'Проведи границу вслух: фича-флаг — операционный переключатель без даты смерти, эксперимент — гипотеза с заранее посчитанным размером выборки и датой окончания. И добавь про SRM: расхождение долей 48/52 означает, что данные эксперимента недействительны, сколько бы красивым ни был результат.' },

  { id: 'sdx21',
    q: 'Спроектируй клиентскую аналитику: как надёжно доставлять события?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что за события и какая цена потери: продуктовая воронка терпит 1-2% потерь, биллинговые события — нет, и это определяет всю архитектуру. Какой объём: 10 событий на сессию или 500 (второе требует семплирования). Нужны ли события до получения согласия на трекинг и что считается технически необходимым. Есть ли требование обрабатывать данные внутри страны (тогда endpoint и хранилище региональные). Нужна ли связка клиентских событий с серверными по общему идентификатору сессии или трейса. Насколько важна точность порядка событий. И практическое: сколько пользователей с блокировщиками — от этого зависит, нужен ли first-party endpoint.</p>

<h4>Ключевые решения</h4>
<p><strong>Ядро — очередь с батчингом и устойчивой доставкой.</strong> События кладём в буфер и флашим по трём условиям: набрался батч (например, 20 событий), сработал таймер (5-10 секунд) или страница уходит в фон (<code>visibilitychange</code> в hidden). Последнее условие — главное: <code>beforeunload</code> ненадёжен на мобильных, где вкладку убивают без него, а <code>pagehide</code> и переход в hidden срабатывают.</p>
<p><strong>Транспорт при уходе:</strong> <code>navigator.sendBeacon</code> или <code>fetch(url, { keepalive: true })</code>. Обычный fetch браузер отменит вместе с документом, и события просто исчезнут — это самая частая причина «дыр» в аналитике.</p>
<p><strong>Идемпотентность.</strong> Каждое событие несёт клиентский <code>event_id</code> (UUID), <code>client_ts</code> и порядковый номер в сессии. Сервер дедуплицирует по event_id, клиент спокойно ретраит при 5xx с экспоненциальным backoff. Порядковый номер позволяет обнаружить пропуски и посчитать реальный loss rate.</p>
<p><strong>Персистентная очередь</strong> в IndexedDB: офлайн, краш вкладки и закрытие браузера не должны терять события — отправляем при следующем запуске. Клиентское время корректируем по расхождению с серверным (<code>server_ts - client_ts</code> из ответа), иначе события с неверно выставленными часами устройства уезжают в прошлое или будущее.</p>
<p><strong>Схема событий типизирована</strong> и генерируется из общего реестра, единого с бэкендом и мобильными.</p>

<h4>Компромиссы</h4>
<p>Батчинг снижает нагрузку и увеличивает окно потери: чем больше батч, тем больше теряется при внезапном закрытии. Компромисс — маленький батч плюс обязательный флаш на hidden. Персистентная очередь спасает события и стоит записей в IndexedDB на горячем пути — поэтому пишем не каждое событие, а батч, и делаем это в idle. First-party endpoint поднимает доставляемость на 10-30% у пользователей с блокировщиками, но перекладывает на нас трафик и ответственность за приватность. Семплирование экономит деньги и делает редкие события ненаблюдаемыми — критичные события всегда шлём без семплирования.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Лимит <code>sendBeacon</code> около 64 КБ: большой батч молча не отправится, вернув <code>false</code>. Возвращаемое значение надо проверять и резать батч.</li>
<li>Конкуренцию с UI: сериализация и запись в хранилище в обработчике клика добавляют задержку к INP. Всё тяжёлое — в <code>requestIdleCallback</code> или в воркер.</li>
<li>Consent: до согласия не отправляем ничего, кроме технически необходимого, а накопленный буфер либо отправляем после согласия, либо честно удаляем. Отправить «задним числом» без согласия — это нарушение, а не оптимизация.</li>
<li>Переполнение очереди: при длительном офлайне нужен лимит и дроп по приоритету (сначала низкоприоритетная телеметрия), а не бесконечный рост.</li>
<li>Несколько вкладок: каждая со своей очередью даёт дубли сессионных событий. Нужен общий идентификатор сессии и по возможности один отправитель через Web Locks.</li>
<li>Измерение потерь: без сравнения клиентских порядковых номеров с полученными на сервере вы не знаете реальный loss rate и спорите о цифрах вслепую.</li>
<li>PII: в события легко утекают email в URL и текст полей. Нужна явная схема и запрет свободных полей.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как доказать, что аналитика надёжна?»</strong> — сравнение серверных счётчиков с клиентскими и отчёт по loss rate по браузерам и платформам; это же аргумент в спорах с продуктом о «странных» цифрах. <strong>«Что если события нужны в реальном времени?»</strong> — отдельный низколатентный канал для узкого набора критичных событий, остальное остаётся батчами. <strong>«Что нового в платформе?»</strong> — <code>fetchLater()</code> (PendingBeacon) позволяет запланировать отправку заранее и не зависеть от момента ухода со страницы; пока это прогрессивное улучшение.</p>`,
    code: `// очередь: батч по размеру, по таймеру и обязательно на уходе страницы
const MAX_BATCH = 20;
const BEACON_LIMIT = 60 * 1024;
let buffer = [];

function track(name, props) {
  buffer.push({
    event_id: crypto.randomUUID(),
    name,
    props,
    client_ts: Date.now(),
    seq: ++sessionSeq,          // пропуски в seq = измеримый loss rate
    session_id: sessionId,
  });
  if (buffer.length >= MAX_BATCH) requestIdleCallback(flush);
}

function flush(reason) {
  if (!buffer.length || !consent.analytics) return;
  const batch = buffer;
  buffer = [];

  const body = JSON.stringify({ batch, reason });
  if (body.length > BEACON_LIMIT) return sendInChunks(batch);

  const ok = navigator.sendBeacon('/collect', body);   // свой домен
  if (!ok) persist(batch);      // не влезло или отказ -> в IndexedDB
}

// самый важный момент: страница уходит в фон
addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'hidden') flush('hidden');
});
addEventListener('pagehide', () => flush('pagehide'));`,
    tip: 'Назови, как измеряете доставляемость: порядковый номер события в сессии позволяет посчитать точный процент потерь по браузерам. Кандидат, который говорит «мы знаем, что теряем 3% на iOS Safari», выглядит принципиально иначе, чем тот, кто перечисляет sendBeacon.' },

  { id: 'sdx22',
    q: 'Спроектируй файловый менеджер в браузере (как Google Drive).',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько файлов в одной папке в худшем случае и какой общий объём аккаунта. Нужны ли шаринг и права на уровне файла и папки с наследованием — это самая недооценённая часть задачи. Нужен ли поиск по содержимому файлов или только по именам. Нужны ли версии файлов и восстановление. Есть ли совместная работа: два человека переименовывают одну папку одновременно. Нужна ли офлайн-доступность выбранных файлов (как «доступно офлайн» в Drive). Какие превью нужны: картинки, PDF, видео — генерация превью это отдельный пайплайн на бэке. И есть ли интеграция с локальной ФС пользователя.</p>

<h4>Ключевые решения</h4>
<p><strong>Модель — дерево, навигация — по уровням.</strong> Папки и файлы с <code>parent_id</code>, но клиент никогда не грузит дерево целиком: только текущий уровень с курсорной пагинацией и серверной сортировкой. Путь в URL (<code>/folder/:id</code>), хлебные крошки восстанавливаются запросом предков; вид (список/сетка) и сортировка — в URL и в пользовательских настройках.</p>
<p><strong>Массовые операции — центральная часть UX,</strong> а не дополнение: множественный выбор (Shift для диапазона, Ctrl для точечного, рамкой мышью), drag-n-drop между папками и из операционной системы, копирование и перемещение.</p>
<p><strong>Долгие операции асинхронные.</strong> Перемещение 10 тысяч файлов не может быть синхронным HTTP-запросом: сервер возвращает job id, клиент показывает прогресс и позволяет уйти со страницы. Статус job опрашивается или приходит по сокету.</p>
<p><strong>Загрузка</strong> — чанковая и возобновляемая, с глобальной очередью и лимитом параллельности; очередь живёт на уровне приложения и переживает переход между папками.</p>
<p><strong>Оптимистичные операции</strong> для переименования и перемещения; удаление — всегда soft delete в корзину, потому что цена ошибки для файлов пользователя несоизмерима с удобством.</p>
<p><strong>Превью</strong> генерируются на бэке, грузятся лениво через IntersectionObserver, с плейсхолдером фиксированного размера — иначе сетка прыгает при загрузке.</p>

<h4>Компромиссы</h4>
<p>Загрузка по уровням проста и масштабируется, но делает поиск по всему дереву серверной задачей и мешает показать «дерево слева» целиком — его тоже приходится грузить лениво по раскрытию. Оптимистичное перемещение мгновенно и создаёт неприятный случай: файл уехал в папку, куда у пользователя нет прав на запись, и вернулся через две секунды. Поэтому права проверяем на клиенте до старта операции, а не только после ответа. Виртуализация сетки нужна на тысячах элементов и ломает выделение рамкой через весь список — приходится считать выделение в координатах данных.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Конфликт имён при загрузке: правило «(1), (2)» или диалог «заменить / сохранить оба / пропустить», с опцией «применить ко всем». Это решается заранее, а не в момент, когда пользователь льёт 300 файлов.</li>
<li>Права: приходят с каждым элементом, UI прячет недоступные действия, но настоящая проверка на сервере. Скрытая кнопка — не механизм безопасности.</li>
<li>Перемещение папки внутрь самой себя: без серверной проверки получаете отсоединённый цикл в дереве, который потом невозможно найти.</li>
<li>Гонки: переименовали и сразу переместили; ответы вразнобой возвращают старое имя. Нужна очередь операций по элементу.</li>
<li>Drag-n-drop из ОС: <code>DataTransferItem.webkitGetAsEntry()</code> для папок, потому что обычный <code>files</code> отдаёт только плоский список.</li>
<li>Доступность: сетка файлов — это <code>role="grid"</code> с клавиатурным выделением; DnD обязан иметь клавиатурную альтернативу («переместить в...»).</li>
<li>Отмена массовой операции: job должен уметь останавливаться и откатываться, иначе «отмена» — это кнопка, которая ничего не делает.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как дать настоящую работу с локальными папками?»</strong> — File System Access API (<code>showDirectoryPicker</code>) там, где поддерживается, с деградацией на <code>input type=file</code>; OPFS — для внутреннего кеша приложения. <strong>«Как показать прогресс перемещения 10 тысяч файлов?»</strong> — сервер публикует прогресс job, клиент подписывается; при уходе со страницы уведомление о завершении. <strong>«Как сделать поиск?»</strong> — серверный индекс по именам и метаданным, обязательно с учётом прав: результат не должен раскрывать существование файлов, к которым нет доступа.</p>`,
    code: `// длинная операция — это job, а не HTTP-запрос
const { jobId } = await api.post('/files/move', { ids: selected, targetId });

for await (const p of subscribeJob(jobId)) {
  setProgress(p.done / p.total);
  if (p.state === 'failed') showPartialFailure(p.errors); // частичный успех тоже
}

// выделение считаем в координатах данных, чтобы пережить виртуализацию
function selectRange(anchorIndex, focusIndex) {
  const [a, b] = [anchorIndex, focusIndex].sort((x, y) => x - y);
  return visibleOrder.slice(a, b + 1).map((i) => items[i].id);
}

// drag-n-drop из ОС: обычный files не отдаёт структуру папок
async function onDrop(e) {
  const entries = [...e.dataTransfer.items]
    .map((i) => i.webkitGetAsEntry())
    .filter(Boolean);
  for (const entry of entries) await walkAndEnqueue(entry);  // рекурсивно
}

// конфликт имён решается политикой, а не диалогом на каждый файл
// { onConflict: 'rename' | 'replace' | 'skip', applyToAll: true }`,
    tip: 'Отдельно скажи, что удаление всегда soft delete с корзиной и TTL: для пользовательских файлов цена ошибочного удаления несопоставима с удобством, и это продуктовое решение, которое инженер обязан предложить сам.' },

  { id: 'sdx23',
    q: 'Спроектируй мультиязычный сайт: i18n, роутинг, SEO.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько локалей и есть ли среди них RTL (арабский, иврит) — это меняет вёрстку целиком, а не добавляет атрибут. Локаль равна языку или языку плюс региону: <code>en-US</code> и <code>en-GB</code> различаются валютой, форматом дат и частью терминологии. Отличается ли контент между локалями или это перевод одного и того же — если ассортимент, цены и юридические тексты разные, это уже разные сайты с общим кодом. Важен ли органический трафик в каждой стране (тогда hreflang и локальные URL обязательны). Кто и как переводит: подрядчик через TMS, машинный перевод с постредактурой или разработчики руками. Нужны ли локальные форматы адресов, имён и телефонов.</p>

<h4>Ключевые решения</h4>
<p><strong>Роутинг — подпуть на локаль:</strong> <code>/ru/catalog</code>, <code>/en/catalog</code>. Это лучше кук и редиректов по Accept-Language: URL уникален для языка, а значит кешируется на CDN, индексируется и корректно шарится. Поддомены и отдельные домены оправданы, только когда за локалью стоит отдельное юрлицо, отдельный ассортимент или требование локального хостинга.</p>
<p><strong>SEO.</strong> На каждой странице полный набор <code>hreflang</code>, включая самоссылку и <code>x-default</code>, канонический URL — на себя в своей локали. Автоопределение языка делаем мягко: баннер «перейти на русскую версию», а не жёсткий редирект. Жёсткий редирект по Accept-Language ломает индексацию (краулер приходит из США и не видит русскую версию) и раздражает пользователей, читающих не на языке системы.</p>
<p><strong>Бандл.</strong> Словари грузятся по локали и по маршруту через code splitting; все языки в одном чанке — самая частая и самая дорогая ошибка, легко добавляющая сотни килобайт.</p>
<p><strong>Форматирование</strong> чисел, дат, валют, относительного времени и множественного числа — только через <code>Intl</code> и ICU MessageFormat. Русские склонения и три формы множественного числа нельзя собрать конкатенацией.</p>
<p><strong>Переводы живут в TMS</strong> (Crowdin, Lokalise) с двусторонней синхронизацией в репозиторий; ключи типизированы кодогеном, отсутствующий перевод ломает CI, а не показывается пользователю как <code>common.button.ok</code>.</p>

<h4>Компромиссы</h4>
<p>Подпуть на локаль прост и хорош для SEO, но умножает число URL и кеш-ключей; при десяти локалях это заметная нагрузка на сборку статики и на бюджет CDN. Fallback на английский при отсутствующем переводе делает продукт непадающим и создаёт смешанные экраны — иногда честнее показать оригинал, иногда лучше скрыть блок; решение принимается по типу контента. Автоперевод даёт быстрый охват и низкое качество в юридических и продающих текстах: обычно комбинируют — интерфейс переводят люди, пользовательский контент машинно.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Длину строк: немецкий и русский длиннее английского на 30-40%, японский короче. Это требование к вёрстке, а не «проблема переводчиков» — кнопки не должны ломаться.</li>
<li>RTL целиком: <code>dir="rtl"</code> недостаточно. Нужны логические CSS-свойства (<code>margin-inline-start</code>, <code>padding-inline</code>), зеркалирование направленных иконок и правильное поведение смешанного bidi-текста с числами и латиницей.</li>
<li>Псевдолокализацию в dev-режиме: она ловит захардкоженные строки и переполнение вёрстки до того, как это увидит переводчик.</li>
<li>Форматы, которые кажутся универсальными: порядок имени и фамилии, формат адреса, первый день недели, десятичный разделитель.</li>
<li>Гидрацию: сервер отрендерил дату в своей таймзоне и локали, клиент — в своей, React ругается на несовпадение. Локаль и таймзона должны передаваться явно.</li>
<li>Шрифты: набор глифов для кириллицы, греческого, CJK — это разные подмножества и разный вес; загрузка всего шрифта ради одной локали бьёт по LCP.</li>
<li>Доступность: <code>lang</code> на <code>html</code> и на вставках на другом языке — от него зависит, как скринридер произносит текст.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как не грузить лишние словари?»</strong> — разбиение по маршрутам и ленивая догрузка неймспейсов, плюс инлайн критичных строк первого экрана в HTML. <strong>«Как переводить пользовательский контент?»</strong> — отдельная задача: машинный перевод по требованию с пометкой и кешированием, а не через TMS. <strong>«Как проверить, что ничего не сломалось в арабском?»</strong> — скриншотные тесты ключевых экранов в RTL и с псевдолокализацией в CI.</p>`,
    code: `// ICU MessageFormat: множественное число нельзя собирать конкатенацией
// ru.json
{
  "cart.items": "{count, plural, one {# товар} few {# товара} other {# товаров}}",
  "order.eta": "Доставим {date, date, medium} в {city}"
}

// форматирование — только через Intl, с явной локалью и таймзоной
new Intl.NumberFormat(locale, { style: 'currency', currency }).format(price);
new Intl.DateTimeFormat(locale, { dateStyle: 'medium', timeZone: userTz }).format(d);
new Intl.PluralRules(locale).select(count);          // 'one' | 'few' | 'other'

// hreflang: полный набор с самоссылкой и x-default
// <link rel="alternate" hreflang="ru" href="https://site.com/ru/catalog" />
// <link rel="alternate" hreflang="en" href="https://site.com/en/catalog" />
// <link rel="alternate" hreflang="x-default" href="https://site.com/en/catalog" />
// <link rel="canonical" href="https://site.com/ru/catalog" />

// логические свойства вместо left/right — RTL работает без отдельной таблицы стилей
.card { padding-inline-start: 16px; border-inline-end: 1px solid var(--line); }`,
    tip: 'Скажи про псевдолокализацию как про инструмент, который окупается в первый же день: строки вида [!!! Ödèr Sümmärÿ !!!] сразу показывают и захардкоженный текст, и все места, где вёрстка не переживёт немецкий.' },

  { id: 'sdx24',
    q: 'Спроектируй админку с ролями и правами.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько ролей и меняются ли они: три фиксированные роли и конструктор ролей у клиента — разные системы. Нужны ли права на уровне конкретных записей («только заказы своего региона») — это ABAC, и он не решается ролью в токене. Есть ли мультиарендность: один пользователь работает в нескольких организациях с разными правами. Требуется ли аудит действий и с какой детализацией — комплаенс напрямую влияет на UI (подтверждения, поле «причина»). Нужна ли работа под другим пользователем (impersonation) для поддержки. Как часто меняются права и должно ли изменение применяться мгновенно или до следующего входа. Сколько разделов в админке — от этого зависит, нужен ли реестр маршрутов.</p>

<h4>Ключевые решения</h4>
<p><strong>Первая фраза, которую ждёт интервьюер: фронтенд не является механизмом безопасности.</strong> UI прячет недоступное ради удобства, но каждый запрос проверяется на сервере. Скрытая кнопка защищает от случайного клика, а не от злоумышленника с devtools.</p>
<p><strong>Модель прав.</strong> RBAC для грубого разделения (admin/editor/viewer) плюс permission-строки для точности: <code>orders:refund</code>, <code>users:read</code>. Компоненты проверяют разрешения, а не роли, — тогда добавление новой роли не требует правок по всему коду. Для данных с владельцем добавляется ABAC-слой: права приходят вместе с ресурсом (<code>order.permissions: ["read", "refund"]</code>), потому что «можно ли рефанднуть этот заказ» зависит от заказа, а не только от пользователя.</p>
<p><strong>Загрузка и хранение.</strong> Набор разрешений приходит один раз при старте сессии и живёт в контексте. В коде — <code>useCan('orders:refund')</code> и компонент <code>&lt;Can&gt;</code> вместо разбросанных сравнений с ролью.</p>
<p><strong>Роутинг.</strong> Маршруты декларируют требуемые права в реестре; guard редиректит на экран 403. Ленивые чанки закрытых разделов не грузятся тем, кому они не положены, — это и вес бандла, и меньше утечки информации о существовании разделов.</p>
<p><strong>Меню и действия рендерятся из того же реестра,</strong> что и маршруты: тогда новая фича не может «забыть» проверку — её просто негде не написать.</p>

<h4>Компромиссы</h4>
<p>Права в токене — быстро и без запросов, но изменение прав применяется только после обновления токена; права отдельным запросом — актуальнее ценой раунд-трипа на старте и необходимости их инвалидации. Обычный компромисс: грубые права в токене, точные — вместе с ресурсом. Проверка прав на клиенте улучшает UX и дублирует серверную логику: две реализации расходятся, и в UI появляется кнопка, которая всегда возвращает 403. Лечится тем, что сервер отдаёт список разрешённых действий по ресурсу, а не клиент вычисляет их сам.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Разницу 401 и 403: первое — истёк вход, нужен тихий refresh и повтор запроса; второе — нет прав, повторять бессмысленно. Клиенты, которые ретраят 403, устраивают шторм запросов.</li>
<li>Смену прав в рантайме: администратор забрал доступ, пользователь сидит с открытой вкладкой. Ответ 403 должен перезагружать набор разрешений и обновлять UI, а не показывать ошибку.</li>
<li>Гонку при обновлении токена: пять параллельных запросов получают 401 и запускают пять refresh. Нужен единый промис обновления.</li>
<li>Утечку данных в UI: список сущностей, к которым нет доступа, не должен приходить вообще — фильтрация на сервере, а не скрытие на клиенте.</li>
<li>Impersonation: вход под пользователем нужен поддержке, но обязателен явный несбрасываемый баннер, ограничение опасных действий и отдельный аудит-след.</li>
<li>Аудит и подтверждения: критичные действия требуют подтверждения и поля «причина» — это требование комплаенса, влияющее на дизайн экранов.</li>
<li>Доступность: скрывать недоступные действия или показывать их отключёнными — разные решения; отключённая кнопка без объяснения хуже, чем её отсутствие.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как не дублировать правила прав на клиенте и сервере?»</strong> — сервер возвращает вычисленные разрешения вместе с ресурсом, клиент их только отображает; общие константы имён разрешений генерируются из одной схемы. <strong>«Как сделать конструктор ролей?»</strong> — роли как наборы разрешений в БД, UI строится из реестра разрешений с описаниями; иначе список ролей захардкожен и каждый новый клиент требует релиза. <strong>«Что показывать при 403 на вложенном виджете?»</strong> — локальную заглушку вместо экрана-ошибки: частичная деградация лучше пустой страницы.</p>`,
    code: `// реестр: маршруты, меню и права описаны в одном месте
export const routes = [
  { path: '/orders', permission: 'orders:read', lazy: () => import('./Orders') },
  { path: '/orders/:id/refund', permission: 'orders:refund', lazy: () => import('./Refund') },
  { path: '/users', permission: 'users:read', lazy: () => import('./Users') },
];

// проверяем разрешение, а не роль
function useCan(permission, resource) {
  const { permissions } = useSession();
  if (resource?.permissions) return resource.permissions.includes(permission);
  return permissions.has(permission);
}

<Can permission="orders:refund" resource={order} fallback={null}>
  <Button onClick={refund}>Вернуть деньги</Button>
</Can>

// 401 и 403 обрабатываются по-разному, refresh — один на всех
let refreshing = null;

api.interceptors.response.use(null, async (err) => {
  if (err.status === 401) {
    refreshing ??= refreshToken().finally(() => (refreshing = null));
    await refreshing;
    return api.retry(err.config);          // повторяем один раз
  }
  if (err.status === 403) {
    await session.reloadPermissions();     // права могли измениться
    throw new ForbiddenError(err);         // ретрай бессмысленен
  }
  throw err;
});`,
    tip: 'Начни ответ с фразы «UI прячет действия ради удобства, авторизация живёт на сервере» — интервьюер ждёт именно её. А сильным добавлением будет impersonation с несбрасываемым баннером и отдельным аудит-следом: это то, что всплывает в любой реальной админке на второй месяц.' },

  { id: 'sdx25',
    q: 'Спроектируй виджет для встраивания на чужие сайты. iframe или web components?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Кто хосты: наши партнёры по договору или произвольные сайты в интернете — от этого зависит уровень паранойи. Нужна ли виджету аутентификация пользователя и чьими куками (свой домен в iframe против third-party storage). Должен ли виджет выглядеть частью страницы хоста (тогда нужны наследование шрифтов и токенов) или он визуально самостоятелен. Нужны ли элементы, выходящие за границы виджета: модалки, тултипы, автокомплиты — iframe их физически не выпустит. Насколько сильно ограничен бюджет загрузки на стороне хоста. Есть ли требование работать при жёстком CSP хоста. И нужна ли двусторонняя интеграция: виджет читает данные страницы или отдаёт события в аналитику хоста.</p>

<h4>Ключевые решения</h4>
<p><strong>Выбор определяется требуемой изоляцией.</strong> Cross-origin iframe — единственная настоящая граница: отдельный JS-контекст, своё хранилище, свой CSP. Чужие глобалы, чужой сломанный полифилл и чужие ошибки вас не касаются, и вы не ломаете хост. Цена: коммуникация только через <code>postMessage</code>, ручной авторесайз, невозможность вывести оверлей за границу фрейма и проблемы с third-party storage.</p>
<p><strong>Web Components плюс Shadow DOM</strong> изолируют стили и DOM, но живут в том же JS-контексте: конфликт версий библиотек, глобальные ошибки, медленный хост и его блокирующие скрипты влияют на вас напрямую. Это правильный выбор, когда виджет доверенный и должен визуально сливаться со страницей.</p>
<p><strong>Практичный компромисс, который и стоит предложить:</strong> крошечный embed-скрипт (единицы килобайт) на своём CDN, который создаёт iframe, реализует протокол сообщений и авторесайз. Хост подключает одну строку и не знает про внутренности.</p>
<p><strong>Протокол postMessage строго типизирован,</strong> с версией и обязательной проверкой <code>event.origin</code> с обеих сторон. Отсутствие проверки origin — это готовая уязвимость: любая страница может слать команды вашему виджету.</p>
<p><strong>Версионирование:</strong> embed-скрипт стабилен и не ломает API никогда, внутренности виджета обновляются свободно. Кеш embed-скрипта короткий, ассеты — иммутабельные с хешем в имени.</p>

<h4>Компромиссы</h4>
<p>Iframe даёт безопасность и стоит удобства: каждое взаимодействие с хостом становится сообщением, а размеры приходится синхронизировать через ResizeObserver. Shadow DOM даёт удобство и стоит устойчивости: одна несовместимая версия библиотеки на хосте — и виджет падает у клиента, а разбираться будете вы. Если виджет обрабатывает деньги, персональные данные или авторизацию — iframe без вариантов. Если это виджет отзывов, который должен выглядеть родным, Shadow DOM оправдан. Отдельный размен: инлайн-стили внутри Shadow DOM защищают от хоста, но лишают возможности темизации — её возвращают через явный набор CSS-переменных, объявленных как публичный API.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Бюджет: виджет не должен блокировать загрузку хоста. Только <code>async</code>, ленивая инициализация по IntersectionObserver, никаких синхронных запросов и никакого <code>document.write</code>.</li>
<li>Ошибки: падение виджета не должно ломать страницу хоста; в Shadow DOM-варианте нужен свой error boundary и глобальный перехват, не мешающий хосту.</li>
<li>Third-party storage: Safari ITP и разделённое хранилище блокируют куки третьих сторон. Нужен partitioned storage, Storage Access API или обмен токеном через postMessage.</li>
<li>CSP хоста: строгий <code>script-src</code> запретит инлайн-инициализацию, а <code>frame-src</code> — ваш iframe. Требования к CSP надо документировать для интеграторов.</li>
<li>Ресайз: без ResizeObserver и протокола высоты виджет либо обрезается, либо оставляет пустоту. Модалки внутри iframe требуют отдельного сообщения «раскрой меня на весь экран».</li>
<li>Доступность: фокус не должен теряться при входе в iframe, у фрейма обязателен <code>title</code>, а контраст должен работать на любом фоне хоста.</li>
<li>Наблюдаемость: ошибки виджета на чужих сайтах видны только вам через собственный мониторинг с указанием домена хоста и версии embed.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как показать модалку поверх страницы хоста из iframe?»</strong> — сообщение хосту, embed-скрипт разворачивает фрейм в позиционированный оверлей на весь экран; внутри фрейма рисуется затемнение. <strong>«Как обновлять виджет без релизов у клиентов?»</strong> — стабильный embed-скрипт с коротким TTL кеша плюс иммутабельные версионированные ассеты. <strong>«Что если хост злонамеренный?»</strong> — sandbox-атрибут, <code>frame-ancestors</code> со списком доменов, никакого доступа к DOM хоста и подпись origin на серверной стороне при выдаче данных.</p>`,
    code: `// embed: одна строка у хоста, всё остальное берёт на себя скрипт
// <script async src="https://cdn.widget.io/v1/embed.js" data-widget-id="abc"></script>

const ALLOWED = 'https://widget.io';

const frame = document.createElement('iframe');
frame.src = ALLOWED + '/w/' + id;
frame.title = 'Виджет бронирования';
frame.setAttribute('sandbox', 'allow-scripts allow-forms allow-popups');
frame.style.cssText = 'border:0;width:100%;height:0;display:block';

// протокол: версия, проверка origin с обеих сторон, никакого доверия по умолчанию
addEventListener('message', (e) => {
  if (e.origin !== ALLOWED) return;              // без этой строки — уязвимость
  const msg = e.data;
  if (msg.v !== 1) return;
  switch (msg.type) {
    case 'resize':  frame.style.height = msg.height + 'px'; break;
    case 'overlay': setFullscreen(frame, msg.on); break;   // модалка поверх хоста
    case 'event':   host.dispatchEvent(new CustomEvent('widget:' + msg.name)); break;
  }
});

// ленивая инициализация: не конкурируем за загрузку хоста
new IntersectionObserver(([e], o) => {
  if (!e.isIntersecting) return;
  o.disconnect();
  container.appendChild(frame);
}, { rootMargin: '200px' }).observe(container);`,
    tip: 'Сформулируй критерий выбора одной фразой: «если виджет трогает деньги, авторизацию или персональные данные — только cross-origin iframe; если он должен выглядеть родным и хост доверенный — Shadow DOM». И назови бюджет: embed-скрипт до 5 КБ, async, инициализация по появлению во вьюпорте.' },

  { id: 'sdx26',
    q: 'Спроектируй offline-first приложение. Чем оно отличается от «приложения с кешем»?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Что именно должно работать без сети: чтение, создание, редактирование, всё сразу — от объёма зависит, нужен ли полноценный sync-движок или хватит кеша с очередью. Насколько долгие офлайн-периоды: минуты в лифте или недели в поле (второе означает серьёзные конфликты и рост очереди). Один пользователь на одном устройстве или несколько устройств и несколько людей на одних данных. Какой объём данных нужен локально и влезает ли он в квоту. Есть ли операции, которые офлайн делать нельзя в принципе. Какие требования безопасности к локальному хранению: данные на устройстве — это новая поверхность атаки, и в корпоративных продуктах это иногда запрещено политикой.</p>

<h4>Ключевые решения</h4>
<p><strong>Разница в источнике истины, и это единственный правильный ответ на вопрос.</strong> «Приложение с кешем» ходит в сеть, а кеш использует как ускорение и запасной вариант — источник истины на сервере, и офлайн деградирует до «ничего не работает». В offline-first <strong>локальная база является первичным хранилищем</strong>, а сеть — фоновая синхронизация. UI читает из IndexedDB и пишет в IndexedDB, спиннера в ожидании сети нет вообще.</p>
<p><strong>Слои.</strong> Service Worker кеширует оболочку приложения (precache app shell) и статику по stale-while-revalidate. IndexedDB через Dexie или RxDB хранит данные. Outbox-очередь хранит мутации. Background Sync доставляет их при возвращении сети.</p>
<p><strong>Клиентские идентификаторы.</strong> UUID сущности генерируется на клиенте, чтобы офлайн можно было создать заказ и сразу добавить в него позиции — связи должны работать до первого контакта с сервером. Сервер принимает клиентский id как есть.</p>
<p><strong>Идемпотентность мутаций</strong> обязательна: у каждой операции свой UUID, сервер дедуплицирует. Без этого ретрай после таймаута создаёт дубли, а таймауты в офлайн-сценариях — норма.</p>
<p><strong>Синхронизация инкрементальная</strong> по курсору или <code>updatedAt</code>, обязательно с tombstones: без записей об удалении удалённые сущности воскресают на устройстве, которое было офлайн.</p>
<p><strong>Стратегия конфликтов выбирается по типу данных:</strong> last-write-wins на уровне поля для простых сущностей, CRDT для текста и списков, ручное разрешение для критичных данных.</p>

<h4>Компромиссы</h4>
<p>Offline-first даёт мгновенный интерфейс и работу без сети, а платит сложностью, которая не исчезает никогда: два источника изменений, миграции локальной схемы у пользователей со старой версией, отладка багов, которые воспроизводятся только при определённой последовательности офлайн-действий. Разработка занимает в полтора-два раза больше, поэтому решение принимается по сценарию использования, а не по моде. Если пользователи всегда в офисе с Wi-Fi, правильный ответ — обычный кеш. Локальное хранение ускоряет всё и создаёт риск утечки данных с устройства.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Границу применимости: платежи, уникальные брони и любые операции с внешним дефицитным ресурсом офлайн не делаются. Это надо проговорить продукту явно, а не изображать, что всё работает.</li>
<li>Честный статус в UI: «сохранено локально», «отправляется», «конфликт» — тихая потеря данных хуже видимой ошибки.</li>
<li>Квоты: <code>QuotaExceededError</code> надо обрабатывать, а <code>navigator.storage.persist()</code> запрашивать, иначе браузер вычистит данные без предупреждения.</li>
<li>Миграции локальной схемы: пользователь вернулся через полгода с данными старого формата и несинхронизированной очередью — путь миграции нужен обязательно.</li>
<li>Многовкладочность: два таба пишут в одну базу и оба флашат очередь. Нужен лидер через Web Locks.</li>
<li>Порядок применения: мутации из очереди должны применяться в исходном порядке, иначе «создать и удалить» превращается в «удалить и создать».</li>
<li>Растущая очередь: после недели офлайна в ней тысячи операций. Схлопывание операций по сущности обязательно.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как показать пользователю конфликт?»</strong> — не диалогом «выберите версию» на каждое поле, а понятным экраном с различиями и разумным умолчанием; для большинства типов данных конфликт разрешается автоматически, ручное разрешение оставляют для дорогих сущностей. <strong>«Как тестировать офлайн?»</strong> — не только тумблером в devtools: сценарные тесты с искусственной задержкой, обрывом посреди запроса и повторной доставкой. <strong>«Чем RxDB лучше голого IndexedDB?»</strong> — готовые реактивные запросы, репликация и схема с миграциями; цена — вес и привязка к их модели.</p>`,
    code: `// outbox: порядок, идемпотентность и схлопывание
async function mutate(op) {
  await db.transaction('rw', db.entities, db.outbox, async () => {
    applyLocally(op);                       // UI обновился мгновенно
    await db.outbox.add({
      opId: crypto.randomUUID(),            // сервер дедуплицирует по нему
      entityId: op.entityId,
      type: op.type,
      payload: op.payload,
      seq: ++localSeq,                      // порядок применения на сервере
    });
  });
  if (navigator.onLine) flush();
  else registration.sync.register('outbox'); // Background Sync
}

// создание офлайн: id генерирует клиент, иначе связи не построить
const orderId = crypto.randomUUID();
await mutate({ type: 'order.create', entityId: orderId, payload: { ... } });
await mutate({ type: 'item.add', entityId: crypto.randomUUID(),
               payload: { orderId, sku } });   // ссылка работает до синка

// синк с tombstones: без них удаления не доезжают
const { changes, deleted, cursor } = await api.sync({ since: lastCursor });
await db.transaction('rw', db.entities, async () => {
  await db.entities.bulkPut(changes);
  await db.entities.bulkDelete(deleted);
});

// квота — это не гипотетическая проблема
try { await db.entities.bulkPut(batch); }
catch (e) { if (e.name === 'QuotaExceededError') await evictColdData(); }`,
    tip: 'Ответ на сам вопрос формулируй в одном предложении: «в приложении с кешем источник истины на сервере, а кеш — ускорение; в offline-first источник истины на устройстве, а сервер — точка синхронизации». Дальше добавь границу: часть операций офлайн не делается, и честно сказать об этом продукту — тоже часть проектирования.' },

  { id: 'sdx27',
    q: 'Спроектируй бесконечную ленту с сохранением позиции при возврате назад.',
    a: `<h4>Уточняющие вопросы</h4>
<p>Как глубоко реально листают: если 95-й перцентиль это 50 элементов, задача сильно проще, чем при тысячах. Меняется ли лента в реальном времени и должны ли новые элементы вставляться сверху. Открывается ли карточка как отдельный маршрут или как модалка поверх ленты — во втором случае проблема почти исчезает. Нужен ли SEO и индексация ленты. Есть ли медиа с неизвестными размерами — от этого зависит, будет ли скакать вёрстка. SPA-навигация или полная перезагрузка страницы. И надо ли восстанавливать позицию после закрытия браузера или достаточно кнопки «назад» в рамках сессии.</p>

<h4>Ключевые решения</h4>
<p><strong>Проблема:</strong> пользователь долистал до трёхсотого элемента, открыл карточку, нажал «назад» — и оказался в начале. Решение состоит из трёх частей, и все три обязательны.</p>
<p><strong>Первое — сохранение состояния ленты:</strong> загруженные страницы, курсоры и данные кешируются и привязываются к записи в history. React Query с infinite query делает это, если не инвалидировать кеш при возврате; ключ должен включать параметры ленты, но не меняться от навигации.</p>
<p><strong>Второе — восстановление позиции по якорю, а не по пикселям.</strong> Запоминаем id последнего видимого элемента и смещение внутри него; после восстановления данных скроллим к этому элементу. Абсолютный <code>scrollTop</code> не работает, потому что высоты зависят от загрузки картинок, шрифтов и подстановки рекламы — к моменту восстановления те же 12000 пикселей указывают на другое место.</p>
<p><strong>Третье — отключение браузерного авто-восстановления:</strong> <code>history.scrollRestoration = 'manual'</code>, иначе браузер попытается восстановить скролл до того, как данные загрузились, и промахнётся.</p>
<p><strong>Пагинация — курсорная.</strong> С offset новые элементы сверху сдвигают выдачу: пользователь получает дубликаты и пропуски. Дедупликация по id нужна даже с курсором, потому что реалтайм-вставки всё равно дадут пересечения.</p>
<p><strong>Подгрузка</strong> — через IntersectionObserver на сторожевом элементе с запасом около двух экранов, а не на событии scroll.</p>

<h4>Компромиссы</h4>
<p>Хранение всех загруженных страниц даёт идеальный возврат и растёт в памяти: 5000 элементов с картинками — это заметные мегабайты и тормозящий DOM. Виртуализация решает память и конфликтует с восстановлением: нужен кеш измеренных высот, иначе при возврате лента «схлопывается» и уезжает. Компромисс — окно из нескольких экранов вокруг позиции плюс возможность перезапросить выгруженные страницы при обратной прокрутке. Восстановление через sessionStorage переживает перезагрузку, но требует TTL: возвращаться через час к устаревшей ленте хуже, чем начать заново.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>CLS: медиа без зарезервированного размера через <code>aspect-ratio</code> двигают контент при загрузке, и любое восстановление позиции промахивается.</li>
<li>Реалтайм-вставки сверху: они сдвигают всё под пальцем. Показываем плашку «новые записи», применяем по клику.</li>
<li>Гонки: пользователь быстро скроллит, летят три запроса следующей страницы. Нужен флаг «загрузка идёт» и ключ по курсору, а не только debounce.</li>
<li>Ошибку подгрузки: в конце ленты должно быть состояние «не удалось загрузить, повторить», а не бесконечный спиннер, который выглядит как «данные кончились».</li>
<li>Конец ленты: явное состояние «это всё» обязательно, иначе пользователь скроллит в пустоту.</li>
<li>SEO и доступность: бесконечная лента не индексируется и не проходится с клавиатуры. Нужна альтернативная постраничная навигация и кнопка «загрузить ещё» с корректным переводом фокуса на первый новый элемент.</li>
<li>Память: при тысячах элементов нужно освобождать дальние страницы и уметь их перезапросить — иначе вкладка на мобильном перезагружается.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«А если пользователь пришёл по ссылке в середину ленты?»</strong> — курсор в URL и загрузка окна вокруг него с возможностью листать в обе стороны. <strong>«Почему не native scroll restoration?»</strong> — потому что она восстанавливает пиксели до загрузки данных; она работает только для статических страниц. <strong>«Как проверить, что работает?»</strong> — e2e-тест: пролистать до N, открыть карточку, вернуться, проверить, что тот же элемент во вьюпорте; плюс замер CLS при восстановлении.</p>`,
    code: `// отключаем браузерное восстановление: оно сработает раньше данных
if ('scrollRestoration' in history) history.scrollRestoration = 'manual';

// запоминаем якорь, а не scrollTop
const anchorObserver = new IntersectionObserver((entries) => {
  const top = entries.filter((e) => e.isIntersecting)
    .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top)[0];
  if (!top) return;
  sessionStorage.setItem('feed:anchor', JSON.stringify({
    id: top.target.dataset.id,
    offset: top.boundingClientRect.top,
    cursor: lastLoadedCursor,
    ts: Date.now(),
  }));
}, { threshold: 0.1 });

// восстановление после того, как данные и медиа получили размеры
async function restore() {
  const raw = sessionStorage.getItem('feed:anchor');
  if (!raw) return;
  const a = JSON.parse(raw);
  if (Date.now() - a.ts > 30 * 60 * 1000) return;    // TTL: не воскрешаем старое
  await loadUntilCursor(a.cursor);
  const el = document.querySelector('[data-id="' + a.id + '"]');
  if (!el) return;
  const y = el.getBoundingClientRect().top + window.scrollY - a.offset;
  window.scrollTo({ top: y, behavior: 'instant' });
}

// курсорная пагинация + дедуп: реалтайм всё равно даст пересечения
const merged = dedupeById([...pages.flat(), ...nextPage.items]);`,
    tip: 'Сильный ответ называет ограничение памяти числом: при нескольких тысячах элементов дальние страницы выгружаются и перезапрашиваются при обратной прокрутке. И добавляет, что резервирование размеров медиа через aspect-ratio — не про CLS ради метрики, а необходимое условие того, чтобы восстановление позиции вообще попадало в цель.' },

  { id: 'sdx28',
    q: 'Спроектируй realtime-курсоры и presence («кто сейчас в документе»).',
    a: `<h4>Уточняющие вопросы</h4>
<p>Сколько одновременных участников в комнате в худшем случае: 5, 50 или 500 — на пятистах курсоры перестают быть полезными, и нужен другой UI. Что показываем: только «кто здесь», курсоры мыши, выделение текста, статус «печатает» — каждый пункт добавляет свой поток данных. Курсор в свободном холсте (координаты) или в тексте (логическая позиция) — вторая задача заметно сложнее. Насколько важна приватность: presence раскрывает, кто какой документ смотрит, и в некоторых продуктах это неприемлемо. Есть ли уже WebSocket для данных или соединение придётся заводить. Нужна ли история присутствия («был здесь 5 минут назад») — это уже не presence, а аналитика.</p>

<h4>Ключевые решения</h4>
<p><strong>Presence — эфемерное состояние, и это главный архитектурный тезис.</strong> Его не надо писать в основную базу и не надо гонять через тот же канал, что и данные документа. Отдельный awareness-канал: клиент периодически шлёт heartbeat со своим состоянием (позиция курсора, выделение, статус), сервер разносит подписчикам комнаты и удаляет запись по TTL около 30 секунд при пропаже heartbeat. Состояние живёт в памяти сервера или в Redis с истечением, а не в PostgreSQL.</p>
<p><strong>Частота — главный компромисс.</strong> Мышь двигается 60 раз в секунду, но 60 сообщений в секунду на участника при 20 участниках — это 1200 сообщений в секунду на комнату. Троттлим отправку до 10-20 Гц, а на приёме сглаживаем интерполяцией между последними двумя позициями: тогда 10 Гц выглядят плавно.</p>
<p><strong>Позиция курсора в тексте — логическая, а не пиксельная.</strong> Относительная позиция в CRDT или пара «индекс + версия документа». Пиксели и абсолютные индексы уезжают, как только кто-то вставил абзац выше.</p>
<p><strong>Транспорт</strong> — тот же WebSocket, что и для данных, но отдельный тип сообщений; на масштабе фанаут через Redis pub/sub, потому что участники комнаты сидят на разных инстансах сервера.</p>
<p><strong>Отключение</strong> — явный <code>leave</code> на <code>pagehide</code> плюс серверный TTL: вкладки закрываются грубо, и на явное сообщение полагаться нельзя.</p>

<h4>Компромиссы</h4>
<p>Высокая частота даёт плавные курсоры и линейно растущий трафик; низкая экономит канал и требует интерполяции на клиенте. Практический ориентир — 10-15 Гц с интерполяцией, это неотличимо от 60 для глаза. Хранение presence в Redis даёт переживание рестарта инстанса и добавляет сетевой хоп в горячий путь; хранение в памяти быстрее и теряется при деплое (что терпимо: клиенты пришлют heartbeat через секунду). Показ всех курсоров честнее, но при пятнадцати участниках экран превращается в кашу — тогда переходим на стек аватаров и рисуем курсоры только активных.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Вкладку в фоне: по <code>visibilitychange</code> надо прекращать отправку курсора (пользователь его не двигает) и переводить статус в idle, но соединение держать.</li>
<li>Реконнект: после разрыва presence надо переустановить целиком, а не досылать дельту — состояние на сервере уже истекло.</li>
<li>Дубли одного пользователя: две вкладки дают два курсора с одним аватаром. Нужен clientId отдельно от userId и схлопывание в UI.</li>
<li>Приватность: режим «невидимка» и учёт прав — presence не должен раскрывать существование документа тому, кто его не видит, и не должен показывать имя пользователя, который этого не хочет.</li>
<li>Гонка курсора и правки: сообщение о позиции приходит раньше апдейта документа, к которому она относится. Позиция должна нести версию документа и применяться после неё.</li>
<li>Доступность: цветные курсоры соавторов бесполезны для скринридера — нужен список участников как текст и объявление входа-выхода.</li>
<li>Масштаб: широковещание всем всех состояний квадратично. При большом числе участников шлём агрегат (число зрителей) и детально — только активных.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Как сделать статус „печатает“?»</strong> — не отдельное событие на каждый символ, а флаг в том же heartbeat с автоматическим сбросом через 3 секунды тишины. <strong>«Как показать 500 зрителей?»</strong> — presence вырождается в счётчик плюс список нескольких активных; персональные курсоры при таком числе бессмысленны. <strong>«Что при рестарте сервера?»</strong> — клиенты переподключаются с backoff и присылают своё состояние заново; ничего восстанавливать не нужно, в этом и смысл эфемерности.</p>`,
    code: `// отправка курсора: троттлинг до 15 Гц, логическая позиция, не пиксели
const sendCursor = throttle((sel) => {
  if (document.visibilityState !== 'visible') return;
  awareness.setLocalStateField('cursor', {
    anchor: Y.createRelativePositionFromTypeIndex(ytext, sel.anchor),
    head: Y.createRelativePositionFromTypeIndex(ytext, sel.head),
    docVersion: doc.clientID,
  });
}, 66);

// приём: интерполируем между двумя последними позициями
function renderRemoteCursor(id, next) {
  const prev = lastKnown.get(id) ?? next;
  lastKnown.set(id, next);
  animate(prev, next, 100);        // 15 Гц выглядят как 60
}

// сервер: presence живёт в Redis с TTL, а не в основной БД
await redis.hset('room:' + roomId, clientId, JSON.stringify(state));
await redis.expire('room:' + roomId, 60);
await redis.publish('presence:' + roomId, payload);   // фанаут между инстансами

// уход: явный leave не гарантирован, поэтому TTL обязателен
addEventListener('pagehide', () => {
  navigator.sendBeacon('/presence/leave', JSON.stringify({ roomId, clientId }));
});`,
    tip: 'Главный принцип назови отдельной фразой: «эфемерное состояние не смешивается с персистентными данными — разные каналы, разное хранилище, разные гарантии». Это экономит трафик, место и, что важнее, избавляет от вопроса «а что делать с курсорами при откате версии документа».' },

  { id: 'sdx29',
    q: 'Как ты выстраиваешь слоистую архитектуру фронтенда? Что такое Feature-Sliced Design?',
    a: `<h4>Коротко</h4>
<p>Смысл слоёв — <strong>однонаправленные зависимости</strong>: код нижнего уровня не знает про верхний, поэтому изменение фичи физически не может сломать общий модуль. Feature-Sliced Design — конкретная реализация этой идеи для фронтенда с фиксированной вертикалью слоёв и правилом «импортировать можно только вниз».</p>

<h4>Как это работает</h4>
<p>FSD задаёт вертикаль <code>app → pages → widgets → features → entities → shared</code>. Слой импортирует только нижележащие; внутри одного слоя слайсы не импортируют друг друга — это второе правило, и именно его нарушают чаще всего. Каждый слайс наружу торчит только через публичный API (<code>index.ts</code>), внутренности разложены по сегментам <code>ui</code>, <code>model</code>, <code>api</code>, <code>lib</code>.</p>
<p>Практически это выглядит так: <code>entities/user</code> знает про модель пользователя и умеет её отрисовать; <code>features/user-block</code> реализует действие над ней; <code>widgets/user-card</code> собирает их вместе; <code>pages</code> компонует виджеты. Взаимодействие между фичами идёт через вышестоящий слой или события, а не прямым импортом соседа — иначе вертикаль превращается в граф.</p>
<p>Ключевая часть, без которой всё разваливается: правило должно проверяться автоматически. <code>eslint-plugin-boundaries</code>, <code>dependency-cruiser</code> или Nx-теги в CI. Договорённость, которая держится на памяти ревьюеров, умирает за квартал.</p>

<h4>Почему так</h4>
<p>Альтернатива — «папки по типу файла» (components, hooks, utils, api). На двадцати файлах это работает, на двухстах перестаёт что-либо объяснять: чтобы удалить фичу, нужно обойти шесть папок и угадать, что ещё этим пользуется. Слои решают две конкретные боли: неконтролируемые циклические зависимости и невозможность удалить или перенести фичу целиком.</p>
<p>Платим церемонией: больше папок, больше файлов <code>index.ts</code>, споры о том, фича это или сущность, и болезненные переносы, когда классификация оказалась неверной. На маленьком проекте эта цена выше пользы.</p>

<h4>Подводные камни</h4>
<ul>
<li>Бизнес-логика в <code>shared</code>: shared обязан быть доменно-нейтральным, иначе он становится вторым приложением, от которого зависят все.</li>
<li>Барели на всё: <code>index.ts</code>, реэкспортирующий сотню модулей, ломает tree-shaking и раздувает бандл — публичный API должен быть узким.</li>
<li>Формальное следование: сорок слайсов-однофайловиков в <code>entities</code> ради «правильности» — это не архитектура, а бюрократия.</li>
<li>Циклы между слайсами одного слоя: формально импорт «не вверх», а по сути связность та же самая.</li>
<li>Отсутствие линтера границ: без CI-проверки за квартал появляются глубокие импорты в чужие внутренности.</li>
<li>Попытка переехать на FSD одним большим PR — он не пройдёт ревью и не смержится.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Бизнесу не продаётся «правильная архитектура». Продаётся предсказуемость сроков: сейчас правка в одном месте требует изменений в трёх и приводит к регрессиям в четвёртом; после разделения границы фичи изменение локально, и оценка перестаёт врать вдвое. Приведи измеримое: число файлов, которые в среднем меняет один PR, доля багов в чужих модулях после релиза, время на онбординг нового человека.</p>
<p>Команде продаётся иначе: не «давайте перепишем по FSD», а «давайте новый код класть по этим правилам и включим линтер только для новых путей». Постепенная миграция по касанию: трогаешь модуль — переносишь его. Плюс ADR с решением и явно названными альтернативами, чтобы через полгода спор не начался заново.</p>

<h4>Что спросят следом</h4>
<p><strong>«Чем это отличается от чистой архитектуры?»</strong> — FSD по сути её фронтовая адаптация: те же однонаправленные зависимости и порты-адаптеры, но с готовым словарём слоёв и без обязательной инверсии зависимостей везде. <strong>«Что делать, если фиче нужна другая фича?»</strong> — поднимать композицию на уровень выше (widget или page) либо общаться событиями; прямой импорт соседа запрещён именно потому, что он делает фичи неудаляемыми. <strong>«Нужен ли FSD в маленьком проекте?»</strong> — нет: <code>features</code> плюс <code>shared</code> достаточно, лишние слои дороже пользы.</p>`,
    code: `// правило границ, проверяемое в CI (dependency-cruiser)
module.exports = {
  forbidden: [
    {
      name: 'no-upward-imports',
      severity: 'error',
      from: { path: '^src/(entities|shared)' },
      to:   { path: '^src/(features|widgets|pages|app)' },
    },
    {
      name: 'no-cross-slice',           // внутри слоя слайсы не знают друг друга
      severity: 'error',
      from: { path: '^src/features/([^/]+)/' },
      to:   { path: '^src/features/(?!$1)([^/]+)/' },
    },
    {
      name: 'public-api-only',          // никаких глубоких импортов
      severity: 'error',
      from: {},
      to: { path: '^src/(entities|features)/[^/]+/(?!index)' },
    },
    { name: 'no-cycles', severity: 'error', from: {}, to: { circular: true } },
  ],
};

// узкий публичный API вместо барреля на всё
// src/features/user-block/index.ts
export { UserBlockButton } from './ui/UserBlockButton';
export { useBlockUser } from './model/useBlockUser';
// внутренности (api/, lib/, store) наружу не выходят`,
    tip: 'Не пересказывай список слоёв — его знают все. Назови боль: «нельзя удалить фичу, не обойдя половину репозитория» и «циклические зависимости, о которых узнаём по сломанной сборке». И сразу добавь, что правило без линтера в CI — это не архитектура, а пожелание.' },

  { id: 'sdx30',
    q: 'Как ты определяешь границы модулей и контролируешь зависимости в большом проекте?',
    a: `<h4>Коротко</h4>
<p>Граница модуля проходит там, где <strong>меняются причины изменения</strong> — это SRP, поднятый на уровень модулей. Рабочий критерий проверки: модуль должен удаляться целиком вместе с фичей, одним движением. Если удаление тянет правки в пяти местах, граница проведена неправильно.</p>

<h4>Как это работает</h4>
<p>Технически граница — это публичный API плюс запрет на глубокие импорты. Внутренние файлы модуля недоступны снаружи: в обычном репозитории это обеспечивает ESLint-правило, в монорепо — поле <code>exports</code> в package.json, которое рантайм и сборщик соблюдают физически.</p>
<p>Дальше нужен контроль в CI. <code>dependency-cruiser</code> описывает правила «слой A не может импортировать слой B» и детектит циклы; Nx делает то же самое через теги проектов; для точечных запретов достаточно <code>no-restricted-imports</code>. Проверка обязана падать в CI, а не выводить предупреждение — предупреждения не читают.</p>
<p>Наблюдение за здоровьем графа ведётся метриками: fan-in (сколько модулей зависит от этого), fan-out (от скольких зависит он), число циклов, глубина графа. Растущий fan-out — сигнал, что модуль превращается в «бога» и его пора делить. Растущий fan-in у модуля, который часто меняется, — сигнал будущей боли: каждое изменение задевает всех.</p>
<p>Когда общему модулю нужна конкретная реализация, применяется инверсия зависимостей: нижний слой объявляет интерфейс, верхний внедряет реализацию. Так сущность не тянет за собой транспортный слой и остаётся тестируемой.</p>

<h4>Почему так</h4>
<p>Границы нужны не ради чистоты, а ради возможности менять систему по частям. Без них любой рефакторинг становится проектом: непонятно, кто чем пользуется, и любое изменение требует полного регресса. Цена границ — индирекция и споры о том, куда что положить, плюс реальные ситуации, когда правильная с точки зрения архитектуры граница мешает сделать простую вещь быстро.</p>
<p>Отдельный принцип: общий код выносится не раньше третьего повторения. Преждевременное обобщение создаёт связность, которая хуже дублирования, потому что дублирование легко удалить, а неправильную абстракцию — нет.</p>

<h4>Подводные камни</h4>
<ul>
<li>Барель-файлы, реэкспортирующие всё: они ломают tree-shaking, замедляют сборку и создают циклы, которых не видно в коде.</li>
<li>Модуль <code>utils</code>: у него нет причины изменения, поэтому он собирает всё подряд и получает fan-in по всему проекту.</li>
<li>Границы, которые проверяет только ревьюер: держатся ровно до первого горящего релиза.</li>
<li>Инверсия зависимостей ради инверсии: интерфейс с единственной реализацией, которая никогда не сменится, — это лишний слой.</li>
<li>Циклы, спрятанные за динамическим импортом: линтер их не видит, а проблема остаётся.</li>
<li>Границы, не совпадающие с организационной структурой: если модуль трогают три команды, конфликты неизбежны независимо от качества кода.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Аргумент для бизнеса — стоимость изменений и риск релиза. Конкретно: «сейчас средний PR трогает 14 файлов в 5 модулях, поэтому регресс нужен полный и релиз занимает два дня; после разделения изменение локально, тестируется точечно и катится за час». Или через инциденты: «последние три отката были вызваны изменениями в общем модуле, который трогают все».</p>
<p>Команде продаётся через уменьшение боли, а не через принципы: линтер границ ловит ошибку в PR за секунды вместо часового спора на ревью, а CODEOWNERS на границах делает понятным, кого звать. Важно не вводить правила задним числом на весь легаси: включаем проверку для новых путей и постепенно расширяем зону покрытия, иначе первый же красный CI на чужом старом коде похоронит инициативу.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как понять, что модуль надо разделить?»</strong> — по метрикам (растущий fan-out, частота изменений вместе с другими модулями) и по факту: если два человека постоянно конфликтуют в одном файле, там проходит невыявленная граница. <strong>«Дублировать или обобщать?»</strong> — правило трёх плюс проверка, что повторения меняются по одной причине; два похожих куска, меняющиеся независимо, обобщать нельзя. <strong>«Как это связано с командами?»</strong> — закон Конвея: границы модулей стоит выравнивать по границам команд, иначе архитектура будет постоянно сопротивляться организации.</p>`,
    code: `// физическая граница в монорепо: exports закрывает внутренности рантаймом
// packages/orders/package.json
{
  "name": "@app/orders",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/index.ts"
  }
}
// import { x } from '@app/orders/src/model/internal'  ->  ошибка резолвинга

// инверсия зависимостей: entity не знает про транспорт
// entities/order/model/ports.ts
export interface OrderGateway {
  load(id: string): Promise<Order>;
  save(order: Order): Promise<void>;
}

// features/order-edit внедряет реализацию, entity остаётся тестируемой
const gateway: OrderGateway = createHttpOrderGateway(apiClient);

// метрики графа как часть CI-отчёта, а не разовое любопытство
// npx depcruise src --output-type metrics
// module                     fan-in  fan-out
// src/shared/lib/utils          87       12   <- кандидат на разделение
// src/entities/order            23        3`,
    tip: 'Фраза «дублирование дешевле неправильной абстракции» звучит зрело только с продолжением: обобщаем на третьем повторении и только если повторения меняются по одной причине. Второй сильный ход — назвать метрику: fan-in/fan-out модуля в CI-отчёте превращает спор о вкусе в наблюдаемый показатель.' },

  { id: 'sdx31',
    q: 'Когда монорепо оправдано? Как устроены кеширование сборок и версионирование пакетов?',
    a: `<h4>Коротко</h4>
<p>Монорепо оправдано, когда есть <strong>общий код и потребность в атомарных изменениях через несколько пакетов</strong>: правку дизайн-системы и всех её потребителей делаешь одним PR, а не цепочкой из пяти релизов и недели ожидания. Цена — CI, который нужно уметь готовить: без affected-графа и кеша он превращается в часовой прогон на каждую опечатку.</p>

<h4>Как это работает</h4>
<p><strong>Кеширование сборок.</strong> Инструмент считает хеш входов задачи: исходники проекта и его зависимостей, конфиги, версия тулчейна, переменные окружения, влияющие на результат. Если такой хеш уже собирался, задача не выполняется — из кеша достаются артефакты и даже сохранённый stdout. Remote cache расширяет это на всю команду: сборка, сделанная на машине разработчика или в предыдущем прогоне CI, переиспользуется всеми.</p>
<p><strong>Affected.</strong> Из графа зависимостей проектов вычисляется, какие проекты затронуты изменениями относительно merge-base, и задачи запускаются только для них. Без этого монорепо линейно теряет скорость по мере роста.</p>
<p><strong>Инструменты.</strong> Turborepo проще и хорош как ускоритель существующего репозитория: хеширование, локальный и remote cache, минимум навязанных решений. Nx — платформа: граф проектов, генераторы, enforced module boundaries по тегам, плагины под фреймворки. Nx окупается в крупных организациях и стоит дороже во внедрении и в связанности с его моделью.</p>
<p><strong>Версионирование.</strong> Внутренние потребители живут на <code>workspace:*</code>, semver для них не нужен. Для публикуемых пакетов — changesets: разработчик прикладывает к PR файл с описанием изменения и типом бампа, бот собирает release PR с версиями и changelog. Fixed-версионирование (все пакеты двигаются вместе) проще и шумит лишними релизами; independent точнее и сложнее в поддержке.</p>

<h4>Почему так</h4>
<p>Монорепо решает конкретную боль: в polyrepo изменение общего пакета требует релиза, обновления зависимости у каждого потребителя и синхронизации команд — это дни. Взамен монорепо приносит свои проблемы: медленный CI без правильного тулинга, права доступа на уровне папок вместо репозиториев, огромный git-репозиторий и необходимость дисциплины владения кодом.</p>
<p>Альтернатива честная — polyrepo с автоматическим обновлением зависимостей (Renovate) и хорошим semver. Выбор определяется вопросом: что вы делаете чаще — меняете общий код или релизите независимо.</p>

<h4>Подводные камни</h4>
<ul>
<li>Нестабильные входы кеша: таймстемп сборки, git sha в бандле, абсолютные пути. Hit rate падает до нуля, и все считают, что «кеш не работает».</li>
<li>Секреты и переменные окружения в хеше: если <code>API_URL</code> влияет на артефакт, но не входит в ключ, из кеша прилетит сборка с чужим адресом.</li>
<li>Affected от неверной базы: сравнение с <code>main</code> вместо merge-base после долгой ветки запускает пересборку всего.</li>
<li>Общий <code>node_modules</code> с hoisting: пакет случайно импортирует зависимость, которую не объявил, и падает при публикации.</li>
<li>Отсутствие CODEOWNERS: в монорепо любой может изменить что угодно, и ревью превращается в лотерею.</li>
<li>Кеш без инвалидации по версии тулчейна: обновили Node, а из кеша приходят артефакты, собранные старым.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Считайте в деньгах и во времени ожидания. «CI 22 минуты, 40 прогонов в день — это 15 часов ожидания команды ежедневно; remote cache и affected сокращают средний прогон до 6 минут». Или через lead time: сколько сейчас проходит от правки в дизайн-системе до её появления в проде — если три дня из-за цепочки релизов, монорепо превращает это в один PR.</p>
<p>Команде важно другое: миграция не должна быть большим взрывом. Начинают с объединения двух-трёх связанных репозиториев с сохранением истории (<code>git subtree</code> или <code>git-filter-repo</code>), включают Turborepo как ускоритель поверх существующих скриптов и только потом обсуждают Nx. Обратный путь тоже стоит проговорить: из монорепо можно выделить пакет обратно, и то, что решение обратимо, снимает большую часть возражений.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как не сломать hit rate?»</strong> — детерминированные сборки: никаких таймстемпов и абсолютных путей в артефактах, версия тулчейна в ключе, явный список переменных окружения, входящих в хеш. <strong>«Turborepo или Nx?»</strong> — Turborepo, если нужна скорость на существующем репозитории; Nx, если нужны границы модулей, генераторы и единая модель на десятки проектов. <strong>«Как делать breaking change в общем пакете?»</strong> — в монорепо это один PR с codemod по всем потребителям, и именно это его главное преимущество.</p>`,
    code: `// turbo.json: явные входы и выходы задачи определяют качество кеша
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "package.json", "tsconfig.json", "!**/*.test.ts"],
      "outputs": ["dist/**"],
      "env": ["NODE_ENV", "NEXT_PUBLIC_API_URL"]   // влияет на артефакт -> в ключ
    },
    "test": { "dependsOn": ["build"], "outputs": ["coverage/**"] },
    "dev": { "cache": false, "persistent": true }
  }
}

// CI: только затронутые проекты, база — merge-base, а не main
// turbo run build test --filter=...[origin/main]
// nx affected -t build test --base=$(git merge-base origin/main HEAD)

// changesets: описание изменения приезжает вместе с кодом
// .changeset/olive-cats-smile.md
// ---
// "@app/ui": minor
// "@app/tokens": patch
// ---
// Button: добавлен variant="ghost", токен color-action-ghost

// убийца hit rate, который встречается в каждом втором репозитории
// define: { __BUILD_TIME__: JSON.stringify(Date.now()) }  <- хеш всегда новый`,
    tip: 'Назови цифру: «CI 22 минуты × 40 прогонов в день = 15 часов ожидания команды ежедневно». Затем добавь, что remote cache и affected дают больший выигрыш, чем любая оптимизация бандлера, и что самая частая причина неработающего кеша — таймстемп или git sha, попавшие в артефакт.' },

  { id: 'sdx32',
    q: 'Как выстроить контракт с бэкендом? Что даёт кодогенерация?',
    a: `<h4>Коротко</h4>
<p>Контракт должен быть <strong>машиночитаемым и единственным</strong>: OpenAPI, GraphQL-схема или protobuf в общем месте, а не «Иван скинул пример JSON в чате». Из схемы генерируются типы и клиент, поэтому несовместимое изменение бэкенда ломает сборку в CI, а не пользователя в проде.</p>

<h4>Как это работает</h4>
<p>Схема живёт в репозитории (своём или общем) и версионируется. Из неё генерируются типы и клиент: <code>openapi-typescript</code> плюс orval для REST, graphql-codegen для GraphQL, ts-proto для protobuf. Генерация запускается в CI и в pre-commit, сгенерированный код коммитится — иначе разработчик без свежего запуска генератора видит другие типы, чем CI.</p>
<p>В пайплайне бэкенда стоит diff-проверка схемы на breaking changes (<code>oasdiff</code>, graphql-inspector): удаление поля, сужение типа, новое обязательное поле в запросе — всё это ломает клиентов и должно требовать явного решения, а не проезжать молча.</p>
<p>Кодоген даёт типы, но не гарантирует, что сервер действительно отдаёт то, что обещал. Поэтому на границе добавляется рантайм-валидация (zod, valibot) — минимум для критичных ответов, и контрактное тестирование (Pact) там, где команды релизятся независимо: потребитель описывает ожидания, провайдер прогоняет их у себя в CI.</p>
<p>Моки тоже растут из схемы: MSW плюс генератор данных даёт фронту работающее API до готовности бэкенда, причём на правильной форме данных, а не на выдуманной.</p>

<h4>Почему так</h4>
<p>Ручные интерфейсы всегда расходятся с реальностью: их правят при первом столкновении с багом и забывают при втором. Ошибка при этом обнаруживается в рантайме у пользователя. Схема как источник правды переносит обнаружение на стадию сборки, где оно стоит минуты вместо инцидента.</p>
<p>Платим за это связностью с генератором и тем, что сгенерированные типы часто неудобны: длинные имена, лишняя вложенность, <code>| undefined</code> там, где по факту всегда есть значение. Поэтому генерируем типы, но пишем свой доменный слой поверх — смена генератора тогда переписывает один модуль, а не всё приложение.</p>

<h4>Подводные камни</h4>
<ul>
<li>Схема, которая не соответствует реальному коду бэкенда: если она пишется руками отдельно, она врёт. Схема должна генерироваться из кода или проверяться тестами.</li>
<li>Слепая вера в типы: <code>as</code> и отсутствие рантайм-проверки означают, что ошибка бэкенда обернётся падением в неожиданном месте.</li>
<li>Формат ошибок не описан в контракте: типы ответов сгенерированы, а обработка ошибок остаётся «парсим строку сообщения». Нужен единый формат (RFC 7807 problem+json) с машинным кодом.</li>
<li>Обратная совместимость: добавлять поля можно, удалять — только через депрекацию с окном и метрикой использования. Иначе сломается старая вкладка и мобильное приложение прошлой версии.</li>
<li>Сгенерированный клиент, торчащий во всех компонентах: смена версии генератора превращается в тысячестрочный PR.</li>
<li>Моки, которые расходятся со схемой: если они написаны руками, тесты зелёные, а прод красный.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Для менеджера аргумент не «типобезопасность», а время и баги. Конкретно: сколько инцидентов за квартал было вызвано расхождением ожиданий фронта и бэка, сколько часов ушло на выяснение «а какое поле теперь приходит», сколько раз фронт ждал готовности бэкенда вместо работы на моках. Контракт превращает это в проверку в CI и в параллельную разработку — то есть в сокращение времени поставки, а не в инженерное украшение.</p>
<p>С бэкенд-командой договариваться проще всего через симметричную выгоду: diff-проверка защищает и их тоже — они узнают, что ломают клиента, до релиза, а не из инцидента. Начинать стоит с одного критичного домена, показать результат, потом расширять. Требовать «сначала опишите всё API в OpenAPI» — верный способ не начать никогда.</p>

<h4>Что спросят следом</h4>
<p><strong>«Кодоген или zod вручную?»</strong> — не альтернатива: кодоген для типов и клиента, zod для валидации на границе критичных данных; писать типы руками при существующей схеме бессмысленно. <strong>«Что делать, если бэкенд не хочет OpenAPI?»</strong> — начать со своей схемы, генерируемой из реальных ответов, и контрактных тестов; это создаёт давление фактами. <strong>«Как выкатывать breaking change?»</strong> — expand/contract: добавили новое поле, перевели клиентов, дождались нулевого использования старого по метрике, удалили.</p>`,
    code: `// генерация типов и клиента из схемы, свой доменный слой поверх
// package.json
// "api:gen": "openapi-typescript ./schema/openapi.yaml -o ./src/shared/api/schema.d.ts"

// сгенерированный клиент не торчит наружу — его оборачивает домен
import type { paths } from './schema';
type OrderDto = paths['/orders/{id}']['get']['responses']['200']['content']['application/json'];

export async function loadOrder(id: string): Promise<Order> {
  const dto = await http.get<OrderDto>('/orders/' + id);
  return OrderSchema.parse(toDomain(dto));   // рантайм-проверка на границе
}

// единый формат ошибок как часть контракта (RFC 7807)
// {
//   "type": "https://api.app/errors/insufficient-funds",
//   "title": "Недостаточно средств",
//   "status": 409,
//   "code": "INSUFFICIENT_FUNDS",        // машинный код, по нему мапим текст
//   "errors": [{ "field": "amount", "code": "max_exceeded" }]
// }

// CI бэкенда: ломающее изменение схемы не проезжает молча
// oasdiff breaking --base main/openapi.yaml --revision openapi.yaml --fail-on ERR`,
    tip: 'Для менеджера переводи это в цифры: число интеграционных багов за квартал и время ожидания бэкенда. Для бэкенд-команды — в симметричную выгоду: diff-проверка сообщает им, что они ломают клиента, до релиза. Аргумент «так правильнее» не работает ни с кем.' },

  { id: 'sdx33',
    q: 'GraphQL, REST или tRPC — как выбираешь?',
    a: `<h4>Коротко</h4>
<p>Выбор определяется двумя вопросами: <strong>кто владеет формой данных и сколько у вас разнородных клиентов</strong>. Один клиент и одна команда на TypeScript — tRPC. Много клиентов с разными потребностями в данных — GraphQL. Публичный API, где важен HTTP-кеш и простота интеграции — REST.</p>

<h4>Как это работает</h4>
<p><strong>REST</strong> — ресурсная модель поверх HTTP: кешируется браузером и CDN штатными механизмами (<code>ETag</code>, <code>Cache-Control</code>), отлаживается через curl, понятен любому интегратору. Расплата — over-fetching (приходит больше, чем нужно) и under-fetching, из которого рождается вотерфолл: чтобы собрать экран, клиент делает три последовательных запроса.</p>
<p><strong>GraphQL</strong> переносит владение формой ответа на клиент: один запрос описывает ровно нужные поля и связи, экран собирается за один раунд-трип. Схема строго типизирована, федерация позволяет собрать один граф из нескольких сервисов. Расплата — HTTP-кеш почти не работает (всё POST на один URL), появляется проблема N+1 на резолверах, авторизация уезжает на уровень полей, а клиентская библиотека с нормализованным кешем весит десятки килобайт.</p>
<p><strong>tRPC</strong> даёт end-to-end типы без кодогена: фронт импортирует тип роутера прямо из бэкенда, и переименование поля моментально краснеет в IDE. Оверхед почти нулевой. Условие — фронт и бэк в одном TypeScript-репозитории; для мобильного приложения или внешнего интегратора это не работает.</p>

<h4>Почему так</h4>
<p>Все три решают одну задачу — доставку данных на клиент — но по-разному распределяют сложность. REST кладёт её на клиента (собери экран сам), GraphQL — на сервер (реши произвольный запрос эффективно), tRPC — на организацию (живите в одном репозитории и на одном языке). Правильный выбор — тот, чья сложность вам по карману операционно, а не тот, что элегантнее на диаграмме.</p>
<p>Гибрид совершенно нормален и встречается чаще чистых вариантов: REST или gRPC между сервисами, GraphQL или BFF на границе фронта, tRPC во внутренней админке.</p>

<h4>Подводные камни</h4>
<ul>
<li>GraphQL без persisted queries и лимитов глубины и сложности — это открытая дверь для DoS: один вложенный запрос кладёт базу.</li>
<li>N+1 в резолверах: без DataLoader список из 50 элементов превращается в 51 запрос к базе.</li>
<li>Кеширование GraphQL: браузерный и CDN-кеш выключены по умолчанию; APQ с GET возвращает часть возможностей, но требует настройки.</li>
<li>tRPC и версионирование: у внутреннего API нет версий, и это удобно ровно до появления мобильного приложения, которое обновляется не сразу.</li>
<li>REST и вотерфолл: три последовательных запроса на мобильной сети с RTT 200 мс — это 600 мс пустого ожидания; лечится агрегирующим эндпоинтом или BFF.</li>
<li>Авторизация в GraphQL: проверка на уровне полей легко превращается в дырявое решето, если её пишут в резолверах вручную.</li>
</ul>

<h4>Как продать выбор команде и бизнесу</h4>
<p>Решение подаётся не как «выберем GraphQL», а как ответ на бизнес-вопрос. Например: «у нас три клиента с разными экранами, и каждый новый экран требует нового эндпоинта и релиза бэкенда — это две недели задержки на фичу; GraphQL убирает эту зависимость, но добавляет дежурство за сложностью запросов и требует человека, который это умеет». Дальше — честная стоимость эксплуатации: кто будет настраивать persisted queries, кто следит за N+1, что делать, если этот человек уйдёт.</p>
<p>Внутри команды помогает прототип с таймбоксом: неделя на реальный экран на обоих вариантах и замер того, что важно — время разработки, вес бандла, число запросов, латентность. Решение фиксируется в ADR с отвергнутыми альтернативами, иначе через полгода спор начнётся заново без новых аргументов.</p>

<h4>Что спросят следом</h4>
<p><strong>«А если клиентов много, но GraphQL некому эксплуатировать?»</strong> — BFF на REST под каждый клиент: дешевле в поддержке, решает ту же проблему over-fetching, ценой дублирования. <strong>«Как кешировать GraphQL?»</strong> — persisted queries с GET и нормализованный клиентский кеш; полноценного CDN-кеша по-прежнему не будет. <strong>«Можно ли перейти с REST на GraphQL постепенно?»</strong> — да, шлюз поверх существующего REST, но тогда N+1 просто переезжает внутрь шлюза, и выигрыш будет только в форме ответа.</p>`,
    code: `// один экран, три подхода — видно, кто платит за форму ответа

// REST: вотерфолл, если нет агрегирующего эндпоинта
const user = await api.get('/users/' + id);
const orders = await api.get('/users/' + id + '/orders?limit=5');
const bonus = await api.get('/loyalty/' + user.loyaltyId);   // 3 RTT

// GraphQL: один раунд-трип, форму выбирает клиент
const QUERY = gql(
  'query Profile($id: ID!) {' +
  '  user(id: $id) { name avatarUrl' +
  '    orders(last: 5) { id total status }' +
  '    loyalty { tier points } } }'
);

// tRPC: типы приходят прямо из бэкенда, кодогена нет
const profile = await trpc.user.profile.query({ id });   // тип выведен

// обязательная гигиена GraphQL в проде
app.use(depthLimit(8), costAnalysis({ maximumCost: 1000 }), persistedQueries());
const orderLoader = new DataLoader((ids) => ordersByUserIds(ids));  // против N+1`,
    tip: 'Заканчивай ответ критерием, а не выбором: «сколько у нас типов клиентов и кто платит за изменение формы ответа — фронт, бэк или интегратор». И добавь операционную честность: GraphQL без persisted queries, лимитов сложности и DataLoader в проде — это не GraphQL, а инцидент, который ещё не случился.' },

  { id: 'sdx34',
    q: 'Когда нужен BFF-слой и какие у него минусы?',
    a: `<h4>Коротко</h4>
<p>BFF (Backend for Frontend) нужен, когда фронтенд вынужден делать <strong>оркестрацию за бэкенд</strong>: собирать экран из пяти микросервисов, обрезать лишние поля, склеивать разные форматы и прятать секреты. BFF переносит эту работу на сервер: один запрос с клиента вместо пяти, меньше вотерфолла, меньше трафика на мобильных. Второй сильный кейс — безопасность токенов, третий — независимость от релизного цикла платформенных команд.</p>

<h4>Как это работает</h4>
<p>BFF — тонкий сервис, принадлежащий фронтенд-команде, который стоит между браузером и доменными сервисами. Клиент ходит в один эндпоинт, BFF параллельно опрашивает нужные сервисы, собирает ответ в форме, удобной именно этому экрану, и отдаёт его. Ключевое слово — параллельно: три запроса по 80 мс превращаются в 80 мс, а не в 240.</p>
<p>Для безопасности используется BFF-паттерн OAuth: токены доступа и refresh живут на сервере, в браузер уходит только httpOnly-кука сессии. Тогда XSS не приводит к краже токена, и не нужно решать вечный спор «где хранить JWT».</p>
<p>Технически это чаще всего Node: route handlers в Next.js, NestJS или Fastify. И к нему применимы те же требования, что к любому бэкенду: таймауты на каждый исходящий вызов, ретраи только для идемпотентных операций, circuit breaker, трейсинг с прокидыванием <code>traceparent</code>, health-check и внятные логи.</p>

<h4>Почему так</h4>
<p>Без BFF работа по сборке экрана всё равно делается — просто в браузере, на слабом устройстве и по медленной сети, и повторяется у каждого клиента отдельно. BFF переносит её туда, где сеть быстрая, а CPU дешёвый.</p>
<p>Взамен появляется ещё один сервис: его надо деплоить, мониторить, дежурить по нему и держать в актуальном состоянии. Для команды, которая раньше не эксплуатировала серверы, это заметное изменение. И появляется риск размножения логики: BFF под web, под iOS, под Android — и в каждом свой вариант одного и того же правила.</p>

<h4>Подводные камни</h4>
<ul>
<li>BFF, разрастающийся в бизнес-слой: правило простое — агрегация и адаптация да, бизнес-правила нет. Бизнес-логика остаётся в доменных сервисах, иначе появляется второй нетестируемый бэкенд.</li>
<li>Владение чужой командой: если BFF принадлежит платформенной команде, теряется весь смысл — фронт снова ждёт чужого релиза.</li>
<li>Отсутствие таймаутов: один медленный сервис вешает весь экран. Нужны таймауты и частичная деградация — отдаём то, что успели собрать.</li>
<li>BFF как единая точка отказа: он ложится — не работает ничего. Нужны те же SLO и алерты, что у любого сервиса.</li>
<li>Дублирование между платформами: общую часть выносим в библиотеку, а не копируем в три сервиса.</li>
<li>Забытый кеш: BFF — идеальное место для кеширования и дедупликации запросов, и это часто не делают вовсе.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Бизнесу говорим про скорость поставки и про пользовательские метрики. Скорость: «каждый новый экран сейчас требует изменения в платформенном сервисе, это две недели ожидания в чужом бэклоге; с BFF фронт-команда делает это сама за день». Метрики: «главный экран собирается пятью последовательными запросами, LCP на 4G — 3.8 секунды; агрегация даёт один запрос и укладывается в 1.5».</p>
<p>Возражение «ещё один сервис в поддержке» снимается честным расчётом: сколько стоит дежурство и деплой против того, сколько сейчас стоит ожидание чужих релизов. И важный аргумент, который часто забывают: если у вас уже есть Next.js-сервер для SSR, BFF получается почти бесплатно — инфраструктура и дежурство уже существуют, добавляются только route handlers. Это радикально меняет расчёт и обычно закрывает спор.</p>

<h4>Что спросят следом</h4>
<p><strong>«Чем BFF отличается от API Gateway?»</strong> — gateway общий и занимается маршрутизацией, аутентификацией, лимитами; BFF специфичен для одного клиента и занимается формой данных. Они сосуществуют. <strong>«Один BFF на все платформы или по одному на каждую?»</strong> — по одному, иначе он превращается в общий API с флагами «а для мобильного отдай без картинок»; общую часть выносим в библиотеку. <strong>«Альтернативы?»</strong> — GraphQL-шлюз с федерацией или edge-функции; сравнивать надо по стоимости эксплуатации, а не по элегантности.</p>`,
    code: `// BFF: параллельная агрегация, таймауты и частичная деградация
export async function GET(req) {
  const userId = await session.require(req);      // токен живёт на сервере

  const withTimeout = (p, ms, fallback) =>
    Promise.race([p, sleep(ms).then(() => fallback)]);

  const [profile, orders, recs] = await Promise.all([
    withTimeout(users.get(userId), 300, null),        // критично
    withTimeout(ordersSvc.recent(userId, 5), 300, []),
    withTimeout(recsSvc.forUser(userId), 150, []),    // некритично: пустой список
  ]);

  if (!profile) return Response.json({ error: 'PROFILE_UNAVAILABLE' }, { status: 503 });

  return Response.json({
    name: profile.displayName,                    // форма под конкретный экран
    orders: orders.map(toCardDto),
    recommendations: recs,
    degraded: recs.length === 0,                  // честно сообщаем о деградации
  }, { headers: { 'Cache-Control': 'private, max-age=30' } });
}

// сквозной трейс: браузер -> BFF -> сервисы
const headers = { traceparent: req.headers.get('traceparent') ?? newTraceparent() };`,
    tip: 'Самый действенный аргумент на интервью: «если у нас уже есть Next.js-сервер для SSR, BFF получается почти бесплатно — инфраструктура и дежурство уже оплачены». И сразу назови границу: BFF агрегирует и адаптирует, но бизнес-правила в нём не живут, иначе через год это второй бэкенд без тестов и без владельца.' },

  { id: 'sdx35',
    q: 'Как ты выстраиваешь обработку ошибок как систему, а не как набор try/catch?',
    a: `<h4>Коротко</h4>
<p>Система начинается с <strong>классификации</strong>: у разных категорий ошибок разные политики. Сетевые и таймауты — ретраить. 4xx — показать пользователю и не повторять. 401 — тихий refresh, 403 — «нет прав». 5xx — ретрай с backoff и деградация. Ошибки рендера — ErrorBoundary. Нарушения бизнес-правил — это вообще не ошибки, а состояния экрана. Один общий «Что-то пошло не так» означает, что классификации нет.</p>

<h4>Как это работает</h4>
<p><strong>Транспортный слой</strong> — единая точка, где живут interceptor-ы: ретраи с экспоненциальным backoff и джиттером, дедупликация одинаковых запросов в полёте, circuit breaker при массовых 5xx (не добивать лежащий бэкенд), единый разбор формата ошибки. Компоненты не знают про HTTP-коды, они получают типизированные доменные ошибки.</p>
<p><strong>Типы ошибок в домене:</strong> либо классы (<code>NetworkError</code>, <code>ForbiddenError</code>, <code>ValidationError</code>), либо Result-подобный тип, где ошибка — часть возвращаемого значения. Второе честнее для ожидаемых исходов: «недостаточно средств» — это не исключение, а результат.</p>
<p><strong>Границы в UI</strong> ставятся на нескольких уровнях: корень приложения (последний рубеж), маршрут (упал экран — остальное приложение живо), виджет (упал блок рекомендаций — страница работает). Одна граница на всё приложение равносильна её отсутствию: любая ошибка даёт белый экран.</p>
<p><strong>Наблюдаемость.</strong> Каждая необработанная ошибка уходит в мониторинг с версией релиза, идентификатором пользователя или сессии, активными фича-флагами и trace id. Без этого контекста ошибка в трекере — это строчка, по которой ничего нельзя сделать.</p>

<h4>Почему так</h4>
<p>Разрозненные try/catch дают иллюзию обработки: каждый пишет своё сообщение, половина ошибок глотается, ретраи появляются там, где их не должно быть, и не появляются там, где нужны. Системный подход переносит решения в одно место, где их можно менять централизованно и тестировать.</p>
<p>Цена — индирекция: чтобы понять, почему запрос повторился три раза, нужно идти в транспортный слой. И риск переусложнения: полноценный circuit breaker в приложении с тремя эндпоинтами избыточен.</p>

<h4>Подводные камни</h4>
<ul>
<li>Ретрай неидемпотентного POST: повтор создаёт второй заказ. Ретраить можно только с idempotency key.</li>
<li>Ретрай на 4xx: бессмысленная нагрузка; особенно вредно ретраить 403 — получается шторм от всех клиентов сразу.</li>
<li>Отсутствие джиттера: после падения бэкенда все клиенты возвращаются синхронно и роняют его повторно.</li>
<li>Парсинг человекочитаемых сообщений бэкенда: текст меняется, логика ломается. Нужен машинный код ошибки.</li>
<li>Ошибка загрузки чанка после деплоя: пользователь сидит на старой версии, файлы уже удалены. Самая частая «непонятная» ошибка в SPA — ловится и лечится перезагрузкой страницы с защитой от цикла.</li>
<li>Проглоченные промисы: <code>unhandledrejection</code> должен быть подключён, иначе целый класс ошибок невидим.</li>
<li>Тексты для пользователя, написанные разработчиком: «Ошибка 500 при выполнении запроса» не помогает никому.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Бизнесу продаётся не «обработка ошибок», а конверсия и стоимость поддержки. Конкретно: «6% попыток оформления заказа завершаются экраном ошибки; половина из них — таймауты, которые лечатся ретраем, это прямые потерянные заказы». Или: «в поддержку приходит 40 обращений в неделю с формулировкой „ничего не работает“, потому что в интерфейсе нет id ошибки — с ним разбор занимает минуту вместо получаса».</p>
<p>Команде это продаётся через сокращение рутины: единый слой означает, что новый экран не пишет свою обработку с нуля, а ревью перестаёт быть местом споров о том, где показывать тост. Внедрять стоит не большим рефакторингом, а по слоям: сначала транспорт с ретраями и единым форматом, потом границы по маршрутам, потом деградация виджетов. Каждый шаг даёт видимый эффект в метриках ошибок.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как отличить ошибку от состояния?»</strong> — по тому, ожидаема ли ситуация в нормальном сценарии: «корзина пуста» и «недостаточно средств» — состояния, они не попадают в мониторинг и не считаются в error rate. <strong>«Сколько ретраить?»</strong> — 2-3 попытки с backoff и общим бюджетом времени, потому что пользователь всё равно не ждёт дольше нескольких секунд. <strong>«Как тестировать?»</strong> — MSW со сценариями отказов, инъекция задержек и 5xx в e2e, chaos-режим в стейджинге.</p>`,
    code: `// один транспортный слой: политика ретраев зависит от категории ошибки
const RETRIABLE = new Set([408, 429, 500, 502, 503, 504]);

async function request(config, attempt = 0) {
  try {
    return await http(config);
  } catch (err) {
    const status = err.status ?? 0;

    if (status === 401) { await refreshOnce(); return request(config, attempt); }
    if (status === 403) { await session.reloadPermissions(); throw new ForbiddenError(err); }
    if (status >= 400 && status < 500 && !RETRIABLE.has(status)) throw toDomainError(err);

    const idempotent = config.method === 'GET' || config.headers['Idempotency-Key'];
    if (!idempotent || attempt >= 2) throw toDomainError(err);

    const delay = 300 * 2 ** attempt + Math.random() * 200;   // джиттер обязателен
    await sleep(delay);
    return request(config, attempt + 1);
  }
}

// chunk load error после деплоя — с защитой от цикла перезагрузок
addEventListener('vite:preloadError', (e) => {
  e.preventDefault();
  const key = 'reloaded-at';
  const last = Number(sessionStorage.getItem(key) ?? 0);
  if (Date.now() - last < 10000) return showUpdateBanner();  // не зацикливаемся
  sessionStorage.setItem(key, String(Date.now()));
  location.reload();
});

addEventListener('unhandledrejection', (e) => report(e.reason, { kind: 'unhandled' }));`,
    tip: 'Пример с chunk load error после деплоя — короткий и очень узнаваемый признак реального продакшн-опыта. Добавь к нему защиту от цикла перезагрузок: без неё «умное» решение превращается в бесконечно моргающую страницу у части пользователей.' },

  { id: 'sdx36',
    q: 'Что такое observability на фронтенде и что именно ты собираешь?',
    a: `<h4>Коротко</h4>
<p>Observability — это возможность объяснить, что произошло у конкретного пользователя, не воспроизводя баг. Три сигнала: <strong>метрики</strong> (RUM: LCP, INP, CLS, TTFB по p75, error rate, доля успешных ключевых действий), <strong>ошибки и логи</strong> (Sentry с source maps, релизом и контекстом) и <strong>трейсы</strong> (OpenTelemetry: trace id из браузера уходит в бэкенд через <code>traceparent</code>, и виден весь путь запроса до базы).</p>

<h4>Как это работает</h4>
<p><strong>Метрики.</strong> Core Web Vitals собираются библиотекой <code>web-vitals</code> и отправляются как события при уходе страницы. Смотреть надо p75, а не среднее: среднее прячет хвост, а именно хвост — это уходящие пользователи. Ориентиры: LCP ≤ 2.5 с, INP ≤ 200 мс, CLS ≤ 0.1.</p>
<p><strong>Ошибки.</strong> Обязательны source maps, привязанные к релизу (иначе стек нечитаем), контекст пользователя, версия, активные фича-флаги и последние действия (breadcrumbs). Группировка по fingerprint должна быть настроена руками для тех ошибок, где текст меняется.</p>
<p><strong>Трейсы.</strong> Браузер начинает трейс, кладёт <code>traceparent</code> в заголовок запроса, бэкенд его подхватывает. В итоге на одном экране видно: клик, запрос, время в шлюзе, время в сервисе, запрос к базе. Это единственный способ ответить на «где именно те 3 секунды».</p>
<p><strong>Связующее звено — единый корреляционный контекст:</strong> session id, user id, release и набор фича-флагов присутствуют во всех трёх сигналах. Без него метрика говорит «медленно у 3%», а найти этих 3% невозможно.</p>

<h4>Почему так</h4>
<p>Мониторинг отвечает на вопрос «всё ли в порядке», observability — на вопрос «что именно произошло». Первое строится на заранее известных метриках, второе — на возможности задать новый вопрос данным, которые уже собраны. Для фронтенда это критично, потому что среда выполнения принадлежит пользователю: вы не можете зайти на его устройство, подключить профайлер и повторить.</p>
<p>Платим объёмом данных и деньгами: полный сбор всего быстро становится дороже фичи, которую он обслуживает. Отсюда семплирование и бюджет.</p>

<h4>Подводные камни</h4>
<ul>
<li>Синтетика вместо RUM: Lighthouse на CI ловит регрессии в PR, но показывает лабораторную правду. Нужны оба, они не заменяют друг друга.</li>
<li>Отсутствие сегментации: p75 по всем сразу бесполезен. Разбивка по стране, устройству, типу сети и версии релиза — обязательна.</li>
<li>Session Replay без маскирования: это персональные данные, а иногда и платёжные. Маскирование полей и семплирование обязательны, а в некоторых юрисдикциях нужно согласие.</li>
<li>Алерты на причины вместо симптомов: «выросло число 500 в сервисе X» будит дежурного, «упала конверсия шага оплаты» описывает проблему пользователя.</li>
<li>Отсутствие бизнес-метрик фронта: успешность оформления, время до первого действия, доля сессий с ошибкой — это тоже observability, а не «продуктовая аналитика где-то отдельно».</li>
<li>Стоимость: 100% трейсов на высоконагруженном фронте — счёт, который заметят. Разумно: ошибки 100%, трейсы 1-10%, метрики агрегированно.</li>
<li>Source maps, залитые в публичный доступ: удобно и раздаёт исходники всем желающим.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Аргумент для бизнеса — время до восстановления и цена невидимых проблем. «Последний инцидент мы диагностировали 40 минут, потому что не видели, на чьей стороне задержка; со сквозным трейсом это две минуты». Или через деньги: «мы не знали, что у 8% пользователей на Android оформление падает на третьем шаге — это N заказов в месяц, обнаружено за день после подключения RUM».</p>
<p>Возражение про стоимость снимается бюджетом: называете конкретную сумму в месяц и что за неё покупается, плюс семплирование как рычаг регулировки. Внутри команды главное — не превратить это в проект «внедрим observability»: подключается по одному сигналу, начиная с ошибок с source maps (самая быстрая отдача), затем RUM, затем трейсы. И обязательно назначается владелец: дашборд без владельца перестаёт открываться через месяц.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как связать фронтовую ошибку с бэкендом?»</strong> — общий trace id: клиент генерирует <code>traceparent</code>, он попадает и в Sentry-событие, и в бэкендовые логи. <strong>«Что мерить кроме Core Web Vitals?»</strong> — свои пользовательские тайминги: время до готовности ключевого экрана к взаимодействию, длительность бизнес-операции, доля успешных её завершений. <strong>«Как понять, что релиз плохой?»</strong> — сравнение метрик по версии релиза с автоматическим алертом на деградацию, а не ручной просмотр графиков.</p>`,
    code: `// один контекст на все сигналы: без этого сигналы не связываются
const ctx = {
  sessionId: getSessionId(),
  release: __APP_VERSION__,
  flags: activeFlags(),               // вариант эксперимента объясняет половину аномалий
};
Sentry.setContext('app', ctx);

// RUM: p75 считается на бэкенде, клиент шлёт сырые значения
import { onLCP, onINP, onCLS, onTTFB } from 'web-vitals';
const send = (m) => queue.track('web_vital', {
  name: m.name, value: m.value, rating: m.rating, navType: m.navigationType,
  ...ctx,
  device: navigator.userAgent, conn: navigator.connection?.effectiveType,
});
[onLCP, onINP, onCLS, onTTFB].forEach((fn) => fn(send));

// сквозной трейс: браузер -> BFF -> сервис -> база
const traceparent = '00-' + traceId + '-' + spanId + '-01';
fetch('/api/checkout', { headers: { traceparent } });
Sentry.setTag('trace_id', traceId);   // ошибка ищется по тому же id, что и трейс

// собственная бизнес-метрика важнее любого технического графика
performance.mark('checkout:start');
// ...
performance.measure('checkout:complete', 'checkout:start');
queue.track('checkout_completed', { ms: measure.duration, ...ctx });`,
    tip: 'Фраза «мы прокидываем traceparent из браузера и видим сквозной трейс до базы» мгновенно отличает того, кто настраивал observability, от того, кто про неё читал. Второй такой маркер — упоминание, что активные фича-флаги лежат в контексте каждой ошибки: именно они объясняют большинство аномалий, которые иначе выглядят необъяснимыми.' },

  { id: 'sdx37',
    q: 'Как устроены фича-флаги и trunk-based development на фронте?',
    a: `<h4>Коротко</h4>
<p>Trunk-based development — это <strong>короткоживущие ветки и мердж в main минимум раз в день</strong>. Незавершённая функциональность прячется за фича-флагом, а не живёт в ветке месяц. Результат: исчезает ад больших мерджей, а релиз перестаёт быть событием — код уже в проде, включение отдельным действием.</p>

<h4>Как это работает</h4>
<p>Ветка живёт часы или день, PR маленький, конфликты не успевают накопиться. Незаконченная фича мерджится в main в выключенном состоянии: код есть, точка входа скрыта флагом. Раскат идёт отдельно от деплоя: 1% → 10% → 100% с наблюдением за метриками, и выключение не требует релиза.</p>
<p><strong>Типы флагов различаются по жизненному циклу,</strong> и это ключевое различение. Release toggle живёт недели и обязан быть удалён. Experiment flag живёт срок теста. Ops toggle (kill switch) живёт всегда и является частью эксплуатации. Permission toggle — доступ по тарифу или роли — вообще не флаг, а часть продукта. Смешивание этих типов в одном списке и есть главный источник флагового болота.</p>
<p><strong>Оценка флага должна быть детерминированной и доступной при SSR</strong> — иначе сервер отрендерит одно, клиент другое, и получите flicker плюс ошибку гидрации. Практически это значит: значение вычисляется на сервере или на edge и передаётся в разметку, а не запрашивается асинхронным SDK после монтирования.</p>
<p><strong>Изменения схемы данных тоже идут под флагом</strong> по паттерну expand/contract: сначала добавляем новое поле и пишем в оба, потом переключаем чтение, потом удаляем старое. Каждый шаг обратим.</p>

<h4>Почему так</h4>
<p>Долгоживущие ветки — это отложенная интеграция, а отложенная интеграция всегда стоит дороже, чем кажется: конфликты, устаревшие предположения, недельный «мердж-марафон» перед релизом. Trunk-based платит другим: нужна дисциплина, хорошее покрытие тестами (main всегда должен быть релизопригоден) и умение прятать незаконченное.</p>
<p>Флаги дают безопасность, но каждый добавляет ветвление в коде и в тестах. Десять одновременных флагов — это формально 1024 комбинации; реально тестируется единицы, и остальные существуют как непроверенные состояния продакшена.</p>

<h4>Подводные камни</h4>
<ul>
<li>Флаги без владельца и даты удаления: через год их сотня, и никто не знает, что будет, если выключить. Нужны обязательные поля и CI-проверка просроченных.</li>
<li>Флаг, размазанный по двадцати местам в коде: удалить его страшно. Точка ветвления должна быть одна, максимум две.</li>
<li>Тесты только на дефолтном значении: прод-конфигурация оказывается непокрытой. Критичные комбинации фиксируются явно.</li>
<li>Флаг, влияющий на схему БД или на формат сохранённых данных: выключение перестаёт быть безопасным, потому что данные уже записаны в новом формате.</li>
<li>Оценка флага асинхронным клиентским SDK на верхнем экране: гарантированный flicker.</li>
<li>Kill switch, который никогда не проверяли: в момент инцидента выясняется, что он не работает.</li>
<li>Trunk-based без нормального CI: main ломается, и вся команда стоит.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Бизнесу продаётся снижение риска и предсказуемость. «Раскат на 1% с автоматическим откатом по метрикам означает, что худший исход неудачного релиза — это 1% пользователей на пять минут, а не полный откат и вечер разбора». И скорость: «фича доезжает до прода по частям и включается в нужный день, а не ждёт релизного окна».</p>
<p>Команде тяжелее: trunk-based требует менять привычки, и главное сопротивление — «я не могу мерджить недоделанное». Отвечать надо конкретикой: недоделанное мерджится выключенным и не имеет точки входа в UI, а риск снимается тестами и флагом. Внедрять постепенно: сначала ограничить возраст ветки (например, три дня), потом ввести флаги для крупных фич, потом отказаться от релизных веток. И сразу вводить процесс удаления флагов, иначе через полгода команда получит болото и решит, что «флаги не работают».</p>

<h4>Что спросят следом</h4>
<p><strong>«Чем флаг отличается от эксперимента?»</strong> — флаг это операционный переключатель без статистики, эксперимент — гипотеза с посчитанным размером выборки и датой окончания; у них разный жизненный цикл и разные владельцы. <strong>«Как чистить флаги?»</strong> — обязательная дата протухания, автоматическая задача в бэклоге, CI-предупреждение, а лучше — падающая сборка после срока. <strong>«Как не сломать долгую миграцию?»</strong> — expand/contract с обратимыми шагами и метрикой использования старого пути перед удалением.</p>`,
    code: `// у флага есть тип, владелец и дата смерти — иначе он бессмертен
export const flags = {
  'checkout-v2': {
    type: 'release',                  // release | experiment | ops | permission
    owner: 'team-checkout',
    expiresAt: '2026-11-01',          // CI ругается после этой даты
    default: false,
  },
  'payments-kill-switch': {
    type: 'ops',
    owner: 'team-payments',
    expiresAt: null,                  // ops-флаги живут всегда, и это законно
    default: true,
  },
};

// CI: просроченный release-флаг ломает сборку, а не пишет предупреждение в лог
// node scripts/check-flags.js --fail-on-expired

// оценка на сервере, чтобы не было flicker и рассинхрона гидрации
export async function getServerFlags(req) {
  const ctx = { userId: req.userId, country: req.geo.country };
  return evaluateAll(flags, ctx);     // уезжает в HTML, клиент не запрашивает
}

// одна точка ветвления, а не проверки по всему коду
const Checkout = (await getFlag('checkout-v2')) ? CheckoutV2 : CheckoutV1;

// expand/contract: каждый шаг обратим
// 1) пишем в оба поля  2) читаем из нового  3) удаляем старое`,
    tip: 'Скажи прямо: «фича-флаг без даты удаления — это техдолг, который мы создали сознательно», и сразу опиши механизм: владелец, дата протухания, падающий CI после срока. Второй сильный ход — упомянуть, что kill switch надо периодически проверять учениями: непроверенный аварийный переключатель в момент инцидента обычно не работает.' },

  { id: 'sdx38',
    q: 'Какие стратегии релиза фронтенда ты используешь: canary, blue-green, поэтапный раскат?',
    a: `<h4>Коротко</h4>
<p>Фронтенд отличается от бэкенда одним свойством: <strong>у пользователя может быть открыта старая версия приложения</strong>, пока API уже новое. Поэтому любая стратегия начинается не с выбора canary или blue-green, а с иммутабельных ассетов (хеш в имени файла, вечный кеш) и отдельного версионирования HTML — тогда старая вкладка продолжает работать, а не падает с ошибкой загрузки чанка.</p>

<h4>Как это работает</h4>
<p><strong>Blue-green:</strong> два полных окружения, трафик переключается на роутере. Откат мгновенный — переключаем обратно. Цена: двойная инфраструктура и требование совместимости обеих версий с общей базой.</p>
<p><strong>Canary:</strong> 1-5% трафика идёт на новую версию, метрики (error rate, INP, конверсия ключевого шага) автоматически сравниваются с контрольной группой, при превышении порога — автоматический откат. Это самая полезная стратегия для фронта, потому что многие проблемы видны только на реальных устройствах и сетях.</p>
<p><strong>Поэтапный раскат по когортам</strong> (внутренние сотрудники → бета-пользователи → один регион → все) уместен для рискованных продуктовых изменений, где нужна не только техническая, но и продуктовая обратная связь.</p>
<p><strong>Раскат за фича-флагом отличается от раската за деплоем</strong> и почти всегда безопаснее: артефакт один, поведение переключается конфигурацией, откат не требует релиза и занимает секунды.</p>

<h4>Почему так</h4>
<p>Цель релизной стратегии — не «катить чаще», а сократить размер и время воздействия ошибки. Мелкий частый релиз с быстрым откатом даёт меньший ожидаемый ущерб, чем редкий большой, даже если вероятность ошибки в каждом релизе одинакова. Именно поэтому ключевая метрика — не частота релизов, а <strong>время до отката</strong>.</p>
<p>Платим сложностью инфраструктуры и требованием обратной совместимости API: во время canary одновременно живут две версии клиента, и бэкенд обязан обслуживать обе.</p>

<h4>Подводные камни</h4>
<ul>
<li>Мутабельные имена файлов и агрессивный кеш HTML: пользователь получает старый HTML со ссылками на удалённые чанки — классический белый экран после деплоя.</li>
<li>Удаление старых ассетов сразу после деплоя: открытые вкладки ломаются. Старые версии должны жить хотя бы сутки.</li>
<li>Отсутствие уведомления о новой версии: пользователь неделю сидит на старой сборке и получает ошибки от нового API. Нужен баннер «доступна новая версия» и мягкая перезагрузка в момент бездействия.</li>
<li>Canary без автоматического отката: человек заметит деградацию через 20 минут, автоматика — через две.</li>
<li>Canary по метрикам, которые не двигаются за час: конверсия покупки на низком трафике статистически не покажет ничего, нужны быстрые прокси-метрики.</li>
<li>Липкость сессии: пользователь при перезагрузке страницы прыгает между версиями и получает несовместимое состояние в localStorage.</li>
<li>Миграции локального состояния: новая версия читает старый формат из localStorage или IndexedDB — версия схемы обязательна.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Бизнесу говорим языком риска и денег: «сейчас откат занимает 25 минут — это 25 минут сломанного оформления заказа; с blue-green это 30 секунд». Или через частоту: «мы релизим раз в две недели, потому что релиз страшный; страх обоснован — откатываться нечем. Если сделать откат дешёвым, релизить можно ежедневно, и каждая фича доезжает до пользователя на неделю раньше».</p>
<p>Хорошо работает связка с DORA-метриками: lead time, частота деплоя, change failure rate и время восстановления — это язык, который менеджмент понимает и который позволяет сравнивать «до» и «после». Команде внедрять стоит по порядку: сначала иммутабельные ассеты и версионирование (дёшево, снимает самый частый класс ошибок), потом фича-флаги, потом canary с автоматикой. И обязательно репетиция отката: процедура, которую ни разу не выполняли, в момент инцидента не работает.</p>

<h4>Что спросят следом</h4>
<p><strong>«Что делать со старой открытой вкладкой?»</strong> — держать старые ассеты, версионировать API и показывать баннер обновления; для критичных изменений — принудительная перезагрузка при простое. <strong>«По каким метрикам откатывать canary?»</strong> — быстрые и чувствительные: JS error rate, доля неуспешных запросов, INP; конверсия слишком медленная для автоматики. <strong>«Пятничные релизы?»</strong> — это вопрос не технический, а про то, есть ли дежурство и быстрый откат; при их наличии день недели перестаёт иметь значение.</p>`,
    code: `// иммутабельные ассеты + версионированный HTML — фундамент любой стратегии
// /assets/app.9f2c1b.js      Cache-Control: public, max-age=31536000, immutable
// /index.html                Cache-Control: no-cache  (всегда сверяется с сервером)

// клиент узнаёт о новой версии и предлагает мягкое обновление
setInterval(async () => {
  const res = await fetch('/version.json', { cache: 'no-store' });
  const { version } = await res.json();
  if (version !== __APP_VERSION__) showUpdateBanner(version);
}, 5 * 60 * 1000);

// мягкая перезагрузка в момент бездействия, а не посреди заполнения формы
function softReloadWhenIdle() {
  if (hasUnsavedWork()) return;
  requestIdleCallback(() => location.reload());
}

// canary: автоматический откат по быстрым метрикам, а не по конверсии
// rollout:
//   steps: [1, 10, 50, 100]
//   analysis:
//     interval: 2m
//     metrics:
//       - name: js_error_rate     threshold: "+50%"   vs: baseline
//       - name: api_5xx_rate      threshold: "+20%"
//       - name: inp_p75           threshold: "+15%"
//     onFailure: rollback

// версия локального состояния: новая сборка не должна падать на старых данных
const STORAGE_VERSION = 4;
if (readVersion() !== STORAGE_VERSION) migrateOrReset();`,
    tip: 'Назови время до отката ключевой метрикой релизного процесса — она важнее частоты релизов и прямо задаёт допустимый риск. И добавь, что процедуру отката надо репетировать: откат, который ни разу не выполняли, в момент инцидента обнаруживает недостающий доступ или сломанный скрипт.' },

  { id: 'sdx39',
    q: 'Как ты выстраиваешь code review как процесс?',
    a: `<h4>Коротко</h4>
<p>Цель ревью — <strong>поиск дизайн-проблем и распространение знаний</strong>, а не ловля опечаток. Форматирование, стиль импортов, типы и простые ошибки ловятся форматтером, линтером, типами и тестами в CI. Если люди спорят о кавычках в комментариях к PR, сломан процесс, а не люди.</p>

<h4>Как это работает</h4>
<p><strong>Размер PR — главный рычаг.</strong> Исследования и практика сходятся: качество ревью резко падает после 200-400 изменённых строк. Большой PR получает либо «LGTM» без чтения, либо две недели обсуждения. Поэтому крупная работа разбивается на серию PR, а рефакторинг отделяется от функциональных изменений — иначе в диффе на тысячу строк невозможно найти те три, которые меняют поведение.</p>
<p><strong>Автор ведёт ревью.</strong> Описание с контекстом (зачем, а не что), скриншот или видео для UI, комментарии автора в сложных местах и явное указание, куда смотреть в первую очередь. Это экономит ревьюеру десятки минут и заметно повышает качество замечаний.</p>
<p><strong>Явная градация замечаний:</strong> префиксы <code>blocking:</code>, <code>question:</code>, <code>nit:</code>. Без них автор не понимает, что мешает мержу, а что мнение; и любой комментарий воспринимается как требование.</p>
<p><strong>SLA на первый отклик</strong> — несколько часов. PR, лежащий сутки, устаревает: базовая ветка ушла вперёд, автор переключился, контекст потерян.</p>
<p><strong>CODEOWNERS</strong> распределяет обязательных ревьюеров по границам модулей, делая владение явным и убирая вопрос «кого позвать».</p>

<h4>Почему так</h4>
<p>Ревью — самый дорогой инструмент контроля качества: оно тратит время двух старших инженеров. Поэтому всё, что можно автоматизировать, должно быть автоматизировано, а человеческое внимание — направлено туда, где машина бессильна: архитектурные решения, пропущенные краевые случаи, безопасность, читаемость для будущего сопровождающего.</p>
<p>Вторая функция ревью — распространение знаний: после хорошего ревью в контексте фичи разбираются двое, а не один. Это прямо влияет на bus factor.</p>

<h4>Подводные камни</h4>
<ul>
<li>Ревью как ворота качества вместо диалога: замечания в повелительном наклонении, спор ради победы. Тон обсуждаем код, а не человека; вопрос («что будет, если сюда придёт null?») работает лучше приговора.</li>
<li>Пересмотр архитектуры на этапе ревью: если крупное решение не согласовано заранее через RFC или ADR, автор уже потратил неделю, и любое возражение становится конфликтом.</li>
<li>Бесконечные итерации: спор длиннее двух кругов переносится в звонок на пять минут, иначе он тянется дни.</li>
<li>Метрики ревью, применяемые к людям: «число комментариев на человека» мгновенно превращается в игру. Метрики (время до первого ревью, время жизни PR, размер PR) существуют для процесса.</li>
<li>Один ревьюер на всё: он становится бутылочным горлышком и точкой отказа.</li>
<li>Отсутствие ревью у senior-ов «потому что они и так знают»: именно их изменения обычно самые рискованные.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Бизнесу ревью продаётся не качеством кода, а скоростью и риском: «время жизни PR — три дня, значит каждая фича теряет три дня на ожидании; при SLA в четыре часа мы возвращаем эту неделю в месяц». И через change failure rate: доля релизов, приводящих к откату, до и после введения обязательного ревью на критичных модулях.</p>
<p>Команде тяжелее продать дисциплину маленьких PR — она ощущается как лишняя работа. Помогает показать цифры на своих данных: средний размер PR против времени до мержа и против числа найденных проблем. Обычно график говорит сам за себя. Полезно ввести правило, снимающее напряжение: часть замечаний сознательно не блокирует мерж, а превращается в задачу — тогда ревьюер не выбирает между «промолчать» и «затормозить поставку».</p>

<h4>Что спросят следом</h4>
<p><strong>«Что делать с огромным PR, который уже написан?»</strong> — принять его нельзя ревьюить честно, поэтому либо разбить, либо провести совместный разбор голосом с записью решений; молчаливый LGTM хуже отказа. <strong>«Как ревьюить в маленькой команде?»</strong> — асинхронное ревью плюс парное программирование на сложных участках; в команде из двух человек парное часто эффективнее. <strong>«Нужно ли обязательное ревью на всё?»</strong> — нет: тексты, конфиги стендов и эксперименты могут идти по упрощённому пути; обязательность стоит держать там, где цена ошибки высока.</p>`,
    code: `// градация замечаний снимает половину конфликтов на ревью
// blocking: здесь гонка — два клика подряд создадут два заказа, нужен ключ идемпотентности
// question: почему debounce 500, а не 200? если это подбор — стоит вынести в константу
// nit: имя getData ничего не говорит; можно loadOrderSummary (не блокирует мерж)

// CODEOWNERS: владение границами модулей, а не «кого не жалко»
// /src/entities/order/     @team-checkout
// /src/features/payment/   @team-payments @security-guild
// /src/shared/ui/          @design-system

// шаблон PR, который экономит ревьюеру больше всего времени
// .github/pull_request_template.md
// ## Зачем
// ## Что изменилось (и что НЕ входит в этот PR)
// ## Как проверить
// ## Скриншот / видео
// ## Риски и что делать при откате

// метрики процесса, а не людей
// median time-to-first-review, median PR lifetime, median PR size (LOC)`,
    tip: 'Сигнал зрелости — сказать, что часть замечаний вы сознательно не блокируете, а заводите задачей: это снимает вечный конфликт между качеством и поставкой. Второй сильный ход — признать, что крупные решения обсуждаются до кода в ADR, потому что ревью — плохое место для пересмотра архитектуры: там уже потрачена неделя чужого труда.' },

  { id: 'sdx40',
    q: 'Как измерять техдолг и как продавать его исправление бизнесу?',
    a: `<h4>Коротко</h4>
<p>Бизнесу не продаётся «плохой код». Продаются <strong>деньги, риск и скорость</strong>. Значит, техдолг нужно измерить и перевести в эти три величины: сколько времени он съедает, какую вероятность инцидента создаёт, насколько замедляет поставку. Разговор «нам нужен рефакторинг» без чисел проигрывает любой фиче.</p>

<h4>Как это работает</h4>
<p><strong>Измеримые индикаторы</strong> бывают двух видов. Прямые: время сборки и CI (минуты × прогонов в день × стоимость часа команды), доля времени спринта на поддержку и багфиксы, процент багов, приходящих из конкретного модуля, число устаревших зависимостей с известными CVE. Косвенные: покрытие критичных путей тестами, число циклических зависимостей, размер бандла и Core Web Vitals, средний размер PR и время его жизни.</p>
<p><strong>DORA-метрики</strong> — самый переводимый на язык менеджмента набор: lead time (от коммита до прода), частота деплоя, change failure rate (доля релизов с откатом) и время восстановления. Долг проявляется в них раньше, чем в жалобах разработчиков.</p>
<p><strong>Реестр долга.</strong> Список записей с описанием, оценкой стоимости исправления и оценкой ежемесячных потерь от бездействия. Это превращает «мы все знаем, что там плохо» в объект, который можно приоритизировать наравне с фичами.</p>
<p><strong>Разделение типов долга.</strong> Сознательный (взяли осознанно ради срока, записали, знаем как вернуть) и случайный (появился от незнания или дрейфа). Первый обслуживается по плану, второй сначала нужно обнаружить.</p>

<h4>Почему так</h4>
<p>Менеджер не отказывает в рефакторинге из вредности — он сравнивает предложения на одной шкале, и «улучшить код» на этой шкале не имеет значения. Как только появляется расчёт («две недели работы возвращают по неделе на каждой из следующих пяти задач»), решение становится обычным инвестиционным.</p>
<p>Обратная сторона: измерение долга требует времени и честности. Легко скатиться в метрики ради метрик — покрытие 90% ничего не говорит, если покрыты геттеры. Поэтому индикаторы выбираются под конкретную проблему, а не собираются все подряд.</p>

<h4>Подводные камни</h4>
<ul>
<li>Просьба «дайте спринт на рефакторинг»: это разовое событие, после которого всё возвращается. Работает постоянный бюджет — 15-20% каждого спринта на инженерные задачи, зафиксированный как норма, а не как одолжение.</li>
<li>Рефакторинг ради рефакторинга: без привязки к предстоящим фичам он не окупается и не воспринимается всерьёз. Долг платится там, где скоро пойдёт работа.</li>
<li>Большой переписыватель: попытка исправить всё сразу заканчивается веткой на три месяца и отменой.</li>
<li>Метрики без базовой линии: «покрытие выросло до 70%» ничего не значит без «было 65% и вот что это дало».</li>
<li>Молчание о сознательном долге: если срез углов не записан, через полгода никто не помнит, что это было временно.</li>
<li>Игнорирование социальной части: долг часто живёт там, где нет владельца; технический fix без назначения ответственного отрастает обратно.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Формула для менеджера: <strong>текущая стоимость → предлагаемая инвестиция → возврат → как проверим</strong>. Например: «эта фича займёт 3 недели вместо 1, потому что каждое изменение требует правок в четырёх местах и полного регресса. Две недели на выделение границ — и следующие пять задач в этой области станут по неделе. Проверим по среднему времени задачи в этом модуле за следующий квартал».</p>
<p>Сильнейший момент для инвестиции — сразу после инцидента: постмортем даёт готовую цифру ущерба, к которой легко привязать работу, и в этот момент никто не спорит. Второй приём — привязка к дорожной карте: если через квартал планируется крупная фича в этой области, рефакторинг становится её частью, а не отдельной статьёй.</p>
<p>Команде важно другое: не превращать реестр долга в свалку. Запись без оценки влияния и без владельца туда не попадает, иначе список растёт до сотни пунктов и перестаёт читаться.</p>

<h4>Что спросят следом</h4>
<p><strong>«А если бизнес всё равно говорит нет?»</strong> — фиксируем риск письменно с оценкой последствий, платим долг мелкими шагами внутри обычных задач и возвращаемся к разговору с новыми данными (инцидент, сорванный срок). <strong>«Как понять, что рефакторинг удался?»</strong> — заранее выбранная метрика: время задачи в модуле, число багов оттуда, время CI. Без неё это вопрос веры. <strong>«Всякий ли долг надо платить?»</strong> — нет: в коде, который скоро удалят или который никто не трогает, долг обходится дешевле, чем его исправление.</p>`,
    code: `// реестр долга: запись без цифр и владельца не принимается
{
  "id": "TD-014",
  "title": "Дублирование логики цен в 3 местах",
  "area": "features/checkout",
  "type": "deliberate",                  // deliberate | accidental
  "owner": "team-checkout",
  "costToFix": "8 человеко-дней",
  "monthlyCost": "≈6 дней/мес на синхронизацию правок и разбор расхождений",
  "risk": "цена на витрине и в чеке расходятся — 2 инцидента за квартал",
  "trigger": "перед эпиком «промокоды» в Q4",
  "successMetric": "среднее время задачи в модуле: 4.2 дня -> цель 2 дня"
}

// расчёт, который понимает менеджер
// CI 22 мин × 40 прогонов/день = 14.6 ч ожидания команды в день
// remote cache + affected -> 6 мин -> экономия ≈ 10 ч/день
// стоимость внедрения: 5 дней -> окупаемость меньше недели

// индикаторы, которые считаются автоматически и попадают в дашборд
// - lead time, deploy frequency, change failure rate, MTTR (DORA)
// - циклы в графе зависимостей (depcruise)
// - размер бандла на маршрут (size-limit)
// - зависимости с CVE (npm audit --production)`,
    tip: 'Всегда переводи долг в единицу, которую можно умножить: «CI 22 минуты × 40 прогонов в день = почти 15 часов ожидания команды ежедневно». И заранее называй метрику успеха рефакторинга — без неё через квартал невозможно доказать, что инвестиция сработала, и следующую уже не одобрят.' },

  { id: 'sdx41',
    q: 'Как ты мигрируешь большое легаси-приложение? Что такое strangler fig?',
    a: `<h4>Коротко</h4>
<p>Big bang rewrite почти всегда провал: он замораживает поставку фич, растягивается вдвое против плана и заканчивается системой с теми же проблемами, только новыми словами. Рабочий подход — <strong>strangler fig</strong> («душащая смоковница»): перед легаси ставится маршрутизатор, который по URL или признаку отправляет трафик в старое или новое приложение. Доля нового растёт, старое умирает по частям, поставка не останавливается.</p>

<h4>Как это работает</h4>
<p><strong>Маршрутизатор</strong> — reverse proxy, edge middleware или роутер приложения. Он решает, кто обслуживает конкретный запрос. Это же даёт мгновенный откат: не сработало — вернули маршрут на легаси.</p>
<p><strong>Общий слой.</strong> Аутентификация, сессия, шапка, футер и дизайн-токены выносятся туда, где их видят обе системы, — иначе пользователь замечает границу по разной шапке и по повторному логину. Часто это самая недооценённая часть работы.</p>
<p><strong>Anti-corruption layer</strong> — адаптер между старой и новой моделью данных. Без него легаси-структуры протекают в новый код, и через полгода выясняется, что новое приложение унаследовало ту же модель, ради ухода от которой всё затевалось.</p>
<p><strong>Порядок кусков.</strong> Начинают с наименее связанного и при этом ценного участка: он быстро доказывает работоспособность подхода и даёт видимый результат. Самый сложный узел не первым — на нём миграция может умереть до первой демонстрации.</p>
<p><strong>Промежуточный вариант</strong> — микрофронтенды или встраивание нового виджета внутрь старой страницы: полезно, когда маршрут целиком перенести нельзя, но нужно начать.</p>

<h4>Почему так</h4>
<p>Ценность strangler fig не в технике, а в том, что риск дробится. Каждый шаг маленький, обратимый и приносит пользу сам по себе; проект не имеет точки «всё или ничего». В обмен приходится жить в двух системах одновременно и платить за это двойной поддержкой, двумя сборками и двумя наборами проблем.</p>

<h4>Подводные камни</h4>
<ul>
<li>Миграция без даты окончания: главный риск не технический, а организационный — две системы навсегда. Это худший из возможных исходов, хуже, чем не начинать.</li>
<li>Отсутствие частичного фича-фриза: если новые фичи продолжают писать в легаси, финишная черта отодвигается быстрее, чем вы к ней идёте. Правило: критичные баги чиним в старом, новое — только в новом.</li>
<li>Незамеченная двойная работа: в план и в коммуникацию с бизнесом она закладывается честно, иначе через два месяца команда выглядит медленной без объяснений.</li>
<li>Расхождение поведения: старая и новая версии экрана ведут себя по-разному в краевых случаях. Помогает сравнение на реальном трафике (теневой запуск) до переключения.</li>
<li>Потеря состояния на границе: пользователь переходит из нового приложения в старое, и корзина или фильтры теряются. Общее состояние должно жить в URL или на сервере.</li>
<li>SEO и аналитика: разные системы дают разные URL, разные метки и ломают воронки. Это учитывается до первого переключения, а не после.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Бизнесу нельзя продавать миграцию как проект «перепишем фронтенд» — это запрос на месяцы без пользы. Продаётся поток результатов: «каждые две недели один раздел переезжает, становится быстрее на X и дешевле в поддержке; поставка фич при этом не останавливается». И обязательно называется цена: «первые три месяца скорость поставки будет ниже примерно на 20% из-за работы в двух системах — это плата за то, чтобы не замораживать продукт на полгода».</p>
<p>Прогресс должен быть виден в цифрах, иначе поддержка иссякнет: доля трафика на новом, число оставшихся маршрутов, объём удалённого кода, метрики скорости на перенесённых экранах. Отдельно стоит зафиксировать критерий завершения и дату удаления легаси в письменном виде — устная договорённость «потом уберём» не переживает смену приоритетов.</p>
<p>Команде помогает правило «трогаешь — переносишь» и запрет на новые фичи в старом коде: без него миграция превращается в добровольную инициативу энтузиастов.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как убедиться, что новая версия ведёт себя так же?»</strong> — теневой запуск: обе версии обрабатывают запрос, ответы сравниваются, пользователю отдаётся старый; расхождения логируются. <strong>«Микрофронтенды — это хорошо?»</strong> — как переходный механизм да, как целевая архитектура редко: они дают независимость деплоя ценой дублирования зависимостей и сложной согласованности версий. <strong>«Что делать, если миграция застряла?»</strong> — честно посчитать оставшееся, и если она не закончится, принять решение: либо выделенный ресурс с датой, либо остановиться и признать текущее состояние целевым — это лучше, чем вечно тлеющий проект.</p>`,
    code: `// маршрутизатор: доля нового растёт, откат мгновенный
export function middleware(req) {
  const path = new URL(req.url).pathname;
  const migrated = ['/checkout', '/orders', '/profile'];

  if (migrated.some((p) => path.startsWith(p))) {
    return NextResponse.rewrite(new URL(path, NEW_APP_ORIGIN));
  }
  return NextResponse.rewrite(new URL(path, LEGACY_ORIGIN));
}

// anti-corruption layer: легаси-модель не протекает в новый код
function toDomainOrder(legacy) {
  return {
    id: String(legacy.ORDER_ID),
    status: LEGACY_STATUS_MAP[legacy.STS] ?? 'unknown',   // 'A'|'B'|'C' -> смысл
    total: { amount: legacy.SUM_KOP / 100, currency: 'RUB' },
    createdAt: parseLegacyDate(legacy.CRT_DT),            // 'DDMMYYYY'
  };
}

// теневой запуск: сравниваем поведение до переключения трафика
const [oldRes, newRes] = await Promise.allSettled([legacy(req), next(req)]);
if (!deepEqual(oldRes.value, newRes.value)) {
  logDivergence({ path, diff: diffOf(oldRes.value, newRes.value) });
}
return oldRes.value;                     // пользователь пока получает старое

// прогресс, который видит бизнес
// migrated_routes: 14/41 | traffic_on_new: 62% | legacy_loc_removed: 38k`,
    tip: 'Самая сильная фраза: «главный риск миграции не технический, а то, что она не закончится». Дальше сразу расскажи механизм: дата удаления легаси в ADR, частичный фича-фриз и публичная метрика доли трафика на новом. Без этих трёх вещей strangler fig превращается в две системы навсегда.' },

  { id: 'sdx42',
    q: 'Как ты выбираешь технологию и как обосновываешь выбор?',
    a: `<h4>Коротко</h4>
<p>Решение принимается по заранее названным критериям, а не по вкусу и не по популярности на конференциях. Базовый набор: соответствие задаче, зрелость и размер сообщества, стоимость поддержки (bus factor, частота релизов, возраст открытых issue), опыт команды, влияние на бандл и производительность, <strong>стоимость выхода</strong>, лицензия и безопасность.</p>

<h4>Как это работает</h4>
<p>Процесс из пяти шагов. Первый — сформулировать проблему и критерии успеха <strong>до</strong> просмотра вариантов, иначе критерии подгонятся под понравившийся ответ. Второй — 2-3 варианта, обязательно включая «ничего не делать» и «взять то, что уже используется»: эти два часто выигрывают и почти всегда исключаются из рассмотрения незаслуженно. Третий — spike с жёстким таймбоксом на самую рискованную часть, а не на демонстрацию счётчика. Четвёртый — сравнение по критериям с замерами на своём кейсе. Пятый — ADR с решением и явно перечисленными отвергнутыми альтернативами.</p>
<p><strong>Обратимость — главный фильтр скорости.</strong> Легко откатываемое решение (форматтер, библиотека для дат) можно принять за час: цена ошибки мала. Необратимое (фреймворк, стейт-менеджер, схема данных) требует прототипа, согласования и записи. Смешивать эти два режима — значит либо тормозить на мелочах, либо принимать судьбоносные решения за пять минут.</p>
<p><strong>Инновационный бюджет.</strong> Команда может позволить себе одну-две новых технологии одновременно, не пять. Всё остальное берётся скучным и проверенным, потому что бюджет внимания на разбор незнакомых проблем конечен.</p>

<h4>Почему так</h4>
<p>Стоимость технологии — это не время внедрения, а совокупная стоимость владения: обучение команды, найм, мажорные миграции, интеграция с тулингом, поддержка через три года, когда автор потерял интерес. Выбор по бенчмаркам из README оценивает одну переменную из десяти.</p>
<p>Записанное решение защищает от повторного спора: без ADR через полгода придёт новый человек, задаст тот же вопрос, и команда потратит день на дискуссию без новых аргументов.</p>

<h4>Подводные камни</h4>
<ul>
<li>Выбор по бенчмаркам вместо замеров на своём кейсе: реальный бандл, LCP и опыт разработки на вашем коде часто расходятся с рекламными цифрами.</li>
<li>Игнорирование стоимости выхода: библиотека, чей API проник в двести компонентов, становится несменяемой. Обёртка адаптером снижает эту цену радикально.</li>
<li>Резюме-ориентированная разработка: выбор ради строчки в резюме, который потом поддерживает вся команда.</li>
<li>Bus factor автора: популярный пакет с одним мейнтейнером и без спонсора — риск, который надо назвать вслух.</li>
<li>Лицензия: смена лицензии на копилефтную или коммерческую в мажорной версии — реальный сценарий последних лет.</li>
<li>Отсутствие критериев до сравнения: без них выбирается то, что понравилось, а обоснование пишется задним числом.</li>
<li>Решение без даты пересмотра: «мы выбрали это в 2023» перестаёт быть аргументом, но никто не проверяет.</li>
</ul>

<h4>Как продать выбор бизнесу и команде</h4>
<p>Бизнесу говорим на языке срока, риска и стоимости поддержки, а не «эта библиотека современнее». Работающая формулировка: «вариант A — две недели, риск средний, поддерживаем сами; вариант B — три дня, но привязывает нас к вендору и стоит N в месяц при нашем объёме; рекомендую A, потому что через год мы планируем X, и B туда не масштабируется». Три числа и рекомендация — этого достаточно.</p>
<p>Команде решение продаётся участием: критерии обсуждаются до выбора, spike делает не только автор идеи, ADR ревьюится как код. Решение, принятое единолично и объявленное в чате, саботируется независимо от его качества. Полезно явно оговаривать условия пересмотра: «возвращаемся к вопросу, если вес бандла превысит X или если релизов не будет полгода» — это снимает ощущение необратимости и уменьшает сопротивление.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как оценить, что библиотека не умрёт?»</strong> — частота релизов за последний год, число мейнтейнеров с правом мержа, возраст открытых issue, наличие корпоративного спонсора, число зависимых пакетов. <strong>«Что если команда против?»</strong> — spike обеими сторонами и сравнение по заранее согласованным критериям; спор о вкусе разрешается только данными. <strong>«Когда писать ADR?»</strong> — когда решение дорого откатывать или когда через год кто-то спросит «почему так»; на каждую библиотеку писать не нужно, иначе процесс умрёт.</p>`,
    code: `// ADR: короткий документ рядом с кодом, ревьюится как код
// docs/adr/0012-vybor-state-managera.md

# 12. Выбор стейт-менеджера для админки

Статус: Принято (2026-04-12). Пересмотр: при росте бандла свыше 90 КБ gzip
Контекст: 40+ экранов, 3 команды, много серверного состояния и мало клиентского.
Критерии: вес, кривая обучения, поддержка SSR, devtools, стоимость выхода.

Решение: TanStack Query для серверного состояния + Zustand для локального UI-стейта.

Замеры на своём прототипе (экран заказов, не бенчмарк из README):
| вариант            | бандл gzip | LCP лаб. | дней на переписывание экрана |
|--------------------|-----------|----------|------------------------------|
| Query + Zustand    | 14 КБ     | 1.9 с    | 1.5                          |
| Redux Toolkit      | 23 КБ     | 2.0 с    | 2.5                          |
| ничего не менять   | 0 КБ      | 2.4 с    | 0 (но 3 бага/мес на гонках)  |

Отвергнуто: Redux Toolkit — избыточен, серверное состояние всё равно требует
слоя кеширования. MobX — нет опыта в команде, инновационный бюджет занят.

Последствия: минус — два инструмента вместо одного, нужна договорённость о границе.
Плюс — серверное состояние перестаёт жить в глобальном сторе, гонки уходят.`,
    tip: 'Два маркера сильного ответа: «ничего не делать» как полноценный рассматриваемый вариант и явно названная стоимость выхода. Третий — условие пересмотра в ADR: оно превращает необратимое на вид решение в обратимое и снимает большую часть сопротивления команды.' },

  { id: 'sdx43',
    q: 'Как ты оцениваешь задачи и декомпозируешь их?',
    a: `<h4>Коротко</h4>
<p>Оценка начинается не с числа, а с декомпозиции: пока задача крупнее 2-3 дней, её оценка — фантазия, потому что внутри спрятано то, чего вы ещё не видели. Дальше — диапазон с явными допущениями вместо точечного числа, и отдельной строкой не-код: ревью, тесты, интеграция, багфиксы после QA, релиз и наблюдение.</p>

<h4>Как это работает</h4>
<p><strong>Декомпозиция по вертикальным срезам,</strong> а не по слоям. Срез — это кусок, который можно выкатить и который даёт ценность: «список заказов только для чтения, без фильтров». Разбиение по слоям («сначала все компоненты, потом вся логика, потом интеграция») выглядит логично и приводит к тому, что ничего не работает до последнего дня, а все риски интеграции всплывают в конце.</p>
<p><strong>Диапазон с допущениями:</strong> «5-8 дней при условии, что API готово и дизайн финальный». Точечная оценка создаёт ложную уверенность и слышится как обязательство. Диапазон честно передаёт неопределённость, а допущения фиксируют, при каких условиях он верен, — и при нарушении условий пересмотр оценки перестаёт быть «срывом».</p>
<p><strong>Не-код закладывается отдельно</strong> и составляет обычно 30-50%: ревью и итерации по нему, тесты, стыковка с бэкендом, правки после QA, релиз, наблюдение за метриками. Кандидаты, оценивающие только время написания кода, ошибаются вдвое систематически.</p>
<p><strong>Неизвестность лечится исследованием, а не оценкой.</strong> Если непонятно, как делать, честный ответ — таймбоксированный spike на два дня, после которого будет оценка. Оценивать то, чего не понимаешь, значит называть случайное число.</p>
<p><strong>Референс-класс:</strong> сравнение с похожими задачами, которые команда уже делала («прошлая интеграция платежей заняла три недели»), точнее, чем восстановление оценки из первых принципов.</p>

<h4>Почему так</h4>
<p>Планирование систематически смещено в оптимистичную сторону — это устойчивый когнитивный эффект, а не свойство конкретного разработчика. Бороться с ним раздуванием каждой оценки бессмысленно: буфер, размазанный по задачам, будет израсходован. Правильнее держать буфер на уровне спринта или проекта, где статистика работает.</p>
<p>Главное: оценка — это коммуникация, а не обязательство. Её задача — дать бизнесу возможность принять решение, а не связать инженера обещанием.</p>

<h4>Подводные камни</h4>
<ul>
<li>Молчание до дедлайна: узнав о сдвиге на третий день, но сообщив на четырнадцатый, вы лишаете бизнес возможности отреагировать. Сдвиг сообщается в момент обнаружения.</li>
<li>Оценка без учёта зависимостей: ожидание чужого API, дизайна, доступов — основной источник срывов, а не скорость набора кода.</li>
<li>Оценка в часах вместо дней: создаёт иллюзию точности там, где её нет.</li>
<li>Согласие на срок под давлением: «сможешь за неделю?» — «да» — это не оценка, а капитуляция, и она бьёт по доверию сильнее честного «нет».</li>
<li>Отсутствие пересмотра: оценка, данная до появления новой информации, не обязана оставаться верной.</li>
<li>Оценка чужой работы: оценивает тот, кто будет делать, иначе она не имеет отношения к реальности.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Ключевая фраза для менеджера: <strong>одновременно фиксировать срок, объём и качество нельзя</strong> — выбирается два. Если срок жёсткий (выставка, юридический дедлайн), обсуждаем скоуп: что войдёт в первую версию, а что можно выпустить через две недели. Разговор о скоупе всегда продуктивнее спора об оценке.</p>
<p>Хорошо работает подача через варианты: «полный вариант — 4 недели; вариант с ручной админкой вместо автоматической — 1.5 недели и покрывает 80% случаев; какой берём?». Это переводит разговор из «почему так долго» в совместное решение, и именно этого ждут от сеньора.</p>
<p>Внутри команды помогает вести историю оценок против фактов — не для оценки людей, а для калибровки: через несколько итераций видно, что задачи определённого типа стабильно занимают в полтора раза больше, и это можно закладывать открыто.</p>

<h4>Что спросят следом</h4>
<p><strong>«Что делать, если требуют точную дату?»</strong> — дать дату с уровнем уверенности («с вероятностью 80% к 15-му, при условии X») и предложить контрольную точку через неделю, когда неопределённость снизится. <strong>«Story points или дни?»</strong> — не принципиально; важнее декомпозиция и диапазон. Points защищают от восприятия оценки как обязательства, дни понятнее бизнесу. <strong>«Как оценивать задачу с неизвестной технологией?»</strong> — только через spike; любая цифра до него — выдумка.</p>`,
    code: `// декомпозиция по вертикальным срезам: каждый кусок можно выкатить
// плохо (по слоям): всё готово только в последний день
//   1. вёрстка всех экранов        5д
//   2. вся бизнес-логика           4д
//   3. интеграция с API            3д   <- все риски всплывают здесь

// хорошо (по срезам): ценность и обратная связь с первой недели
//   1. список заказов, read-only, без фильтров     2д  -> можно показать
//   2. фильтры + сохранение в URL                  1.5д
//   3. изменение статуса с оптимистичным апдейтом  2д
//   4. массовые операции                           3д  -> можно вырезать

// формат оценки, который не создаёт ложной уверенности
{
  range: '5-8 дней',
  assumptions: [
    'эндпоинт /orders/bulk готов и отвечает к началу работ',
    'дизайн финальный, состояния ошибок нарисованы',
    'доступ к стейджингу есть',
  ],
  excluded: ['нагрузочное тестирование', 'миграция старых данных'],
  nonCode: '~35% (ревью, тесты, правки после QA, релиз)',
  risks: [
    { what: 'формат bulk-ответа не определён', impact: '+2 дня', mitigation: 'spike 4ч' },
  ],
}`,
    tip: 'Две фразы, которые работают лучше любой методики: «оценка — это распределение, а не число» и «срок, объём и качество одновременно зафиксировать нельзя, выбираем два». Подкрепи их примером, как ты сообщил о сдвиге срока в день обнаружения, а не в день дедлайна — именно это отличает сеньора от исполнителя.' },

  { id: 'sdx44',
    q: 'Как ты работаешь с продуктом и требованиями?',
    a: `<h4>Коротко</h4>
<p>Задача сеньора — не «получить ТЗ и реализовать», а <strong>понять проблему и метрику успеха</strong>. Первый вопрос всегда один: какую пользовательскую или бизнес-проблему решаем и как поймём, что решили. Довольно часто выясняется, что заявленное решение не оптимально, а исходная проблема закрывается дешевле — и это самая ценная вещь, которую инженер приносит в продуктовую работу.</p>

<h4>Как это работает</h4>
<p><strong>Сначала проблема, потом решение.</strong> Продукт приходит с решением («нужен экспорт в Excel»), за которым стоит проблема («аналитик не может посмотреть срез данных»). Иногда экспорт действительно нужен, иногда достаточно фильтра на существующем экране — и это два дня вместо двух недель.</p>
<p><strong>Границы и краевые случаи</strong> уточняются до кода: что не входит в скоуп, что показывать в пустом состоянии, при ошибке, офлайн, при длинном тексте и отсутствии прав, какие платформы и локали, какие требования к скорости и доступности. Эти вопросы стоят десять минут до начала и переделку после демо, если их не задать.</p>
<p><strong>Инженерный контекст возвращается в продуктовое решение.</strong> «Вариант A — две недели; вариант B даёт 80% ценности за три дня, но не покрывает случай X» — этого продукт сам не знает, потому что не видит стоимость. Именно здесь инженер влияет на результат сильнее всего.</p>
<p><strong>Письменная фиксация.</strong> После обсуждения — короткое резюме «правильно ли я понял: делаем это, не делаем то, успех меряем этим». Устные договорённости расходятся у двух участников одной встречи в течение недели.</p>
<p><strong>Раннее участие в дизайн-ревью:</strong> половина дорогих проблем видна на макете — отсутствующие состояния, невозможный адаптив, недоступные контролы, поле, для которого нет данных на бэкенде.</p>

<h4>Почему так</h4>
<p>Стоимость изменения растёт на порядок на каждой следующей стадии: вопрос на встрече стоит минуты, изменение в макете — часы, переделка после релиза — недели плюс потерянное доверие. Поэтому инвестиция во входные вопросы окупается почти всегда.</p>
<p>Обратная сторона: можно перестараться и превратиться в человека, который на любой запрос отвечает списком из двадцати вопросов и блокирует движение. Баланс — задавать те вопросы, ответ на которые меняет решение.</p>

<h4>Подводные камни</h4>
<ul>
<li>Технические ограничения на языке архитектуры: «у нас нет нормализованного стора» ничего не значит для продукта. Переводим в последствия: «этот экран будет обновляться с задержкой в 5 секунд».</li>
<li>Молчаливое согласие с требованием, которое кажется неверным: несогласие, высказанное после релиза, бесполезно.</li>
<li>Защита написанного кода: если гипотеза не сработала, правильное предложение — откатить или упростить, а не аргументировать, что «уже сделано».</li>
<li>Разногласия в формате «так нельзя»: эскалация работает только с вариантами и цифрами.</li>
<li>Отсутствие метрики успеха: фича, для которой не определено, что считается успехом, не может быть ни признана удачной, ни удалена.</li>
<li>Игнорирование краевых случаев на этапе обсуждения: пустое состояние и состояние ошибки почти всегда отсутствуют в макетах и всплывают за день до релиза.</li>
</ul>

<h4>Как продать это продукту и бизнесу</h4>
<p>Инженер, который приходит с вопросами, воспринимается как тормоз, пока не начинает приходить с вариантами. Формат, который работает: не «зачем нам это», а «я вижу три способа решить эту проблему, вот их стоимость и что теряем в каждом». Тогда вопрос читается как вклад, а не как сопротивление.</p>
<p>Второй приём — говорить о рисках заранее и в терминах бизнеса: «если сделаем так, при пиковой нагрузке в чёрную пятницу экран будет грузиться 6 секунд, а это обычно означает отток». Предупреждение, сделанное до, превращает вас в партнёра; сказанное после — в человека, который «мог бы и раньше».</p>
<p>И самое сильное — приносить сокращения скоупа, которые сохраняют ценность. Продукт-менеджеры это ценят выше, чем героические реализации, потому что это освобождает их время и бюджет.</p>

<h4>Что спросят следом</h4>
<p><strong>«Что делать, если продукт настаивает на плохом решении?»</strong> — озвучить риск письменно с последствиями и стоимостью, предложить альтернативу, реализовать принятое решение и вернуться к разговору с данными после релиза. Саботаж и «я же говорил» не работают. <strong>«Как понять, что фича не нужна?»</strong> — по метрике, определённой заранее; без неё любой результат объявляется успехом. <strong>«Вы участвуете в приоритизации?»</strong> — да, через стоимость и риск: инженер единственный, кто может сказать, что задача на два дня, а не на две недели.</p>`,
    code: `// чек-лист вопросов, который экономит недели переделок
// 1. Какую проблему решаем? (не «что делаем»)
// 2. Как поймём, что решили? Какая метрика и какое целевое значение?
// 3. Кто пользователь и как он делает это сейчас?
// 4. Что НЕ входит в скоуп этой итерации?
// 5. Состояния: пусто / загрузка / ошибка / нет прав / офлайн / очень длинный текст
// 6. Платформы, локали, минимальные устройства
// 7. Требования к скорости и доступности
// 8. Что произойдёт, если мы этого не сделаем?

// резюме после встречи — три абзаца, которые предотвращают половину споров
// Договорились:
//   делаем: фильтр по статусу и периоду на существующем экране заказов
//   не делаем: экспорт в Excel (вернёмся, если фильтра окажется мало)
//   успех: доля сессий аналитиков с использованием фильтра > 40% за месяц
//   допущение: бэкенд отдаёт фильтрацию, отдельный эндпоинт не нужен

// подача альтернатив вместо возражений
// A: полный экспорт с настройкой колонок   — 2 недели
// B: фильтр + копирование в буфер          — 3 дня, закрывает 80% случаев
// C: не делать                              — аналитики продолжат просить раз в неделю`,
    tip: 'Самый сильный маркер сеньорности здесь — конкретный пример, где ты сократил скоуп и сохранил ценность: «предложили фильтр вместо экспорта, три дня вместо двух недель, метрика достигнута». Это ценят выше героических реализаций, потому что показывает, что ты оптимизируешь результат, а не объём своей работы.' },

  { id: 'sdx45',
    q: 'Как вы ведёте документацию и что такое ADR?',
    a: `<h4>Коротко</h4>
<p>Документация делится по сроку жизни. Быстро устаревающее (как устроен компонент, какие у него пропсы) обязано жить в коде: типы, JSDoc, Storybook, тесты как спецификация. Долгоживущее (<strong>почему</strong> мы так решили) живёт в ADR — Architecture Decision Record: короткий markdown в репозитории с секциями Контекст, Решение, Альтернативы, Последствия, Статус.</p>

<h4>Как это работает</h4>
<p><strong>ADR фиксирует контекст решения,</strong> а не само решение — решение и так видно в коде. Через год никто не помнит, почему выбрали именно этот стейт-менеджер, какие были ограничения и что рассматривали. Без записи команда либо боится трогать («вдруг была причина»), либо переспорит заново без новых аргументов. Оба исхода дорогие.</p>
<p><strong>ADR иммутабельны.</strong> Решение, оказавшееся неверным, не редактируется: пишется новый ADR, а старый помечается как superseded со ссылкой. Так сохраняется история мышления команды, а не только её текущее состояние.</p>
<p><strong>Хранить рядом с кодом</strong> (<code>docs/adr/0007-vybor-state-managera.md</code>) и ревьюить в том же PR. Документация в отдельной вики теряет связь с кодом за пару месяцев и превращается в кладбище.</p>
<p><strong>Дополняющие форматы под свои задачи:</strong> README модуля отвечает «что это и как запустить», runbook — «что делать, когда сломалось, по шагам», onboarding-гайд — «как начать», диаграммы C4 — верхнеуровневая картина для тех, кто видит систему впервые. Каждый формат отвечает на свой вопрос конкретного читателя; универсальный документ не читает никто.</p>

<h4>Почему так</h4>
<p>Документация конкурирует с кодом за внимание и всегда проигрывает, поэтому единственный устойчивый подход — минимум рукописного текста. Всё, что можно генерировать (типы, схема API, changelog из changesets, таблица пропсов в Storybook), генерируется. Остаётся то, что машина вывести не может: причины, ограничения и отвергнутые варианты.</p>
<p>Цена ADR — дисциплина: писать их лень, и первые месяцы процесс держится на одном энтузиасте. Компенсируется тем, что документ короткий (одна страница) и пишется один раз на решение, а не поддерживается.</p>

<h4>Подводные камни</h4>
<ul>
<li>ADR на каждый чих: если писать их для выбора линтера, процесс умрёт под собственным весом. Критерий — дорого ли откатить решение и спросят ли о нём через год.</li>
<li>Раздел «Последствия», описывающий только плюсы: это рекламная записка, а не ADR. Честно названные минусы — главный признак настоящего документа.</li>
<li>Документ без владельца и даты: протухает молча и вводит в заблуждение сильнее, чем его отсутствие.</li>
<li>Описание того, что и так видно в коде: устаревает в первый же спринт и приучает не доверять документации вообще.</li>
<li>Вики, не связанная с репозиторием: никто не обновляет её в том же PR, поэтому она всегда отстаёт.</li>
<li>Отсутствие runbook: в момент инцидента дежурный читает архитектурные описания вместо инструкции по откату.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Бизнесу документация продаётся через bus factor и онбординг. Конкретно: «сейчас три решения знает только один человек — если он в отпуске, изменения в этой области останавливаются» и «новый разработчик выходит на самостоятельную работу за 6 недель; с картой системы и runbook — за 3». Второе легко проверить на следующем нанятом.</p>
<p>Ещё один аргумент, который хорошо работает после инцидента: сколько минут разбора ушло на выяснение того, как устроена система, и сколько бы заняло с runbook. Постмортем — идеальный момент, чтобы завести первый runbook, потому что боль свежая.</p>
<p>Команде процесс продаётся минимальностью: ADR — одна страница по шаблону, пишется в том же PR, ревьюится как код. Важно начать с одного-двух реальных решений, а не объявлять «теперь пишем ADR на всё» — второе гарантированно умрёт.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как понять, что документация работает?»</strong> — проверяемый критерий: новый разработчик делает первый PR без личных объяснений; если не получилось, чинится то место, где он застрял. <strong>«Кто обновляет?»</strong> — тот, кто меняет код: изменение поведения и обновление документа идут одним PR, иначе не сходится никогда. <strong>«Чем ADR отличается от RFC?»</strong> — RFC пишется до решения для обсуждения, ADR фиксирует принятое решение; часто RFC превращается в ADR после утверждения.</p>`,
    code: `// docs/adr/0007-vybor-state-managera.md — шаблон целиком помещается на экран

# 7. Серверное состояние выносим в TanStack Query

- Статус: Принято (2026-02-03), заменяет ADR-0003
- Владелец: @team-web
- Дата пересмотра: при переходе на React Server Components

## Контекст
40+ экранов, серверные данные лежат в Redux вместе с UI-состоянием.
3 инцидента за квартал из-за устаревшего кеша и гонок при быстрой навигации.
Ручная инвалидация кеша дублируется в 12 местах.

## Решение
Серверное состояние — TanStack Query. Redux остаётся только для UI-состояния
и постепенно сокращается. Граница: всё, что приходит по сети, не попадает в Redux.

## Альтернативы
- Оставить Redux + RTK Query: меньше миграции, но сохраняет привычку
  складывать серверные данные в глобальный стор.
- SWR: легче, но нет мутаций и devtools нужного уровня.
- Ничего не менять: инциденты продолжатся, оценили в ~6 дней/квартал разбора.

## Последствия
- Плюс: инвалидация в одном месте, дедупликация запросов, retry из коробки.
- МИНУС: два источника состояния на время миграции — нужна договорённость
  о границе, иначе появится третий способ хранить данные.
- МИНУС: +12 КБ gzip к бандлу.
- Требуется: codemod для 12 мест ручной инвалидации, гайд в onboarding.`,
    tip: 'Раздел «Последствия» с честно названными минусами — то, что отличает настоящий ADR от рекламной записки, и на интервью это стоит проговорить отдельно. Второй сильный ход — критерий качества документации: новый разработчик делает первый PR без личных объяснений; всё остальное — вопрос вкуса, а это проверяемо.' },

  { id: 'sdx46',
    q: 'Как построить performance culture и перформанс-бюджеты?',
    a: `<h4>Коротко</h4>
<p>Производительность держится не на разовой оптимизации, а на <strong>бюджете, который проверяется автоматически и блокирует</strong>. Без этого любое улучшение съедается за квартал: пять килобайт в неделю — это 250 килобайт в год, и никто не может назвать PR, который всё испортил, потому что его не было.</p>

<h4>Как это работает</h4>
<p><strong>Бюджеты задаются в двух плоскостях.</strong> Лабораторные проверяются на каждом PR: размер JS на маршрут, число запросов, вес изображений, время сборки. Полевые (RUM) отражают реальность: p75 по LCP ≤ 2.5 с, INP ≤ 200 мс, CLS ≤ 0.1. Лабораторные ловят регрессию до релиза, полевые говорят правду о пользователях.</p>
<p><strong>Проверка обязана быть блокирующей.</strong> size-limit или bundlesize на PR, Lighthouse CI на ключевых маршрутах, порог превышен — красный CI и разговор. Предупреждение в логе сборки не читает никто; это проверено во всех командах, которые пробовали мягкий вариант.</p>
<p><strong>Владелец бюджета — команда, а не «человек по перфу».</strong> Как только появляется отдельный ответственный, производительность становится его задачей, а для остальных — чужой; и в споре с фичей она проигрывает всегда.</p>
<p><strong>Отдельный бюджет на третьи стороны.</strong> Аналитика, чат поддержки, теги маркетинга и A/B-инструменты нередко весят больше всего вашего кода и вставляются без ревью. Для них нужен собственный лимит и явный владелец.</p>

<h4>Почему так</h4>
<p>Производительность — это свойство, которое непрерывно деградирует под давлением фич, потому что каждая отдельная фича добавляет немного, а совокупный эффект замечает только пользователь. Единственный способ этому противостоять — сделать ухудшение видимым в момент его внесения, когда автор ещё помнит контекст и может исправить дёшево.</p>
<p>Цена — трение: иногда бюджет блокирует нужную фичу, и приходится либо повышать порог осознанно, либо искать компромисс. Это нормально: осознанное повышение порога с обсуждением лучше, чем незаметное сползание.</p>

<h4>Подводные камни</h4>
<ul>
<li>Оптимизация по лабораторным тестам на MacBook: реальность живёт на среднем Android и 4G. Без сегментации RUM по устройству, сети и стране цифры вводят в заблуждение.</li>
<li>Средние значения вместо p75: среднее прячет хвост, а уходят именно те, кто в хвосте.</li>
<li>Бюджет без владельца и без процесса пересмотра: через полгода он либо игнорируется, либо блокирует всё подряд.</li>
<li>Внимание только к загрузке: INP и отзывчивость после загрузки часто важнее LCP для приложений, где пользователь проводит время.</li>
<li>Отсутствие бюджета на третьи стороны: чат поддержки, добавленный маркетингом через тег-менеджер, обнуляет квартал вашей работы.</li>
<li>Разовый «спринт производительности»: даёт эффект на месяц, после чего всё возвращается, потому что причина была в отсутствии ограничения, а не в отсутствии усилий.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Единственный аргумент, который держится в споре о приоритетах, — деньги. Считается на своих данных: сегментируем пользователей по LCP или INP и смотрим конверсию и отказы в каждом сегменте. «У сессий с LCP выше 4 секунд конверсия ниже на 23%; это N заказов в месяц» — после этой фразы разговор меняется. Публичные исследования полезны как поддержка, но своя корреляция убедительнее любого чужого кейса.</p>
<p>Второй приём — сделать деградацию видимой руководству: график p75 по релизам с отметками деплоев. Когда виден момент, где метрика ухудшилась, появляется и готовность вкладываться.</p>
<p>Команде дешевле встроить дефолты, чем требовать дисциплину: ленивая загрузка по маршрутам из коробки, обёртка над изображениями с обязательными размерами, автоматический комментарий бота в PR с дельтой бандла. Правильные умолчания работают, напоминания — нет. Плюс регулярный ритуал: раз в спринт смотрим топ-3 самых тяжёлых зависимости и топ-3 медленных маршрута.</p>

<h4>Что спросят следом</h4>
<p><strong>«Какой бюджет ставить?»</strong> — отталкиваться от текущего значения и не давать ему расти, а целевое выводить из конкурентов и из порогов Core Web Vitals; бюджет «как у идеального сайта» не выдержит первого же спора. <strong>«Что делать, если фича не влезает в бюджет?»</strong> — осознанное решение с записью: либо повышаем порог и объясняем, либо освобождаем место (удаляем зависимость, делим маршрут). <strong>«Lighthouse или RUM?»</strong> — оба: Lighthouse ловит регрессии в PR детерминированно, RUM показывает правду, но с задержкой и шумом.</p>`,
    code: `// блокирующий бюджет на PR: не предупреждение, а красный CI
// .size-limit.json
[
  { "name": "route: /", "path": "dist/assets/index-*.js", "limit": "180 KB" },
  { "name": "route: /checkout", "path": "dist/assets/checkout-*.js", "limit": "90 KB" },
  { "name": "third-party", "path": "dist/assets/vendor-analytics-*.js", "limit": "40 KB" }
]

// Lighthouse CI: ключевые маршруты, пороги как assertions
// lighthouserc.json
{
  "ci": {
    "collect": { "url": ["/", "/catalog", "/checkout"], "numberOfRuns": 3 },
    "assert": {
      "assertions": {
        "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
        "cumulative-layout-shift":  ["error", { "maxNumericValue": 0.1 }],
        "total-byte-weight":        ["warn",  { "maxNumericValue": 1600000 }]
      }
    }
  }
}

// аргумент, который считается на своих данных
// сегмент по LCP        сессий     конверсия
// < 2.5 c               41%        4.8%
// 2.5-4 c               37%        3.9%
// > 4 c                 22%        3.7%   <- разница ≈ N заказов в месяц`,
    tip: 'Формулировка, которая закрывает тему: «производительность — это не проект, а ограничение, встроенное в CI». Подкрепи её конкретным порогом, который вы поставили, и обязательно расскажи про корреляцию скорости с конверсией на своих данных — это единственный аргумент, который переживает спор о приоритетах с продуктом.' },

  { id: 'sdx47',
    q: 'Как выстроить безопасность фронтенда как процесс?',
    a: `<h4>Коротко</h4>
<p>Безопасность фронтенда — это не «санитизируем ввод», а набор постоянно действующих механизмов плюс процесс, который не зависит от памяти конкретного разработчика. База: <strong>CSP</strong> с nonce вместо unsafe-inline, правильные куки, защита от XSS, контроль цепочки поставки зависимостей и автоматическая проверка всего перечисленного в CI.</p>

<h4>Как это работает</h4>
<p><strong>CSP</strong> — главный механизм. Nonce или hash для скриптов вместо <code>unsafe-inline</code>, <code>frame-ancestors</code> против кликджекинга, <code>report-uri</code> для сбора нарушений. Внедряется всегда в два шага: 2-4 недели в режиме report-only со сбором отчётов, разбор реальных нарушений, и только потом enforcing. Включение сразу в блокирующем режиме ломает продакшен в первый же час — обычно на аналитике и на инлайн-стилях библиотек.</p>
<p><strong>Токены.</strong> Access-токен в памяти, refresh — в httpOnly-куке с <code>Secure</code> и <code>SameSite</code>. <code>localStorage</code> для токенов означает, что любая XSS даёт полную компрометацию сессии. Для OAuth в браузере — PKCE, а лучше BFF-паттерн, при котором токены вообще не попадают в JavaScript.</p>
<p><strong>XSS.</strong> Фреймворки экранируют по умолчанию, поэтому опасные точки наперечёт: <code>dangerouslySetInnerHTML</code> (только через DOMPurify), <code>href</code> с пользовательским значением (<code>javascript:</code>), рендер SVG из внешнего источника, подстановка в <code>eval</code> и в шаблоны. <code>Trusted Types</code> закрывают это на уровне платформы там, где поддерживаются.</p>
<p><strong>Цепочка поставки</strong> — сегодня самый реальный вектор. Lockfile обязателен, аудит зависимостей в CI, Renovate или Dependabot, запрет постустановочных скриптов, проверка на typosquatting, пиннинг версий в CI-экшенах.</p>

<h4>Почему так</h4>
<p>Все перечисленные механизмы работают слоями: CSP не отменяет экранирование, httpOnly-кука не отменяет CSP. Смысл в глубине обороны — одна пробитая линия не должна давать полную компрометацию. Второй принцип: механизм, который надо помнить, рано или поздно забудут, поэтому всё, что можно, проверяется автоматически и падает в CI.</p>
<p>Платим удобством: строгий CSP усложняет подключение сторонних инструментов, а запрет постустановочных скриптов ломает часть пакетов. Это осознанная цена, и её стоит называть вслух.</p>

<h4>Подводные камни</h4>
<ul>
<li>Секреты в бандле: всё, что попало в клиентский код, публично. Переменная с префиксом public — это осознанное решение, а не удобное имя; ключи сторонних API часто утекают именно так.</li>
<li>Заголовки безопасности, настроенные один раз вручную: при смене хостинга или CDN они тихо исчезают. Проверка заголовков должна быть в CI или в синтетическом мониторинге.</li>
<li>CSP с <code>unsafe-inline</code> «временно»: это отключает основную часть защиты и остаётся навсегда.</li>
<li>Третьи стороны без ограничений: SRI для внешних скриптов, sandbox для iframe, минимум сторонних тегов на страницах оплаты. Тег-менеджер с правом вставлять произвольный скрипт — это XSS, санкционированный маркетингом.</li>
<li>Открытый редирект и <code>window.opener</code>: <code>rel="noopener"</code> и валидация целевого URL — мелочи, которые регулярно всплывают в отчётах.</li>
<li>Отсутствие процесса: разовый пентест находит проблемы, которые возвращаются через полгода, если нет threat modeling и security-пункта в ревью.</li>
<li>Логи и мониторинг, собирающие персональные данные: маскирование обязательно, иначе безопасность одной рукой строится, а другой ломается.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Безопасность продаётся риском и регуляторикой, а не страхом. Конкретно: «утечка сессий через XSS означает компрометацию аккаунтов и уведомление регулятора; при нашем объёме персональных данных штраф составляет X, плюс репутационный эффект». Или через требования: аудит, сертификация, требования крупного клиента — это часто главный рычаг, потому что превращает безопасность в условие сделки.</p>
<p>Практичный ход — привязать работу к уже существующему событию: подготовка к аудиту, инцидент у конкурента, требование партнёра. И предлагать не «займёмся безопасностью», а конкретный список из 5-7 пунктов с оценкой в днях: CSP в report-only, аудит зависимостей в CI, перенос токенов из localStorage, проверка заголовков, SRI для внешних скриптов.</p>
<p>Команде это продаётся автоматизацией: security-пункт в чек-листе PR раздражает, а падающий CI с понятной ошибкой воспринимается как обычная проверка. И обучение через разбор реальных инцидентов (своих или публичных) работает несопоставимо лучше регламента, который никто не дочитывает.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как внедрить CSP на живом проекте?»</strong> — report-only, сбор нарушений 2-4 недели, разбор, постепенное ужесточение директив, только потом enforcing. <strong>«Где хранить JWT?»</strong> — не в localStorage: access в памяти, refresh в httpOnly-куке, а лучше BFF, где токен вообще не доходит до браузера. <strong>«Что делать с уязвимостью в транзитивной зависимости?»</strong> — оценить достижимость (используется ли уязвимый путь), обновить или переопределить версию через overrides, при невозможности — зафиксировать риск и мониторить.</p>`,
    code: `// CSP: сначала report-only, через 2-4 недели — enforcing
// шаг 1
Content-Security-Policy-Report-Only:
  default-src 'self';
  script-src 'self' 'nonce-{RANDOM}' 'strict-dynamic';
  style-src 'self' 'nonce-{RANDOM}';
  img-src 'self' data: https://cdn.example.com;
  connect-src 'self' https://api.example.com;
  frame-ancestors 'none';
  report-uri /csp-report;

// шаг 2: тот же заголовок без -Report-Only, когда отчёты чистые

// проверка заголовков в CI: иначе они тихо исчезнут при смене хостинга
test('security headers', async () => {
  const res = await fetch(STAGING_URL);
  expect(res.headers.get('content-security-policy')).toContain("frame-ancestors 'none'");
  expect(res.headers.get('strict-transport-security')).toMatch(/max-age=\\d{7,}/);
  expect(res.headers.get('x-content-type-options')).toBe('nosniff');
  expect(res.headers.get('referrer-policy')).toBe('strict-origin-when-cross-origin');
});

// единственное допустимое использование сырого HTML
element.innerHTML = DOMPurify.sanitize(userHtml, { ALLOWED_TAGS: ['b', 'i', 'a', 'p'] });

// цепочка поставки: постустановочные скрипты — самый прямой путь в вашу машину
// .npmrc
ignore-scripts=true`,
    tip: 'Самая узнаваемая практическая деталь: CSP внедряют в report-only на 2-4 недели, собирают нарушения и только потом включают enforcing. Второй сильный ход — назвать цепочку поставки главным современным вектором и упомянуть ignore-scripts в .npmrc: это показывает, что ты следишь за реальными инцидентами, а не пересказываешь OWASP десятилетней давности.' },

  { id: 'sdx48',
    q: 'Как ты организуешь онбординг новых разработчиков?',
    a: `<h4>Коротко</h4>
<p>Онбординг измеряется двумя числами: <strong>время до первого PR в прод</strong> (цель 1-3 дня) и время до самостоятельной фичи (2-4 недели). Если новичок два дня не может поднять проект, проблема в проекте, а не в новичке — и чинить надо проект.</p>

<h4>Как это работает</h4>
<p><strong>Первое — запуск одной командой.</strong> <code>make dev</code> или devcontainer, актуальный README, сид-данные и моки, чтобы для локальной работы не требовались доступы ко всем сервисам сразу. Это самая частая точка, где теряются дни: человек ждёт доступы, чинит окружение и не пишет код.</p>
<p><strong>Второе — карта системы.</strong> Диаграмма верхнего уровня, границы модулей, кто чем владеет, где лежат ADR. Без неё новичок месяц собирает картину по обрывкам разговоров.</p>
<p><strong>Третье — buddy на 2-4 недели:</strong> конкретный человек, которому можно задать «глупый» вопрос без социальной цены. Общий чат эту роль не выполняет: в него стесняются писать.</p>
<p><strong>Четвёртое — план на 30/60/90 дней</strong> с проверяемыми результатами вместо «осваивайся». Первые задачи подбираются осознанно: маленькие, но настоящие, из разных частей системы. «Поправь текст в футере» ничему не учит и не даёт ощущения вклада.</p>
<p><strong>Пятое — онбординг в процессы,</strong> а не только в код: как проходит релиз, что делать при инциденте, как устроено дежурство, кто принимает решения. Без этого человек боится выкатывать и месяцами просит нажать кнопку кого-то другого.</p>

<h4>Почему так</h4>
<p>Онбординг — это не забота о новичке, а инвестиция с быстрой окупаемостью: разница между выходом на продуктивность за две недели и за два месяца при найме нескольких человек в год измеряется месяцами работы. И это самая честная диагностика состояния проекта: всё, на чём спотыкается новичок, каждый день незаметно тормозит и остальных, просто они привыкли.</p>

<h4>Подводные камни</h4>
<ul>
<li>Устаревшая инструкция по запуску: она перестаёт работать через месяц после написания и никто этого не замечает, потому что у всех уже настроено окружение.</li>
<li>Онбординг как разовое событие в первый день: неделя лекций забывается, а нужные знания приходят по мере столкновения с задачами.</li>
<li>Отсутствие обратной связи в обе стороны: обсуждение на второй и шестой неделе даёт и человеку понимание, как он справляется, и команде — список проблем, которые видит только свежий взгляд.</li>
<li>Перегрузка контекстом: тридцать документов в первый день не читаются. Нужен порядок и минимум обязательного.</li>
<li>Buddy без выделенного времени: если у него полный спринт задач, помощь превращается в раздражение.</li>
<li>Первая задача в самом сложном модуле: демотивирует и создаёт ложное впечатление о человеке.</li>
</ul>

<h4>Как продать это команде и бизнесу</h4>
<p>Бизнесу это считается прямо: «наём стоит N, а человек выходит на полную продуктивность за 8 недель; сокращение до 4 недель при пяти наймах в год — это пять человеко-месяцев». И через риск: bus factor и скорость замены, если ключевой человек уходит.</p>
<p>Внутри команды главное возражение — «нет времени писать документацию». Оно снимается приёмом, который почти ничего не стоит: <strong>каждый новый человек чинит один шаг из onboarding-инструкции, на котором споткнулся</strong>, и делает это своим первым PR. Документация чинится сама, руками того, кто единственный видит в ней пробелы, а первый PR получается полезным и безопасным одновременно.</p>
<p>Второй аргумент для команды: время, потраченное на автоматизацию запуска проекта, возвращается всем — тот же devcontainer чинит окружение и старожилам после переустановки системы.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как онбордить в распределённой команде?»</strong> — асинхронно и письменно: записанные видео-обзоры системы, письменный план, явные окна для вопросов; buddy становится важнее, потому что случайных разговоров нет. <strong>«Как понять, что онбординг работает?»</strong> — по метрикам (время до первого PR, до самостоятельной фичи) и по ретроспективе на шестой неделе. <strong>«Как онбордить сеньора?»</strong> — быстрее в код, но дольше в контекст: почему приняты решения, где границы влияния, кто принимает решения; технически он справится сам, а вот социальный и исторический контекст ему никто не даст, если не рассказать.</p>`,
    code: `// цель: работающее окружение одной командой, без доступов ко всем сервисам
// Makefile
dev:            ## поднять локально: фронт + моки + сид-данные
	@docker compose up -d db
	@pnpm install --frozen-lockfile
	@pnpm db:seed
	@pnpm msw:init          # моки вместо доступа к 8 сервисам
	@pnpm dev

doctor:         ## проверить окружение и подсказать, что чинить
	@node scripts/doctor.js  # версия node, порты, .env, доступ к реестру

// план 30/60/90 с проверяемыми результатами, а не «осваивайся»
// 30: 5+ смерженных PR, самостоятельный релиз под присмотром, знает карту модулей
// 60: ведёт небольшую фичу целиком, участвует в ревью, дежурит в паре
// 90: владеет областью, дежурит самостоятельно, онбордит следующего

// приём, который чинит документацию бесплатно
// Первая задача каждого новичка: пройти docs/onboarding.md и прислать PR
// с исправлением всего, что не сработало. Обязательно, а не «если будет время».`,
    tip: 'Идея, которая почти всегда впечатляет интервьюера: первый PR каждого новичка — исправление onboarding-инструкции на том шаге, где он споткнулся. Документация чинится сама, а первый PR оказывается полезным и безопасным. И назови метрику вслух: время до первого PR в прод — 1-3 дня.' },

  { id: 'sdx49',
    q: 'Когда писать свою библиотеку, а когда брать чужую?',
    a: `<h4>Коротко</h4>
<p>Решение принимается через <strong>полную стоимость владения</strong>, а не через «сколько строк придётся написать». Чужая библиотека стоит: вес в бандле, риск заброшенности, мажорные миграции, ненужные возможности и уязвимости в транзитивных зависимостях. Своя стоит: разработка, тесты, документация, поддержка навсегда и bus factor на одного человека.</p>

<h4>Как это работает</h4>
<p><strong>Берём чужое там, где домен сложный и хорошо изучен:</strong> даты и таймзоны, парсинг, криптография, текстовые редакторы, доступные примитивы интерфейса (headless-компоненты), виртуализация, drag-n-drop. Общее свойство этих областей — они выглядят простыми и содержат сотни краевых случаев, которые уже кто-то нашёл ценой лет работы.</p>
<p><strong>Пишем своё,</strong> когда задача узкая, требований мало и библиотека тянет 40 килобайт ради тридцати строк логики; или когда нужна кастомизация, против которой борется чужой API — в этом случае вы всё равно напишете свою реализацию, только поверх чужой и с двойной сложностью.</p>
<p><strong>Проверка зависимости перед принятием:</strong> частота релизов за год, число мейнтейнеров с правом мержа, возраст открытых issue, размер и tree-shakability, лицензия, глубина транзитивного дерева, наличие типов. Пять минут работы, которые предотвращают годы страданий.</p>
<p><strong>Адаптер обязателен.</strong> Чужая библиотека оборачивается своим модулем с собственным API: тогда замена стоит один файл, а не двести компонентов. Это же дисциплинирует — если обёртка получается тонкой, зависимость нужна; если она превращается в перевод одного API в другой, зависимость лишняя.</p>
<p><strong>Промежуточный вариант — vendoring:</strong> скопировать нужные тридцать строк с указанием лицензии и источника вместо добавления пакета ради двух функций. Это законно, часто правильно и почему-то считается стыдным.</p>

<h4>Почему так</h4>
<p>Зависимость — это не однократное решение, а обязательство: обновлять, чинить конфликты версий, мигрировать на мажорах, реагировать на CVE. Своя библиотека — обязательство ещё большее, потому что к нему добавляется «объяснять другим, как этим пользоваться».</p>
<p>Отдельный принцип: <strong>внутренняя библиотека оправдана только повторяемостью</strong> — если её используют две команды или больше. Библиотека для одного потребителя — это просто модуль с лишним слоем версионирования.</p>

<h4>Подводные камни</h4>
<ul>
<li>Недооценка доступности и локализации: самая частая ошибка при написании своего. Свой date-picker или свой select почти всегда провальны именно на клавиатуре, скринридере, IME и RTL.</li>
<li>Оценка «на выходные»: считается только счастливый путь, без тестов, документации, поддержки браузеров и краевых случаев.</li>
<li>Отсутствие адаптера: библиотека прорастает в двести файлов, и замена становится невозможной.</li>
<li>Зависимость ради одной функции: пакет с деревом из двадцати транзитивных зависимостей ради проверки email.</li>
<li>Своя библиотека без владельца: через год автор ушёл, никто не знает, как она работает, а зависят от неё три продукта.</li>
<li>Внутренняя библиотека без версионирования и changelog: изменение ломает три команды одновременно, и виноват тот, кто улучшал.</li>
<li>Игнорирование лицензии: смена на копилефт или коммерческую в мажорной версии — реальный сценарий последних лет.</li>
</ul>

<h4>Как продать решение бизнесу и команде</h4>
<p>Для бизнеса аргумент «напишем сами, это быстрее» почти всегда ложный, и это надо уметь показать честным расчётом: не «три дня на реализацию», а «три дня плюс тесты, плюс доступность, плюс поддержка — и это навсегда наш код, который придётся чинить в момент, когда мы будем заняты чем-то другим». Обратный случай тоже считается: «эта библиотека добавляет 60 килобайт и вендор-локин, а нам нужны две функции».</p>
<p>Для решения «пишем свою внутреннюю библиотеку» аргумент должен быть в дублировании: «три команды написали свой загрузчик файлов, у каждого свои баги; общая библиотека стоит две недели и снимает три источника инцидентов». Без такого счёта внутренняя библиотека выглядит как хобби.</p>
<p>И обязательно называйте условие отказа: если на продуктовый подход (версии, changelog, миграции, документация, владелец) ресурса нет — не делайте библиотеку, сделайте общий модуль в монорепо. Это честно и снимает половину будущих проблем.</p>

<h4>Что спросят следом</h4>
<p><strong>«Как оценить риск заброшенности?»</strong> — релизы за год, число мейнтейнеров, наличие спонсора, число зависимых пакетов; и главное — есть ли план, если пакет умрёт (форк, замена, vendoring). <strong>«Что делать с уязвимостью в чужой библиотеке?»</strong> — оценить достижимость, обновить или переопределить версию через overrides, при отсутствии патча — форк с исправлением. <strong>«Когда форк оправдан?»</strong> — когда нужное исправление не принимают месяцами, а зависимость критична; форк с планом возврата в апстрим, а не навсегда.</p>`,
    code: `// адаптер: замена библиотеки стоит один файл, а не двести компонентов
// shared/lib/date/index.ts — единственное место, где импортируется библиотека
import { format, addDays, differenceInDays } from 'date-fns';
import { tz } from '@date-fns/tz';

export const dateApi = {
  formatShort: (d: Date, locale: string, timeZone: string) =>
    format(d, 'd MMM', { in: tz(timeZone), locale: locales[locale] }),
  addDays,
  daysBetween: differenceInDays,
};
// весь остальной код импортирует dateApi, а не date-fns

// чек-лист перед добавлением зависимости (5 минут)
// npm view <pkg> time.modified   -> когда последний релиз
// npm view <pkg> maintainers     -> сколько людей может мержить
// npx bundlephobia <pkg>         -> вес, tree-shakability
// npm view <pkg> license         -> лицензия и её история
// npm ls <pkg>                   -> глубина транзитивного дерева

// vendoring: честный компромисс для тридцати строк
// shared/lib/slugify.ts
// Взято из https://github.com/... (MIT), commit abc123, адаптировано под кириллицу.
// Причина: пакет тянет 4 транзитивные зависимости ради одной функции.`,
    tip: 'Самый убедительный конкретный аргумент: «мы не пишем свой date-picker, потому что доступность, IME, локализация и RTL в нём стоят больше, чем вся экономия на весе». И назови обратное условие для внутренней библиотеки: она оправдана только с двумя потребителями и только если есть ресурс на версии, changelog и миграции — иначе это общий модуль, а не библиотека.' },

  { id: 'sdx50',
    q: 'Что такое SLO для фронтенда и как вы работаете с инцидентами и постмортемами?',
    a: `<h4>Коротко</h4>
<p>SLI для фронтенда — измеримые показатели пользовательского опыта: доля сессий с LCP ≤ 2.5 с, доля успешно завершённых ключевых действий, JS error rate, доступность страницы. SLO — целевое значение этого показателя (например, 99% успешных оформлений заказа за 28 дней). Разница между 100% и целью — <strong>error budget</strong>. Пока бюджет есть, катимся быстро; бюджет выжжен — приоритет уходит на надёжность.</p>

<h4>Как это работает</h4>
<p><strong>SLI выбираются по пользователю, а не по инфраструктуре.</strong> «Аптайм CDN 99.99%» ничего не говорит, если оформление заказа падает у 3% пользователей на Safari. Правильный SLI описывает, получил ли пользователь то, за чем пришёл.</p>
<p><strong>Error budget как механизм переговоров.</strong> При SLO 99% за 28 дней бюджет — это 1% неудачных операций. Пока он не израсходован, команда релизит с обычной скоростью и берёт риск. Как только выжжен — по заранее оговорённому правилу приоритет переключается на надёжность. Это переводит вечный спор «фичи против стабильности» в договорённость с числом, принятую заранее и обеими сторонами.</p>
<p><strong>Инцидент-процесс.</strong> Обнаружение по алерту на симптом (не на причину), объявление инцидента с явными ролями: incident commander (координирует и сам не чинит), человек на коммуникациях, исполнители. Первым делом митигация — откат, kill switch, переключение флага. Расследование только после восстановления. Статус сообщается каждые N минут, даже если новостей нет.</p>
<p><strong>Постмортем безвинный:</strong> ищем условия, при которых ошибка стала возможной, а не виноватого. Содержит таймлайн, влияние в цифрах (сколько пользователей, сколько денег, сколько времени), анализ причин и <strong>конкретные action items с владельцем и сроком</strong>. Без последнего это художественная литература.</p>

<h4>Почему так</h4>
<p>Стопроцентная надёжность недостижима и экономически бессмысленна: каждая следующая девятка стоит кратно дороже предыдущей и почти не заметна пользователю. Error budget формализует эту мысль и делает риск управляемым ресурсом, а не поводом для спора.</p>
<p>Blameless-подход нужен не из гуманизма, а из практики: в культуре поиска виноватых люди скрывают проблемы и сообщают об инцидентах поздно, что многократно увеличивает ущерб.</p>

<h4>Подводные камни</h4>
<ul>
<li>Алерты на причины вместо симптомов: «выросло число 500 в сервисе X» будит дежурного, «упала доля успешных оформлений» описывает проблему пользователя.</li>
<li>Фронтовая специфика: инцидент часто виден только у части пользователей — один браузер, один регион, одна версия релиза. Без сегментированных алертов такое не ловится: общий график остаётся ровным.</li>
<li>SLO, взятые с потолка: цель опирается на текущее значение и на то, что важно бизнесу, иначе бюджет либо всегда выжжен, либо всегда полон.</li>
<li>Постмортем без действий или с действиями без владельца и срока: документ пишется, ничего не меняется, через квартал повторяется тот же инцидент.</li>
<li>Отсутствие учений: runbook, который ни разу не проверяли, в момент инцидента обнаруживает недостающие доступы и сломанные ссылки. Game day раз в квартал это лечит.</li>
<li>Incident commander, который сам чинит: координация теряется, и три человека делают одно и то же.</li>
<li>Игнорирование MTTD: время до обнаружения часто больше времени до починки, и улучшать надо именно его.</li>
</ul>

<h4>Как продать это бизнесу и команде</h4>
<p>Error budget продаётся как <strong>инструмент переговоров, а не техническая метрика</strong>, и именно так о нём стоит говорить. Формулировка для продукта: «договоримся заранее: пока 99% оформлений проходят успешно, мы катим фичи в обычном темпе и не тормозим тебя лишними проверками; если показатель падает ниже — следующий спринт уходит на надёжность. Тогда мы не спорим каждый раз, а смотрим на число». Продукт обычно соглашается, потому что получает предсказуемость и право на риск.</p>
<p>Инцидент-процесс продаётся деньгами и временем: «прошлый инцидент длился 50 минут, из которых 35 ушло на выяснение, кто чем занимается; с назначенными ролями и runbook это 10 минут». DORA-метрики (change failure rate, время восстановления) дают руководству понятный способ видеть, улучшается ли процесс.</p>
<p>Команде важнее культурная часть: первый постмортем задаёт тон на годы. Если на нём ищут виноватого, следующие инциденты будут замалчиваться, и никакой процесс это не исправит.</p>

<h4>Что спросят следом</h4>
<p><strong>«Какой SLO ставить?»</strong> — от текущего фактического значения и от точки, где пользователь начинает замечать: не «99.9%, потому что красиво», а «мы сейчас на 98.6%, ставим 99% и разбираемся, что мешает». <strong>«Что делать, когда бюджет выжжен?»</strong> — заранее записанное правило: заморозка рискованных изменений, приоритет на надёжность, разбор причин. Правило принимается до, а не во время. <strong>«Как считать SLI на фронте?»</strong> — по клиентским событиям начала и успешного завершения операции, с учётом отвалившихся сессий; иначе метрика посчитает только тех, кто дошёл до конца, и всегда будет красивой.</p>`,
    code: `// SLI фронтенда считается по пользовательской операции, а не по аптайму
analytics.track('checkout_started',   { flowId, ...ctx });
analytics.track('checkout_completed', { flowId, ms, ...ctx });
analytics.track('checkout_failed',    { flowId, reason, ...ctx });
// success_rate = completed / started  (брошенные сессии тоже в знаменателе)

// SLO и error budget как записанная договорённость, а не график в дашборде
{
  sli: 'checkout_success_rate',
  slo: 0.99,
  window: '28d',
  errorBudget: '1% операций',
  policy: {
    remaining_gt_25: 'обычный темп релизов, риск допустим',
    remaining_lt_25: 'заморозка рискованных изменений, приоритет надёжности',
    fastBurn:        '2% бюджета за час -> объявляем инцидент, поднимаем дежурного',
  },
  owner: 'team-checkout',
}

// сегментированный алерт: фронтовые инциденты видны только части пользователей
// alert: checkout_success_rate < 0.97
//   group_by: [browser, country, app_version]
//   for: 10m
// общий график при этом остаётся ровным — поломку видно только в разрезе

// единственная обязательная часть постмортема
// | Действие                                    | Владелец | Срок  | Тикет  |
// | Idempotency key на создание заказа          | @ivan    | 12.05 | WEB-41 |
// | Алерт на success_rate в разрезе браузера    | @olga    | 08.05 | WEB-42 |`,
    tip: 'Про error budget скажи явно, что это инструмент переговоров с продуктом, а не техническая метрика: договорённость принимается заранее и снимает спор «фичи против стабильности» с каждого спринта. Добавь фронтовую специфику: инцидент бывает виден только в одном браузере или регионе, поэтому алерты обязаны быть сегментированными — общий график в такой ситуации остаётся ровным.' }
];
