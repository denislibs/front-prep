/* Тестовые вопросы блока C: браузер, сеть, безопасность (колода web)
   и проектирование систем, включая разбор мессенджера (колода sd).
   qc1–qc35 — web, qc36–qc65 — sd. */

const QUIZ_C = [

  { id: 'qc1',
    deck: 'web',
    q: 'Когда браузер отправит запрос за /hero.jpg?',
    snippet: `<head>
  <link rel="stylesheet" href="/main.css">
  <script src="/analytics.js"></script>
  <style>.hero { background-image: url(/hero.jpg); height: 60vh; }</style>
</head>
<body><div class="hero"></div></body>`,
    options: [
      'Сразу: preload scanner находит URL в разметке ещё до разбора CSS',
      'После построения CSSOM и layout — фоновые изображения сканер не видит',
      'Одновременно с /main.css: оба ресурса объявлены в head',
      'После события load: фоновые изображения грузятся в последнюю очередь',
    ],
    correct: 1,
    why: `<p><strong>Preload scanner читает сырой HTML</strong> и ставит в очередь то, что записано атрибутами <code>src</code> и <code>href</code>. Фон, объявленный в CSS, для него не существует: чтобы узнать про <code>/hero.jpg</code>, браузеру нужно разобрать CSS в CSSOM, построить render tree и выполнить layout — только тогда станет известно, что элемент с классом <code>hero</code> вообще отрисовывается.</p>
    <p>Именно поэтому первый вариант так соблазнителен и так неверен: сканер действительно работает раньше основного парсера, но он смотрит в разметку, а не в стили. Если <code>/hero.jpg</code> — это LCP-элемент, запрос уйдёт на сотни миллисекунд позже, чем мог бы, даже на быстром канале. Лечение — держать LCP-изображение в теге <code>img</code> (тогда его увидит сканер) и добавить <code>fetchpriority="high"</code>.</p>
    <p>Вариант про <code>load</code> путает с ленивой загрузкой: браузер не откладывает фоны до конца загрузки документа, он просто узнаёт о них поздно.</p>`,
    cardId: 'wx1' },

  { id: 'qc2',
    deck: 'web',
    q: 'Что не так с этими двумя подсказками?',
    snippet: `<link rel="preconnect" href="https://fonts.site.ru">
<link rel="preload" as="font" type="font/woff2" href="https://fonts.site.ru/inter.woff2">`,
    options: [
      'Атрибут as лишний: тип уже задан через type, и они конфликтуют',
      'preconnect обязан идти после preload, иначе соединение не переиспользуется',
      'Обоим не хватает crossorigin: шрифт будет скачан дважды',
      'preload шрифта требует media="all", без него подсказка игнорируется',
    ],
    correct: 2,
    why: `<p>Шрифты <strong>всегда</strong> запрашиваются в режиме CORS, даже со своего домена. Предзагруженный без <code>crossorigin</code> ответ лежит в кеше как «не-CORS» и не подходит под настоящий запрос из <code>@font-face</code>: браузер качает файл второй раз и пишет в консоль «was preloaded but not used». Ровно та же логика у <code>preconnect</code> — без атрибута прогреется не то соединение, и под CORS-запрос откроется второе.</p>
    <p><code>as</code> и <code>type</code> не конфликтуют: <code>as</code> задаёт приоритет, режим CORS и заголовок <code>Accept</code>, <code>type</code> позволяет браузеру пропустить формат, который он не понимает. Без <code>as</code> подсказка как раз ломается.</p>
    <p>Порядок тегов на корректность не влияет: и <code>preconnect</code>, и <code>preload</code> обрабатываются по мере разбора head, а <code>media</code> для шрифтов не обязателен.</p>`,
    cardId: 'wx2' },

  { id: 'qc3',
    deck: 'web',
    q: 'Герой-изображение стоит первым тегом img в разметке, но в водопаде его запрос уходит после четырёх скриптов. Почему?',
    options: [
      'Все изображения стартуют с приоритетом Low до первого layout — помогает fetchpriority="high"',
      'По спецификации изображения грузятся после скриптов — надо вынести скрипты в конец body',
      'Не хватает decoding="sync": без него запрос откладывается до момента декодирования',
      'Так работает HTTP/2: он всегда отдаёт скрипты раньше — помогает preconnect к домену картинок',
    ],
    correct: 0,
    why: `<p>До первого layout браузер не знает, попадает ли картинка во вьюпорт, поэтому <strong>любое изображение стартует с приоритетом Low</strong>. Повышение до High происходит уже после разбора CSS и первого layout, а к этому моменту очередь занята скриптами. <code>fetchpriority="high"</code> снимает разрыв: приоритет проставляется сразу, когда тег находит preload scanner.</p>
    <p>Вариант про «вынести скрипты вниз» выглядит разумно и часть проблемы действительно снимает, но это лечение симптома: как только скрипты вернутся в head (а аналитику туда вернут), всё повторится. Приоритеты работают как относительная шкала, поэтому полезно и обратное — <code>fetchpriority="low"</code> на слайдах карусели ниже первого и на оффскрин-превью.</p>
    <p><code>decoding</code> управляет декодированием уже полученных байтов и на момент запроса не влияет. HTTP/2 не имеет собственного мнения о типах ресурсов — он раздаёт полосу по весам, которые проставил браузер.</p>`,
    cardId: 'wx3' },

  { id: 'qc4',
    deck: 'web',
    q: 'Страницы товара шлют page_view при монтировании. Что произойдёт после включения этих правил?',
    snippet: `<script type="speculationrules">
{
  "prerender": [
    { "where": { "href_matches": "/product/*" }, "eagerness": "moderate" }
  ]
}
</script>`,
    options: [
      'Ничего: prerender только скачивает документ, JS в нём не выполняется',
      'Просмотры засчитаются для страниц, которые пользователь так и не открыл',
      'Аналитика не уйдёт вообще: в prerender-контексте сетевые запросы заблокированы',
      'События уйдут дважды: prerender-копия и активированная страница — разные документы',
    ],
    correct: 1,
    why: `<p><code>prerender</code> — это полноценный рендер страницы в скрытом процессе: HTML разбирается, JS выполняется, эффекты монтирования срабатывают. Поэтому счётчик просмотра отправится в момент спекуляции, а перехода может и не быть — метрика раздувается, а конверсия падает на ровном месте.</p>
    <p>Лечится проверкой <code>document.prerendering</code> и событием <code>prerenderingchange</code>: отправку событий и любые побочные эффекты (POST, списание лимита, отметка «прочитано») откладывают до активации.</p>
    <p>Вариант с двойной отправкой звучит правдоподобно, но неверен механически: активация не создаёт новый документ, а показывает уже отрендеренный, поэтому обработчик монтирования выполняется один раз. Вариант «JS не выполняется» описывает <code>prefetch</code>, а не <code>prerender</code>.</p>`,
    cardId: 'wx4' },

  { id: 'qc5',
    deck: 'web',
    q: 'Новый Service Worker вызывает skipWaiting() прямо в обработчике install, а в activate удаляет старые кеши. Пользователь работает во вкладке уже час. Что он увидит?',
    options: [
      'Ничего: страница останется под контролем старого воркера до перезагрузки',
      'Вкладка автоматически перезагрузится на новую версию',
      'Ошибки загрузки ленивых чанков старой сборки прямо посреди работы',
      'Старый кеш продолжит отдаваться, новая версия начнётся со следующего визита',
    ],
    correct: 2,
    why: `<p>Новый воркер активируется немедленно и в <code>activate</code> вычищает кеши предыдущей версии. Открытая вкладка при этом продолжает жить на старом HTML и старом графе модулей: как только пользователь откроет раздел, подгружаемый через <code>import()</code>, чанк не найдётся ни в кеше, ни на CDN (его удалили при деплое) — получаем ChunkLoadError посреди сессии.</p>
    <p>Вариант «страница останется под старым воркером» частично верен по букве (без <code>clients.claim</code> контроль не перехватывается), но не спасает: удалённые кеши и удалённые файлы уже недоступны, и это ровно тот случай, когда формально корректное рассуждение даёт неверный практический вывод.</p>
    <p>Правильный протокол: воркер устанавливается и <strong>ждёт</strong>, приложение по <code>updatefound</code> показывает плашку «доступна новая версия», по клику шлёт <code>postMessage</code> с командой <code>skipWaiting</code> и перезагружается на <code>controllerchange</code>. Плюс держим предыдущую версию ассетов хотя бы сутки.</p>`,
    cardId: 'wx5' },

  { id: 'qc6',
    deck: 'web',
    q: 'Какая ошибка в этом обработчике проявится первой?',
    snippet: `self.addEventListener('fetch', (e) => {
  e.respondWith(caches.open('v1').then(async (cache) => {
    const hit = await cache.match(e.request);
    if (hit) return hit;
    const res = await fetch(e.request);
    cache.put(e.request, res);
    return res;
  }));
});`,
    options: [
      'cache.put получит уже использованное тело ответа — нужен res.clone()',
      'caches.open внутри обработчика fetch бросает InvalidStateError',
      'match без ignoreSearch не найдёт ни одного совпадения',
      'respondWith принимает только готовый Response, а не промис',
    ],
    correct: 0,
    why: `<p>Тело <code>Response</code> — поток, и прочитать его можно один раз. Здесь <code>cache.put</code> и <code>return res</code> претендуют на одно и то же тело, и второй потребитель получает ошибку «body already used». Правильно: <code>cache.put(e.request, res.clone())</code> — клонировать надо <strong>до</strong> первого чтения.</p>
    <p>Второй дефект того же кода, который стоит назвать вслух: кешируется любой ответ. Нужна проверка <code>res.ok</code> и <code>res.type</code> — иначе в кеш попадут 404 и opaque-ответы, у которых статус непрозрачен, а в квоте они занимают с округлением вверх, иногда мегабайты вместо килобайт.</p>
    <p><code>caches.open</code> в обработчике абсолютно легален, <code>respondWith</code> принимает промис (иначе асинхронные стратегии были бы невозможны), а <code>ignoreSearch</code> — опция для совпадения без query, её отсутствие ничего не ломает.</p>`,
    cardId: 'wx6' },

  { id: 'qc7',
    deck: 'web',
    q: 'Интерфейс замирает на 400 мс при клике. Почему await не помог?',
    snippet: `async function filterRows(rows) {
  await Promise.resolve();
  const out = [];
  for (const r of rows) if (match(r)) out.push(r);   // 200 000 строк, ~400 мс
  return out;
}

button.onclick = () => filterRows(bigRows).then(renderTable);`,
    options: [
      'Promise.resolve резолвится синхронно — нужен await над setTimeout',
      'Асинхронность освобождает поток от ожидания ввода-вывода, но не от загрузки CPU',
      'Замирает не цикл, а renderTable: 200 тысяч узлов нельзя вставить за один кадр',
      'onclick вместо addEventListener не даёт браузеру вставить кадр между обработчиками',
    ],
    correct: 1,
    why: `<p>Это самая частая путаница на собеседовании. <code>async/await</code> решает задачу <strong>ожидания</strong>: пока ждём сеть или диск, поток свободен. Здесь ждать нечего — 400 мс занимает сам цикл, и он выполняется синхронно от начала до конца в одной задаче. Поток занят, кадр не собирается, ввод не обрабатывается.</p>
    <p>Первый вариант привлекателен и наполовину верен: <code>await Promise.resolve()</code> действительно уступает только микротаску, то есть не уступает вовсе, а <code>setTimeout</code> дал бы настоящую точку выхода. Но выигрыш был бы ровно один кадр перед циклом — сам цикл всё равно занял бы 400 мс. Чинится либо разбиением на чанки с <code>await scheduler.yield()</code>, либо переносом в Web Worker.</p>
    <p>Порог окупаемости воркера — несколько десятков миллисекунд работы: старт стоит 5–40 мс, плюс structured clone на границе. 400 мс — уверенно за порогом.</p>`,
    cardId: 'wx8' },

  { id: 'qc8',
    deck: 'web',
    q: 'Автор хотел разбить длинную задачу. Что реально даёт этот await?',
    snippet: `async function process(items) {
  for (let i = 0; i < items.length; i += 100) {
    handleChunk(items.slice(i, i + 100));
    await Promise.resolve();
  }
}`,
    options: [
      'Ничего: микротаски выполняются в конце той же задачи, до отрисовки — поток не уступлен',
      'Поток уступается каждые 100 элементов, но отрисовка всё равно отложится до конца цикла',
      'Уступка есть, но слишком частая: 1–5 мс накладных на каждый await съедают весь выигрыш',
      'Работает только в Chromium: в Safari задачи ввода вытесняют очередь микротасок',
    ],
    correct: 0,
    why: `<p>Микротаски — это не выход в event loop. Очередь микротасок вычерпывается <strong>до конца текущей задачи</strong> и до следующей возможности отрисовать кадр, поэтому <code>await Promise.resolve()</code> в цикле не даёт браузеру ни обработать клик, ни нарисовать кадр. Разбиение получилось только на бумаге.</p>
    <p>Настоящая уступка — это <code>await scheduler.yield()</code> (Chromium), <code>scheduler.postTask</code> или, как фолбэк, <code>setTimeout(fn, 0)</code>. Третий вариант описывает реальную стоимость такой уступки (1–5 мс на каждую, поэтому режут чанками по 50–100, а не по одному элементу) — но применительно к настоящему yield, а не к микротаске.</p>
    <p>Поведение микротасок одинаково во всех движках, это часть спецификации, а не особенность Chromium.</p>`,
    cardId: 'wx9' },

  { id: 'qc9',
    deck: 'web',
    q: 'Что покажет панель Performance на этом цикле?',
    snippet: `for (const el of items) {          // items — 400 элементов
  el.style.width = el.offsetWidth + 10 + 'px';
}`,
    options: [
      'Долгий Recalculate Style: запись стилей в цикле пересчитывает каскад на каждой итерации',
      'Forced synchronous layout: чтение offsetWidth после записи заставляет пересчитывать геометрию 400 раз',
      'Долгий Paint: изменение width перерисовывает слой на каждой итерации',
      'Ничего заметного: браузер батчит изменения стилей и делает один layout в конце кадра',
    ],
    correct: 1,
    why: `<p>Браузер действительно батчит записи в стили — ровно до того момента, пока кто-нибудь не прочитает свойство, зависящее от геометрии. <code>offsetWidth</code> — как раз такое: чтобы вернуть актуальное число, движок обязан применить все отложенные изменения и выполнить layout. В цикле это 400 принудительных layout-ов подряд, в профиле — короткие фиолетовые блоки внутри вашей функции, суммарно сотни миллисекунд.</p>
    <p>Последний вариант описывает правильное поведение при чистой записи, и именно поэтому он опасен: люди помнят про батчинг и не помнят, что чтение его отменяет. Лечение — разделить фазы: сначала прочитать все <code>offsetWidth</code> в массив, потом одним проходом записать стили.</p>
    <p>Recalculate Style и Paint здесь тоже будут, но они не объясняют квадратичного роста времени с числом элементов — а именно он и делает такой цикл убийцей INP.</p>`,
    cardId: 'wx10' },

  { id: 'qc10',
    deck: 'web',
    q: 'В SPA после сотни переходов между маршрутами heap растёт монотонно, а в сравнении снапшотов много Detached HTMLElement. Что проверять первым?',
    options: [
      'Кеш декодированных изображений: браузер держит битмапы до ближайшей сборки мусора',
      'Замыкания в обработчиках: их надо переписать на WeakRef',
      'Не снятые слушатели, ResizeObserver и IntersectionObserver — они держат узлы, которых уже нет в документе',
      'Сам роутер: history-стек растёт с каждой навигацией и никогда не очищается',
    ],
    correct: 2,
    why: `<p>Detached-узлы — это элементы, удалённые из документа, но удерживаемые чем-то из JS. Классический держатель в SPA — подписка: <code>addEventListener</code> без снятия, <code>ResizeObserver</code> и <code>IntersectionObserver</code> без <code>disconnect()</code>, <code>setInterval</code> без <code>clearInterval</code>, подписка на стор, неотменённый <code>fetch</code>. Цепочка retainers в снапшоте показывает держателя прямо.</p>
    <p><code>WeakRef</code> и <code>WeakMap</code> — средство проектирования, а не лечение утечки: если ссылку держит массив обработчиков, слабая ссылка внутри ничего не изменит. Это самый привлекательный неверный ответ, потому что звучит как «продвинутое» решение.</p>
    <p>Замыкание захватывает всю область видимости целиком, а не одну переменную, поэтому одна функция способна удерживать мегабайтный ответ, который в ней даже не используется — но и это следствие живой подписки, а не самостоятельная причина.</p>`,
    cardId: 'wx11' },

  { id: 'qc11',
    deck: 'web',
    q: 'Пользователь набрал site.ru в адресной строке. Что даст наибольший выигрыш в TTFB?',
    snippet: `http://site.ru/         301 → https://site.ru/        184 мс
https://site.ru/        301 → https://www.site.ru/    212 мс
https://www.site.ru/    200                            640 мс  (Waiting 612 мс)`,
    options: [
      'Включить HTTP/3: QUIC экономит по одному RTT на каждом из трёх запросов',
      'Сжать HTML в brotli — 640 мс в основном уходят на передачу документа',
      'Убрать первый переход через HSTS preload и склеить оставшиеся редиректы в один',
      'Добавить preconnect к www.site.ru в самый первый ответ',
    ],
    correct: 2,
    why: `<p>Цепочка редиректов — самая недооценённая часть TTFB: здесь она стоит примерно 400 мс чистого ожидания, каждый шаг с собственным DNS, TCP и TLS. <strong>HSTS preload убирает первый переход целиком</strong> — браузер сразу идёт на https. Оставшийся редирект на канонический хост настраивается на уровне CDN, а лучше на уровне DNS, чтобы его не было вовсе.</p>
    <p>Сжатие соблазнительно, но не помогает: из 640 мс на передачу тела приходятся единицы миллисекунд, 612 мс — это Waiting, то есть работа сервера. Brotli уменьшит Content Download, которого в бюджете почти нет.</p>
    <p>HTTP/3 экономит на установке соединения и потому даёт ощутимый эффект именно там, где соединений много — то есть после устранения редиректов выигрыш будет уже меньше. <code>preconnect</code> в редиректе не спасает: чтобы его прочитать, браузер должен сначала получить сам редирект.</p>`,
    cardId: 'wx12' },

  { id: 'qc12',
    deck: 'web',
    q: 'Lighthouse даёт 98 баллов и TBT 40 мс, а в поле INP на 75-м перцентиле — 480 мс. Как это возможно?',
    options: [
      'TBT считается только на загрузке, а тяжёлый обработчик срабатывает на конкретном действии, до которого лаборатория не доходит',
      'INP и TBT измеряют одно и то же — значит, в поле неверно настроен сбор web-vitals',
      'В поле в INP попадает сеть: медленные ответы API включаются в интервал взаимодействия',
      'Lighthouse считает по 75-му перцентилю прогонов, а поле — по среднему',
    ],
    correct: 0,
    why: `<p>TBT — лабораторный прокси: он суммирует блокирующее время длинных задач в фиксированном окне загрузки. INP измеряет реальные взаимодействия за всю сессию. Корреляция между ними есть, совпадения нет: страница с идеальным TBT спокойно даёт ужасный INP, если тормозит обработчик, до которого Lighthouse просто не кликает — открытие фильтра, ввод в поиск, разворот аккордеона.</p>
    <p>Второй вариант — самая частая реакция («значит, метрика врёт»), и она уводит от разбора. Правильный шаг: собрать INP с атрибуцией через <code>PerformanceObserver</code> на тип <code>event</code> (или импорт <code>web-vitals/attribution</code>) и разложить на фазы — задержка до обработчика, сам обработчик, отрисовка. Без атрибуции цифра бесполезна.</p>
    <p>Сеть в INP не входит: метрика заканчивается на следующей отрисовке после взаимодействия, а не на приезде данных. И оба инструмента считают по p75, только по разным выборкам — прогонов и сессий.</p>`,
    cardId: 'wx13' },

  { id: 'qc13',
    deck: 'web',
    q: 'После релиза средний LCP не изменился, а жалоб на медленную загрузку стало заметно больше. Что смотреть?',
    options: [
      'Число сессий: рост трафика сам по себе ухудшает восприятие скорости',
      'Синтетику в CI: лабораторный прогон покажет регрессию точнее, чем поле',
      'Суммарный LCP по всем страницам: разбивка по маршрутам слишком груба',
      'p75 и p95 в разрезе устройства, страны и типа страницы: среднее прячет хвост',
    ],
    correct: 3,
    why: `<p>Среднее — худшая метрика для производительности: несколько быстрых сессий на десктопе компенсируют катастрофу на среднем Android, и график остаётся плоским. Google оценивает Core Web Vitals по <strong>75-му перцентилю</strong> именно поэтому. Плюс обязательна сегментация: без неё смена состава аудитории (рекламная кампания на мобильных) неотличима от регрессии кода.</p>
    <p>Синтетика на этот вопрос не отвечает вообще. Она хороша как страж регрессий в PR и как инструмент «почему», но лабораторный прогон не знает ни про сеть пользователей, ни про их устройства, ни про сторонние скрипты, добавленные через тег-менеджер после релиза.</p>
    <p>Отдельно стоит помнить про выживших: RUM видит только тех, кто дождался загрузки скрипта. Самые медленные сессии уходят, и поле выглядит лучше реальности — ещё один аргумент смотреть хвост, а не центр.</p>`,
    cardId: 'wx14' },

  { id: 'qc14',
    deck: 'web',
    q: 'Это LCP-элемент главной страницы. Что произойдёт с метриками?',
    snippet: `<img src="/hero.jpg" loading="lazy" decoding="async" class="hero" alt="">`,
    options: [
      'LCP улучшится: ленивая загрузка освободит канал для критичного CSS',
      'LCP ухудшится: запрос отложится до layout, а отсутствие width и height добавит CLS',
      'LCP не изменится, но вырастет TBT из-за асинхронного декодирования',
      'Ничего не изменится: браузер игнорирует lazy для изображений в первом экране',
    ],
    correct: 1,
    why: `<p><code>loading="lazy"</code> на LCP-изображении — типовая ошибка ценой примерно в секунду. Ленивая картинка не может быть обнаружена preload scanner-ом как срочная: браузер обязан дождаться layout, чтобы понять расстояние до вьюпорта, и только потом отправляет запрос. Вторая беда в том же теге — нет <code>width</code> и <code>height</code> (и нет <code>aspect-ratio</code> в CSS), поэтому место не резервируется и подстановка картинки двигает макет.</p>
    <p>Последний вариант отражает распространённое поверье. Браузеры действительно применяют эвристику и подгружают lazy-изображения с запасом от вьюпорта, но <strong>не игнорируют атрибут</strong>: запрос всё равно уходит после layout, а не из сканера. Разница в сотни миллисекунд.</p>
    <p><code>decoding="async"</code> сам по себе не портит TBT: он снимает декодирование с критического пути. На LCP-картинке его эффект иногда обратный — это стоит измерять, а не ставить по привычке.</p>`,
    cardId: 'wx16' },

  { id: 'qc15',
    deck: 'web',
    q: 'Изображение занимает 400 CSS-пикселей на любом экране. Какой файл скачает браузер на ноутбуке с DPR 1 и шириной окна 1440?',
    snippet: `<img
  src="/photo-1600.jpg"
  srcset="/photo-400.jpg 400w, /photo-800.jpg 800w, /photo-1600.jpg 1600w"
  alt="">`,
    options: [
      '/photo-400.jpg — браузер измерит реальную ширину элемента и выберет подходящий кандидат',
      '/photo-1600.jpg — при отсутствии sizes используется файл, указанный в src',
      '/photo-800.jpg — браузер берёт средний кандидат как безопасный компромисс',
      '/photo-1600.jpg — без sizes подразумевается 100vw, то есть 1440 пикселей ширины',
    ],
    correct: 3,
    why: `<p>Значение <code>sizes</code> по умолчанию — <code>100vw</code>. Браузер считает, что картинка займёт всю ширину вьюпорта, берёт 1440 и выбирает ближайший достаточный кандидат — 1600w. Трафик потрачен вчетверо больше нужного, а <code>srcset</code> формально «настроен».</p>
    <p>Первый вариант — главное заблуждение темы: браузер <strong>не измеряет</strong> элемент, потому что выбор делается до layout, из preload scanner. Исключение появилось недавно: <code>sizes="auto"</code> вместе с <code>loading="lazy"</code> действительно откладывает выбор до layout и берёт реальную ширину — но для LCP-изображения этот приём не годится, оно не должно быть ленивым.</p>
    <p>Правильно указать <code>sizes="400px"</code> (или выражение из тех же токенов брейкпоинтов, что и в CSS) и проверить выбор в DevTools по значению <code>currentSrc</code>. <code>src</code> тут только фолбэк для старых браузеров, и он не должен быть самым тяжёлым файлом.</p>`,
    cardId: 'wx17' },

  { id: 'qc16',
    deck: 'web',
    q: 'Заголовок h1 набран самостоятельно захостенным шрифтом с font-display: block и является LCP-кандидатом страницы. Чем это плохо?',
    options: [
      'До трёх секунд текст невидим (FOIT), и LCP не засчитывается, пока шрифт не приедет',
      'Текст сразу рисуется системным шрифтом, а подмена даёт сдвиг макета и портит CLS',
      'block отключает кеширование шрифта, и файл качается на каждой навигации',
      'block запрещает применять шрифт до события load документа',
    ],
    correct: 0,
    why: `<p><code>font-display: block</code> даёт блокирующий период около 3 секунд: контейнер отрисован, текста в нём нет. Для LCP это худший сценарий — кандидат не засчитывается, пока не появятся пиксели текста, поэтому метрика уезжает ровно на время загрузки шрифта.</p>
    <p>Второй вариант описывает <code>font-display: swap</code> и его беду — FOUT со сдвигом. Он привлекателен тем, что действительно является второй половиной этой темы; отличать их надо чётко: <strong>block прячет текст, swap подменяет шрифт</strong>. Нулевой сдвиг при swap достигается метрически подогнанным фолбэком через <code>size-adjust</code>, <code>ascent-override</code> и <code>descent-override</code>.</p>
    <p>Кеширование <code>font-display</code> не трогает вообще. И полезно помнить: с 2020 года HTTP-кеш партиционирован по сайту верхнего уровня, поэтому аргумент «шрифт уже лежит в кеше от другого сайта» больше не работает — self-hosting плюс <code>preload</code> с <code>crossorigin</code> честнее.</p>`,
    cardId: 'wx19' },

  { id: 'qc17',
    deck: 'web',
    q: 'Насколько эта политика защищает от XSS?',
    snippet: `Content-Security-Policy:
  default-src 'self';
  script-src 'self' 'unsafe-inline' https://cdn.site.ru;
  img-src *;`,
    options: [
      'Защищает: внешние скрипты ограничены своим доменом и одним доверенным CDN',
      'Почти не защищает: unsafe-inline разрешает выполнение любого внедрённого inline-скрипта',
      'Защищает частично: дыра только в img-src *, через который можно вытащить данные',
      'Не защищает, потому что нет report-uri и нарушения политики не видны',
    ],
    correct: 1,
    why: `<p><code>unsafe-inline</code> в <code>script-src</code> обесценивает заголовок целиком: типовая XSS вставляет именно inline-скрипт, и политика его разрешает. Заголовок остаётся, ощущение защищённости остаётся, защиты нет. Если фреймворк требует <code>unsafe-inline</code> или <code>unsafe-eval</code> — это повод чинить сборку, а не политику.</p>
    <p>Первый вариант — ровно та логика, по которой такие политики и попадают в прод: allowlist доменов выглядит убедительно, но он ограничивает <strong>откуда грузить</strong>, а не <strong>что выполнять из самой страницы</strong>.</p>
    <p>Здесь же не хватает трёх директив, которые забывают чаще всего: <code>base-uri 'none'</code> (без неё инъекция <code>base href</code> уводит все относительные скрипты на чужой хост), <code>object-src 'none'</code> и <code>form-action 'self'</code>. И отдельно: CSP — второй рубеж, она не заменяет экранирование.</p>`,
    cardId: 'wx20' },

  { id: 'qc18',
    deck: 'web',
    q: 'HTML отдаётся с CDN с этими заголовками. Что здесь сломано?',
    snippet: `Cache-Control: public, max-age=600
Content-Security-Policy: script-src 'nonce-8IBTHwOdqNKAWeKl7plt8g==' 'strict-dynamic'`,
    options: [
      'strict-dynamic несовместим с nonce: работает либо один механизм, либо другой',
      'Значение nonce слишком короткое, его подберут перебором',
      'Кешированный HTML раздаёт всем один и тот же nonce — это эквивалент unsafe-inline',
      'Для кешируемого ответа nonce надо продублировать в мета-теге, иначе он теряется',
    ],
    correct: 2,
    why: `<p>Смысл nonce в том, что он <strong>уникален для каждого ответа</strong> и атакующему неизвестен. Как только HTML попал в общий кеш на десять минут, один и тот же nonce уезжает тысячам пользователей: атакующий открывает страницу, читает значение из разметки и подставляет его в свой тег. Политика превращается в <code>unsafe-inline</code> с лишними шагами.</p>
    <p>Выхода два: либо hash-основанная политика для статики на CDN, либо подстановка nonce на edge при отдаче (тогда сам HTML кешируется как шаблон). Первый вариант неверен: <code>strict-dynamic</code> — это как раз добавка к nonce или hash, снимающая проблему бандлеров, которые подгружают чанки программно.</p>
    <p>Nonce в примере — 16 случайных байт в base64, то есть 128 бит, ровно как требуется. И <code>meta</code> здесь ничего бы не дал: он не поддерживает <code>frame-ancestors</code>, <code>report-uri</code> и <code>sandbox</code>, а nonce и так виден в заголовке.</p>`,
    cardId: 'wx21' },

  { id: 'qc19',
    deck: 'web',
    q: 'Строгая CSP с nonce уже внедрена, но пентест нашёл XSS в этом коде. Что закрывает дыру?',
    snippet: `// CSP: script-src 'nonce-…' 'strict-dynamic'; никаких unsafe-inline
const q = new URLSearchParams(location.search).get('q');
resultsEl.innerHTML = 'Вы искали: ' + q;`,
    options: [
      'Директива require-trusted-types-for "script" плюс санитайзер в точке записи',
      'Замена innerHTML на insertAdjacentHTML: он не выполняет вставленную разметку',
      'Перевод сайта на HSTS: без строгого HTTPS CSP применяется не полностью',
      'Добавление object-src none и base-uri none — именно они закрывают DOM XSS',
    ],
    correct: 0,
    why: `<p>CSP управляет тем, <strong>какой код разрешено загружать и выполнять как скрипт</strong>. DOM XSS живёт внутри уже доверенного кода: ваш собственный скрипт с валидным nonce берёт данные из URL и кладёт их в опасный sink. Trusted Types закрывают именно этот класс — они запрещают передавать в <code>innerHTML</code>, <code>outerHTML</code>, <code>document.write</code>, <code>eval</code> обычную строку, и требуют объект, прошедший через явную политику с санитайзером.</p>
    <p><code>insertAdjacentHTML</code> — это тот же sink, а не лечение: он разбирает переданную строку как HTML ровно так же. Такой ответ выглядит правдоподобно и встречается в ревью чаще всего.</p>
    <p>Внедряют Trusted Types не флагом, а проектом: сначала <code>Content-Security-Policy-Report-Only</code>, сбор нарушений, правка библиотек (старые UI-киты и обёртки над jQuery пишут в <code>innerHTML</code> повсеместно). Политика <code>default</code>, возвращающая строку как есть, отключает всю защиту — если её и добавлять, то с санитайзером и логированием.</p>`,
    cardId: 'wx22' },

  { id: 'qc20',
    deck: 'web',
    q: 'В первый же день на домен с двумя десятками поддоменов ставят Strict-Transport-Security с max-age на год, includeSubDomains и preload. Часть поддоменов — внутренние стенды по http. Чем это опасно?',
    options: [
      'Ничем: HSTS действует только на те поддомены, которые пользователь уже посещал',
      'includeSubDomains сделает http-стенды недоступными, а выход из preload-списка займёт месяцы',
      'max-age в год слишком велик — браузер отбросит такой заголовок как невалидный',
      'preload нельзя сочетать с includeSubDomains: это взаимоисключающие директивы',
    ],
    correct: 1,
    why: `<p><code>includeSubDomains</code> распространяет требование HTTPS на <strong>все</strong> поддомены сразу, включая забытые стенды, IoT-железки и почтовые шлюзы. А <code>preload</code> — это заявка на вшитый в браузеры список, который доезжает до пользователей вместе с новой версией браузера: удаление занимает месяцы, и быстро откатить решение нельзя.</p>
    <p>Первый вариант описывает поведение HSTS <strong>без</strong> preload: там политика действительно узнаётся из ответа, то есть только после визита. Именно этим preload и отличается — первый визит тоже защищён, но и ошибка становится необратимой.</p>
    <p>Правильная последовательность: <code>max-age=300</code> → проверить абсолютно все поддомены → неделя → год → добавить <code>includeSubDomains</code> → выждать → только затем <code>preload</code>. И помнить, что HSTS не защищает от ошибочно выданного сертификата — для этого CAA-записи и мониторинг Certificate Transparency.</p>`,
    cardId: 'wx23' },

  { id: 'qc21',
    deck: 'web',
    q: 'Насколько страница защищена от clickjacking?',
    snippet: `X-Frame-Options: ALLOWALL
Content-Security-Policy: default-src 'self'

// и на странице:
if (top !== self) top.location = self.location;`,
    options: [
      'Защищена: более строгая CSP перекрывает ошибочный X-Frame-Options',
      'Защищена скриптом: frame-busting сработает раньше, чем пользователь успеет кликнуть',
      'Не защищена: ALLOWALL — несуществующее значение, а frame-busting гасится атрибутом sandbox',
      'Не защищена только в Safari: остальные браузеры трактуют неизвестное значение как DENY',
    ],
    correct: 2,
    why: `<p><code>ALLOWALL</code> не существует в спецификации: валидны только <code>DENY</code> и <code>SAMEORIGIN</code>. Неизвестное значение браузер игнорирует, то есть заголовок стоит, а защиты нет — самый коварный вид ошибки. И <code>default-src</code> его не подстрахует: у <code>frame-ancestors</code> нет отката к <code>default-src</code>, её нужно писать явно.</p>
    <p>Скриптовый frame-busting обходится тривиально: атакующий встраивает iframe с <code>sandbox</code> без <code>allow-top-navigation</code>, и <code>top.location = …</code> просто ничего не делает. Полагаться на него нельзя, это второй по популярности неверный ответ после «CSP перекроет».</p>
    <p>Правильно: <code>Content-Security-Policy: frame-ancestors 'none'</code> (или список доверенных), и обязательно <strong>на всех роутах</strong>, включая страницы ошибок и редиректы. Дополнительные рубежи — <code>SameSite=Lax</code> на сессионной куке и повторное подтверждение для критичных операций.</p>`,
    cardId: 'wx24' },

  { id: 'qc22',
    deck: 'web',
    q: 'Страница сброса пароля открыта по адресу вида /reset?token=9f3c1a…, сайт отдаёт Referrer-Policy: unsafe-url, а на странице стоит виджет поддержки со стороннего домена. Что произойдёт?',
    options: [
      'Ничего: Referer не отправляется со страниц, открытых по одноразовой ссылке',
      'Полный URL вместе с токеном уедет в заголовке Referer в логи стороннего домена',
      'Уйдёт только origin: браузер обрезает query у кросс-доменных запросов',
      'Ничего: unsafe-url влияет на переходы по ссылкам, но не на загрузку подресурсов',
    ],
    correct: 1,
    why: `<p><code>unsafe-url</code> означает буквально «отправляй полный URL всегда» — и при переходах, и при загрузке подресурсов, и на чужие домены, и даже при downgrade на http. Токен сброса пароля окажется в access-логах чужого веб-сервера, в их аналитике и в их системе мониторинга.</p>
    <p>Третий вариант описывает поведение <code>strict-origin-when-cross-origin</code> — разумного значения по умолчанию в современных браузерах. Именно поэтому он выглядит правдоподобно: люди помнят дефолт и не замечают, что его переопределили.</p>
    <p>Но корень проблемы не в заголовке: <strong>секрета не должно быть в URL</strong>. Referrer-Policy лишь смягчает последствия. Токен должен быть одноразовым, короткоживущим и в идеале передаваться POST-формой. И помните, что полностью убирать реферер тоже не всегда правильно: на нём держатся антифрод и часть партнёрских интеграций.</p>`,
    cardId: 'wx25' },

  { id: 'qc23',
    deck: 'web',
    q: 'Документ загружен с адреса A. С каким из остальных у него совпадает origin?',
    snippet: `A: https://site.ru:443/page
B: https://site.ru/other
C: https://app.site.ru/page
D: http://site.ru/page`,
    options: [
      'Только с B',
      'С B и C: поддомен входит в тот же origin',
      'С B и D: схема в origin не входит, важны хост и порт',
      'Ни с одним: явный порт 443 делает origin отличным от неявного',
    ],
    correct: 0,
    why: `<p>Origin — это тройка <strong>схема + хост + порт</strong>. Порт 443 для https подразумевается, поэтому A и B — один и тот же origin. C отличается хостом, D — схемой.</p>
    <p>Вариант с поддоменом — источник половины ошибок в теме, потому что путает origin и site. Для SOP <code>app.site.ru</code> и <code>site.ru</code> изолированы полностью: DOM друг друга они не прочитают. А вот для куки это один сайт: значение с <code>Domain=site.ru</code> уедет обоим, и <code>SameSite</code> между ними не сработает — оба запроса same-site.</p>
    <p>Полезные следствия: <code>localhost</code> и <code>127.0.0.1</code> — разные origin, <code>:3000</code> и <code>:3001</code> изолированы, а <code>file://</code> в современных браузерах считает каждый файл отдельным origin (отсюда «работает через сервер, не работает по двойному клику»). Механизм <code>document.domain</code>, который раньше позволял поддоменам договориться, признан опасным и по умолчанию отключён.</p>`,
    cardId: 'wx26' },

  { id: 'qc24',
    deck: 'web',
    q: 'Что сделает браузер с этой кукой?',
    snippet: `Set-Cookie: sid=8f21a0...; SameSite=None; HttpOnly; Path=/`,
    options: [
      'Примет и будет отправлять во всех кросс-сайтовых запросах',
      'Примет, но молча понизит до SameSite=Lax',
      'Примет, но отправлять будет только в same-site запросах',
      'Отбросит целиком: SameSite=None без атрибута Secure невалиден',
    ],
    correct: 3,
    why: `<p><code>SameSite=None</code> обязан идти вместе с <code>Secure</code> — иначе кука отбрасывается при установке. Симптом на проде выглядит как «пользователь не логинится, а в ответе Set-Cookie есть», и ищут его долго, потому что ошибка не в приложении, а в атрибутах.</p>
    <p>Понижение до <code>Lax</code> — самый привлекательный неверный ответ, потому что браузеры действительно применяют <code>Lax</code> по умолчанию, когда атрибут <strong>не указан вовсе</strong>. Явный <code>None</code> без <code>Secure</code> — не «нет значения», а невалидная комбинация.</p>
    <p>Про SameSite стоит помнить ещё три вещи: <code>Lax</code> не защищает от CSRF через изменяющий состояние GET; SameSite не изолирует поддомены (для этого префикс <code>__Host-</code>, который требует Secure, запрещает Domain и требует Path=/); и SameSite не заменяет CSRF-токен — он бесполезен при XSS и при подконтрольном атакующему поддомене.</p>`,
    cardId: 'wx27' },

  { id: 'qc25',
    deck: 'web',
    q: 'Что выведет последняя строка и кто в этом виноват?',
    snippet: `const body = '{"__proto__": {"isAdmin": true}}';
const cfg = JSON.parse(body);
deepMerge(defaults, cfg);          // рекурсивный мердж без фильтрации ключей
console.log({}.isAdmin);`,
    options: [
      'undefined: JSON.parse отбрасывает ключ __proto__ при разборе',
      'true: виноват JSON.parse, он записывает значение прямо в прототип',
      'true: JSON.parse создал обычное собственное свойство, а прототип загрязнил deepMerge',
      'undefined: присваивание через __proto__ работает только в Object.assign',
    ],
    correct: 2,
    why: `<p><code>JSON.parse</code> сам по себе безопасен: он создаёт у объекта <strong>собственное</strong> свойство с именем <code>__proto__</code>, минуя сеттер прототипа. Опасен следующий шаг — рекурсивный мердж, который обходит ключи и делает <code>target[key] = …</code>: вот здесь срабатывает сеттер, и запись уходит в <code>Object.prototype</code>. После этого <code>isAdmin</code> есть у каждого объекта приложения.</p>
    <p>Вариант «виноват JSON.parse» — самая частая ошибка на собеседовании, и различие принципиально: чинить надо мердж, а не парсинг. Помогают отбрасывание опасных ключей (<code>__proto__</code>, <code>constructor</code>, <code>prototype</code>), <code>Object.create(null)</code> для словарей и схема-валидация входа.</p>
    <p>Тонкость, о которой узнают больно: проверка <code>if (key in target)</code> вместо <code>Object.hasOwn(target, key)</code> сама становится гаджетом — загрязнённый прототип отвечает «есть» на любое имя. Исторические CVE есть у lodash merge и set, jQuery extend, minimist и ряда парсеров query-строк.</p>`,
    cardId: 'wx28' },

  { id: 'qc26',
    deck: 'web',
    q: 'Что произойдёт с этими двумя тегами?',
    snippet: `<script src="https://cdn.other.ru/lib.js"
        integrity="sha384-oqVuAfXRKap7fdgcCY5uykM6+R9GqQ8K/uxy9rx7HNQlGYl1kPzQho1wx4JwY8wC"></script>

<script src="https://www.googletagmanager.com/gtm.js?id=GTM-XXXX"
        integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo"
        crossorigin="anonymous"></script>`,
    options: [
      'Первый не выполнится из-за отсутствия crossorigin, второй сломается при первом же изменении контейнера GTM',
      'Оба отработают: crossorigin нужен только для стилей и шрифтов',
      'Первый отработает, второй заблокируется CORS: GTM не отдаёт Access-Control-Allow-Origin',
      'Оба заблокируются: SRI не применяется к кросс-доменным ресурсам в принципе',
    ],
    correct: 0,
    why: `<p>Чтобы посчитать хеш, браузер должен получить полный доступ к телу ответа, а для кросс-доменного скрипта это возможно только в режиме CORS. Без <code>crossorigin="anonymous"</code> ответ непрозрачен, проверка провалится и скрипт не выполнится — тег выглядит «защищённым», а на деле просто сломан.</p>
    <p>Второй тег синтаксически корректен, но бессмыслен по замыслу: <code>gtm.js</code> генерируется под текущее содержимое контейнера и меняется каждый раз, когда маркетолог правит теги. Хеш протухнет в тот же день и уронит загрузку. И это грустная ирония SRI: она бесполезна ровно для того стороннего кода, который представляет главный риск — аналитики, A/B-скриптов, «latest»-версий библиотек.</p>
    <p>Для собственных ассетов SRI обычно избыточна: если вы контролируете CDN и используете имена с contenthash, подмена уже исключена. Для динамического <code>import()</code> атрибута нет — целостность модулей задаётся через <code>integrity</code> в import map.</p>`,
    cardId: 'wx30' },

  { id: 'qc27',
    deck: 'web',
    q: 'Сколько здесь проблем и какая главная?',
    snippet: `// header
{ "alg": "none", "typ": "JWT" }
// payload
{ "sub": 42, "role": "admin", "email": "a@b.ru", "exp": 1893456000 }

// проверка на сервере
jwt.verify(token, secret);          // без опции algorithms`,
    options: [
      'Одна: exp слишком далеко, токен живёт годами',
      'Две: подпись фактически не проверяется из-за alg none, а payload читает любой, кто перехватил токен',
      'Ни одной: alg none допустим, раз verify вызван с секретом',
      'Одна: email в payload — утечка, но подпись при явно переданном secret всё равно проверяется',
    ],
    correct: 1,
    why: `<p><code>verify</code> без явного списка <code>algorithms</code> доверяет полю <code>alg</code> из самого токена — то есть атакующему. Указав <code>none</code>, он отключает проверку подписи целиком и подставляет любой payload, включая <code>role: admin</code>. Правильно всегда передавать <code>algorithms: ['RS256']</code> (или тот, что вы реально используете).</p>
    <p>Вторая проблема — содержимое. JWT <strong>подписан, но не зашифрован</strong>: base64url читается в одну строку, и <code>email</code> утекает при любом логировании — токены оседают в access-логах прокси, в Sentry, в URL при неаккуратной интеграции. Четвёртый вариант ловит эту половину и промахивается по первой: явный secret не помогает, если алгоритм берётся из токена.</p>
    <p>И архитектурный минус, который спрашивают следом: JWT нельзя отозвать без состояния на сервере — до <code>exp</code> он валиден, даже если пользователя заблокировали. Отсюда короткий TTL access-токена в 5–15 минут и отдельный механизм refresh.</p>`,
    cardId: 'wx31' },

  { id: 'qc28',
    deck: 'web',
    q: 'Пять параллельных запросов одновременно получают 401. Refresh-токен ротируется при каждом обновлении. Что будет без дополнительной защиты?',
    options: [
      'Все пять обновятся успешно: сервер выдаст пять валидных пар токенов',
      'Четыре запроса предъявят уже использованный refresh, сервер сочтёт это кражей и убьёт сессию',
      'Браузер сам сериализует одинаковые запросы к одному URL, поэтому обновление будет одно',
      'Обновление не запустится вовсе: refresh в HttpOnly-куке недоступен из JS',
    ],
    correct: 1,
    why: `<p>Ротация означает, что каждый refresh выдаёт новую пару и инвалидирует старую, а повторное предъявление старого токена трактуется как кража. Пять интерцепторов уходят обновляться одновременно: первый успевает, остальные четыре приходят со ставшим недействительным токеном и вызывают ложное срабатывание reuse detection — пользователя выкидывает без всякой атаки.</p>
    <p>Третий вариант — распространённое заблуждение: браузер дедуплицирует только кешируемые GET-запросы, а никак не POST к <code>/auth/refresh</code>. Четвёртый тоже неверен: кука <code>HttpOnly</code> недоступна для чтения из JS, но браузер прикладывает её к запросу автоматически — именно поэтому такая схема и работает.</p>
    <p>Лечение — единый promise-мьютекс в интерцепторе: первый запрос запускает обновление, остальные ждут его результата и повторяются. Между вкладками ту же роль играет <code>navigator.locks.request('auth-refresh', …)</code> или координация через BroadcastChannel.</p>`,
    cardId: 'wx32' },

  { id: 'qc29',
    deck: 'web',
    q: 'Со страницы https://app.site.ru идёт fetch на https://api.site.ru/me с credentials: include. API отвечает заголовками Access-Control-Allow-Origin: * и Access-Control-Allow-Credentials: true. Что получит клиент?',
    options: [
      'Данные: заголовки заданы, а звёздочка разрешает любой origin',
      'Данные, но без возможности прочитать заголовки ответа из JS',
      'Ошибку CORS: со включёнными credentials звёздочка в Allow-Origin запрещена',
      'Ошибку: Allow-Credentials работает только в паре с preflight-запросом OPTIONS',
    ],
    correct: 2,
    why: `<p>Спецификация запрещает комбинацию <code>Access-Control-Allow-Origin: *</code> и <code>Access-Control-Allow-Credentials: true</code>. С учётными данными сервер обязан назвать конкретный origin — <code>https://app.site.ru</code> — и добавить <code>Vary: Origin</code>, чтобы промежуточные кеши не отдали чужой заголовок. Иначе любой сайт мог бы читать ответы, полученные с чужой сессией.</p>
    <p>Первый вариант — обычная логика «звёздочка значит всем можно», и именно она приводит к получасовому поиску в логах бэкенда. Второй описывает другой механизм: читаемость заголовков ответа регулируется <code>Access-Control-Expose-Headers</code>, и это отдельная настройка.</p>
    <p>Заодно стоит помнить, что CORS — это не защита сервера: он ограничивает <strong>чтение ответа скриптом</strong>, а сам запрос уходит и обрабатывается. Защита от кросс-сайтовых действий — это SameSite, CSRF-токены и проверка Fetch Metadata.</p>`,
    cardId: 'w9' },

  { id: 'qc30',
    deck: 'web',
    q: 'Заголовки деплоя выглядят так, а старая папка assets удаляется сразу после выкладки. Что сломается?',
    snippet: `/index.html            Cache-Control: public, max-age=31536000, immutable
/assets/app.4f2a19.js  Cache-Control: no-cache`,
    options: [
      'Ничего: имя с хешем всё равно обеспечивает корректную инвалидацию',
      'Пользователи год не увидят новых релизов, а открытые вкладки поймают ChunkLoadError',
      'Пострадает только скорость: no-cache на статике добавит по одному запросу на файл',
      'Сломается только Service Worker: он закеширует index.html навсегда',
    ],
    correct: 1,
    why: `<p>Заголовки перепутаны местами. Точка входа обязана ревалидироваться (<code>no-cache</code> или короткий TTL со <code>stale-while-revalidate</code>), а хешированная статика — кешироваться вечно как <code>immutable</code>. Здесь наоборот: <code>index.html</code> замораживается на год, поэтому пользователь остаётся на старой разметке со ссылками на файлы, которых уже нет.</p>
    <p>Вторая половина беды — удаление старой папки при деплое. Даже с правильными заголовками открытая час назад вкладка при переходе в ленивый раздел получит «Failed to fetch dynamically imported module». Старые ассеты должны жить хотя бы сутки, а обработчик такой ошибки — показывать «вышла новая версия, обновите страницу» с одной попыткой ретрая, а не белый экран.</p>
    <p>Первый вариант звучит убедительно ровно до момента, когда понимаешь: contenthash решает задачу «не отдать старый файл под новым именем», но ничего не может сделать с HTML, который сам застрял в кеше.</p>`,
    cardId: 'wx41' },

  { id: 'qc31',
    deck: 'web',
    q: 'Что здесь стоит чинить в первую очередь?',
    snippet: `GET /api/orders
  Queueing            3 мс
  Stalled           412 мс
  DNS / TLS           0 мс
  Request sent        1 мс
  Waiting (TTFB)    880 мс
  Content Download   24 мс`,
    options: [
      'Уменьшить размер ответа: 880 мс — это передача тела',
      'Сократить число одновременных запросов к хосту или перейти на HTTP/2: 412 мс Stalled — ожидание свободного сокета',
      'Добавить preconnect: нулевые DNS и TLS означают, что соединение каждый раз новое',
      'Ускорить рендер: 24 мс Content Download показывают, что узкое место на клиенте',
    ],
    correct: 1,
    why: `<p>Stalled в 412 мс — это время в очереди до отправки. Почти всегда это HTTP/1.1 с лимитом в шесть соединений на хост: запрос ждёт, пока освободится сокет. Лечится переходом на HTTP/2 или HTTP/3, где мультиплексирование снимает лимит, либо сокращением параллельных запросов.</p>
    <p>880 мс Waiting — это TTFB, то есть работа сервера. Фронтенд его не починит ничем, кроме кеширования и CDN, поэтому «уменьшить размер ответа» мимо: на передачу тела ушло 24 мс, размер тут ни при чём.</p>
    <p>Нулевые DNS и TLS означают ровно обратное: соединение <strong>переиспользовано</strong>, устанавливать нечего, <code>preconnect</code> бесполезен. И общее правило чтения водопада: без включённого троттлинга картина искажена, а строки вроде «from disk cache» и «ServiceWorker» означают, что сети не было вовсе — время в них ни о чём не говорит.</p>`,
    cardId: 'wx35' },

  { id: 'qc32',
    deck: 'web',
    q: 'Анализатор показывает 780 КБ в основном чанке. Какое утверждение верно?',
    snippet: `import _ from 'lodash';
import moment from 'moment';
import { Chart } from 'chart.js';

// package.json — поля sideEffects нет`,
    options: [
      'Дефолтный импорт lodash безопасен: бандлер вытрясет неиспользуемое сам',
      'moment попадёт в бандл вместе со всеми локалями — tree shaking его не берёт',
      'Отсутствие sideEffects ускоряет сборку, но на размер бандла не влияет',
      'chart.js при именованном импорте выносится в отдельный чанк автоматически',
    ],
    correct: 1,
    why: `<p><code>moment</code> — CommonJS-пакет, который подтягивает локали динамическим require по вычисляемому пути. Статический анализ здесь бессилен: бандлер не может доказать, какие локали не нужны, и кладёт все. Это десятки килобайт мёртвого кода, и починить их можно только заменой библиотеки (date-fns, Day.js, Temporal) или плагином, вырезающим локали.</p>
    <p>Первый вариант — самое популярное заблуждение: <code>import _ from 'lodash'</code> тянет весь пакет, потому что это CommonJS-модуль с единым экспортом. Нужны либо точечные импорты, либо <code>lodash-es</code>.</p>
    <p>Отсутствие <code>sideEffects</code> напрямую влияет на размер: без этого поля бандлер обязан считать, что импорт модуля может что-то сделать «в сторону», и не удаляет его. А динамическое разделение <code>chart.js</code> надо делать руками через <code>import()</code> — само оно не произойдёт. И смотреть всегда надо сжатый размер: несжатые цифры в анализаторе вводят в заблуждение.</p>`,
    cardId: 'wx37' },

  { id: 'qc33',
    deck: 'web',
    q: 'В конфиге сборки стоит sourcemap: true, и .map-файлы деплоятся рядом с бандлами на публичный CDN. Чем это плохо?',
    options: [
      'Ничем: карты нужны Sentry для читаемых стектрейсов, их публичность безвредна',
      'Только весом: карты удваивают объём деплоя, достаточно поставить sourcemap: hidden',
      'Карты с sourcesContent публикуют исходники; правильно — hidden, загрузка в мониторинг и удаление с CDN отдельным шагом',
      'Браузер грузит карты всем пользователям, и это напрямую бьёт по LCP',
    ],
    correct: 2,
    why: `<p><code>sourcemap: true</code> кладёт рядом с бандлом карту с полем <code>sourcesContent</code> — то есть фактически выкладывает исходники в открытый доступ: комментарии, внутренние URL, имена приватных эндпоинтов, иногда ключи и заметки вида «TODO: тут нет проверки прав». Это не только утечка кода, но и готовая разведка для атакующего.</p>
    <p>Правильная схема: <code>sourcemap: 'hidden'</code> (карта собирается, но ссылки на неё в бандле нет), загрузка карт в мониторинг на деплое и удаление с публичного CDN отдельным шагом пайплайна. Забытый шаг удаления — самая частая утечка исходников.</p>
    <p>Последний вариант неверен механически: браузер запрашивает карту только при открытых devtools, обычные пользователи её не качают, и на LCP она не влияет. И отдельная деталь про Sentry: нужен стабильный идентификатор (<code>debug_id</code> или связка release и dist) — без него карты загрузятся, но не применятся, и стектрейсы останутся минифицированными.</p>`,
    cardId: 'wx40' },

  { id: 'qc34',
    deck: 'web',
    q: 'Нужен канал серверных уведомлений: клиент только читает, обновления идут пачками раз в несколько секунд, у заказчика корпоративный прокси. Что выбрать?',
    options: [
      'WebSocket с реконнектом: другого способа получать инициативу от сервера нет',
      'Long polling: он единственный проходит через любые прокси',
      'SSE: односторонний поток поверх обычного HTTP, с автопереподключением и Last-Event-ID',
      'Опрос раз в 5 секунд: проще и предсказуемее любого стрима',
    ],
    correct: 2,
    why: `<p>Требования описывают SSE почти дословно: поток нужен только в одну сторону, это обычный HTTP-ответ с <code>Content-Type: text/event-stream</code>, поэтому он проходит через прокси и корпоративные фильтры без отдельной настройки апгрейда протокола. Плюс встроенное переподключение и заголовок <code>Last-Event-ID</code>, по которому сервер досылает пропущенное за разрыв.</p>
    <p>WebSocket — правильный выбор, когда нужен двусторонний канал с низкой задержкой (набор текста, presence, подтверждения прочтения). Здесь двусторонности нет, а платить пришлось бы своим реконнектом, своим heartbeat и проблемами с прокси. Это самый привлекательный неверный ответ, потому что «реалтайм» на автомате переводится в «вебсокет».</p>
    <p>Опрос раз в 5 секунд — честный фолбэк, но при десятках тысяч клиентов это постоянная нагрузка вхолостую и задержка до 5 секунд. Long polling держит соединение на каждый запрос и добавляет лишний RTT.</p>`,
    cardId: 'w12' },

  { id: 'qc35',
    deck: 'web',
    q: 'Клиенту нужно хранить 200 МБ истории и читать её из воркера диапазонными запросами. Что выбрать?',
    options: [
      'localStorage: он синхронный, а значит самый быстрый на чтении',
      'Cache API: он не ограничен квотой и умеет запросы по диапазону ключей',
      'sessionStorage с разбиением на чанки: переживает перезагрузку вкладки',
      'IndexedDB: асинхронна, доступна из воркера, даёт составные ключи и курсоры по диапазону',
    ],
    correct: 3,
    why: `<p>IndexedDB — единственное хранилище браузера, которое одновременно переживает перезагрузку, держит сотни мегабайт, поддерживает составные ключи и индексы, отдаёт курсоры по диапазону и доступно из Worker и Service Worker. Составной ключ вида <code>[chatId, messageId]</code> сам по себе даёт и уникальность, и сортировку, и запрос «50 записей старше X».</p>
    <p>localStorage отпадает физически: он синхронный (каждое чтение блокирует главный поток), ограничен 5–10 МБ на origin, хранит только строки и недоступен из воркера. «Синхронный значит быстрый» — ровно то заблуждение, из-за которого им продолжают пользоваться не по назначению.</p>
    <p>Cache API отлично подходит для бинарей и ответов, но его ключ — это <code>Request</code>: запрос по диапазону превращается в перебор всех ключей. Разумная схема — данные в IndexedDB, байты медиа в Cache API. И квота есть у всех: в Chromium это порядка 60% свободного места на диске, а при нехватке браузер вытесняет origin целиком.</p>`,
    cardId: 'w13' },

  { id: 'qc36',
    deck: 'sd',
    q: 'Менеджер обновлений мессенджера получил это событие. Что он обязан сделать?',
    snippet: `// локальное состояние клиента
state.pts = 1041

// событие из сокета
{ type: 'newMessage', pts: 1045, ptsCount: 1, message: { … } }`,
    options: [
      'Применить: pts больше локального, значит событие свежее',
      'Отбросить: разрыв в счётчике означает, что событие адресовано другому клиенту',
      'Применить и выставить локальный pts в 1045 — пропущенное догонится следующим событием',
      'Не применять: положить в буфер, подождать около полусекунды и, если пропуск не закрылся, запросить разницу',
    ],
    correct: 3,
    why: `<p>Правило применения ровно одно: событие применяется, если <code>local_pts + ptsCount === pts</code>. Здесь 1041 + 1 = 1042, а пришло 1045 — значит, три позиции пропущено. Применить такое событие означает <strong>молча потерять три обновления</strong> и никогда об этом не узнать: счётчик уедет вперёд, и сервер будет считать пропущенное доставленным.</p>
    <p>Пауза перед объявлением пропуска — деталь из реального протокола: события регулярно приходят не по порядку из-за конкурентных запросов, и недостающее часто приезжает само за десятки миллисекунд. Полсекунды ожидания снимают большую часть ложных срабатываний и лишних запросов разницы.</p>
    <p>Дальше важен порядок: пока разница тянется, поток обновлений <strong>только копится и не применяется</strong>, разница применяется первой, буфер — после неё и отсортированным по pts. Иначе состояние разъедется необратимо. Событие с <code>pts</code> ниже локального, наоборот, просто выбрасывается — оно уже применено.</p>`,
    cardId: 'msg6' },

  { id: 'qc37',
    deck: 'sd',
    q: 'Клиент был офлайн двое суток. На запрос разницы сервер отвечает «слишком велика». Что делать с локальной базой?',
    options: [
      'Оставить историю, перезагрузить список диалогов и досинхронизировать чаты лениво при открытии',
      'Стереть базу целиком и скачать историю заново — только так гарантируется консистентность',
      'Продолжать запрашивать разницу порциями, пока сервер не отдаст всё накопленное',
      'Переключиться на синхронизацию по timestamp: счётчик состояния уже бесполезен',
    ],
    correct: 0,
    why: `<p>Ящик событий на сервере конечен. Если клиентский счётчик вывалился за его окно, рассказать историю по шагам сервер физически не может — но <strong>старые сообщения от этого не перестали быть валидными</strong>. Невалидны только счётчики непрочитанного и факты удаления. Поэтому: берём свежее состояние, перезагружаем топ диалогов по свежести, для открытых чатов дозагружаем хвост, остальное помечаем «возможно устарело» и досинхронизируем при открытии.</p>
    <p>Стереть базу — самый частый и самый дорогой ответ. Это минуты ожидания на медленной сети, потеря локального индекса поиска и потеря того, что сервер может уже не отдать. Радикальная очистка оправдана только при реально битой базе (пробная запись в kv не проходит).</p>
    <p>Ответ «запрашивать порциями» не работает: сервер не «не хочет», он не хранит эти события. А переход на timestamp ломается на рассинхроне часов, одинаковых метках и событиях без сообщений — прочитано, отредактировано, удалено.</p>
    <p>Отдельная неприятность: сообщение, удалённое за время офлайна, останется у нас навсегда, если событие не поместилось в разницу. Лечится сверкой при загрузке окна — сервер отдаёт актуальные id диапазона, лишнее локальное вычищаем.</p>`,
    cardId: 'msg6' },

  { id: 'qc38',
    deck: 'sd',
    q: 'По какому ключу сортировать сообщения в ленте чата?',
    options: [
      'По серверному времени: оно нормализовано и одинаково у всех клиентов',
      'По времени получения клиентом: только оно отражает порядок, который реально видел пользователь',
      'По составному ключу (дата, id автора): так стабильно группируются серии сообщений одного человека',
      'По messageId, монотонному внутри чата; время показываем, но в сортировке не используем',
    ],
    correct: 3,
    why: `<p>Время не годится в качестве ключа порядка сразу по трём причинам: часы клиента врут (сообщение с телефона с уехавшим на пять минут временем встанет не туда), два сообщения могут иметь одинаковую метку в миллисекундах, и даже нормализованное серверное время не даёт отношения «строго после» — сообщения из разных датацентров приходят с одинаковой секундой. Монотонный <code>messageId</code> внутри чата решает всё это по построению.</p>
    <p>Серверное время — самый привлекательный неверный ответ, потому что оно снимает проблему кривых часов клиента. Но оставшиеся две причины никуда не деваются, а именно они дают «два сообщения меняются местами при перезагрузке».</p>
    <p>Отдельно нужен порядок для неподтверждённых: у них ещё нет серверного id, поэтому сортируем по паре <code>(messageId или бесконечность, localSeq)</code> — исходящие всегда в хвосте и в порядке отправки, а при подтверждении переезжают на своё место без прыжка. И счётчик id обязан быть внутри чата, а не глобальным: общий счётчик локально сортировать не помогает.</p>`,
    cardId: 'msg2' },

  { id: 'qc39',
    deck: 'sd',
    q: 'Лента читает все сообщения чата 12345 из IndexedDB и сортирует их по messageId. Что увидит пользователь?',
    snippet: `chatRanges[12345] = [
  { from: 1,     to: 340    },   // начало переписки
  { from: 98000, to: 100000 },   // текущее окно
];`,
    options: [
      'Корректную ленту: курсор IndexedDB вернёт только непрерывный диапазон',
      'Пустую ленту: составной ключ не позволяет читать два диапазона одним запросом',
      'Сообщение 340 прямо над 98000 — переписка будет выглядеть оборванной, и никакого признака этого не будет',
      'Дубликаты на стыке: 340 и 98000 попадут сразу в оба окна рендера',
    ],
    correct: 2,
    why: `<p>Мы никогда не держим локально всю историю. У чата есть набор <strong>загруженных диапазонов с дырами между ними</strong>, и наивное «взять всё из базы и отсортировать» склеит несмежные куски: пользователь увидит сообщение из первой недели переписки вплотную к сегодняшнему и решит, что часть истории пропала.</p>
    <p>Правильно: лента рендерит только текущий диапазон, а на его границе стоит явный маркер «есть ещё», по которому идёт подгрузка. Прыжок к сообщению вне диапазонов создаёт новый диапазон, а соприкоснувшиеся диапазоны сливаются.</p>
    <p>Первый вариант приписывает базе знание, которого у неё нет: курсор по составному ключу честно отдаёт всё, что записано, и ничего не знает про смысловые дыры. Дубликатов тоже не будет — <code>put</code> по ключу <code>[chatId, messageId]</code> исключает их по построению.</p>`,
    cardId: 'msg2' },

  { id: 'qc40',
    deck: 'sd',
    q: 'Запрос на отправку ушёл, сервер создал сообщение, ответ потерялся при обрыве. Клиент повторяет. Что не даст появиться дублю?',
    options: [
      'Блокировка кнопки отправки до подтверждения от сервера',
      'Проверка на сервере, что текст совпадает с последним сообщением этого автора',
      'Запись сообщения в IndexedDB через put вместо add',
      'Тот же clientId в повторе: сервер помнит использованные значения и вернёт уже созданное сообщение',
    ],
    correct: 3,
    why: `<p>Идемпотентность строится на клиентском идентификаторе, сгенерированном <strong>до</strong> первой попытки (в Telegram это random_id). Сервер хранит использованные значения и на повторный вызов возвращает результат первого вызова вместо создания второго сообщения. Это единственная защита, работающая в сценарии «запрос дошёл, ответ потерялся».</p>
    <p>Блокировка кнопки — это UX, а не гарантия: повторить может сама сеть, прокси, ретрай-логика клиента или пользователь через историю браузера. Сравнение текстов на сервере ломается на честном «+1» дважды подряд.</p>
    <p><code>put</code> вместо <code>add</code> — нужная, но другая защита: она спасает от повторной записи события, у которого <strong>уже есть</strong> серверный id. У неподтверждённого исходящего сообщения серверного id ещё нет, поэтому составной ключ тут не помогает. На практике нужны все три уровня: сравнение pts, put по составному ключу и clientId для исходящих.</p>`,
    cardId: 'msg7' },

  { id: 'qc41',
    deck: 'sd',
    q: 'Пользователь отправил три сообщения подряд за секунду, отправщик шлёт их параллельно. Что произойдёт?',
    options: [
      'Порядок разъедется: серверные id присвоятся в порядке прихода запросов, а не отправки',
      'Ничего: сервер упорядочит их по времени, проставленному клиентом',
      'Отправится только последнее: предыдущие запросы отменятся как устаревшие',
      'Все три получат один messageId, потому что уходят в одном TCP-сегменте',
    ],
    correct: 0,
    why: `<p>Порядок в чате держится на серверном <code>messageId</code>, а сервер присваивает его в момент обработки запроса. Три параллельных запроса приходят к нему в произвольном порядке — и «раз», «два», «три» превращаются в «два», «раз», «три». Замечают это обычно только в проде и только на быстро печатающих.</p>
    <p>Решение: <strong>очередь с параллелизмом 1 в пределах чата</strong>, между чатами параллелить можно и нужно. Плюс правило на отказ: если первое сообщение упало насовсем, остальные из этого чата нельзя молча отправлять вперёд — блокируем очередь чата, показываем ошибку на первом, остальные держим в статусе ожидания.</p>
    <p>Вариант с сортировкой по клиентскому времени — то самое заблуждение, которое разбирается в модели данных: часы клиента врут, и сортировка по ним даёт другой класс багов. Медиа из схемы очереди выбивается: байты грузятся параллельно и заранее, а публикация сообщения с готовым файлом встаёт в общую очередь чата.</p>`,
    cardId: 'msg7' },

  { id: 'qc42',
    deck: 'sd',
    q: 'Отправка сообщения вернула 403 «нет прав писать в этот чат». Что должен сделать отправщик?',
    options: [
      'Повторять с экспоненциальным backoff: 403 часто оказывается временным после переподключения',
      'Повторить один раз сразу и, если снова 403, ждать события online',
      'Уважать заголовок Retry-After и повторить ровно через указанное в нём время',
      'Не повторять вовсе: пометить сообщение failed и дать пользователю повтор или удаление',
    ],
    correct: 3,
    why: `<p>Ошибки отправки делятся на три класса, и политика у них разная. <strong>Сетевые и 5xx</strong> — повторяем с backoff и джиттером. <strong>Флуд-контроль</strong> — уважаем присланное сервером число секунд, а не свою формулу. <strong>Явные отказы</strong> (403, пользователь заблокировал, текст слишком длинный) — не повторяем никогда: бесконечный ретрай на 403 греет батарею, создаёт шторм запросов от всех клиентов сразу и ничего не чинит.</p>
    <p>Retry-After — правильный механизм, но не для этого случая: он приходит с 429 и флуд-контролем, а не с «нет прав». Взять его тут значит перепутать класс ошибки.</p>
    <p>Важная деталь UI: сообщение в статусе failed нельзя молча удалять при перезагрузке. Оно живёт в персистентной очереди и обязано быть видно в ленте после перезапуска — иначе пользователь уверен, что отправил.</p>`,
    cardId: 'msg7' },

  { id: 'qc43',
    deck: 'sd',
    q: 'Виртуализированная лента прыгает каждый раз, когда догружается картинка в сообщении. Что лечит это правильно?',
    options: [
      'Задать всем сообщениям среднюю высоту 60px и пересчитывать её после ResizeObserver',
      'Отключить виртуализацию для сообщений с медиа и рендерить их всегда',
      'Показывать спиннер вместо картинки, пока байты не приехали',
      'Резервировать место из width и height медиа, которые приходят в метаданных ещё до загрузки байтов',
    ],
    correct: 3,
    why: `<p>Размеры медиа известны заранее: они лежат в метаданных сообщения рядом с blurhash и ключом файла. Из них считается точная высота пузырька <strong>до первого байта картинки</strong>, место резервируется, и подстановка изображения не двигает ничего. Это тот самый ответ, который отличает человека, боровшегося с прыгающей лентой, от прочитавшего статью про виртуализацию.</p>
    <p>Средняя высота в 60px — популярный старт, и он промахивается в пять раз на любом фото: пересчёт после <code>ResizeObserver</code> случится уже после отрисовки, то есть прыжок пользователь увидит. Оценка нужна, но по типу сообщения и из метаданных, а не единым числом.</p>
    <p>Дальше механика такая: после монтирования <code>ResizeObserver</code> отдаёт реальную высоту, она пишется в кеш по стабильному ключу (clientId), и при расхождении больше пикселя пересчитываются префиксные суммы. Кеш высот переживает выгрузку элемента из DOM — второй проход по тому же участку уже без прыжков, и чистить его не надо: 8 байт на запись.</p>`,
    cardId: 'msg3' },

  { id: 'qc44',
    deck: 'sd',
    q: 'Позиция при подгрузке истории сверху компенсируется вручную в useLayoutEffect, но контент всё равно уезжает — ровно на двойную величину вставки. В чём дело?',
    options: [
      'Компенсацию надо делать в useEffect: useLayoutEffect срабатывает до вставки узлов в DOM',
      'Браузерное scroll anchoring компенсирует сдвиг само — нужен overflow-anchor: none на контейнере',
      'Нужен scrollIntoView на первом видимом элементе вместо арифметики со scrollTop',
      'Обязателен flex-direction: column-reverse, иначе scrollTop отсчитывается от другого края',
    ],
    correct: 1,
    why: `<p>Браузеры сами пытаются удержать позицию при изменении контента выше вьюпорта — это scroll anchoring. Когда вы добавляете свою компенсацию, сдвиг применяется дважды: браузерный плюс ваш. Симптом ровно такой, как в вопросе, и ищут его обычно полдня. Лечение — <code>overflow-anchor: none</code> на контейнере ленты и полный контроль позиции руками.</p>
    <p>Первый вариант перевёрнут: <code>useLayoutEffect</code> выполняется <strong>после</strong> мутации DOM и <strong>до</strong> отрисовки — это ровно то место, где компенсация обязана быть. Перенос в <code>useEffect</code> даст видимый рывок в один кадр.</p>
    <p><code>column-reverse</code> решает только якорь у нижнего края (новые сообщения снизу не двигают вид) и ничего не делает с переменными высотами и подгрузкой сверху; вдобавок он ломает выделение текста в части браузеров и путает скринридеры порядком. Самый устойчивый приём — якорь на элементе: запомнить id первого видимого сообщения и его отступ от верха, после вставки восстановить через <code>getBoundingClientRect</code>.</p>`,
    cardId: 'msg3' },

  { id: 'qc45',
    deck: 'sd',
    q: 'Пользователь жмёт «перейти к сообщению» из поиска: цель на 40 тысяч позиций выше. Что делает клиент?',
    options: [
      'Вызывает scrollIntoView на целевом элементе, догружая историю по мере движения скролла',
      'Загружает всю историю между текущей позицией и целью, затем выставляет scrollTop',
      'Одним запросом берёт окно из полусотни сообщений вокруг цели, пересобирает ленту и рисует первый кадр уже на нужной позиции',
      'Открывает цель в отдельном представлении, не трогая состояние основной ленты',
    ],
    correct: 2,
    why: `<p>Это не скролл, а переезд. Лента сбрасывает окно рендера и пересобирается вокруг цели: один запрос вида «25 сообщений до и 25 после id X», новый диапазон в списке загруженных, и <strong>первый кадр рисуется сразу на нужной позиции</strong>. Не «отрендерить сверху и потом проскроллить» — это дало бы вспышку чужого контента и лишний layout.</p>
    <p><code>scrollIntoView</code> — самый частый ответ, и он не работает по простой причине: элемента нет в DOM, лента виртуализирована. А прокрутка сквозь 40 тысяч сообщений — это порядка восьмисот запросов подгрузки.</p>
    <p>Что ещё обязано быть в ответе: в стек возврата кладём <strong>id</strong> первого видимого сообщения и его отступ, а не scrollTop (высоты могли пересчитаться, и пиксели соврут); каждый новый прыжок отменяет предыдущий запрос через <code>AbortController</code>, иначе быстрые клики по результатам поиска перетрут ленту чужим окном; фокус переносится на целевое сообщение, иначе для клавиатуры и скринридера функции просто нет.</p>`,
    cardId: 'msg4' },

  { id: 'qc46',
    deck: 'sd',
    q: 'Локальный поиск по 300 тысячам сообщений должен отвечать за 10–30 мс. Как устроено хранилище индекса?',
    options: [
      'Обратный индекс в IndexedDB с составным ключом [основа, чат]: диапазонный курсор даёт префиксный поиск бесплатно',
      'Map в памяти вида термин → массив id, которая строится при старте приложения',
      'Перебор курсором по хранилищу messages с проверкой includes — IndexedDB достаточно быстрая',
      'Отдельная запись на каждую пару (термин, сообщение) с индексом по термину',
    ],
    correct: 0,
    why: `<p>Ключевая причина держать индекс именно в IndexedDB, а не в памяти, — <strong>диапазонный курсор по составному ключу</strong>. Он же даёт префиксный поиск даром: <code>IDBKeyRange</code> от <code>['отч']</code> до <code>['отч\\uffff']</code> обходит все термины с этим префиксом, чего Map не умеет в принципе. Значение — постинг-лист id на пару (термин, чат).</p>
    <p>Отдельная запись на каждую пару (последний вариант) выглядит «нормализованно» и убивает решение: 300 тысяч сообщений по 15 значимых слов дают 4.5 миллиона записей, то есть гигабайты оверхеда IndexedDB против 30–40 МБ у постинг-листов массивами.</p>
    <p>Перебор с <code>includes</code> — это 3–8 секунд и заблокированный воркер. И отдельно про русский: без стемминга (Snowball, порядка 6 КБ) поиск по естественному запросу не находит ничего — «отчёт», «отчёта», «отчётами» должны сводиться к одной основе и в индексе, и в запросе.</p>`,
    cardId: 'msg8' },

  { id: 'qc47',
    deck: 'sd',
    q: 'Локальная выдача рисуется за 20 мс, серверная приходит через 400 мс. Пользователь набирает «отч», затем «отчёт». Что обязательно предусмотреть?',
    options: [
      'Показывать только серверную выдачу: локальная всё равно неполная и путает пользователя',
      'Отбрасывать устаревшие ответы по номеру запроса и дедуплицировать по messageId, дописывая новое снизу',
      'Дождаться обоих ответов и отрисовать общий список одним кадром',
      'Перестраивать список сверху при каждом новом ответе, чтобы релевантное всегда было первым',
    ],
    correct: 1,
    why: `<p>Две беды гибридной выдачи. Первая — гонка: ответ на «отч» может прийти позже ответа на «отчёт», и список моргнёт назад к предыдущему запросу. Спасает монотонный номер запроса, проверяемый перед рендером; отмена через <code>AbortController</code> сама по себе не гарантирует, что ответ не придёт. Вторая — дубли: одно и то же сообщение есть и локально, и в серверной выдаче, дедупликация по id обязательна.</p>
    <p>Четвёртый вариант ломает главное свойство схемы: пользователь уже целится мышью в результат, и перестроение списка сверху означает промах по цели. Новое дописывается снизу, показанное не переупорядочивается.</p>
    <p>Ждать оба ответа — значит отдать ровно то, ради чего строился локальный индекс: экран остаётся пустым 400 мс. А отказ от локальной выдачи вовсе делает поиск неработающим офлайн и невозможным при сквозном шифровании, где серверного поиска нет в принципе.</p>`,
    cardId: 'msg8' },

  { id: 'qc48',
    deck: 'sd',
    q: 'Клиент пишет новое значение pts одной транзакцией, а пачку из 500 сообщений — следующей. Браузер упал между ними. Что произойдёт?',
    options: [
      'Ничего: при старте клиент всё равно перезапрашивает последние 50 сообщений каждого чата',
      'Обе транзакции откатятся вместе: IndexedDB группирует последовательные транзакции в одну',
      'Сообщения применятся повторно при следующем подключении, а put по составному ключу их перезапишет',
      'pts ушёл вперёд без сообщений: сервер сочтёт эти события доставленными, и в истории останется тихая дыра',
    ],
    correct: 3,
    why: `<p>Счётчик состояния — это обещание серверу «всё до этой позиции я применил». Записав его раньше данных, мы обещаем то, чего не сделали: после падения клиент запросит разницу от нового pts, сервер отдаст только то, что случилось позже, и пятьсот сообщений не вернутся никогда. На проде это выглядит как «пропали сообщения за вчера», и причину ищут неделями.</p>
    <p>Лечение простое и почти никем не проговариваемое вслух: <strong>состояние синхронизации пишется в той же транзакции, что и сами сообщения</strong>. Транзакция IndexedDB атомарна, поэтому либо есть и данные, и счётчик, либо нет ни того, ни другого.</p>
    <p>Третий вариант описывает безопасный порядок — сначала данные, потом счётчик: там повторное применение действительно безвредно, <code>put</code> по ключу <code>[chatId, messageId]</code> перезапишет тем же. Именно поэтому он так похож на правду. Две транзакции IndexedDB никогда не объединяются в одну, а «перезапросить последние 50» не поможет: дыра может быть в середине.</p>`,
    cardId: 'msg5' },

  { id: 'qc49',
    deck: 'sd',
    q: 'На диске у пользователя кончается место, и браузер начинает вытеснять данные origin. К чему готовиться?',
    options: [
      'Терять нельзя ничего: navigator.storage.persist() гарантирует сохранность во всех браузерах',
      'Вытесняется только Cache API, IndexedDB защищена от вытеснения по умолчанию',
      'Вытесняется origin целиком — и IndexedDB, и Cache API; единственное, что терять нельзя, — исходящая очередь',
      'Вытесняются самые старые записи внутри каждого хранилища, поэтому достаточно самим чистить историю',
    ],
    correct: 2,
    why: `<p>Браузер вытесняет данные по принципу «давно не использовался» и делает это <strong>целиком по origin</strong>: IndexedDB и Cache API исчезают вместе, выборочной чистки внутри хранилищ не происходит. В Safari при включённой защите от трекинга данные скриптового происхождения удаляются просто после семи дней без взаимодействия с сайтом.</p>
    <p>Отсюда два вывода. Первый: просить <code>navigator.storage.persist()</code> — в установленном PWA его выдают почти всегда, но <strong>гарантии это не даёт</strong>, поэтому первый вариант неверен. Второй, более важный: потеря локальной базы должна быть штатным сценарием, а не катастрофой — история восстановится с сервера.</p>
    <p>Единственное, что действительно невосстановимо, — очередь неотправленных сообщений: её нет ни на каком сервере. Поэтому её дублируют в отдельную маленькую базу и никогда не чистят по квоте. Сверх этого держат свой бюджет на медиа (скажем, 1 ГБ) с LRU-вытеснением, чтобы не доводить дело до браузерной чистки.</p>`,
    cardId: 'msg5' },

  { id: 'qc50',
    deck: 'sd',
    q: 'Видео полностью лежит в Cache API, но в Safari не начинает воспроизводиться. Почему и как чинить?',
    options: [
      'cache.match отдаёт полный 200-ответ, а элемент video ждёт 206 — Service Worker должен резать диапазон сам',
      'Safari не поддерживает Cache API для медиа, файл надо перекладывать в IndexedDB',
      'Не хватает Accept-Ranges на исходном ответе — достаточно добавить его при cache.put',
      'Надо класть в кеш сами 206-ответы по диапазонам, тогда match найдёт нужный кусок',
    ],
    correct: 0,
    why: `<p>Элемент <code>video</code> запрашивает файл Range-запросами: сначала первые сотни килобайт с индексом кадров, потом куски по мере воспроизведения. Cache API этого не понимает — <code>cache.match()</code> на запрос с <code>Range</code> вернёт полный 200-ответ, а Safari без 206 воспроизведение не начнёт. Решение — перехват в Service Worker: достать полный ответ из кеша, нарезать по диапазону и вернуть рукотворный 206 с корректным <code>Content-Range</code>.</p>
    <p>Четвёртый вариант — ровно тот, к которому приходят самостоятельно, и он упирается в жёсткое ограничение: <code>cache.put()</code> с частичным ответом <strong>бросает исключение</strong>. В кеш кладут только 200, режут на лету.</p>
    <p>Перекладывать в IndexedDB бессмысленно и вредно: Blob там проходит через structured clone, то есть лишняя копия в памяти и заметная пауза на десятках мегабайт, и отдать его в <code>video</code> потоком нельзя. Именно эта пара фактов — необходимость 206 и запрет на его кеширование — и объясняет, зачем в схеме медиа вообще нужен Service Worker.</p>`,
    cardId: 'msg9' },

  { id: 'qc51',
    deck: 'sd',
    q: 'Открыто три вкладки мессенджера. Как выбрать единственного владельца сокета так, чтобы смерть владельца не подвешивала приложение?',
    options: [
      'Лидер пишет heartbeat в localStorage, остальные считают его мёртвым после пяти секунд молчания',
      'SharedWorker: он один на все вкладки и поддержан во всех современных браузерах',
      'Service Worker: он живёт независимо от вкладок и переживает их закрытие',
      'navigator.locks.request в режиме exclusive с промисом, который никогда не резолвится',
    ],
    correct: 3,
    why: `<p>Первая вкладка берёт эксклюзивный лок и держит его промисом, который не резолвится; остальные висят в очереди. Когда лидер закрывается или падает, <strong>браузер освобождает лок автоматически</strong>, и следующий становится лидером за миллисекунды. В этом и весь смысл: окно переизбрания измеряется миллисекундами, а не секундами.</p>
    <p>Самодельный heartbeat в localStorage — работающая, но заметно худшая схема: смерть лидера ловится по таймауту, и всё это время приложение мертво. Плюс localStorage синхронный.</p>
    <p>SharedWorker архитектурно красивее, но не поддержан в Safari на iOS, поэтому как единственное решение не годится. Service Worker не годится вовсе — он засыпает через десятки секунд бездействия и сокет не удержит. И отдельно: очередь отправки должна быть только у лидера, иначе одно сообщение уйдёт трижды, а по BroadcastChannel гоняют <strong>сигналы</strong> «перечитай диапазон», а не сами данные — источник правды остаётся IndexedDB.</p>`,
    cardId: 'msg14' },

  { id: 'qc52',
    deck: 'sd',
    q: 'В канале на миллион подписчиков реакции летят сотнями в секунду. Что хранить на сообщении?',
    options: [
      'Агрегаты вида «эмодзи, счётчик, поставил ли я», а список авторов подгружать отдельным запросом по клику',
      'Полный список авторов реакций: без него нельзя показать, кто именно поставил',
      'Только собственную реакцию, а чужие счётчики запрашивать при появлении сообщения во вьюпорте',
      'Множество id последних ста отреагировавших: этого хватает и для счётчика, и для списка',
    ],
    correct: 0,
    why: `<p>Модель выбирается по худшему случаю. Агрегат — это несколько десятков байт на сообщение <strong>независимо от аудитории</strong>: миллион подписчиков и десять дают одинаковый объём. Флаг «поставил ли я» приходит с сервера вместе с сообщением, а список авторов — отдельный запрос страницами по 50 с курсором, кеш на минуту и выброс при закрытии поповера.</p>
    <p>Хранить список авторов на клиенте — прямой путь к разносу памяти на канале. Вариант с «последними ста» коварнее: счётчик по нему будет неверным, как только реакций станет больше ста, а UI покажет 100 вместо 1240.</p>
    <p>Что ещё обязательно в ответе: чужие обновления схлопываются в буфер по messageId (последнее побеждает) и вливаются раз в 300–500 мс, обновления вне вьюпорта до UI не доходят вовсе; у агрегата есть версия, и событие с версией ниже применённой игнорируется; после своего нажатия 2–3 секунды входящие события не трогают мой флаг — иначе собственный лайк мигает на каждом входящем агрегате.</p>`,
    cardId: 'msg10' },

  { id: 'qc53',
    deck: 'sd',
    q: 'Совместное редактирование документа: нужен офлайн-режим и слияние правок без центрального арбитра. Что выбрать и чем за это платить?',
    options: [
      'OT: сервер трансформирует координаты операций, память минимальна, офлайн получается сам собой',
      'CRDT вроде Yjs: сходится без арбитра и переживает офлайн, но платит метаданными на каждую вставку и требует сборки tombstone-ов',
      'Last-write-wins на весь документ: дёшево, предсказуемо и не требует библиотек',
      'Блокировка абзаца на время редактирования: конфликтов не возникнет в принципе',
    ],
    correct: 1,
    why: `<p>Требование «без центрального арбитра плюс офлайн» отсекает OT сразу: операционная трансформация построена вокруг авторитетного сервера, который приводит операции к общему порядку. CRDT сходится сама, поэтому местные правки можно копить в <code>y-indexeddb</code> и влить при подключении. Цена честная: метаданные на каждую вставку, необходимость GC удалённых узлов и сжатия структуры.</p>
    <p>OT — самый привлекательный неверный ответ, потому что «как в Google Docs» звучит как эталон. Он и правда экономнее по памяти и точнее сохраняет намерение пользователя, но алгоритм трансформаций почти невозможно написать без багов на rich text, и офлайн-мерж он не даёт.</p>
    <p>Что ещё стоит проговорить: эфемерное состояние (курсоры, выделения, «печатает») идёт отдельным awareness-каналом и в CRDT не попадает; апдейт от клиента нельзя применять без серверной проверки прав, иначе участник с read-only правит документ через devtools; undo нужен per-user, иначе глобальный стек отменяет чужие правки.</p>`,
    cardId: 'sdx1' },

  { id: 'qc54',
    deck: 'sd',
    q: 'Два пользователя одновременно перетаскивают карточки в одной колонке Kanban. Как хранить порядок, чтобы перемещение меняло ровно один объект?',
    options: [
      'Целочисленный индекс позиции с переиндексацией всей колонки после каждого перемещения',
      'Вычислять порядок на клиенте из времени последнего перемещения карточки',
      'Связный список: каждая карточка хранит id следующей за ней',
      'Fractional index — лексикографическая строка между соседями; перемещение пишет один rank',
    ],
    correct: 3,
    why: `<p>Дробный индекс — это значение «посередине» между соседями. Перемещение меняет ровно одну карточку, поэтому два клиента, двигающие разные карточки, физически не пишут в одни и те же строки, и конкурентные перемещения безопасны. Строковые ранги (base62 в лексикографическом порядке) лучше чисел: у double кончается мантисса примерно после полусотни вставок в одну точку.</p>
    <p>Переиндексация колонки — интуитивный и самый разрушительный вариант: одно перемещение пишет десятки объектов, и любые два одновременных действия конфликтуют. Связный список звучит красиво, но перемещение трогает три узла сразу, а разрыв цепочки при частичном применении делает колонку неотображаемой.</p>
    <p>Дополнительно: коллизия одинаковых рангов допустима, тай-брейк по id даёт всем клиентам детерминированно одинаковый порядок; свои echo-события фильтруются по clientId, иначе карточка дёрнется дважды; офлайн-очередь перемещений должна схлопываться — десять перемещений одной карточки это одно финальное положение.</p>`,
    cardId: 'sdx2' },

  { id: 'qc55',
    deck: 'sd',
    q: 'Пользователь нажал «Оплатить», ответ не пришёл, он нажал «назад» и повторил. Что гарантирует единственный заказ?',
    options: [
      'Блокировка кнопки оплаты на время запроса',
      'Проверка на сервере, что у пользователя нет заказа с тем же составом за последнюю минуту',
      'Idempotency-Key, сгенерированный один раз при входе в чекаут и переживающий перезагрузку страницы',
      'Отправка заказа через sendBeacon: он доставляется ровно один раз',
    ],
    correct: 2,
    why: `<p>Ключ идемпотентности генерируется <strong>один раз при входе в чекаут</strong> и кладётся в sessionStorage, чтобы пережить перезагрузку и возврат через историю браузера. Любой повтор — после таймаута, после реконнекта, после нажатия «назад» — уходит с тем же ключом, и сервер возвращает уже созданный заказ вместо второго.</p>
    <p>Блокировка кнопки — это UX, а не гарантия: повторить может сама сеть, прокси или история браузера. Проверка «такой же состав за минуту» ломается на честном повторном заказе того же товара и всё равно оставляет окно гонки между двумя параллельными запросами.</p>
    <p><code>sendBeacon</code> никаких гарантий доставки «ровно один раз» не даёт — это способ отправить данные при уходе со страницы, и для платежа он не подходит вовсе. Рядом стоит помнить про ресинхронизацию перед оплатой (цена или наличие могли измениться — нужен явный дифф «было / стало») и про экран «проверяем оплату» с опросом, когда платёж прошёл, а webhook ещё не доехал.</p>`,
    cardId: 'sdx5' },

  { id: 'qc56',
    deck: 'sd',
    q: 'Загрузка файла на 30 ГБ через S3 multipart с presigned URL идёт третий час. Что обязательно предусмотреть?',
    options: [
      'Ничего: presigned URL действует до завершения multipart-сессии',
      'Увеличить размер части до 500 МБ, чтобы уложиться в срок жизни ссылки',
      'Прочитать файл целиком в память один раз, чтобы не зависеть от диска',
      'Перезапрос ссылки на часть при 403: presigned URL живёт около часа, а загрузка дольше',
    ],
    correct: 3,
    why: `<p>Presigned URL подписывается на ограниченный срок — обычно до часа. Через час посреди многочасовой загрузки части начнут отваливаться с 403, и наивный клиент решит, что это фатальная ошибка. Нужен явный сценарий: поймали 403 на части, запросили новую ссылку у бэкенда, продолжили с того же места.</p>
    <p>Укрупнение частей проблему не решает, а усугубляет: цена повтора при обрыве на мобильной сети растёт пропорционально, а лимит времени всё равно упрётся. Рабочий размер части — единицы или десятки мегабайт, 3–4 части параллельно.</p>
    <p>Чтение файла в память убивает вкладку: <code>File</code> — это ссылка, а не копия, и работать с ним надо через <code>slice()</code>, который даёт ленивый срез. Возобновление держится на записанных в IndexedDB id загрузки, списке подтверждённых частей с ETag и отпечатке файла (имя, размер, lastModified) — чтобы после перезагрузки убедиться, что пользователь выбрал тот же файл. И ретраить надо чанк, а не всю загрузку.</p>`,
    cardId: 'sdx7' },

  { id: 'qc57',
    deck: 'sd',
    q: 'Событие «планёрка в 10:00 по Москве, каждый понедельник». Как хранить время?',
    options: [
      'UTC-инстант первого вхождения плюс правило повтора',
      'Локальное время в миллисекундах от полуночи плюс смещение зоны в минутах',
      'Локальное время плюс идентификатор зоны IANA: при изменении правил перехода UTC-значение уехало бы на час',
      'Развернуть серию в список timestamp-ов на год вперёд прямо при создании',
    ],
    correct: 2,
    why: `<p>Есть три разных типа времени, и путать их нельзя. Точечное событие («созвон вот в этот момент») хранится как UTC-инстант. Событие, привязанное к локальному времени, — как пара «локальное время + IANA zone id»: <code>2026-03-12T10:00</code> и <code>Europe/Moscow</code>. Правила таймзон меняются несколько раз в год, и сохранённый UTC сдвинул бы встречу на час. Третий тип — событие на весь день: это календарная дата без времени и без зоны, превращать её в инстант нельзя.</p>
    <p>Смещение в минутах вместо zone id — та же ошибка в профиль: смещение верно только на конкретную дату, а правило меняется вместе с законодательством.</p>
    <p>Разворачивать серию в список нельзя: она бесконечна. Повторы описываются RRULE плюс EXDATE для исключений и RECURRENCE-ID для переопределённых вхождений, а раскрываются только на видимое окно с кешем по ключу «серия + окно + версия». И редактирование серии — это три разные операции: «только это вхождение», «это и последующие» (сплит серии), «вся серия».</p>`,
    cardId: 'sdx9' },

  { id: 'qc58',
    deck: 'sd',
    q: 'В браузерной игре собственный персонаж ощущается «резиновым»: между нажатием и движением заметная задержка. Какая техника это лечит?',
    options: [
      'Интерполяция объектов с задержкой около 100 мс — она сгладит и свой ввод тоже',
      'Повышение тикрейта сервера до 60 Гц — задержка станет незаметной',
      'Переход с WebSocket на WebRTC DataChannel: без head-of-line blocking задержка исчезнет',
      'Client-side prediction: свой ввод применяем сразу, а по снапшоту сервера откатываемся и переигрываем неподтверждённые инпуты',
    ],
    correct: 3,
    why: `<p>«Резиновость» — это ожидание ответа сервера перед отрисовкой собственного движения. Лечится предсказанием: свой ввод применяется локально мгновенно и складывается в буфер неподтверждённых. Сервер присылает состояние с номером последнего обработанного ввода, клиент откатывается к нему и переигрывает буфер — это server reconciliation, вторая половина той же техники.</p>
    <p>Интерполяция — третья техника из того же набора, но она про <strong>чужие</strong> объекты: их рисуют в прошлом, между двумя полученными снапшотами, чтобы убрать дёрганье при джиттере. К своему персонажу её применять как раз нельзя — задержка станет только заметнее.</p>
    <p>Тикрейт и транспорт уменьшают задержку, но не убирают её: RTT никуда не денется. WebRTC DataChannel в режиме unreliable нужен для тиков состояния (потерять один тик нормально, ретрансмиссия TCP только добавит задержку), а WebSocket остаётся для матчмейкинга и надёжных событий. И отдельно про античит: клиент сообщает ввод, а попадания считает сервер — любая архитектура, где результат приходит от клиента, ломается в первый же день.</p>`,
    cardId: 'sdx14' },

  { id: 'qc59',
    deck: 'sd',
    q: 'A/B-эксперимент на первом экране раскатили через асинхронный клиентский SDK. Какие две беды получили?',
    options: [
      'Мигание контрольного варианта и загрязнение выборки экспозицией тех, кто варианта так и не увидел',
      'Рост TTFB и падение hit rate CDN: SDK добавляет запрос до отдачи HTML',
      'Рассинхрон бакетов между устройствами и удвоение размера бандла',
      'Блокировку рендера и невозможность выключить эксперимент без релиза',
    ],
    correct: 0,
    why: `<p>Асинхронный SDK по определению отвечает уже после первого рендера: пользователь видит контрольный вариант, затем перестановку. Это FOOC — мигание, прямой удар по CLS и одновременно порча самих данных, потому что часть аудитории успела увидеть контроль. Вторая беда — экспозиция: если её логировать в момент назначения, в выборку попадут пользователи, которые до тестируемого экрана вообще не дошли, и эффект размоется до статистической незначимости.</p>
    <p>Второй вариант описывает реальную, но другую проблему — она возникает у серверного назначения, если вариант не входит в ключ кеша CDN. Кстати, это самый разрушительный баг A/B на фронте: без варианта в cache key половина пользователей получит чужой HTML.</p>
    <p>Правильно принимать решение <strong>до рендера</strong>: назначение на edge или сервере (middleware считает вариант, пишет в cookie, отдаёт готовый HTML) либо синхронный бутстрап конфигурации в head. Назначение детерминированное — hash(experimentId + subjectId) % 100, с обязательной солью из experimentId. Асинхронный клиентский SDK допустим только ниже первого экрана.</p>`,
    cardId: 'sdx20' },

  { id: 'qc60',
    deck: 'sd',
    q: 'События аналитики теряются на переходах и при закрытии вкладки на мобильных. Что это чинит?',
    options: [
      'Флаш по visibilitychange в состояние hidden через sendBeacon или fetch с keepalive',
      'Отправка в обработчике beforeunload обычным fetch — он успевает уйти синхронно',
      'Уменьшение батча до одного события: маленькие запросы уходят быстрее',
      'Перенос отправки в Service Worker: он сам доставит накопленное после закрытия вкладки',
    ],
    correct: 0,
    why: `<p>Обычный <code>fetch</code> браузер отменяет вместе с документом — это и есть главная причина «дыр» в аналитике. Работают только <code>navigator.sendBeacon</code> и <code>fetch(url, { keepalive: true })</code>. А триггером должен быть <code>visibilitychange</code> в hidden (плюс <code>pagehide</code>): <code>beforeunload</code> на мобильных часто не срабатывает вовсе, вкладку убивают без него.</p>
    <p>Второй вариант ошибается дважды — и в событии, и в транспорте, и именно поэтому встречается в проде чаще всего. Батч по одному событию не помогает: проблема не в размере, а в отмене запроса.</p>
    <p>Что ещё нужно: у <code>sendBeacon</code> лимит около 64 КБ, и при превышении он <strong>молча возвращает false</strong> — возвращаемое значение надо проверять и резать батч. Персистентная очередь в IndexedDB спасает от краха вкладки и офлайна, event_id даёт дедупликацию на сервере, а порядковый номер в сессии позволяет посчитать реальный процент потерь.</p>`,
    cardId: 'sdx21' },

  { id: 'qc61',
    deck: 'sd',
    q: 'Чем offline-first приложение принципиально отличается от «приложения с кешем»?',
    options: [
      'Источником истины: UI читает и пишет в локальную базу, а сеть работает как фоновая синхронизация',
      'Наличием Service Worker: без него офлайн невозможен в принципе',
      'Объёмом кеша: offline-first кеширует все ответы API, а не только оболочку приложения',
      'Стратегией Service Worker: cache-first вместо network-first для всех без исключения запросов',
    ],
    correct: 0,
    why: `<p>Разница не в количестве кеша, а в направлении потока данных. «Приложение с кешем» ходит в сеть, а кеш использует как ускорение и запасной вариант: источник истины на сервере, и офлайн деградирует до «ничего не работает». В offline-first локальная база — первичное хранилище: компоненты читают и пишут в неё, спиннера в ожидании сети нет вообще, а сеть догоняет фоном.</p>
    <p>Третий вариант — самая распространённая подмена: кешировать ответы API можно сколько угодно, но пока запись идёт напрямую в сеть, приложение остаётся онлайновым. И наоборот, тотальный cache-first в Service Worker даёт не offline-first, а показ вчерашних данных поверх сегодняшних.</p>
    <p>Отсюда вытекает остальное: клиентские UUID сущностей (чтобы офлайн можно было создать заказ и сразу добавить в него позиции), идемпотентность мутаций, outbox с сохранением порядка и схлопыванием операций, инкрементальная синхронизация с обязательными tombstone-ами — без записей об удалении удалённое воскресает на устройстве, которое было офлайн. И честная граница: платежи и уникальные брони офлайн не делаются.</p>`,
    cardId: 'sdx26' },

  { id: 'qc62',
    deck: 'sd',
    q: 'Пользователь долистал ленту до трёхсотого элемента, открыл карточку и нажал «назад» — попал в начало. Кеш страниц ленты уже сохраняется. Чего не хватает?',
    options: [
      'Сохранять scrollTop и восстанавливать его сразу после гидратации',
      'Заменить курсорную пагинацию на offset: она стабильнее при возврате',
      'Оставить history.scrollRestoration в auto — браузер справится точнее ручного кода',
      'Якоря по id последнего видимого элемента и history.scrollRestoration = manual',
    ],
    correct: 3,
    why: `<p>Восстанавливать надо не пиксели, а элемент. Абсолютный scrollTop врёт: к моменту возврата высоты изменились из-за загрузки картинок, подстановки шрифтов и рекламы, и те же 12000 пикселей указывают на другое место. Правильно запомнить id последнего видимого элемента и смещение внутри него, а после восстановления данных проскроллить к нему.</p>
    <p>Второе обязательное условие — <code>history.scrollRestoration = 'manual'</code>. Иначе браузер попытается восстановить позицию <strong>до</strong> того, как данные загрузились, промахнётся, и его попытка подерётся с вашей. Именно поэтому третий вариант неверен: авто-восстановление рассчитано на статические документы.</p>
    <p>Курсорную пагинацию менять на offset нельзя ни в коем случае — с offset новые элементы сверху сдвигают выдачу, и пользователь получает дубликаты и пропуски. Дедупликация по id нужна даже с курсором. И для точного восстановления обязателен зарезервированный размер медиа через aspect-ratio, иначе промахнётся любой якорь.</p>`,
    cardId: 'sdx27' },

  { id: 'qc63',
    deck: 'sd',
    q: 'Публичный API: интеграторов много, они чужие и кешируют ответы на своих CDN. Что выбрать?',
    options: [
      'GraphQL: клиенты возьмут ровно нужные поля и не будут просить новые эндпоинты',
      'tRPC: типы приезжают прямо из бэкенда, интеграция обходится дешевле всего',
      'GraphQL с подпиской на изменения: интеграторам тогда вообще не понадобится кеш',
      'REST: HTTP-кеш и предсказуемые URL для чужих интеграторов важнее гибкости выборки',
    ],
    correct: 3,
    why: `<p>Выбор определяется двумя вопросами: кто владеет формой данных и сколько у вас разнородных клиентов. Здесь клиенты чужие, их много, и они рассчитывают на обычный HTTP-кеш — то есть на GET, ETag, Cache-Control и CDN «из коробки». REST это даёт бесплатно, GraphQL — нет: браузерный и промежуточный кеш у него выключены по умолчанию, потому что всё едет одним POST на один URL.</p>
    <p>GraphQL заманчив гибкостью, и именно поэтому его тянут в публичный API. Но без persisted queries и лимитов глубины и сложности это открытая дверь для DoS — один вложенный запрос кладёт базу; APQ с GET возвращает часть кеширования, но требует настройки на обеих сторонах и от каждого интегратора.</p>
    <p>tRPC здесь не подходит по определению: он рассчитан на одну команду с общим TypeScript и не имеет версионирования, а публичному API версии нужны обязательно. Правило простое: один клиент и одна команда — tRPC, много клиентов с разными потребностями в данных — GraphQL, публичный API — REST.</p>`,
    cardId: 'sdx33' },

  { id: 'qc64',
    deck: 'sd',
    q: 'После деплоя часть пользователей получает «Failed to fetch dynamically imported module». В чём первопричина?',
    options: [
      'Браузерный кеш отдаёт старый HTML — достаточно поставить no-store на index.html',
      'Service Worker перехватывает динамические импорты — надо отключать его на время деплоя',
      'Ошибка сборки: чанк не попал в артефакт и физически отсутствует на CDN',
      'Старые ассеты удалены сразу после деплоя, а у пользователя открыта вкладка со старым HTML',
    ],
    correct: 3,
    why: `<p>У фронтенда есть свойство, которого нет у бэкенда: <strong>у пользователя может быть открыта старая версия приложения</strong>. Её HTML ссылается на чанки предыдущей сборки, и если папка со старыми ассетами удалена сразу после деплоя, любой <code>import()</code> в этой вкладке падает. Мерой является не хитрая обработка ошибки, а хранение предыдущих версий хотя бы сутки.</p>
    <p>Первый вариант лечит соседнюю болезнь — застрявший в кеше HTML — и потому выглядит правдоподобно. Он нужен, но сам по себе проблему открытой вкладки не решает: вкладка уже загружена, ей не важно, что стоит в заголовках. Ошибка сборки дала бы 100% отказ сразу, а не «у части пользователей».</p>
    <p>Полный набор мер: иммутабельные имена с contenthash и вечный кеш на ассеты, отдельное версионирование HTML с коротким TTL, хранение старых версий, обработчик ошибки с одной попыткой ретрая и предложением перезагрузиться, баннер «доступна новая версия» и версия схемы для локального состояния — новая сборка не должна падать на старом формате в localStorage или IndexedDB.</p>`,
    cardId: 'sdx38' },

  { id: 'qc65',
    deck: 'sd',
    q: 'Подсказки поиска изредка показывают результаты к предыдущему префиксу, хотя AbortController уже используется. Почему?',
    options: [
      'Debounce слишком короткий: 150 мс не хватает, нужно поднять до 500',
      'AbortController отменяет только чтение тела, заголовки ответа всё равно доезжают',
      'Отмена не гарантирует, что ответ не придёт: нужна проверка монотонного номера запроса перед рендером',
      'Виновата композиция ввода: IME шлёт промежуточные значения, которые уходят как отдельные запросы',
    ],
    correct: 2,
    why: `<p>Это главный практический баг поиска. <code>AbortController</code> прерывает запрос, но между решением об отмене и реальным прерыванием есть окно, а часть ответов уже в пути. Единственная надёжная защита — монотонный номер запроса: перед рендером сравниваем номер пришедшего ответа с текущим и молча выбрасываем устаревший.</p>
    <p>Увеличение debounce уменьшает частоту проблемы и ухудшает отзывчивость — классическая замена лечения на маскировку. Для suggest рабочий диапазон 150–250 мс, и он подобран не случайно.</p>
    <p>Композиционный ввод — реальная и отдельная беда (при наборе на японском или китайском промежуточные значения улетают мусором, поэтому запросы шлют после <code>compositionend</code>), но она не объясняет показ результатов к <strong>предыдущему</strong> префиксу. Рядом полезны локальный LRU-кеш по префиксу, чтобы стирание символа отвечало мгновенно, и обязательная обработка пустого запроса: отменить полёт и показать историю, а не оставить подсказки от предыдущего слова.</p>`,
    cardId: 'sdx11' },

];
