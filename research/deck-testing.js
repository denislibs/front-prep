const DECK_TESTING = [
  { id: 'tst1',
    q: 'Ты пришёл в команду, где тестов почти нет. С чего начнёшь и что решишь не тестировать вообще?',
    a: `<h4>Коротко</h4>
<p>Я не спрашиваю «что покрыть», я спрашиваю «где нам будет больно, если сломается, и как мы узнаем об этом раньше пользователя». Тест окупается там, где цена ошибки высокая, а вероятность ошибки ненулевая: деньги, права доступа, необратимые действия, логика с ветвлениями. Не тестирую разметку, тонкие обёртки над библиотеками и код, который через месяц выкинут.</p>

<h4>Как это работает</h4>
<p>Раскладываю код по двум осям: цена поломки и частота изменений. Получается четыре угла, и в каждом своя тактика. Дорого и часто меняется — расчёт корзины, правила скидок, валидация оплаты, стейт-машина визарда: сюда плотные юнит-тесты на чистые функции плюс один интеграционный на сценарий целиком. Дорого, но почти не меняется — платёжный флоу: сюда один e2e, он дешёвый в поддержке, потому что редко краснеет. Дёшево и часто меняется — вёрстка лендинга, тексты, порядок карточек: тестов нет совсем, они будут падать на каждом изменении дизайна и ничего при этом не ловить. Дёшево и редко — не трогаю.</p>
<p>Второй вход, более честный, — постмортемы. Открываю последние тридцать багов с прода и смотрю, где они рождались. Обычно половина приходит из одной зоны, и тестов там нет. Это точнее любых рассуждений о пирамиде, потому что опирается на факты про конкретный продукт, а не на общую теорию.</p>

<h4>Почему так</h4>
<p>Тест — не бесплатная страховка, у него есть цена владения: время написания, минуты в CI на каждом пуше, починка при рефакторинге и разбор ложного падения в три часа ночи перед релизом. Если тест ловит настоящие баги реже, чем требует внимания на ложные срабатывания, он в минусе, и его надо удалить, а не чинить. Поэтому решение «тестировать или нет» — экономическое, а не моральное.</p>

<h4>Подводные камни</h4>
<ul>
<li>Тесты на геттеры, реэкспорты и тонкие обёртки: покрытие растёт, ценность нулевая.</li>
<li>Проверка, что в разметке есть класс <code>btn-primary</code>: ломается от смены дизайн-системы, багов не ловит ни одного.</li>
<li>Покрытие ради цифры в отчёте — команда пишет тесты на то, что легко тестировать, а не на то, что ломается.</li>
<li>Дублирование типов: тест проверяет, что функция вернула число, хотя TypeScript это уже гарантирует.</li>
<li>Тест на сторонний код: проверяем, что react-router умеет переходить по ссылке.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>А если менеджер требует 80% покрытия?</strong> Соглашусь на порог только по критичным модулям и попрошу вести метрику «сколько багов дошло до прода», иначе цифра становится самоцелью. <strong>С чего начнёшь в первый спринт?</strong> С одного e2e на главный денежный сценарий: он ловит «всё умерло» и даёт время спокойно разбираться в коде.</p>`,
    code: `// хрупкий тест: проверяет разметку и имя класса
test('рендерит цену со скидкой', () => {
  const { container } = render(<Price value={1990} discount={0.1} />);
  expect(container.querySelector('.price__value').className)
    .toContain('price__value--discounted');
});
// упадёт, когда дизайнер переименует модификатор. Бага не поймает никогда.

// устойчивый тест: проверяет правило, на котором теряют деньги
test('скидка применяется к товару, но не к доставке', () => {
  expect(total({ price: 1990, delivery: 300, discount: 0.1 })).toBe(2091);
  //            1990 * 0.9 + 300 = 2091, а не (1990 + 300) * 0.9 = 2061
});`,
    tip: 'Назови цену владения тестом вслух: минуты в CI, починка при рефакторинге, разбор ложного падения перед релизом. Кандидат, который умеет обосновать отказ от теста, звучит сильнее того, кто хочет покрыть всё.' },

  { id: 'tst2',
    q: 'Пирамида тестов или «трофей» Кента Доддса — что выберешь для фронтенда и почему?',
    a: `<h4>Коротко</h4>
<p>На фронтенде я держу форму трофея: основная масса — интеграционные тесты, где компонент рендерится вместе со своими хуками, стором и перехваченной сетью; юнитов немного, только на чистую логику; e2e — единицы на критичные сценарии. А в основании вместо широкого слоя юнитов стоит статика: TypeScript, ESLint, строгий режим.</p>

<h4>Как это работает</h4>
<p>Пирамида родилась на бэкенде, где юнит — это класс с внятной границей и своим контрактом, а интеграционный тест дорог, потому что поднимает базу и очередь. На фронте пропорции другие. Граница осмысленного поведения проходит не по файлу, а по экрану: компонент, его хук с загрузкой, редьюсер, форматирование и разметка вместе дают то, что видит пользователь. Разрезав это на четыре юнита, я получу четыре зелёных теста и сломанный экран — потому что баг живёт в стыке.</p>
<p>Второе отличие в цене. Интеграционный тест на фронте — это jsdom плюс перехват запросов на уровне сети, старт за десятки миллисекунд, никакой инфраструктуры. То есть аргумент «интеграционные дорогие» здесь просто не выполняется, а именно на нём стояла пирамида.</p>
<p>Статика внизу заменяет целый класс юнит-тестов. Опечатка в имени поля, забытый <code>await</code>, пропущенная ветка в размеченном объединении — всё это ловится компилятором на каждом сохранении файла, бесплатно и без поддержки.</p>

<h4>Почему так</h4>
<p>Форма фигуры — это не эстетика, а ответ на вопрос «сколько уверенности даёт единица усилий». Интеграционный тест на фронте даёт максимум уверенности на минимум строк, поэтому его и больше всех. E2E даёт ещё больше уверенности, но стоит на порядок дороже: реальный браузер, сеть, данные, время. Юнит даёт мало уверенности сам по себе, но незаменим там, где комбинаций много: даты, деньги, парсинг, права.</p>

<h4>Подводные камни</h4>
<ul>
<li>Трофей понимают как «пишем только интеграционные» и теряют юниты там, где двадцать граничных случаев логики.</li>
<li>Интеграционный тест разрастается до полстраницы сценария и начинает мигать — это уже плохой e2e внутри jsdom.</li>
<li>Забывают, что jsdom — не браузер: нет layout, нет реальных стилей, <code>IntersectionObserver</code> отсутствует.</li>
<li>Спорят про фигуры вместо того, чтобы посмотреть, откуда реально приходят баги.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Где граница между юнитом и интеграционным?</strong> Для меня интеграционный — тот, где в тесте участвует больше одного модуля и есть рендер. <strong>Сколько e2e нормально?</strong> Столько, чтобы весь набор укладывался в несколько минут, обычно десятки, а не сотни.</p>`,
    code: `// было: три юнита, каждый зелёный, экран сломан
test('useUserForm валидирует email', () => { /* ... */ });
test('formatPhone форматирует номер', () => { /* ... */ });
test('SubmitButton дизейблится по пропу', () => { /* ... */ });
// баг: форма не передаёт disabled в кнопку. Ни один тест этого не видит.

// стало: один интеграционный тест на поведение экрана
test('не отправляет форму с невалидным email', async () => {
  const user = userEvent.setup();
  render(<ProfileForm />);
  await user.type(screen.getByLabelText('Почта'), 'not-an-email');
  await user.click(screen.getByRole('button', { name: 'Сохранить' }));

  expect(await screen.findByText('Проверьте адрес почты')).toBeInTheDocument();
  expect(saveProfile).not.toHaveBeenCalled();
});`,
    tip: 'Скажи, что форма фигуры выводится из цены за единицу уверенности, а не из моды. И добавь, что на фронте основание пирамиды занимает не юнит-тест, а компилятор: TypeScript ловит целый класс ошибок бесплатно и без поддержки.' },

  { id: 'tst3',
    q: 'Бывает, что тест обходится дороже бага, который он ловит. Как ты понимаешь, что это тот самый случай?',
    a: `<h4>Коротко</h4>
<p>Считаю две величины. Слева — ожидаемый ущерб: вероятность бага, умноженная на его цену. Справа — стоимость владения тестом за год: написание, прогоны в CI, починка при каждом рефакторинге и разбор ложных падений. Если справа больше — тест не пишу или удаляю существующий.</p>

<h4>Как это работает</h4>
<p>Стоимость владения почти всегда недооценивают, потому что видят только первый пункт. На деле она растянута во времени. Тест выполняется на каждом пуше: сорок секунд лишнего времени в наборе, который гоняется двести раз в неделю, — это два часа ожидания команды в неделю. Тест ломается при рефакторинге: если он держится за структуру, а не за поведение, каждый рефакторинг превращается в правку тестов, и это прямой налог на изменения. Тест мигает: одно ложное падение стоит не пять минут, а всё доверие к набору — после третьего раза люди начинают перезапускать CI не глядя, и тогда набор перестаёт работать вообще.</p>
<p>Цена бага, наоборот, часто переоценивается. Сдвинутая на два пикселя иконка в футере — это не инцидент. Списание лишних денег с карты — это инцидент, поддержка, возвраты и репутация. Между ними три порядка разницы, и одинаковое отношение к ним — главная ошибка.</p>

<h4>Почему так</h4>
<p>Тесты существуют, чтобы ускорять изменения, а не чтобы быть. Набор, который тормозит CI и краснеет от каждого переименования, замедляет команду сильнее, чем редкий баг, который чинится за час хотфиксом. Обратное тоже верно: там, где откат невозможен — отправленное письмо, проведённый платёж, удалённые данные, — тест почти любой цены оправдан, потому что ущерб необратим.</p>

<h4>Подводные камни</h4>
<ul>
<li>Мигающий тест держат «пока», добавив <code>retry</code>: набор формально зелёный, доверия к нему нет.</li>
<li>Тест на снапшот всей разметки страницы: обновляется флагом <code>-u</code> не глядя, значит не проверяет ничего.</li>
<li>Тесты не удаляют никогда: считается, что удалить тест — признаться в плохой работе.</li>
<li>Не смотрят на время прогона: набор незаметно вырастает с двух минут до двадцати, и его начинают выключать.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как решаешь, удалить или починить?</strong> Смотрю историю: если тест за год падал пять раз и ни разу по делу — удаляю. <strong>А регрессия?</strong> Тест на конкретный исправленный баг обычно дешёвый и точный, такие оставляю почти всегда.</p>`,
    code: `// дорогой и бесполезный: снапшот всей страницы
test('страница профиля', () => {
  const { container } = render(<ProfilePage />);
  expect(container).toMatchSnapshot();   // 400 строк diff при любой правке
});
// упадёт от смены отступа. Обновят через -u, не читая. Багов не ловит.

// дешёвый и точный: регрессия на конкретный баг из прода
test('телефон без кода страны нормализуется, а не режется', () => {
  // баг PROD-1421: '9161234567' превращался в '+7 916 123 45 6'
  expect(normalizePhone('9161234567')).toBe('+79161234567');
});`,
    tip: 'Сформулируй правило про мигание: одно ложное падение стоит не пять минут, а доверие ко всему набору - после третьего раза CI перезапускают не глядя. Это аргумент, который сразу выдаёт человека с опытом поддержки тестов, а не только их написания.' },

  { id: 'tst4',
    q: 'Часто говорят, что тесты — это документация. На практике это редко правда. Как ты пишешь тесты, которые действительно читаются?',
    a: `<h4>Коротко</h4>
<p>Тест читается как документация, если по одному его названию и трём строкам тела понятно правило предметной области, а не устройство кода. Значит: название формулирует поведение и условие, тело разбито на «дано — действие — проверка», а вся подготовка спрятана за фабрику с говорящим именем.</p>

<h4>Как это работает</h4>
<p>Начинаю с названия. Плохое имя описывает функцию: «тест checkout». Хорошее описывает правило и условие: «не даёт оформить заказ, если в корзине товар из другого города». Второе можно прочитать аналитику вслух, и он подтвердит или опровергнет. Это и есть проверка на документацию — если правило нельзя обсудить с человеком без кода, тест документацией не является.</p>
<p>Дальше структура. Тело держу в три блока без комментариев-заголовков: подготовка данных, одно действие, одна проверка. Как только в тесте появляется второе действие, он перестаёт читаться: непонятно, какое из них проверяет утверждение внизу. Подготовку выношу в фабрики вида <code>makeCart({ items: 3 })</code>, где явно указано только то, что важно для этого правила, а остальное берётся по умолчанию. Тест из восьми строк подготовки не документирует ничего, кроме формы объекта.</p>
<p>Группировка через <code>describe</code> задаёт оглавление: внешний блок — сущность, внутренний — состояние или роль. Открыл файл, свернул тела — получил список правил.</p>

<h4>Почему так</h4>
<p>Комментарии и вики устаревают молча, тест устаревает громко: он краснеет. Это единственная документация с автоматической проверкой актуальности. Но свойство работает, только если тест написан на языке предметной области. Тест, который проверяет, что вызвался <code>useEffect</code>, документирует React, а не продукт, и после рефакторинга станет ложью.</p>

<h4>Подводные камни</h4>
<ul>
<li>Условия и циклы внутри теста: читатель должен исполнять его в голове, чтобы понять, что проверяется.</li>
<li>Общий <code>beforeEach</code> на весь файл: чтобы понять один тест, надо прокрутить наверх и держать состояние в голове.</li>
<li>Магические числа без объяснения: <code>expect(res).toBe(2091)</code> без строки, откуда взялась сумма.</li>
<li>Названия вида «работает корректно» и «случай 2» — оглавления не получается.</li>
<li>Слишком общие фабрики: <code>makeUser()</code> с тридцатью полями скрывает то самое поле, из-за которого тест зелёный.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Чем это лучше сторибука?</strong> Сторибук документирует внешний вид, тест — правила. Они дополняют друг друга. <strong>Не дублирование ли это требований?</strong> Дублирование, но исполняемое: расхождение с кодом обнаружится в CI, а не через полгода на демо.</p>`,
    code: `// было: название про код, подготовка на девять строк
test('checkout', async () => {
  const store = createStore(rootReducer, { cart: { items: [
    { id: 1, sku: 'A', qty: 1, price: 100, warehouse: 'msk' },
    { id: 2, sku: 'B', qty: 2, price: 50, warehouse: 'spb' },
  ], promo: null, city: 'msk' } });
  render(<Checkout />, { wrapper: withStore(store) });
  expect(screen.getByRole('button', { name: 'Оформить' })).toBeDisabled();
});

// стало: правило в названии, в теле только то, что его определяет
test('не даёт оформить заказ, если товар с чужого склада', () => {
  renderCheckout({ city: 'msk', items: [item({ warehouse: 'spb' })] });
  expect(screen.getByRole('button', { name: 'Оформить' })).toBeDisabled();
});`,
    tip: 'Дай проверку на документацию одной фразой: если название теста нельзя прочитать аналитику вслух и получить «да, так и должно быть», это не документация, а описание реализации.' },

  { id: 'tst5',
    q: 'Тимлид требует 100% покрытия. Что ты ему ответишь и на какие метрики предложишь смотреть вместо?',
    a: `<h4>Коротко</h4>
<p>Скажу, что покрытие измеряет исполнение строк, а не проверку поведения: код можно исполнить и не проверить ничего. Стопроцентная цель гарантирует только одно — что в проекте появятся тесты без утверждений и моки на всё подряд. Вместо цифры предлагаю смотреть на покрытие критичных модулей, на утечку багов в прод и на время починки.</p>

<h4>Как это работает</h4>
<p>Инструменты считают четыре разных вещи, и их путают. Строчное покрытие — сколько строк исполнилось. Ветвевое — сколько развилок <code>if</code> прошли по обеим сторонам. Функциональное — сколько функций вызвали. Покрытие условий — сколько комбинаций внутри одного условия проверили. Строчное самое оптимистичное и самое бесполезное: один вызов функции без единого <code>expect</code> даёт сто процентов по её строкам. Если уж выбирать порог, то по ветвям — он хотя бы заставляет пройти обе стороны развилки.</p>
<p>Последние проценты — самые дорогие. Это обработчики ошибок сети, защитные ветки, недостижимые <code>default</code>. Чтобы их покрыть, приходится ломать архитектуру ради тестируемости или мокать всё вокруг. Кривая усилий загибается вверх, а кривая пользы — вниз.</p>
<p>Практически я делаю так: порог ставлю дифференцированный. Восемьдесят по ветвям на модуле расчётов и прав, ноль требований на разметке. И включаю проверку покрытия только на изменённых строках в пулл-реквесте — это заставляет писать тест к новому коду, но не заставляет закрывать легаси разом.</p>

<h4>Почему так</h4>
<p>Покрытие — хорошая диагностика и плохая цель. Как диагностика оно показывает, куда никто не заглядывал: открыл отчёт, увидел красный модуль оплаты — это сигнал. Как цель оно подчиняется закону Гудхарта: показатель, ставший целью, перестаёт быть показателем. Команда начинает оптимизировать метрику, а не риск.</p>

<h4>Подводные камни</h4>
<ul>
<li>Тесты без утверждений: вызвали функцию, ничего не проверили, покрытие выросло.</li>
<li>Игнор-комментарии, расставленные ради прохождения порога.</li>
<li>Порог на весь репозиторий: один большой сгенерированный файл роняет цифру, и его начинают исключать.</li>
<li>Сто процентов на строках при непокрытых ветках — самая частая иллюзия безопасности.</li>
<li>Мутационное тестирование не пробовали, хотя именно оно отвечает на вопрос «а тест бы это поймал?».</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Какие метрики предлагаешь?</strong> Число багов, дошедших до прода, доля упавших сборок не по делу, время прогона набора. <strong>Что такое мутационное тестирование?</strong> Инструмент портит код — меняет знак, убирает условие — и смотрит, покраснел ли хоть один тест. Если нет, покрытие фиктивное.</p>`,
    code: `function priceWithVat(price, isExempt) {
  if (isExempt) return price;
  return Math.round(price * 1.2);
}

// 100% строк, 100% функций - и ноль пользы
test('priceWithVat не падает', () => {
  priceWithVat(100, false);         // ни одного expect
});

// покрывает обе ветки и фиксирует правило округления
test.each([
  [100, false, 120],
  [100, true, 100],
  [1, false, 1],                    // 1.2 -> 1, а не 2: банковское округление не используем
])('priceWithVat(%i, %s) = %i', (price, exempt, expected) => {
  expect(priceWithVat(price, exempt)).toBe(expected);
});`,
    tip: 'Назови закон Гудхарта: показатель, ставший целью, перестаёт быть показателем. И предложи покрытие только по изменённым строкам в пулл-реквесте - это заставляет покрывать новый код, не требуя закрывать легаси разом.' },

  { id: 'tst6',
    q: 'Тебе дали модуль на три тысячи строк без единого теста, и его надо срочно менять. Что делаешь?',
    a: `<h4>Коротко</h4>
<p>Не пишу тесты на весь модуль — на это нет времени и смысла. Сначала обкладываю характеризующими тестами тот кусок, который трогаю: фиксирую текущее поведение, каким бы оно ни было, включая странности. Потом делаю рефакторинг под защитой этих тестов и только затем меняю логику.</p>

<h4>Как это работает</h4>
<p>Характеризующий тест не спрашивает «как должно быть». Он спрашивает «как есть сейчас». Я подаю на вход реальные данные с прода, записываю выход и превращаю его в утверждение. Если функция округляет в неожиданную сторону — фиксирую именно это. Цель не описать правильное поведение, а поймать момент, когда я его случайно изменю.</p>
<p>Главная проблема легаси — не размер, а сцепленность: функция ходит в глобальный объект, читает время, дёргает сеть и мутирует аргумент. Тест не написать, не разорвав хотя бы один шов. Здесь работает приём «шов вместо переписывания»: выделяю зависимость в параметр со значением по умолчанию, чтобы вызывающий код не менялся. Одна строка правки, и функция стала тестируемой. Сеть перехватываю на уровне сети, не переписывая слой запросов.</p>
<p>Дальше правило прикосновения: любой файл, который я открыл ради задачи, уходит из репозитория чуть более покрытым, чем был. Не весь — только тронутая часть. За полгода это закрывает именно те места, которые реально меняются, а мёртвые три тысячи строк так и остаются без тестов, и это правильно.</p>

<h4>Почему так</h4>
<p>Легаси-код опасен тем, что его поведение никто не знает целиком, а пользователи уже на него полагаются — включая баги, которые стали фичами. Попытка сначала «сделать красиво» кончается тем, что ломается сценарий, о котором не знал никто. Характеризующий тест переводит неизвестное поведение в зафиксированное, и после этого рефакторинг становится обычной работой, а не сапёрной.</p>

<h4>Подводные камни</h4>
<ul>
<li>Рефакторинг и изменение логики в одном коммите: тест упал, и непонятно, из-за чего именно.</li>
<li>Характеризующий тест на снапшот огромного объекта — падает от любой мелочи и не объясняет, что сломалось.</li>
<li>Данные для теста придуманы, а не взяты с прода: покрывают ровно те случаи, о которых уже подумали.</li>
<li>Разрыв зависимостей заходит слишком далеко и превращается в переписывание модуля до первого теста.</li>
<li>Зафиксировали баг как эталон и забыли пометить комментарием, что это не желаемое поведение.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>А если поведение недетерминированное?</strong> Значит первым делом выношу источник недетерминизма — часы, случайность, порядок — в зависимость. <strong>Как выбрать данные?</strong> Беру выборку из логов и добавляю границы: пустое, ноль, максимум, отрицательное.</p>`,
    code: `// было: тестировать нечем - время и сеть внутри
export async function buildReport(userId) {
  const now = new Date();
  const rows = await api.get('/stats/' + userId);
  return rows.filter(r => r.date > now - 7 * 864e5);
}

// стало: шов через параметры со значением по умолчанию.
// Вызывающий код не меняется ни в одном месте.
export async function buildReport(userId, deps = { now: () => new Date(), api }) {
  const now = deps.now();
  const rows = await deps.api.get('/stats/' + userId);
  return rows.filter(r => r.date > now - 7 * 864e5);
}

// характеризующий тест: фиксируем как есть, а не как должно быть
test('в отчёт попадают строки за последние 7 суток включительно', async () => {
  const rows = await buildReport('u1', {
    now: () => new Date('2026-03-10T00:00:00Z'),
    api: { get: async () => fixtureRows },
  });
  expect(rows.map(r => r.id)).toEqual([3, 4, 5]);
});`,
    tip: 'Назови приём своим именем: характеризующий тест фиксирует текущее поведение, а не желаемое. И добавь правило прикосновения - покрываем только тронутую часть файла, за счёт этого покрытие само стягивается к живым местам кода.' },

  { id: 'tst7',
    q: 'Кто у вас в команде пишет тесты и в какой момент — до кода, после, или это ловят на ревью?',
    a: `<h4>Коротко</h4>
<p>Тесты пишет автор кода, в том же пулл-реквесте. На ревью тесты не «ловят» — их читают первыми, потому что по ним быстрее всего понять, что именно автор считает поведением. Момент выбираю по типу задачи: баг — строго сначала тест, новая логика с понятным контрактом — тоже сначала, исследовательский UI — после.</p>

<h4>Как это работает</h4>
<p>На баге я пишу тест до фикса всегда, без исключений. Причина техническая: тест, написанный после починки, не доказан. Он зелёный, но я не знаю, покраснел бы он на сломанном коде. Красный тест перед фиксом — это доказательство того, что тест смотрит в нужное место, и заодно точная формулировка бага, которую можно приложить к задаче.</p>
<p>На новой логике с известным контрактом — расчёты, валидация, преобразование данных — тест вперёд экономит время: я сразу проектирую сигнатуру от места вызова, а не подгоняю тест под уже написанную функцию. На UI, где форма компонента ещё не устоялась, писать тест первым дорого: перепишу трижды. Там сначала прототип, потом тест на устоявшееся поведение, до слияния.</p>
<p>Порядок чтения на ревью я держу такой: описание задачи, затем тесты, затем код. Если из тестов не понятно, что делает изменение, это первое замечание — и почти всегда оно вскрывает, что автор сам не сформулировал требование.</p>

<h4>Почему так</h4>
<p>Отдельный «этап тестирования» после разработки не работает: к тому моменту код уже написан так, что тестируется плохо, и автор потерял контекст. Тестируемость — свойство архитектуры, и она возникает только тогда, когда о вызове теста думают в момент проектирования. Отдельные тестировщики при этом нужны, но не для юнитов: их сила в исследовательском тестировании и в сценариях, которые разработчик не придумает, потому что знает, как оно устроено внутри.</p>

<h4>Подводные камни</h4>
<ul>
<li>Тесты отдельным пулл-реквестом «потом»: этот пулл-реквест не появляется никогда.</li>
<li>Фикс без красного теста: тест написан под уже работающий код и может не проверять баг вообще.</li>
<li>Ревью тестов по остаточному принципу — прокрутили вниз, увидели зелёный CI, апрувнули.</li>
<li>Требование писать тест первым на всё подряд, включая вёрстку: это тратит время и злит команду.</li>
<li>Тесты пишет другой человек: он документирует своё понимание кода, а не контракт автора.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Практикуешь ли TDD в чистом виде?</strong> На алгоритмической логике — да, на UI — нет. <strong>Что делать с горящим хотфиксом?</strong> Фикс сразу, тест — следующим пулл-реквестом в тот же день, с задачей в трекере, иначе не вернёмся.</p>`,
    code: `// шаг 1. Тест на баг из трекера. Должен быть КРАСНЫМ до фикса.
test('PROD-2210: пустая корзина не даёт NaN в итоге', () => {
  expect(cartTotal({ items: [], delivery: 300 })).toBe(0);
  // фактически возвращает NaN: reduce без начального значения
});

// шаг 2. Убеждаюсь, что упал именно с NaN, а не с ошибкой импорта.
// шаг 3. Правлю код.
export const cartTotal = ({ items, delivery }) =>
  items.length === 0 ? 0 : items.reduce((s, i) => s + i.price * i.qty, 0) + delivery;

// шаг 4. Тест зелёный. Теперь он доказан: я видел его красным.`,
    tip: 'Сформулируй принцип доказанного теста: тест, который никогда не был красным, ничего не доказывает - неизвестно, смотрит ли он в нужное место. Это ровно тот аргумент, из-за которого на баге тест пишется до фикса.' },

  { id: 'tst8',
    q: 'Почему Testing Library принципиально не даёт добраться до состояния компонента и его внутренних методов?',
    a: `<h4>Коротко</h4>
<p>Потому что состояние компонента — не контракт. Контракт — это то, что видит и делает пользователь: что отрисовано, что доступно с клавиатуры, что ушло в сеть. Тест, который читает <code>state</code>, привязывается к решению, которое завтра поменяют, и краснеет при рефакторинге, где поведение не изменилось ни на йоту.</p>

<h4>Как это работает</h4>
<p>Технически библиотека просто не даёт таких API. <code>render</code> возвращает контейнер и набор запросов по DOM, а не инстанс. Запросы ищут по тому, что видно: роль, доступное имя, подпись, текст. Всё, что тест может узнать о компоненте, он узнаёт из отрисованного дерева — то есть ровно из того канала, через который с ним общается пользователь и скринридер.</p>
<p>Прямое следствие: тест переживает переписывание внутренностей. Заменили три <code>useState</code> на <code>useReducer</code>, вынесли логику в кастомный хук, перевели на другую библиотеку форм — тест не тронут, потому что кнопка по-прежнему называется «Сохранить» и по-прежнему дизейблится на невалидном адресе. Ровно за это и платят: набор тестов, который не мешает менять код, а страхует изменения.</p>
<p>Обратное тоже работает как индикатор. Если поведение нельзя проверить через DOM, обычно это значит, что его нельзя и увидеть — то есть либо оно не нужно, либо интерфейс недоступен: нет подписи у поля, нет роли у кастомной кнопки, нет объявления об ошибке для скринридера.</p>

<h4>Почему так</h4>
<p>Формулировка автора библиотеки: чем больше тест похож на то, как софтом пользуются, тем больше уверенности он даёт. Уверенность — это вероятность, что зелёный тест означает работающий продукт. Тест на внутренности эту вероятность почти не поднимает: он проверяет, что React работает как React. Платим за это скоростью локализации — упавший тест на поведение говорит «форма не отправилась», а не «редьюсер вернул не тот статус», и до причины идти дольше.</p>

<h4>Подводные камни</h4>
<ul>
<li>Обходят ограничение через <code>container.querySelector</code> по классам — получают ту же хрупкость другим путём.</li>
<li>Дёргают внутренние коллбэки напрямую через <code>rerender</code> с новыми пропсами вместо действия пользователя.</li>
<li>Считают, что раз внутренности проверять нельзя, то и сложную чистую логику тестировать негде — её надо вынести из компонента и покрыть юнитом.</li>
<li>Проверяют вызов моканого хука вместо результата на экране.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>А если состояние действительно сложное?</strong> Выношу редьюсер или машину состояний в чистый модуль и тестирую отдельно — это уже не внутренности компонента. <strong>Когда всё-таки нужен <code>data-testid</code>?</strong> Когда у узла нет ни роли, ни текста: контейнер списка, обёртка виртуализации, канвас.</p>`,
    code: `// хрупкий тест: держится за внутреннее состояние
const { result } = renderHook(() => useToggle());
act(() => result.current.toggle());
expect(result.current.state.isOpen).toBe(true);
// переименовали isOpen в expanded внутри хука - тест красный, продукт цел

// устойчивый тест: держится за то, что видит пользователь
const user = userEvent.setup();
render(<Accordion title="Доставка">Пункты выдачи</Accordion>);

expect(screen.queryByText('Пункты выдачи')).not.toBeInTheDocument();
await user.click(screen.getByRole('button', { name: 'Доставка' }));
expect(screen.getByText('Пункты выдачи')).toBeVisible();
// внутри может быть useState, useReducer или xstate - тесту всё равно`,
    tip: 'Переверни принцип в диагностику: если поведение невозможно проверить через DOM, чаще всего его невозможно и увидеть - у поля нет подписи, у кастомной кнопки нет роли. Тест на доступное дерево заодно ловит проблемы доступности.' },

  { id: 'tst9',
    q: 'В пулл-реквесте getByTestId на каждой строчке. Что скажешь на ревью и какой порядок запросов правильный?',
    a: `<h4>Коротко</h4>
<p>Скажу, что <code>data-testid</code> — последний рубеж, а не первый выбор: он ничего не проверяет про доступность и живёт только для теста, поэтому его легко потерять при рефакторинге разметки. Порядок такой: сначала доступные всем запросы (роль, подпись, текст), потом семантические, и только потом тестовые идентификаторы.</p>

<h4>Как это работает</h4>
<p>Приоритет строится по одному критерию — насколько запрос совпадает с тем, как элемент находит человек. Первая группа: <code>getByRole</code> с опцией <code>name</code>, <code>getByLabelText</code> для полей формы, <code>getByPlaceholderText</code>, <code>getByText</code>, <code>getByDisplayValue</code>. Вторая: <code>getByAltText</code> и <code>getByTitle</code> — их видит не каждый пользователь. Третья: <code>getByTestId</code>.</p>
<p><code>getByRole</code> стоит первым, потому что ищет по дереву доступности, а не по DOM. Роль берётся из тега или атрибута <code>role</code>, а доступное имя вычисляется по алгоритму: сначала <code>aria-labelledby</code>, затем <code>aria-label</code>, затем содержимое элемента, затем связанный <code>label</code>. Это ровно то, что произнесёт скринридер. Значит один запрос проверяет сразу две вещи: элемент на месте и он доступен. Кастомный «див-кнопка» без роли просто не найдётся — и это полезное падение, а не помеха.</p>
<p>Ещё одна причина: при промахе <code>getByRole</code> печатает в консоль весь список доступных ролей с именами. Это самая быстрая отладка разметки, которая есть в наборе.</p>

<h4>Почему так</h4>
<p>Идея в том, чтобы тест не мог быть зелёным на интерфейсе, которым нельзя пользоваться. Цена — скорость: <code>getByRole</code> заметно медленнее <code>querySelector</code>, потому что считает дерево доступности, и на больших деревьях в jsdom это чувствуется. Лечится сужением области поиска через <code>within</code>, а не отказом от роли.</p>

<h4>Подводные камни</h4>
<ul>
<li><code>name</code> сверяется полным совпадением строки: лишний пробел или иконка внутри кнопки — и элемент не найден.</li>
<li>Скрытые элементы по умолчанию не попадают в выдачу: <code>display: none</code>, <code>aria-hidden</code>, атрибут <code>hidden</code>.</li>
<li>Ставят <code>role="button"</code> на <code>div</code>, чтобы «пройти тест», вместо настоящей кнопки — теряют клавиатуру и фокус.</li>
<li>Идентификаторы вида <code>data-testid="btn-1"</code>: при изменении порядка элементов тест начинает проверять не то.</li>
<li>Используют <code>getByText</code> на кнопке вместо роли — найдут и <code>span</code> с тем же текстом.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Когда <code>data-testid</code> оправдан?</strong> Контейнеры без семантики, элементы виртуализованного списка, канвас, сторонние виджеты. <strong>Как искать по части текста?</strong> Передать регулярное выражение или функцию в <code>name</code>.</p>`,
    code: `// было: тест не знает ничего о доступности
render(<Dialog />);
fireEvent.click(screen.getByTestId('dialog-submit'));
expect(screen.getByTestId('dialog-error')).toBeInTheDocument();

// стало: тот же сценарий через доступное дерево
render(<Dialog />);
const dialog = screen.getByRole('dialog', { name: 'Подтверждение' });

await user.click(within(dialog).getByRole('button', { name: 'Удалить' }));
expect(await within(dialog).findByRole('alert')).toHaveTextContent('Нет прав');
// within сужает поиск - и быстрее, и не поймает такую же кнопку в шапке`,
    tip: 'Объясни, почему getByRole первый: он ищет по дереву доступности и вычисляет имя тем же алгоритмом, что скринридер - aria-labelledby, aria-label, содержимое, label. Один запрос проверяет и наличие элемента, и то, что интерфейсом можно пользоваться без мыши.' },

  { id: 'tst10',
    q: 'getBy, queryBy и findBy — объясни разницу так, чтобы стало понятно, когда какой брать.',
    a: `<h4>Коротко</h4>
<p>Различаются двумя признаками: синхронный или асинхронный и что делают при отсутствии элемента. <code>getBy</code> — синхронный, бросает исключение, если не нашёл. <code>queryBy</code> — синхронный, возвращает <code>null</code>. <code>findBy</code> — асинхронный, ждёт появления и возвращает промис. Правило: есть сейчас — <code>getBy</code>, нет сейчас — <code>queryBy</code>, появится позже — <code>findBy</code>.</p>

<h4>Как это работает</h4>
<p><code>findBy</code> — это не отдельный механизм, а <code>getBy</code>, обёрнутый в <code>waitFor</code>. Внутри стоит наблюдатель за мутациями DOM: библиотека подписывается через <code>MutationObserver</code> на изменения контейнера и перезапускает запрос после каждой пачки изменений, плюс подстраховывается интервалом в пятьдесят миллисекунд. Таймаут по умолчанию — тысяча миллисекунд, после чего промис отклоняется с сообщением и распечаткой текущего DOM.</p>
<p>Отсюда практическое следствие: <code>findBy</code> ждёт появления, но не умеет ждать исчезновения — там нужен <code>waitForElementToBeRemoved</code>. И ещё: у каждого запроса есть форма во множественном числе (<code>getAllBy</code>, <code>queryAllBy</code>, <code>findAllBy</code>), и она меняет поведение при нескольких совпадениях: одиночная форма бросает ошибку «найдено несколько», множественная возвращает массив.</p>
<p>Про <code>queryBy</code> есть отдельная ловушка. Он единственный, кем можно проверить отсутствие: <code>expect(screen.queryByText('Ошибка')).not.toBeInTheDocument()</code>. С <code>getBy</code> такая проверка невозможна физически — он бросит исключение раньше, чем дойдёт до утверждения.</p>

<h4>Почему так</h4>
<p>Разделение сделано ради читаемости сообщений об ошибках. <code>getBy</code> бросает исключение с полным дампом DOM в момент, когда элемент не найден, — это точная диагностика. Если бы он возвращал <code>null</code>, ошибка всплывала бы позже и звучала как «cannot read properties of null», а искать пришлось бы вручную.</p>

<h4>Подводные камни</h4>
<ul>
<li><code>queryBy</code> для проверки наличия: <code>expect(queryByText(...)).toBeInTheDocument()</code> при промахе даёт невнятную ошибку про null вместо дампа DOM.</li>
<li>Забытый <code>await</code> перед <code>findBy</code>: утверждение применяется к промису и всегда проходит.</li>
<li><code>queryBy</code> сразу после действия для проверки, что элемент не появился: он и не успел бы появиться, тест ложно зелёный.</li>
<li>Ожидание исчезновения через <code>await waitFor(() =&gt; expect(queryBy(...)).toBeNull())</code> без гарантии, что элемент вообще был.</li>
<li>Одиночная форма там, где элементов несколько: ошибка «found multiple» вместо ожидаемого результата.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Почему <code>findBy</code> иногда висит секунду и падает, хотя элемент есть?</strong> Обычно неточное имя или элемент скрыт от дерева доступности. <strong>Можно ли менять таймаут?</strong> Да, вторым аргументом, но чаще это лечение симптома.</p>`,
    code: `// хрупко: ложно зелёный тест
await user.click(screen.getByRole('button', { name: 'Сохранить' }));
expect(screen.queryByText('Сохранено')).not.toBeInTheDocument();
// сообщение придёт через 200 мс. Тест проверил пустой момент времени.

// хрупко: утверждение применилось к промису, тест зелёный всегда
expect(screen.findByText('Сохранено')).toBeInTheDocument();

// устойчиво: три инструмента на своих местах
expect(screen.queryByText('Сохранено')).not.toBeInTheDocument();  // до действия
await user.click(screen.getByRole('button', { name: 'Сохранить' }));
expect(await screen.findByText('Сохранено')).toBeVisible();       // ждём появления`,
    tip: 'Скажи, что findBy - это getBy внутри waitFor, а waitFor подписан на MutationObserver и перепроверяет запрос после каждой пачки изменений DOM, а не просто опрашивает по таймеру. И упомяни, что eslint-plugin-testing-library ловит забытый await автоматически.' },

  { id: 'tst11',
    q: 'В чём практическая разница между userEvent и fireEvent? Приведи случай, где fireEvent даёт зелёный тест на сломанном коде.',
    a: `<h4>Коротко</h4>
<p><code>fireEvent</code> диспатчит ровно одно событие, которое я назвал. <code>userEvent</code> воспроизводит всю цепочку, которую в реальности порождает браузер: наведение, нажатие, фокус, ввод посимвольно, отпускание, клик. Поэтому <code>fireEvent</code> легко проходит там, где интерфейс на самом деле не работает.</p>

<h4>Как это работает</h4>
<p>Возьмём ввод в поле. <code>fireEvent.change(input, { target: { value: 'abc' } })</code> напрямую записывает значение и стреляет одним событием <code>change</code>. Ни <code>focus</code>, ни <code>keydown</code>, ни <code>keyup</code>, ни промежуточных состояний. Если компонент ставит валидацию на <code>onBlur</code>, ограничивает длину в <code>onKeyDown</code> или маскирует ввод по символам — ничего из этого не сработает, а тест будет зелёным.</p>
<p><code>userEvent.type</code> делает иначе: наводит указатель, кликает, ставит фокус, затем на каждый символ шлёт <code>keydown</code>, <code>keypress</code>, <code>input</code>, <code>keyup</code> и двигает каретку. И проверяет то, что проверил бы браузер: у отключённого элемента событий не будет вовсе, у элемента с <code>pointer-events: none</code> клик не пройдёт.</p>
<p>Важная деталь версии 14: API стал асинхронным и требует <code>userEvent.setup()</code> перед рендером. Каждое действие возвращает промис, внутри применяется <code>advanceTimers</code> и оборачивание в <code>act</code>. Забытый <code>await</code> — самый частый источник мигания на этих тестах.</p>

<h4>Почему так</h4>
<p>Смысл — сдвинуть тест ближе к реальности, чтобы зелёный означал «работает», а не «событие дошло». Платим временем: <code>userEvent.type</code> на длинной строке заметно медленнее одного <code>change</code>, потому что честно проходит все символы. Поэтому в подготовке данных, где ввод не является предметом теста, я иногда сознательно оставляю <code>fireEvent</code> — но только в подготовке, не в проверяемом действии.</p>

<h4>Подводные камни</h4>
<ul>
<li><code>setup()</code> вызван после <code>render</code>: часть слушателей документа не навешана, поведение отличается.</li>
<li>Забытый <code>await</code>: действие уходит в микрозадачу, утверждение проверяет состояние до него.</li>
<li>Фейковые таймеры без <code>advanceTimers</code> в <code>setup</code> — <code>userEvent</code> зависает на внутренних задержках.</li>
<li><code>fireEvent.click</code> на отключённой кнопке проходит и вызывает обработчик, чего в браузере не случилось бы.</li>
<li>Ждут, что <code>userEvent</code> проверит перекрытие элемента модалкой: в jsdom нет layout, видимость считается по стилям, а не по геометрии.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Когда <code>fireEvent</code> всё-таки нужен?</strong> События без пользовательского аналога: <code>scroll</code>, <code>resize</code>, <code>animationend</code>, кастомные события. <strong>Как ввести текст мгновенно?</strong> <code>user.paste</code> или <code>type</code> с опцией пропуска задержки.</p>`,
    code: `// компонент режет ввод длиннее 10 символов в onKeyDown
function Code({ onDone }) {
  const [v, setV] = useState('');
  return <input aria-label="Код" value={v}
    onKeyDown={(e) => { if (v.length >= 10 && e.key.length === 1) e.preventDefault(); }}
    onChange={(e) => setV(e.target.value)}
    onBlur={() => v.length === 10 && onDone(v)} />;
}

// хрупкий тест: зелёный на сломанном ограничении
fireEvent.change(screen.getByLabelText('Код'), { target: { value: '12345678901234' } });
expect(screen.getByLabelText('Код')).toHaveValue('12345678901234');
// onKeyDown не вызывался, onBlur не вызывался - проверили только setState

// устойчивый тест: проходит ту же цепочку, что браузер
const user = userEvent.setup();
render(<Code onDone={onDone} />);
await user.type(screen.getByLabelText('Код'), '12345678901234');
await user.tab();                                  // уводим фокус -> blur

expect(screen.getByLabelText('Код')).toHaveValue('1234567890');
expect(onDone).toHaveBeenCalledWith('1234567890');`,
    tip: 'Приведи конкретную цепочку для type: наведение, клик, фокус, затем на каждый символ keydown, keypress, input, keyup. И упомяни ловушку версии 14 - setup() вызывается до render, иначе часть слушателей документа окажется не навешана.' },

  { id: 'tst12',
    q: 'Как ты тестируешь кастомный хук? И когда его вообще не стоит тестировать отдельно?',
    a: `<h4>Коротко</h4>
<p>По умолчанию не тестирую отдельно: проверяю его через компонент, который им пользуется. Отдельный тест через <code>renderHook</code> завожу, когда хук переиспользуется в нескольких местах, имеет много веток и его контракт важен сам по себе — например, хук пагинации или синхронизации с адресной строкой.</p>

<h4>Как это работает</h4>
<p><code>renderHook</code> из Testing Library — тонкая обёртка: она создаёт технический компонент, вызывает внутри него хук и складывает возвращённое значение в <code>result.current</code>. Отсюда два правила. Первое: <code>result.current</code> — снимок последнего рендера, а не живая ссылка. Сохранил его в переменную до действия — читаешь устаревшее значение. Второе: любое изменение состояния снаружи React-обработчика нужно оборачивать в <code>act</code>, иначе React предупредит, что обновление не обработано, а утверждение увидит старый рендер.</p>
<p>Зависимости хука подставляю через <code>wrapper</code>: провайдеры контекста, роутер, клиент запросов. Это же место, где видно, тестируем ли мы хук или пол-приложения вокруг него — если обёртка на двадцать строк, хук слишком сцеплен.</p>
<p>Важно, что проверяю: возвращаемые значения и переходы между ними, стабильность ссылок на функции, поведение при размонтировании — отписки, отмена запроса. Последнее часто и есть настоящая причина писать отдельный тест: утечку подписки через компонент увидеть тяжело.</p>

<h4>Почему так</h4>
<p>Хук — деталь реализации, а не публичный контракт продукта, если у него один потребитель. Тест на такой хук фиксирует форму, которую завтра захочется поменять, и тормозит рефакторинг ровно так же, как тест на внутреннее состояние. Но у переиспользуемого хука потребители — другие разработчики, и тогда его форма и есть контракт: тест становится и проверкой, и документацией к API.</p>

<h4>Подводные камни</h4>
<ul>
<li>Сохранённая ссылка на <code>result.current</code> до действия — читают старый снимок и не понимают, почему значение не изменилось.</li>
<li>Отсутствие <code>act</code> вокруг вызова возвращённой функции: предупреждение в консоли и гонка.</li>
<li>Асинхронный хук проверяют без ожидания: <code>expect(result.current.data)</code> сразу после рендера всегда <code>undefined</code>.</li>
<li>Тест на хук вместо теста на экран: хук зелёный, компонент не подключил его результат.</li>
<li>Проверка количества рендеров как основного утверждения — ломается от любой оптимизации React.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как проверить очистку?</strong> Вызвать <code>unmount()</code> и убедиться, что слушатель снят или запрос отменён. <strong>Чем заменить старый <code>react-hooks-testing-library</code>?</strong> Он вошёл в основную библиотеку с версии 13.1, отдельный пакет больше не нужен.</p>`,
    code: `// хрупко: ссылка сохранена до действия
const { result } = renderHook(() => useCounter());
const { increment, count } = result.current;      // снимок первого рендера
act(() => increment());
expect(count).toBe(1);                            // 0: читаем старый снимок

// устойчиво: обращаемся к result.current каждый раз
const { result, unmount } = renderHook(() => useCounter());
act(() => result.current.increment());
expect(result.current.count).toBe(1);

// то, ради чего отдельный тест и нужен: очистка при размонтировании
const remove = jest.spyOn(window, 'removeEventListener');
const { unmount } = renderHook(() => useOnlineStatus());
unmount();
expect(remove).toHaveBeenCalledWith('online', expect.any(Function));`,
    tip: 'Дай критерий выбора: у хука один потребитель - тестируем через компонент, несколько потребителей - хук становится публичным API, и тест на него превращается в документацию. И объясни, что result.current - снимок рендера, а не живая ссылка.' },

  { id: 'tst13',
    q: 'Форма из восьми полей с валидацией и отправкой. Ты будешь писать тест на каждое поле?',
    a: `<h4>Коротко</h4>
<p>Нет. На каждое поле пишу юниты на схему валидации — там их дёшево делать таблицей. А на саму форму — три-четыре теста на сценарии: успешная отправка, отправка с ошибкой валидации, ошибка сервера, состояние отправки. Форма проверяется как целое, потому что баги в ней живут в связях, а не в полях.</p>

<h4>Как это работает</h4>
<p>Разделяю на два слоя. Схема валидации — чистая функция: объект на вход, список ошибок на выход. Её покрываю таблицей <code>test.each</code>: двадцать строк на все граничные случаи, выполняется за миллисекунды, без рендера. Никакого DOM здесь не нужно.</p>
<p>Форма как компонент отвечает за другое: связывает поля со схемой, показывает ошибки рядом с нужным полем, блокирует повторную отправку, шлёт запрос с правильным телом, обрабатывает ответ. Вот это я и проверяю. В успешном сценарии заполняю форму через <code>userEvent</code> и утверждаю два факта: запрос ушёл с ожидаемым телом и пользователь увидел подтверждение. Тело запроса беру из перехватчика сети, а не из мока функции — так проверяется и сериализация.</p>
<p>Один тест на валидацию через интерфейс всё-таки пишу — не ради правила, а ради связи: что сообщение об ошибке действительно привязано к полю через <code>aria-describedby</code> и что фокус уезжает на первое ошибочное поле. Этого юнит на схему не покажет.</p>

<h4>Почему так</h4>
<p>Восемь тестов на восемь полей — это восемь медленных рендеров, проверяющих одно и то же соединение проводов. Они дублируют юниты на схему и при этом не ловят настоящие ошибки формы: отправку дважды по двойному клику, потерянное поле в теле запроса, ошибку сервера, которая не показалась. Экономия времени тут не главное — главное, что набор перестаёт врать про то, что он проверяет.</p>

<h4>Подводные камни</h4>
<ul>
<li>Проверяют только вызов <code>onSubmit</code>: тело запроса не проверено, потерянное поле не поймано.</li>
<li>Не проверяют защиту от двойной отправки — самый частый баг форм в проде.</li>
<li>Ищут сообщение об ошибке через <code>getByText</code> по всему документу, а не рядом с полем.</li>
<li>Асинхронная валидация проверяется без <code>findBy</code>, тест мигает.</li>
<li>Тест зависит от порядка полей: <code>getAllByRole('textbox')[2]</code> вместо подписи.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как проверить, что ошибка связана с полем?</strong> Через <code>toHaveAccessibleDescription</code> или сравнение <code>aria-describedby</code> с идентификатором сообщения. <strong>А если полей сорок?</strong> Тем более сценарии, плюс генератор заполнения, который проходит по подписям.</p>`,
    code: `// слой 1: правила - таблицей, без рендера
test.each([
  ['', 'Укажите почту'],
  ['a@b', 'Проверьте адрес почты'],
  ['a@b.ru', null],
])('email %s -> %s', (email, error) => {
  expect(validate({ email }).email ?? null).toBe(error);
});

// слой 2: форма целиком - один сценарий, много связей сразу
test('отправляет заполненную форму один раз и показывает подтверждение', async () => {
  const user = userEvent.setup();
  const bodies = [];
  server.use(http.post('/api/profile', async ({ request }) => {
    bodies.push(await request.json());
    return HttpResponse.json({ ok: true });
  }));

  render(<ProfileForm />);
  await fillProfile(user, { name: 'Ирина', email: 'i@example.com' });
  const submit = screen.getByRole('button', { name: 'Сохранить' });
  await user.dblClick(submit);                     // двойной клик - частый баг

  expect(await screen.findByRole('status')).toHaveTextContent('Профиль сохранён');
  expect(bodies).toHaveLength(1);                  // не два запроса
  expect(bodies[0]).toEqual({ name: 'Ирина', email: 'i@example.com' });
});`,
    tip: 'Назови два слоя явно: правила валидации - чистая функция и таблица test.each, форма - сценарии. И добавь проверку двойного клика: защита от повторной отправки ломается чаще всего и юнитом на схему не ловится вообще.' },

  { id: 'tst14',
    q: '«Тестируй так, как пользуется пользователь» — где эта идея перестаёт работать?',
    a: `<h4>Коротко</h4>
<p>Идея работает как приоритет, но не как догма. Она ломается там, где среда теста не является браузером: jsdom не считает геометрию и не рисует; там, где комбинаций слишком много, чтобы кликать; и там, где важное поведение пользователь вообще не видит — например, отмена лишнего запроса или отписка от сокета.</p>

<h4>Как это работает</h4>
<p>Первая граница — jsdom. Он реализует DOM и события, но не движок вёрстки: нет размеров, <code>getBoundingClientRect</code> возвращает нули, <code>IntersectionObserver</code> и <code>ResizeObserver</code> отсутствуют, каскад применяется частично. Значит проверить перекрытие модалкой, попадание в область клика, залипание шапки или виртуализацию списка в jsdom нельзя — здесь честный ответ «это уходит в браузерный тест», а не мок обсерверов ради зелёного.</p>
<p>Вторая граница — комбинаторика. Правила скидок с пятью параметрами дают сотни сочетаний. Проходить их кликами — часы в CI и нечитаемые падения. Такое место я вырезаю в чистую функцию и покрываю таблицей. Это не отказ от принципа, а признание, что сюда пользователь и не смотрит: он смотрит на итоговую сумму, и её проверяет один сценарий.</p>
<p>Третья граница — невидимое поведение с реальными последствиями. Гонка запросов при быстром переключении вкладок, отмена через <code>AbortController</code>, повторные подписки, идемпотентность отправки. Пользователь этого не увидит, а деньги спишутся дважды. Здесь проверяю обращения к сети через перехватчик — то есть контракт с внешним миром, а не внутренности.</p>

<h4>Почему так</h4>
<p>Принцип — это про источник уверенности, а не про запрет. Формулирую его точнее: тест должен проверять контракт с внешним миром — с пользователем через DOM и с сервером через сеть. Всё, что между, деталь реализации. При такой формулировке проверка отменённого запроса перестаёт противоречить принципу, а проверка внутреннего состояния по-прежнему ему противоречит.</p>

<h4>Подводные камни</h4>
<ul>
<li>Моки <code>IntersectionObserver</code> ради зелёного теста виртуализации: проверяется мок, не поведение.</li>
<li>Попытка через jsdom проверить, что элемент реально видим на экране — там нет layout.</li>
<li>Догматический отказ от юнитов на чистую логику: набор становится медленным и плохо локализующим.</li>
<li>Проверка отправки аналитики кликами по интерфейсу вместо перехвата запроса.</li>
<li>Считают браузерные тесты заменой всему — время прогона растёт до десятков минут.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Чем заменить jsdom, если нужен layout?</strong> Компонентные тесты в реальном браузере: Playwright Component Testing или Vitest в браузерном режиме. <strong>Как понять, что пора?</strong> Когда в тесте больше двух моков браузерных API.</p>`,
    code: `// не работает: в jsdom нет layout, все размеры нулевые
test('подсказка не вылезает за экран', () => {
  render(<Tooltip text="..." />);
  const rect = screen.getByRole('tooltip').getBoundingClientRect();
  expect(rect.right).toBeLessThan(window.innerWidth);  // 0 < 1024 - зелёный всегда
});

// работает: невидимое поведение через контракт с сетью
test('при быстром переключении вкладок отменяет предыдущий запрос', async () => {
  const aborted = [];
  server.use(http.get('/api/tab/:id', async ({ params, request }) => {
    request.signal.addEventListener('abort', () => aborted.push(params.id));
    await delay(50);
    return HttpResponse.json({ id: params.id });
  }));

  render(<Tabs />);
  await user.click(screen.getByRole('tab', { name: 'Заказы' }));
  await user.click(screen.getByRole('tab', { name: 'Возвраты' }));

  expect(await screen.findByText('Возвраты: 3')).toBeVisible();
  expect(aborted).toEqual(['orders']);
});`,
    tip: 'Переформулируй принцип точнее: тест проверяет контракты с внешним миром - с пользователем через DOM и с сервером через сеть, а всё между ними деталь реализации. Такая формулировка сразу объясняет, почему проверять отменённый запрос можно, а внутреннее состояние нельзя.' },

  { id: 'tst15',
    q: 'У тебя тест зелёный локально и красный в CI примерно через раз. Как ты это чинишь?',
    a: `<h4>Коротко</h4>
<p>Мигание — это всегда неявная зависимость от времени или порядка. Локально машина быстрее и не нагружена, поэтому нужный порядок случайно совпадает; в CI на общем раннере с четырьмя параллельными воркерами он не совпадает. Чиню не увеличением таймаута, а поиском конкретной гонки: ищу, чего тест ждёт неявно, и делаю ожидание явным.</p>

<h4>Как это работает</h4>
<p>Источников немного, и они узнаваемы. Первый — проверка сразу после действия, которое разрешается через микрозадачу: промис резолвится, React планирует рендер, а утверждение уже выполнилось. Локально успевает, в CI нет. Лечится <code>findBy</code> вместо <code>getBy</code>.</p>
<p>Второй — общее состояние между тестами: модуль-синглтон, кеш клиента запросов, счётчик в замыкании, таймер из предыдущего теста, не очищенный <code>localStorage</code>. Тесты в jest и vitest идут в одном процессе на воркер, поэтому модульное состояние переживает файл. Проявляется как «падает только при определённом порядке» — и ловится запуском с рандомизацией порядка.</p>
<p>Третий — реальное время: <code>setTimeout(300)</code> для дебаунса против таймаута ожидания в тысячу миллисекунд. На загруженном раннере задержка растягивается. Четвёртый — параллелизм: два теста пишут в одну временную папку или в один порт.</p>
<p>Пятый, отдельный, — часовой пояс и локаль. Раннер в UTC, ноутбук в плюс три: тест на форматирование даты падает только для дат около полуночи.</p>

<h4>Почему так</h4>
<p>Мигающий тест хуже отсутствующего: он тратит время команды и обучает игнорировать красное. Поэтому политика простая — мигающий тест либо чинится в тот же день, либо помечается <code>skip</code> с задачей в трекере. Держать его в наборе с ретраями значит платить за него и не получать сигнала.</p>

<h4>Подводные камни</h4>
<ul>
<li>Увеличили таймаут — тест стал падать реже, но набор замедлился, а гонка осталась.</li>
<li>Добавили <code>await new Promise(r =&gt; setTimeout(r, 100))</code>: работает до первого медленного дня в CI.</li>
<li>Ретраи на уровне раннера скрывают проблему и одновременно скрывают настоящую регрессию.</li>
<li>Не чистят моки и таймеры между тестами: порядок начинает влиять на результат.</li>
<li>Ищут причину по одному падению вместо прогона в цикле сто раз с рандомизацией.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как воспроизвести локально?</strong> Прогнать файл сто раз подряд, ограничив процессору долю времени, и с тем же часовым поясом и локалью, что в CI. <strong>Ретраи вообще нельзя?</strong> В e2e один ретрай допустим как защита от сетевых сбоев, в юнитах — нет.</p>`,
    code: `// мигает: проверка выполняется до того, как промис дошёл до рендера
test('показывает имя пользователя', async () => {
  render(<Profile id="u1" />);
  expect(screen.getByText('Ирина')).toBeInTheDocument();   // гонка
});

// мигает иначе: общее состояние переживает файл
let cache = new Map();                    // модульный синглтон
test('a', () => { cache.set('k', 1); /* ... */ });
test('b', () => { expect(cache.size).toBe(1); });   // зависит от порядка

// устойчиво: явное ожидание + изоляция состояния
beforeEach(() => {
  cache = new Map();
  queryClient.clear();
  jest.clearAllMocks();
  localStorage.clear();
});

test('показывает имя пользователя', async () => {
  render(<Profile id="u1" />);
  expect(await screen.findByText('Ирина')).toBeVisible();
});

// команда для поиска: 100 прогонов с рандомным порядком
// npx jest profile.test.tsx --runInBand --randomize --verbose=false -- -t ''`,
    tip: 'Скажи главное правило: мигание - это неявная зависимость от времени или порядка, и лечится оно явным ожиданием, а не увеличением таймаута. И назови политику: мигающий тест чинится в тот же день или уходит в skip с задачей, потому что с ретраями он стоит денег и не даёт сигнала.' },

  { id: 'tst16',
    q: 'Когда нужен act, а когда waitFor? И почему предупреждение «not wrapped in act» иногда появляется на ровном месте?',
    a: `<h4>Коротко</h4>
<p><code>act</code> — синхронная граница: «я сделал изменение, прогони все запланированные обновления и эффекты до конца». <code>waitFor</code> — ожидание: «повторяй проверку, пока не пройдёт или не кончится таймаут». Первое нужно, когда я сам инициирую обновление вне React-обработчика; второе — когда обновление придёт само, из промиса или таймера.</p>

<h4>Как это работает</h4>
<p>React в тестовом окружении собирает обновления в пачку и выполняет эффекты не мгновенно. <code>act</code> открывает область, внутри которой обновления копятся, а на выходе синхронно прогоняются: применяются состояния, вызываются эффекты, коммитится DOM. Без этого утверждение может увидеть промежуточный кадр. Именно об этом предупреждение: React заметил обновление, случившееся вне такой области, и сообщает, что тест мог наблюдать несогласованное состояние.</p>
<p>Отсюда «на ровном месте». Обновление приходит после того, как тест логически закончился: запрос долетел после утверждения, дебаунс сработал после размонтирования, подписка обновила состояние уже размонтированного компонента. Само предупреждение не баг — это симптом того, что в тесте есть незавершённая асинхронная работа. Правильная реакция не подавить его, а дождаться результата явно или отменить работу в очистке.</p>
<p>На практике <code>act</code> вручную я пишу редко: <code>render</code>, <code>fireEvent</code> и все действия <code>userEvent</code> уже обёрнуты внутри. Руками он нужен для ручной прокрутки таймеров и для вызова функций, возвращённых из <code>renderHook</code>. А <code>waitFor</code> внутри тоже использует <code>act</code>, поэтому комбинация <code>await act(async () =&gt; ...)</code> вокруг <code>waitFor</code> — лишняя.</p>

<h4>Почему так</h4>
<p>Модель React асинхронна по своей природе: обновления приоритезируются и могут прерываться. Тест же хочет детерминизма. <code>act</code> — это шов, который на время теста делает поведение синхронным и предсказуемым. Платим тем, что тест видит не совсем ту же картину времени, что браузер: промежуточные кадры схлопываются.</p>

<h4>Подводные камни</h4>
<ul>
<li>Подавление предупреждения через мок <code>console.error</code> — прячут настоящую гонку.</li>
<li>Утверждения внутри <code>waitFor</code>, которых там несколько: при падении непонятно, какое из них не выполнилось, и коллбэк успевает выполниться много раз.</li>
<li>Побочные действия внутри <code>waitFor</code> — клик или запрос повторится десятки раз.</li>
<li><code>await act(async () =&gt; {})</code> как способ «прокрутить микрозадачи»: работает случайно и ломается при смене версии.</li>
<li>Асинхронный <code>act</code> вокруг синхронного кода — предупреждение исчезает, гонка остаётся.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Почему <code>findBy</code> лучше <code>waitFor</code>?</strong> Он короче, содержит одну проверку и печатает дамп DOM при падении. <strong>Что делать с обновлением после размонтирования?</strong> Отменять запрос в функции очистки эффекта.</p>`,
    code: `// плохо: несколько утверждений и действие внутри waitFor
await waitFor(() => {
  fireEvent.click(screen.getByRole('button'));   // выполнится много раз
  expect(save).toHaveBeenCalled();
  expect(screen.getByText('Готово')).toBeVisible();
});

// хорошо: действие снаружи, ожидание одной проверкой
await user.click(screen.getByRole('button', { name: 'Сохранить' }));
expect(await screen.findByText('Готово')).toBeVisible();
expect(save).toHaveBeenCalledTimes(1);

// act нужен руками там, где обновление инициирую я сам
jest.useFakeTimers();
render(<Toast text="Сохранено" />);
act(() => { jest.advanceTimersByTime(3000); });   // без act React не прогонит эффекты
expect(screen.queryByText('Сохранено')).not.toBeInTheDocument();`,
    tip: 'Дай точное разделение: act - синхронная граница «прогони запланированные обновления и эффекты», waitFor - повтор проверки до успеха. И добавь, что предупреждение «not wrapped in act» почти всегда означает незавершённую асинхронную работу, которая доехала после конца теста, а не проблему в самом act.' },

  { id: 'tst17',
    q: 'Ты включил фейковые таймеры, и тест повис на waitFor. Что произошло?',
    a: `<h4>Коротко</h4>
<p>Классический дедлок. <code>waitFor</code> внутри опрашивает условие по интервалу, а интервал — это тоже таймер. Фейковые таймеры остановили время, поэтому <code>waitFor</code> ждёт вечно, а прокрутить его некому: код теста стоит на <code>await</code>. Лечится либо передачей функции продвижения таймеров в <code>userEvent.setup</code>, либо ручной прокруткой внутри <code>act</code> без <code>waitFor</code>.</p>

<h4>Как это работает</h4>
<p>Фейковые таймеры подменяют <code>setTimeout</code>, <code>setInterval</code>, <code>Date.now</code> и часто <code>queueMicrotask</code> и <code>performance.now</code> на управляемые реализации. Время двигается только вручную: <code>advanceTimersByTime</code>, <code>runAllTimers</code>, <code>runOnlyPendingTimers</code>. Всё, что опирается на реальные часы, замирает.</p>
<p>Второй камень — промисы. <code>advanceTimersByTime</code> двигает макрозадачи, но не даёт очереди микрозадач прокрутиться между ними, если код теста синхронный. Поэтому цепочка «таймер сработал — вызвал промис — промис обновил состояние» может не доехать. Нужна асинхронная версия <code>advanceTimersByTimeAsync</code> или явный <code>await Promise.resolve()</code> между шагами.</p>
<p>Третий — <code>userEvent</code> версии 14 внутри использует задержки. С фейковыми таймерами он зависает, если не сказать ему, как двигать время: <code>userEvent.setup({ advanceTimers: jest.advanceTimersByTime })</code>. Это самая частая причина «тест висит после включения фейковых таймеров».</p>
<p>И последнее: таймеры нужно возвращать обратно в <code>afterEach</code>, иначе следующий файл унаследует остановленное время и начнёт мигать по непонятной причине.</p>

<h4>Почему так</h4>
<p>Фейковые таймеры существуют, чтобы не ждать реального времени: тест дебаунса на пятьсот миллисекунд выполняется мгновенно, а тест ретраев с экспоненциальной паузой — вместо тридцати секунд за миллисекунды. Цена — тест перестаёт видеть реальный порядок событий, и всё, что зависит от переплетения таймеров с сетью, приходится двигать руками.</p>

<h4>Подводные камни</h4>
<ul>
<li>Включили фейковые таймеры глобально в конфиге: половина набора начала висеть без видимой причины.</li>
<li><code>runAllTimers</code> на коде с повторяющимся интервалом — бесконечный цикл и зависший процесс.</li>
<li>Забыли <code>jest.useRealTimers()</code> в <code>afterEach</code>: протекает в соседние файлы.</li>
<li>Двигают время, но не дают доехать промисам — состояние не обновилось, тест падает загадочно.</li>
<li>Мокают <code>Date</code>, но не <code>performance.now</code>, и код, замеряющий длительность, ведёт себя странно.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как тестировать дебаунс?</strong> Фейковые таймеры плюс прокрутка на порог минус один и на порог. <strong>Когда лучше вообще без них?</strong> Когда задержки маленькие: проще подождать реально, чем разбираться с переплетением очередей.</p>`,
    code: `// зависает: waitFor опрашивает по таймеру, а время остановлено
jest.useFakeTimers();
render(<Search />);
await user.type(screen.getByRole('searchbox'), 'кот');
await waitFor(() => expect(search).toHaveBeenCalled());   // висит до таймаута jest

// работает: userEvent знает, чем двигать время
beforeEach(() => jest.useFakeTimers());
afterEach(() => jest.useRealTimers());

test('дебаунс 500 мс: один запрос вместо трёх', async () => {
  const user = userEvent.setup({ advanceTimers: jest.advanceTimersByTime });
  render(<Search onSearch={search} />);

  await user.type(screen.getByRole('searchbox'), 'кот');
  await act(async () => { jest.advanceTimersByTime(499); });
  expect(search).not.toHaveBeenCalled();

  await act(async () => { jest.advanceTimersByTime(1); });   // граница порога
  expect(search).toHaveBeenCalledTimes(1);
  expect(search).toHaveBeenCalledWith('кот');
});`,
    tip: 'Объясни дедлок механикой: waitFor опрашивает условие через setInterval, а фейковые таймеры остановили именно его - двигать время некому, потому что тест стоит на await. И назови рецепт: advanceTimers в userEvent.setup плюс асинхронная прокрутка, чтобы между макрозадачами доезжали микрозадачи.' },

  { id: 'tst18',
    q: 'Как правильно дождаться, что элемент исчез, и почему наивная проверка тут врёт?',
    a: `<h4>Коротко</h4>
<p>Наивная проверка врёт, потому что не отличает «уже исчез» от «ещё не появлялся». Если спиннер отрисуется через кадр после клика, а я проверю его отсутствие сразу, тест будет зелёным на любом коде, включая полностью сломанный. Правильный инструмент — <code>waitForElementToBeRemoved</code>: он требует, чтобы элемент существовал в момент вызова, и падает, если его нет.</p>

<h4>Как это работает</h4>
<p><code>waitForElementToBeRemoved</code> принимает либо уже найденный узел, либо функцию-запрос. Сначала он проверяет наличие: если элемента нет — сразу бросает ошибку с внятным текстом. Дальше подписывается на мутации контейнера и ждёт, пока запрос перестанет находить элемент или узел покинет документ. Таймаут по умолчанию тот же, что у <code>waitFor</code>.</p>
<p>Разница с <code>await waitFor(() =&gt; expect(queryBy...).toBeNull())</code> ровно в первой проверке. Второй вариант проходит мгновенно, если элемента не было изначально, и это тихо ломает смысл теста. Поэтому я использую его только там, где наличие элемента уже утверждено строкой выше.</p>
<p>Отдельный случай — исчезновение по таймеру, например тост на три секунды. Тут либо фейковые таймеры с ручной прокруткой, либо увеличенный таймаут ожидания. Реальное ожидание трёх секунд в наборе из двухсот тестов складывается в минуты, поэтому предпочитаю таймеры.</p>
<p>И ещё нюанс с ролями: элемент может остаться в DOM, но выпасть из дерева доступности — например, получив <code>aria-hidden</code> при открытии модалки. Тогда запрос по роли перестанет его находить, хотя узел на месте, и <code>toBeInTheDocument</code> по-прежнему пройдёт.</p>

<h4>Почему так</h4>
<p>Асимметрия «появление против исчезновения» — общее свойство асинхронных проверок. Появление доказывается наблюдением: увидел — значит есть. Отсутствие не доказывается наблюдением в один момент времени, потому что момент может быть выбран неудачно. Единственный корректный способ — зафиксировать наличие, затем дождаться перехода.</p>

<h4>Подводные камни</h4>
<ul>
<li>Проверка отсутствия сразу после клика: тест зелёный на сломанном коде.</li>
<li><code>waitForElementToBeRemoved</code> с <code>getBy</code> внутри: запрос бросит исключение в момент удаления, а не вернёт null — нужен <code>queryBy</code>.</li>
<li>Ждут исчезновения спиннера, который отрисовывается только при задержке ответа: локально ответ мгновенный, элемента не было, тест падает.</li>
<li>Проверяют исчезновение и тут же утверждают наличие данных без <code>findBy</code>: между этими событиями есть кадр.</li>
<li>Элемент скрыт стилем, а не удалён: запрос по тексту его всё ещё находит.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>А если спиннер может не появиться вообще?</strong> Тогда не жду его исчезновения, а жду появления данных. <strong>Как проверить, что чего-то не произошло?</strong> Дождаться наблюдаемого события после и только тогда утверждать отсутствие.</p>`,
    code: `// врёт: зелёный, даже если загрузка вообще не начиналась
await user.click(screen.getByRole('button', { name: 'Обновить' }));
expect(screen.queryByRole('progressbar')).not.toBeInTheDocument();

// врёт мягче: пройдёт мгновенно, если спиннера не было
await waitFor(() => expect(screen.queryByRole('progressbar')).toBeNull());

// честно: требует, чтобы элемент был, и ждёт именно перехода
await user.click(screen.getByRole('button', { name: 'Обновить' }));
const spinner = await screen.findByRole('progressbar');
await waitForElementToBeRemoved(spinner);
expect(await screen.findByRole('table')).toBeVisible();

// исчезновение по таймеру - через фейковые таймеры, а не реальное ожидание
act(() => { jest.advanceTimersByTime(3000); });
expect(screen.queryByRole('status')).not.toBeInTheDocument();`,
    tip: 'Назови асимметрию явно: появление доказывается наблюдением, отсутствие в один момент времени не доказывается ничем. Отсюда требование waitForElementToBeRemoved падать, если элемента не было изначально - это защита от теста, зелёного на пустом месте.' },

  { id: 'tst19',
    q: 'Компонент шлёт три запроса, ответы приходят в произвольном порядке. Как написать тест, который не мигает?',
    a: `<h4>Коротко</h4>
<p>Первым делом делаю порядок ответов детерминированным на стороне перехватчика: каждый обработчик отдаёт ответ по явной команде из теста, а не через случайную задержку. Тогда я сам решаю, что придёт первым, и проверяю именно тот случай, который меня интересует — включая тот, где медленный первый запрос обгоняет быстрый второй.</p>

<h4>Как это работает</h4>
<p>Ставлю обработчики, которые ждут промис, управляемый из теста. Это даёт полный контроль: отпустил второй, проверил экран, отпустил первый, проверил, что он не затёр данные. Ровно этот сценарий и есть содержательный — в проде именно он ломается, когда пользователь быстро переключает фильтры.</p>
<p>Дальше проверяю не «сколько было запросов», а что видит пользователь после того, как всё улеглось. Число запросов — деталь реализации: библиотека данных может дедуплицировать, повторить при фокусе окна, сходить в кеш. Утверждение <code>toHaveBeenCalledTimes(3)</code> на таком коде мигает само по себе.</p>
<p>Отдельно проверяю защиту от гонки в самом коде: устаревший ответ должен игнорироваться. Это делается либо отменой через <code>AbortController</code>, либо сравнением ключа запроса с текущим. Тест на это выглядит так: отпустить ответы в обратном порядке и убедиться, что на экране данные последнего запроса, а не последнего пришедшего.</p>
<p>Если запросы независимы и порядок не важен, то проверяю итог, а не промежуточные состояния: три блока данных на экране после того, как все ответы доставлены.</p>

<h4>Почему так</h4>
<p>Мигание в таких тестах почти всегда рождается из задержек: <code>delay(50)</code> и <code>delay(100)</code> в моках дают нужный порядок на быстрой машине и произвольный на загруженном раннере. Управляемые промисы убирают время из уравнения полностью, и тест становится проверкой логики, а не гонкой планировщиков.</p>

<h4>Подводные камни</h4>
<ul>
<li>Задержки вместо явного управления: порядок «обычно правильный», а значит мигание.</li>
<li>Утверждения на число вызовов там, где библиотека данных дедуплицирует или ретраит.</li>
<li>Проверка промежуточного состояния между двумя ответами без явной синхронизации.</li>
<li>Забытая отмена в очистке эффекта: предупреждение про act и обновление размонтированного компонента.</li>
<li>Тест проверяет только счастливый путь, где ответы приходят по порядку, — то есть ровно тот случай, который не ломается.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как код должен защищаться от гонки?</strong> Отмена предыдущего запроса или проверка, что ответ соответствует текущему ключу. <strong>А если данных много и они грузятся параллельно?</strong> Проверяю конечное состояние экрана, а не порядок прибытия.</p>`,
    code: `// мигает: порядок задан задержками
server.use(
  http.get('/api/a', async () => { await delay(50); return HttpResponse.json(A); }),
  http.get('/api/b', async () => { await delay(10); return HttpResponse.json(B); }),
);
// на загруженном раннере 50 и 10 мс могут поменяться местами

// устойчиво: порядок задаётся тестом явно
const gate = () => { let open; const p = new Promise(r => { open = r; }); return { p, open }; };
const first = gate(), second = gate();

server.use(
  http.get('/api/items', async ({ request }) => {
    const q = new URL(request.url).searchParams.get('q');
    await (q === 'кот' ? first.p : second.p);
    return HttpResponse.json({ q, items: [q] });
  }),
);

render(<Search />);
await user.type(screen.getByRole('searchbox'), 'кот');
await user.clear(screen.getByRole('searchbox'));
await user.type(screen.getByRole('searchbox'), 'пёс');

second.open();                                   // отвечает актуальный запрос
expect(await screen.findByText('пёс')).toBeVisible();
first.open();                                    // устаревший ответ приходит позже
await waitFor(() => expect(screen.queryByText('кот')).not.toBeInTheDocument());`,
    tip: 'Скажи, что убираешь время из уравнения: вместо delay в обработчиках - промисы, которые тест отпускает вручную. И добавь, что проверять toHaveBeenCalledTimes на запросах опасно - библиотека данных дедуплицирует, ретраит и ходит в кеш, поэтому проверять надо экран после того, как всё улеглось.' },

  { id: 'tst20',
    q: 'Опиши свою процедуру отладки флакающего теста — по шагам, а не «посмотрю логи».',
    a: `<h4>Коротко</h4>
<p>Процедура из пяти шагов: воспроизвести с измеримой частотой, изолировать (файл отдельно, порядок случайный, один воркер), сузить до строки, назвать конкретный механизм гонки, починить причину и доказать сотней прогонов. Пока частота падения не измерена, любая «починка» — это гадание.</p>

<h4>Как это работает</h4>
<p>Шаг первый: воспроизведение. Гоняю файл в цикле сто раз и записываю, сколько раз упал. Три из ста — это база, с которой я сравню результат починки. Без числа нельзя отличить исправление от везения.</p>
<p>Шаг второй: изоляция. Запускаю только этот файл. Если перестал падать — виновато соседство: общее модульное состояние, незакрытые таймеры, моки. Если падает и в одиночку — гонка внутри самого теста. Дальше меняю порядок тестов внутри файла и число воркеров: разное поведение при одном и нескольких воркерах указывает на общий ресурс.</p>
<p>Шаг третий: сужение. Убираю половину утверждений и половину действий, пока падение не исчезнет. Заодно снимаю разницу окружения: часовой пояс, локаль, доля процессорного времени. Часто ответ находится здесь — CI в UTC, ноутбук в другом поясе.</p>
<p>Шаг четвёртый: назвать механизм. Требую от себя фразы вида «утверждение выполняется до того, как микрозадача с ответом дошла до коммита React». Если такой фразы нет, я не понял причину и лечу симптом.</p>
<p>Шаг пятый: починка и доказательство. Сто прогонов подряд без падений. Одновременно фиксирую в коде тестов защиту, которая не даст этому повториться: сброс состояния в <code>beforeEach</code>, фиксированный часовой пояс в конфиге.</p>

<h4>Почему так</h4>
<p>Мигание — статистическое явление, и разбирать его надо статистически. Один прогон ничего не доказывает ни до, ни после. Эта дисциплина заодно защищает от самого частого исхода: человек добавляет таймаут, тест не падает три раза подряд, задача закрывается, и через неделю всё возвращается.</p>

<h4>Подводные камни</h4>
<ul>
<li>Проверка «починил» одним зелёным прогоном при базовой частоте падения три процента.</li>
<li>Отладка по логам CI без локального воспроизведения — тратятся часы на ожидание сборок.</li>
<li>Не фиксируют часовой пояс и локаль в конфиге, и половина мигания живёт вокруг полуночи.</li>
<li>Чинят тест, а не код: гонка была настоящая и доедет до прода.</li>
<li>Не записывают найденную причину — та же гонка появляется в соседнем тесте через месяц.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>А если не воспроизводится локально вообще?</strong> Ограничиваю ресурсы контейнера и повторяю окружение CI, включая число воркеров. <strong>Как найти общее состояние?</strong> Прогон с рандомизацией порядка и бинарный поиск по списку файлов.</p>`,
    code: `# шаг 1: измерить частоту падения
for i in $(seq 100); do
  npx jest src/Search.test.tsx --silent || echo "fail $i"
done | tee before.log | grep -c fail

# шаг 2: изоляция - один файл, один воркер, случайный порядок
npx jest src/Search.test.tsx --runInBand --randomize

# шаг 3: снять разницу окружения
TZ=UTC LANG=en_US.UTF-8 npx jest src/Search.test.tsx

# шаг 5: защита в конфиге, чтобы причина не вернулась
# jest.config.js
module.exports = {
  globalSetup: './jest.tz.js',   // process.env.TZ = 'UTC'
  restoreMocks: true,            // моки восстанавливаются автоматически
  resetModules: true,            // модульные синглтоны не переживают файл
  testTimeout: 5000,
};`,
    tip: 'Ключевая мысль: мигание - статистическое явление, поэтому и до, и после починки нужно число, а не один прогон. И потребуй от себя формулировку механизма вслух - «утверждение выполнилось до того, как микрозадача с ответом дошла до коммита» - без неё лечится симптом.' },

  { id: 'tst21',
    q: 'Почему MSW лучше, чем jest.mock на fetch? Что конкретно ты этим выигрываешь?',
    a: `<h4>Коротко</h4>
<p>Потому что MSW перехватывает на уровне сети, а не подменяет функцию. Код под тестом остаётся нетронутым: он честно вызывает <code>fetch</code> или axios, честно собирает URL, заголовки и тело, честно разбирает ответ и статус. Мок функции всё это пропускает — и вместе с этим пропускает целый класс багов.</p>

<h4>Как это работает</h4>
<p>В окружении Node MSW подменяет транспорт на уровне <code>http</code> и <code>fetch</code>, в браузере — через сервис-воркер. Дальше запрос сопоставляется с обработчиком по методу и шаблону пути, а обработчик возвращает настоящий объект <code>Response</code>. Для тестируемого кода это неотличимо от сервера.</p>
<p>Что становится проверяемым. Первое — формирование запроса: правильный путь, параметры строки запроса, заголовок авторизации, сериализация тела. При <code>jest.mock</code> я подменяю свой же слой API и просто верю, что он собирает URL правильно. Второе — обработка ответа: статус 404 и 500 идут по разным веткам, а <code>fetch</code> не бросает исключение на четырёхсотых, и код обязан это учитывать. Третье — ретраи, отмена, таймауты: обработчик видит <code>request.signal</code> и может отреагировать на отмену.</p>
<p>Практический бонус: одни и те же обработчики переиспользуются в трёх местах — в юнит-тестах, в Storybook и в режиме разработки без бэкенда. Плюс тесты перестают зависеть от того, чем именно мы ходим в сеть: замена axios на <code>fetch</code> не трогает ни один тест.</p>

<h4>Почему так</h4>
<p>Правило простое: мок ставится на самой дальней границе, до которой можно дотянуться. Чем дальше граница, тем больше своего кода остаётся под проверкой. Сеть — естественная граница системы, за ней чужая ответственность. Платим настройкой: нужен сервер моков, перехват и аккуратный сброс обработчиков между тестами. И платим тем, что MSW не проверит контракт — если бэкенд поменяет поле, обработчик останется старым, и тесты будут зелёными.</p>

<h4>Подводные камни</h4>
<ul>
<li>Забытый <code>server.resetHandlers()</code> в <code>afterEach</code>: обработчик из одного теста протекает в следующий.</li>
<li><code>onUnhandledRequest</code> оставлен по умолчанию — незамоканный запрос проходит тихо, вместо того чтобы уронить тест.</li>
<li>Обработчики пишутся вручную и расходятся с реальным API — нужен генератор из схемы или контрактные тесты.</li>
<li>В обработчик кладут логику: он начинает быть вторым бэкендом, который тоже надо поддерживать.</li>
<li>Ошибку сети имитируют статусом 500 вместо настоящего обрыва — ветка <code>catch</code> остаётся непроверенной.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как проверить тело запроса?</strong> Прочитать его в обработчике и сравнить в утверждении. <strong>Что MSW не заменяет?</strong> Контрактные тесты и типы, сгенерированные из схемы: он не знает, каков API на самом деле.</p>`,
    code: `// мок функции: подменили свой же слой, URL и статусы не проверены
jest.mock('../api', () => ({ getUser: jest.fn().mockResolvedValue({ name: 'Ирина' }) }));
// баг «в путь не подставляется id» и ветка на 403 остаются невидимыми

// перехват на уровне сети: код собирает запрос сам
const server = setupServer(
  http.get('/api/users/:id', ({ params }) =>
    params.id === 'u1'
      ? HttpResponse.json({ name: 'Ирина' })
      : new HttpResponse(null, { status: 404 })),
);

beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

test('на 404 показывает «пользователь не найден», а не спиннер навсегда', async () => {
  render(<Profile id="u-unknown" />);
  expect(await screen.findByText('Пользователь не найден')).toBeVisible();
});

test('обрыв сети уводит в ветку catch', async () => {
  server.use(http.get('/api/users/:id', () => HttpResponse.error()));
  render(<Profile id="u1" />);
  expect(await screen.findByRole('alert')).toHaveTextContent('Нет связи');
});`,
    tip: 'Сформулируй правило границы: мок ставится на самой дальней границе, до которой дотягиваешься, потому что всё до неё остаётся под проверкой. И честно назови предел MSW - он не знает реального контракта, поэтому не заменяет типы из схемы и контрактные тесты.' },

  { id: 'tst22',
    q: 'Когда мок начинает вредить? Как понять, что тест проверяет мок, а не код?',
    a: `<h4>Коротко</h4>
<p>Признак один и очень простой: удалите реализацию, оставьте мок — тест останется зелёным. Значит он проверяет мок. Второй признак — тест падает после рефакторинга, при котором поведение не изменилось: значит он зафиксировал не результат, а способ его получения.</p>

<h4>Как это работает</h4>
<p>Мок вредит в трёх ситуациях. Первая — мок стоит слишком близко к проверяемому коду. Замокали функцию, которую сами же и тестируем через соседа, — под проверкой не осталось ничего, кроме склейки. Классика: замоканный кастомный хук и утверждение <code>expect(useCart).toHaveBeenCalled()</code>.</p>
<p>Вторая — мок расходится с реальностью. Он возвращает объект, который сервер уже не возвращает: поле переименовали полгода назад. Тесты зелёные, прод падает. Особенно опасно, когда мок написан «по памяти», а не снят с реального ответа.</p>
<p>Третья — утверждения на взаимодействие вместо результата. <code>toHaveBeenCalledWith</code> фиксирует конкретный протокол вызова: имя метода, порядок и форму аргументов. Любая внутренняя перестановка красит тест, хотя пользователю ничего не изменилось. Такие утверждения оправданы там, где вызов и есть наблюдаемый эффект: отправка платежа, запись аналитики, удаление файла. Всё остальное лучше проверять по результату.</p>
<p>Мой рабочий критерий: мокаю только то, что я не контролирую и что имеет побочный эффект во внешнем мире — сеть, время, случайность, хранилище, платёжный виджет. Свой код не мокаю почти никогда.</p>

<h4>Почему так</h4>
<p>Мок — это утверждение о том, как ведёт себя чужая часть системы. Оно не проверяется ничем, и потому со временем стареет. Чем больше моков, тем больше непроверенных утверждений лежит в основании набора, и тем выше шанс, что все тесты зелёные при сломанном продукте. Отсюда правило: количество моков — это долг, а не инструмент.</p>

<h4>Подводные камни</h4>
<ul>
<li>Мок на собственный кастомный хук: тест превращается в проверку того, что хук вызван.</li>
<li>Частичный мок модуля, где случайно замокали и то, что тестируете.</li>
<li>Моки не сбрасываются между тестами: вызовы накапливаются, <code>toHaveBeenCalledTimes</code> мигает.</li>
<li>Мок возвращает форму, которой не бывает: <code>undefined</code> вместо пустого массива, и ветка не проверена.</li>
<li>Проверка порядка вызовов там, где порядок не является контрактом.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Когда <code>toHaveBeenCalledWith</code> уместен?</strong> Когда сам вызов и есть наблюдаемый эффект: платёж, аналитика, удаление. <strong>Как не дать мокам устареть?</strong> Строить их из типов, сгенерированных по схеме API, и держать контрактные тесты.</p>`,
    code: `// тест проверяет мок: удалите тело CartSummary - тест всё равно зелёный
jest.mock('../useCart');
useCart.mockReturnValue({ items: [], total: 0 });

test('показывает корзину', () => {
  render(<CartSummary />);
  expect(useCart).toHaveBeenCalled();          // утверждение ни о чём
});

// тест проверяет код: данные приходят с границы, проверяем результат
server.use(http.get('/api/cart', () =>
  HttpResponse.json({ items: [{ id: 1, price: 100, qty: 2 }], delivery: 300 })));

test('итог считается с доставкой и показывается пользователю', async () => {
  render(<CartSummary />);
  expect(await screen.findByText('500 ₽')).toBeVisible();   // 100*2 + 300
});

// здесь мок уместен: вызов и есть наблюдаемый эффект
expect(analytics.track).toHaveBeenCalledWith('checkout_started', { total: 500 });`,
    tip: 'Дай проверку одной фразой: удали реализацию и оставь мок - если тест зелёный, он проверяет мок. И назови критерий, что мокать: только то, что не контролируешь и что имеет побочный эффект во внешнем мире - сеть, время, случайность, хранилище.' },

  { id: 'tst23',
    q: 'Мок модуля через jest.mock или внедрение зависимости параметром — что выберешь и почему?',
    a: `<h4>Коротко</h4>
<p>Предпочитаю внедрение: зависимость видна в сигнатуре, подмена происходит обычным кодом, ничего не ломается при смене сборщика. <code>jest.mock</code> оставляю там, где зависимость нельзя пробросить: сторонняя библиотека внутри чужого компонента, побочный эффект на уровне модуля, тяжёлый импорт вроде редактора кода.</p>

<h4>Как это работает</h4>
<p><code>jest.mock</code> — это не обычный вызов. Он поднимается сборщиком в начало файла, до импортов, и подменяет модуль в реестре. Отсюда странности: нельзя ссылаться на переменные, объявленные выше, — нужен префикс <code>mock</code> в имени; при замене на vitest поведение отличается; ESM и CommonJS ведут себя по-разному. Плюс подмена глобальная на файл: все потребители модуля получают мок, включая тех, о ком вы не думали.</p>
<p>Внедрение работает иначе. Зависимость приходит параметром со значением по умолчанию или через фабрику модуля. В тесте я передаю другую реализацию — это обычный аргумент, никакой магии. Побочный эффект: тестируемость становится видимой в сигнатуре, и если параметров стало пять, это честный сигнал, что модуль делает слишком много.</p>
<p>В React роль контейнера зависимостей играет контекст: провайдер с клиентом API или с часами. В тесте оборачиваю в другой провайдер. Это то же внедрение, только через дерево.</p>

<h4>Почему так</h4>
<p>Разница в связанности с инструментом. <code>jest.mock</code> завязывает архитектуру теста на конкретный раннер и его модульную систему: миграция на vitest превращается в правку сотен файлов. Внедрение — это язык, оно переживёт любой раннер. Цена внедрения — многословность: приходится тащить зависимость через слои, и на глубоких деревьях это раздражает. Поэтому граница у меня такая: своя логика — внедрение, чужой модуль и глобальные эффекты — <code>jest.mock</code>.</p>

<h4>Подводные камни</h4>
<ul>
<li>Ссылка на внешнюю переменную в фабрике <code>jest.mock</code>: ошибка про доступ до инициализации из-за поднятия.</li>
<li>Забытый сброс: моки живут между тестами, если не включён <code>restoreMocks</code>.</li>
<li>Частичный мок без <code>jest.requireActual</code> — остальные экспорты стали <code>undefined</code>.</li>
<li>Внедрение доведено до крайности: у функции восемь параметров-зависимостей, читать невозможно.</li>
<li>Дефолтное значение параметра вычисляется на импорте и захватывает боевой клиент навсегда.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как мокать только один экспорт?</strong> Через фабрику с расширением реального модуля. <strong>Чем плох мок глобального <code>fetch</code>?</strong> Он глобален для всего файла и не проверяет формирование запроса — лучше перехват на уровне сети.</p>`,
    code: `// jest.mock: подмена в реестре модулей, поднимается наверх файла
jest.mock('../analytics', () => ({
  ...jest.requireActual('../analytics'),        // иначе остальные экспорты - undefined
  track: jest.fn(),
}));
// имя переменной обязано начинаться с mock, иначе ошибка про поднятие
const mockTrack = require('../analytics').track;

// внедрение: обычный параметр, никакой магии сборщика
export function createOrder(cart, deps = { clock: () => new Date(), api }) {
  return deps.api.post('/orders', { cart, at: deps.clock().toISOString() });
}

test('проставляет время создания заказа', async () => {
  const post = jest.fn().mockResolvedValue({ id: 1 });
  await createOrder(cart, { clock: () => new Date('2026-01-01T10:00:00Z'), api: { post } });

  expect(post).toHaveBeenCalledWith('/orders',
    expect.objectContaining({ at: '2026-01-01T10:00:00.000Z' }));
});`,
    tip: 'Назови техническую причину странностей jest.mock: вызов поднимается сборщиком выше импортов и подменяет модуль в реестре - отсюда и требование к префиксу mock в именах, и глобальность подмены на весь файл. Внедрение этой связи с раннером не создаёт и переживает миграцию на vitest.' },

  { id: 'tst24',
    q: 'В коде new Date() и Math.random(). Как ты такой код тестируешь?',
    a: `<h4>Коротко</h4>
<p>Недетерминизм не мокаю в каждом тесте, а выношу за границу модуля: часы и генератор случайных чисел становятся зависимостями. Там, где это невозможно, — сторонний код, глубокий вызов — фиксирую системное время фейковыми таймерами и подменяю генератор на детерминированный.</p>

<h4>Как это работает</h4>
<p>С датой три отдельные проблемы, и их путают. Первая — текущий момент: <code>Date.now()</code> и <code>new Date()</code> без аргументов. Фиксируется <code>jest.setSystemTime</code> вместе с фейковыми таймерами; важно, что это подменяет и <code>Date</code>, и таймеры согласованно.</p>
<p>Вторая — часовой пояс. Форматирование через <code>toLocaleDateString</code> зависит от пояса и локали процесса. В CI обычно UTC, на ноутбуке плюс три, и тест на дату около полуночи мигает по географическому признаку. Лечится не моком, а фиксацией <code>TZ</code> и локали в конфигурации раннера.</p>
<p>Третья — арифметика с датами: переход на летнее время, високосный день, последний день месяца. Здесь мок не нужен, нужна таблица случаев с явными датами.</p>
<p>Со случайностью проще: подменяю источник на генератор с зерном. Не <code>jest.spyOn(Math, 'random')</code> с возвратом константы — константа скрывает баги вроде выхода за границы массива, — а последовательность, повторяемая от запуска к запуску. Для идентификаторов лучше вообще внедрять фабрику <code>newId</code>: тогда в тесте идентификаторы предсказуемые и читаемые.</p>

<h4>Почему так</h4>
<p>Время и случайность — это скрытый вход функции. Функция, которая читает их изнутри, перестаёт быть чистой, и её нельзя проверить, не управляя глобальным окружением. Сделав вход явным, я возвращаю функции чистоту и заодно получаю возможность подставлять граничные моменты: 31 декабря, 29 февраля, переход часов.</p>

<h4>Подводные камни</h4>
<ul>
<li>Мок <code>Date</code>, но не <code>performance.now</code>: замеры длительности начинают давать бессмыслицу.</li>
<li>Фиксированное значение <code>Math.random</code>: тест зелёный, а на 0.999 код выходит за границу массива.</li>
<li>Не зафиксирован <code>TZ</code> в конфиге — мигание вокруг полуночи, которое ищут неделями.</li>
<li>Проверка «дата примерно сейчас» через сравнение с <code>Date.now()</code> в утверждении: гонка в пределах миллисекунд.</li>
<li>Фейковые таймеры включены глобально ради даты — половина набора зависает на ожиданиях.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Почему не мокать <code>Date</code> напрямую?</strong> Мок глобального конструктора ломает библиотеки, которые с ним работают, и протекает между тестами. <strong>Что делать с UUID?</strong> Внедрять фабрику идентификаторов — заодно тесты становятся читаемее.</p>`,
    code: `// хрупко: недетерминизм внутри, тест ловит границу суток случайно
export function makeTicket(text) {
  return { id: Math.random().toString(36).slice(2),
           createdAt: new Date().toISOString(), text };
}

// устойчиво: часы и генератор - явные зависимости
export function makeTicket(text, deps = { now: () => new Date(), newId }) {
  return { id: deps.newId(), createdAt: deps.now().toISOString(), text };
}

test('фиксирует момент создания и идентификатор', () => {
  let n = 0;
  const t = makeTicket('Не грузится отчёт', {
    now: () => new Date('2026-02-29T23:59:59Z'),   // високосный день, край суток
    newId: () => 'id-' + ++n,
  });
  expect(t).toEqual({ id: 'id-1', createdAt: '2026-02-29T23:59:59.000Z',
                      text: 'Не грузится отчёт' });
});

// там, где пробросить нельзя - системное время целиком
jest.useFakeTimers().setSystemTime(new Date('2026-02-29T23:59:59Z'));`,
    tip: 'Разложи дату на три разные проблемы: текущий момент, часовой пояс с локалью и арифметика календаря. Первая лечится setSystemTime, вторая - фиксацией TZ в конфиге раннера, третья вообще не требует моков, только таблицу граничных дат.' },

  { id: 'tst25',
    q: 'Бэкенд переименовал поле в ответе, все твои тесты зелёные, прод лежит. Как это предотвратить?',
    a: `<h4>Коротко</h4>
<p>Никакой мок этого не поймает по определению: он написан мной и повторяет моё представление об API. Нужен источник правды, общий с бэкендом. Минимум — типы и моки, сгенерированные из схемы, и падающая сборка при их расхождении. Максимум — контрактные тесты, где потребитель публикует ожидания, а поставщик проверяет их у себя в CI.</p>

<h4>Как это работает</h4>
<p>Первый уровень: генерация. Из OpenAPI или GraphQL-схемы генерируются типы и клиент, генерация запускается в CI на каждой сборке. Бэкенд переименовал поле — схема поменялась — типы поменялись — код перестал компилироваться. Это ловит переименования и изменения формы, но не ловит семантику: поле осталось строкой, а формат внутри стал другим.</p>
<p>Второй уровень: проверка ответов на границе в рантайме. Схема на zod или valibot разбирает ответ перед тем, как отдать его приложению. В тестах ту же схему прогоняю по фикстурам, поэтому устаревшая фикстура падает сразу. В проде расхождение превращается во внятную ошибку с именем поля, а не в <code>undefined</code>, который всплывёт через три экрана.</p>
<p>Третий уровень: контрактные тесты. Потребитель описывает, какие запросы он шлёт и какие ответы понимает; из этого собирается контракт и публикуется в брокер. Поставщик в своём CI проигрывает контракт против настоящего сервиса. Ломающее изменение краснеет у бэкенда до релиза, а не у нас в проде.</p>

<h4>Почему так</h4>
<p>Корень проблемы в том, что мок — это копия знания, а не само знание. Любая копия расходится с оригиналом со временем. Контракт решает это тем, что делает знание общим и проверяемым с обеих сторон. Платим инфраструктурой: брокер, дисциплина версий, договорённость с командой бэкенда. Поэтому на маленьком проекте я обхожусь схемой на границе, а контрактные тесты завожу там, где команд несколько и релизы независимые.</p>

<h4>Подводные камни</h4>
<ul>
<li>Схема сгенерирована один раз и лежит в репозитории руками — расхождение обнаружится через полгода.</li>
<li>Проверка ответа схемой только в разработке: в проде отключили ради скорости и потеряли сигнал.</li>
<li>Контракт описывает всё подряд, включая необязательные поля, — бэкенд не может ничего менять.</li>
<li>Фикстуры для MSW пишутся руками вместо генерации из схемы.</li>
<li>Считают, что типы TypeScript что-то гарантируют в рантайме: с сервера приходит что угодно.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Не дорого ли разбирать каждый ответ схемой?</strong> На типичных ответах это доли миллисекунды, и стоит того. <strong>Как быть с GraphQL?</strong> Там схема уже есть, достаточно генерации типов и проверки запросов против неё в CI.</p>`,
    code: `// не спасает: фикстура повторяет моё представление об API
server.use(http.get('/api/user', () =>
  HttpResponse.json({ firstName: 'Ирина', createdAt: '2026-01-01' })));
// бэкенд переименовал firstName в name - фикстура старая, тесты зелёные

// уровень 2: схема на границе, одна и та же в проде и в тестах
export const User = z.object({
  name: z.string(),
  createdAt: z.string().datetime(),
});

export async function getUser(id) {
  const res = await fetch('/api/users/' + id);
  if (!res.ok) throw new ApiError('HTTP ' + res.status);
  return User.parse(await res.json());        // расхождение падает здесь, с именем поля
}

// тест ловит устаревшую фикстуру до того, как это сделает прод
test('фикстуры соответствуют схеме ответа', () => {
  expect(() => User.parse(userFixture)).not.toThrow();
});`,
    tip: 'Сформулируй корень проблемы: мок - это копия знания об API, а копии расходятся с оригиналом. Дальше разложи защиту по уровням - генерация типов из схемы, проверка ответа схемой на границе в рантайме, контрактные тесты - и скажи, где проходит граница окупаемости каждого.' },

  { id: 'tst26',
    q: 'Сколько e2e-тестов у вас в проекте и что именно туда попадает?',
    a: `<h4>Коротко</h4>
<p>Десятки, не сотни. В e2e попадает то, что нельзя проверить дешевле: сквозные сценарии через несколько страниц и настоящий бэкенд, работа с реальным браузером — загрузка файла, печать, буфер обмена, несколько вкладок, — и денежные пути, где цена ошибки максимальна. Всё, что можно проверить в jsdom, туда не попадает.</p>

<h4>Как это работает</h4>
<p>Отбор веду по критерию «что сломается, если оно сломается». Регистрация, вход, оформление заказа, оплата, восстановление доступа — по одному сценарию на каждый, счастливый путь плюс одна главная развилка. Валидацию форм, тексты ошибок, состояния кнопок в e2e не тащу: это стоит секунды в jsdom и минуты в браузере, а ловит одно и то же.</p>
<p>Данные — отдельная тема, и обычно именно она делает набор нестабильным. Каждый тест создаёт своё состояние через API, а не через интерфейс: пользователь заводится запросом, корзина наполняется запросом, и только проверяемый шаг проходит кликами. Это на порядок быстрее и убирает зависимость между тестами. Общий аккаунт на весь набор — гарантированное мигание при параллельном прогоне.</p>
<p>Ожидания — только по состоянию интерфейса: видимый элемент, конкретный текст, завершённый запрос. Никаких пауз по времени. И селекторы по ролям и подписям, а не по классам, иначе смена вёрстки красит весь набор разом.</p>

<h4>Почему так</h4>
<p>E2E даёт самую высокую уверенность и стоит дороже всех: время прогона, инфраструктура, нестабильность, время на разбор падений. Поэтому его объём ограничен не «сколько нужно», а «сколько выдержит цикл разработки». Практический порог у меня — весь набор укладывается в десять-пятнадцать минут при разумном параллелизме, иначе его начинают пропускать, и он превращается в декорацию.</p>

<h4>Подводные камни</h4>
<ul>
<li>Тесты зависят от порядка: первый создаёт данные, второй их использует. Параллельный прогон ломает всё.</li>
<li>Состояние готовят через интерфейс: десять кликов подготовки на один клик проверки.</li>
<li>Ожидание паузой вместо ожидания состояния — главный источник мигания.</li>
<li>Тестовое окружение делят с ручным тестированием: кто-то поменял данные, набор покраснел.</li>
<li>E2E пишут вместо интеграционных, потому что «надёжнее»: прогон растёт до часа.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Как поступаете с внешними системами?</strong> Платёжный шлюз в песочнице или стаб на уровне сети; в проверку на проде выносится дымовой тест. <strong>Гоняете на каждый пулл-реквест?</strong> Критичный набор на каждый, полный — ночью.</p>`,
    code: `// плохо: подготовка кликами, ожидание паузой, зависимость от порядка
await page.goto('/register');
await page.fill('#email', 'test@test.ru');       // и ещё девять полей
await page.waitForTimeout(2000);                 // источник мигания
await page.click('.btn-primary');

// хорошо: состояние через API, ожидание по состоянию интерфейса
test('оформление заказа с оплатой картой', async ({ page, request }) => {
  const user = await request.post('/test-api/users', {
    data: { email: uniqueEmail(), cart: [{ sku: 'A-1', qty: 2 }] },
  }).then(r => r.json());

  await loginAs(page, user);                     // через cookie, не через форму
  await page.goto('/checkout');

  await page.getByRole('button', { name: 'Оплатить' }).click();
  await expect(page.getByRole('heading', { name: 'Заказ оформлен' })).toBeVisible();
  await expect(page.getByText('Номер заказа')).toContainText(/\\d{6}/);
});`,
    tip: 'Дай числовой критерий объёма: набор e2e должен укладываться в десять-пятнадцать минут при разумном параллелизме, иначе его начинают пропускать. И скажи, что состояние готовится через API, а кликами проходится только проверяемый шаг - это убирает и медленность, и половину мигания.' },

  { id: 'tst27',
    q: 'Playwright или Cypress? Только по существу, без «Playwright новее».',
    a: `<h4>Коротко</h4>
<p>Ключевая разница в архитектуре: Cypress выполняет тест внутри браузера, в той же вкладке, что приложение; Playwright управляет браузером снаружи по протоколу отладки. Отсюда всё остальное — вкладки, домены, параллелизм, работа с сетью. Для нового проекта я беру Playwright; на существующем большом наборе Cypress миграция редко окупается.</p>

<h4>Как это работает</h4>
<p>Тест Cypress — это код в браузере, что даёт удобную отладку и доступ к приложению изнутри, но накладывает ограничения: одна вкладка, сложности с несколькими источниками, своя модель асинхронности через цепочку команд, где нельзя просто написать <code>await</code>. Параллелизм исторически платный через их сервис.</p>
<p>Playwright работает снаружи и умеет то, что снаружи естественно: несколько вкладок и контекстов, разные домены, изолированные сессии в одном прогоне, три движка — Chromium, Firefox, WebKit. Параллелизм встроенный и бесплатный, включая шардирование по машинам в CI.</p>
<p>Практически ощутимые вещи — три. Первое: автоожидание. В Playwright оно проверяет пригодность элемента к действию — присоединён, видим, стабилен по позиции, принимает события, не отключён — и только тогда кликает. Это убирает большой класс мигания. Второе: перехват сети через <code>page.route</code> на уровне протокола, что работает и для сторонних доменов. Третье: трассировка — запись с DOM-снимками на каждый шаг, сетью и консолью, которая открывается как машина времени. Разбор упавшего в CI теста занимает минуты, а не полдня.</p>
<p>Ещё разница в изоляции: в Playwright каждый тест получает свежий контекст с чистыми cookie и хранилищем, и это не опция, а модель по умолчанию.</p>

<h4>Почему так</h4>
<p>Обе архитектуры — компромиссы. Внутри браузера удобнее видеть приложение, снаружи — больше контроля над самим браузером. Для e2e важнее второе, потому что типичные проблемы набора лежат в контроле окружения: изоляция, параллельность, сеть, отладка постфактум.</p>

<h4>Подводные камни</h4>
<ul>
<li>Ждут, что WebKit в Playwright равен Safari: движок тот же, но обвязка операционной системы — нет.</li>
<li>Автоожидание считают серебряной пулей: если приложение показывает старые данные, тест пройдёт по устаревшему экрану.</li>
<li>Не включают трассировку на повторный прогон и разбирают падения по скриншоту.</li>
<li>Переносят набор с Cypress механически: асинхронная модель другая, тесты выходят кривые.</li>
<li>Выбор инструмента обсуждают дольше, чем стабилизацию существующего набора.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Что даёт trace viewer?</strong> Снимки DOM по шагам с сетью и консолью — разбор падения без воспроизведения. <strong>Компонентные тесты?</strong> Есть у обоих, в браузере, и они закрывают дыру jsdom с версткой.</p>`,
    code: `// Cypress: код исполняется в браузере, своя модель цепочек
cy.get('[data-cy=submit]').click();
cy.contains('Заказ оформлен').should('be.visible');
// одна вкладка, один источник; await в привычном виде не применить

// Playwright: управление снаружи, обычный async/await
const context = await browser.newContext({ storageState: 'admin.json' });
const page = await context.newPage();

// перехват сети работает и для сторонних доменов
await page.route('**/payments.example.com/**', route =>
  route.fulfill({ status: 200, json: { status: 'approved' } }));

// автоожидание проверяет пригодность элемента, а не просто наличие
await page.getByRole('button', { name: 'Оплатить' }).click();
await expect(page.getByRole('heading', { name: 'Заказ оформлен' })).toBeVisible();

// вторая вкладка и второй пользователь в том же тесте
const admin = await browser.newContext({ storageState: 'manager.json' });
await admin.newPage().then(p => p.goto('/admin/orders'));`,
    tip: 'Начни с архитектурного различия - тест внутри браузера против управления снаружи по протоколу - и выведи из него всё остальное: вкладки, домены, изоляцию, параллелизм. И назови trace viewer конкретной причиной выбора: разбор упавшего в CI теста по снимкам DOM занимает минуты вместо полудня.' },

  { id: 'tst28',
    q: 'Когда визуальная регрессия окупается, а когда превращается в шум?',
    a: `<h4>Коротко</h4>
<p>Окупается на дизайн-системе и на небольшом наборе ключевых экранов: там визуальный результат и есть продукт, а изменений в нём мало. Превращается в шум на живых страницах с настоящими данными: каждый второй прогон краснеет из-за другого аватара, другой даты или подгрузившегося шрифта, и снимки начинают обновлять не глядя.</p>

<h4>Как это работает</h4>
<p>Инструмент делает снимок, сравнивает с эталоном попиксельно или с учётом восприятия и показывает разницу. Всё держится на детерминизме рендера. Источников недетерминизма ровно пять, и все они лечатся: динамические данные — фиксировать; время и даты — подменять; анимации и переходы — отключать через стили; шрифты — ждать <code>document.fonts.ready</code> или вшивать локально, потому что подмена шрифта сдвигает всё; окружение рендера — прогонять в одинаковом контейнере, потому что сглаживание отличается между операционными системами.</p>
<p>Практически я строю набор от компонентов, а не от страниц. Снимки на компоненты дизайн-системы во всех состояниях — обычное, наведение, фокус, ошибка, отключено, длинный текст, — плюс несколько ключевых страниц в двух-трёх ширинах и в тёмной теме. Данные для снимков фиксированные, из фикстур.</p>
<p>Порог сравнения ставлю не нулевой, но маленький: доли процента, чтобы пережить сглаживание, но заметить сдвиг на пиксель. И обязательно ревью различий человеком — визуальный тест не отвечает на вопрос «баг ли это», он отвечает «изменилось».</p>

<h4>Почему так</h4>
<p>Визуальная регрессия — единственный способ поймать целый класс поломок, невидимых для DOM-тестов: съехавшая сетка, наложение элементов, пропавший отступ после обновления библиотеки, сломанная тёмная тема. DOM при этом абсолютно валиден, и обычный тест зелёный. Цена — хранилище снимков, время прогона, ревью различий на каждый пулл-реквест и обязательная дисциплина в обновлении эталонов.</p>

<h4>Подводные камни</h4>
<ul>
<li>Обновление эталонов одной командой без просмотра — регрессия въезжает вместе с ожидаемым изменением.</li>
<li>Снимки на всю страницу целиком: одно изменение в шапке красит сорок снимков.</li>
<li>Локальные снимки на macOS против CI на Linux — различия из-за сглаживания шрифтов.</li>
<li>Не отключены анимации: снимок ловит случайный кадр перехода.</li>
<li>Реальные данные вместо фикстур — вечное мигание на именах и датах.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Сколько снимков нормально?</strong> Столько, сколько команда готова просматривать: сотни на дизайн-систему, единицы на страницы. <strong>Чем это отличается от снапшотов разметки?</strong> Снапшот проверяет DOM, визуальный тест — результат рендера, включая CSS.</p>`,
    code: `// шумно: живая страница с реальными данными и анимациями
await page.goto('/dashboard');
await expect(page).toHaveScreenshot();   // краснеет от даты, аватара, спиннера

// полезно: компонент дизайн-системы в фиксированных состояниях
test.beforeEach(async ({ page }) => {
  await page.emulateMedia({ reducedMotion: 'reduce' });
  await page.addStyleTag({ content: '*,*::before,*::after{' +
    'animation:none!important;transition:none!important;caret-color:transparent}' });
  await page.clock.setFixedTime(new Date('2026-01-15T12:00:00Z'));
});

for (const state of ['default', 'hover', 'focus', 'error', 'disabled']) {
  test('Button / ' + state, async ({ page }) => {
    await page.goto('/iframe.html?id=button--' + state);
    await page.evaluate(() => document.fonts.ready);   // иначе поймаем запасной шрифт
    await expect(page.getByRole('button')).toHaveScreenshot(state + '.png', {
      maxDiffPixelRatio: 0.002,
    });
  });
}`,
    tip: 'Перечисли пять источников недетерминизма - данные, время, анимации, шрифты, окружение рендера - и скажи, что визуальный тест окупается только после того, как все пять зафиксированы. И добавь ключевое: инструмент отвечает на вопрос «изменилось», а «баг ли это» решает человек на ревью различий.' },

  { id: 'tst29',
    q: 'Как ты проверяешь доступность в CI и что такой проверкой поймать нельзя?',
    a: `<h4>Коротко</h4>
<p>Ставлю автоматическую проверку правил в трёх точках: линтер по разметке в редакторе, проверка компонента в тестах, проверка страницы в e2e. Ловит это примерно треть реальных проблем — контраст, отсутствующие имена, неверные роли, порядок заголовков. Остальное — логика фокуса, осмысленность подписей, работа со скринридером — проверяется руками.</p>

<h4>Как это работает</h4>
<p>Основа всех инструментов одна — движок axe с набором правил. В юнит-тестах подключается через <code>jest-axe</code> и прогоняется по отрисованному DOM компонента, в Playwright — через <code>axe-core</code> по готовой странице. Правила проверяют то, что формализуемо: у интерактивного элемента есть доступное имя, у изображения есть альтернатива, контраст текста не ниже 4.5 к 1 для обычного размера и 3 к 1 для крупного, роли не противоречат вложенности, поле связано с подписью.</p>
<p>Из практики важнее не разовый прогон, а порог. Ставлю проверку на новые компоненты и падение сборки на серьёзных нарушениях, а существующие проблемы фиксирую списком исключений с задачами. Иначе включение проверки на старом проекте даёт триста ошибок разом, и её выключают в тот же день.</p>
<p>Отдельно проверяю клавиатуру, потому что автоматика этого не умеет: обход всех интерактивных элементов табом, ловушка фокуса в модалке, возврат фокуса на кнопку-открыватель при закрытии, работа Escape. Это обычный тест на <code>userEvent.tab()</code>, и он ловит больше настоящих проблем, чем движок правил.</p>

<h4>Почему так</h4>
<p>Автоматика проверяет наличие атрибутов, а не смысл. Кнопка с именем «Кнопка 3» пройдёт все правила и будет бесполезна для незрячего пользователя. Логичный порядок обхода, понятность подписи, уместность объявления об изменении — всё это требует человека. Поэтому автоматическая проверка — это фильтр от очевидного мусора, освобождающий время ручной проверки для содержательного.</p>

<h4>Подводные камни</h4>
<ul>
<li>Проверка компонента в изоляции не видит контраста: цвета приходят от темы страницы.</li>
<li>Прогон только на главной странице — модалки и формы, где проблемы и живут, не проверяются.</li>
<li>Забывают проверить состояния: ошибка формы, открытое меню, загрузка.</li>
<li>Считают, что зелёный axe означает доступный интерфейс, и не проверяют клавиатуру.</li>
<li>Скрывают проблемы через <code>aria-hidden</code> вместо исправления.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Какая доля проблем ловится автоматически?</strong> По разным оценкам треть-половина, остальное руками. <strong>Чем проверять вручную?</strong> Клавиатура, масштаб 200 процентов, VoiceOver или NVDA на ключевых сценариях.</p>`,
    code: `// уровень 1: компонент в тестах
test('карточка товара не нарушает правила доступности', async () => {
  const { container } = render(<ProductCard product={product} />);
  expect(await axe(container)).toHaveNoViolations();
});

// уровень 2: страница целиком, включая состояния
test('форма заказа доступна в состоянии ошибки', async ({ page }) => {
  await page.goto('/checkout');
  await page.getByRole('button', { name: 'Оплатить' }).click();   // вызвали ошибки

  const results = await new AxeBuilder({ page })
    .withTags(['wcag2a', 'wcag2aa'])
    .analyze();
  expect(results.violations).toEqual([]);
});

// уровень 3: то, что axe не проверит - фокус и клавиатура
test('модалка удерживает фокус и возвращает его при закрытии', async () => {
  const user = userEvent.setup();
  render(<DeleteDialog />);
  const opener = screen.getByRole('button', { name: 'Удалить' });

  await user.click(opener);
  await user.tab(); await user.tab(); await user.tab();
  expect(screen.getByRole('dialog')).toContainElement(document.activeElement);

  await user.keyboard('{Escape}');
  expect(opener).toHaveFocus();
});`,
    tip: 'Назови честную цифру: автоматика ловит примерно треть проблем, потому что проверяет наличие атрибутов, а не смысл - имя «Кнопка 3» пройдёт все правила. И расскажи про порог на старом проекте: правила включаются на новый код, старые нарушения уходят в список исключений с задачами, иначе проверку выключат в первый же день.' },

  { id: 'tst30',
    q: 'Как тестировать производительность фронтенда, чтобы это не было театром?',
    a: `<h4>Коротко</h4>
<p>Театр начинается там, где меряют один прогон в CI и радуются цифре. Настоящая проверка состоит из трёх слоёв: жёсткие бюджеты на размер бандла в сборке, лабораторные замеры на фиксированном окружении с медианой по нескольким прогонам, и полевые данные с реальных пользователей — последние и есть истина.</p>

<h4>Как это работает</h4>
<p>Бюджет размера — самый дешёвый и самый полезный слой. Порог на размер точки входа и на общий вес после сжатия, проверка на каждом пулл-реквесте, сборка падает при превышении. Ловит самое частое: кто-то импортировал целую библиотеку иконок ради одной. Здесь же полезно сравнение состава бандла с базовой веткой.</p>
<p>Лабораторные замеры дают воспроизводимость, но не реальность. Числа зависят от машины, поэтому в CI сравнивать абсолютные значения бессмысленно: нужен один и тот же профиль контейнера, эмуляция медленного процессора и сети, три-пять прогонов и медиана, а сравнение — с базовой веткой в том же прогоне, а не с историческим порогом.</p>
<p>Полевые данные собираю через <code>web-vitals</code>: LCP, INP, CLS с реальных сессий, разбитые по перцентилям и типу устройства. Ориентир — 75-й перцентиль: LCP до 2.5 секунды, INP до 200 миллисекунд, CLS до 0.1. Средние значения не использую вообще, они скрывают хвост, где и живут страдающие пользователи.</p>
<p>Отдельный класс — тесты на конкретный горячий сценарий: измерить время реакции на ввод в большой таблице при десяти тысячах строк и зафиксировать порог. Такой тест точечный и стабильный, в отличие от общей метрики страницы.</p>

<h4>Почему так</h4>
<p>Производительность — распределение, а не число, и оно определяется устройствами пользователей, а не мощностью раннера. Поэтому CI отвечает на вопрос «стало ли хуже относительно базовой ветки», а на вопрос «достаточно ли быстро» отвечают только полевые данные.</p>

<h4>Подводные камни</h4>
<ul>
<li>Один прогон Lighthouse в CI: разброс между запусками больше, чем эффект оптимизации.</li>
<li>Меряют на пустых данных, а в проде список на две тысячи элементов.</li>
<li>Бюджет только на общий вес: точка входа растёт, а общий вес держат ленивыми чанками.</li>
<li>Смотрят на среднее вместо перцентилей — регрессия на слабых устройствах не видна.</li>
<li>Оптимизируют оценку Lighthouse вместо метрик, которые чувствует пользователь.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>Почему INP заменил FID?</strong> FID мерил только задержку первого отклика, INP — полную длительность взаимодействия до перерисовки. <strong>Как ловить регрессии точечно?</strong> Замер конкретного сценария с порогом, а не общая оценка страницы.</p>`,
    code: `// слой 1: бюджет размера - падает сборка, а не отчёт
// package.json
{
  "bundlesize": [
    { "path": "dist/assets/index-*.js", "maxSize": "180 kB", "compression": "brotli" },
    { "path": "dist/assets/vendor-*.js", "maxSize": "220 kB", "compression": "brotli" }
  ]
}

// слой 2: точечный замер горячего сценария, медиана из пяти прогонов
test('ввод в фильтр таблицы на 10 000 строк отвечает быстрее 100 мс', async ({ page }) => {
  await page.goto('/orders?seed=10000');
  await page.emulateCPUThrottling?.(4);

  const samples = [];
  for (let i = 0; i < 5; i++) {
    const t = await page.evaluate(async () => {
      const input = document.querySelector('input[type=search]');
      const start = performance.now();
      input.value = 'ирина';
      input.dispatchEvent(new Event('input', { bubbles: true }));
      await new Promise(r => requestAnimationFrame(() => requestAnimationFrame(r)));
      return performance.now() - start;
    });
    samples.push(t);
  }
  samples.sort((a, b) => a - b);
  expect(samples[2]).toBeLessThan(100);       // медиана, не единичный прогон
});`,
    tip: 'Раздели лабораторию и поле: CI отвечает на вопрос «стало ли хуже относительно базовой ветки», а «достаточно ли быстро» отвечают только полевые данные по 75-му перцентилю. Назови пороги - LCP 2.5 с, INP 200 мс, CLS 0.1 - и скажи, что средние значения не используешь вообще, они прячут хвост распределения.' },
];
