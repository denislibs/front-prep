const DECK_WEB_EXTRA = [
  { id: 'wx1',
    q: 'Опишите критический путь рендеринга детально. Что его блокирует?',
    a: `<h4>Коротко</h4>
    <p>Критический путь — цепочка HTML → DOM, CSS → CSSOM, render tree, layout, paint, composite. Блокируют его два класса ресурсов: весь подходящий по <code>media</code> CSS блокирует построение render tree, а синхронный <code>&lt;script&gt;</code> блокирует парсер HTML и сам ждёт применения предшествующих стилей. Всё остальное — производные от этих двух фактов.</p>

    <h4>Как это работает</h4>
    <p>Байты приходят чанками, парсер инкрементальный: DOM строится по мере поступления, и первый рендер возможен ещё до <code>DOMContentLoaded</code>. Параллельно основному парсеру работает <strong>preload scanner</strong> — облегчённый сканер, который читает сырой HTML дальше по документу и ставит в очередь найденные <code>src</code> и <code>href</code>, даже пока основной парсер стоит на скрипте. CSS складывается в CSSOM; пока не разобран последний блокирующий стиль, render tree построить нельзя — вычисленные стили ещё могут измениться, и браузер не хочет рисовать дважды.</p>
    <p>Render tree содержит только видимые узлы: <code>display: none</code> выпадает целиком, <code>visibility: hidden</code> остаётся и занимает место. Layout (reflow) считает геометрию каждого бокса, paint раскладывает всё в списки команд рисования, composite складывает слои — эту стадию обычно выполняет GPU, поэтому анимации <code>transform</code> и <code>opacity</code> не требуют layout и paint.</p>
    <p>Синхронный скрипт останавливает парсер, потому что может вызвать <code>document.write</code> и переписать поток символов. <code>defer</code> качается параллельно и выполняется после парсинга в порядке объявления, <code>async</code> — сразу по готовности и без гарантии порядка, <code>type="module"</code> отложен по умолчанию.</p>

    <h4>Почему так</h4>
    <p>Блокировка на CSS — осознанный размен: без неё пользователь увидел бы нестилизованный документ (FOUC), а это воспринимается как поломка, а не как быстрая загрузка. Блокировка на скриптах — наследие <code>document.write</code>: спецификация обязана гарантировать, что скрипт видит DOM ровно до своего тега, ни байтом больше. Preload scanner придуман как компенсация: он вынимает сеть из-под этих замков и оставляет заблокированным только CPU, поэтому загрузка ресурсов идёт даже во время «стоп-кадра» парсера.</p>

    <h4>Подводные камни</h4>
    <ul><li>Изображение, вставленное из JS или через CSS <code>background</code>, для preload scanner невидимо: запрос уйдёт только после построения CSSOM и layout. Это самая частая причина LCP в 4+ секунды на быстром канале.</li>
    <li>Некритичный CSS подключают как <code>media="print"</code> и переключают в <code>all</code> по <code>onload</code> — тогда он скачивается, но не задерживает рендер.</li>
    <li>Шрифт с <code>font-display: block</code> даёт FOIT до 3 секунд: контейнер отрисован, текста нет, LCP-кандидат не засчитывается.</li>
    <li>Инлайновый скрипт на сотни килобайт блокирует парсер вообще без сетевого запроса — в Network это не видно, только в Performance как длинный жёлтый Evaluate Script.</li>
    <li>Синхронный XHR и <code>@import</code> внутри CSS создают последовательную цепочку, которую сканер тоже не может распараллелить.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>CSS блокирует выполнение JS?</strong> Косвенно да: синхронный скрипт не стартует, пока не применены предшествующие стили, потому что ему может понадобиться <code>getComputedStyle</code>. <strong>Что такое <code>blocking="render"</code>?</strong> Явное указание задержать первый рендер до загрузки конкретного ресурса — применяют для инлайн-модулей и критичных полифиллов, когда без них страница мигнёт. <strong>Как увидеть путь в DevTools?</strong> В Performance дорожки Parse HTML, Recalculate Style, Layout и Paint показывают порядок стадий, а в Network колонка Initiator — кто и когда обнаружил каждый ресурс; расхождение между началом парсинга и стартом запроса картинки и есть цена позднего обнаружения.</p>`,
    code: `<link rel="stylesheet" href="/print.css" media="print" onload="this.media='all'">
<script src="/app.js" defer></script>`,
    tip: 'Упоминание preload scanner и того, что он не видит ресурсы из JS/CSS, — сильный ответ про LCP-изображения.' },

  { id: 'wx2',
    q: 'Чем отличаются preload, prefetch, preconnect, dns-prefetch и modulepreload?',
    snippet: `<link rel="dns-prefetch" href="https://cdn.site.ru">
<link rel="preconnect" href="https://cdn.site.ru" crossorigin>
<link rel="preload" as="font" type="font/woff2" href="/inter.woff2" crossorigin>
<link rel="modulepreload" href="/assets/route-checkout.js">
<link rel="prefetch" href="/checkout" as="document">`,
    a: `<h4>Коротко</h4>
    <p>Это resource hints, и различаются они двумя вещами: <strong>сколько работы</strong> браузер делает заранее и <strong>для какой навигации</strong>. <code>dns-prefetch</code> и <code>preconnect</code> готовят соединение, <code>preload</code> и <code>modulepreload</code> качают ресурс для текущей страницы, <code>prefetch</code> — для следующей.</p>

    <h4>Как это работает</h4>
    <ul><li><code>dns-prefetch</code> — только DNS-резолв. Стоит копейки, работает даже в старых браузерах, экономит 20–120 мс. Подходит для доменов, которые понадобятся <strong>возможно</strong>.</li>
    <li><code>preconnect</code> — DNS + TCP + TLS. На мобильной сети это 100–300 мс, иногда больше. Но открытое соединение занимает ресурсы и на клиенте, и на сервере, и браузер закроет его секунд через 10, если им не воспользовались; больше 2–4 preconnect обычно вредят.</li>
    <li><code>preload</code> — настоящая загрузка с высоким приоритетом для <strong>текущего</strong> документа. Обязателен <code>as</code>: без него браузер не знает ни приоритета, ни CORS-режима, ни <code>Accept</code>, и часто качает ресурс второй раз.</li>
    <li><code>modulepreload</code> — preload для ES-модуля: файл не только скачивается, но и <strong>парсится, компилируется и разворачивается граф зависимостей</strong> с рекурсивной подгрузкой импортов. Обычный <code>preload as="script"</code> кладёт файл в кеш как байты и графа не строит.</li>
    <li><code>prefetch</code> — загрузка с самым низким приоритетом в HTTP-кеш ради <strong>будущей</strong> навигации. Стартует, когда сеть свободна, и в HTTP-кеше живёт минимум 5 минут даже без заголовков кеширования.</li></ul>

    <h4>Почему так</h4>
    <p>Спецификация разделила подсказки по стоимости сознательно: браузер не может доверять разработчику неограниченно, поэтому дал градацию от «почти бесплатно» до «трачу полосу прямо сейчас». Полоса — ресурс с нулевой суммой: каждый preload не добавляет скорости, а <strong>перераспределяет</strong> её, отнимая у чего-то другого. Отсюда правило: hints работают, пока их мало.</p>

    <h4>Подводные камни</h4>
    <ul><li>Забытый <code>crossorigin</code> у <code>preload</code> шрифта — классика: шрифты всегда запрашиваются в режиме CORS, и без атрибута предзагруженный ответ не подойдёт под запрос из <code>@font-face</code>. Получаем две загрузки одного файла и предупреждение в консоли.</li>
    <li>Тот же <code>crossorigin</code> нужен и <code>preconnect</code> к домену шрифтов, иначе откроется второе соединение под CORS-запрос и вся экономия пропадёт.</li>
    <li>Неиспользованный <code>preload</code> через несколько секунд даёт предупреждение «was preloaded but not used» — верный признак, что кто-то скопировал теги из чужого проекта.</li>
    <li>Массовый <code>prefetch</code> на мобильном трафике — реальные деньги пользователя. Проверяйте <code>navigator.connection.saveData</code> и <code>effectiveType</code>.</li>
    <li><code>preload</code> LCP-картинки без <code>fetchpriority="high"</code> помогает меньше, чем ожидают: приоритет остаётся средним.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем preload отличается от Early Hints?</strong> <code>103 Early Hints</code> отдаётся сервером до основного ответа, поэтому загрузка стартует ещё во время генерации HTML — выигрыш равен времени работы бэкенда. <strong>Что заменяет prefetch сегодня?</strong> Speculation Rules: там браузер сам решает, когда и сколько спекулировать, с учётом памяти и Save-Data. <strong>Почему hint не сработал?</strong> Три типовые причины: ресурс уже был обнаружен парсером раньше подсказки (тег стоит ниже по документу), несовпадение режима CORS между hint и реальным запросом, либо промах по типу — <code>as="script"</code> для модуля вместо <code>modulepreload</code>. Проверяется сравнением двух строк в Network с одинаковым URL и разными Initiator.</p>`,
    code: `<link rel="preload" as="image" href="/hero.avif" fetchpriority="high">`,
    tip: 'Забытый crossorigin у preload шрифта — классическая ошибка, приводящая к двойной загрузке. Назовите её.' },

  { id: 'wx3',
    q: 'Как браузер расставляет приоритеты загрузки и что делает fetchpriority?',
    a: `<h4>Коротко</h4>
    <p>У каждого запроса есть внутренний приоритет — Highest, High, Medium, Low, Lowest. Он определяет и порядок отправки, и распределение полосы в HTTP/2. <code>fetchpriority</code> — это <strong>подсказка</strong>, сдвигающая приоритет вверх или вниз; браузер вправе её проигнорировать.</p>

    <h4>Как это работает</h4>
    <p>Эвристики Chromium примерно такие: HTML и блокирующий CSS — Highest; шрифты, обнаруженные из CSS, — High; синхронный скрипт в <code>&lt;head&gt;</code> — High; <code>defer</code> и <code>async</code>-скрипты — Low; <code>fetch</code> и XHR — High; <code>preload</code> наследует приоритет по <code>as</code>; <code>prefetch</code> — Lowest.</p>
    <p>Отдельная история у изображений. До layout браузер не знает, попадает ли картинка во вьюпорт, поэтому <strong>все изображения стартуют с Low</strong>. После первого layout те, что видимы в начальном экране, повышаются до High — но повышение происходит уже после разбора CSS, а первые пять картинок в разметке к этому моменту могли уйти в очередь позади скриптов. Именно этот разрыв и лечит <code>fetchpriority="high"</code> на герой-изображении: приоритет ставится сразу при обнаружении тега preload scanner-ом.</p>
    <p>Атрибут принимает <code>high</code>, <code>low</code>, <code>auto</code> и работает на <code>img</code>, <code>link</code>, <code>script</code>, <code>iframe</code> и в опциях <code>fetch()</code>. <code>loading="lazy"</code> держит картинку на очень низком приоритете, пока она не приблизится к вьюпорту.</p>

    <h4>Почему так</h4>
    <p>Приоритеты существуют потому, что полоса конечна, а HTTP/2 мультиплексирует всё в одном соединении: без весов сотня параллельных потоков просто поделила бы канал поровну и критичный CSS приехал бы одновременно с футерной картинкой. Браузерные эвристики покрывают типовой случай, но не знают семантики страницы — какой из десяти <code>img</code> является LCP, известно только вам. <code>fetchpriority</code> и есть способ передать это знание.</p>

    <h4>Подводные камни</h4>
    <ul><li>Антипаттерн «поднять всё»: если все ресурсы High, относительный порядок не изменился, а конкуренция за полосу выросла. Приоритеты имеют смысл только как относительные.</li>
    <li>Не менее полезно <strong>понижение</strong>: <code>fetchpriority="low"</code> на слайдах карусели ниже первого, на аналитике, на превью в оффскрине освобождает канал для LCP.</li>
    <li>CSS <code>background-image</code> нельзя пометить приоритетом вообще — ещё одна причина держать LCP в теге <code>img</code>.</li>
    <li>В HTTP/1.1 приоритетов нет: там действует лимит в 6 соединений на хост, и «повышенный» запрос просто ждёт свободного сокета.</li>
    <li>Проверять надо в колонке Priority панели Network — её нужно включить правой кнопкой по заголовку таблицы; там же видно «Initial» и итоговый приоритет.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что важнее — preload или fetchpriority?</strong> Они решают разные задачи: preload ускоряет <strong>обнаружение</strong> ресурса, fetchpriority меняет его <strong>вес</strong>. Для LCP-картинки из CSS нужны оба. <strong>Почему повышение не сработало?</strong> Чаще всего сервер отдаёт ресурс по HTTP/1.1 или запрос упёрся не в приоритет, а в очередь соединений. <strong>Как приоритеты выглядят на проводе?</strong> В HTTP/2 это дерево зависимостей с весами, которое сервер вправе игнорировать; в HTTP/3 механизм заменён на заголовок <code>Priority</code> со значениями <code>u</code> (urgency 0–7) и <code>i</code> (incremental), и его понимают не все CDN — поэтому эффект от приоритизации стоит проверять на своём проде, а не считать гарантированным.</p>`,
    code: `<img src="/hero.avif" fetchpriority="high" width="1200" height="630" alt="">
<img src="/slide-2.avif" fetchpriority="low" loading="lazy" alt="">`,
    tip: 'Объясните механику: картинка low до layout — именно поэтому fetchpriority на LCP-изображении даёт реальный выигрыш.' },

  { id: 'wx4',
    q: 'Что такое Speculation Rules API и чем он лучше ручного prefetch?',
    snippet: `<script type="speculationrules">
{
  "prerender": [
    { "where": { "href_matches": "/product/*" }, "eagerness": "moderate" }
  ],
  "prefetch": [
    { "where": { "selector_matches": ".nav a" }, "eagerness": "conservative" }
  ]
}
</script>`,
    a: `<h4>Коротко</h4>
    <p>Декларативный JSON внутри <code>&lt;script type="speculationrules"&gt;</code>, где вы описываете, какие URL стоит <strong>prefetch</strong> (скачать документ) или <strong>prerender</strong> (полностью отрендерить в скрытом процессе, включая исполнение JS). При переходе страница активируется мгновенно: LCP получается близким к нулю, а навигация выглядит как в SPA.</p>

    <h4>Как это работает</h4>
    <p>Правила бывают двух видов. <code>list</code> — явный список URL. <code>document</code> — набор условий: <code>href_matches</code> по шаблону, <code>selector_matches</code> по CSS-селектору ссылки, комбинаторы <code>and</code>/<code>or</code>/<code>not</code>. У каждого правила есть <code>eagerness</code>: <code>immediate</code> (сразу), <code>eager</code> (при малейшем намёке), <code>moderate</code> (наведение курсора примерно на 200 мс), <code>conservative</code> (нажатие кнопки мыши, ещё до отпускания).</p>
    <p>Дальше решает браузер. Chromium держит лимиты — порядка 10 одновременных prefetch и 2 prerender для не-immediate правил, отменяет спекуляции при нехватке памяти, уважает Save-Data и режим экономии батареи. Prerender поднимает полноценный скрытый рендер: выполняется JS, работают запросы, но страница помечена как <code>document.prerendering === true</code> и не считается видимой.</p>

    <h4>Почему так</h4>
    <p>Ручной <code>&lt;link rel="prefetch"&gt;</code> требовал, чтобы разработчик сам угадал момент, количество и условия — и в итоге либо спекулировали слишком мало, либо жгли чужой трафик. Speculation Rules переносит политику в браузер: вы описываете <strong>что</strong> уместно предзагрузить, а <strong>когда и сколько</strong> решает движок, у которого есть данные о памяти, сети и поведении пользователя. Это тот же сдвиг, что произошёл с <code>loading="lazy"</code>.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Двойной подсчёт аналитики</strong> — главная проблема prerender: страница реально выполняется, счётчик просмотра срабатывает, а перехода могло не быть. Лечится проверкой <code>document.prerendering</code> и событием <code>prerenderingchange</code>, до которого отправку событий надо откладывать.</li>
    <li>Побочные эффекты на старте (POST-запрос, списание лимита, отметка «прочитано») выполнятся зря — их выносят за пределы prerender.</li>
    <li>Prerender не сработает при <code>Cache-Control: no-store</code>, при конфликте с CSP, для URL, требующих другого состояния авторизации, и для не-GET навигаций.</li>
    <li>Спекуляция на кросс-доменные адреса без <code>Supports-Loading-Mode</code> заголовка не выполнится — межсайтовый prerender требует явного согласия целевого сайта.</li>
    <li>Слишком агрессивный <code>immediate</code> на каталог из ста ссылок съест мобильный трафик — начинайте с <code>moderate</code>.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как проверить, что сработало?</strong> В DevTools есть панель Application → Speculative loads: там видны статус, причина отказа и время активации. <strong>Что делать с bfcache?</strong> Они дополняют друг друга: bfcache ускоряет «назад», speculation rules — «вперёд»; в связке с cross-document view transitions это даёт MPA, неотличимую от SPA. <strong>Сколько это стоит?</strong> Prefetch документа — это лишний запрос на каждый предугаданный переход, prerender — ещё и полный рендер с исполнением JS, то есть память и CPU устройства. При точности предсказания около 50% и <code>eagerness: moderate</code> вы платите примерно одним лишним документом на переход и получаете почти нулевой LCP на половине навигаций — размен обычно выгодный, но на слабых устройствах его стоит ограничивать. <strong>Что с приватностью?</strong> Prerender отправляет запрос к странице, которую пользователь не открывал: если у вас есть аудит доступа или счётчики, они увидят обращение, которого не было, — фильтруйте по заголовку <code>Sec-Purpose: prefetch;prerender</code>.</p>`,
    code: `if (document.prerendering) {
  document.addEventListener('prerenderingchange', sendPageview, { once: true });
} else {
  sendPageview();
}`,
    tip: 'Проблема с двойным подсчётом аналитики при prerender — то, что реально ловят в проде.' },

  { id: 'wx5',
    q: 'Опишите жизненный цикл Service Worker и проблему обновления.',
    a: `<h4>Коротко</h4>
    <p>Цикл: <code>register</code> → <code>installing</code> → <code>waiting</code> → <code>activating</code> → <code>activated</code> и <code>controlling</code>. Главная боль — состояние <strong>waiting</strong>: новый воркер не активируется, пока хоть одна вкладка контролируется старым. Reload вкладки этого не решает, нужно закрыть все вкладки origin.</p>

    <h4>Как это работает</h4>
    <p>При <code>navigator.serviceWorker.register()</code> браузер скачивает файл и сравнивает его байты с установленной версией. Отличается хотя бы на байт — начинается установка. В <code>install</code> обычно кладут прекеш; если промис в <code>event.waitUntil</code> отклонён, установка проваливается целиком. Дальше воркер уходит в <code>waiting</code> и ждёт, пока освободятся все клиенты. В <code>activate</code> удаляют старые кеши по префиксу версии. Только после этого воркер начинает контролировать страницы — причём страницы, загруженные <strong>до</strong> активации, остаются без контроллера до следующей навигации, если не вызвать <code>clients.claim()</code>.</p>
    <p>Сам файл воркера браузер перепроверяет при каждой навигации и минимум раз в 24 часа, игнорируя длинный <code>max-age</code>: с 2019 года заголовки кеширования для скрипта SW ограничены сверху сутками.</p>

    <h4>Почему так</h4>
    <p>Waiting существует, чтобы две версии приложения не работали одновременно. Открытая вкладка уже загрузила старый HTML и старые чанки; если подсунуть ей новый воркер с новым precache-манифестом, динамический <code>import()</code> запросит чанк, которого на сервере уже нет. Браузер выбрал консервативный вариант — «одна версия на сессию» — и отдал разработчику ручки <code>skipWaiting</code> и <code>clients.claim</code>, чтобы он мог осознанно этот контракт нарушить.</p>

    <h4>Подводные камни</h4>
    <ul><li><code>skipWaiting()</code> в обработчике <code>install</code> без спроса — самая частая ошибка: пользователь получает ошибки динамического импорта прямо посреди работы. Правильный паттерн — тост «доступна новая версия», <code>postMessage</code> с типом SKIP_WAITING по клику и перезагрузка по событию <code>controllerchange</code> с флагом от повторного цикла.</li>
    <li>Отсутствие kill switch: битый воркер живёт у пользователей неделями. Держите ветку, умеющую <code>registration.unregister()</code> плюс очистку <code>caches.keys()</code>, и способ её включить удалённо.</li>
    <li>Кеширование самого <code>sw.js</code> надолго через CDN — обновление залипает; ставьте <code>Cache-Control: no-cache</code>.</li>
    <li>Смена <code>scope</code> или пути к файлу создаёт <strong>вторую</strong> регистрацию, а не обновляет первую.</li>
    <li>Ошибка в <code>install</code> из-за одного недоступного URL в прекеше отменяет всю установку — <code>cache.addAll()</code> атомарен.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как обновить страницы без перезагрузки?</strong> Никак безопасно: <code>clients.claim()</code> отдаст контроль, но уже исполняющийся JS останется старым. <strong>Что такое navigation preload?</strong> Механизм, при котором браузер начинает сетевой запрос навигации параллельно со стартом воркера — иначе холодный старт SW добавляет 50–200 мс к каждому переходу. <strong>Как отлаживать?</strong> Вкладка Application → Service Workers показывает текущее состояние, кнопки Update, Skip waiting и Unregister, чекбоксы «Update on reload» (переустанавливает воркер при каждой перезагрузке) и «Bypass for network» (полностью исключает SW из цепочки). Первый чекбокс экономит часы при разработке, второй — единственный быстрый способ проверить, что баг именно в воркере, а не в приложении. <strong>Сколько живёт воркер?</strong> Браузер убивает простаивающий SW через десятки секунд и поднимает заново на следующий запрос, поэтому глобальные переменные в нём — не хранилище состояния: всё, что должно пережить перезапуск, кладут в IndexedDB или Cache Storage.</p>`,
    code: `self.addEventListener('message', (e) => {
  if (e.data && e.data.type === 'SKIP_WAITING') self.skipWaiting();
});
navigator.serviceWorker.addEventListener('controllerchange', () => {
  if (!reloaded) { reloaded = true; location.reload(); }
});`,
    tip: 'Обязательно скажите про kill switch — история про залипший SW есть у всех, кто его внедрял.' },

  { id: 'wx6',
    q: 'Какие стратегии кеширования в Service Worker и когда какую применять?',
    a: `<h4>Коротко</h4>
    <p>Пять базовых стратегий: cache first, network first, stale-while-revalidate, cache only, network only. Выбор определяется одним вопросом — что дороже для конкретного ресурса, <strong>задержка или устаревание</strong>. Иммутабельная статика — cache first, HTML и API — network first, всё промежуточное — SWR.</p>

    <h4>Как это работает</h4>
    <ul><li><strong>Cache first</strong>: ищем в Cache Storage, при промахе идём в сеть и кладём ответ. Для файлов с хешем в имени это идеально — ответ гарантированно актуален, потому что при изменении меняется URL.</li>
    <li><strong>Network first</strong>: сеть с таймаутом (обычно 3–5 секунд), при неудаче — кеш. Свежесть важнее скорости: HTML, лента, баланс счёта.</li>
    <li><strong>Stale-while-revalidate</strong>: мгновенно отдаём из кеша и параллельно запускаем фоновое обновление, результат которого увидит следующий запрос. Лучший компромисс для аватарок, справочников, некритичных списков; цена — пользователь видит данные на один заход устаревшими.</li>
    <li><strong>Cache only</strong> — прекешированная оболочка приложения; <strong>network only</strong> — всё, что кешировать нельзя: аналитика, платежи, эндпоинты авторизации.</li></ul>
    <p>Технически всё это обработчики <code>fetch</code>, вызывающие <code>event.respondWith()</code> с промисом Response. Если <code>respondWith</code> не вызван, запрос уходит в сеть как обычно.</p>

    <h4>Почему так</h4>
    <p>HTTP-кеш решает ту же задачу, но управляется сервером и не умеет ни фолбэков, ни офлайна, ни логики «отдай старое, обнови в фоне» с гарантией. Service Worker даёт программируемый прокси на клиенте — за это платим сложностью и риском: любая ошибка в стратегиях кешируется у пользователей надолго.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Приватные данные в общем кеше</strong>: Cache Storage живёт на origin, а не на пользователя. На общем устройстве или после выхода из аккаунта следующий человек увидит чужие ответы. Либо разделяйте имя кеша по идентификатору сессии и чистите его при логауте, либо не кешируйте персональное вовсе.</li>
    <li>Кешировать надо только успешные ответы: проверяйте <code>response.ok</code> и <code>response.type</code>. Opaque-ответы (<code>no-cors</code>) непрозрачны по статусу — ошибка 404 запишется как валидная — и занимают в квоте с округлением вверх, иногда мегабайты вместо килобайт.</li>
    <li>Ответ можно прочитать только один раз: перед <code>cache.put</code> нужен <code>response.clone()</code>, иначе получите ошибку «body already used».</li>
    <li>Range-запросы (видео, аудио) через наивный cache first ломаются: нужно либо пропускать их в сеть, либо использовать плагин Workbox.</li>
    <li>Бесконечный рост кеша упирается в квоту origin, после чего браузер выселяет данные целиком. Нужна политика expiration по количеству и возрасту.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Писать самому или Workbox?</strong> Workbox покрывает стратегии, expiration, precache-манифест и broadcast-обновления; вручную обычно переписывают то же самое хуже. <strong>Как обрабатывать навигацию?</strong> Через navigation preload плюс app shell с фолбэком на офлайн-страницу. <strong>Как соотносятся SW-кеш и HTTP-кеш?</strong> Это два независимых слоя: <code>fetch()</code> внутри воркера сам по себе проходит через HTTP-кеш, поэтому можно случайно получить устаревший ответ из браузерного кеша и положить его в Cache Storage как «свежий». Управляется опцией <code>cache: 'no-store'</code> или <code>'reload'</code> в запросе — для прекеша это обязательно, иначе обновление залипает вдвойне. <strong>Сколько можно хранить?</strong> Квота origin в Chromium — порядка 60% свободного места на диске, но она общая для Cache Storage, IndexedDB и localStorage, и при её исчерпании браузер выселяет данные origin <strong>целиком</strong>, а не по одному ключу; <code>navigator.storage.estimate()</code> показывает текущий расход, а <code>persist()</code> запрашивает защиту от выселения.</p>`,
    code: `// SWR: отдаём кеш сразу, сеть обновляет фон
const cached = await caches.match(req);
const network = fetch(req).then((res) => {
  if (res.ok) caches.open(V).then((c) => c.put(req, res.clone()));
  return res;
});
return cached || network;`,
    tip: 'Риск утечки приватных данных через SW-кеш — тема, которую почти никто не поднимает, и она сразу запоминается.' },

  { id: 'wx7',
    q: 'Что делает веб-приложение PWA и когда это оправдано?',
    snippet: `{
  "name": "Расписание",
  "short_name": "Расписание",
  "start_url": "/?source=pwa",
  "display": "standalone",
  "background_color": "#0b0b0f",
  "icons": [
    { "src": "/i-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/i-512.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}`,
    a: `<h4>Коротко</h4>
    <p>Технический минимум — три вещи: HTTPS, web app manifest с <code>name</code>, иконками 192 и 512, <code>start_url</code> и <code>display</code>, и зарегистрированный Service Worker с обработчиком <code>fetch</code>. Это даёт установку на домашний экран, запуск без адресной строки и работу офлайн.</p>

    <h4>Как это работает</h4>
    <p>Браузер читает манифест из <code>&lt;link rel="manifest"&gt;</code> и, если критерии установки выполнены, показывает свой промпт или иконку в адресной строке. В Chromium установку нельзя вызвать программно: событие <code>beforeinstallprompt</code> надо перехватить, вызвать <code>preventDefault()</code>, сохранить и позже вызвать <code>prompt()</code> — обязательно внутри обработчика реального жеста пользователя. Установленное приложение получает отдельную запись в списке приложений ОС, свой оконный режим и в Chromium отдельный процесс.</p>
    <p>Сверх минимума открываются возможности: Web Push (на iOS — только для установленных PWA, начиная с 16.4, и только после жеста), Background Sync и Periodic Sync, Badging для счётчика на иконке, File Handling, Web Share и Web Share Target, Screen Wake Lock, App Shortcuts в контекстном меню иконки, Protocol Handlers.</p>

    <h4>Почему так</h4>
    <p>PWA — попытка отдать вебу те возможности нативных приложений, которые не требуют доверия к бинарю: установка и офлайн вместо магазина и подписи. Плата за это — модель безопасности вокруг origin и жестов пользователя, поэтому почти всё требует HTTPS и явного действия. И плата за офлайн — Service Worker, то есть слой, который может сломать продакшен надолго.</p>

    <h4>Подводные камни</h4>
    <ul><li>Ограничения iOS: жёсткая квота хранилища, удаление данных сайта после примерно 7 дней без визитов, отсутствие ряда API, установка только через Share → Add to Home Screen без промпта.</li>
    <li><code>start_url</code> должен быть в scope воркера и обязан работать офлайн — иначе установленное приложение показывает динозавра при первом же запуске без сети.</li>
    <li>Иконка без <code>purpose: maskable</code> на Android обрезается по кругу вместе с логотипом.</li>
    <li>Ошибка в SW кешируется у пользователей: без kill switch починка недоступна.</li>
    <li>Установленное приложение не показывает URL — фишинговые сценарии и потерянная навигация «назад» становятся вашей ответственностью.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Когда PWA не нужен?</strong> Лендингам, новостным статьям и всему, куда заходят раз в полгода: повторных визитов нет, а риск от SW есть. <strong>PWA против нативного?</strong> Если нужны Bluetooth, фоновое геопозиционирование, серьёзная работа с файлами или магазин как канал продаж — нативное; если это внутренний инструмент или сервис с частыми возвратами, PWA дешевле в разы. <strong>Как понять, что приложение запущено как установленное?</strong> Медиазапрос <code>display-mode: standalone</code> в CSS и <code>matchMedia</code> в JS; параметр в <code>start_url</code> позволяет отделить установленные сессии в аналитике и честно посчитать, окупилась ли установка. <strong>Как обновлять установленное приложение?</strong> Тем же механизмом Service Worker: манифест кешируется браузером отдельно, и смена иконок или имени доезжает не мгновенно — иногда только после переустановки. <strong>Что чаще всего забывают?</strong> Офлайн-страницу для навигаций и обработку состояния «сеть есть, но сервер не отвечает»: пользователь установленного приложения не видит адресной строки и не понимает, что произошло, если экран просто пустой.</p>`,
    code: `let deferred = null;
addEventListener('beforeinstallprompt', (e) => { e.preventDefault(); deferred = e; showButton(); });
button.onclick = async () => { deferred.prompt(); await deferred.userChoice; deferred = null; };`,
    tip: 'Честная оценка «PWA не нужен лендингу, а SW — это риск» звучит взрослее, чем восторг по поводу офлайна.' },

  { id: 'wx8',
    q: 'Когда реально нужен Web Worker и в чём его цена?',
    a: `<h4>Коротко</h4>
    <p>Worker нужен, когда есть <strong>длительные синхронные вычисления</strong>, блокирующие главный поток и, значит, INP: парсинг больших JSON и CSV, обработка изображений, криптография, диффы, поиск и фильтрация по десяткам тысяч записей, синтаксическая подсветка, WASM. Цена — сериализация данных на границе потоков, которая иногда съедает весь выигрыш.</p>

    <h4>Как это работает</h4>
    <p>Worker — это отдельный поток со своим event loop, своей кучей и своим глобальным объектом (<code>DedicatedWorkerGlobalScope</code>). Общей памяти нет: <code>postMessage</code> выполняет <strong>структурированное клонирование</strong> — рекурсивно копирует объект, сохраняя циклы, Map, Set, Date, TypedArray, но не функции, не DOM-узлы и не прототипы классов. Копия строится синхронно в потоке-отправителе, поэтому передача мегабайтного объекта сама по себе блокирует поток на десятки миллисекунд.</p>
    <p>Обходов три. <strong>Transferable</strong>: <code>ArrayBuffer</code>, <code>MessagePort</code>, <code>ImageBitmap</code>, <code>OffscreenCanvas</code> и потоки можно передать вторым аргументом <code>postMessage</code> — владение переходит, копирования нет, у отправителя буфер становится detached с нулевой длиной. <strong>SharedArrayBuffer</strong> даёт настоящую общую память, но требует заголовков <code>Cross-Origin-Opener-Policy: same-origin</code> и <code>Cross-Origin-Embedder-Policy: require-corp</code> — это плата за Spectre. <strong>OffscreenCanvas</strong> позволяет рисовать прямо из воркера, минуя main thread.</p>
    <p>В воркере нет DOM, <code>window</code>, <code>localStorage</code> и синхронных диалогов; доступны <code>fetch</code>, IndexedDB, WebSocket, Cache API, <code>crypto</code>, WASM и вложенные воркеры.</p>

    <h4>Почему так</h4>
    <p>Модель «нет общей памяти по умолчанию» выбрана намеренно: JS-разработчику не пришлось изучать мьютексы и гонки данных, а браузер получил возможность гарантировать, что DOM трогает ровно один поток. Изоляция стоит копирования — это честный размен простоты на пропускную способность. SharedArrayBuffer возвращает скорость, но вместе с ней возвращает и весь класс ошибок многопоточности, поэтому и спрятан за заголовками.</p>

    <h4>Подводные камни</h4>
    <ul><li><code>await</code> вокруг тяжёлого цикла не освобождает поток: асинхронность решает проблему ожидания I/O, а не загрузки CPU. Это самая частая путаница на собеседовании.</li>
    <li>Порог окупаемости — примерно несколько десятков миллисекунд работы. Ниже дешевле разбить задачу на чанки с <code>scheduler.yield()</code>: старт воркера стоит 5–40 мс, плюс загрузка его бандла.</li>
    <li>Передача результата обратно тоже клонируется. Если воркер вернул массив на 50 МБ, вы просто перенесли фриз с середины операции на её конец.</li>
    <li>Отладка сложнее: у воркера отдельный контекст в DevTools, ошибки без <code>onerror</code> теряются молча, а source maps подключаются отдельно.</li>
    <li>Модульные воркеры (<code>type: 'module'</code>) не поддерживались в Firefox долгое время — проверяйте требования к браузерам, иначе понадобится classic-сборка.</li>
    <li>Пул воркеров ограничен: создавать по воркеру на задачу — верный способ упереться в память на мобильном.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как удобно писать код воркера?</strong> Comlink оборачивает <code>postMessage</code> в промисы и proxy-объекты, и вызов выглядит как обычный <code>await api.parse(buf)</code>. <strong>Чем отличается Shared Worker?</strong> Один экземпляр на все вкладки origin — удобен для общего WebSocket, но отлаживать его сложнее и в мобильных браузерах поддержка неполная. <strong>А Worklet?</strong> Это узкоспециализированные потоки (audio, paint, animation) с жёстко ограниченным API и требованием предсказуемого времени выполнения. <strong>Как измерить, что воркер помог?</strong> Не по времени вычисления — оно не изменится, а по длительности long tasks на главном потоке и по INP: в трейсе главный поток должен стать пустым на всё время работы, а дорожка воркера — занятой. Если суммарное время выросло, а главный поток всё равно занят, значит вы уперлись в клонирование, и надо переходить на transfer.</p>`,
    code: `const w = new Worker(new URL('./parse.worker.ts', import.meta.url), { type: 'module' });
w.postMessage(buffer, [buffer]); // transfer: копирования нет
console.log(buffer.byteLength);  // 0 — буфер detached`,
    tip: 'Фраза «стоимость передачи данных может съесть весь выигрыш» показывает реальный опыт, а не теорию.' },

  { id: 'wx9',
    q: 'Как правильно разбивать длинные задачи: requestIdleCallback, scheduler API, yielding?',
    a: `<h4>Коротко</h4>
    <p>Задача дольше <strong>50 мс</strong> считается long task: пока она идёт, браузер не может обработать ввод и отрисовать кадр, и это прямо портит INP. Разбивка — это возврат управления в event loop между кусками работы. Современный инструмент — <code>scheduler.postTask()</code> и <code>await scheduler.yield()</code>, а не <code>setTimeout(fn, 0)</code>.</p>

    <h4>Как это работает</h4>
    <ul><li><code>setTimeout(fn, 0)</code> — старый способ уступить поток. Работает, но продолжение попадает в конец очереди макротасков без приоритета, а вложенные таймеры после пятого уровня клампятся до 4 мс. Если рядом крутится чужой код, ваша работа может «зависнуть» надолго.</li>
    <li><code>requestIdleCallback</code> — колбэк в период простоя, с <code>timeRemaining()</code> и гарантией по <code>timeout</code>. Подходит для аналитики, префетча, отправки логов, прогрева кеша. Для работы, которую ждёт пользователь, не подходит: простоя может не наступить минутами.</li>
    <li><code>scheduler.postTask(fn, { priority, signal })</code> — планировщик с тремя приоритетами: <code>user-blocking</code> (важнее всего), <code>user-visible</code> (по умолчанию), <code>background</code>. Задачу можно отменить через <code>TaskController</code> и даже изменить её приоритет на лету.</li>
    <li><code>await scheduler.yield()</code> — уступить поток и вернуться <strong>с повышенным приоритетом</strong>: продолжение встанет впереди обычных задач, а не в хвост очереди. Именно это отличие делает его пригодным для резки длинной работы без риска, что она растянется на секунды.</li></ul>

    <h4>Почему так</h4>
    <p>Event loop однопоточный и невытесняющий: браузер физически не может прервать выполняющуюся функцию. Единственная точка, где он способен вклиниться, — возврат из задачи. Поэтому «отзывчивость» на фронтенде — это не скорость кода, а <strong>частота возвратов</strong> в цикл. <code>setTimeout</code> плох не тем, что медленный, а тем, что он теряет приоритет: вы отдали управление и встали в общую очередь наравне с чужой аналитикой.</p>

    <h4>Подводные камни</h4>
    <ul><li>INP измеряет задержку до <strong>следующей отрисовки</strong> после взаимодействия, а не всю работу. Приём: в обработчике клика сразу обновить UI (спиннер, состояние кнопки), уступить поток, и только потом считать. Метрика улучшится, даже если общий объём работы не изменился.</li>
    <li><code>navigator.scheduling.isInputPending()</code> позволяет уступать только тогда, когда ввод действительно ждёт, — но злоупотребление им превращает код в polling и мешает отрисовке кадров.</li>
    <li>Разбивка не бесплатна: каждый yield добавляет 1–5 мс накладных, поэтому резать по одному элементу из десяти тысяч — хуже, чем чанками по 50–100.</li>
    <li>Работа в микротасках (<code>Promise.resolve().then</code>) поток не уступает: микротаски выполняются до отрисовки, в конце той же задачи.</li>
    <li><code>scheduler</code> есть в Chromium; для Safari и Firefox нужен полифилл или фолбэк на <code>setTimeout</code> — проверяйте наличие, а не предполагайте.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как найти long tasks в проде?</strong> PerformanceObserver по типу <code>longtask</code>, а лучше <code>long-animation-frame</code> (LoAF): он показывает не только длительность, но и конкретный скрипт и функцию-виновника. <strong>Когда лучше воркер?</strong> Когда работу нельзя разбить на короткие независимые куски — например, один тяжёлый парсинг или криптография. <strong>Что делать с длинным рендером React?</strong> Разбивка вручную там не работает: рендер — единая синхронная операция. Помогают конкурентные возможности React — <code>startTransition</code> и <code>useDeferredValue</code>, которые позволяют прервать несрочный рендер ради ввода, — плюс виртуализация списка, чтобы работы просто стало меньше. <strong>Почему 50 мс?</strong> Это порог, при котором отклик на ввод перестаёт восприниматься мгновенным: при 60 кадрах в секунду один кадр — 16,7 мс, и три пропущенных кадра уже заметны глазу.</p>`,
    code: `async function process(items) {
  for (let i = 0; i < items.length; i++) {
    handle(items[i]);
    if (i % 50 === 0 && navigator.scheduling?.isInputPending?.()) {
      await scheduler.yield();
    }
  }
}`,
    tip: 'Объяснение, почему scheduler.yield лучше setTimeout(0) (продолжение имеет повышенный приоритет), — сильная деталь.' },

  { id: 'wx10',
    q: 'Как вы профилируете производительность в Performance-панели?',
    a: `<h4>Коротко</h4>
    <p>Сначала воспроизводимые условия: CPU throttling 4–6x, сетевой профиль Slow 4G, чистый профиль браузера без расширений. На рабочем ноутбуке проблем пользователей просто не видно. Затем — трейс и разбор по дорожкам: Timings, Main, Interactions, Network.</p>

    <h4>Как это работает</h4>
    <ul><li><strong>Timings / Metrics</strong> — отметки FCP, LCP, DCL, Load и ваши собственные <code>performance.mark()</code> и <code>measure()</code>. Разметка вокруг бизнес-этапов («получили данные», «отрендерили таблицу») — самое полезное, что можно добавить в код: без неё вы смотрите на анонимные функции после минификации.</li>
    <li><strong>Main</strong> — flame chart главного потока. Ширина блока равна времени, вложенность — стеку вызовов. Красный треугольник в углу и штриховка помечают long task. Вкладка Bottom-Up показывает суммарное self time (кто реально жжёт CPU), Call Tree — иерархию сверху вниз, Event Log — хронологию с фильтрами.</li>
    <li><strong>Interactions</strong> — дорожка реальных взаимодействий с разбивкой на input delay, processing duration и presentation delay. Сразу видно, что именно ухудшает INP: занятый поток до обработчика, тяжёлый обработчик или дорогой рендер после него.</li>
    <li><strong>Experience / Layout Shifts</strong> — каждый сдвиг кликается и подсвечивает сдвинувшийся элемент вместе с вкладом в CLS.</li></ul>
    <p>Признаки проблем: сплошные фиолетовые Recalculate Style и Layout (принудительный reflow помечается предупреждением и ссылкой на строку), длинные жёлтые Evaluate Script, частые пилы GC, пометка «Non-composited animation» в Insights.</p>

    <h4>Почему так</h4>
    <p>Панель показывает не «медленный код», а <strong>занятость главного потока по времени</strong>, потому что для пользователя медленно — это когда кадр не отрисовался и клик не обработался. Поэтому измерять надо не длительность функции в изоляции, а сколько она держит поток и что из-за этого не произошло. Троттлинг нужен по той же причине: на быстром CPU 200-миллисекундная работа сжимается в 30 мс и просто не попадает в порог long task.</p>

    <h4>Подводные камни</h4>
    <ul><li>Профилирование в dev-сборке вводит в заблуждение: React в development делает двойной рендер и лишние проверки, Vite отдаёт немодульный граф из сотен файлов. Мерять надо прод-сборку.</li>
    <li>Расширения браузера дают собственные long tasks и искажают трейс — записывайте в инкогнито или в чистом профиле.</li>
    <li>Один прогон ничего не доказывает: разброс между запусками достигает десятков процентов, нужно 3–5 повторов и медиана.</li>
    <li>Forced synchronous layout легко пропустить: он выглядит как короткий фиолетовый блок внутри вашей функции, но в цикле превращается в сотни миллисекунд.</li>
    <li>Скриншоты в трейсе снимаются не каждый кадр — по ним нельзя судить о точном моменте отрисовки, для этого есть дорожка Frames.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем полезна вкладка Coverage?</strong> Показывает долю неиспользованного CSS и JS на конкретном сценарии — прямой аргумент за code splitting. <strong>Что даёт Rendering?</strong> Paint flashing, layer borders, Core Web Vitals overlay и эмуляция <code>prefers-reduced-motion</code> — быстрый способ увидеть перерисовки без записи трейса. <strong>С чего начинать разбор трейса?</strong> Сверху вниз: сначала дорожка Timings — где вообще проблема, LCP или отклик; потом Interactions или самый широкий блок в Main; и только затем внутрь стека. Погружение в flame chart без гипотезы почти всегда заканчивается оптимизацией функции, которая занимает 2% времени. <strong>Что делать, если виновник — чужой код?</strong> Найти его домен в Initiator, оценить вклад в TBT в разделе Third-party summary у Lighthouse и обсуждать не «оптимизацию», а условия загрузки: отложить, ограничить приоритет или убрать.</p>`,
    code: `performance.mark('table:start');
renderTable(rows);
performance.measure('table', 'table:start');`,
    tip: 'Скажите про throttling и про собственные performance.mark — без них профилирование обычно бесполезно.' },

  { id: 'wx11',
    q: 'Как искать утечки памяти во фронтенде?',
    a: `<h4>Коротко</h4>
    <p>Методика — <strong>три снапшота</strong>: снять heap snapshot, прогнать сценарий N раз, вернуться в исходное состояние, снять снова и сравнить в режиме Comparison. Объекты, которые не исчезли, — кандидаты в утечку. Смотреть надо на retained size и на цепочку retainers.</p>

    <h4>Как это работает</h4>
    <p>Снятие снапшота принудительно вызывает сборку мусора, поэтому всё, что осталось в куче, действительно удерживается живой ссылкой — это ключевая гарантия метода. В сравнении двух снапшотов видно delta по количеству объектов и по размеру. Два числа различаются: <strong>shallow size</strong> — память самого объекта, <strong>retained size</strong> — сколько освободится, если его удалить, то есть весь поддеревом удерживаемый граф. Утечку ищут по retained size.</p>
    <p>Панель Retainers показывает путь от корня GC до объекта: именно там видно, что узел удерживается замыканием обработчика, который висит на <code>document</code>. Для отсоединённого DOM есть фильтр по слову «Detached»: <code>Detached HTMLDivElement</code> с большим retained size означает, что вы удалили узел из документа, но ссылка на него осталась в JS.</p>
    <p>Второй инструмент — Allocation instrumentation on timeline: он рисует столбики аллокаций во времени; синие полосы, которые так и остались синими к концу записи, — выжившие объекты, и по клику видно стек их создания.</p>

    <h4>Почему так</h4>
    <p>JS-сборщик мусора работает по достижимости от корней, а не по счётчику ссылок, поэтому циклы он собирает сам, а «утечка» в вебе почти всегда означает одно: где-то есть <strong>живая ссылка из долгоживущей структуры</strong> в короткоживущий объект. Долгоживущие структуры наперечёт — глобальные переменные, модульные переменные, DOM, реестры слушателей, таймеры, store. Поэтому диагностика сводится к вопросу «кто из этих пяти держит».</p>

    <h4>Подводные камни</h4>
    <ul><li>Типичные источники в SPA: не снятые <code>addEventListener</code>, <code>ResizeObserver</code> и <code>IntersectionObserver</code> без <code>disconnect()</code>, <code>setInterval</code> без <code>clearInterval</code>, подписки на store, не отменённые <code>fetch</code> без <code>AbortController</code>, безлимитные кеши в <code>Map</code>.</li>
    <li>HMR в dev создаёт ложные утечки: модульные переменные переживают перезагрузку модуля. Проверять надо на прод-сборке.</li>
    <li>Рост памяти сам по себе не утечка: движок может просто не спешить с GC. Утечка — это <strong>монотонный</strong> рост при повторении одного и того же цикла действий.</li>
    <li><code>WeakMap</code> и <code>WeakRef</code> — средство проектирования, а не лечение: они не помогут, если ссылку держит массив обработчиков.</li>
    <li>Снапшот большого приложения весит сотни мегабайт и подвешивает вкладку — на слабой машине лучше сравнивать по <code>performance.measureUserAgentSpecificMemory()</code>.</li>
    <li>Замыкание захватывает всю область видимости, а не одну переменную: одна функция может удерживать мегабайтный ответ, который в ней даже не используется.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как мониторить память в проде?</strong> <code>performance.measureUserAgentSpecificMemory()</code> даёт агрегированную оценку по контекстам и требует изоляции (COOP/COEP); отправляйте её сэмплированно вместе с длительностью сессии. <strong>Как понять, что виноват именно фронтенд?</strong> Сравните вкладку Memory в Chrome Task Manager для вашей страницы и для пустой — там же видно потребление GPU-процессом при чрезмерном количестве слоёв. <strong>Чем утечка отличается от раздутого кеша?</strong> Кеш растёт до предела и останавливается, утечка растёт линейно с числом повторений сценария. Поэтому первый вопрос при подозрении — «сколько раз надо повторить действие, чтобы память выросла вдвое»: если ответ есть, это утечка, если рост выходит на плато — просто дорогая структура данных, и лечится она лимитом, а не поиском ссылки.</p>`,
    code: `useEffect(() => {
  const ac = new AbortController();
  el.addEventListener('scroll', onScroll, { signal: ac.signal });
  return () => ac.abort(); // один signal снимает все подписки
}, []);`,
    tip: 'Приём «три снапшота» и фильтр Detached — конкретная методика, которая сразу выделяет ответ.' },

  { id: 'wx12',
    q: 'Что такое TTFB, из чего он складывается и как его улучшать?',
    a: `<h4>Коротко</h4>
    <p>TTFB — время от начала навигации до первого байта ответа документа. В Core Web Vitals он не входит, но служит <strong>нижней границей</strong> для FCP и LCP: при TTFB 1,5 с получить LCP меньше уже невозможно. Ориентир Google — до <strong>800 мс</strong> для «хорошо» и до 1800 мс для «требует улучшения».</p>

    <h4>Как это работает</h4>
    <p>Складывается из шести частей: редиректы, DNS, TCP-хендшейк, TLS-хендшейк, время генерации ответа на сервере и передача первых байтов. Разложить можно через Navigation Timing: <code>responseStart - startTime</code> даёт полный TTFB, а промежуточные метки (<code>domainLookupEnd</code>, <code>connectEnd</code>, <code>requestStart</code>) показывают, где именно потеряно время. В DevTools то же самое видно в панели Timing запроса документа: строка Waiting for server response — это чистая серверная часть.</p>
    <p>Важная деталь: <code>redirectStart</code>/<code>redirectEnd</code> заполняются только для same-origin редиректов; цепочка через чужой домен превращается в непрозрачную дыру, которую видно лишь в WebPageTest или по разнице между <code>startTime</code> и <code>fetchStart</code>.</p>

    <h4>Почему так</h4>
    <p>Пока не пришёл первый байт HTML, браузер не знает вообще ничего: ни какие подключить стили, ни какая картинка будет LCP. Preload scanner не может сканировать то, чего нет. Поэтому TTFB — не просто «часть» загрузки, а её потолок; оптимизация картинок при TTFB в 2 секунды не даст ничего.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Редиректы</strong> — самая недооценённая часть. Цепочка <code>http://site.ru</code> → <code>https://site.ru</code> → <code>https://www.site.ru</code> стоит два полных RTT с хендшейками, легко 300–600 мс на мобильной сети. Лечится HSTS preload (первый переход исчезает) и настройкой канонического хоста на уровне CDN.</li>
    <li><strong>Серверная генерация</strong>: edge-кеш для HTML, а не только для статики, <code>stale-while-revalidate</code>, устранение N+1 в запросах к БД, отказ от последовательных блокирующих вызовов при SSR.</li>
    <li><strong>Сеть</strong>: CDN ближе к пользователю, HTTP/2 или HTTP/3 (QUIC экономит RTT на установке), TLS 1.3, session resumption и 0-RTT для повторных визитов.</li>
    <li><strong>Streaming SSR</strong> не улучшает TTFB как число, но позволяет отдать <code>&lt;head&gt;</code> сразу и запустить загрузку CSS и шрифтов, пока сервер ещё считает тело. По ощущениям и по FCP это часто важнее самого TTFB.</li>
    <li><code>103 Early Hints</code> отправляет <code>Link</code>-заголовки до основного ответа: браузер начинает грузить критичные ресурсы во время работы бэкенда. Поддерживается Chromium и требует поддержки на CDN.</li>
    <li>TTFB в поле и в лабе расходятся радикально: у пользователей есть медленный DNS, дальние регионы и перегруженные сети. Смотрите p75 в CrUX, а не одно измерение.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Почему TTFB вырос после включения SSR?</strong> Потому что сервер теперь ждёт данные перед первым байтом — либо стримить, либо кешировать, либо часть данных отдавать клиенту. <strong>Влияет ли TTFB на ранжирование?</strong> Напрямую нет, он не CWV, но через LCP влияет вполне. <strong>Что делать, если TTFB плохой только у части пользователей?</strong> Смотреть срез по стране и типу сети: обычно это либо отсутствие узла CDN в регионе, либо origin в одном датацентре, либо мобильные операторы с медленным DNS. <strong>Как понять, кеш это или генерация?</strong> По заголовкам ответа: <code>Age</code>, <code>X-Cache: HIT|MISS</code> и <code>Server-Timing</code> с фазами бэкенда — последний стоит отдавать всегда, он виден и в DevTools, и в Navigation Timing через <code>serverTiming</code>, и превращает «сервер думал 800 мс» в конкретную строчку про запрос к БД.</p>`,
    code: `const nav = performance.getEntriesByType('navigation')[0];
console.log('TTFB', nav.responseStart);
console.log('redirects', nav.redirectEnd - nav.redirectStart);`,
    tip: 'Тезис «TTFB — потолок для LCP» плюс упоминание 103 Early Hints делают ответ конкретным.' },

  { id: 'wx13',
    q: 'Как связаны FCP, LCP, TBT и INP? Что мерить в лабе, а что в поле?',
    a: `<h4>Коротко</h4>
    <p>TTFB задаёт старт, FCP фиксирует первый любой контент, LCP — отрисовку крупнейшего элемента, TBT суммирует занятость потока, INP измеряет отклик на реальные действия. Пороги «хорошо» по 75-му перцентилю поля: <strong>LCP ≤ 2,5 с, INP ≤ 200 мс, CLS ≤ 0,1</strong>. TBT — лабораторный прокси для INP, а не сама метрика.</p>

    <h4>Как это работает</h4>
    <p><strong>FCP</strong> наступает, когда отрисован первый текст или изображение; его тормозят блокирующие CSS и JS, шрифты с <code>font-display: block</code> и, разумеется, TTFB. <strong>LCP</strong> — момент отрисовки самого большого по площади элемента в первом экране (изображение, видео-постер, блок текста); кандидат может меняться по мере загрузки, и засчитывается последний до первого взаимодействия. <strong>TBT</strong> — сумма превышений над 50 мс у всех long tasks между FCP и TTI: три задачи по 70 мс дают TBT 60 мс. <strong>INP</strong> берёт почти худшее взаимодействие за визит (при большом числе взаимодействий — примерно 98-й перцентиль) и раскладывает его на input delay, processing duration и presentation delay.</p>
    <p>Связь причинная: тяжёлый JS одновременно откладывает LCP (конкуренция за сеть и поток), раздувает TBT и через занятость потока ухудшает INP. Поэтому одно исправление часто двигает три метрики.</p>

    <h4>Почему так</h4>
    <p>CWV намеренно измеряют <strong>восприятие</strong>, а не техническое событие: пользователю безразличен <code>DOMContentLoaded</code>, ему важно, когда появился контент, когда страница ответила на клик и не прыгнула ли вёрстка. Поле выбрано как источник истины, потому что распределение устройств и сетей воспроизвести в лаборатории нельзя. Лаборатория оставлена для диагностики, где нужна повторяемость.</p>

    <h4>Подводные камни</h4>
    <ul><li>Корреляция TBT и INP есть, но совпадения нет: страница с отличным TBT может иметь ужасный INP, если тяжёлый обработчик срабатывает только на конкретном клике, до которого Lighthouse не доходит.</li>
    <li>Улучшение лабораторного балла без движения p75 в поле — не результат. Lighthouse считает балл по своей взвешенной формуле и легко «чинится» косметикой.</li>
    <li>LCP-элемент часто не тот, что кажется: проверяйте атрибуцию в web-vitals или в панели Performance, иначе оптимизируете не ту картинку.</li>
    <li>CLS измеряется по худшему <strong>окну сдвигов в 5 секунд</strong>, а не за всю сессию — иначе длинные страницы с ленивой подгрузкой были бы обречены.</li>
    <li>FID устарел и в 2024 году заменён на INP; ответы, которые всё ещё оперируют FID, выдают неактуальность.</li>
    <li>SPA-навигации в CWV не учитываются: LCP считается один раз на загрузку документа, поэтому для SPA нужны собственные метрики маршрута.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что такое LoAF?</strong> Long Animation Frames API — замена Long Tasks для диагностики INP: он отдаёт затянутый кадр целиком, с разбивкой по скриптам, их источникам и времени на стили и layout. <strong>Откуда берутся полевые данные?</strong> CrUX (реальные пользователи Chrome, агрегация за 28 дней, доступна в PageSpeed Insights и BigQuery) и собственный RUM на библиотеке <code>web-vitals</code>. <strong>С какой метрики начинать?</strong> С той, что хуже порога у 75-го перцентиля и покрывает больше трафика: чинить INP на странице с 2% визитов, когда LCP плох на главной, — потраченное время. <strong>Как одно исправление двигает несколько метрик?</strong> Удаление 200 КБ синхронного JS одновременно освобождает полосу (LCP), убирает long tasks (TBT) и разгружает поток в момент первого клика (INP) — поэтому приоритизировать надо не метрики, а причины.</p>`,
    code: `import { onLCP, onINP } from 'web-vitals/attribution';
onINP((m) => send('inp', m.value, m.attribution.interactionTarget));`,
    tip: 'Упоминание LoAF API как замены Long Tasks для диагностики INP — свежая и очень предметная деталь.' },

  { id: 'wx14',
    q: 'RUM или синтетика: что и когда использовать?',
    a: `<h4>Коротко</h4>
    <p>Это не альтернативы, а разные роли. RUM отвечает на вопрос <strong>«что чинить»</strong> и <strong>«помогло ли»</strong>, синтетика — на вопрос <strong>«почему»</strong>. Проект без RUM оптимизирует вслепую, проект без синтетики не ловит регрессии до релиза.</p>

    <h4>Как это работает</h4>
    <p><strong>Синтетика</strong> — Lighthouse, WebPageTest, прогоны в CI — это управляемая среда: фиксированное устройство, фиксированная сеть, чистый профиль. Отсюда воспроизводимость, полные трейсы, водопады, скриншоты по кадрам и возможность запускать на каждый PR. Отсюда же ограничения: одна конфигурация вместо распределения, холодный кеш вместо реального, отсутствие INP как такового (вместо него TBT), нулевая видимость региональной специфики и редких сценариев.</p>
    <p><strong>RUM</strong> — библиотека <code>web-vitals</code> плюс свой сборщик, либо CrUX, либо коммерческое решение. Даёт распределение по устройствам, странам, типам подключения, маршрутам и версиям релиза. CrUX бесплатен, но агрегирует за 28 дней и покрывает только Chrome и только достаточно посещаемые URL; собственный RUM даёт свежие данные и произвольные срезы, но требует объёма трафика, согласия на сбор и бюджета на хранение.</p>

    <h4>Почему так</h4>
    <p>Производительность — это распределение, а не число. У одного пользователя LCP 1,2 с на оптоволокне и флагмане, у другого — 6 с на Android за 100 долларов в роуминге. Синтетика по определению выбирает одну точку этого распределения; вопрос лишь в том, насколько удачно она выбрана. RUM видит распределение целиком, но не может объяснить причину, потому что не пишет трейс каждому пользователю.</p>

    <h4>Подводные камни</h4>
    <ul><li>Смотреть надо перцентили (p75, p95), а не среднее: среднее прячет хвост, а хвост и есть боль. Google оценивает CWV именно по p75.</li>
    <li>Обязательна сегментация по устройству, стране и типу страницы. Без неё смена состава аудитории (например, рекламная кампания на мобильных) выглядит как регресс производительности.</li>
    <li>Lighthouse-балл шумит на 5–10 пунктов между прогонами: одиночный запуск в CI даёт ложные срабатывания, нужна медиана из 3–5 прогонов на стабильном раннере.</li>
    <li>RUM без атрибуции почти бесполезен — вы знаете, что INP плохой, но не знаете, какой элемент. Импорт <code>web-vitals/attribution</code> закрывает это: он отдаёт селектор элемента, тип события и разбивку по фазам.</li>
    <li>Синтетика на локальной машине разработчика не считается измерением вообще: у неё нет ни троттлинга, ни расширений, ни реальной сети.</li>
    <li>Выжившие: RUM видит только тех, кто дождался загрузки скрипта. Самые медленные сессии часто просто уходят, и метрика выглядит лучше реальности.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как связать релиз с регрессией?</strong> Отправляйте версию сборки в каждое RUM-событие и ставьте маркер деплоя на графике — тогда скачок p75 привязывается к конкретному PR. <strong>Что делать при малом трафике?</strong> Опираться на синтетику и CrUX по домену в целом, а RUM собирать с большим окном агрегации. <strong>Сколько стоит собственный RUM?</strong> Библиотека <code>web-vitals</code> весит около 2 КБ, отправка идёт через <code>sendBeacon</code> одним пакетом на выгрузке страницы, так что накладные на клиенте пренебрежимы; дорого обходится хранение и агрегация — поэтому события сэмплируют и агрегируют по маршруту, а не по URL. <strong>Что мерить, кроме CWV?</strong> Свои продуктовые тайминги: время до появления данных в таблице, время открытия модала, длительность первого поиска — именно они объясняют бизнесу, зачем всё это.</p>`,
    code: `import { onCLS, onINP, onLCP } from 'web-vitals/attribution';
const send = (m) => navigator.sendBeacon('/rum', JSON.stringify({
  name: m.name, value: m.value, id: m.id, release: __BUILD__, target: m.attribution
}));
onCLS(send); onINP(send); onLCP(send);`,
    tip: 'Формула «RUM — что чинить, синтетика — почему, RUM — помогло ли» — компактный и убедительный ответ.' },

  { id: 'wx15',
    q: 'Как встроить бюджеты производительности в CI?',
    snippet: `// .size-limit.json
[
  { "path": "dist/assets/entry-*.js", "limit": "160 kB", "gzip": true },
  { "path": "dist/assets/vendor-*.js", "limit": "220 kB", "gzip": true }
]

// lighthouserc.json
{ "ci": { "assert": { "assertions": {
  "largest-contentful-paint": ["error", { "maxNumericValue": 2500 }],
  "total-blocking-time":      ["error", { "maxNumericValue": 300 }],
  "cumulative-layout-shift":  ["error", { "maxNumericValue": 0.1 }]
} } } }`,
    a: `<h4>Коротко</h4>
    <p>Бюджет — это числовой порог, нарушение которого видно в PR и, при желании, блокирует мердж. Уровней три: <strong>размер</strong> бандла, <strong>метрики</strong> Lighthouse и <strong>правила</strong> вроде количества запросов. Начинать надо с размера: он дешёвый, детерминированный и почти не шумит.</p>

    <h4>Как это работает</h4>
    <p><strong>Размерный бюджет.</strong> После сборки инструмент считает вес чанков после gzip или brotli и сравнивает с лимитом. Реализации: <code>size-limit</code> (умеет считать ещё и время выполнения на эмулированном устройстве), <code>bundlesize</code>, встроенные <code>performance.maxAssetSize</code> у webpack, <code>output.experimentalMinChunkSize</code> у Rollup. Считать надо именно сжатый размер: brotli на текстовом JS даёт 3,5–4,5x, и несжатые цифры вводят в заблуждение.</p>
    <p><strong>Метрический бюджет.</strong> Lighthouse CI поднимает прод-сборку, прогоняет аудит несколько раз и проверяет <code>assertions</code>. Ключевое требование — стабильный раннер: на общих GitHub-раннерах разброс TBT достигает 40%, поэтому берут медиану из 3–5 прогонов, а лучше выделенную машину или контейнер с фиксированными CPU-квотами.</p>
    <p><strong>Правила.</strong> Количество запросов на маршрут, отсутствие незапланированных third-party доменов, максимальный вес изображения, запрет на синхронные скрипты в head. Это дешёвые проверки, ловящие «прилетело из GTM» и «дизайнер положил PNG на 4 МБ».</p>

    <h4>Почему так</h4>
    <p>Производительность деградирует не рывками, а по 3–5 килобайт за спринт: каждый отдельный PR выглядит безобидно, а за полгода бандл удваивается. Ревьюер этого не заметит, потому что у него нет базы для сравнения. Бюджет — это способ дать сравнение автоматически и в тот момент, когда изменение ещё дёшево откатить. Метрики в CI при этом вторичны: они шумят и требуют инфраструктуры, а размер бандла коррелирует с TBT почти линейно и стоит одну команду.</p>

    <h4>Подводные камни</h4>
    <ul><li>Порог берут от <strong>текущего значения плюс небольшой запас</strong>, а не от идеала. Бюджет, красный с первого дня, через неделю игнорируют, а через месяц отключают.</li>
    <li>У бюджета должен быть владелец и понятная процедура исключения: временное повышение лимита с обязательной задачей на возврат. Без неё команда либо блокируется, либо удаляет проверку.</li>
    <li>Жёсткий блок мерджа на метриках почти всегда ошибка: шум даёт ложные срабатывания, и доверие к проверке падает. Комментарий бота с дельтой по чанкам работает лучше — видно, что именно выросло и на сколько.</li>
    <li>Бюджет на суммарный вес прячет перекос: 500 КБ, размазанные по десяти маршрутам, и те же 500 КБ в одном входном чанке — разные истории. Считайте по точкам входа и по маршрутам.</li>
    <li>CI не видит того, что добавляется в рантайме: теги в GTM, A/B-скрипты, баннеры согласия. Для них нужен алерт на регресс p75 в поле.</li>
    <li>Сравнение с <code>main</code>, а не с абсолютом, ловит регрессию точнее, но требует кеша артефактов базовой ветки.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как убедить продукт?</strong> Через деньги: связать p75 LCP с конверсией на своих же данных, а не ссылаться на чужие кейсы. <strong>Что делать с легаси?</strong> Заморозить текущий размер («не хуже, чем сейчас») и снижать порог по мере рефакторинга. <strong>Где хранить историю?</strong> Lighthouse CI Server или простая таблица с артефактами сборки, чтобы был график, а не одиночные числа. <strong>Что делать с внезапным ростом от зависимости?</strong> Бюджет должен показывать не только «стало больше», но и «за счёт чего»: отчёт анализатора как артефакт сборки плюс дифф по составу чанков превращает спор «нужна ли эта библиотека» в разговор про конкретные 40 КБ. <strong>Нужен ли бюджет на CSS?</strong> Да, и отдельный: неиспользуемый CSS блокирует рендер целиком, поэтому его вес влияет на FCP сильнее, чем такой же объём отложенного JS.</p>`,
    code: `- run: npx size-limit --json > size.json
- run: npx lhci autorun --numberOfRuns=5 --collect.settings.preset=desktop`,
    tip: 'Мысль «бюджет без владельца и процедуры исключения быстро отключают» — управленческая зрелость, её ценят.' },

  { id: 'wx16',
    q: 'Какие форматы изображений выбирать и по каким критериям?',
    a: `<h4>Коротко</h4>
    <p>По умолчанию <strong>AVIF</strong> с фолбэком в <strong>WebP</strong> и <strong>JPEG</strong> через <code>&lt;picture&gt;</code>; PNG — только для безпотерьной прозрачности и пиксель-арта; SVG — для иконок и иллюстраций; анимация — видео, а не GIF. Но чаще всего выигрыш даёт не формат, а <strong>правильный размер в пикселях</strong>.</p>

    <h4>Как это работает</h4>
    <p><strong>AVIF</strong> (кодек AV1) даёт на 20–50% меньший вес, чем WebP, при сопоставимом качестве, умеет 10-битный цвет, HDR, альфу и хорошо держит плавные градиенты. Платим кодированием: сжать большую картинку в AVIF на сервере в десятки раз дороже по CPU, чем в JPEG, а на мелких изображениях (иконки, аватарки до 100 px) накладные расходы контейнера съедают выигрыш. На низком качестве AVIF не «шумит», а замыливает — на фотографиях с текстурой это заметнее артефактов JPEG.</p>
    <p><strong>WebP</strong> — безопасный универсальный вариант: примерно на 25–30% меньше JPEG, поддержка везде, кодирование быстрое. <strong>JPEG</strong> остаётся фолбэком и вполне конкурентен, если сжат современным энкодером (MozJPEG, jpegli) с прогрессивной развёрткой. <strong>PNG</strong> почти всегда проигрывает WebP lossless. <strong>SVG</strong> векторный и масштабируется бесплатно, но пользовательские SVG обязаны проходить санитайзинг — внутри допустимы <code>&lt;script&gt;</code> и обработчики событий, то есть это готовый вектор XSS.</p>
    <p>В <code>&lt;picture&gt;</code> браузер берёт <strong>первый поддерживаемый</strong> <code>&lt;source&gt;</code> и дальше не смотрит, поэтому порядок строго от лучшего сжатия к худшему.</p>

    <h4>Почему так</h4>
    <p>Content negotiation через <code>Accept</code> на сервере или CDN обычно удобнее разметки, но требует правильного <code>Vary: Accept</code> — иначе прокси отдаст AVIF браузеру, который его не понимает. Разметка через <code>&lt;picture&gt;</code> надёжнее и прозрачнее, зато прибивает список форматов к HTML.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Лишние пиксели важнее формата</strong>: изображение 2000 px в контейнере 400 px тратит трафик впустую независимо от кодека. Сначала <code>srcset</code> и <code>sizes</code>, потом уже AVIF.</li>
    <li>GIF на 3 МБ заменяется на MP4/WebM на 200 КБ. GIF в 2025 году — почти всегда ошибка, кроме случая, когда нужен именно <code>&lt;img&gt;</code> в письме.</li>
    <li>Отсутствие <code>width</code> и <code>height</code> (или <code>aspect-ratio</code>) даёт сдвиг макета и портит CLS: браузер не может зарезервировать место до загрузки.</li>
    <li><code>loading="lazy"</code> на LCP-изображении — прямой удар по метрике; ленивую загрузку ставят только ниже сгиба.</li>
    <li><code>decoding="async"</code> снимает декодирование с критического пути, но на LCP-картинке иногда даёт обратный эффект — проверяйте, а не ставьте по привычке.</li>
    <li>Качество 80 для JPEG и 50 для AVIF — разные шкалы; переносить число из одного энкодера в другой нельзя, надо смотреть на SSIM/butteraugli или глазами.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что с JPEG XL?</strong> Формат технически сильный, но Chrome убрал его из сборки, так что в вебе он пока нерелевантен. <strong>Как выбрать качество автоматически?</strong> Перцептивные метрики в пайплайне (SSIMULACRA2) либо режим <code>q_auto</code> у image CDN. <strong>Когда inline base64?</strong> Только для крошечных иконок в критическом CSS: base64 раздувает данные на 33% и не кешируется отдельно. <strong>Как выбрать между picture и content negotiation?</strong> Если формат — единственное различие и у вас есть CDN с корректным <code>Vary: Accept</code>, проще отдавать один URL и разруливать на сервере: разметка остаётся чистой, а политика меняется без релиза. Разметка выигрывает там, где нужен явный контроль и предсказуемость на всех прокси. <strong>Что чаще всего даёт самый быстрый выигрыш?</strong> Аудит десяти самых тяжёлых изображений на ключевых страницах: обычно два-три из них весят больше, чем весь JS.</p>`,
    code: `<picture>
  <source type="image/avif" srcset="/h-800.avif 800w, /h-1600.avif 1600w" sizes="100vw">
  <source type="image/webp" srcset="/h-800.webp 800w, /h-1600.webp 1600w" sizes="100vw">
  <img src="/h-800.jpg" width="1600" height="900" alt="" fetchpriority="high">
</picture>`,
    tip: 'Замечание, что чаще всего проблема не в формате, а в лишних пикселях, — очень практичный акцент.' },

  { id: 'wx17',
    q: 'Как работают srcset и sizes и в чём разница с art direction?',
    snippet: `<img src="/i-800.avif"
     srcset="/i-400.avif 400w, /i-800.avif 800w, /i-1600.avif 1600w"
     sizes="(min-width: 1024px) 640px, 100vw"
     width="1600" height="900" alt="">`,
    a: `<h4>Коротко</h4>
    <p><code>srcset</code> с <code>sizes</code> — это <strong>оптимизация трафика</strong>: один и тот же кадр в разных разрешениях, выбор делает браузер. <code>&lt;picture&gt;</code> с <code>media</code> — <strong>дизайнерское решение</strong>: разное кадрирование под разные экраны, выбор делаете вы. Смешивать эти задачи — самая частая ошибка.</p>

    <h4>Как это работает</h4>
    <p>У <code>srcset</code> два режима. <strong>x-дескрипторы</strong> (<code>a.png 1x, a@2x.png 2x</code>) — для изображений фиксированного CSS-размера: браузер смотрит только на <code>devicePixelRatio</code>. <strong>w-дескрипторы</strong> (<code>i-800.avif 800w</code>) описывают реальную ширину файла в пикселях и работают только в паре с <code>sizes</code>.</p>
    <p><code>sizes</code> — это список медиавыражений с шириной, которую изображение займёт <strong>в макете</strong>. Браузер берёт первое подходящее условие, получает CSS-ширину слота, умножает на DPR и выбирает наименьшего кандидата, который не меньше нужного. Пример: слот 640 px при DPR 2 требует 1280 px, значит будет выбран файл 1600w.</p>
    <p>Критический нюанс: выбор делается в <strong>preload scanner</strong>, до применения CSS и до layout. Браузер физически не знает, какой ширины окажется контейнер, поэтому вы обязаны сообщить это вручную. Именно поэтому <code>sizes="100vw"</code> в трёхколоночной сетке приводит к загрузке файла втрое больше нужного, и responsive images не дают никакой экономии.</p>

    <h4>Почему так</h4>
    <p>Спецификация сознательно оставила выбор за браузером, а не за разработчиком: только браузер знает DPR, ширину вьюпорта, zoom, настройки экономии трафика и что уже лежит в кеше. Разработчик знает то, чего не знает браузер, — раскладку. Отсюда разделение ответственности: вы даёте <code>sizes</code>, браузер даёт решение. Побочный эффект — браузеру разрешено выбрать <strong>более крупный</strong> кандидат, если он уже в кеше или сеть быстрая; это не баг и не повод «чинить» разметку.</p>

    <h4>Подводные камни</h4>
    <ul><li>Неверный <code>sizes</code> — причина номер один, почему адаптивные картинки не работают. Генерируйте его из тех же токенов брейкпоинтов, что и CSS, или проверяйте в DevTools: наведение на <code>currentSrc</code> показывает выбранный файл.</li>
    <li><code>sizes="auto"</code> вместе с <code>loading="lazy"</code> решает проблему радикально: ленивая картинка выбирается после layout, поэтому браузер берёт реальную ширину. Для LCP-изображения это не подходит — оно не должно быть lazy.</li>
    <li>Кандидаты с шагом меньше 30–40% бессмысленны: экономия теряется в накладных, а вариантов на CDN становится втрое больше.</li>
    <li><code>src</code> обязателен как фолбэк, и он не должен быть самым большим файлом: старые браузеры скачают именно его.</li>
    <li>В <code>&lt;picture&gt;</code> атрибут <code>sizes</code> ставится на каждый <code>&lt;source&gt;</code>, а не только на <code>&lt;img&gt;</code>.</li>
    <li><code>media</code>-условия в <code>&lt;picture&gt;</code> проверяются по порядку, первый подошедший выигрывает — перепутанный порядок брейкпоинтов даёт вечно один и тот же source.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как проверить, что выбран правильный файл?</strong> <code>img.currentSrc</code> в консоли и колонка Size в Network. <strong>Что с фоновыми изображениями?</strong> Для них есть <code>image-set()</code> в CSS с x-дескрипторами и указанием типа, но приоритетом и preload они управляются хуже — критичные картинки держите в разметке. <strong>Сколько кандидатов делать?</strong> Три-четыре ступени с шагом примерно вдвое покрывают весь диапазон от мобильного до 2x-десктопа; больше вариантов даёт единицы процентов экономии и заметно ухудшает hit ratio на CDN. <strong>Что происходит при ресайзе окна?</strong> Браузер не понижает качество: если уже загружен большой кандидат, он остаётся. Апгрейд при увеличении окна происходит, даунгрейд — нет, и это ожидаемое поведение, а не баг вёрстки.</p>`,
    code: `<picture>
  <source media="(max-width: 640px)" srcset="/hero-portrait.avif">
  <img src="/hero-wide.avif" width="1600" height="600" alt="">
</picture>`,
    tip: 'Объясните, что sizes читается до CSS — отсюда все ошибки; и упомяните sizes="auto" как современное решение.' },

  { id: 'wx18',
    q: 'Зачем image CDN и как он сочетается с priority hints и ленивой загрузкой?',
    a: `<h4>Коротко</h4>
    <p>Image CDN превращает один исходник в бесконечное множество производных по параметрам URL: размер, формат, качество, кадрирование, DPR. Это снимает генерацию десятков файлов на сборке и позволяет менять политику качества без редеплоя. С hints он сочетается по простому правилу: LCP-картинка — <code>fetchpriority="high"</code> и никакого <code>lazy</code>, всё ниже сгиба — наоборот.</p>

    <h4>Как это работает</h4>
    <p>Запрос вида <code>/img/hero.jpg?w=800&amp;q=70&amp;fm=auto</code> приходит на edge-узел. Если вариант уже в кеше — отдаётся мгновенно. Если нет, узел тянет оригинал, выполняет трансформацию и кладёт результат. Формат выбирается по заголовку <code>Accept</code> (нужен корректный <code>Vary: Accept</code>), DPR — по <code>Sec-CH-DPR</code> или явному параметру. Реализации бывают внешние (Cloudinary, imgix), self-hosted (imgproxy, Thumbor) и встроенные в фреймворк (<code>next/image</code>, <code>nuxt/image</code>).</p>
    <p>Дальше вступают hints. Изображение первого экрана: <code>fetchpriority="high"</code>, <strong>без</strong> <code>loading="lazy"</code>, желательно с <code>&lt;link rel="preload"&gt;</code>, если оно приходит из CSS. Всё, что ниже сгиба: <code>loading="lazy"</code> и при желании <code>fetchpriority="low"</code>. Порог срабатывания lazy в Chrome зависит от типа сети и составляет сотни или тысячи пикселей до вьюпорта, поэтому «пустые места» при быстром скролле появляются редко.</p>

    <h4>Почему так</h4>
    <p>Генерация вариантов на сборке масштабируется плохо: десять брейкпоинтов на три формата — тридцать файлов на изображение, и любое изменение политики качества означает полный ребилд и инвалидацию кеша. CDN переносит трансформацию в момент первого запроса, платя за это задержкой ровно один раз на вариант. Это классический размен «дорогая сборка» на «дорогой первый запрос».</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Cache miss на первом запросе</strong>: трансформация занимает от 100 мс до нескольких секунд для больших исходников. Для LCP-картинки это значит, что первый посетитель после релиза получит худший опыт — варианты надо прогревать при деплое.</li>
    <li><strong>Взрыв кеша</strong>: произвольные параметры в URL создают бесконечное число вариантов, hit ratio падает, счёт за трансформации растёт. Ограничивайте набор допустимых значений и подписывайте URL, иначе получите ещё и вектор DoS.</li>
    <li>Отдельный домен CDN добавляет DNS + TCP + TLS — нужен <code>preconnect</code>, а лучше проксирование через свой домен, чтобы соединение переиспользовалось.</li>
    <li><code>loading="lazy"</code> на LCP-изображении — типовая ошибка, легко добавляющая секунду к метрике: браузер откладывает запрос до layout.</li>
    <li>Плейсхолдеры LQIP и blurhash улучшают восприятие, но inline-base64 раздувает HTML — при сотне карточек это десятки килобайт в критическом ответе и рост TTFB.</li>
    <li>Автоматическое кадрирование по «умному» алгоритму режет головы и логотипы: для карточек товаров нужна фокусная точка в данных, а не эвристика.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как оценить эффект?</strong> Сравнить суммарный вес изображений и p75 LCP до и после; экономия трафика 40–70% типична. <strong>Нужен ли CDN, если картинок мало?</strong> Нет: три статичных изображения дешевле собрать на билде. <strong>Как не зависеть от вендора?</strong> Держать слой абстракции над построением URL — переезд с одного провайдера на другой тогда сводится к одной функции. <strong>Как прогревать варианты?</strong> Шагом деплоя дёргать ключевые URL первого экрана для основных брейкпоинтов и форматов — десяток запросов на релиз, зато первый живой пользователь попадает в кеш. <strong>Что делать с приватными изображениями?</strong> Подписанные URL с ограниченным сроком и запрет кеширования на общих узлах: иначе аватар или документ пользователя окажется доступен по угадываемой ссылке всем.</p>`,
    code: `<img src="/cdn/hero?w=800&q=70" srcset="/cdn/hero?w=800&q=70 800w, /cdn/hero?w=1600&q=70 1600w"
     sizes="100vw" fetchpriority="high" width="1600" height="900" alt="">`,
    tip: 'Пункт «lazy на LCP-картинке — типовая ошибка» практически всегда попадает в точку.' },

  { id: 'wx19',
    q: 'Как оптимизировать шрифты: FOIT, FOUT, font-display, subsetting, size-adjust?',
    a: `<h4>Коротко</h4>
    <p><strong>FOIT</strong> — невидимый текст, пока грузится шрифт: LCP-кандидат не засчитывается. <strong>FOUT</strong> — сначала системный шрифт, потом подмена: сдвиг и CLS. Управляется <code>font-display</code>, а нулевой сдвиг достигается метрически подогнанным fallback через <code>size-adjust</code> и <code>ascent-override</code>.</p>

    <h4>Как это работает</h4>
    <p>У загрузки шрифта три периода. <strong>Block</strong>: текст невидим, место занято. <strong>Swap</strong>: рисуется fallback, при готовности идёт подмена. <strong>Failure</strong>: fallback остаётся навсегда для этой загрузки. Значения <code>font-display</code> задают их длительность: <code>block</code> — блок 3 с; <code>swap</code> — блок 0, своп бесконечный; <code>fallback</code> — блок 100 мс, своп 3 с; <code>optional</code> — блок 100 мс, свопа нет вовсе, зато браузер вправе догрузить шрифт в фон и применить его на следующей навигации; <code>auto</code> отдан на усмотрение браузера и обычно ведёт себя как <code>block</code>.</p>
    <p>Сдвиг при подмене возникает потому, что у fallback другие метрики: высота строки, ширина глифов, ascent и descent. Дескрипторы <code>size-adjust</code>, <code>ascent-override</code>, <code>descent-override</code> и <code>line-gap-override</code> в отдельном <code>@font-face</code> для локального шрифта позволяют подогнать метрики так, что текст занимает ровно ту же площадь. Fontaine и <code>next/font</code> считают эти коэффициенты автоматически.</p>
    <p><strong>Subsetting</strong> по <code>unicode-range</code> режет файл по алфавитам: браузер скачивает только те подмножества, символы которых реально встречаются на странице. Для кириллического сайта это разница между 120 КБ и 25 КБ. <strong>Вариативный шрифт</strong> заменяет 4–6 начертаний одним файлом — выгодно, если нужны хотя бы три из них, иначе статические начертания легче.</p>

    <h4>Почему так</h4>
    <p>Спецификация не может выбрать за вас между «текст появится позже» и «текст дёрнется»: это продуктовое решение. Новостному сайту важнее показать текст немедленно (<code>swap</code>), интерфейсу с плотной вёрсткой — не дёргать раскладку (<code>optional</code> плюс подогнанный fallback). <code>optional</code> при этом даёт лучший CLS ценой того, что первый визит вообще пройдёт системным шрифтом.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Self-hosting вместо Google Fonts</strong>: сторонний домен стоит лишних DNS + TCP + TLS, а общий кеш между сайтами исчез — с 2020 года браузеры партиционируют HTTP-кеш по сайту-верхнего уровня. Аргумент «шрифт уже у пользователя в кеше» больше не работает.</li>
    <li><code>preload</code> шрифта обязателен с <code>crossorigin</code>, иначе будет вторая загрузка. И не больше одного-двух: шрифты конкурируют за полосу с LCP-картинкой.</li>
    <li>Шрифт, объявленный в CSS, но не использованный ни одним элементом, не скачивается вовсе — а вот <code>preload</code> его скачает. Отсюда предупреждения «preloaded but not used».</li>
    <li>Формат: только <code>woff2</code>. <code>woff</code>, <code>ttf</code> и <code>eot</code> в списке <code>src</code> — легаси, которое сегодня только путает.</li>
    <li>Иконочные шрифты дают квадратики при FOIT и ломают доступность; заменяйте на SVG-спрайты.</li>
    <li><code>document.fonts.ready</code> и Font Loading API позволяют управлять моментом подмены вручную — но самодельный лоадер почти всегда хуже, чем <code>font-display</code>.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как измерить эффект?</strong> Сравнить CLS и время до отрисовки текста в трейсе, а в поле — атрибуцию LCP: если LCP-элемент текстовый, шрифт в критическом пути. <strong>Что даёт system font stack?</strong> Ноль загрузок и ноль сдвига — для интерфейсных элементов это часто лучший выбор. <strong>Сколько начертаний реально нужно?</strong> Обычно два: regular и полужирный. Каждое дополнительное — это отдельный файл в критическом пути, а синтетическое начертание браузера почти всегда выглядит хуже, чем кажется дизайнеру. <strong>Как проверить, что fallback подогнан?</strong> Записать трейс с медленной сетью и посмотреть Layout Shifts вокруг момента загрузки шрифта: при верных <code>size-adjust</code> и <code>ascent-override</code> сдвига там быть не должно вовсе, а не «почти не должно».</p>`,
    code: `@font-face { font-family: 'Inter'; src: url('/inter.woff2') format('woff2');
  font-display: optional; unicode-range: U+0400-04FF, U+0000-00FF }
@font-face { font-family: 'Inter Fallback'; src: local('Arial');
  size-adjust: 107%; ascent-override: 90%; descent-override: 22% }
body { font-family: 'Inter', 'Inter Fallback', sans-serif }`,
    tip: 'Аргумент про cache partitioning (общий кеш Google Fonts больше не работает) — свежий и убедительный.' },

  { id: 'wx20',
    q: 'Расскажите про CSP: как она устроена и как её реально внедрить?',
    snippet: `Content-Security-Policy:
  default-src 'self';
  script-src 'self' https://cdn.example.com 'unsafe-inline';
  img-src *;
  report-uri /csp-report`,
    a: `<h4>Коротко</h4>
    <p>CSP — заголовок ответа, ограничивающий, откуда можно грузить ресурсы и как выполнять код. Главная цель — сделать XSS <strong>неэксплуатируемым</strong>: даже внедрённый скрипт не запустится, если не проходит политику. Политика в сниппете выше защиты почти не даёт: <code>'unsafe-inline'</code> в <code>script-src</code> обесценивает весь заголовок.</p>

    <h4>Как это работает</h4>
    <p>Браузер разбирает директивы и для каждой загрузки проверяет источник. <code>default-src</code> — фолбэк для тех fetch-директив, которые не заданы явно; на <code>base-uri</code>, <code>form-action</code> и <code>frame-ancestors</code> он <strong>не</strong> распространяется, что регулярно упускают. Основной набор: <code>script-src</code>, <code>style-src</code>, <code>img-src</code>, <code>connect-src</code> (fetch, XHR, WebSocket, sendBeacon, EventSource), <code>font-src</code>, <code>frame-src</code>, <code>worker-src</code>, <code>object-src</code>, <code>manifest-src</code>. Плюс не-fetch директивы: <code>frame-ancestors</code> (кто может встроить вас), <code>base-uri</code>, <code>form-action</code>, <code>require-trusted-types-for</code>, <code>upgrade-insecure-requests</code>.</p>
    <p>Внедрение всегда идёт через <code>Content-Security-Policy-Report-Only</code>: браузер не блокирует, но шлёт отчёты на <code>report-to</code>. Собираете неделю-две, разбираете нарушения, чините свой код, потом переключаете заголовок в блокирующий. Оба заголовка можно держать одновременно: боевой послабее, Report-Only построже — так вы проверяете следующую итерацию на живом трафике.</p>

    <h4>Почему так</h4>
    <p>Модель allowlist-доменов оказалась <strong>слабой</strong>: исследование Google по 1 млн сайтов показало, что около 95% политик обходятся, потому что на разрешённых доменах живут JSONP-эндпоинты и хостинги пользовательских бандлов — атакующему достаточно найти на белом домене адрес, отдающий произвольный JS. Поэтому современная модель — не «список доверенных мест», а «список доверенных <strong>тегов</strong>»: nonce или hash плюс <code>'strict-dynamic'</code>, который распространяет доверие на скрипты, загруженные программно.</p>

    <h4>Подводные камни</h4>
    <ul><li>Три директивы забывают чаще всего: <code>base-uri 'none'</code> (без неё инъекция <code>&lt;base href&gt;</code> перенаправит все относительные скрипты на чужой сервер), <code>object-src 'none'</code> и <code>form-action 'self'</code> (иначе форму можно перенаправить и увести введённые данные).</li>
    <li><code>'unsafe-inline'</code> и <code>'unsafe-eval'</code> сводят пользу к нулю. Если фреймворк требует <code>eval</code> — это повод чинить сборку, а не политику.</li>
    <li>Отчёты завалены шумом от расширений браузера и мобильных прокси: фильтруйте схемы <code>chrome-extension:</code>, <code>moz-extension:</code>, <code>about:</code>, иначе полезные нарушения утонут.</li>
    <li>CSP в <code>&lt;meta&gt;</code> работает, но не поддерживает <code>frame-ancestors</code>, <code>report-uri</code> и sandbox — заголовок предпочтительнее.</li>
    <li>Политика применяется к документу, а не к origin: забыли заголовок на одном роуте — там XSS работает как раньше.</li>
    <li>CSP не заменяет экранирование. Это второй рубеж (defense in depth), и его наличие не оправдывает вставку неочищенных данных в разметку.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как отлаживать?</strong> Консоль показывает нарушенную директиву и заблокированный URL, а <code>SecurityPolicyViolationEvent</code> позволяет отправить то же самое в свой мониторинг. <strong>Сколько стоит?</strong> Заголовок 200–400 байт на каждый ответ плюс генерация nonce; на трафике это заметно меньше, чем один аватар. <strong>С чего начать на живом проекте?</strong> С трёх директив, которые почти ничего не ломают: <code>frame-ancestors</code>, <code>object-src 'none'</code> и <code>base-uri 'none'</code>. Они дают реальную защиту сразу, не требуют трогать скрипты и позволяют показать пользу до того, как начнётся долгая работа над <code>script-src</code>. <strong>Что делать с legacy-страницами?</strong> Разные политики на разные маршруты — это нормально: строгая на новые разделы, ослабленная на старые, с планом схождения.</p>`,
    code: `Content-Security-Policy: default-src 'self'; script-src 'nonce-r4nd0m' 'strict-dynamic' https: 'unsafe-inline'; object-src 'none'; base-uri 'none'; form-action 'self'; report-to csp`,
    tip: 'Ссылка на исследование Google про обходимость allowlist через JSONP — сильный и конкретный аргумент.' },

  { id: 'wx21',
    q: 'Nonce, hash или strict-dynamic: что выбрать и почему?',
    snippet: `// Ответ A (SSR)
Content-Security-Policy: script-src 'nonce-AbC123' 'strict-dynamic'
<script nonce="AbC123" src="/app.js"></script>

// Ответ B (статика на CDN, тот же nonce во всех ответах)
Content-Security-Policy: script-src 'nonce-AbC123' 'strict-dynamic'`,
    a: `<h4>Коротко</h4>
    <p><strong>Nonce</strong> — для динамически рендерящихся страниц, <strong>hash</strong> — для статики на CDN, <strong>strict-dynamic</strong> — добавка к обоим, снимающая проблему бандлеров, которые подгружают чанки программно. Вариант B в сниппете сломан: повторяющийся nonce эквивалентен <code>'unsafe-inline'</code>, потому что атакующий просто прочитает его в HTML и подставит в свой тег.</p>

    <h4>Как это работает</h4>
    <p><strong>Nonce</strong> — случайное значение, которое сервер генерирует <strong>на каждый ответ</strong> и проставляет одновременно в заголовок и в атрибут <code>nonce</code> каждого доверенного тега. Браузер сверяет их и удаляет атрибут из DOM, чтобы его нельзя было прочитать через <code>getAttribute</code> (но <code>script.nonce</code> из того же документа его всё ещё видит). Требование одно: HTML должен генерироваться динамически.</p>
    <p><strong>Hash</strong> (<code>'sha256-…'</code>) — контрольная сумма содержимого инлайн-скрипта, посчитанная на этапе сборки. Работает на полностью статических страницах и совместим с кешированием на CDN. Цена — пересчёт при каждом изменении кода и невозможность покрыть скрипты, содержимое которых зависит от запроса. Хеши применимы и к внешним файлам, если в политике указан <code>'strict-dynamic'</code> либо используется SRI-совместимая проверка.</p>
    <p><strong>strict-dynamic</strong> распространяет доверие: скрипт, прошедший проверку по nonce или hash, может через <code>createElement('script')</code> или <code>import()</code> загрузить другие скрипты, и они выполнятся без проверки источника. Одновременно директива <strong>отключает</strong> все URL-выражения, <code>'self'</code> и <code>https:</code> для <code>script-src</code>. Это её смысл: политика перестаёт зависеть от списка доменов, значит и обходить через JSONP на разрешённом домене становится нечего.</p>

    <h4>Почему так</h4>
    <p>Бандлеры и аналитика повсеместно грузят чанки динамически, а allowlist эти URL перечислить не может. Разработчик в ответ добавлял <code>'unsafe-inline'</code> — и политика умирала. <code>strict-dynamic</code> перевернул модель: доверяем не месту, откуда пришёл код, а <strong>цепочке происхождения</strong> — если корневой скрипт разрешён явно, его потомки тоже.</p>

    <h4>Подводные камни</h4>
    <ul><li>Nonce нельзя переиспользовать между ответами и нельзя класть в кешируемый HTML. Если страница отдаётся с CDN — либо hash, либо подстановка nonce на edge.</li>
    <li>Nonce должен быть криптографически случайным, минимум 128 бит (<code>crypto.randomBytes(16)</code> в base64), и его нельзя логировать.</li>
    <li>Совместимость: пишите <code>'nonce-…' 'strict-dynamic' https: 'unsafe-inline'</code>. Браузеры, знающие <code>strict-dynamic</code>, игнорируют два последних токена; старые игнорируют первые два и получают хотя бы allowlist. Порядок важен.</li>
    <li>Тег, вставленный через <code>innerHTML</code>, не выполняется вовсе — не потому что CSP, а по правилам HTML. Динамические скрипты создавайте через <code>document.createElement</code>.</li>
    <li>Атрибут <code>nonce</code> на <code>&lt;style&gt;</code> работает так же, но <code>strict-dynamic</code> для стилей не определён.</li>
    <li>Nonce не защищает от инъекции <strong>внутрь</strong> уже доверенного скрипта: если ваш код пишет пользовательскую строку в <code>innerHTML</code>, CSP тут бессилен — нужны Trusted Types.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как посчитать hash?</strong> <code>openssl dgst -sha256 -binary file.js | openssl base64 -A</code> или плагин сборщика. <strong>Что делать с inline-стилями от UI-библиотек?</strong> Либо <code>style-src</code> с nonce и переводом библиотеки на CSSOM, либо <code>'unsafe-inline'</code> только для стилей — риск здесь несопоставимо ниже, чем для скриптов. <strong>Как проверить силу своей политики?</strong> Google CSP Evaluator разбирает заголовок и прямо указывает обходимые места: <code>'unsafe-inline'</code> без nonce, allowlist с известными JSONP-эндпоинтами, отсутствие <code>object-src</code> и <code>base-uri</code>. Это быстрее, чем ревью глазами, и даёт формулировки, с которыми можно идти к бэкенду. <strong>Что выбрать для гибридного случая?</strong> Кешируемая оболочка с хешами плюс подстановка nonce на edge — так статика остаётся статикой, а политика остаётся строгой.</p>`,
    code: `const nonce = crypto.randomBytes(16).toString('base64');
res.setHeader('Content-Security-Policy',
  "script-src 'nonce-" + nonce + "' 'strict-dynamic' https: 'unsafe-inline'; object-src 'none'; base-uri 'none'");`,
    tip: 'Ключевая фраза: strict-dynamic отключает allowlist — это и есть его смысл, а не просто «разрешить больше».' },

  { id: 'wx22',
    q: 'Что такое Trusted Types и какую проблему они решают?',
    snippet: `// Найдите уязвимость
function renderComment(comment) {
  document.querySelector('#list').innerHTML += '<li>' + comment.text + '</li>';
}
// CSP: default-src 'self'; script-src 'nonce-abc' 'strict-dynamic'`,
    a: `<h4>Коротко</h4>
    <p>CSP не спасает от <strong>DOM XSS</strong>: в сниппете выше ваш собственный доверенный код пишет пользовательскую строку в <code>innerHTML</code>, и <code>&lt;img src=x onerror=alert(1)&gt;</code> сработает, потому что обработчик события — это не загрузка скрипта, и nonce тут ни при чём. Trusted Types закрывают именно этот класс: они запрещают передавать в опасные sink обычные строки.</p>

    <h4>Как это работает</h4>
    <p>Директива <code>require-trusted-types-for 'script'</code> переводит все DOM XSS sink в строгий режим. Список sink фиксирован спецификацией: <code>innerHTML</code>, <code>outerHTML</code>, <code>insertAdjacentHTML</code>, <code>document.write</code>, <code>eval</code>, <code>Function</code>, <code>setTimeout</code>/<code>setInterval</code> со строкой, <code>script.src</code>, <code>script.text</code>, <code>iframe.srcdoc</code>, <code>Worker</code>, <code>setAttribute</code> для опасных атрибутов. После включения они принимают только объекты <code>TrustedHTML</code>, <code>TrustedScript</code> и <code>TrustedScriptURL</code>; передача строки бросает <code>TypeError</code>.</p>
    <p>Создать такой объект можно исключительно через политику, зарегистрированную явно: <code>trustedTypes.createPolicy(name, { createHTML, createScript, createScriptURL })</code>. Список разрешённых имён политик задаётся директивой <code>trusted-types</code>, а <code>'allow-duplicates'</code> определяет, можно ли регистрировать имя дважды. Есть особая политика <code>default</code>: она вызывается автоматически, когда в sink попала обычная строка, — это спасательный круг для легаси, но злоупотребление ею возвращает вас к исходной точке.</p>
    <p>Внутри политики обычно вызывают DOMPurify. То есть Trusted Types <strong>не чистят HTML</strong> — они гарантируют, что чистка не была пропущена. Весь небезопасный код физически собирается в одном аудируемом файле, который легко покрыть ревью и тестами.</p>

    <h4>Почему так</h4>
    <p>Обычное ревью не масштабируется: в приложении на пару сотен тысяч строк тысячи мест пишут в DOM, и найти одно опасное невозможно ни грепом, ни линтером — линтер не видит, откуда пришла строка. Trusted Types меняют задачу с «проверить все места» на «проверить одну политику»: браузер сам гарантирует, что мимо неё не пройти. Внутри Google это дало кратное падение числа DOM XSS.</p>

    <h4>Подводные камни</h4>
    <ul><li>Внедрение — не флаг, а проект: сначала <code>Content-Security-Policy-Report-Only</code>, сбор нарушений, правка библиотек. Многие старые UI-киты и любые обёртки над jQuery пишут в <code>innerHTML</code> и не имеют режима совместимости.</li>
    <li>Политика <code>default</code>, возвращающая строку как есть, отключает всю защиту. Если её и добавлять, то с санитайзером и с логированием каждого вызова.</li>
    <li>Trusted Types не защищают от <code>javascript:</code> в <code>href</code> и от <code>on*</code>-атрибутов, добавленных не через sink — за это отвечает санитайзер.</li>
    <li>Стороннему коду (реклама, чаты, аналитика) политика мешает: он либо ломается, либо просит зарегистрировать свою политику. Это повод вынести его в изолированный iframe.</li>
    <li>Поддержка: Chromium давно, Firefox и Safari подтянулись позже. В неподдерживающих браузерах директива — no-op, поэтому внедрять безопасно, но и полагаться на неё как на единственную защиту нельзя.</li>
    <li>React с <code>dangerouslySetInnerHTML</code> и Vue с <code>v-html</code> проходят через те же sink — обёртки над ними тоже надо переводить на политику.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что такое Sanitizer API?</strong> Нативный <code>element.setHTML(str)</code>, который парсит и вычищает разметку по встроенному списку без сторонней библиотеки; в связке с Trusted Types это самый короткий безопасный путь. <strong>Чем это отличается от экранирования?</strong> Экранирование превращает разметку в текст, санитайзинг оставляет безопасную разметку — для комментариев и WYSIWYG нужен именно второй вариант. <strong>Что делать с существующим кодом?</strong> Начать с Report-Only и посчитать нарушения: обычно их десятки, а не тысячи, и половина приходится на две-три общие утилиты. <strong>Не медленно ли это?</strong> Санитайзинг стоит десятые доли миллисекунды на типичный фрагмент; заметно это становится только при массовой вставке в цикле — там правильнее собирать разметку один раз, а не вызывать политику на каждый элемент.</p>`,
    code: `// небезопасно
el.innerHTML = userText;
// безопасно
const policy = trustedTypes.createPolicy('app', { createHTML: (s) => DOMPurify.sanitize(s) });
el.innerHTML = policy.createHTML(userText);
// или нативно
el.setHTML(userText);`,
    tip: 'Формулировка «Trusted Types не чистят HTML, а гарантируют, что чистка вызвана» точно передаёт суть.' },

  { id: 'wx23',
    q: 'Что делает HSTS и в чём риск preload-списка?',
    snippet: `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`,
    a: `<h4>Коротко</h4>
    <p>Заголовок велит браузеру ходить на домен <strong>только</strong> по HTTPS в течение <code>max-age</code>, даже если пользователь набрал <code>http://</code> или кликнул по http-ссылке. Это закрывает SSL-stripping. <code>preload</code> в сниппете — заявка на включение в вшитый в браузеры список, и это решение практически необратимое.</p>

    <h4>Как это работает</h4>
    <p>Заголовок принимается браузером только по HTTPS-соединению с валидным сертификатом — по http его игнорируют, иначе злоумышленник мог бы поставить чужому домену <code>max-age</code> на десять лет и устроить DoS. Получив заголовок, браузер записывает домен в свой HSTS-кеш. Дальше любая попытка перехода по http превращается во <strong>внутренний</strong> редирект 307 ещё до выхода в сеть: в DevTools он виден как «Non-Authoritative-Reason: HSTS». <code>includeSubDomains</code> распространяет правило на все поддомены рекурсивно. <code>max-age</code> обновляется при каждом успешном ответе, то есть у активных пользователей срок фактически скользящий.</p>
    <p>Остаётся дыра <strong>первого визита</strong>: до первого HTTPS-ответа браузер о политике не знает, и этот единственный запрос можно перехватить. Её закрывает preload-список hstspreload.org, который компилируется прямо в бинарники Chromium, Firefox, Safari и Edge. Требования: валидный сертификат, редирект всего http-трафика на https на том же хосте, HTTPS на всех поддоменах, <code>max-age</code> не меньше года, <code>includeSubDomains</code> и токен <code>preload</code>.</p>

    <h4>Почему так</h4>
    <p>Пользователь почти никогда не набирает схему: он пишет «site.ru», и браузер исторически шёл по http. Один незашифрованный запрос — достаточная точка входа для подмены ответа и кражи сессии в открытом Wi-Fi. HSTS — это способ сделать «только HTTPS» свойством домена, а не свойством отдельного запроса. Preload-список нужен потому, что механизм самозагружающийся: политика доставляется по тому самому каналу, который она защищает.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Необратимость preload</strong>: удаление из списка занимает месяцы и доезжает до пользователей только со сменой версии браузера. Если внутренний сервис, dev-стенд, IoT-устройство или почтовый шлюз на поддомене не умеет HTTPS, он станет недоступен для всех, и быстро откатить это нельзя.</li>
    <li>Правильная последовательность: <code>max-age=300</code> → проверить абсолютно все поддомены, включая забытые → поднять до недели → до года → добавить <code>includeSubDomains</code> → выждать → только затем <code>preload</code>.</li>
    <li>Откат тоже требует времени: чтобы снять HSTS у уже посетивших, надо какое-то время отдавать <code>max-age=0</code> и дождаться, пока все зайдут.</li>
    <li>HSTS не защищает от <strong>скомпрометированного или ошибочно выданного сертификата</strong>: соединение всё так же будет HTTPS. Для этого нужны CAA-записи в DNS и мониторинг Certificate Transparency.</li>
    <li>HPKP (закрепление ключей) признан вредным и удалён из браузеров: одна ошибка в ротации ключа выводила домен из строя на весь срок пиннинга.</li>
    <li>Заголовок нужно отдавать на HTTPS-ответах, а не на редиректе с http — на редиректе он не будет прочитан.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как проверить статус?</strong> <code>chrome://net-internals/#hsts</code> позволяет посмотреть и удалить запись для домена локально — незаменимо при отладке. <strong>Нужен ли HSTS, если весь трафик и так по HTTPS?</strong> Да: без него первый запрос всё равно уходит по http и перехватывается. <strong>Что такое upgrade-insecure-requests?</strong> Директива CSP, которая переписывает http-ссылки внутри вашей страницы, — она про подресурсы, а не про навигацию, и HSTS не заменяет. <strong>Как это выглядит для пользователя?</strong> Никак: внутренний редирект происходит до сетевого запроса и не добавляет задержки — наоборот, убирает один round-trip по сравнению с обычным 301. <strong>Есть ли риск для сайта без поддоменов?</strong> Практически нет: без <code>includeSubDomains</code> и без <code>preload</code> HSTS — это почти бесплатная мера, которую стоит включить сразу.</p>`,
    code: `# 1) обкатка
Strict-Transport-Security: max-age=300
# 2) через месяц проверок
Strict-Transport-Security: max-age=63072000; includeSubDomains
# 3) только когда уверены навсегда
Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`,
    tip: 'Необратимость preload-списка и риск сломать внутренние поддомены — это то, что должен знать senior.' },

  { id: 'wx24',
    q: 'Что такое clickjacking и как от него защищаться?',
    snippet: `<!-- страница атакующего -->
<h1>Кликните, чтобы получить приз</h1>
<button>Получить</button>
<iframe src="https://bank.example/transfer?to=attacker"
        style="opacity:0; position:absolute; inset:0; width:100%; height:100%"></iframe>`,
    a: `<h4>Коротко</h4>
    <p><strong>Что делает атака:</strong> ваш сайт встраивается прозрачным iframe поверх приманки, пользователь думает, что жмёт кнопку атакующего, а физически попадает по кнопке в вашем интерфейсе — и действие выполняется от его имени, с его сессией. <strong>Что останавливает:</strong> заголовок, запрещающий встраивание, то есть <code>frame-ancestors</code> в CSP, а как легаси-дубль — <code>X-Frame-Options</code>.</p>

    <h4>Как это работает</h4>
    <p>Атакующий размещает ваш документ в <code>iframe</code> с нулевой прозрачностью и позиционирует его так, чтобы нужная кнопка оказалась ровно под визуальной приманкой. Браузер отправляет клик в фрейм, а вместе с запросом уходят ваши куки, потому что это обычная навигация внутри фрейма. Разновидности: likejacking (лайк и подписка), cursorjacking (подменённый курсор смещает восприятие точки клика), атаки с drag-and-drop для вытаскивания выделенного текста из фрейма, и «двойной клик» на диалогах OAuth-согласия.</p>
    <p><strong>Что именно останавливает атаку.</strong> <code>Content-Security-Policy: frame-ancestors 'none'</code> — браузер отказывается рендерить документ во фрейме, проверяя <strong>всю цепочку</strong> вложенных фреймов, а не только непосредственного родителя. Директива принимает список источников, поэтому «можно партнёру, нельзя остальным» выражается точно. <code>X-Frame-Options: DENY|SAMEORIGIN</code> делает почти то же для старых браузеров, но у него нет списка источников: <code>ALLOW-FROM</code> никогда не поддерживался в Chrome и удалён из Firefox. Держат оба заголовка; при конфликте современные браузеры отдают приоритет <code>frame-ancestors</code>.</p>

    <h4>Почему так</h4>
    <p>Same-Origin Policy запрещает атакующему <strong>читать</strong> содержимое вашего фрейма, но не запрещает его <strong>показывать</strong> и не запрещает пользователю по нему кликать. Клик — это ввод пользователя, и браузер не может отличить осознанный клик от обманутого. Поэтому решение вынесено на уровень сервера: только вы знаете, легитимно ли встраивание вашей страницы.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Когда защита не срабатывает.</strong> Заголовок поставлен не на всех роутах: достаточно одной забытой страницы с опасным действием. Заголовок отдаётся только на HTML-ответ — если приложение SPA и опасное действие делается по API, атакующему хватит любой встраиваемой страницы.</li>
    <li>Ошибка вида <code>frame-ancestors *</code> или <code>X-Frame-Options: ALLOWALL</code> (несуществующее значение, трактуется как отсутствие) — защита выключена, а выглядит как включённая.</li>
    <li>JS frame-busting (<code>if (top !== self) top.location = self.location</code>) обходится атрибутом <code>sandbox</code> без <code>allow-top-navigation</code>: скрипт бессилен изменить адрес верхнего окна. Полагаться на него нельзя.</li>
    <li><strong>SameSite=Lax|Strict</strong> — второй рубеж: во фрейме чужого сайта запрос кросс-сайтовый, сессионная кука не уйдёт, и действие выполнится от анонима. Но <code>SameSite=None</code>, нужный виджетам и SSO, эту защиту снимает.</li>
    <li>Для критичных операций нужен ещё один слой: повторное подтверждение с вводом суммы или кода — вслепую его не прокликать.</li>
    <li>Заголовок надо отдавать и на страницах ошибок, и на редиректах: 302 без заголовка может быть встроен и промежуточно показан.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что если нам нужно встраиваться в партнёров?</strong> Явный список в <code>frame-ancestors</code> плюс проверка <code>Sec-Fetch-Dest: iframe</code> и <code>Origin</code> на сервере для опасных операций. <strong>Как проверить?</strong> Простая страничка с iframe на локалхосте: если она отрисовалась — защиты нет. <strong>А если атака идёт не через iframe?</strong> Родственный класс — tapjacking на мобильных и подмена курсора: помогает то же требование явного подтверждения для критичных действий. <strong>Что с popup-окнами?</strong> Открытое через <code>window.open</code> окно может управлять открывшей его страницей через <code>window.opener</code>; закрывается атрибутом <code>rel="noopener"</code> (по умолчанию для <code>target="_blank"</code> в современных браузерах) и заголовком <code>Cross-Origin-Opener-Policy: same-origin</code>.</p>`,
    code: `Content-Security-Policy: frame-ancestors 'self' https://partner.ru
X-Frame-Options: SAMEORIGIN`,
    tip: 'Скажите, что frame-ancestors переопределяет X-Frame-Options и что оба всё ещё ставят вместе.' },

  { id: 'wx25',
    q: 'Что делают Referrer-Policy и Permissions-Policy?',
    snippet: `Referrer-Policy: unsafe-url
Permissions-Policy: geolocation=*, camera=*

<!-- на странице -->
https://app.example/reset?token=9f3c1a...  →  ссылка на https://help.thirdparty.com`,
    a: `<h4>Коротко</h4>
    <p><strong>Referrer-Policy</strong> управляет тем, сколько информации об исходной странице уходит в заголовке <code>Referer</code>. В сниппете <code>unsafe-url</code> означает, что полный URL со <strong>токеном сброса пароля</strong> уедет на сторонний домен в логи их веб-сервера. <strong>Permissions-Policy</strong> выключает браузерные возможности для документа и его фреймов; <code>camera=*</code> здесь раздаёт камеру любому встроенному виджету.</p>

    <h4>Как это работает</h4>
    <p>Referer отправляется при переходах по ссылкам, при загрузке подресурсов и при fetch. Значения по возрастанию строгости: <code>unsafe-url</code> (полный URL всегда), <code>no-referrer-when-downgrade</code> (всё, кроме перехода с https на http), <code>origin</code>, <code>origin-when-cross-origin</code>, <code>strict-origin-when-cross-origin</code>, <code>same-origin</code>, <code>strict-origin</code>, <code>no-referrer</code>. Современные браузеры по умолчанию используют <code>strict-origin-when-cross-origin</code>: свой origin получает полный URL, чужой — только схему и хост, при downgrade на http не отправляется ничего. Настраивается заголовком, тегом <code>&lt;meta name="referrer"&gt;</code> или атрибутом <code>referrerpolicy</code> на конкретной ссылке, картинке или iframe — точечное послабление обычно лучше глобального.</p>
    <p><strong>Permissions-Policy</strong> (бывший Feature-Policy) перечисляет возможности и allowlist для каждой: <code>camera=()</code> — запрещено всем, <code>geolocation=(self)</code> — только своему документу, <code>payment=(self "https://pay.example")</code> — плюс конкретному origin. Атрибут <code>allow</code> на <code>&lt;iframe&gt;</code> делегирует разрешение вниз, но <strong>никогда не расширяет</strong> то, что запрещено сверху: политика документа — потолок для всех вложенных контекстов.</p>

    <h4>Почему так</h4>
    <p>Referer появился в эпоху, когда URL не считался секретом, и по инерции продолжает утекать: id заказа, поисковый запрос, токен приглашения, имя пользователя в пути. Браузеры уже сдвинули дефолт в сторону приватности, но <code>unsafe-url</code> всё ещё встречается в старых конфигах и в рекомендациях аналитических систем. Permissions-Policy решает другую задачу: <strong>ограничение стороннего кода</strong>. Даже скомпрометированный виджет или встроенная реклама не смогут запросить камеру или геолокацию, если возможность выключена на уровне документа — это работает и тогда, когда сам виджет вам неподконтролен.</p>

    <h4>Подводные камни</h4>
    <ul><li>Секреты в URL — корень проблемы, а Referrer-Policy лишь смягчает последствия. Токен сброса пароля должен быть одноразовым и короткоживущим, а лучше передаваться через POST-форму.</li>
    <li>Полностью убирать реферер не всегда правильно: часть партнёрских программ, антифрод-систем и аналитик на нём держится, а <code>no-referrer</code> ломает и корректный переход в вашу же CDN-подпись.</li>
    <li>Referer не отправляется при downgrade и из <code>rel="noreferrer"</code>, но по-прежнему уходит с картинок и шрифтов на чужих доменах.</li>
    <li>Permissions-Policy с пустым allowlist выключает возможность так, что даже собственный код получит отказ, — проверьте, что вы не выключили то, чем пользуетесь (частая история с <code>fullscreen</code> у видеоплеера).</li>
    <li>Синтаксис отличается от старого Feature-Policy (<code>camera 'none'</code> против <code>camera=()</code>) — скопированные из старых статей заголовки молча игнорируются.</li>
    <li>Оба заголовка не про запрет на сервере: они меняют поведение браузера, и клиент без их поддержки просто их не увидит.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как отследить нарушения?</strong> Permissions-Policy умеет отчёты через Reporting API — видно, какой фрейм запросил что. <strong>Что ещё стоит выключить?</strong> <code>interest-cohort</code>, датчики, <code>autoplay</code>, <code>usb</code>, <code>serial</code> — всё, чем вы не пользуетесь. <strong>Куда их ставить?</strong> Оба заголовка удобно централизовать на уровне обратного прокси или edge-функции: тогда они гарантированно есть на всех маршрутах, включая статику и страницы ошибок, и не зависят от того, вспомнил ли о них конкретный обработчик. <strong>Как проверить?</strong> Securityheaders.com и вкладка Headers в DevTools дают быстрый ответ, а Reporting API — данные с реального трафика.</p>`,
    code: `Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=(self), payment=(self "https://pay.example")`,
    tip: 'Пример с токеном сброса пароля в URL, утекающим через Referer, — самая наглядная мотивация.' },

  { id: 'wx26',
    q: 'Что именно закрывает Same-Origin Policy и чего она не закрывает?',
    a: `<h4>Коротко</h4>
    <p>Origin — это тройка схема + хост + порт. SOP запрещает документу <strong>читать</strong> данные другого origin. Она не запрещает <strong>отправлять</strong> запросы и не запрещает встраивать чужие ресурсы — и именно из этого зазора растут CSRF и XS-Leaks.</p>

    <h4>Как это работает</h4>
    <p>Под запрет на чтение попадают: DOM чужого фрейма и чужого окна, тело кросс-доменного ответа в JS, куки и <code>localStorage</code> другого origin, содержимое <code>canvas</code>, «испорченного» рисованием чужой картинки без CORS, пиксели чужого видео, детальные тайминги в Resource Timing.</p>
    <p>Не запрещено, и это главная часть ответа:</p>
    <ul><li><strong>Отправка запросов.</strong> Форма с <code>method="POST"</code>, <code>&lt;img src&gt;</code>, <code>&lt;script src&gt;</code>, <code>fetch(url, { mode: 'no-cors' })</code> уходят на чужой origin вместе с куками. Ответ вы не прочитаете, но побочный эффект на сервере уже произошёл — это и есть CSRF.</li>
    <li><strong>Встраивание.</strong> Картинки, скрипты, стили, шрифты, фреймы и медиа грузятся кросс-доменно по умолчанию: веб на этом построен.</li>
    <li><strong>Навигация.</strong> Ссылки, <code>window.open</code>, изменение <code>location</code> дочернего окна на запись.</li>
    <li><strong>Ограниченное взаимодействие окон.</strong> <code>postMessage</code>, <code>window.length</code>, <code>window.frames</code>, <code>window.closed</code>, факт успешной или неуспешной загрузки.</li>
    <li><strong>Побочные каналы.</strong> Время загрузки, наличие события <code>error</code>, размеры отрисованного фрейма, поведение кеша — на этом строятся XS-Leaks, позволяющие узнать, авторизован ли пользователь или есть ли у него доступ к ресурсу.</li></ul>

    <h4>Почему так</h4>
    <p>SOP появилась в 1995 году как минимальный компромисс: полностью запретить кросс-доменные обращения нельзя — рухнут ссылки, картинки и реклама, то есть сам веб. Поэтому изолировали то, что защищает данные: чтение. Отправка осталась разрешённой, и уже поверх этого пришлось надстраивать SameSite-куки, CSRF-токены и Fetch Metadata. Понимание этого исторического зазора и есть разница между «знаю термин» и «понимаю модель».</p>

    <h4>Подводные камни</h4>
    <ul><li><code>document.domain</code> раньше позволял двум поддоменам «договориться» об общем origin. Механизм признан опасным, по умолчанию отключён в Chromium и требует заголовка <code>Origin-Agent-Cluster: ?0</code>. В новом коде его быть не должно.</li>
    <li>Поддомены — <strong>разные</strong> origin для SOP, но <strong>один</strong> сайт для куки: <code>a.site.ru</code> не прочитает DOM <code>b.site.ru</code>, но кука с <code>Domain=site.ru</code> уйдёт обоим. Путаница между origin и site — источник половины ошибок в этой теме.</li>
    <li><code>localhost</code> и <code>127.0.0.1</code> — разные origin, и порт входит в origin: <code>:3000</code> и <code>:3001</code> изолированы полностью.</li>
    <li><code>file://</code> ведёт себя особым образом и в современных браузерах считает каждый файл отдельным origin — отсюда «работает на сервере, не работает по двойному клику».</li>
    <li>Ослабляют и усиливают SOP разные механизмы: CORS даёт контролируемое разрешение на чтение, CORP/COEP/COOP изолируют процесс и включают <code>SharedArrayBuffer</code>, атрибут <code>crossorigin</code> переводит подресурс в CORS-режим, Fetch Metadata (<code>Sec-Fetch-Site</code>, <code>Sec-Fetch-Mode</code>, <code>Sec-Fetch-Dest</code>) позволяет серверу отклонить кросс-сайтовый запрос ещё до бизнес-логики.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Почему JSONP работал?</strong> Потому что <code>&lt;script&gt;</code> встраивать можно, а ответ исполняется как код — SOP это не нарушает и потому же делает JSONP-эндпоинты дырой в CSP-allowlist. <strong>Что такое site vs origin?</strong> Site — это eTLD+1 (<code>site.ru</code>), origin — схема, хост и порт целиком; SameSite оперирует первым, SOP — вторым. <strong>Зачем нужны COOP и COEP?</strong> Они дают <strong>cross-origin isolation</strong>: страница попадает в отдельный процесс без чужих документов, и только тогда браузер возвращает точные таймеры и <code>SharedArrayBuffer</code>, отобранные после Spectre. Цена — все подресурсы обязаны явно разрешить встраивание через <code>Cross-Origin-Resource-Policy</code> или CORS, и сторонние виджеты обычно ломаются. <strong>Почему CORS не «отключает» SOP?</strong> Он не снимает запрет, а даёт серверу способ выборочно разрешить чтение конкретному origin — решение принимает владелец данных, а не тот, кто их запрашивает.</p>`,
    code: `// уйдёт на чужой сервер вместе с куками, ответ прочитать нельзя
fetch('https://bank.example/transfer', { method: 'POST', mode: 'no-cors', credentials: 'include', body });
// сервер должен отклонить это сам
if (req.headers['sec-fetch-site'] === 'cross-site') return res.status(403).end();`,
    tip: 'Акцент «SOP запрещает читать, но не запрещает отправлять» — прямой мост к объяснению CSRF.' },

  { id: 'wx27',
    q: 'Разберите SameSite-cookie подробно. Что изменилось и где ловушки?',
    snippet: `Set-Cookie: sid=abc; HttpOnly; Secure; SameSite=None
Set-Cookie: theme=dark; SameSite=Strict
Set-Cookie: csrf=xyz; SameSite=None`,
    a: `<h4>Коротко</h4>
    <p><code>SameSite</code> определяет, отправлять ли куку в кросс-сайтовом запросе. Первая кука в сниппете корректна, если это виджет или SSO; вторая разлогинит пользователя при переходе из поиска; третья <strong>будет отброшена браузером</strong> — <code>SameSite=None</code> без <code>Secure</code> невалиден.</p>

    <h4>Как это работает</h4>
    <ul><li><strong>Strict</strong> — кука уходит только при запросах, инициированных тем же сайтом. Максимальная защита, но при переходе по внешней ссылке первый запрос идёт без куки, и пользователь видит себя неавторизованным. Отсюда типовая жалоба «зашёл из поисковика — меня разлогинило».</li>
    <li><strong>Lax</strong> — кука отправляется дополнительно при <strong>навигации верхнего уровня методом GET</strong>: клик по ссылке работает, а вот POST-форма с чужого сайта, запрос из <code>&lt;img&gt;</code>, из iframe и любой fetch — нет. Это значение по умолчанию в современных браузерах.</li>
    <li><strong>None</strong> — отправляется всегда, но обязателен атрибут <code>Secure</code>, иначе браузер молча отбрасывает <code>Set-Cookie</code>. Нужен встраиваемым виджетам, SSO-редиректам и кросс-доменным API.</li></ul>
    <p>Ключевое понятие — <strong>site</strong>, и это не origin, а eTLD+1: <code>a.site.ru</code> и <code>b.site.ru</code> — один сайт, а <code>site.ru</code> и <code>site.com</code> — разные. Список eTLD берётся из Public Suffix List, поэтому <code>user1.github.io</code> и <code>user2.github.io</code> считаются разными сайтами.</p>

    <h4>Почему так</h4>
    <p>Куки придумали до того, как появился кросс-доменный веб, и правило «отправлять всегда» сделало CSRF структурной проблемой. Полностью запретить кросс-сайтовые куки нельзя было из-за встраиваемых сервисов и SSO, поэтому выбрали компромисс: Lax по умолчанию блокирует всё, кроме безопасной по семантике навигации GET. Это ломает меньше всего сценариев и убирает большинство CSRF-векторов бесплатно.</p>

    <h4>Подводные камни</h4>
    <ul><li>Lax <strong>не защищает</strong> от CSRF через GET. Если у вас есть изменяющий состояние GET-эндпоинт (<code>/logout</code>, <code>/subscribe?id=…</code>), атака работает при любом SameSite.</li>
    <li>SameSite не изолирует <strong>поддомены</strong>: скомпрометированный <code>blog.site.ru</code> шлёт запросы на <code>app.site.ru</code> как same-site. Для изоляции используют префикс <code>__Host-</code> — он требует <code>Secure</code>, запрещает атрибут <code>Domain</code> и требует <code>Path=/</code>, поэтому кука привязывается ровно к одному хосту.</li>
    <li>Chrome применял двухминутную поблажку «Lax + POST» для свежевыданных кук, чтобы не ломать OAuth-редиректы. На неё нельзя проектировать: она может исчезнуть в любой версии.</li>
    <li>SameSite — <strong>не замена</strong> CSRF-токенам: он бесполезен при XSS, при поддомене под контролем атакующего, при браузере без поддержки и при <code>SameSite=None</code>, который вам всё равно понадобится для части сценариев.</li>
    <li>Старые браузеры (Safari 12, ряд встроенных WebView) трактовали <code>None</code> как <code>Strict</code> — если поддерживаете их, нужен двойной <code>Set-Cookie</code> с определением UA.</li>
    <li>Сторонние куки в Chrome и так ограничиваются: виджетам нужен CHIPS (<code>Partitioned</code>, отдельная кука на каждый встраивающий сайт) или Storage Access API с явным запросом у пользователя.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Какая схема правильная сегодня?</strong> Сессия в <code>__Host-</code> куке с <code>HttpOnly; Secure; SameSite=Lax</code>, плюс проверка <code>Origin</code> или <code>Sec-Fetch-Site</code> на сервере для всех небезопасных методов, плюс CSRF-токен там, где нужна максимальная строгость. <strong>Почему не Strict для сессии?</strong> Из-за перехода по внешним ссылкам; компромисс — Strict для «опасной» куки и Lax для куки «я узнал пользователя». <strong>Как это отлаживать?</strong> Вкладка Application → Cookies показывает атрибуты каждой куки, а панель Issues в DevTools отдельно предупреждает о куках, отброшенных из-за <code>SameSite=None</code> без <code>Secure</code> или заблокированных как сторонние. Часто «сессия не сохраняется» объясняется ровно одной строкой оттуда. <strong>Что такое CHIPS на практике?</strong> Кука с атрибутом <code>Partitioned</code> хранится отдельно для каждого встраивающего сайта: виджет продолжает работать, но не может отследить пользователя между разными сайтами.</p>`,
    code: `Set-Cookie: __Host-sid=...; HttpOnly; Secure; SameSite=Lax; Path=/
// сервер, до бизнес-логики:
const site = req.headers['sec-fetch-site'];
if (req.method !== 'GET' && site && site !== 'same-origin') return res.status(403).end();`,
    tip: 'Уточнение, что site = eTLD+1 и поддомены не изолированы, — деталь, отличающая понимание от заучивания.' },

  { id: 'wx28',
    q: 'Что такое prototype pollution и как она эксплуатируется во фронтенде?',
    snippet: `function merge(target, source) {
  for (const key in source) {
    if (typeof source[key] === 'object') merge(target[key] ??= {}, source[key]);
    else target[key] = source[key];
  }
  return target;
}
merge(options, JSON.parse(location.hash.slice(1)));
// #{"__proto__":{"sanitize":false}}`,
    a: `<h4>Коротко</h4>
    <p><strong>Что делает атака:</strong> код рекурсивно копирует пользовательские данные в объект и не фильтрует ключ <code>__proto__</code>, поэтому запись уходит не в объект, а в <code>Object.prototype</code>. После этого <strong>все</strong> объекты приложения получают подставленное свойство. <strong>Что останавливает:</strong> отбрасывание опасных ключей при мердже, <code>Object.create(null)</code> для словарей и схема-валидация входа.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>target[key]</code> при <code>key === '__proto__'</code> обращается не к собственному свойству, а к прототипу объекта, потому что <code>__proto__</code> — это аксессор, определённый на <code>Object.prototype</code>. Рекурсия проваливается внутрь прототипа и пишет туда <code>sanitize: false</code>. Дальше любая проверка вида <code>if (!options.sanitize)</code> в любом месте приложения начинает возвращать значение атакующего — даже там, где объект создан пустым литералом. Аналогичный эффект даёт цепочка <code>constructor.prototype</code>.</p>
    <p>Эксплуатация на клиенте идёт через «гаджеты» — места, где код читает свойство с фолбэком по умолчанию. Классика: загрязнение опций санитайзера или шаблонизатора превращается в DOM XSS; подмена флага фичи открывает скрытый функционал; свойство <code>src</code> или <code>onerror</code> в объекте, из которого потом строится разметка, даёт прямое выполнение кода. Вектор часто приходит прямо из URL: <code>?__proto__[onerror]=alert(1)</code>, если query-строка парсится наивным парсером.</p>

    <h4>Почему так</h4>
    <p>Прототипное наследование в JS устроено так, что «нет собственного свойства» и «есть унаследованное» неразличимы при обычном чтении через точку. Это удобно для методов и катастрофично для данных: словарь, построенный на обычном объекте, изначально содержит десяток чужих ключей (<code>toString</code>, <code>constructor</code>, <code>hasOwnProperty</code>), и разделить «данные» и «наследство» язык не помогает. Отсюда правило: объект как словарь — только через <code>Object.create(null)</code> или <code>Map</code>.</p>

    <h4>Подводные камни</h4>
    <ul><li><code>JSON.parse</code> сам по себе безопасен: он создаёт собственное свойство <code>__proto__</code> как обычный ключ и прототип не меняет. Опасен именно последующий рекурсивный merge — это важное уточнение, на котором многие сыплются.</li>
    <li>Исторические CVE: lodash <code>merge</code> и <code>set</code>, jQuery <code>extend</code>, minimist, querystring-парсеры, Handlebars, ряд конфиг-лоадеров. Проверять надо не только свой код, но и зависимости.</li>
    <li>Проверка <code>if (key in target)</code> вместо <code>Object.hasOwn(target, key)</code> сама становится гаджетом: загрязнённый прототип отвечает «есть» на любое имя.</li>
    <li><code>Object.freeze(Object.prototype)</code> — радикальная и рабочая мера, но ломает библиотеки, дописывающие полифиллы в прототип; включать только после прогона тестов.</li>
    <li>Загрязнение может произойти на одной странице SPA, а выстрелить на другой — воспроизвести баг по одному экрану не получится.</li>
    <li>На сервере в Node то же самое приводит к RCE через gadget-цепочки (например, подмена <code>shell</code> в опциях <code>child_process</code>), так что это не «безобидный фронтовый баг».</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как найти у себя?</strong> Проверить <code>({}).polluted</code> после прогона сценариев, поискать в коде рекурсивные merge и <code>for…in</code> без <code>hasOwn</code>, прогнать зависимости через <code>npm audit</code>. <strong>Чем отличается от XSS?</strong> Prototype pollution сама по себе не выполняет код — она меняет поведение приложения, а выполнение достигается через гаджет; поэтому опасность зависит от того, что ещё есть в вашем коде. <strong>Есть ли встроенная защита?</strong> Частично: <code>Object.groupBy</code>, <code>Map</code> и объекты с <code>null</code>-прототипом безопасны по построению, а современные парсеры query-строк отбрасывают <code>__proto__</code>. Но структурное решение — валидация входа по схеме (zod, ajv), которая просто не пропускает неизвестные ключи внутрь приложения. <strong>Почему это до сих пор встречается?</strong> Потому что уязвим не редкий экзотический код, а самая обычная функция глубокого слияния настроек, которую пишут в каждом втором проекте.</p>`,
    code: `const BAD = new Set(['__proto__', 'constructor', 'prototype']);
function safeMerge(target, source) {
  for (const key of Object.keys(source)) {
    if (BAD.has(key)) continue;
    // ...
  }
}
const dict = Object.create(null); // словарь без прототипа`,
    tip: 'Пример вектора через query-строку и последующий DOM XSS показывает, что вы понимаете цепочку эксплуатации.' },

  { id: 'wx29',
    q: 'Как вы защищаете фронтенд от supply chain атак в npm?',
    snippet: `{
  "dependencies": { "chalk": "^5.3.0", "left-pad": "*" },
  "scripts": { "postinstall": "node ./node_modules/analytics/setup.js" }
}
// npm install без lockfile, CI с правом публикации в том же job`,
    a: `<h4>Коротко</h4>
    <p><strong>Что делает атака:</strong> вредоносный код попадает в сборку не через ваш репозиторий, а через зависимость — тайпсквоттинг, угнанный токен мейнтейнера, вредоносное обновление давно доверенного пакета или скрипт <code>postinstall</code>, который исполняется на машине разработчика и в CI с полными правами. <strong>Что останавливает:</strong> lockfile, запрет install-скриптов, задержка обновлений и минимальные права токенов в CI.</p>

    <h4>Как это работает</h4>
    <p>В сниппете три проблемы сразу. <code>"*"</code> в версии означает «любая», то есть следующая установка притащит что угодно. Отсутствие lockfile делает сборку невоспроизводимой: <code>^5.3.0</code> завтра станет 5.9.0 вместе с транзитивным деревом. <code>postinstall</code> выполняет произвольный код на этапе установки — исторически именно там срабатывало большинство атак (event-stream, ua-parser-js, colors, серия атак 2024–2025 через угнанные токены публикации и через самокопирующийся червь в пакетах).</p>
    <p>Слои защиты выстраиваются так. <strong>Воспроизводимость</strong>: lockfile коммитится, в CI только <code>npm ci</code> или <code>pnpm install --frozen-lockfile</code>, изменения lockfile обязательно смотрят в ревью — там видно и новый пакет, и смену registry. <strong>Изоляция установки</strong>: <code>--ignore-scripts</code> по умолчанию, у pnpm — <code>onlyBuiltDependencies</code> с явным списком тех, кому скрипты нужны (esbuild, sharp). <strong>Время</strong>: Renovate или Dependabot с минимальным возрастом версии — не ставить релиз в первые несколько дней; большинство вредоносных публикаций снимают из реестра за часы. <strong>Проверка</strong>: <code>npm audit</code>, Snyk, генерация SBOM, проверка npm provenance и подписей sigstore. <strong>Права</strong>: токены с минимальным скоупом, публикация отдельным job, 2FA у мейнтейнеров, запрет исходящей сети на этапе сборки.</p>

    <h4>Почему так</h4>
    <p>Каждая транзитивная зависимость — это доверие постороннему человеку с правом выполнить код на вашей машине и в вашем проде. Типичное фронтенд-приложение тянет 800–1500 пакетов, то есть сотни точек отказа, и проверить их вручную невозможно. Поэтому стратегия не «проверить всё», а «сузить окно»: сделать сборку воспроизводимой, лишить установку прав и ограничить радиус поражения украденного токена.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Когда защита не срабатывает.</strong> Lockfile не спасает, если вредоносный код уже был в зафиксированной версии; <code>--ignore-scripts</code> не спасает от кода, исполняющегося при импорте в рантайме сборки; <code>npm audit</code> видит только известные CVE и молчит про свежую атаку.</li>
    <li>Обновление ради «нулевого audit» иногда опаснее самой уязвимости: прод-фронтенд редко подвержен ReDoS в dev-зависимости, а внеплановое обновление тянет новый код.</li>
    <li>Ревью lockfile игнорируют из-за размера диффа — используйте инструменты, показывающие только новые пакеты и смену источников.</li>
    <li>Приватный registry-прокси (Verdaccio, Artifactory) с кешированием защищает от удаления пакета и даёт точку контроля, но требует политики обновления кеша.</li>
    <li>Для стороннего кода в рантайме дисциплина другая: CSP, SRI, вынос виджетов в изолированный iframe с ограничивающей <code>Permissions-Policy</code>.</li>
    <li>Минимизация зависимостей — самая недооценённая мера: <code>left-pad</code>-подобные пакеты заменяются тремя строками, и это минус одна точка доверия навсегда.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что делать при подтверждённой компрометации?</strong> Считать скомпрометированными все секреты, доступные CI на момент установки: ротировать токены, проверить артефакты сборки, зафиксировать предыдущую версию пакета в overrides. <strong>Что такое provenance?</strong> Подписанное подтверждение, что пакет собран из конкретного коммита конкретным CI-воркфлоу — проверяемая связь между исходником и тем, что лежит в реестре. <strong>Что делать с dev-зависимостями?</strong> Они опаснее прод-зависимостей, а не безопаснее: именно они выполняются в CI с доступом к секретам и к артефактам сборки. Разделение на dev и prod ничего не значит для supply chain — значение имеет только то, где и с какими правами исполняется код. <strong>Как оценить риск быстро?</strong> Посмотреть на число мейнтейнеров, дату последней публикации и количество транзитивных зависимостей: пакет с одним мейнтейнером и сорока зависимостями — это сорок один человек, которым вы доверяете.</p>`,
    code: `# .npmrc
ignore-scripts=true
# CI
npm ci --ignore-scripts && npm run build`,
    tip: 'Практика «не обновляться в первые дни после релиза» — простая и очень действенная мера, её редко называют.' },

  { id: 'wx30',
    q: 'Что такое SRI и какие у неё ограничения?',
    snippet: `<script src="https://cdn.example.com/lib.js"
        integrity="sha384-oqVuAfXRKap7fdgcCY5uykM6+R9GqQ8K/uxy9rx7HNQlGYl1kPzQho1wx4JwY8wC">
</script>
<script src="https://www.googletagmanager.com/gtm.js?id=GTM-XXXX"
        integrity="sha384-..." crossorigin="anonymous"></script>`,
    a: `<h4>Коротко</h4>
    <p>Subresource Integrity — атрибут <code>integrity</code> с хешем содержимого: браузер скачивает ресурс, считает хеш и <strong>отказывается выполнять</strong>, если не совпало. В сниппете первый тег сломан — для кросс-доменного ресурса обязателен <code>crossorigin="anonymous"</code>. Второй бессмыслен: GTM меняется при каждом изменении контейнера, и хеш протухнет в тот же день.</p>

    <h4>Как это работает</h4>
    <p>Значение — это алгоритм и base64 от дайджеста: <code>sha256-</code>, <code>sha384-</code> или <code>sha512-</code>. Можно указать несколько хешей через пробел — подойдёт любой совпавший, что удобно при плавной ротации версий. Если хеш не совпал, ресурс не применяется, генерируется событие <code>error</code>, и в консоли появляется сообщение с ожидаемым и полученным значением.</p>
    <p>Требование <code>crossorigin</code> не формальность: без CORS-режима ответ для скрипта остаётся <strong>opaque</strong>, браузер не имеет права прочитать его тело и потому не может посчитать хеш — единственный безопасный выход для него блокировать ресурс. Сервер CDN при этом обязан отдавать <code>Access-Control-Allow-Origin</code>. Работает SRI на <code>&lt;script&gt;</code> и <code>&lt;link rel="stylesheet"&gt;</code> (а также <code>preload</code> и <code>modulepreload</code>); для картинок и шрифтов её нет.</p>

    <h4>Почему так</h4>
    <p>Модель угрозы у SRI узкая и честная: она защищает от <strong>подмены байтов</strong> на чужом хостинге — взломанный CDN, MITM при неправильном TLS, компрометация бакета. Она принципиально не может защитить от того, что легитимный владелец сам опубликовал вредоносное обновление: хеш ведь пересчитают и обновят в разметке. Именно поэтому SRI и не является ответом на supply chain в целом.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Когда защита не работает.</strong> Ресурсы, которые меняются по замыслу: аналитика, A/B-скрипты, GTM, шрифты по content negotiation, «latest»-версии библиотек. А это ровно тот сторонний код, который и представляет главный риск.</li>
    <li>SRI не защищает от вредоносного кода, загруженного <strong>изнутри</strong> проверенного скрипта: прошедший проверку файл может подтянуть что угодно динамически.</li>
    <li>Для динамического <code>import()</code> атрибута нет — целостность модулей задаётся через <code>integrity</code> в import map, и это относительно свежая возможность.</li>
    <li>Изменение кодировки или сжатия на CDN меняет байты только на транспорте, а не после декодирования, — но перенос файла с автоматической «оптимизацией» (минификация на лету, вставка комментария) хеш ломает.</li>
    <li>Для собственных ассетов SRI обычно избыточна: если вы контролируете CDN и используете иммутабельные имена с contenthash, подмена уже исключена. Ценность именно в стороннем коде.</li>
    <li>Забытое обновление хеша при релизе библиотеки роняет страницу целиком — нужен автоматический расчёт в сборке, а не ручная вставка.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как совместить с CSP?</strong> Директива <code>require-sri-for</code> из черновиков не прижилась; практический вариант — плагин сборщика, проставляющий <code>integrity</code> всем внешним тегам, плюс <code>strict-dynamic</code>. <strong>Что делать с меняющимися скриптами?</strong> Проксировать их через свой домен с фиксацией версии, либо изолировать в iframe с ограниченными правами — тогда компрометация не даёт доступ к вашему DOM и кукам. <strong>Как SRI ведёт себя при ошибке?</strong> Ресурс не применяется, срабатывает <code>onerror</code>, и приложение обязано это пережить: скрипт с интегрити должен иметь фолбэк или хотя бы не ронять инициализацию. <strong>Какой алгоритм выбирать?</strong> <code>sha384</code> — разумный дефолт: <code>sha256</code> тоже допустим, а вот смешивать хеши разной силы в одном атрибуте не стоит, браузер возьмёт сильнейший из поддерживаемых и остальные проигнорирует.</p>`,
    code: `<script src="https://cdn.example.com/lib@1.4.2/lib.js"
        integrity="sha384-oqVuAfXRKap7fdgcCY5uykM6+R9GqQ8K"
        crossorigin="anonymous"></script>`,
    tip: 'Честное «SRI неприменим к тому, что меняется — то есть к самому опасному стороннему коду» звучит зрело.' },

  { id: 'wx31',
    q: 'Как устроен JWT и в чём его типичные уязвимости?',
    snippet: `// header
{ "alg": "none", "typ": "JWT" }
// payload
{ "sub": "42", "role": "admin", "email": "user@example.com", "exp": 1893456000 }
// проверка на сервере
jwt.verify(token, secret);  // алгоритм берётся из header`,
    a: `<h4>Коротко</h4>
    <p>JWT — три base64url-части через точку: header, payload и подпись. В сниппете две классические дыры: <code>alg: none</code> вместе с <code>verify</code> без явного списка алгоритмов означает, что подпись вообще не проверяется, а <code>role: admin</code> и email в payload лежат <strong>открытым текстом</strong> — JWT подписан, но не зашифрован.</p>

    <h4>Как это работает</h4>
    <p>Header содержит <code>alg</code> и часто <code>kid</code> — идентификатор ключа. Payload — набор claims: <code>sub</code> (кто), <code>iss</code> (кто выдал), <code>aud</code> (кому предназначен), <code>exp</code> и <code>nbf</code> (окно валидности), <code>iat</code>, <code>jti</code> (уникальный идентификатор токена). Подпись считается по <code>base64url(header) + '.' + base64url(payload)</code> симметричным ключом (HS256) или приватным ключом пары (RS256, ES256). Проверка — пересчёт подписи плюс валидация claims. Base64url — это кодирование, а не шифрование: любой, кто держит токен, читает payload за секунду.</p>
    <p><strong>Уязвимости и что их останавливает.</strong> <code>alg: none</code> — библиотека принимает токен без подписи; останавливает явное указание ожидаемого алгоритма в вызове (<code>algorithms: ['RS256']</code>), а не чтение его из header. <strong>Путаница алгоритмов</strong> — токен, подписанный HS256 с публичным RSA-ключом в роли секрета, проходит проверку, если код доверяет header; останавливает то же самое явное указание плюс раздельные ключи. <strong>Слабый секрет</strong> HS256 брутфорсится офлайн за часы; останавливает ключ от 256 бит из CSPRNG. <code>kid</code> с path traversal или SQL-инъекцией, а также <code>jku</code>/<code>x5u</code>, указывающие на сервер атакующего, — останавливает allowlist ключей вместо загрузки по ссылке из токена. Непроверенные <code>aud</code> и <code>iss</code> позволяют предъявить валидный токен от соседнего сервиса.</p>

    <h4>Почему так</h4>
    <p>JWT проектировали как <strong>самодостаточный</strong> формат: сервис проверяет токен, не ходя в базу, — это ценно для микросервисов и горизонтального масштабирования. Гибкость header (свой <code>alg</code>, свой <code>kid</code>, ссылка на ключ) была нужна для интероперабельности, но именно она дала атакующему возможность влиять на алгоритм проверки. Отсюда практическое правило: header — <strong>данные от клиента</strong>, доверять ему нельзя.</p>

    <h4>Подводные камни</h4>
    <ul><li>Главный архитектурный минус: токен <strong>невозможно отозвать</strong> без состояния на сервере — до <code>exp</code> он валиден, даже если пользователь заблокирован. Отсюда короткий TTL access-токена (5–15 минут) и отдельный механизм refresh.</li>
    <li>Персональные данные в payload — утечка при любом логировании: токены попадают в access-логи прокси, в Sentry, в URL при неаккуратной интеграции. Для шифрования есть JWE, но чаще правильнее просто не класть лишнего.</li>
    <li>Проверка <code>exp</code> без допуска на рассинхронизацию часов даёт плавающие 401 — стандартный запас 30–60 секунд.</li>
    <li>Ротация ключей требует <code>kid</code> и JWKS-эндпоинта с кешированием; без неё смена ключа мгновенно разлогинивает всех.</li>
    <li>Размер: 1–2 КБ токена в каждом запросе — это заметный оверхед, а в куке ещё и риск упереться в лимит 4 КБ.</li>
    <li>Декодирование payload на клиенте для отображения имени пользователя допустимо, но принимать по нему решения о правах нельзя — их проверяет только сервер.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>JWT или сессия?</strong> Если фронт и бэк на одном сайте — opaque session id в HttpOnly-куке проще и безопаснее; JWT оправдан для межсервисной авторизации и федерации. <strong>Как отозвать?</strong> Denylist по <code>jti</code> или <code>tokenVersion</code> в claim со сверкой в БД — тогда stateless-преимущество теряется, и это осознанный размен. <strong>Что делать с правами внутри токена?</strong> Роли в claims удобны, но они «замерзают» на время жизни токена: понижение прав вступит в силу только после истечения <code>exp</code>. Для чувствительных операций права проверяют в базе, а не по токену. <strong>Можно ли класть JWT в URL?</strong> Нет: он попадёт в логи, историю и Referer. Только заголовок <code>Authorization</code> или кука.</p>`,
    code: `jwt.verify(token, publicKey, {
  algorithms: ['RS256'],       // не из header
  audience: 'api.example.com',
  issuer: 'https://auth.example.com',
  clockTolerance: 30
});`,
    tip: 'Фраза «JWT — это подписанные, а не зашифрованные данные» плюс проблема отзыва закрывают половину вопросов.' },

  { id: 'wx32',
    q: 'Где хранить токены, как делать ротацию refresh и как отзывать доступ?',
    a: `<h4>Коротко</h4>
    <p>Refresh-токен — в куке <code>HttpOnly; Secure; SameSite=Strict; Path=/auth/refresh</code>, access-токен — в памяти JS. <code>localStorage</code> читается любым XSS, поэтому там токенам не место. Ротация: каждый refresh выдаёт новый токен и инвалидирует старый, повторное использование старого трактуется как кража.</p>

    <h4>Как это работает</h4>
    <p><strong>Хранение.</strong> Кука с <code>HttpOnly</code> недоступна из JS, значит XSS её не украдёт — но она автоматически прикрепляется к запросам, поэтому нужна защита от CSRF: <code>SameSite</code> плюс проверка <code>Origin</code>/<code>Sec-Fetch-Site</code> на сервере. Узкий <code>Path</code> ограничивает радиус: refresh-кука уходит только на эндпоинт обновления и не светится в каждом API-запросе. Access-токен держат в замыкании или в переменной модуля: при перезагрузке страницы он теряется, и приложение делает тихий refresh на старте. Полностью cookie-based сессия с opaque session id остаётся самым простым и безопасным вариантом, когда фронт и бэк на одном сайте.</p>
    <p><strong>Ротация с reuse detection.</strong> Каждый вызов <code>/refresh</code> выдаёт новую пару и помечает предъявленный <code>jti</code> использованным. Если приходит уже использованный refresh-токен, это означает одно из двух: либо гонка клиента, либо кража. Сервер не пытается угадать и аннулирует <strong>всё семейство</strong> токенов, выданное от одного логина, — пользователь разлогинивается везде и получает уведомление. Плюс абсолютный срок жизни семейства (например, 30 дней) поверх скользящего.</p>
    <p><strong>Отзыв.</strong> Access-токен короткоживущий, поэтому реальный отзыв — это инвалидация refresh-семейства. Если нужна мгновенность, добавляют denylist по <code>jti</code> или поле <code>tokenVersion</code> в claim, которое сверяется с БД при каждом запросе, — и тогда stateless-преимущество JWT исчезает; это осознанный размен, о котором стоит сказать вслух.</p>

    <h4>Почему так</h4>
    <p>Выбор хранилища — это выбор, против какой атаки защищаться. <code>localStorage</code> уязвим к XSS, кука — к CSRF. Разница в том, что CSRF закрывается детерминированно (SameSite, Origin, токен), а XSS — вероятностно: одна дыра в одном компоненте, и все токены из <code>localStorage</code> уезжают одним <code>fetch</code>. Поэтому индустрия сошлась на куках, добавив к ним антиCSRF-обвязку.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Гонка refresh.</strong> Пять параллельных запросов получают 401 одновременно и дружно идут обновляться: при ротации четыре из них предъявят устаревший токен и вызовут ложное срабатывание reuse detection. Нужен единый promise-мьютекс в интерцепторе и очередь ожидающих запросов.</li>
    <li>Несколько вкладок усугубляют гонку — синхронизируйте через <code>BroadcastChannel</code> или Web Locks API.</li>
    <li>Sliding-срок без абсолютного означает вечную сессию: украденный токен живёт, пока им пользуются.</li>
    <li>Хранение access-токена в <code>sessionStorage</code> «потому что он живёт недолго» — та же дыра, что и <code>localStorage</code>: XSS читает и его.</li>
    <li>Логаут должен убивать серверную запись, а не только очищать клиент: удаление токена из памяти ничего не отзывает.</li>
    <li>Мобильные WebView и сторонние встраивания ломают куки — там придётся либо BFF, либо secure storage платформы.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Почему не хранить в памяти всё?</strong> Тогда каждая перезагрузка требует полного логина; refresh-кука — компромисс между удобством и безопасностью. <strong>Как выглядит BFF?</strong> Свой бэкенд держит токены и отдаёт браузеру обычную сессионную куку; для SPA это официальная рекомендация OAuth 2.0 for Browser-Based Apps. <strong>А если XSS всё-таки случился?</strong> Честный ответ: при XSS проиграно всё независимо от хранилища — атакующий может дождаться обновления токена и слать запросы от имени пользователя прямо из вашей страницы. HttpOnly-кука не делает приложение неуязвимым, она лишь не отдаёт токен наружу, что ограничивает атаку временем присутствия скрипта на странице. Поэтому первичной защитой остаётся CSP и отсутствие XSS, а схема хранения — второй линией.</p>`,
    code: `let refreshing = null;
async function refresh() {
  refreshing ??= fetch('/auth/refresh', { method: 'POST', credentials: 'include' })
    .then((r) => r.json())
    .finally(() => { refreshing = null; });
  return refreshing; // все параллельные 401 ждут один запрос
}`,
    tip: 'Reuse detection и аннулирование семейства токенов — конкретика, по которой видно, что вы это проектировали.' },

  { id: 'wx33',
    q: 'Как работает OAuth2/OIDC с PKCE во фронтенде?',
    a: `<h4>Коротко</h4>
    <p>OAuth2 — про <strong>авторизацию</strong> (доступ к ресурсу от имени пользователя), OIDC — надстройка про <strong>аутентификацию</strong> (кто пользователь), добавляющая <code>id_token</code> и эндпоинт <code>/userinfo</code>. Для SPA и мобильных единственный рекомендованный поток — <strong>Authorization Code + PKCE</strong>; implicit flow объявлен устаревшим.</p>

    <h4>Как это работает</h4>
    <p>Клиент генерирует случайный <code>code_verifier</code> (43–128 символов из CSPRNG), считает <code>code_challenge = base64url(sha256(verifier))</code> и уходит на <code>/authorize</code> с параметрами <code>response_type=code</code>, <code>client_id</code>, <code>redirect_uri</code>, <code>scope</code>, <code>state</code>, <code>nonce</code>, <code>code_challenge</code> и <code>code_challenge_method=S256</code>. Пользователь логинится у провайдера, тот редиректит обратно с <code>code</code> и <code>state</code>. Клиент сверяет <code>state</code> со своим сохранённым, затем обменивает код на токены на <code>/token</code>, предъявляя исходный <code>code_verifier</code>. Сервер пересчитывает хеш и сверяет с тем, что видел в начале.</p>
    <p>Обязательные проверки на стороне клиента: совпадение <code>state</code> (защита от подсунутого чужого кода), совпадение <code>nonce</code> в <code>id_token</code> (привязка к этой сессии), валидация подписи <code>id_token</code> по JWKS, проверка <code>iss</code>, <code>aud</code>, <code>exp</code> и точное совпадение <code>redirect_uri</code> с зарегистрированным.</p>

    <h4>Почему так</h4>
    <p>Implicit flow отдавал токен прямо в URL-фрагменте: он попадал в историю браузера, в реферер и в логи, и отозвать его было нельзя. Code flow вернул токен в POST-ответ, но породил новую проблему: публичный клиент (SPA, мобильное приложение) не может хранить <code>client_secret</code>, значит перехваченный код можно обменять кому угодно. PKCE решает это <strong>динамическим секретом на один обмен</strong>: verifier никогда не покидает клиента, а challenge бесполезен без него. Метод <code>plain</code> существует только для устройств без SHA-256 и в вебе применяться не должен.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Куда девать токены после обмена</strong> — вопрос, который PKCE не решает. Хранение в <code>localStorage</code> сводит всю схему к одному XSS. Отсюда рекомендация <strong>BFF</strong>: обмен кода и хранение токенов выполняет свой бэкенд, а браузер работает по обычной HttpOnly-сессии.</li>
    <li>Silent renew через скрытый iframe перестал работать из-за блокировки сторонних кук: провайдер не видит своей сессии в фрейме. Остаётся refresh token rotation в SPA либо BFF.</li>
    <li><code>id_token</code> нельзя отправлять как bearer к API — он предназначен клиенту, а не ресурсу; для API есть <code>access_token</code> с нужным <code>aud</code>.</li>
    <li>Открытый редирект в <code>redirect_uri</code> сводит PKCE на нет: провайдер обязан требовать точное совпадение, без подстановочных знаков и без свободного query.</li>
    <li><code>state</code> должен быть случайным и одноразовым, а не «просто путь для возврата»: путь храните отдельно, привязанным к state.</li>
    <li>Логаут в OIDC — отдельный протокол (<code>end_session_endpoint</code>, front-channel и back-channel logout); очистка своей куки не завершает сессию у провайдера, и следующий вход пройдёт молча.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Зачем nonce, если есть state?</strong> <code>state</code> защищает редирект (CSRF), <code>nonce</code> связывает конкретный <code>id_token</code> с конкретным запросом — это разные слои. <strong>Что такое DPoP?</strong> Привязка токена к ключу клиента, чтобы украденный токен нельзя было предъявить с другого устройства. <strong>Можно ли обойтись без библиотеки?</strong> Технически да, но валидация JWT и обработка граничных случаев — тот код, который лучше взять готовым и проверенным. <strong>Чем scope отличается от прав в приложении?</strong> Scope ограничивает, что клиент вправе просить от имени пользователя, а не что пользователю разрешено: проверка прав всё равно остаётся на ресурсе. <strong>Почему PKCE теперь и для конфиденциальных клиентов?</strong> Свежие рекомендации требуют его везде — он защищает от подмены кода на редиректе независимо от того, есть ли у клиента секрет.</p>`,
    code: `const verifier = base64url(crypto.getRandomValues(new Uint8Array(32)));
const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(verifier));
const challenge = base64url(new Uint8Array(digest));
sessionStorage.setItem('pkce', verifier);
location.assign(authorizeUrl + '?code_challenge=' + challenge + '&code_challenge_method=S256');`,
    tip: 'Упоминание BFF как современной рекомендации вместо хранения токенов в SPA — сильный архитектурный ответ.' },

  { id: 'wx34',
    q: 'Чем отличается аутентификация в SPA и в SSR-приложении?',
    a: `<h4>Коротко</h4>
    <p>В SPA сессия нужна <strong>клиентскому коду</strong>, поэтому появляется вспышка неавторизованного состояния, silent refresh и защита маршрутов на клиенте, которую всё равно надо дублировать на API. В SSR сервер уже знает пользователя к моменту рендера: первый HTML персонализирован, редирект делается на сервере, а токен вообще не попадает в JS.</p>

    <h4>Как это работает</h4>
    <p><strong>SPA.</strong> Браузер получает статический бандл с CDN, монтирует приложение, только потом выясняет, кто пользователь. Пока запрос <code>/me</code> в полёте, показывается скелет или, что хуже, гостевой интерфейс. Access-токен живёт в памяти и теряется при перезагрузке, поэтому на старте выполняется тихий refresh. Маршруты закрываются guard-ами, но это лишь UX: любой пользователь откроет DevTools, поэтому реальная проверка прав — только на API.</p>
    <p><strong>SSR.</strong> Кука приходит вместе с запросом документа, сервер загружает данные пользователя и рендерит готовую разметку. Неавторизованного можно развернуть ответом 302 ещё до генерации страницы — быстрее и надёжнее клиентского редиректа. Токен остаётся в <code>HttpOnly</code>-куке, JS его не видит, XSS не крадёт.</p>
    <p><strong>Гибрид</strong>, к которому в итоге приходят: SSR или BFF владеет сессией, клиент ходит запросами на свой же origin. Тогда CSRF решается через <code>SameSite</code> плюс проверку <code>Sec-Fetch-Site</code>, а CORS не нужен вовсе.</p>

    <h4>Почему так</h4>
    <p>Разница сводится к тому, <strong>кто первым узнаёт о пользователе</strong>. В SPA это клиент, и все проблемы — производные: лишний round-trip до персонализации, мигание, необходимость хранить токен там, где его можно украсть. В SSR это сервер, и цена другая: персонализация делает ответ некешируемым, а рендер — дороже.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Кеширование персонализированного HTML</strong> — самая опасная ошибка в этой теме. Один ответ, попавший в общий CDN-кеш, покажет данные одного пользователя всем остальным. Нужны <code>Cache-Control: private, no-store</code> и <code>Vary: Cookie</code>, а лучше — жёсткое разделение персонализированных и публичных маршрутов.</li>
    <li>При гидратации состояние сериализуется в HTML (<code>__NEXT_DATA__</code> и аналоги). Туда легко утекает целый объект пользователя с внутренними полями, хешем пароля из ORM или служебными токенами — сериализуемые данные надо фильтровать явно.</li>
    <li>В Next.js App Router проверка только в middleware недостаточна: RSC-эндпоинты можно запросить напрямую, и известны CVE с обходом middleware. Проверка должна быть в самом Server Component или в слое доступа к данным.</li>
    <li>Ошибка «сервер отрендерил гостя, клиент знает пользователя» даёт hydration mismatch и мигание — источник истины должен быть один.</li>
    <li>В SSR легко случайно закешировать fetch с авторизацией на уровне фреймворка: у Next есть свой кеш запросов, и персональные ответы надо помечать <code>no-store</code>.</li>
    <li>Логаут в SSR должен инвалидировать серверную сессию и очистить куку с теми же атрибутами (<code>Path</code>, <code>Domain</code>), иначе кука останется висеть.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как избежать вспышки в SPA?</strong> Кешировать факт авторизации (не сами данные) в <code>localStorage</code> и рендерить оптимистично — с откатом, если <code>/me</code> вернул 401. <strong>Где хранить сессию при статическом хостинге?</strong> В BFF или serverless-функции на своём домене: без серверной точки безопасного варианта нет. <strong>Что происходит при истечении сессии в SSR?</strong> Сервер редиректит на логин, но фоновые клиентские запросы уже открытой страницы начнут получать 401 — нужен общий обработчик, который переводит пользователя на вход с сохранением текущего маршрута. <strong>Как тестировать?</strong> Отдельные e2e-сценарии для гостя, для истёкшей сессии и для пользователя без прав на конкретный ресурс: последний случай ловит именно те дыры, где UI спрятал кнопку, а API остался открытым.</p>`,
    code: `// Server Component: проверка в data layer, не только в middleware
export async function getOrders() {
  const user = await requireUser();       // бросает redirect(), если гость
  return db.orders.findMany({ where: { userId: user.id } });
}`,
    tip: 'Риск закешировать персонализированный SSR-ответ на CDN — тот пример, который сразу показывает продовый опыт.' },

  { id: 'wx35',
    q: 'Что означают тайминги в Network waterfall DevTools?',
    snippet: `Queueing            0.4 ms
Stalled           412.0 ms
DNS Lookup         38.0 ms
Initial connection 96.0 ms
SSL                61.0 ms
Request sent        0.2 ms
Waiting (TTFB)    880.0 ms
Content Download   24.0 ms`,
    a: `<h4>Коротко</h4>
    <p>В сниппете два виновника. <strong>Stalled 412 мс</strong> — запрос ждал свободного сокета: почти наверняка HTTP/1.1 и лимит в 6 соединений на хост. <strong>Waiting 880 мс</strong> — сервер думал: это TTFB, и фронтенд его не починит ничем, кроме кеша и CDN. Content Download всего 24 мс, то есть размер ответа тут ни при чём.</p>

    <h4>Как это работает</h4>
    <ul><li><strong>Queueing</strong> — запрос отложен браузером: есть более приоритетные, идёт запись в дисковый кеш, или уже открыто максимум соединений.</li>
    <li><strong>Stalled</strong> — ожидание сокета, прокси или согласования; сюда же попадает время на проверку HTTP-кеша.</li>
    <li><strong>DNS Lookup</strong>, <strong>Initial connection</strong> (TCP), <strong>SSL</strong> — установка соединения; в сумме один-два RTT на TCP плюс один-два на TLS 1.2 или один на TLS 1.3.</li>
    <li><strong>Request sent</strong> — отправка заголовков, обычно доли миллисекунды.</li>
    <li><strong>Waiting (TTFB)</strong> — время до первого байта ответа: сеть до сервера плюс генерация.</li>
    <li><strong>Content Download</strong> — приём тела; зависит от размера и полосы.</li></ul>
    <p>Как читать целиком: длинный Waiting — бэкенд или дальний регион; длинный Content Download — большой ресурс или узкий канал; длинный Queueing/Stalled — исчерпаны соединения либо низкий приоритет. <strong>Ступенчатый водопад</strong>, где каждый запрос начинается ровно после предыдущего, — признак цепочки зависимостей: HTML → CSS → шрифт или HTML → JS → fetch → картинка. Это главный сигнал, что нужен preload или изменение архитектуры загрузки, а не микрооптимизация.</p>

    <h4>Почему так</h4>
    <p>Панель показывает не «скорость сети», а <strong>структуру ожиданий</strong>. Большинство проблем загрузки — не про килобайты, а про последовательность: браузер не мог узнать о ресурсе раньше, чем разобрал то, что на него ссылается. Поэтому важнее всего в водопаде не длина полосок, а их <strong>смещение по горизонтали</strong>.</p>

    <h4>Подводные камни</h4>
    <ul><li>Без включённого троттлинга водопад не отражает реальность: на гигабите Content Download почти нулевой у всего, и картина искажена.</li>
    <li>«Disable cache» меняет всё: с ним вы видите первый визит, без него — повторный. Мерить надо оба сценария и не путать их.</li>
    <li>Строки <code>(from disk cache)</code>, <code>(from memory cache)</code>, <code>(from prefetch cache)</code> и <code>(ServiceWorker)</code> означают, что сети не было вовсе, — время в них не показатель.</li>
    <li>Колонка Initiator даёт кликабельную цепочку «кто запросил» — быстрый способ найти виновника лишнего запроса; Protocol показывает h2/h3, Priority — вес.</li>
    <li>Запросы, заблокированные CSP, CORS или блокировщиком, попадают в раздел «Blocked requests» и легко теряются, если не включить фильтр.</li>
    <li>Расширения браузера добавляют собственные запросы и искажают лимит соединений — записывайте в чистом профиле.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Почему в HTTP/2 нет Stalled?</strong> Потому что все запросы к хосту мультиплексируются в одном соединении: вместо очереди сокетов появляется распределение полосы по приоритетам. <strong>Как экспортировать?</strong> HAR-файл: его можно приложить к тикету и открыть в WebPageTest. <strong>Что такое «Big request rows»?</strong> Режим, показывающий одновременно размер и переданный объём — сразу видно, было ли сжатие. <strong>Почему запрос вообще не начинается?</strong> Три частые причины: он ждёт своей очереди за шестью открытыми соединениями, он заблокирован CSP или CORS и попал в отдельный раздел, либо его перехватил Service Worker и ответ пришёл из Cache Storage — в последнем случае в колонке Size стоит «ServiceWorker», а сети не было вовсе.</p>`,
    code: `// то же самое программно, для RUM
const r = performance.getEntriesByName(url)[0];
console.log('ttfb', r.responseStart - r.requestStart,
            'download', r.responseEnd - r.responseStart,
            'transferSize', r.transferSize); // 0 → ответ из кеша`,
    tip: 'Умение назвать ступенчатый водопад как признак цепочки зависимостей — это то, что реально спрашивают.' },

  { id: 'wx36',
    q: 'Как отлаживать фронтенд на реальных мобильных устройствах?',
    a: `<h4>Коротко</h4>
    <p>Эмулятор в DevTools меняет размер вьюпорта и user agent — и всё. Он не воспроизводит реальный CPU, память, GPU, тепловой троттлинг, выгрузку вкладок и особенности мобильных движков. Поэтому Android отлаживают через <code>chrome://inspect</code>, iOS — через Safari Web Inspector, а когда провода нет — Eruda или vConsole прямо на странице.</p>

    <h4>Как это работает</h4>
    <p><strong>Android.</strong> Включаем режим разработчика и USB-отладку, подключаем кабель, открываем <code>chrome://inspect/#devices</code> — получаем полноценные DevTools для вкладки на телефоне: Elements, Network, Performance с реальным профилем CPU, Memory. Там же есть port forwarding: <code>localhost:5173</code> на телефоне маппится на дев-сервер ноутбука, и HTTPS-сертификат для локалхоста при этом считается доверенным.</p>
    <p><strong>iOS.</strong> В настройках Safari на телефоне включаем Web Inspector, на маке в меню «Разработка» выбираем устройство и страницу. Тот же путь работает для WKWebView внутри нативных приложений, если разработчик выставил <code>isInspectable</code>. Для симулятора всё то же самое без кабеля.</p>
    <p><strong>Без кабеля.</strong> Eruda и vConsole подключаются одним скриптом и дают консоль, network и элементы прямо в странице — незаменимо для отладки на чужом устройстве или в WebView мессенджера. Charles и Proxyman перехватывают трафик через прокси с установленным корневым сертификатом. Удалённые фермы (BrowserStack, LambdaTest) закрывают матрицу устройств и версий.</p>

    <h4>Почему так</h4>
    <p>Разрыв между эмуляцией и устройством не количественный, а качественный: мобильный SoC при длительной нагрузке уходит в <strong>тепловой троттлинг</strong> и через минуту работает вдвое медленнее, чем в первые секунды. Ни один бенчмарк на десктопе такого не покажет. Плюс мобильная ОС агрессивно выгружает фоновые вкладки, поэтому страница обязана корректно переживать заморозку и восстановление, а не рассчитывать на непрерывную жизнь.</p>

    <h4>Подводные камни</h4>
    <ul><li><code>unload</code> на мобильных практически не срабатывает и ломает bfcache — используйте <code>pagehide</code> и <code>visibilitychange</code>, а отправку данных делайте через <code>navigator.sendBeacon</code>.</li>
    <li><code>100vh</code> в Safari считается по вьюпорту без панелей браузера, поэтому контент уезжает под адресную строку. Современный ответ — <code>100dvh</code>, но с фолбэком.</li>
    <li>Нет hover: интерфейсы, где действие спрятано под наведение, на тач-устройстве недоступны. Проверяйте через <code>@media (hover: hover)</code>.</li>
    <li>Клавиатура перекрывает поля и меняет вьюпорт: для корректного поведения нужен <code>visualViewport</code>, а не <code>window.innerHeight</code>.</li>
    <li>Автоплей видео со звуком запрещён, фоновые таймеры замедляются до одного тика в секунду и реже, WebSocket в фоне рвётся.</li>
    <li>Safari чистит storage сайта примерно после 7 дней без визитов (ITP) — офлайн-данные исчезают, и приложение должно это переживать.</li>
    <li>Дев-серверу нужен HTTPS, если тестируете Service Worker, геолокацию, камеру или Web Share: на <code>http://</code> они недоступны, кроме локалхоста.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как воспроизвести медленное устройство без него?</strong> CPU throttling 4–6x плюс Slow 4G — грубое приближение к среднему Android; для точности всё равно нужен реальный аппарат за 100–150 долларов в парке команды. <strong>Как ловить ошибки, которых нет на десктопе?</strong> Мониторинг с разбивкой по устройству и версии ОС: специфика Safari и WebView видна только в срезах. <strong>Как проверить bfcache?</strong> В DevTools есть Application → Back/forward cache с кнопкой теста: он показывает конкретную причину, по которой страница не была закеширована, — чаще всего это открытый WebSocket, обработчик <code>unload</code> или заголовок <code>Cache-Control: no-store</code>. Восстановление из bfcache даёт мгновенный переход «назад», и на мобильных это заметнее любой другой оптимизации.</p>`,
    code: `// корректная отправка метрик на мобильных
addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'hidden') navigator.sendBeacon('/rum', payload);
});`,
    tip: 'Тепловой троттлинг и выгрузка вкладок — то, чего не увидишь в эмуляторе, и это ценный практический акцент.' },

  { id: 'wx37',
    q: 'Как вообще работает бандлинг и что реально уменьшает размер?',
    snippet: `import _ from 'lodash';
import moment from 'moment';
import { Chart } from 'chart.js';

export function formatRow(row) {
  return _.pick(row, ['id', 'title']);
}
// package.json без "sideEffects"`,
    a: `<h4>Коротко</h4>
    <p>Сборщик строит <strong>граф модулей</strong> от точек входа, разрешает импорты, применяет трансформации, делает tree shaking, режет граф на чанки и генерирует рантайм-загрузчик. В сниппете три классические проблемы: дефолтный импорт всей lodash, moment с локалями и chart.js в основном чанке — плюс отсутствие <code>sideEffects</code>, из-за которого tree shaking частично отключается.</p>

    <h4>Как это работает</h4>
    <p><strong>Tree shaking</strong> — это анализ достижимости экспортов на статическом графе ESM. Он ломается на трёх вещах: <code>require</code> и любой CommonJS (граф неизвестен до выполнения), динамические ключи (<code>obj[name]</code>), реэкспорт звёздочкой из большого барреля. Главный ограничитель — <strong>побочные эффекты</strong>: без <code>"sideEffects": false</code> в package.json бандлер обязан сохранить модуль целиком, даже если ни один его экспорт не используется, потому что импорт мог что-то зарегистрировать. Отсюда парадокс: <code>import { pick } from 'lodash'</code> без <code>lodash-es</code> тянет всю библиотеку.</p>
    <p><strong>Code splitting</strong> идёт по трём линиям: точки входа, динамический <code>import()</code> (маршруты и тяжёлые компоненты — редактор, графики, карта, PDF-вьюер) и выделение общих чанков. <strong>Scope hoisting</strong> сливает модули в одну область видимости: меньше обёрток, лучше минификация и быстрее старт. Минификатор дополнительно делает dead code elimination по константам, поэтому <code>process.env.NODE_ENV</code> нужно подставлять на этапе сборки.</p>

    <h4>Почему так</h4>
    <p>Настройки бандлера дают проценты, а решения о зависимостях — разы. Заменить moment (около 70 КБ gzip со всеми локалями) на date-fns с точечными импортами или на нативный <code>Intl</code> — минус десятки килобайт одним PR. Вынести редактор в динамический чанк — минус сотни. Поэтому разговор о размере всегда начинают с отчёта анализатора, а не с конфига.</p>

    <h4>Подводные камни</h4>
    <ul><li>Смотреть надо <strong>сжатый</strong> размер (gzip или brotli): несжатые цифры в анализаторе вводят в заблуждение, потому что повторяющийся код сжимается почти в ноль.</li>
    <li>Чрезмерная дробность вредна: сто мелких чанков — это сто запросов и водопад зависимостей, а HTTP/2 не отменяет накладных на каждый.</li>
    <li>Дублирующиеся версии одной библиотеки в дереве (две React, три lodash) — частая причина «откуда 300 КБ»; лечится <code>dedupe</code>, <code>overrides</code> и проверкой <code>npm ls</code>.</li>
    <li>Полифиллы: <code>core-js</code> целиком весит больше приложения. Нужны <code>browserslist</code> и <code>useBuiltIns: 'usage'</code>, а лучше — вообще не поддерживать браузеры, которых нет в аналитике.</li>
    <li>Barrel-файлы (<code>index.ts</code> с реэкспортом сотни компонентов) ломают splitting: один импорт тянет весь баррель, пока бандлер не докажет обратное.</li>
    <li>Иконочные библиотеки при неправильном импорте кладут в бандл несколько тысяч SVG.</li>
    <li>Вкладка Coverage показывает неиспользованный код на конкретном сценарии — она не заменяет анализатор, но отвечает на другой вопрос: не «что весит», а «что вообще не выполнилось».</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что положить в vendor-чанк?</strong> Осторожно: один большой vendor инвалидируется при любом обновлении. Лучше несколько групп по частоте изменений. <strong>Как ловить регрессии?</strong> Бюджет размера в CI с комментарием дельты в PR. <strong>Что даёт module/nomodule?</strong> Современным браузерам — код без полифиллов, старым — legacy-сборку; в 2025 году чаще проще не поддерживать legacy вовсе. <strong>Как понять, что чанк лишний?</strong> Через Coverage на реальном сценарии и через сопоставление отчёта анализатора с маршрутами: код, который выполняется меньше чем у 10% пользователей, почти всегда должен быть за динамическим импортом. <strong>Даёт ли что-то CSS-splitting?</strong> Да, и заметно: CSS блокирует рендер, поэтому вынести стили редко используемых экранов в отдельные файлы часто выгоднее, чем такой же объём JS.</p>`,
    code: `import { pick } from 'lodash-es';       // вместо import _ from 'lodash'
const Chart = lazy(() => import('./Chart'));
// package.json
{ "sideEffects": ["*.css", "./src/polyfills.ts"] }`,
    tip: 'Поле sideEffects как условие работы tree shaking — конкретика, которую знают далеко не все.' },

  { id: 'wx38',
    q: 'Чем отличаются esbuild, Rollup, Vite, webpack и Turbopack?',
    a: `<h4>Коротко</h4>
    <p><strong>esbuild</strong> — сверхбыстрый транспайлер на Go, <strong>Rollup</strong> — эталон сборки библиотек, <strong>Vite</strong> — дев-сервер на нативных ESM плюс прод-сборка Rollup-ом, <strong>webpack</strong> — самый зрелый и гибкий, <strong>Turbopack</strong> — инкрементальный сборщик Next.js на Rust. Выбор практический: приложение — Vite или Next, библиотека — Rollup или tsup, легаси с Module Federation — webpack или Rspack.</p>

    <h4>Как это работает</h4>
    <p><strong>esbuild</strong> написан на Go, компилирует в машинный код и распараллеливает всё, что можно: он в 10–100 раз быстрее JS-инструментов. Платит за это упрощённым tree shaking, отсутствием богатой плагинной модели и неполной поддержкой некоторых возможностей TypeScript (декораторы старого образца, <code>const enum</code>). Идеален как трансформер внутри других инструментов.</p>
    <p><strong>Rollup</strong> исторически лучший в tree shaking и в чистоте выходного ESM, с продуманной плагинной моделью, — поэтому библиотеки собирают им. <strong>Vite</strong> — не бандлер: в разработке он отдаёт исходники браузеру как нативные модули, поэтому старт не зависит от размера проекта, а зависимости пребандливает esbuild-ом, чтобы не отдавать тысячу файлов CommonJS. Прод собирает Rollup. Отсюда его историческая слабость — <strong>два разных пайплайна</strong> в dev и prod, и баги, которые проявляются только в одном из них; Rolldown (Rust-порт Rollup) как раз их объединяет.</p>
    <p><strong>webpack</strong> берёт гибкостью: loader-ы, плагины на каждую фазу, Module Federation для микрофронтендов, зрелая поддержка всего на свете. Цена — скорость и сложность конфигурации; Rspack даёт совместимый Rust-порт без переписывания конфига. <strong>Turbopack</strong> строит функционально-кешируемый граф задач: пересборка пересчитывает только затронутые узлы, что даёт очень быстрый HMR на больших проектах, но вне Next применяется мало.</p>

    <h4>Почему так</h4>
    <p>Общий тренд один: тяжёлая работа уезжает в Rust и Go, а конкуренция идёт не за скорость холодной сборки, а за <strong>инкрементальную пересборку</strong> — потому что разработчик ждёт её сто раз в день, а холодную сборку дважды. Второй тренд — унификация dev и prod: расхождение пайплайнов оказалось дороже, чем выигрыш от небандленного дева.</p>

    <h4>Подводные камни</h4>
    <ul><li>Скорость дева не бесплатна: Vite в разработке грузит сотни отдельных модулей, и на очень больших проектах первый заход в маршрут ощутимо тормозит, пока браузер не прогреет кеш.</li>
    <li>esbuild не делает проверку типов — <code>tsc --noEmit</code> нужен отдельным шагом, иначе ошибки типов доедут до прода.</li>
    <li>Плагины Rollup и Vite частично совместимы, но не полностью; миграция конфига webpack на Vite чаще всего означает переписывание, а не портирование.</li>
    <li>Module Federation остаётся сильным аргументом за webpack/Rspack: у альтернатив он либо экспериментальный, либо неполный.</li>
    <li>«Мы перешли на X и стало быстрее» без цифр — плохой аргумент; меряйте холодный старт, HMR-цикл и время CI отдельно, они улучшаются по-разному.</li>
    <li>Разные минификаторы дают разный размер и разное поведение с побочными эффектами: swc и esbuild быстрее terser, но иногда чуть менее агрессивны.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Когда не нужен бандлер вообще?</strong> Небольшие проекты с нативными ESM и import maps живут без сборки, но теряют tree shaking, минификацию и контроль над количеством запросов. <strong>Что выбрать для библиотеки на TypeScript?</strong> tsup или Rollup с генерацией <code>.d.ts</code>, обязательно с полем <code>exports</code> и раздельными ESM/CJS сборками. <strong>Как выбирать на практике?</strong> По трём вопросам: нужен ли Module Federation, есть ли уже работающий конфиг webpack и насколько велик проект. Смена сборщика — это недели работы и риск тонких расхождений в поведении, поэтому она оправдана либо болью в скорости дева, либо переходом на новый фреймворк, а не тем, что вышел инструмент быстрее.</p>`,
    code: `// vite.config.ts — прод-сборка это Rollup, значит и опции его
export default { build: { rollupOptions: { output: { manualChunks: {
  react: ['react', 'react-dom'], charts: ['chart.js']
} } } } };`,
    tip: 'Понимание, что Vite — это два разных пайплайна и что Rolldown их объединяет, показывает актуальность знаний.' },

  { id: 'wx39',
    q: 'Как устроен HMR и почему он иногда не работает?',
    snippet: `// Button.tsx
export const BUTTON_SIZES = ['sm', 'md'];
export default function Button(props) { ... }
// при правке Button страница каждый раз перезагружается целиком`,
    a: `<h4>Коротко</h4>
    <p>Dev-сервер держит WebSocket с клиентом, при изменении файла определяет затронутые модули и шлёт обновление; рантайм подменяет модуль в кеше и вызывает обработчики принятия. В сниппете Fast Refresh ломается, потому что файл экспортирует одновременно компонент и не-компонент — граница принятия не образуется, и обновление всплывает до полной перезагрузки.</p>

    <h4>Как это работает</h4>
    <p>Ключевое понятие — <strong>граница принятия</strong> (accept boundary). Модуль объявляет, что умеет пережить замену, через <code>import.meta.hot.accept</code> в Vite или <code>module.hot.accept</code> в webpack. Если модуль этого не умеет, обновление «всплывает» вверх по графу импортов к ближайшему модулю, который умеет. Если такого нет до самого корня — выполняется полная перезагрузка страницы.</p>
    <p>React Fast Refresh ставит эти границы автоматически, но по правилам: файл должен экспортировать <strong>только компоненты</strong> (именованные функции с большой буквы), состояние сохраняется лишь у компонентов, чья идентичность распознана. Анонимные стрелочные экспорты, HOC-обёртки и экспорт констант рядом с компонентом сбивают анализ. Хуки при изменении сигнатуры тоже сбрасывают состояние — это осознанное поведение, иначе получилось бы несогласованное дерево.</p>
    <p>Состояние <strong>вне</strong> React — модульные переменные, синглтоны, открытые сокеты, подписки, зарегистрированные обработчики — при HMR не сбрасывается: старый модуль выгружен, но его эффекты живы. Отсюда дублирующиеся слушатели, растущее число WebSocket-соединений и «утечки», которых нет в проде. Чистить их нужно в <code>import.meta.hot.dispose</code>.</p>

    <h4>Почему так</h4>
    <p>Полная перезагрузка теряет состояние: заполненную форму, открытый модал, позицию скролла, шаг визарда. На сложных экранах это десятки секунд ручного восстановления на каждую правку. HMR — размен корректности на скорость итерации: рантайм намеренно допускает, что после десятка замен приложение может оказаться в состоянии, недостижимом при обычной загрузке. Поэтому странное поведение в dev всегда сначала проверяют перезагрузкой.</p>

    <h4>Подводные камни</h4>
    <ul><li>Изменения вне графа модулей не подхватываются: конфиги сборщика, <code>.env</code>, tailwind-конфиг, файлы с публичными ассетами — требуется рестарт сервера.</li>
    <li>Циклические импорты ломают порядок инициализации при замене и дают невоспроизводимые ошибки «cannot access before initialization».</li>
    <li>Файловая система в Docker и на сетевых дисках не отдаёт события watch — нужен <code>usePolling</code>, ценой заметной нагрузки на CPU.</li>
    <li>Прокси, HTTPS и туннели ломают WebSocket: если в консоли висят попытки переподключения, HMR молча деградирует до перезагрузок. Настраивается через <code>server.hmr.clientPort</code> и <code>protocol</code>.</li>
    <li>CSS-in-JS с рантаймом иногда не переприменяет стили: остаются классы от предыдущей версии.</li>
    <li>Отладка «почему полная перезагрузка» — через лог сервера: Vite пишет «page reload» и причину, webpack показывает цепочку всплытия.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как сохранить состояние стора?</strong> Redux и Zustand подключают HMR-хук, переустанавливающий редьюсеры без сброса state. <strong>Чем HMR отличается от live reload?</strong> Live reload перезагружает страницу целиком, HMR подменяет модуль в работающем приложении. <strong>Почему после десятка правок приложение ведёт себя странно?</strong> Потому что накопились состояния, недостижимые при обычной загрузке: старые подписки, дважды зарегистрированные обработчики, устаревшие замыкания. Правило простое — любой непонятный баг в dev сначала проверяется полной перезагрузкой, и только если он воспроизводится там, это настоящий баг. <strong>Работает ли HMR в проде?</strong> Нет, и не должен: рантайм HMR — это дополнительный код и открытый WebSocket, которых в прод-сборке быть не может.</p>`,
    code: `// правильно: один компонент на файл, константы — отдельно
export default function Button(props) { /* ... */ }

if (import.meta.hot) {
  import.meta.hot.dispose(() => socket.close()); // иначе сокеты копятся
}`,
    tip: 'Правило «файл экспортирует только компоненты, иначе Fast Refresh ломается» — то, с чем сталкиваются ежедневно.' },

  { id: 'wx40',
    q: 'Как работают source maps и как безопасно использовать их в проде?',
    snippet: `// vite.config.ts
export default { build: { sourcemap: true } };
// в результате на CDN лежат:
//   /assets/app.a1b2c3.js
//   /assets/app.a1b2c3.js.map   ← с полем sourcesContent`,
    a: `<h4>Коротко</h4>
    <p>Source map — JSON, сопоставляющий позиции в собранном файле позициям в исходниках. Конфиг в сниппете публикует карту с полем <code>sourcesContent</code> в открытый доступ, то есть фактически выкладывает исходники, включая комментарии, внутренние URL и иногда ключи. Правильный вариант — <code>hidden</code>, загрузка карт в Sentry на деплое и удаление их с публичного CDN.</p>

    <h4>Как это работает</h4>
    <p>В карте есть <code>version</code>, <code>sources</code> (пути исходников), <code>names</code> (имена идентификаторов), <code>mappings</code> и опционально <code>sourcesContent</code> — сами тексты файлов. <code>mappings</code> — это строка сегментов в кодировке VLQ base64, где каждое число дельта-закодировано относительно предыдущего: так карта на мегабайтный бандл укладывается в сотни килобайт. Подключается либо комментарием <code>sourceMappingURL</code> в конце файла, либо HTTP-заголовком <code>SourceMap</code> — второй вариант позволяет вообще не упоминать карту в публичном коде.</p>
    <p>Режимы в сборщиках различаются компромиссом «скорость против точности»: <code>eval-cheap-module-source-map</code> для дева (быстро, без колонок), <code>source-map</code> для прода (отдельный полноценный файл), <code>hidden-source-map</code> — карта генерируется, но комментарий в файл не добавляется, <code>nosources-source-map</code> — есть маппинг, но нет текстов исходников.</p>

    <h4>Почему так</h4>
    <p>Стектрейс от минифицированного бандла бесполезен: <code>t is not a function at n (app.js:1:48213)</code> не указывает ни на файл, ни на функцию. Без карт мониторинг ошибок превращается в счётчик. Поэтому карты нужны обязательно — вопрос лишь в том, кому они доступны. Отсюда и решение: генерировать всегда, но отдавать только своему сервису мониторинга.</p>

    <h4>Подводные камни</h4>
    <ul><li>Для сопоставления релиза и карт нужен стабильный идентификатор. Sentry использует <code>debug_id</code>, вшитый и в бандл, и в карту, либо связку release + dist. Если его не пробросить в сборку, карты загрузятся, но не применятся, и стектрейсы останутся минифицированными.</li>
    <li>Цепочка трансформаций (TS → SWC → минификация → бандлинг) требует, чтобы каждая ступень корректно сливала входную карту с выходной. Одно звено с <code>sourcemap: false</code> — и строки уезжают на десятки позиций.</li>
    <li>Удалять <code>.map</code> с CDN нужно <strong>после</strong> загрузки в мониторинг, отдельным шагом пайплайна; забытый шаг — самая частая утечка исходников.</li>
    <li>Отдавать карты по авторизации или по списку IP — рабочий компромисс, если разработчикам нужно отлаживать прод из браузера.</li>
    <li>Карты стилей (Sass, PostCSS) настраиваются отдельно и часто ломаются на автопрефиксере.</li>
    <li>Размер: полные карты весят как исходники. Их не должно быть в артефакте, который деплоится на edge с оплатой за трафик.</li>
    <li>Публичная карта — это ещё и разведка для атакующего: имена внутренних эндпоинтов, флаги, комментарии вида «TODO: тут нет проверки прав».</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как проверить, что карты применились?</strong> В Sentry у события должен быть распознанный стек с именами файлов проекта; там же есть диагностика, почему артефакт не сматчился. <strong>Можно ли отлаживать прод без карт?</strong> Частично: <code>nosources-source-map</code> даёт имена файлов и строки без текстов — компромисс между приватностью и читаемостью. <strong>Замедляют ли карты сборку?</strong> Заметно: генерация полных карт добавляет десятки процентов ко времени прод-сборки и памяти. Это нормальная плата, но в CI её стоит учитывать при выборе раннера. <strong>Нужны ли карты для CSS в проде?</strong> Обычно нет: минифицированный CSS читаем достаточно, а карты стилей чаще ломаются, чем помогают.</p>`,
    code: `// vite.config.ts
export default { build: { sourcemap: 'hidden' } };
// CI, после сборки:
//   sentry-cli sourcemaps inject ./dist && sentry-cli sourcemaps upload ./dist
//   find dist -name '*.map' -delete`,
    tip: 'Связка hidden-source-map + загрузка карт в Sentry — стандартное продовое решение, называйте его сразу.' },

  { id: 'wx41',
    q: 'Как правильно организовать кеш-бастинг и деплой фронтенда?',
    snippet: `Cache-Control: public, max-age=31536000, immutable   # /index.html
Cache-Control: no-cache                              # /assets/app.a1b2c3.js

# деплой: удаляем старую папку, заливаем новую, инвалидируем весь CDN`,
    a: `<h4>Коротко</h4>
    <p>В сниппете заголовки перепутаны местами: вечный кеш повешен на <strong>точку входа</strong>, а хешированная статика ревалидируется на каждый запрос. Должно быть наоборот. Плюс удаление старой папки при деплое гарантирует <code>ChunkLoadError</code> у всех, кто держал вкладку открытой.</p>

    <h4>Как это работает</h4>
    <p>Базовая схема: имена ассетов содержат <strong>хеш содержимого</strong> и отдаются с <code>Cache-Control: public, max-age=31536000, immutable</code>. Директива <code>immutable</code> говорит браузеру не слать даже условный запрос при обновлении страницы — иначе Chrome по F5 ревалидирует всё подряд. HTML — точка входа — не кешируется надолго: <code>no-cache</code> (кешировать можно, но обязательно спросить через ETag) или короткий TTL с <code>must-revalidate</code>. Итог: релиз меняет только маленький HTML, вся статика переиспользуется, а инвалидация CDN нужна ровно для одного файла.</p>
    <p><strong>Порядок деплоя</strong> критичен: сначала выкладывается новая статика, только потом переключается HTML. При обратном порядке пользователь получает разметку, ссылающуюся на ещё не существующие файлы, — белый экран на всё время заливки. Атомарность достигается сменой префикса в бакете, симлинком или blue-green.</p>
    <p><strong>Старые ассеты не удаляют сразу.</strong> У пользователя может быть открыта вкладка со старым HTML, и ленивый чанк подгрузится по старому имени через час после релиза. Держите 2–3 предыдущих релиза или чистите по возрасту, а не по факту выката.</p>

    <h4>Почему так</h4>
    <p>Хеш в имени превращает кеширование из вероятностного в детерминированное: URL и содержимое связаны однозначно, поэтому «устаревший ответ» логически невозможен, и можно ставить максимальный срок без риска. Вся неопределённость сосредоточена в одном файле — HTML, — и им управляют явно. Это тот же принцип, что и в контент-адресуемых хранилищах.</p>

    <h4>Подводные камни</h4>
    <ul><li><code>ChunkLoadError</code> и «Failed to fetch dynamically imported module» — прямое следствие удаления старых файлов. Обработчик такой ошибки должен показать «вышла новая версия, обновите страницу» и предложить перезагрузку, а не белый экран; для <code>import()</code> помогает ретрай с одной попыткой и последующим <code>location.reload()</code>.</li>
    <li>Хеш должен быть по <strong>содержимому</strong> (contenthash), а не по сборке: иначе каждый релиз инвалидирует весь кеш. Следите и за стабильностью module id — их перенумерация меняет хеши всех чанков от одного нового импорта.</li>
    <li>Кешировать HTML на CDN можно и нужно, но с коротким TTL и корректным <code>stale-while-revalidate</code>; без этого весь TTFB упирается в origin.</li>
    <li><code>Vary</code> и cookie в cache key: персонализированный HTML в общем кеше — утечка данных между пользователями.</li>
    <li>Service Worker добавляет ещё один слой кеша поверх HTTP: precache-манифест обязан обновляться вместе с релизом, иначе SW будет отдавать старые файлы, которых уже нет.</li>
    <li>Инвалидация всего CDN на каждый деплой стоит денег и обнуляет hit ratio — точечный purge только для HTML.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как откатиться?</strong> Переключить префикс обратно: старая статика на месте, HTML меняется мгновенно. <strong>Что с ETag и Last-Modified?</strong> Для HTML полезны — экономят тело ответа при 304; для иммутабельной статики бессмысленны, потому что условного запроса быть не должно. <strong>Почему пользователи всё равно видят старую версию?</strong> Три обычных виновника: HTML закеширован промежуточным прокси или мобильным оператором, Service Worker отдаёт старую оболочку, или вкладка просто открыта неделю. Первое лечится корректным <code>Cache-Control</code> и <code>Vary</code>, второе — политикой обновления SW, третье — версией сборки в бандле и уведомлением «доступна новая версия» при расхождении.</p>`,
    code: `Cache-Control: public, max-age=31536000, immutable   # /assets/app.a1b2c3.js
Cache-Control: no-cache                              # /index.html

// ретрай динамического импорта после релиза
const load = () => import('./Editor').catch(() => { location.reload(); throw new Error('stale'); });`,
    tip: 'ChunkLoadError после деплоя — идеальная иллюстрация; обязательно упомяните хранение старых ассетов.' },

  { id: 'wx42',
    q: 'Что даёт CDN и что такое edge-рендеринг?',
    a: `<h4>Коротко</h4>
    <p>CDN — сеть узлов, отдающих контент из точки рядом с пользователем. Выигрыш не только в расстоянии: TLS терминируется на edge (экономия одного-двух RTT на хендшейке), соединение до origin уже прогрето и мультиплексировано, сверху добавляются защита от DDoS и сжатие. Edge-рендеринг — выполнение вашего кода на этих же узлах.</p>

    <h4>Как это работает</h4>
    <p>Ключевые понятия: <strong>cache hit ratio</strong> (доля ответов из кеша — главная метрика), <strong>cache key</strong> (что считается тем же ресурсом: путь, метод, заголовки из <code>Vary</code>, набор query-параметров), <strong>TTL</strong> и директивы <code>s-maxage</code> для промежуточных кешей, <code>stale-while-revalidate</code> (отдать устаревшее и обновить в фоне) и <code>stale-if-error</code> (отдать устаревшее, если origin лёг). Инвалидация бывает через purge по пути или тегу, а бывает через версионирование имён — второе надёжнее и дешевле.</p>
    <p><strong>Edge-функции</strong> (Cloudflare Workers, Vercel Edge, Fastly Compute) выполняются в изолятах V8 или в WASM-песочнице рядом с пользователем: холодный старт единицы миллисекунд вместо сотен у обычного serverless. Типичные задачи: A/B-разделение и бакетирование, геолокация и локаль, редиректы и нормализация URL, проверка авторизации, подстановка заголовков безопасности, streaming SSR. Ограничения: не Node (нет файловой системы и большинства нативных модулей), жёсткие лимиты CPU на запрос (обычно десятки миллисекунд), ограничение памяти, отсутствие долгих соединений.</p>

    <h4>Почему так</h4>
    <p>Кеширование HTML на CDN — самый большой одиночный выигрыш в TTFB, потому что оно убирает из критического пути и сеть до origin, и генерацию. Но HTML чаще всего персонализирован, и тут появляется развилка: либо выносить персонализацию на клиент («дырки», подгружаемые отдельно), либо сегментировать кеш по небольшому числу вариантов (страна, язык, залогинен или нет), либо собирать страницу на edge из кешируемых кусков.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>UTM-метки размывают кеш</strong>: каждый уникальный набор параметров создаёт свою запись, hit ratio падает до единиц процентов. Маркетинговые параметры надо игнорировать в cache key.</li>
    <li><strong>Edge-функция, которая ходит в далёкую БД</strong>, теряет весь выигрыш от близости: 40 мс экономии на TLS против 200 мс round-trip до региона с базой. Правило простое: на edge — то, что быстро и не требует данных; всё, что ходит в БД, лучше держать в регионе рядом с ней.</li>
    <li>Персонализированный ответ, случайно попавший в общий кеш, — утечка данных между пользователями. <code>Cache-Control: private</code>, <code>Vary: Cookie</code> и раздельные маршруты обязательны.</li>
    <li><code>Vary: User-Agent</code> практически уничтожает кеш — вариантов тысячи. Для адаптивной отдачи используйте client hints с ограниченным набором значений.</li>
    <li>Отсутствие <code>stale-if-error</code> означает, что падение origin мгновенно становится падением сайта, хотя кеш ещё полон.</li>
    <li>Логи и метрики после внедрения CDN уезжают: origin видит только промахи, и без логов edge вы теряете видимость реального трафика.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как поднять hit ratio?</strong> Нормализовать cache key, увеличить TTL с SWR, вынести персонализацию из HTML, проверить, не мешает ли <code>Set-Cookie</code> — многие CDN не кешируют ответы с ним. <strong>Что такое ESI?</strong> Сборка страницы из фрагментов с разными TTL на стороне edge — старая идея, которая в новом виде вернулась как partial prerendering. <strong>Всегда ли CDN ускоряет?</strong> Нет: для полностью динамического некешируемого ответа он добавляет ещё один прыжок, и выигрыш остаётся только от прогретого соединения и терминации TLS. Если hit ratio близок к нулю, CDN — это про надёжность и защиту, а не про скорость. <strong>Что мерить?</strong> Hit ratio по типам ресурсов, p75 TTFB по регионам и долю ответов, отданных из stale, — последняя показывает, насколько вас спасает кеш при проблемах на origin.</p>`,
    code: `Cache-Control: public, s-maxage=600, stale-while-revalidate=86400, stale-if-error=604800
Vary: Accept-Encoding`,
    tip: 'Замечание про edge-функцию, которая ходит в далёкую БД и теряет весь выигрыш, — зрелый архитектурный аргумент.' },

  { id: 'wx43',
    q: 'Как выстроить мониторинг ошибок фронтенда?',
    a: `<h4>Коротко</h4>
    <p>Ловим четыре источника: <code>window.onerror</code>, <code>unhandledrejection</code>, React error boundary и ошибки загрузки ресурсов (событие <code>error</code> в фазе захвата, потому что оно не всплывает). Плюс нарушения CSP через <code>report-to</code> и <code>SecurityPolicyViolationEvent</code>. Дальше всё решают source maps, группировка и фильтрация шума.</p>

    <h4>Как это работает</h4>
    <p>Событие уходит в сборщик с контекстом: релиз, окружение, user id (с оглядкой на приватность), маршрут, тип устройства, тип сети, значения feature-флагов, breadcrumbs — последовательность действий и запросов до падения. Стектрейс расшифровывается по картам, загруженным на деплое с привязкой к релизу. Группировка сводит однотипные события в одну проблему по fingerprint — по умолчанию по верхним кадрам стека и типу ошибки.</p>
    <p>Что делает мониторинг полезным, а не шумным. <strong>Маркировка деплоев</strong>: всплеск сразу привязывается к релизу и видно, что откатывать. <strong>Настроенный fingerprinting</strong>: без него одна ошибка с динамическим сообщением («Не найден заказ 12345») расползается на тысячи групп. <strong>Фильтрация</strong>: расширения браузера, схемы <code>chrome-extension://</code> и <code>safari-extension://</code>, «Script error.» от кросс-доменных скриптов без <code>crossorigin</code>, «ResizeObserver loop completed with undelivered notifications», отменённые запросы при уходе со страницы, боты и переводчики страниц. <strong>Сэмплирование</strong> трейсов и session replay, чтобы не разорить бюджет.</p>

    <h4>Почему так</h4>
    <p>Фронтенд-ошибки принципиально шумные: вы не контролируете ни браузер, ни расширения, ни сеть, ни блокировщики. Поток «сырых» событий на среднем проекте — тысячи в день, из которых действуют единицы. Поэтому мониторинг — это не «включить SDK», а построение фильтра сигнал/шум. Как только команда перестаёт верить алертам, инструмент становится бесполезен независимо от того, сколько он стоит.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>«Script error.»</strong> без стека — следствие SOP: браузер скрывает детали ошибок из кросс-доменных скриптов. Лечится атрибутом <code>crossorigin="anonymous"</code> на теге плюс <code>Access-Control-Allow-Origin</code> на CDN. Это самая частая слепая зона.</li>
    <li>Алерт по <strong>абсолютному числу</strong> ошибок ломается на любом изменении трафика; правильная метрика — доля сессий с ошибкой или доля неуспешных операций.</li>
    <li>PII в breadcrumbs и в теле запросов: адреса, телефоны, токены. Нужен scrubbing на SDK, до отправки, а не на сервере мониторинга.</li>
    <li>Блокировщики режут запросы к домену вендора — часть событий теряется; помогает проксирование через свой домен (tunnel).</li>
    <li>Ошибки, возникшие до инициализации SDK, теряются: инлайновый ранний хук, складывающий их в буфер, окупается.</li>
    <li>Session replay стоит дорого и трафиком, и приватностью — включайте выборочно и с маскированием полей ввода по умолчанию.</li>
    <li>У алерта должен быть владелец и SLA, иначе через полгода это свалка из 400 неразобранных проблем.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как отличить свою ошибку от чужой?</strong> По источнику кадра стека и по наличию своего релиза; события без единого своего кадра почти всегда из расширений. <strong>Что мониторить помимо исключений?</strong> Неуспешные API-запросы, ошибки загрузки чанков, нарушения CSP и «тихие» отказы — когда операция не упала, но и не завершилась. <strong>Как связать ошибку с бэкендом?</strong> Пробрасывать trace id из ответа сервера в контекст события: тогда из фронтового исключения можно провалиться в конкретный серверный запрос, а не гадать по времени. <strong>Что делать с ошибками, которые нельзя починить?</strong> Явно помечать их как ожидаемые и исключать из алертов, но продолжать считать — рост доли «ожидаемых» ошибок сам по себе сигнал.</p>`,
    code: `// ошибки загрузки ресурсов не всплывают — слушаем в capture-фазе
addEventListener('error', (e) => {
  if (e.target instanceof HTMLElement) report('resource', e.target.src || e.target.href);
}, true);
addEventListener('securitypolicyviolation', (e) => report('csp', e.violatedDirective, e.blockedURI));`,
    tip: 'Отдельно упомяните «Script error.» и необходимость crossorigin на скриптах — это частая слепая зона.' },

  { id: 'wx44',
    q: 'Как использовать feature flags и canary-выкладку на фронтенде?',
    a: `<h4>Коротко</h4>
    <p>Флаги отделяют <strong>деплой</strong> от <strong>релиза</strong>: код уже в проде, но включён для 1% или только для своей команды. Canary — постепенное расширение аудитории с наблюдением за метриками, где откат делается переключением флага за секунды, а не выкаткой предыдущей сборки за минуты.</p>

    <h4>Как это работает</h4>
    <p>Виды флагов различаются сроком жизни и владельцем: <strong>release toggles</strong> (временные, живут до полного раската), <strong>experiment toggles</strong> (A/B, живут до подведения итогов), <strong>ops toggles</strong> (kill switch для тяжёлой фичи или внешней интеграции, живут вечно), <strong>permission toggles</strong> (доступ по тарифу — это уже не флаг, а бизнес-логика).</p>
    <p>Canary разворачивают ступенями 1% → 5% → 25% → 50% → 100%, на каждой смотрят на долю сессий с ошибкой, p75 CWV и продуктовую метрику. Бакетирование должно быть <strong>устойчивым</strong>: хеш от идентификатора пользователя, а не случайное число при загрузке, — иначе пользователь при каждом заходе видит разный интерфейс, а результаты эксперимента становятся шумом.</p>
    <p>Фронтендовая специфика: значение флага должно быть известно <strong>до первого рендера</strong>. Иначе интерфейс мигает и вёрстка сдвигается — тот же ущерб CLS, что и у внешних A/B-скриптов. Решения: получать флаги на сервере при SSR и инлайнить их в HTML, либо держать локальный кеш последних значений и стартовать с него, обновляя в фоне.</p>

    <h4>Почему так</h4>
    <p>Откат релиза на фронтенде дорог: нужно пересобрать или переключить артефакт, дождаться инвалидации CDN, а у части пользователей вкладки уже открыты со старым HTML. Флаг убирает всё это — конфигурация меняется независимо от артефакта. Плата — комбинаторный взрыв: десять булевых флагов дают 1024 возможных состояния приложения, и протестировать их все нельзя. Отсюда дисциплина удаления, а не только создания.</p>

    <h4>Подводные камни</h4>
    <ul><li>Код обеих веток попадает в бандл и увеличивает его. Для крупных фич — динамический <code>import()</code> под флагом; для окончательно мёртвых веток — удаление на этапе сборки через compile-time константы.</li>
    <li><strong>Клиентский флаг не защищает данные</strong>: скрытая кнопка остаётся в бандле, а эндпоинт доступен. Проверка прав — всегда на сервере. И сам список флагов не должен раскрывать неанонсированные фичи: отдавайте только то, что относится к текущему пользователю.</li>
    <li>У флага должны быть владелец и срок жизни, зафиксированные при создании. Задача на удаление заводится одновременно с флагом, иначе через год их сотни и никто не помнит, что можно выключить.</li>
    <li>Флаг, от которого зависит структура данных или миграция, откатить нельзя — это уже не флаг, а этап миграции, и обращаться с ним нужно иначе.</li>
    <li>Задержка провайдера флагов становится вашей задержкой: нужен таймаут, дефолтные значения и кеш, чтобы падение сервиса флагов не роняло приложение.</li>
    <li>Метрики надо смотреть <strong>в разрезе варианта</strong>: усреднённый график скроет, что у 5% всё сломалось.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как тестировать код с флагами?</strong> Покрывать обе ветки в тестах и держать e2e-прогон на конфигурации «все новые флаги включены». <strong>Чем canary отличается от blue-green?</strong> Blue-green переключает трафик целиком между двумя окружениями, canary раскатывает постепенно и позволяет сравнивать метрики групп. <strong>Сколько держать процент?</strong> Столько, сколько нужно для статистической значимости по вашей метрике: на низком трафике 1% может не дать сигнала неделями, и тогда честнее начинать с 10%. <strong>Кто принимает решение о раскатке?</strong> Заранее оговорённые пороги отката (доля ошибок, p75 LCP, ключевая конверсия), а не обсуждение постфактум — иначе каждый шаг превращается в спор.</p>`,
    code: `<!-- SSR инлайнит флаги до первого рендера: ни мигания, ни сдвига -->
<script>window.__FLAGS__ = {"newCheckout":true,"search_v2":false}</script>

const Checkout = flags.newCheckout
  ? lazy(() => import('./CheckoutV2'))
  : lazy(() => import('./CheckoutV1'));`,
    tip: 'Требование «флаг должен быть известен до первого рендера» — специфическая фронтендовая деталь, её оценят.' },

  { id: 'wx45',
    q: 'Почему A/B-тесты портят CLS и как это исправить?',
    snippet: `<!-- anti-flicker snippet, рекомендованный вендором -->
<style>body { opacity: 0 !important }</style>
<script>
  setTimeout(function () { document.body.style.opacity = 1 }, 4000);
</script>
<script src="https://cdn.abtest.example/exp.js" async></script>`,
    a: `<h4>Коротко</h4>
    <p>Внешний скрипт-редактор применяет изменения <strong>после</strong> первого рендера: пользователь видит контрольный вариант, потом перестановку DOM — это FOOC и прямой удар по CLS. Сниппет выше «лечит» мигание, скрывая <code>body</code> на срок до 4 секунд: CLS действительно падает, но LCP и FCP уезжают ровно на это время. Симптом заменён на худшую болезнь.</p>

    <h4>Как это работает</h4>
    <p>Классическая схема с Optimizely, VWO или экспериментами в GTM: страница приходит в контрольном варианте, асинхронный скрипт загружается, решает, в какой бакет попал пользователь, и мутирует DOM — меняет тексты, прячет блоки, переставляет элементы. Каждая такая мутация после отрисовки порождает layout shift, и он попадает в CLS, если произошёл не в течение 500 мс после ввода пользователя.</p>
    <p>Anti-flicker snippet прячет содержимое до готовности эксперимента и снимает скрытие по таймауту. Проблема в том, что LCP засчитывается по <strong>видимой</strong> отрисовке: пока <code>body</code> прозрачен, LCP-кандидата нет. При медленной сети или сбое CDN вендора пользователь смотрит на белый экран весь таймаут, а метрика получает соответствующее значение. Дополнительно синхронный скрипт эксперимента блокирует парсер и растит TBT.</p>

    <h4>Почему так</h4>
    <p>Инструменты визуального редактирования продавались как «эксперименты без разработчиков» — и цена этого удобства в том, что решение о варианте принимается в браузере, уже после начала рендера. Технически иначе и не могло быть: скрипт не может повлиять на HTML, который сервер уже отправил. Значит, единственный способ убрать сдвиг — принимать решение <strong>раньше</strong>, до отдачи разметки.</p>

    <h4>Подводные камни</h4>
    <ul><li><strong>Server-side или edge-эксперименты</strong>: вариант выбирается до отдачи HTML, браузер получает финальную разметку — ни мигания, ни сдвига, ни лишнего скрипта. Требует включить вариант в cache key CDN и держать стабильное бакетирование по cookie, иначе пользователь будет прыгать между вариантами и попадёт в оба сегмента аналитики.</li>
    <li><strong>CSS-класс на <code>&lt;html&gt;</code></strong>, проставленный крошечным инлайновым скриптом в <code>&lt;head&gt;</code>, когда оба варианта уже есть в CSS, — быстрый компромисс без внешнего скрипта и без сдвига. Ограничение: только визуальные различия, не структурные.</li>
    <li><strong>Резервирование места</strong> под изменяемый блок (<code>min-height</code>, <code>aspect-ratio</code>, скелет фиксированного размера) делает даже позднюю подмену безобидной для соседей.</li>
    <li>Измерять CWV надо <strong>в разрезе вариантов</strong>. Эксперимент, выигравший 2% конверсии и ухудшивший LCP на секунду, в долгую проигрывает — но узнать это можно, только если метрики разложены по бакетам.</li>
    <li>Скрипт эксперимента почти всегда third-party: его падение не должно ронять страницу, поэтому <code>async</code>, таймаут и дефолт в контрольный вариант обязательны.</li>
    <li>Один и тот же пользователь на разных устройствах попадает в разные бакеты, если бакетирование по анонимной куке, — это загрязняет результаты.</li></ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как убедить маркетинг отказаться от визуального редактора?</strong> Показать разницу p75 LCP на страницах со скриптом и без — обычно это 0,5–1,5 секунды. <strong>Что такое partial prerendering в этом контексте?</strong> Статическая оболочка из кеша плюс персонализированные и экспериментальные дырки, догружаемые потоком, — способ сохранить и кеш, и вариативность. <strong>А если эксперимент нужен ниже сгиба?</strong> Тогда клиентский вариант почти безвреден: сдвиг за пределами вьюпорта в CLS не попадает, а LCP уже случился. Правило простое — визуальные эксперименты в первом экране делают на сервере, всё остальное можно оставить скрипту. <strong>Как посчитать цену эксперимента?</strong> Сравнить p75 LCP и долю сессий с CLS выше 0,1 на страницах со скриптом и без него: это и есть та цифра, которую стоит принести на обсуждение.</p>`,
    code: `<!-- решение принято до отдачи HTML: варианта в разметке уже нет -->
<html class="exp-checkout-b">
<!-- оба варианта описаны в CSS, сдвига не возникает -->
.exp-checkout-b .cta { order: -1 }`,
    tip: 'Скажите, что anti-flicker snippet лечит симптом ценой LCP — это ровно тот компромисс, который ждут услышать.' }

];
