/**
 * Колода «Паттерны фронтенда»: формат «проблема → решение».
 *
 * Вопрос формулируется как боль в коде, а не как термин. Термин появляется
 * в ответе. Секции: В чём боль / Решение / Почему так лучше / Когда не надо /
 * Что спросят следом. В code — контраст «было / стало».
 */
const DECK_PATTERNS = [

  { id: 'pt1',
    q: 'Проп currentUser прокидывается через пять компонентов, четырём из них он вообще не нужен. Как это лечить?',
    a: `<h4>В чём боль</h4>
<p>Это prop drilling. Каждый промежуточный компонент обязан объявить проп в типах, принять его и передать дальше — при том что сам его не использует. Цена: переименование поля правит пять файлов; в сигнатуре <code>Layout</code> болтается <code>currentUser</code>, к вёрстке отношения не имеющий; тесты промежуточных компонентов вынуждены подставлять фейкового пользователя; <code>React.memo</code> на середине цепочки бесполезен, потому что новый пропс всё равно приходит сверху.</p>

<h4>Решение</h4>
<p>Три средства по возрастанию цены, выбирать самое дешёвое из подходящих.</p>
<ol>
<li><strong>Композиция через children/слоты.</strong> Владелец данных сам рендерит готовый JSX и отдаёт его вниз как <code>children</code>. Промежуточные уровни становятся «трубами»: они не знают, что внутри. Это решает большинство случаев и не добавляет ни одной абстракции.</li>
<li><strong>Контекст</strong> — для «окружения»: текущий пользователь, тема, локаль, i18n-функция. Признак: значение меняется редко и нужно многим в разных ветках.</li>
<li><strong>Внешний стор с подпиской по селектору</strong> (Zustand, Redux, <code>useSyncExternalStore</code>) — когда значение меняется часто и подписчик должен просыпаться только на свой кусок.</li>
</ol>

<h4>Почему так лучше</h4>
<p>Композиция убирает лишнюю связанность бесплатно: сигнатуры сужаются до того, что компонент реально использует, а место, где данные берутся, остаётся одно. Контекст убирает бойлерплейт, но платим неявностью — зависимость больше не видна в сигнатуре, и статически найти всех потребителей нельзя. Стор добавляет гранулярность подписки, но и внешнюю библиотеку с собственными правилами.</p>

<h4>Когда не надо</h4>
<p>Один-два уровня прокидывания — не проблема, а самая читаемая форма связи: явный проп лучше невидимого контекста. Не тащите в контекст часто меняющееся значение (позиция курсора, текст поля) — разбудите всё поддерево. И не начинайте с Redux ради одного пользователя: сначала попробуйте перестроить дерево.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему memo не спасает потребителя контекста?</strong> Подписка на контекст живёт на фибере, а не в пропсах: React будит потребителя в обход сравнения пропсов. <strong>Как понять, что пора в стор?</strong> Когда одному значению нужны подписчики в несвязанных ветках дерева и оно меняется чаще пары раз в минуту.</p>`,
    code: `// было: currentUser едет через Layout и Sidebar, которым он не нужен
function App() {
  const user = useUser();
  return <Layout currentUser={user} />;
}
function Layout({ currentUser }) {          // не использует
  return <Sidebar currentUser={currentUser} />;
}
function Sidebar({ currentUser }) {         // не использует
  return <UserBadge currentUser={currentUser} />;
}

// стало: композиция — Layout и Sidebar ничего не знают о пользователе
function App() {
  const user = useUser();
  return (
    <Layout>
      <Sidebar>
        <UserBadge user={user} />
      </Sidebar>
    </Layout>
  );
}
function Layout({ children }) { return <div className="layout">{children}</div>; }
function Sidebar({ children }) { return <aside>{children}</aside>; }

// либо контекст, если ветки далеко друг от друга
const UserCtx = createContext(null);
const useCurrentUser = () => {
  const v = useContext(UserCtx);
  if (!v) throw new Error('useCurrentUser вне UserProvider');
  return v;                                  // обёртка вместо голого useContext
};`,
    tip: 'Сильный ответ начинается не с контекста, а с вопроса «а можно перестроить дерево через children?». Контекст — это уже про неявную зависимость, за которую платят при рефакторинге.' },

  { id: 'pt2',
    q: 'Одно и то же значение лежит и в сторе, и в локальном useState компонента. Периодически они разъезжаются. Что не так?',
    a: `<h4>В чём боль</h4>
<p>Это дублирование источника правды. Классика: список пришёл в стор, компонент скопировал его в <code>useState</code> для локальной сортировки, а дальше стор обновился — и на экране старая копия. Или наоборот: пользователь отредактировал локальную копию, а другой компонент читает стор и показывает прежнее. Симптомы узнаваемые: «после обновления надо перезайти на вкладку», «в двух местах разные числа», «помогает F5». Каждый такой баг лечат добавлением ещё одного <code>useEffect</code>-синхронизатора, и связей становится больше.</p>

<h4>Решение</h4>
<p><strong>Single source of truth</strong>: у каждого факта ровно один владелец, всё остальное — производные, вычисляемые на рендере. Практический алгоритм: 1) выписать, какие данные реально независимы; 2) оставить их в одном месте; 3) остальное считать функцией от них прямо в рендере (<code>const visible = useMemo(() =&gt; sort(items, order), [items, order])</code>); 4) удалить эффекты-синхронизаторы.</p>
<p>Если локальная копия нужна по существу — например, черновик формы, который применяется по «Сохранить», — это <strong>другое состояние</strong>, а не копия. Тогда назовите его иначе (<code>draft</code>), явно опишите момент фиксации и момент сброса, и не пытайтесь держать его в синхроне непрерывно.</p>

<h4>Почему так лучше</h4>
<p>Расхождение становится невозможным по построению, а не «не встречается». Уходит целый класс эффектов, каждый из которых — лишний рендер и лишняя точка отказа. Код читается сверху вниз: видно, что чем считается.</p>

<h4>Когда не надо</h4>
<p>Осознанный черновик копией не считается — там дублирование законно. Ещё случай: тяжёлая нормализация данных, которую невыгодно пересчитывать каждый рендер. Тогда кешируйте её в <code>useMemo</code> или в селекторе стора — но это по-прежнему производное, а не второй владелец. И не тяните в стор то, что нужно одному компоненту: единый источник правды не означает «всё в глобальном сторе».</p>

<h4>Что спросят следом</h4>
<p><strong>Как отличить производное от настоящего состояния?</strong> Вопрос «могу ли я вычислить это из остального?» Если да — производное. <strong>Что делать с дорогим вычислением?</strong> <code>useMemo</code> или мемоизированный селектор; профилируйте, чаще всего сортировка тысячи строк дешевле лишнего рендера.</p>`,
    code: `// было: копия списка в локальном стейте + эффект-синхронизатор
function List() {
  const items = useStore(s => s.items);
  const [rows, setRows] = useState(items);     // копия
  const [order, setOrder] = useState('asc');

  useEffect(() => { setRows(items); }, [items]);            // синхронизация
  useEffect(() => { setRows(r => sort(r, order)); }, [order]); // и ещё одна
  // items обновился во время сортировки -> порядок и данные разъехались
}

// стало: один владелец, остальное вычисляется
function List() {
  const items = useStore(s => s.items);        // источник правды
  const [order, setOrder] = useState('asc');   // независимое состояние
  const rows = useMemo(() => sort(items, order), [items, order]);
  // эффектов нет, разъехаться нечему
}`,
    tip: 'Формулировка, которая закрывает вопрос: «состояние — это то, что нельзя вычислить из остального». Дальше добавьте, что черновик формы — не копия, а отдельное состояние с явным моментом фиксации.' },

  { id: 'pt3',
    q: 'В компоненте есть useEffect, который на изменение пропа items пересчитывает filtered и кладёт в setState. Что здесь плохо?',
    a: `<h4>В чём боль</h4>
<p>Это derived state, синхронизируемое эффектом — самый частый антипаттерн в React-коде. Механика: React рендерит компонент со старым <code>filtered</code>, коммитит его в DOM, только потом запускает эффект, тот вызывает <code>setState</code> и запускает второй рендер. Итог — <strong>лишний коммит с заведомо устаревшими данными на экране</strong>. При медленном рендере это видимое мигание; при списках — «прыжок» контента.</p>
<p>Дальше хуже: массив зависимостей приходится вручную поддерживать, забытая зависимость даёт залипшее значение, а лишняя — бесконечный цикл. Плюс в StrictMode эффект выполняется дважды, что выпячивает любую нечистоту.</p>

<h4>Решение</h4>
<p>Считать прямо во время рендера. <code>const filtered = items.filter(...)</code> — и всё. Если фильтрация реально дорогая (профайлер, а не ощущение), обернуть в <code>useMemo</code> с честными зависимостями. Правило: <strong>эффекты — для синхронизации с внешним миром</strong> (сеть, DOM-API, подписки, таймеры), а не для синхронизации состояния с состоянием.</p>
<p>Частные случаи: сброс состояния при смене сущности решается пропом <code>key</code> на компоненте, а не эффектом; «поменять состояние в ответ на клик» — обработчиком, а не эффектом на изменение флага.</p>

<h4>Почему так лучше</h4>
<p>Один рендер вместо двух, невозможность рассинхрона, отсутствие массива зависимостей как источника багов. Код короче на 6–8 строк и читается как формула. Цена — вычисление выполняется на каждом рендере, но в 90% случаев это микросекунды против миллисекунд лишнего коммита.</p>

<h4>Когда не надо</h4>
<p>Если результат зависит от асинхронного источника (запрос по изменившемуся фильтру) — это уже не производное, а загрузка данных: эффект или библиотека запросов уместны. Если вычисление действительно тяжёлое и вход меняется редко — <code>useMemo</code>. Если нужно сохранить то, что пользователь редактировал поверх производного значения, — это уже черновик, отдельное состояние.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему useMemo не гарантия?</strong> Это подсказка: React вправе выбросить кеш, поэтому на <code>useMemo</code> нельзя строить корректность, только скорость. <strong>Как сбросить состояние при смене id?</strong> <code>&lt;Editor key={id} /&gt;</code> — React размонтирует и смонтирует заново, состояние инициализируется само.</p>`,
    code: `// было: производное состояние через эффект — два рендера и шанс рассинхрона
function List({ items, query }) {
  const [filtered, setFiltered] = useState([]);
  useEffect(() => {
    setFiltered(items.filter(i => i.name.includes(query)));
  }, [items, query]);           // забыли query -> залипший результат
  return <Rows data={filtered} />;
}

// стало: вычисление на рендере
function List({ items, query }) {
  const filtered = items.filter(i => i.name.includes(query));
  return <Rows data={filtered} />;
}

// стало (если профайлер показал, что дорого)
function List({ items, query }) {
  const filtered = useMemo(
    () => items.filter(i => i.name.includes(query)),
    [items, query]
  );
  return <Rows data={filtered} />;
}`,
    tip: 'Ключевая фраза: «эффект — это синхронизация с внешним миром, а не с собственным состоянием». Добавьте про лишний коммит со старыми данными — это то, что видно пользователю, а не только рефакторинг ради красоты.' },

  { id: 'pt4',
    q: 'В компоненте пять булевых флагов: isLoading, isError, isSuccess, isEmpty, isRetrying. Периодически показываются одновременно спиннер и ошибка. Что делать?',
    a: `<h4>В чём боль</h4>
<p>Пять независимых булевых даёт 32 комбинации, из которых валидны четыре. Остальные 28 — баги, ждущие своего часа: забыли <code>setLoading(false)</code> в <code>catch</code> — спиннер поверх ошибки; не сбросили <code>isError</code> перед повтором — ошибка поверх новых данных. Каждый такой баг лечится ещё одним сбросом в ещё одном месте, и логика превращается в набор ритуалов «не забыть выключить».</p>

<h4>Решение</h4>
<p><strong>Конечный автомат</strong>: одно поле <code>status</code> с перечислением состояний и явные переходы. В TypeScript — размеченное объединение (discriminated union), где у каждого статуса свои поля: <code>data</code> есть только в <code>success</code>, <code>error</code> — только в <code>failure</code>. Невалидные комбинации перестают компилироваться, а не «не встречаться».</p>
<p>Переходы описываются редьюсером: <code>idle → loading → success | failure</code>, из <code>failure</code> по <code>retry</code> снова в <code>loading</code>. Если переходов много и они условные, стоит взять XState; для 4–6 состояний хватает <code>useReducer</code> и <code>switch</code>.</p>

<h4>Почему так лучше</h4>
<p>Во-первых, невозможные состояния становятся непредставимыми — это проверяет компилятор, а не ревьюер. Во-вторых, рендер становится исчерпывающим <code>switch</code>: добавили статус — TypeScript показал все места, где его забыли обработать. В-третьих, переходы тестируются как чистая функция, без рендера.</p>

<h4>Когда не надо</h4>
<p>Один-два независимых флага (<code>isOpen</code>, <code>isChecked</code>) автомата не требуют — это оверинжиниринг. Не сводите в один статус <strong>ортогональные</strong> измерения: «идёт фоновое обновление» и «данные уже есть» — это не альтернативы, а два параллельных признака, как <code>status</code> плюс <code>isFetching</code> в TanStack Query. Попытка утрамбовать ортогональное в один enum рождает состояния вроде <code>successButRefreshing</code>, и вы вернулись к тому же хаосу.</p>

<h4>Что спросят следом</h4>
<p><strong>Когда брать XState?</strong> Когда есть параллельные и вложенные состояния, отложенные переходы, история — то есть когда <code>switch</code> перестаёт читаться. <strong>Как реализовать в TS?</strong> Union по литеральному полю <code>status</code> плюс сужение через <code>if (state.status === 'success')</code> — внутри ветки <code>state.data</code> уже гарантированно есть.</p>`,
    code: `// было: флаги, между которыми возможны невалидные пары
const [isLoading, setLoading] = useState(false);
const [isError, setError] = useState(false);
const [data, setData] = useState(null);

async function load() {
  setLoading(true);
  try { setData(await api.get()); }
  catch { setError(true); }      // забыли setLoading(false) -> спиннер + ошибка
}

// стало: размеченное объединение и явные переходы
type State =
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'success'; data: Item[] }
  | { status: 'failure'; error: string };

function reducer(s: State, a: Action): State {
  switch (a.type) {
    case 'fetch': return { status: 'loading' };
    case 'ok':    return { status: 'success', data: a.data };
    case 'fail':  return { status: 'failure', error: a.error };
    default:      return s;
  }
}

// рендер — исчерпывающий разбор, забыть ветку не даст компилятор
switch (state.status) {
  case 'idle':    return null;
  case 'loading': return <Spinner />;
  case 'success': return <Rows data={state.data} />;
  case 'failure': return <Error text={state.error} onRetry={retry} />;
}`,
    tip: 'Обязательно скажите про ортогональные измерения: «загружается» и «уже есть данные» нельзя схлопывать в один enum — именно поэтому в TanStack Query рядом со status живёт isFetching.' },

  { id: 'pt5',
    q: 'Состояние поля поиска подняли в корневой компонент — теперь при каждом нажатии клавиши перерисовывается вся страница. Как исправить, не ломая архитектуру?',
    a: `<h4>В чём боль</h4>
<p>Состояние подняли выше, чем нужно. React перерисовывает поддерево от компонента, где случился <code>setState</code>, — а это корень. На каждый символ пересобирается всё дерево элементов, включая шапку, сайдбар и футер, которым текст запроса не нужен. Пока символов мало, это незаметно; на сотне узлов ввод начинает отставать от клавиатуры, и появляется классическая жалоба «текст печатается с задержкой».</p>

<h4>Решение</h4>
<p>Три приёма, обычно в такой очерёдности.</p>
<ol>
<li><strong>Опустить состояние вниз</strong> (colocation): выделить из корня маленький компонент, который владеет <code>query</code> и рендерит только поле и то, что от него зависит. Правило: состояние живёт в ближайшем общем предке потребителей, не выше.</li>
<li><strong>Поднять содержимое наверх через children</strong>: если состояние обязано быть в корне, отдать тяжёлые части как <code>children</code>. Элементы, пришедшие пропсом, при рендере родителя не пересоздаются, поэтому React переиспользует их фиберы и не рендерит поддерево заново.</li>
<li><strong>Разделить срочное и несрочное</strong>: <code>useDeferredValue</code> для значения, по которому фильтруется тяжёлый список — поле обновляется мгновенно, список догоняет.</li>
</ol>

<h4>Почему так лучше</h4>
<p>Первые два приёма ничего не стоят в рантайме: это перестановка дерева, а не мемоизация, которую надо поддерживать. Мемоизация же требует, чтобы все пропсы были стабильны, и ломается от одного забытого инлайн-объекта. <code>useDeferredValue</code> честно оставляет один лишний рендер, но снимает лаг ввода.</p>

<h4>Когда не надо</h4>
<p>Если значением реально пользуются в трёх разных ветках, опускать его вниз некуда — тогда стор с подпиской по селектору или контекст плюс дробление. Не начинайте с <code>useDeferredValue</code>: он маскирует проблему, а не убирает работу — тяжёлый рендер по-прежнему происходит, просто с меньшим приоритетом. И не разносите состояние по десяти компонентам ради микросекунд: читаемость дороже.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему children спасает без memo?</strong> Родитель не создаёт эти элементы заново — он получает тот же объект по ссылке, и React на этапе согласования видит идентичный элемент. <strong>Чем useDeferredValue отличается от debounce?</strong> Debounce задерживает всегда на фиксированное время; deferred value отдаёт результат сразу, если машина успевает, и прерывается новым вводом.</p>`,
    code: `// было: query в корне, при каждом символе перерисовывается всё
function App() {
  const [query, setQuery] = useState('');
  return (
    <>
      <Header />                          {/* перерисовывается зря */}
      <input value={query} onChange={e => setQuery(e.target.value)} />
      <HeavyResults query={query} />
      <Footer />                          {/* и этот тоже */}
    </>
  );
}

// стало (1): состояние опущено к своим потребителям
function SearchPanel() {
  const [query, setQuery] = useState('');
  return (
    <>
      <input value={query} onChange={e => setQuery(e.target.value)} />
      <HeavyResults query={query} />
    </>
  );
}
function App() {
  return <><Header /><SearchPanel /><Footer /></>;
}

// стало (2): состояние обязано быть наверху -> тяжёлое приходит как children
function Shell({ children }) {
  const [query, setQuery] = useState('');
  return <><input value={query} onChange={e => setQuery(e.target.value)} />{children}</>;
}
<Shell><VeryHeavyTree /></Shell>   // VeryHeavyTree не рендерится на ввод`,
    tip: 'Приём с children почти никто не называет, а он самый дешёвый: элементы, полученные пропсом, родитель не пересоздаёт, поэтому поддерево не рендерится вообще без всякого memo.' },

  { id: 'pt6',
    q: 'Список пользователей с сервера лежит в Redux: экшены fetchStart, fetchSuccess, fetchError и ручная инвалидация. Кода много, данные всё равно устаревают. Что предложишь?',
    a: `<h4>В чём боль</h4>
<p>Серверные данные положили в клиентский стор и обращаются с ними как с собственным состоянием. Но у серверных данных другая природа: они <strong>не принадлежат клиенту</strong>, устаревают сами по себе, могут измениться в другой вкладке или у другого пользователя. Отсюда ручная работа, которую приходится писать самому: дедупликация одинаковых запросов, отмена гонок, повторы при ошибке, обновление при возврате на вкладку, инвалидация после мутаций. Каждый экран получает свои три экшена и свой редьюсер, и половина стора — это копия базы с лагом.</p>

<h4>Решение</h4>
<p>Разделить состояние на <strong>серверное (кеш)</strong> и <strong>клиентское (UI)</strong>. Серверное отдать библиотеке запросов — TanStack Query, RTK Query, SWR: они дают ключ запроса, дедупликацию, <code>staleTime</code>/<code>gcTime</code>, повторы, отмену, инвалидацию по ключу и фоновое обновление. В сторе остаётся то, чего на сервере нет: открытые модалки, выбранные строки, черновики, настройки отображения.</p>
<p>Ментальная модель меняется: не «положить и синхронизировать», а «спросить у кеша; если протух — он сам сходит на сервер».</p>

<h4>Почему так лучше</h4>
<p>Из кода уходят сотни строк однотипных редьюсеров, а с ними — гонки и забытая инвалидация. Появляется поведение, которое руками почти никто не пишет: дедуп параллельных запросов одного ключа, <code>refetchOnWindowFocus</code>, экспоненциальные повторы. Цена — ещё одна зависимость, свой словарь (<code>queryKey</code>, <code>stale</code>, <code>invalidate</code>) и риск закешировать лишнее.</p>

<h4>Когда не надо</h4>
<p>Если данные читаются один раз при загрузке и больше не меняются (конфиг, справочник) — хватит обычного запроса при старте. Если у вас уже RSC и данные приходят с сервера при навигации — клиентский кеш может оказаться лишним слоем. И не тащите библиотеку в приложение с тремя запросами: выигрыш не окупит новый словарь для команды.</p>

<h4>Что спросят следом</h4>
<p><strong>Что тогда остаётся в Redux?</strong> Клиентское состояние: UI-флаги, мастера-визарды, выбор в таблице, оффлайн-очередь. <strong>Чем staleTime отличается от gcTime?</strong> <code>staleTime</code> — сколько данные считаются свежими и не перезапрашиваются; <code>gcTime</code> — сколько неиспользуемый кеш живёт в памяти до сборки.</p>`,
    code: `// было: серверные данные вручную разложены по редьюсерам
const slice = createSlice({
  name: 'users',
  initialState: { items: [], loading: false, error: null },
  reducers: {
    fetchStart(s) { s.loading = true; s.error = null; },
    fetchSuccess(s, a) { s.loading = false; s.items = a.payload; },
    fetchError(s, a) { s.loading = false; s.error = a.payload; },
  },
});
// плюс thunk, плюс ручная инвалидация после каждой мутации,
// плюс отмена гонок, которую обычно забывают

// стало: серверное — в кеше запросов, клиентское — в сторе
function useUsers(filter) {
  return useQuery({
    queryKey: ['users', filter],       // ключ = идентичность данных
    queryFn: ({ signal }) => api.getUsers(filter, { signal }), // отмена из коробки
    staleTime: 30_000,
  });
}

const { mutate } = useMutation({
  mutationFn: api.createUser,
  onSuccess: () => qc.invalidateQueries({ queryKey: ['users'] }), // одна строка
});

// в Redux остаётся только UI
const ui = createSlice({ name: 'ui', initialState: { selectedIds: [], modal: null } });`,
    tip: 'Сформулируйте границу одной фразой: «в сторе живёт то, чего нет на сервере». Дальше добавьте, что queryKey — это не название, а идентичность данных: именно по нему работают дедуп и инвалидация.' },

  { id: 'pt7',
    q: 'Пользователь настроил фильтры и сортировку в таблице, скинул ссылку коллеге — тот увидел пустую таблицу по умолчанию. Где ошибка проектирования?',
    a: `<h4>В чём боль</h4>
<p>Состояние, которое должно быть адресуемым, держат в <code>useState</code>. Признаки одни и те же: ссылка не воспроизводит экран, кнопка «назад» закрывает страницу вместо отмены фильтра, F5 сбрасывает работу, аналитика видит один URL для десяти разных экранов, а SSR не может отрендерить нужный вид, потому что фильтры существуют только после гидратации.</p>

<h4>Решение</h4>
<p>Признать URL частью состояния приложения. Правило: <strong>если состояние стоит того, чтобы его можно было переслать, оно живёт в URL</strong>. Туда идут: идентификатор открытой сущности, вкладка, фильтры, сортировка, страница пагинации, поисковый запрос. Не идут: позиция скролла, состояние hover, открыт ли дропдаун, черновик неотправленной формы.</p>
<p>Технически — <code>useSearchParams</code> (React Router, Next.js) или тонкая обёртка вокруг <code>URLSearchParams</code> плюс <code>history.replaceState</code>. Ключевые детали: сериализация должна быть однозначной и короткой; при вводе в поле поиска — <code>replace</code>, а не <code>push</code>, иначе каждый символ окажется в истории; значения по умолчанию в URL не пишутся, чтобы ссылка оставалась читаемой.</p>

<h4>Почему так лучше</h4>
<p>Экран становится воспроизводимым: ссылка в тикете открывает ровно то, о чём тикет. Кнопки «назад» и «вперёд» начинают работать бесплатно, потому что историю ведёт браузер. SSR получает всё необходимое из запроса. Поддержка тоже упрощается: одно состояние вместо пары «стейт плюс синхронизация с URL».</p>

<h4>Когда не надо</h4>
<p>Длина URL ограничена практикой (~2000 символов для надёжности) — сложное дерево фильтров туда не влезет, и base64 от JSON превращает ссылку в нечитаемую строку. В таких случаях сохраняют «вид» на сервере и кладут в URL его короткий id. Не выносите в URL приватные данные: ссылки попадают в логи, Referer и историю. И не выносите высокочастотное — <code>replaceState</code> на каждый пиксель скролла тормозит.</p>

<h4>Что спросят следом</h4>
<p><strong>push или replace?</strong> Push — для смысловых переходов (сменил вкладку), replace — для непрерывного ввода. <strong>Как не потерять типы?</strong> Валидируйте параметры схемой (zod) при чтении: URL правят руками, и <code>page=abc</code> обязан деградировать в дефолт, а не ронять экран.</p>`,
    code: `// было: фильтры живут только в памяти вкладки
function Table() {
  const [status, setStatus] = useState('all');
  const [page, setPage] = useState(1);
  // ссылка не воспроизводит экран, back закрывает страницу
}

// стало: URL — источник правды, состояние выводится из него
function Table() {
  const [params, setParams] = useSearchParams();
  const status = params.get('status') ?? 'all';
  const page = Number(params.get('page') ?? 1);

  const update = (patch, mode = 'push') => {
    const next = new URLSearchParams(params);
    for (const [k, v] of Object.entries(patch)) {
      if (v == null || v === DEFAULTS[k]) next.delete(k);  // дефолты не пишем
      else next.set(k, String(v));
    }
    setParams(next, { replace: mode === 'replace' });
  };

  return (
    <>
      <StatusFilter value={status} onChange={v => update({ status: v, page: 1 })} />
      <SearchBox onType={q => update({ q }, 'replace')} />  {/* не засоряем историю */}
      <Pager page={page} onChange={p => update({ page: p })} />
    </>
  );
}`,
    tip: 'Критерий в одну фразу: «состояние, которое имеет смысл переслать ссылкой, живёт в URL». И добавьте про валидацию параметров схемой — URL правят руками, и код обязан это пережить.' },

  { id: 'pt8',
    q: 'Форма из двадцати полей: значения лежат в состоянии родительской страницы, и на каждое нажатие клавиши перерисовывается вся страница. Как перестроить?',
    a: `<h4>В чём боль</h4>
<p>Состояние формы подняли туда, где оно не нужно: странице нужен только <strong>результат</strong> отправки, а она получает каждый промежуточный символ. Каждое нажатие клавиши — рендер страницы целиком, включая соседние виджеты и списки. Плюс сигнатура страницы обрастает двадцатью <code>value</code> и двадцатью <code>onChange</code>, а валидация размазывается между родителем и полями.</p>

<h4>Решение</h4>
<p>Форма владеет своим состоянием, наружу отдаёт только события жизненного цикла: <code>onSubmit(values)</code>, при необходимости <code>onDirtyChange</code>. Внутри — три варианта по нарастанию:</p>
<ol>
<li><strong>Неуправляемые поля</strong>: <code>defaultValue</code> плюс чтение через <code>FormData</code> на сабмите. Ноль рендеров на ввод, минимум кода. Годится, когда во время ввода ничего не зависит от значений.</li>
<li><strong>Локальное управляемое состояние формы</strong> в отдельном компоненте — рендерится только форма, а не страница.</li>
<li><strong>Библиотека</strong> (React Hook Form) — она подписывает на изменения только те поля, которым это нужно, и держит остальное в ref. Валидация схемой (zod/yup) в одном месте.</li>
</ol>

<h4>Почему так лучше</h4>
<p>Радиус перерисовки сжимается с «вся страница» до «одно поле». Контракт между страницей и формой становится узким: одно событие вместо сорока пропсов. Валидация и форматирование живут внутри формы и переиспользуются.</p>

<h4>Когда не надо</h4>
<p>Если значения нужны снаружи <strong>во время</strong> ввода — живой предпросмотр, зависимые фильтры, автосохранение — поднимать придётся, но тогда поднимайте не всё состояние, а только нужные поля, и через <code>useDeferredValue</code> или debounce. Неуправляемые поля плохо дружат с масками, зависимыми полями и программным сбросом. И не тащите RHF в форму из двух полей — там это чистый оверхед.</p>

<h4>Что спросят следом</h4>
<p><strong>Как валидировать?</strong> Схемой на уровне формы, а не проверками в каждом <code>onChange</code>: одна схема даёт и типы, и сообщения, и серверную проверку тем же кодом. <strong>Как показать ошибки сервера?</strong> Мапить их на имена полей в ответе (<code>{ field: 'email', message: ... }</code>) и заливать в состояние формы — иначе пользователь видит общий тост и не знает, где именно ошибка.</p>`,
    code: `// было: страница хранит каждое поле
function Page() {
  const [name, setName] = useState('');
  const [email, setEmail] = useState('');
  /* ...ещё 18 полей... */
  return (
    <>
      <Dashboard />                              {/* рендерится на каждый символ */}
      <Form name={name} onName={setName} email={email} onEmail={setEmail} /* ...40 пропсов */ />
    </>
  );
}

// стало: форма владеет собой, наружу — только результат
function Page() {
  return (
    <>
      <Dashboard />
      <ProfileForm onSubmit={values => save(values)} />
    </>
  );
}

// вариант «ноль рендеров на ввод»
function ProfileForm({ onSubmit }) {
  return (
    <form onSubmit={e => {
      e.preventDefault();
      onSubmit(Object.fromEntries(new FormData(e.currentTarget)));
    }}>
      <input name="name" defaultValue="" />
      <input name="email" type="email" defaultValue="" />
      <button>Сохранить</button>
    </form>
  );
}`,
    tip: 'Сильный ход — назвать контракт: «страница знает только onSubmit, всё остальное внутреннее дело формы». И упомянуть маппинг серверных ошибок на поля: без него UX формы всегда плохой.' },

  { id: 'pt9',
    q: 'В команде спор: класть новое состояние в глобальный стор или держать локально. Дай критерий, а не «зависит от ситуации».',
    a: `<h4>В чём боль</h4>
<p>Спор без критерия заканчивается одной из двух крайностей. Первая: всё в глобальный стор «на будущее» — стор превращается в свалку, любое изменение будит половину приложения, размонтирование компонента не чистит его данные, тесты требуют поднимать весь стор. Вторая: всё локально — и через полгода одно и то же значение живёт в трёх компонентах и разъезжается.</p>

<h4>Решение</h4>
<p>Сначала разделить состояние по <strong>природе</strong>, а не по «важности»:</p>
<ul>
<li><strong>Серверное</strong> (данные из API) — в кеш запросов, не в стор.</li>
<li><strong>URL-состояние</strong> (фильтры, вкладка, id открытой сущности) — в адрес.</li>
<li><strong>Форменное</strong> — внутри формы.</li>
<li><strong>Сессионное/машинное</strong> (токен, тема, фича-флаги, оффлайн-очередь) — глобально.</li>
<li><strong>UI-состояние</strong> — локально, по умолчанию.</li>
</ul>
<p>Для остатка работает алгоритм: держи состояние в ближайшем общем предке всех потребителей; если предок оказался корнем и значение переживает размонтирование ветки — это глобальное. Начинать всегда с локального: поднять дешевле, чем опустить.</p>

<h4>Почему так лучше</h4>
<p>Критерий проверяемый, а не вкусовой: «кто читает» и «переживает ли размонтирование» — два вопроса с однозначными ответами. Локальное состояние умирает вместе с компонентом, поэтому не течёт и не требует ручной очистки. Глобальное остаётся маленьким, и его целиком видно на одном экране.</p>

<h4>Когда не надо</h4>
<p>Не подгоняйте под правило кроссрезные вещи: тосты, модалки и командную палитру логично держать глобально, даже если формально у них один потребитель, — иначе их нельзя открыть откуда угодно. И наоборот, «нужно двум компонентам» — ещё не повод для стора: часто достаточно поднять на один уровень или передать через children.</p>

<h4>Что спросят следом</h4>
<p><strong>Как понять, что стор разросся?</strong> Если в нём есть поля вида <code>usersLoading</code> — там серверные данные, их пора вынести. <strong>Чем Zustand лучше контекста?</strong> Подпиской по селектору: контекст будит всех потребителей на любое изменение value, стор — только тех, у кого изменился выбранный кусок.</p>`,
    code: `// было: всё подряд в глобальном сторе
const useApp = create(set => ({
  users: [], usersLoading: false,     // серверное -> в кеш запросов
  tableFilter: 'all',                 // адресуемое -> в URL
  profileDraft: {},                   // форменное -> внутрь формы
  isSidebarOpen: false,               // сессионное -> ок, оставляем
  hoveredRowId: null,                 // локальное и высокочастотное -> в компонент
}));

// стало: по природе состояния
const { data: users } = useQuery({ queryKey: ['users'], queryFn: api.getUsers });
const [params, setParams] = useSearchParams();       // tableFilter
<ProfileForm onSubmit={save} />                      // profileDraft
const useSession = create(set => ({ isSidebarOpen: false, toggle: () => set(s => ({ isSidebarOpen: !s.isSidebarOpen })) }));
const [hoveredRowId, setHovered] = useState(null);   // внутри Table

// проверка «переживает ли размонтирование»
// закрыли раздел -> hoveredRowId должен исчезнуть -> локальное
// закрыли раздел -> тема должна остаться -> глобальное`,
    tip: 'Два вопроса, которые закрывают спор: «кто это читает?» и «должно ли значение пережить размонтирование ветки?». И сразу отделите серверные данные — половина «глобального состояния» в реальных проектах это просто кеш API.' },

  { id: 'pt10',
    q: 'При переходе на другого пользователя в профиле остаются данные предыдущего: несброшенные поля формы, старая вкладка. Внутри уже три useEffect со сбросами. Как сделать нормально?',
    a: `<h4>В чём боль</h4>
<p>Компонент не размонтируется при смене сущности — меняется только проп <code>userId</code>. React переиспользует тот же фибер, а значит и всё состояние: значения полей, активную вкладку, позицию аккордеона. Разработчик добавляет <code>useEffect(() =&gt; reset(), [userId])</code> — и это работает через раз: сброс происходит <strong>после</strong> коммита, поэтому один кадр показывает старые данные под новым заголовком. Дальше добавляется второй эффект для второго куска состояния, третий — для третьего, и каждый новый стейт нужно не забыть добавить в список сбрасываемых.</p>

<h4>Решение</h4>
<p>Сбрасывать состояние через <code>key</code>. Дать компоненту <code>key={userId}</code>: при смене ключа React считает это <strong>другим</strong> компонентом — размонтирует старый (со всеми эффектами очистки) и монтирует новый с чистым состоянием. Один атрибут заменяет все эффекты-сбросы и работает для любого состояния, включая то, что добавят завтра.</p>
<p>Вариант поменьше: не весь компонент, а только его «грязная» часть — <code>&lt;ProfileForm key={userId} ... /&gt;</code>, если вкладку как раз хочется сохранить.</p>

<h4>Почему так лучше</h4>
<p>Нет промежуточного кадра со старыми данными: новое поддерево рендерится сразу с начальным состоянием. Нет списка «что не забыть сбросить» — он не нужен по построению. Уходят эффекты, а с ними и их зависимости.</p>

<h4>Когда не надо</h4>
<p>Смена <code>key</code> — это полное размонтирование: теряются позиция скролла, фокус, запущенные анимации, а дети переинициализируются с нуля (тяжёлая карта или редактор пересоздадутся — это может быть дороже, чем ручной сброс). Если состояние надо частично сохранить между сущностями, <code>key</code> не подходит. Не ставьте <code>key</code> на компонент, который лежит выше зоны, где реально нужен сброс: получите лишнюю работу на каждом переходе.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему нельзя index как key в списках?</strong> Потому что ключ — это идентичность: при вставке в начало индексы сдвигаются, React сопоставляет чужие элементы, и состояние строк «переезжает» к соседям. <strong>А если нужен сброс только одного поля?</strong> Тогда честный обработчик или производное значение — <code>key</code> не инструмент точечной правки.</p>`,
    code: `// было: сбросы эффектами, и всё равно один кадр со старыми данными
function Profile({ userId }) {
  const [tab, setTab] = useState('info');
  const [draft, setDraft] = useState('');

  useEffect(() => { setTab('info'); }, [userId]);   // сброс после коммита
  useEffect(() => { setDraft(''); }, [userId]);     // и ещё один
  // добавили новое состояние -> нужен третий эффект, о котором забудут
}

// стало: смена key = новый компонент с чистым состоянием
function ProfilePage({ userId }) {
  return <Profile key={userId} userId={userId} />;
}

function Profile({ userId }) {
  const [tab, setTab] = useState('info');   // при смене userId уже 'info'
  const [draft, setDraft] = useState('');
  // эффектов-сбросов нет вообще
}

// точечный вариант: вкладку сохраняем, форму сбрасываем
<>
  <Tabs value={tab} onChange={setTab} />
  <ProfileForm key={userId} userId={userId} />
</>`,
    tip: 'Скажите, что key — это идентичность компонента, а не «подсказка для оптимизации». Тогда и правило про index в списках, и сброс состояния объясняются одним и тем же механизмом согласования.' },

  { id: 'pt11',
    q: 'У компонента Modal семнадцать пропсов, половина нужна в одном случае из десяти: showCloseButton, footerAlign, hideHeader, customIcon... Как это разгрести?',
    a: `<h4>В чём боль</h4>
<p>Это «boolean trap» и разрастание конфигурации. Каждый новый кейс добавляет проп, каждый проп добавляет ветку в JSX, ветки перемножаются: <code>hideHeader &amp;&amp; customIcon</code> — уже комбинация, которую никто не проверял. Внутри компонента появляется лес тернарников, снаружи — вызовы вида <code>&lt;Modal hideHeader footerAlign="right" showCloseButton={false} /&gt;</code>, где по месту вызова непонятно, что получится. Изменение под один экран ломает девять других, поэтому пропсы только добавляют и никогда не удаляют.</p>

<h4>Решение</h4>
<p>Сменить <strong>конфигурацию на композицию</strong>. Компонент перестаёт решать, что рисовать, и начинает предоставлять места: <code>Modal.Header</code>, <code>Modal.Body</code>, <code>Modal.Footer</code> — или именованные слоты пропсами (<code>header</code>, <code>footer</code>) там, где важен порядок и обязательность. Не нужен заголовок — просто не рендерите <code>Modal.Header</code>; не нужна кнопка закрытия — не кладите её. Вариативность уходит на сторону вызова, где она и так известна.</p>
<p>Остаются только пропсы поведения: <code>open</code>, <code>onClose</code>, <code>size</code>, <code>initialFocus</code> — то, что относится к модалке как к механизму, а не к её содержимому.</p>

<h4>Почему так лучше</h4>
<p>Число комбинаций внутри компонента падает до одной: он всегда рендерит то, что дали. Читаемость на месте вызова становится структурной — видно дерево, а не набор флагов. Новый кейс перестаёт требовать правки библиотечного компонента, а значит, перестаёт ломать чужие экраны.</p>

<h4>Когда не надо</h4>
<p>Композиция дороже в двух ситуациях. Первая: контракт должен быть жёстким — например, дизайн-система обязана гарантировать, что у диалога всегда есть заголовок и не больше двух кнопок; там пропсы лучше, потому что их можно валидировать типами. Вторая: компонент используется в одном-двух местах — тогда слоты просто удлиняют код. И помните, что составные компоненты хуже переносятся через сериализацию (нельзя описать модалку JSON-конфигом).</p>

<h4>Что спросят следом</h4>
<p><strong>Слоты пропсами или children?</strong> Пропсы-слоты дают типизацию и обязательность, children — гибкость и произвольный порядок. <strong>Как сохранить единый вид?</strong> Дефолтные подкомпоненты (<code>Modal.Header</code> уже со стилями и кнопкой закрытия) — свобода в составе, а не в оформлении.</p>`,
    code: `// было: конфигурация флагами
<Modal
  open={open} onClose={close}
  title="Удалить проект?" showCloseButton={false}
  hideHeader={false} customIcon={<Warn />}
  footerAlign="right" primaryText="Удалить" primaryDanger
  secondaryText="Отмена" onSecondary={close}
/>
// внутри: 17 пропсов -> десятки непроверенных комбинаций

// стало: композиция, компонент даёт места
<Modal open={open} onClose={close} size="sm">
  <Modal.Header icon={<Warn />}>Удалить проект?</Modal.Header>
  <Modal.Body>Действие необратимо.</Modal.Body>
  <Modal.Footer align="right">
    <Button onClick={close}>Отмена</Button>
    <Button variant="danger" onClick={remove}>Удалить</Button>
  </Modal.Footer>
</Modal>

// внутри остаётся только механика
function Modal({ open, onClose, size = 'md', children }) {
  useLockScroll(open);
  useFocusTrap(open);
  if (!open) return null;
  return <div role="dialog" aria-modal="true" data-size={size}>{children}</div>;
}`,
    tip: 'Разделите пропсы на «про поведение» и «про содержимое»: первые оставляем, вторые превращаем в слоты. Это даёт готовый критерий, что именно выносить, а не общее «используйте композицию».' },

  { id: 'pt12',
    q: 'Компонент Tabs перебирает children через React.Children.map и клонирует их с cloneElement, добавляя пропсы. При обёртке вкладки в div всё ломается. Как переписать?',
    a: `<h4>В чём боль</h4>
<p>Родитель знает слишком много о своих детях. <code>React.Children.map</code> плюс <code>cloneElement</code> предполагают, что дети — плоский список элементов ожидаемого типа. Любое отклонение ломает контракт: обернули вкладку в <code>div</code> или <code>Tooltip</code> — родитель клонирует обёртку и вкладка не получает пропсы; поставили условие <code>{isAdmin &amp;&amp; &lt;Tab /&gt;}</code> — в массиве появляется <code>false</code>; вынесли группу вкладок в отдельный компонент — всё рассыпалось. Дополнительно <code>cloneElement</code> молча перетирает пропсы, которые пользователь передал явно, и в TypeScript типизируется плохо.</p>

<h4>Решение</h4>
<p>Передавать данные <strong>вниз по дереву через контекст</strong>, а не в обход него через клонирование. Родитель создаёт контекст с состоянием и обработчиками; каждый ребёнок сам подписывается на него хуком <code>useTabs()</code>. Глубина вложенности и обёртки перестают иметь значение — контекст проходит через любые уровни.</p>
<p>Хук доступа обязан бросать понятную ошибку вне провайдера: это возвращает то самое ограничение, которое давал <code>cloneElement</code>, но в виде явного сообщения, а не молчаливой поломки.</p>

<h4>Почему так лучше</h4>
<p>Пользователь компонента получает свободу вёрстки: оборачивай, условно рендери, выноси в подкомпоненты. Автор компонента избавляется от рефлексии над деревом — типы становятся честными, потому что описывают контекст, а не гадают о children. Исчезает целый класс багов «работает, пока не тронешь разметку».</p>

<h4>Когда не надо</h4>
<p>Если структура действительно фиксирована и это часть контракта (например, <code>RadioGroup</code>, где дети обязаны быть радио-кнопками), перебор children даёт полезную валидацию. Контекст дороже: он будит всех потребителей на изменение value, поэтому для сотен элементов дробите контекст на «редко меняющийся» (обработчики) и «часто» (активный индекс), либо используйте внешний стор с селекторами.</p>

<h4>Что спросят следом</h4>
<p><strong>Зачем тогда вообще cloneElement?</strong> Точечно: подмешать <code>ref</code> или <code>aria</code> одному переданному элементу (паттерн <code>asChild</code>). Массово по списку children — уже запах. <strong>Как избежать перерисовки всех вкладок?</strong> Два контекста: стабильные экшены и меняющееся значение; вкладка сравнивает свой id с активным и мемоизируется.</p>`,
    code: `// было: родитель клонирует детей и требует плоскую структуру
function Tabs({ children, value, onChange }) {
  return React.Children.map(children, (child, i) =>
    React.cloneElement(child, { active: i === value, onSelect: () => onChange(i) })
  );
}
<Tabs value={v} onChange={setV}>
  <Tab>Один</Tab>
  <div><Tab>Два</Tab></div>   {/* обёртка ломает всё: пропсы уйдут в div */}
</Tabs>

// стало: контекст, дети подписываются сами
const TabsCtx = createContext(null);
const useTabs = () => {
  const ctx = useContext(TabsCtx);
  if (!ctx) throw new Error('<Tab> должен быть внутри <Tabs>');
  return ctx;
};

function Tabs({ value, onChange, children }) {
  const ctx = useMemo(() => ({ value, onChange }), [value, onChange]);
  return <TabsCtx.Provider value={ctx}>{children}</TabsCtx.Provider>;
}

function Tab({ id, children }) {
  const { value, onChange } = useTabs();
  return (
    <button role="tab" aria-selected={value === id} onClick={() => onChange(id)}>
      {children}
    </button>
  );
}
// теперь любая вложенность работает
<Tabs value={v} onChange={setV}>
  <Tab id="a">Один</Tab>
  <div><Tooltip><Tab id="b">Два</Tab></Tooltip></div>
</Tabs>`,
    tip: 'Ключевая мысль: cloneElement связывает родителя с формой разметки, а контекст — только с фактом вложенности. Второе переживает рефакторинг вёрстки, первое — нет.' },

  { id: 'pt13',
    q: 'Дизайнеры просят пятый вариант выпадающего списка: то с поиском, то с группами, то с мультивыбором. Плодить Select2, Select3 не хочется. Что предложишь?',
    a: `<h4>В чём боль</h4>
<p>Компонент пытается быть универсальным через параметры. Каждый новый вариант — это либо новый проп (<code>searchable</code>, <code>grouped</code>, <code>multiple</code>), либо копипаста в <code>SelectWithSearch</code>. В первом случае внутри растёт комбинаторика и падает читаемость, во втором — расползается логика: доступность, клавиатурная навигация и позиционирование дублируются в четырёх файлах и чинятся по отдельности.</p>

<h4>Решение</h4>
<p><strong>Составные компоненты</strong> (compound components). Один «корень» владеет состоянием и логикой (открыт/закрыт, активный индекс, выбранные значения, роли ARIA) и раздаёт их через контекст. Части — <code>Select.Trigger</code>, <code>Select.Search</code>, <code>Select.List</code>, <code>Select.Group</code>, <code>Select.Option</code> — отвечают только за разметку и подписываются на контекст сами.</p>
<p>Вариант «с поиском» перестаёт быть режимом: это просто наличие <code>Select.Search</code> в составе. Мультивыбор остаётся пропсом корня, потому что меняет семантику состояния, а не разметку — это хороший водораздел: <strong>что рисовать — состав, как себя вести — пропсы корня</strong>.</p>

<h4>Почему так лучше</h4>
<p>Логика существует в единственном экземпляре, поэтому исправление навигации стрелками чинит все пять вариантов сразу. Продуктовая команда собирает нужный вид без правок в дизайн-системе. Именно так устроены Radix UI, Headless UI и Ark: снаружи — состав, внутри — один автомат.</p>

<h4>Когда не надо</h4>
<p>Составные компоненты дают свободу собрать <strong>неправильно</strong>: можно забыть <code>Select.List</code> или поставить два <code>Trigger</code>. Если нужен жёсткий контракт (в дизайн-системе с сотней потребителей), лучше закрытый компонент с проверяемыми пропсами. Ещё минус — многословность вызова: для простого «выбери одно из трёх» три строки вместо одной. Компромисс: составной низкоуровневый компонент плюс тонкая обёртка-пресет для типового случая.</p>

<h4>Что спросят следом</h4>
<p><strong>Как связать части, если пользователь их обернул?</strong> Через контекст, а не <code>cloneElement</code> — тогда любая вложенность работает. <strong>Как не сломать доступность?</strong> Все <code>id</code>, <code>role</code>, <code>aria-activedescendant</code> раздаёт корень через контекст; части их только применяют, поэтому забыть атрибут нельзя.</p>`,
    code: `// было: режимы флагами + копипаста
<Select options={opts} searchable multiple grouped placeholder="..." />
// и рядом SelectWithSearch.tsx, где та же навигация стрелками, но починена не везде

// стало: один автомат внутри, состав снаружи
function Select({ value, onChange, multiple = false, children }) {
  const [open, setOpen] = useState(false);
  const [active, setActive] = useState(0);
  const api = useMemo(
    () => ({ value, onChange, multiple, open, setOpen, active, setActive }),
    [value, onChange, multiple, open, active]
  );
  return <SelectCtx.Provider value={api}>{children}</SelectCtx.Provider>;
}
Select.Trigger = Trigger;
Select.Search  = Search;
Select.List    = List;
Select.Group   = Group;
Select.Option  = Option;

// простой случай
<Select value={v} onChange={setV}>
  <Select.Trigger />
  <Select.List>{opts.map(o => <Select.Option key={o.id} value={o.id}>{o.label}</Select.Option>)}</Select.List>
</Select>

// «с поиском и группами» — тот же компонент, другой состав
<Select value={v} onChange={setV} multiple>
  <Select.Trigger />
  <Select.List>
    <Select.Search placeholder="Поиск" />
    <Select.Group label="Активные">...</Select.Group>
    <Select.Group label="Архив">...</Select.Group>
  </Select.List>
</Select>`,
    tip: 'Водораздел, который стоит проговорить: состав отвечает за «что нарисовано», пропсы корня — за «как оно себя ведёт». Мультивыбор остаётся пропсом именно потому, что меняет семантику состояния.' },

  { id: 'pt14',
    q: 'Компонент отдаёт данные через render-prop: <Fetcher>{({data}) => ...}</Fetcher>. Коллега говорит, что это устарело и надо хук. Всегда ли он прав?',
    a: `<h4>В чём боль</h4>
<p>Render-prop до хуков был единственным способом переиспользовать логику с состоянием, поэтому им покрывали всё подряд. Отсюда «callback hell» в JSX: три вложенных <code>&lt;Fetcher&gt;&lt;Mouse&gt;&lt;Media&gt;</code> дают лесенку с отступом в двадцать пробелов, где значения одного уровня не видны на другом без проброса. Плюс каждый уровень — реальный компонент в дереве: лишние фиберы, лишние рендеры, шум в React DevTools. Для чистой логики хук объективно лучше.</p>

<h4>Решение</h4>
<p>Разделить два случая. <strong>Логика без разметки</strong> — кастомный хук: композиция плоская, значения доступны в теле компонента, типы выводятся. <strong>Логика, неотделимая от рендера или от места в дереве</strong> — render-prop (или его современная форма: <code>children</code> как функция, компонент с пропом <code>renderItem</code>).</p>
<p>Render-prop остаётся правильным выбором, когда: 1) вызывающий должен решать, <strong>что именно</strong> рисовать для каждого элемента внутренней структуры (виртуальный список отдаёт индекс и стиль строки, таблица — ячейку); 2) значение зависит от измерений собственного DOM-узла компонента (<code>ResizeObserver</code>, автосайзер); 3) нужна ленивость — функция вызывается только для видимых элементов; 4) библиотека должна работать и в классах.</p>

<h4>Почему так лучше</h4>
<p>Хук убирает вложенность и делает переиспользование дешёвым. Render-prop сохраняет <strong>инверсию управления в точке рендера</strong>: компонент управляет когда и сколько раз вызвать функцию, а вызывающий — что вернуть. Хук так не умеет: он выполняется ровно один раз за рендер.</p>

<h4>Когда не надо</h4>
<p>Не превращайте render-prop в способ передать одиночный кусок разметки — для этого есть <code>children</code> или проп-слот. Не используйте render-prop ради «гибкости на будущее»: каждая функция в JSX — новая ссылка, и мемоизация внутри компонента перестаёт работать. Если функция дорогая и вызывается на каждый элемент, оборачивайте её в <code>useCallback</code> и стабилизируйте зависимости.</p>

<h4>Что спросят следом</h4>
<p><strong>А HOC?</strong> Практически вытеснен: непрозрачные пропсы, коллизии имён, ад типизации. Живёт там, где нужно обернуть компонент снаружи (<code>withErrorBoundary</code>). <strong>Можно ли совместить?</strong> Да, стандартный приём библиотек: хук с логикой плюс тонкий компонент с render-prop поверх него.</p>`,
    code: `// было: три уровня render-prop ради чистой логики
<Fetcher url="/api/user">
  {({ data: user }) => (
    <MousePosition>
      {({ x, y }) => (
        <MediaQuery query="(min-width: 900px)">
          {isWide => <Card user={user} x={x} y={y} wide={isWide} />}
        </MediaQuery>
      )}
    </MousePosition>
  )}
</Fetcher>

// стало: логика без разметки -> хуки, композиция плоская
function Screen() {
  const { data: user } = useFetch('/api/user');
  const { x, y } = useMousePosition();
  const isWide = useMediaQuery('(min-width: 900px)');
  return <Card user={user} x={x} y={y} wide={isWide} />;
}

// а вот здесь render-prop остаётся правильным:
// компонент решает, какие строки существуют, вызывающий — как они выглядят
<VirtualList itemCount={100000} itemHeight={40}>
  {({ index, style }) => <Row style={style} item={items[index]} />}
</VirtualList>
// хук так не умеет: он выполняется один раз за рендер,
// а эта функция вызывается по разу на каждую видимую строку`,
    tip: 'Формулировка, которая отличает сильный ответ: хук переиспользует логику, render-prop — инверсию управления в точке рендера. Виртуальный список хуком не выразить.' },

  { id: 'pt15',
    q: 'В четырёх компонентах повторяется один и тот же код: подписка на resize, дебаунс, очистка. Копипаста расползлась. Как вынести правильно?',
    a: `<h4>В чём боль</h4>
<p>Дублирование stateful-логики. Каждая копия имеет шанс разойтись: в одной забыли снять слушатель (утечка и <code>setState</code> после размонтирования), в другой не поставили <code>passive: true</code>, в третьей дебаунс на 100 мс, а в четвёртой на 300. Баг чинится в одном файле из четырёх, и через месяц возвращается с другого экрана. Плюс компонент раздувается: 40 строк инфраструктуры на 15 строк разметки.</p>

<h4>Решение</h4>
<p><strong>Кастомный хук</strong> — функция с префиксом <code>use</code>, внутри которой живут другие хуки. Он инкапсулирует состояние, подписку и очистку, а наружу отдаёт минимальный результат. Хороший хук проектируется как маленький модуль: явные входные параметры, стабильные ссылки на возвращаемые функции (<code>useCallback</code>/<code>useEffectEvent</code>), очистка в <code>return</code> из эффекта, и никакой разметки внутри.</p>
<p>Важная деталь, которую часто пропускают: хук переиспользует <strong>логику, а не состояние</strong>. Каждый вызывающий компонент получает собственный экземпляр состояния. Если нужно общее состояние — это уже контекст или стор, а не хук.</p>

<h4>Почему так лучше</h4>
<p>Логика чинится в одном месте, компонент читается как описание вида. Хук тестируется отдельно (<code>renderHook</code>), и его поведение при размонтировании проверяется явно. Именование становится документацией: <code>useWindowWidth()</code> в теле компонента говорит больше, чем 40 строк подписок.</p>

<h4>Когда не надо</h4>
<p>Не выносите ради одного использования — «а вдруг пригодится» рождает абстракции, которые потом мешают. Не делайте «хук на все случаи» с десятью опциями: это тот же компонент с семнадцатью пропсами, только хуже, потому что теперь ещё и с ветвлением на условных вызовах хуков (что запрещено). Не прячьте в хук вызовы, у которых есть побочные эффекты, неочевидные по имени: <code>useUser()</code>, который тихо делает POST, — ловушка для следующего разработчика.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему хуки нельзя вызывать условно?</strong> Состояние хранится массивом на фибере и сопоставляется по порядку вызова: пропуск вызова сдвигает всю нумерацию. <strong>Как хуку не пересоздавать колбэки?</strong> <code>useCallback</code> со стабильными зависимостями, а меняющиеся значения читать через ref или <code>useEffectEvent</code>.</p>`,
    code: `// было: одно и то же в четырёх файлах, местами с ошибками
function Sidebar() {
  const [width, setWidth] = useState(window.innerWidth);
  useEffect(() => {
    let t;
    const onResize = () => { clearTimeout(t); t = setTimeout(() => setWidth(window.innerWidth), 200); };
    window.addEventListener('resize', onResize);
    // забыли clearTimeout при размонтировании -> setState после unmount
    return () => window.removeEventListener('resize', onResize);
  }, []);
}

// стало: один хук, одна реализация, честная очистка
function useWindowWidth(delay = 200) {
  const [width, setWidth] = useState(() => window.innerWidth);
  useEffect(() => {
    let timer;
    const onResize = () => {
      clearTimeout(timer);
      timer = setTimeout(() => setWidth(window.innerWidth), delay);
    };
    window.addEventListener('resize', onResize, { passive: true });
    return () => {
      clearTimeout(timer);                       // важное: гасим отложенный вызов
      window.removeEventListener('resize', onResize);
    };
  }, [delay]);
  return width;
}

function Sidebar() {
  const width = useWindowWidth();
  return <aside data-narrow={width < 900}>...</aside>;
}`,
    tip: 'Скажите вслух: «хук переиспользует логику, но не состояние — у каждого вызывающего оно своё». Это ровно то место, где путаются, ожидая от хука общего значения на всё приложение.' },

  { id: 'pt16',
    q: 'Компонент Table пытается угадать все сценарии: сам решает, что делать по клику на строку, сам форматирует ячейки, сам грузит данные. Каждый новый экран требует правки в Table. Что не так?',
    a: `<h4>В чём боль</h4>
<p>Компонент присвоил себе принятие решений. Он знает, откуда берутся данные, как форматируется дата, что происходит по клику, какие колонки бывают. Любое отличие нового экрана превращается в проп-исключение (<code>variant="orders"</code>) или в <code>if</code> внутри. Так возникает компонент, который нельзя изменить безопасно: он используется в двадцати местах, и каждое из них зависит от его внутренних решений.</p>

<h4>Решение</h4>
<p><strong>Инверсия управления</strong>: компонент делает то, что умеет только он (виртуализация, выделение, клавиатура, сортировка), а решения отдаёт наружу. Практически это набор точек расширения: данные приходят пропсом, а не загружаются внутри; колонка описывает свой рендер функцией <code>cell</code>; обработчик события отдаётся вызывающим; вместо флагов поведения — колбэки и слоты.</p>
<p>Крайняя и очень полезная форма — <strong>headless</strong>: хук отдаёт состояние и набор «пропс-геттеров» (<code>getRowProps</code>, <code>getSortProps</code>), а разметку целиком пишет вызывающий. Так устроены TanStack Table, Downshift, React Aria.</p>

<h4>Почему так лучше</h4>
<p>Компонент перестаёт расти: новые сценарии добавляются снаружи и не могут сломать существующие. Тестировать проще — механика отдельно от политики. Появляется возможность переиспользовать логику в другом дизайне, не переписывая её.</p>

<h4>Когда не надо</h4>
<p>Инверсия перекладывает работу на вызывающего. Если у вас двадцать однотипных списков, полный headless заставит двадцать раз писать одинаковую разметку — и они разъедутся по стилям. Правильный компромисс: headless-ядро плюс «толстый» пресет для типового случая. Ещё цена — обучение: пропс-геттеры и колбэки требуют читать документацию, тогда как <code>&lt;Table data={x} /&gt;</code> понятен сразу.</p>

<h4>Что спросят следом</h4>
<p><strong>Как понять, что пора инвертировать?</strong> Если в компоненте появился проп с названием экрана или домена (<code>variant="checkout"</code>) — решение принято не в том месте. <strong>Не потеряется ли единообразие?</strong> Нет, если оформление задаётся пресетами и токенами, а свобода даётся только на состав и поведение.</p>`,
    code: `// было: компонент принимает решения за всех
function Table({ variant }) {
  const { data } = useQuery(['rows', variant], () => api.load(variant)); // знает про загрузку
  const onRowClick = row =>
    variant === 'orders' ? goToOrder(row.id) : openUserModal(row.id);    // знает про домен
  return rows.map(r => <tr onClick={() => onRowClick(r)}>{formatByVariant(r, variant)}</tr>);
}

// стало: механика внутри, решения снаружи
function Table({ rows, columns, onRowClick, rowKey }) {
  return (
    <tbody>
      {rows.map(row => (
        <tr key={rowKey(row)} onClick={() => onRowClick?.(row)}>
          {columns.map(col => <td key={col.id}>{col.cell(row)}</td>)}
        </tr>
      ))}
    </tbody>
  );
}

<Table
  rows={orders}
  rowKey={o => o.id}
  onRowClick={o => navigate('/orders/' + o.id)}
  columns={[
    { id: 'num',  cell: o => o.number },
    { id: 'date', cell: o => formatDate(o.createdAt) },
    { id: 'sum',  cell: o => <Money value={o.total} /> },
  ]}
/>`,
    tip: 'Диагностический признак, который стоит назвать: если внутри переиспользуемого компонента встречается имя конкретного экрана или домена — управление находится не там, где нужно.' },

  { id: 'pt17',
    q: 'На код-ревью спор: делить ли компонент на контейнер и представление. Одни говорят «паттерн умер с хуками», другие — «без него каша». Кто прав?',
    a: `<h4>В чём боль</h4>
<p>Исходная боль реальна: компонент одновременно ходит в сеть, знает про стор, форматирует данные и рисует разметку. Такой компонент нельзя показать в Storybook без мока сети, нельзя переиспользовать на другом источнике данных и трудно тестировать — любой тест начинается с поднятия провайдеров.</p>
<p>Историческое лекарство — контейнер (умный компонент с <code>connect</code>) и презентационный компонент (чистая функция от пропсов). С приходом хуков буквальная форма устарела: хук вынимает логику без дополнительного компонента в дереве, и <code>connect</code>-обёртки исчезли.</p>

<h4>Решение</h4>
<p>Разделение осталось, изменилась только техника. Сегодня это <strong>граница «данные — представление»</strong>, проведённая одним из трёх способов: 1) кастомный хук с логикой плюс компонент с разметкой в одном файле; 2) два компонента, когда презентационный переиспользуется в нескольких источниках или нужен в Storybook; 3) в RSC/Next — серверный компонент грузит, клиентский рисует, и граница становится физической.</p>
<p>Практический критерий: презентационный компонент не должен импортировать ничего из <code>api/</code> и <code>store/</code>. Это правило проверяется линтером (<code>import/no-restricted-paths</code>), а не договорённостью.</p>

<h4>Почему так лучше</h4>
<p>Разметка становится тестируемой без сети: подставил пропсы — получил вид, включая пустое состояние и ошибку. Логику можно переиспользовать между списком и модалкой. В RSC граница ещё и уменьшает бандл: код загрузки не уезжает в браузер.</p>

<h4>Когда не надо</h4>
<p>Механическое деление всего подряд на <code>XContainer</code> и <code>XView</code> — карго-культ: удваивается число файлов, а связность не уменьшается, потому что View всё равно принимает двадцать пропсов, повторяющих форму данных. Для одноразового компонента на 30 строк граница не нужна. И не путайте: вынести <code>useQuery</code> в хук — это разделение; вынести его в компонент-обёртку, который ничего больше не делает, — это лишний фибер.</p>

<h4>Что спросят следом</h4>
<p><strong>Как проверять границу?</strong> ESLint-правилом на импорты плюс тестом в Storybook: если компонент рендерится без провайдеров — граница держится. <strong>Где место форматирования?</strong> В презентационном слое или в отдельных функциях-форматтерах, но не в слое загрузки: форматирование зависит от локали и вида, а не от источника.</p>`,
    code: `// было: всё в одном — нельзя показать в Storybook, тест требует моков сети
function OrderList() {
  const { data, isLoading, error } = useQuery(['orders'], api.getOrders);
  if (isLoading) return <Spinner />;
  if (error) return <ErrorBox error={error} />;
  return <ul>{data.map(o => <li key={o.id}>{o.number} — {formatMoney(o.total)}</li>)}</ul>;
}

// стало: логика в хуке, вид — чистая функция от пропсов
function useOrders() {
  const q = useQuery({ queryKey: ['orders'], queryFn: api.getOrders });
  return { orders: q.data ?? [], isLoading: q.isPending, error: q.error };
}

export function OrderListView({ orders, isLoading, error }) {  // ни api, ни store
  if (isLoading) return <Spinner />;
  if (error) return <ErrorBox error={error} />;
  if (!orders.length) return <Empty />;
  return <ul>{orders.map(o => <li key={o.id}>{o.number} — {formatMoney(o.total)}</li>)}</ul>;
}

export function OrderList() {
  return <OrderListView {...useOrders()} />;
}
// в Storybook: <OrderListView orders={[]} isLoading={false} error={null} /> — без провайдеров`,
    tip: 'Правильная формулировка: умер не паттерн, а его реализация через HOC-контейнеры. Граница «данные — представление» жива, только проводится хуком, а в RSC — границей сервер/клиент.' },

  { id: 'pt18',
    q: 'У нас есть Button, ButtonLink и ButtonAnchor — три почти одинаковых компонента, различие только в корневом теге. Как сделать один?',
    a: `<h4>В чём боль</h4>
<p>Тройная копипаста ради разного корневого тега. Стили, состояния (<code>disabled</code>, <code>loading</code>), иконки и размеры продублированы; правка добирается не до всех. Хуже того, компоненты расходятся по семантике: <code>ButtonLink</code> может оказаться <code>div</code> с <code>onClick</code>, и тогда ссылка перестаёт открываться в новой вкладке и пропадает из клавиатурной навигации.</p>

<h4>Решение</h4>
<p><strong>Полиморфный компонент</strong>: проп <code>as</code> (или <code>component</code>) задаёт, какой элемент рендерить, а остальные пропсы прокидываются к нему. В TypeScript типизируется через дженерик по <code>ElementType</code> с выводом пропсов целевого элемента, чтобы <code>&lt;Button as="a" href&gt;</code> проверялся, а <code>&lt;Button href&gt;</code> — нет.</p>
<p>Альтернатива, которую стоит назвать, — <code>asChild</code> из Radix: компонент не создаёт свой тег, а сливает пропсы и ref в единственного ребёнка. Это удобнее, когда целевой элемент — чужой компонент (<code>Link</code> из роутера) со своими требованиями.</p>

<h4>Почему так лучше</h4>
<p>Один источник стилей и поведения, правильная семантика по месту (кнопка — <code>button</code>, навигация — <code>a</code> с <code>href</code>), корректная передача <code>ref</code>. Уходит целый класс ошибок доступности, потому что тег выбирается осознанно.</p>

<h4>Когда не надо</h4>
<p>Три минуса. Первый: типы полиморфных компонентов сложные — сообщения об ошибках длинные, IDE подтормаживает, а на дженериках легко потерять <code>ref</code>. Второй: свобода тега легко превращается в свободу семантики, и кто-нибудь передаст <code>as="div"</code> с <code>onClick</code>. Третий: если вариантов ровно два, честнее два компонента (<code>Button</code> и <code>LinkButton</code>) поверх общего стилевого примитива — код читается лучше. Полиморфизм оправдан в дизайн-системе, а не в продуктовом коде на пять кнопок.</p>

<h4>Что спросят следом</h4>
<p><strong>Как передать ref?</strong> В React 19 <code>ref</code> — обычный проп, до этого <code>forwardRef</code> плюс дженерик; проверять надо явно, тесты на ref обязательны. <strong>Чем asChild лучше as?</strong> Не нужно знать пропсы целевого компонента: они приходят от ребёнка, а компонент только домешивает свои.</p>`,
    code: `// было: три компонента ради корневого тега
function Button({ children, ...p })       { return <button className={cls} {...p}>{children}</button>; }
function ButtonLink({ to, children })     { return <Link to={to} className={cls}>{children}</Link>; }
function ButtonAnchor({ href, children }) { return <a href={href} className={cls}>{children}</a>; }

// стало: один полиморфный компонент
type Props<T extends React.ElementType> = {
  as?: T;
  variant?: 'primary' | 'ghost';
} & Omit<React.ComponentPropsWithRef<T>, 'as' | 'variant'>;

function Button<T extends React.ElementType = 'button'>({
  as, variant = 'primary', ...rest
}: Props<T>) {
  const Tag = as || 'button';
  return <Tag className={styles[variant]} {...rest} />;
}

<Button onClick={save}>Сохранить</Button>
<Button as="a" href="/docs" target="_blank">Документация</Button>
<Button as={Link} to="/orders">Заказы</Button>
// <Button href="/docs" /> — ошибка типов: у button нет href

// альтернатива без дженериков: asChild сливает пропсы в ребёнка
<Button asChild><Link to="/orders">Заказы</Link></Button>`,
    tip: 'Добавьте про доступность: главный выигрыш не в экономии файлов, а в том, что тег выбирается по смыслу — навигация остаётся ссылкой с href, а не div с onClick.' },

  { id: 'pt19',
    q: 'В карточке товара понадобилось вставлять произвольный бейдж в угол, а иногда — свой футер. Добавлять пропсы badgeText, badgeColor, footerButtons не хочется. Как дать точки вставки?',
    a: `<h4>В чём боль</h4>
<p>Компонент закрыт для расширения: любое отклонение требует нового пропа, а каждый новый проп размывает его назначение. Через десять итераций <code>Card</code> знает про бейджи, кнопки, сноски и промо-плашки, а внутри — цепочка условий. Отдельная проблема — контроль порядка: если вставок несколько, приходится изобретать <code>badgePosition</code> и <code>footerAlign</code>.</p>

<h4>Решение</h4>
<p><strong>Слоты</strong>: компонент объявляет именованные места и рендерит то, что в них положили. Две формы. Первая — <strong>пропсы-слоты</strong>: <code>&lt;Card badge={...} footer={...} media={...} /&gt;</code>. Типизируются как <code>ReactNode</code>, обязательность и допустимость видны в сигнатуре, порядок определяет сам компонент — то, что нужно дизайн-системе. Вторая — <strong>children-слоты</strong>: <code>&lt;Card&gt;&lt;Card.Badge/&gt;&lt;Card.Footer/&gt;&lt;/Card&gt;</code>, где вызывающий управляет и составом, и порядком.</p>
<p>Практическое правило: если мест мало и они фиксированы — пропсы-слоты; если состав произвольный и вложенный — подкомпоненты. Смешивать можно: обязательные слоты пропсами, свободное содержимое через <code>children</code>.</p>

<h4>Почему так лучше</h4>
<p>Компонент перестаёт знать о содержимом: он размечает сетку и стилизует контейнеры. Число условных веток внутри падает до «есть слот или нет». Продуктовый код становится самодокументируемым: видно, что и куда вставлено, без чтения реализации <code>Card</code>.</p>

<h4>Когда не надо</h4>
<p>Слот — это дыра в инкапсуляции: через него легко вставить то, что сломает вёрстку или доступность (например, интерактивный элемент внутрь кликабельной карточки — вложенные интерактивные элементы недопустимы). Если контракт должен быть жёстким, лучше конечный набор вариантов (<code>badge={{ kind: 'sale' }}</code>), чем произвольный узел. Ещё нюанс: узел, переданный пропсом, создаётся при рендере родителя всегда — даже если слот не отрисуется; для тяжёлого содержимого передавайте функцию, а не готовый элемент.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему проп-слот лучше children для обязательных мест?</strong> Потому что <code>children</code> нельзя типизировать «ровно один header и ровно один footer», а проп — можно. <strong>Как сделать слот ленивым?</strong> Передавать <code>() =&gt; ReactNode</code> и вызывать внутри, когда слот действительно рендерится.</p>`,
    code: `// было: расширение через новые пропсы
<Card
  title={p.name} price={p.price}
  badgeText="Скидка" badgeColor="red" badgePosition="top-right"
  footerButtons={[{ text: 'В корзину', onClick: add }]}
  showFootnote footnoteText="Осталось 3 шт."
/>

// стало: именованные слоты
function Card({ media, badge, footer, children }) {
  return (
    <article className="card">
      {media && <div className="card__media">{media}</div>}
      {badge && <div className="card__badge">{badge}</div>}
      <div className="card__body">{children}</div>
      {footer && <div className="card__footer">{footer}</div>}
    </article>
  );
}

<Card
  media={<img src={p.image} alt="" />}
  badge={<Badge tone="danger">Скидка</Badge>}
  footer={<Button onClick={add}>В корзину</Button>}
>
  <h3>{p.name}</h3>
  <Money value={p.price} />
</Card>

// ленивый слот, если содержимое дорогое
function Card({ renderChart }) {
  const [open, setOpen] = useState(false);
  return <>{open && renderChart?.()}</>;   // не создаём узел, пока не нужен
}`,
    tip: 'Разграничьте формы слотов: пропсы дают типизацию и обязательность, children — свободу состава. И упомяните, что элемент, переданный пропсом, создаётся всегда — для тяжёлого содержимого нужна функция.' },

  { id: 'pt20',
    q: 'Завели один ThemeProvider со значением { user, theme, cart, toggleTheme } — теперь при добавлении товара в корзину перерисовывается всё приложение. Что делать?',
    a: `<h4>В чём боль</h4>
<p>Две ошибки наложились. Первая: в одном контексте лежат несвязанные данные с разной частотой изменения — корзина меняется часто, тема почти никогда, но потребители получают обновление всегда. Вторая: значение собирается литералом прямо в JSX, поэтому <strong>новый объект на каждом рендере провайдера</strong> — контекст сравнивает через <code>Object.is</code>, ссылка новая, будятся все.</p>
<p><code>React.memo</code> здесь не помогает: подписка на контекст живёт на фибере потребителя, и React помечает его к обновлению в обход сравнения пропсов.</p>

<h4>Решение</h4>
<p>Три шага, по нарастанию.</p>
<ol>
<li><strong>Стабилизировать value</strong>: <code>useMemo</code> с честными зависимостями, обработчики — <code>useCallback</code>.</li>
<li><strong>Разделить контексты по частоте и смыслу</strong>: <code>ThemeCtx</code>, <code>UserCtx</code>, <code>CartCtx</code>; отдельно — контекст действий (<code>dispatch</code>, <code>addToCart</code>), который стабилен навсегда и потому его потребители не перерисовываются никогда.</li>
<li><strong>Отдавать children пропсом</strong>: провайдер, получающий <code>children</code> сверху, не пересоздаёт поддерево при своём рендере.</li>
</ol>
<p>Если после этого всё равно горячо — контекст неподходящий инструмент: нужен внешний стор с подпиской по селектору (Zustand, Redux, <code>useSyncExternalStore</code>), где потребитель просыпается только на изменение своего среза.</p>

<h4>Почему так лучше</h4>
<p>Разделение контекстов и стабильный <code>value</code> ничего не стоят и убирают большую часть лишних коммитов. Пара «состояние отдельно, действия отдельно» — самый выгодный приём: кнопки и формы, которым нужен только <code>dispatch</code>, выпадают из перерисовок полностью.</p>

<h4>Когда не надо</h4>
<p>Не дробите на десять контекстов приложение, где провайдер обновляется дважды за сессию: получите провайдер-матрёшку и минус читаемость. Не используйте контекст как замену стора для высокочастотных значений (позиция курсора, прогресс) — там нужна подписка по селектору или ref. И не заворачивайте в <code>useMemo</code> value, зависимости которого всё равно меняются каждый рендер, — это самообман.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему memo не спасает потребителя?</strong> Контекст обновляет фибер напрямую, минуя сравнение пропсов. <strong>Что такое context selector?</strong> Отсутствующая в React возможность подписаться на часть значения; эмулируется библиотекой <code>use-context-selector</code> или внешним стором.</p>`,
    code: `// было: один контекст на всё + новый объект каждый рендер
function AppProvider({ children }) {
  const [theme, setTheme] = useState('light');
  const [cart, setCart] = useState([]);
  const user = useUser();
  return (
    <AppCtx.Provider value={{ user, theme, cart, setTheme, setCart }}>
      {children}
    </AppCtx.Provider>
  );
}
// добавили товар -> новый value -> перерисовались все потребители, включая шапку

// стало: контексты разделены, действия отдельно от данных
const CartStateCtx = createContext(null);
const CartApiCtx = createContext(null);   // стабилен навсегда

function CartProvider({ children }) {
  const [cart, dispatch] = useReducer(cartReducer, []);
  const api = useMemo(() => ({
    add: item => dispatch({ type: 'add', item }),
    remove: id => dispatch({ type: 'remove', id }),
  }), []);                                 // dispatch стабилен -> deps пусты
  return (
    <CartApiCtx.Provider value={api}>
      <CartStateCtx.Provider value={cart}>{children}</CartStateCtx.Provider>
    </CartApiCtx.Provider>
  );
}

function AddButton({ item }) {
  const { add } = useContext(CartApiCtx);  // не перерисуется при смене корзины
  return <button onClick={() => add(item)}>В корзину</button>;
}`,
    tip: 'Самый недооценённый приём — разделить контекст состояния и контекст действий: dispatch стабилен, поэтому все кнопки и формы выпадают из перерисовок без единого memo.' },

  { id: 'pt21',
    q: 'В поле поиска над таблицей на пять тысяч строк ввод «залипает»: символ появляется через полсекунды после нажатия. Как чинить?',
    a: `<h4>В чём боль</h4>
<p>Каждое нажатие клавиши запускает синхронную цепочку: <code>setState</code> → рендер таблицы → фильтрация пяти тысяч строк → создание пяти тысяч элементов → согласование → коммит. Всё это на главном потоке, поэтому браузер не может отрисовать введённый символ, пока цепочка не завершится. Пользователь видит задержку ввода — самый заметный вид лага, потому что рассинхрон с клавиатурой ощущается сразу.</p>

<h4>Решение</h4>
<p>Сначала <strong>измерить</strong> профайлером, что именно дорого: фильтрация, рендер строк или сам DOM. Дальше — по природе проблемы:</p>
<ul>
<li><strong>Много DOM-узлов</strong> — виртуализация: рендерить только видимые строки. Это единственное решение, которое убирает работу, а не переносит её.</li>
<li><strong>Дорогой рендер списка при быстром вводе</strong> — <code>useDeferredValue</code> для значения фильтра: поле обновляется срочно, список догоняет в фоне и прерывается новым вводом.</li>
<li><strong>Дорогая фильтрация</strong> — предпосчитанный индекс (нормализованные строки, Map по префиксу) вместо перебора с <code>toLowerCase</code> на каждый символ.</li>
<li><strong>Запрос на сервер</strong> — debounce на отправку, но не на само поле.</li>
</ul>

<h4>Почему так лучше</h4>
<p>Поле остаётся управляемым и отзывчивым, потому что его обновление отделено от тяжёлой работы. <code>useDeferredValue</code> при этом не «ждёт таймаут»: если машина успевает, результат приходит в том же кадре, а на медленной — старый список остаётся на экране, но интерфейс не замирает.</p>

<h4>Когда не надо</h4>
<p><code>useDeferredValue</code> не уменьшает работу: тяжёлый рендер всё равно происходит, просто с низким приоритетом, и на слабом устройстве список будет отставать заметно. Если строк действительно много, это костыль поверх отсутствующей виртуализации. Debounce на самом <code>value</code> поля делать нельзя — символы будут «прыгать», а курсор перескакивать. И не мемоизируйте всё подряд до профилирования: часто виноват один тяжёлый компонент строки, а не количество.</p>

<h4>Что спросят следом</h4>
<p><strong>Чем useDeferredValue отличается от useTransition?</strong> Первый откладывает значение, второй — обновление, инициированное вашим кодом; под капотом один и тот же механизм приоритетов. <strong>Что показать во время отставания?</strong> <code>isStale = value !== deferred</code> и приглушённый список — честнее спиннера.</p>`,
    code: `// было: одно состояние, всё срочное
function Search({ rows }) {
  const [q, setQ] = useState('');
  const found = rows.filter(r => r.name.toLowerCase().includes(q.toLowerCase()));
  return (
    <>
      <input value={q} onChange={e => setQ(e.target.value)} />
      {found.map(r => <Row key={r.id} row={r} />)}   {/* 5000 узлов на каждый символ */}
    </>
  );
}

// стало: срочное поле + несрочный список + предпосчитанный индекс
function Search({ rows }) {
  const [q, setQ] = useState('');
  const deferredQ = useDeferredValue(q);
  const isStale = q !== deferredQ;

  const index = useMemo(
    () => rows.map(r => ({ row: r, key: r.name.toLowerCase() })), [rows]
  );
  const found = useMemo(() => {
    const needle = deferredQ.toLowerCase();
    return index.filter(i => i.key.includes(needle)).map(i => i.row);
  }, [index, deferredQ]);

  return (
    <>
      <input value={q} onChange={e => setQ(e.target.value)} />
      <div style={{ opacity: isStale ? 0.6 : 1 }}>
        <VirtualList items={found} itemHeight={40} />  {/* только видимые строки */}
      </div>
    </>
  );
}`,
    tip: 'Разделите ответ на «убрать работу» (виртуализация, индекс) и «переставить приоритеты» (useDeferredValue). Второе без первого на слабых устройствах только маскирует проблему.' },

  { id: 'pt22',
    q: 'Обернули компонент в React.memo, а он всё равно перерисовывается на каждый рендер родителя. Почему и как найти причину?',
    a: `<h4>В чём боль</h4>
<p><code>React.memo</code> сравнивает пропсы поверхностно через <code>Object.is</code>. Любой проп, создаваемый в JSX родителя, — новая ссылка каждый рендер: инлайн-объект <code>style={{ margin: 8 }}</code>, литерал массива <code>items={[]}</code>, стрелка <code>onClick={() =&gt; ...}</code>, элемент <code>icon={&lt;Icon /&gt;}</code>, результат <code>.filter()</code> или <code>{...spread}</code>. Достаточно одного такого пропа, чтобы <code>memo</code> не сработал ни разу — и вы платите за сравнение, ничего не получая. Отдельный случай: компонент подписан на контекст, тогда <code>memo</code> вообще неприменим — контекст будит фибер напрямую.</p>

<h4>Решение</h4>
<p>Найти нестабильный проп и убрать источник нестабильности, а не добавлять мемоизацию вслепую:</p>
<ol>
<li>Профайлер React DevTools, галка «Record why each component rendered» — он прямо называет изменившиеся пропсы.</li>
<li>Константы вынести из компонента в модуль (<code>const EMPTY = []</code>).</li>
<li>Функции — <code>useCallback</code> со стабильными зависимостями; ещё лучше — передавать <code>dispatch</code> или id вместо замыкания.</li>
<li>Производные объекты — <code>useMemo</code>.</li>
<li>Часто проще <strong>изменить форму пропсов</strong>: передать <code>userId</code> вместо объекта <code>user</code> — примитивы сравниваются надёжно.</li>
</ol>
<p>С React Compiler мемоизация расставляется автоматически, но правило «стабильная ссылка» никуда не девается — компилятор просто делает эту работу за вас.</p>

<h4>Почему так лучше</h4>
<p>Мемоизация начинает окупаться: сравнение пропсов стоит наносекунды, а сэкономленный рендер поддерева — миллисекунды. Плюс код становится честнее: видно, от чего компонент реально зависит.</p>

<h4>Когда не надо</h4>
<p>Если поддерево лёгкое, <code>memo</code> вместе с обвязкой из <code>useCallback</code> дороже, чем перерисовка: вы платите памятью и сложностью за отрицательный выигрыш. Не мемоизируйте компонент, у которого пропсы меняются каждый раз по существу (тикающий таймер) — сравнение никогда не даст выигрыша. И не пытайтесь «починить» контекст мемоизацией потребителя.</p>

<h4>Что спросят следом</h4>
<p><strong>Что делает второй аргумент memo?</strong> Кастомное сравнение; опасен тем, что легко забыть новый проп и получить залипший UI. <strong>Почему children ломает memo?</strong> JSX внутри создаёт новый объект элемента каждый рендер — <code>memo</code> на таком компоненте бесполезен без мемоизации самого <code>children</code>.</p>`,
    code: `// было: memo есть, а толку нет
const Row = React.memo(function Row({ item, style, onSelect }) { /* ... */ });

function List({ items }) {
  return items.map(item => (
    <Row
      key={item.id}
      item={item}
      style={{ height: 40 }}                 // новый объект каждый рендер
      onSelect={() => select(item.id)}       // новая функция каждый рендер
    />
  ));
}

// стало: стабильные ссылки
const ROW_STYLE = { height: 40 };            // вынесли из компонента

const Row = React.memo(function Row({ item, style, onSelect }) {
  return <div style={style} onClick={() => onSelect(item.id)} />;  // id вместо замыкания
});

function List({ items, onSelect }) {
  const handleSelect = useCallback(id => onSelect(id), [onSelect]);
  return items.map(item => (
    <Row key={item.id} item={item} style={ROW_STYLE} onSelect={handleSelect} />
  ));
}

// быстрый способ найти виновника без профайлера
useEffect(() => { console.log('изменились пропсы'); }, [item, style, onSelect]);`,
    tip: 'Назовите порядок действий: сначала профайлер с «why did this render», потом устранение нестабильной ссылки, и только потом memo. Обратный порядок — самая частая причина бесполезной мемоизации.' },

  { id: 'pt23',
    q: 'На странице тяжёлый дашборд с графиками. Любое изменение в шапке — открыли меню, поменяли заголовок — перерисовывает графики. Как изолировать?',
    a: `<h4>В чём боль</h4>
<p>Состояние живёт в общем родителе, поэтому его рендер прогоняет всё поддерево, включая графики, где каждая перерисовка — пересчёт шкал и перерисовка канваса. React не знает, что графики не зависят от состояния меню: он рендерит детей, потому что рендерится родитель.</p>

<h4>Решение</h4>
<p>Изолировать перерисовку одним из трёх способов, в порядке дешевизны.</p>
<ol>
<li><strong>Опустить состояние</strong> в компонент, которому оно принадлежит (меню владеет своим <code>isOpen</code>). Половина случаев закрывается здесь.</li>
<li><strong>Поднять содержимое через children</strong>: тяжёлое поддерево передаётся пропсом сверху, и рендер родителя его не пересоздаёт. Работает без <code>memo</code>, потому что React видит тот же самый объект элемента и переиспользует фибер.</li>
<li><strong>React.memo</strong> на границе тяжёлого поддерева — если пропсы стабильны и первые два способа неприменимы.</li>
</ol>
<p>Дополнительно: если данные для графика приходят из стора, подписывайтесь селектором именно на них, чтобы обновление соседнего среза не будило компонент.</p>

<h4>Почему так лучше</h4>
<p>Первые два приёма меняют структуру, а не добавляют кеш: их не нужно поддерживать, и они не ломаются от забытого <code>useCallback</code>. <code>memo</code> же — контракт, который надо соблюдать во всех местах вызова, иначе он молча перестаёт работать.</p>

<h4>Когда не надо</h4>
<p>Не изолируйте то, что не измеряли: рендер «тяжёлого» компонента часто занимает 0.3 мс, и вся возня бессмысленна. Не превращайте дерево в лабиринт из <code>children</code>-проколов ради микрооптимизации — читаемость важнее. И помните, что <code>memo</code> не спасёт, если компонент подписан на контекст, который обновляется: там нужно дробить контекст.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему приём с children работает?</strong> Элементы создаются в родителе <strong>выше</strong>; при его рендере <code>props.children</code> — та же ссылка, и React пропускает согласование этого поддерева. <strong>Как измерить выигрыш?</strong> Profiler-вкладка: сравнить длительность коммита до и после; полезно записать взаимодействие, а не «попереключать наугад».</p>`,
    code: `// было: состояние меню в общем родителе -> графики рендерятся зря
function Page() {
  const [menuOpen, setMenuOpen] = useState(false);
  return (
    <>
      <Header onToggle={() => setMenuOpen(o => !o)} open={menuOpen} />
      <Charts data={data} />        {/* пересчитывает шкалы на каждый клик по меню */}
    </>
  );
}

// стало (1): состояние опущено туда, где используется
function Header() {
  const [open, setOpen] = useState(false);
  return <header>{open && <Menu />}<button onClick={() => setOpen(o => !o)} /></header>;
}
function Page() { return <><Header /><Charts data={data} /></>; }

// стало (2): состояние обязано быть наверху -> тяжёлое приходит как children
function Shell({ children }) {
  const [menuOpen, setMenuOpen] = useState(false);
  return (
    <>
      <Header open={menuOpen} onToggle={() => setMenuOpen(o => !o)} />
      {children}                    {/* та же ссылка -> React пропускает поддерево */}
    </>
  );
}
function Page() { return <Shell><Charts data={data} /></Shell>; }

// стало (3): последняя линия — memo на границе
const Charts = React.memo(function Charts({ data }) { /* ... */ });`,
    tip: 'Порядок важнее приёмов: сначала переставить состояние, потом children, и только потом memo. Первые два не требуют поддержки и не ломаются от одного инлайн-объекта.' },

  { id: 'pt24',
    q: 'Таблица на десять тысяч строк открывается четыре секунды и потом лагает при скролле. Пагинацию продукт не хочет. Что делать?',
    a: `<h4>В чём боль</h4>
<p>Десять тысяч строк — это десятки тысяч DOM-узлов. Дорого всё сразу: создание элементов React, согласование, вставка в DOM, расчёт стилей и layout браузером, память. Дальше каждый скролл-кадр браузер обрабатывает огромное дерево, а любое обновление состояния прогоняет согласование по всему списку. Это не оптимизируется мемоизацией: работа реально есть, её нужно не ускорять, а не делать.</p>

<h4>Решение</h4>
<p><strong>Виртуализация</strong> (windowing): в DOM держатся только видимые строки плюс небольшой запас (overscan). Контейнер фиксированной высоты со скроллом; распорка задаёт полную высоту <code>itemCount * itemHeight</code>, чтобы полоса прокрутки была честной; по позиции скролла вычисляется диапазон индексов; видимые строки позиционируются абсолютно или через <code>transform</code>.</p>
<p>Строки разной высоты — режим динамических размеров: библиотека измеряет отрендеренные элементы, кеширует высоты и корректирует смещения (TanStack Virtual, react-virtuoso). Писать это руками не стоит: краевых случаев много.</p>

<h4>Почему так лучше</h4>
<p>Стоимость перестаёт зависеть от размера данных: в DOM всегда 20–40 строк независимо от того, десять тысяч их или миллион. Первый рендер становится мгновенным, память падает на порядок, скролл держит 60 fps.</p>

<h4>Когда не надо</h4>
<p>Цена ощутимая. Ломается <code>Ctrl+F</code> по странице и печать — невидимых строк в DOM нет. Ссылки внутри невидимых строк не индексируются поисковиками. Сложнее доступность: нужны <code>aria-rowcount</code>/<code>aria-rowindex</code>, иначе скринридер сообщит неверный размер таблицы. Труднее «прыгнуть к элементу» и восстановить позицию скролла после возврата. При динамических высотах бывает дрожание при быстром скролле вверх. Для списка на 200 строк всё это не окупается — там достаточно мемоизации строки. И если данных много, потому что их <strong>не должно</strong> быть много, правильный ответ — фильтры и серверная пагинация.</p>

<h4>Что спросят следом</h4>
<p><strong>Как совместить с бесконечной прокруткой?</strong> Виртуализатор сообщает, что показан хвост диапазона, — грузим следующую страницу; данные накапливаются в кеше запроса. <strong>Что с CSS?</strong> Для простых случаев есть <code>content-visibility: auto</code> с <code>contain-intrinsic-size</code> — браузер сам пропускает отрисовку невидимого, без JS.</p>`,
    code: `// было: рендерим всё
<tbody>{rows.map(r => <Row key={r.id} row={r} />)}</tbody>
// 10 000 строк -> ~4 c до первого кадра, лаги при скролле

// стало: в DOM только видимое
function VirtualTable({ rows, rowHeight = 40 }) {
  const parentRef = useRef(null);
  const virtual = useVirtualizer({
    count: rows.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => rowHeight,
    overscan: 8,                                 // запас, чтобы не мигало
  });

  return (
    <div ref={parentRef} style={{ height: 600, overflow: 'auto' }}
         role="grid" aria-rowcount={rows.length}>   {/* размер для скринридера */}
      <div style={{ height: virtual.getTotalSize(), position: 'relative' }}>
        {virtual.getVirtualItems().map(v => (
          <div
            key={rows[v.index].id}
            role="row" aria-rowindex={v.index + 1}
            style={{ position: 'absolute', top: 0, left: 0, right: 0,
                     height: v.size, transform: 'translateY(' + v.start + 'px)' }}
          >
            <Row row={rows[v.index]} />
          </div>
        ))}
      </div>
    </div>
  );
}`,
    tip: 'Обязательно назовите цену: ломается поиск по странице и печать, усложняется доступность. Кандидат, который говорит только про fps, границы приёма не знает.' },

  { id: 'pt25',
    q: 'Бандл вырос до 2.5 МБ, первый экран грузится семь секунд. В нём при этом только форма логина. Как разбирать?',
    a: `<h4>В чём боль</h4>
<p>Один бандл на всё приложение: пользователь скачивает и парсит редактор, графики, генератор PDF и локали для тридцати языков, чтобы увидеть два поля и кнопку. Страдают все метрики: сеть, но главное — время парсинга и исполнения JS на слабом телефоне, где 2.5 МБ превращаются в секунды блокировки главного потока. Растёт TBT, отодвигается интерактивность.</p>

<h4>Решение</h4>
<p>Начать с <strong>анализа</strong> (<code>rollup-plugin-visualizer</code>, <code>webpack-bundle-analyzer</code>, <code>source-map-explorer</code>) — почти всегда половину веса дают три-четыре библиотеки. Дальше три уровня разбиения:</p>
<ol>
<li><strong>По маршрутам</strong>: <code>React.lazy</code> плюс <code>Suspense</code> на уровне роутера — самый большой выигрыш за наименьшие усилия.</li>
<li><strong>По взаимодействию</strong>: модалки, редакторы, графики грузятся при первом открытии, а не при загрузке страницы.</li>
<li><strong>По условиям</strong>: полифиллы, админские экраны, тяжёлые локали — только тем, кому нужны.</li>
</ol>
<p>Отдельно — замена тяжёлых зависимостей (moment на date-fns или <code>Intl</code>), проверка tree-shaking (импорт из корня пакета часто тянет всё) и вынос долгих вычислений в воркер.</p>
<p>Ключевой приём против «белого экрана при переходе» — <strong>префетч</strong>: догружать чанк на <code>hover</code>/<code>focus</code> по ссылке, тогда к моменту клика код уже в кеше.</p>

<h4>Почему так лучше</h4>
<p>Первый экран получает ровно то, что нужно для первого экрана. Остальное приезжает по мере надобности и параллельно с работой пользователя. Побочный плюс — лучшее кеширование: правка в редакторе не инвалидирует чанк логина.</p>

<h4>Когда не надо</h4>
<p>Слишком мелкое дробление вредит: десятки чанков — это десятки запросов, водопад «чанк грузит чанк» и потеря сжатия (мелкие файлы жмутся хуже). Не выносите в отдельный чанк то, что нужно сразу — получите каскад ожиданий. Не оборачивайте в <code>lazy</code> компонент, который весит 4 КБ. И следите за сдвигами макета: <code>Suspense</code>-фолбэк должен занимать столько же места, сколько содержимое, иначе испортите CLS.</p>

<h4>Что спросят следом</h4>
<p><strong>Как избежать мигания фолбэка?</strong> Префетч на hover плюс <code>useTransition</code> при навигации: старый экран остаётся, пока грузится новый. <strong>Что с ошибками загрузки чанка?</strong> После деплоя старые хеши исчезают: нужен error boundary с повтором и перезагрузкой страницы, иначе пользователь получит белый экран.</p>`,
    code: `// было: всё в одном бандле
import Editor from './Editor';          // 800 КБ
import Charts from './Charts';          // 600 КБ
import { generatePdf } from './pdf';    // 400 КБ

// стало: разбиение по маршрутам и взаимодействию
const Editor = React.lazy(() => import('./Editor'));
const Charts = React.lazy(() => import('./Charts'));

<Routes>
  <Route path="/login" element={<Login />} />            {/* в основном бандле */}
  <Route path="/editor" element={
    <Suspense fallback={<EditorSkeleton />}><Editor /></Suspense>  // скелет тех же размеров
  } />
</Routes>

// по взаимодействию: код едет при первом открытии
async function onExportClick() {
  const { generatePdf } = await import('./pdf');
  await generatePdf(doc);
}

// префетч на hover — к моменту клика чанк уже в кеше
<Link to="/editor" onMouseEnter={() => import('./Editor')}>Редактор</Link>

// устойчивость к протухшим хешам после деплоя
const lazyWithRetry = factory => React.lazy(() =>
  factory().catch(() => { window.location.reload(); return new Promise(() => {}); })
);`,
    tip: 'Два пункта, которые отличают практика: префетч на hover (иначе разбиение ощущается как тормоза) и обработка ошибки загрузки чанка после деплоя — это реальный инцидент, а не теория.' },

  { id: 'pt26',
    q: 'На поиск повесили debounce 500 мс — жалобы, что поле тормозит. Убрали — сервер получает запрос на каждый символ. Где правильное место для дебаунса?',
    a: `<h4>В чём боль</h4>
<p>Debounce поставили на <strong>обновление состояния поля</strong>, а не на побочный эффект. Итог: пользователь печатает, а символы появляются с задержкой; при быстром вводе курсор прыгает в конец, потому что <code>value</code> приходит из отложенного состояния. Вторая крайность — без задержки: тридцать запросов на слово, лишняя нагрузка и гонки ответов.</p>
<p>Отдельная ошибка — создавать дебаунс-функцию прямо в теле компонента: на каждом рендере это <strong>новая функция с новым таймером</strong>, поэтому дебаунс фактически не работает.</p>

<h4>Решение</h4>
<p>Разделить два потока. Поле обновляется <strong>мгновенно</strong> — оно управляемое и синхронное. Задерживается только эффект: запрос на сервер, запись в URL, отправка аналитики.</p>
<p>Технически: держим <code>query</code> в состоянии, а дебаунс применяем к значению, которое уходит в запрос. В библиотеке запросов ключ строится по отложенному значению, и она сама отменяет неактуальные запросы. Дебаунс-функцию создаём один раз (<code>useMemo</code>/<code>useRef</code>) и обязательно отменяем при размонтировании.</p>
<p>Стоит помнить и про <strong>throttle</strong>: для скролла и ресайза нужен именно он (регулярные вызовы), а не debounce (только последний).</p>

<h4>Почему так лучше</h4>
<p>Ввод остаётся мгновенным — это главное для ощущения скорости. Сеть разгружается. Гонки закрываются на уровне ключа запроса, а не ручными флагами.</p>

<h4>Когда не надо</h4>
<p>Debounce не нужен там, где события редкие (сабмит формы) — он только добавит задержку. Для локальной фильтрации без сети лучше <code>useDeferredValue</code>: он адаптивный и не задерживает результат на слабой машине фиксированным таймаутом. Для автодополнения слишком большая задержка (500 мс и выше) ощущается как тормоза — обычно берут 150–300 мс. И не забывайте <code>leading</code>-вызов там, где первый ввод должен сработать сразу.</p>

<h4>Что спросят следом</h4>
<p><strong>Чем debounce отличается от throttle?</strong> Debounce ждёт паузу и вызывает один раз в конце; throttle вызывает не чаще раза в интервал — для прогресса скролла нужен второй. <strong>Что с гонками?</strong> Дебаунс их уменьшает, но не убирает: нужна отмена через <code>AbortController</code> или проверка актуальности ответа.</p>`,
    code: `// было: дебаунс на само поле — ввод «залипает», курсор прыгает
const setQueryDebounced = debounce(setQuery, 500);   // новая функция каждый рендер!
<input value={query} onChange={e => setQueryDebounced(e.target.value)} />

// стало: поле мгновенное, отложен только запрос
function Search() {
  const [query, setQuery] = useState('');       // срочное: то, что видит пользователь
  const debouncedQuery = useDebouncedValue(query, 250);  // несрочное: то, что уходит в сеть

  const { data, isFetching } = useQuery({
    queryKey: ['search', debouncedQuery],
    queryFn: ({ signal }) => api.search(debouncedQuery, { signal }), // отмена гонок
    enabled: debouncedQuery.length >= 2,
  });

  return (
    <>
      <input value={query} onChange={e => setQuery(e.target.value)} />
      {isFetching && <Spinner size="sm" />}
      <Results items={data} />
    </>
  );
}

function useDebouncedValue(value, delay) {
  const [v, setV] = useState(value);
  useEffect(() => {
    const t = setTimeout(() => setV(value), delay);
    return () => clearTimeout(t);              // без этого дебаунса нет
  }, [value, delay]);
  return v;
}`,
    tip: 'Правило в одну фразу: дебаунсится эффект, а не состояние поля. И упомяните классическую ошибку — debounce, созданный в теле компонента, пересоздаётся каждый рендер и не работает вовсе.' },

  { id: 'pt27',
    q: 'В рендере компонента вижу sort по десяти тысячам элементов, new Date() и вызов formatCurrency в цикле. Профайлер показывает 80 мс на рендер. Что делать?',
    a: `<h4>В чём боль</h4>
<p>Рендер обязан быть быстрым и чистым, потому что вызывается часто и может быть выброшен React при прерывании. Тяжёлые вычисления в теле компонента повторяются на каждый рендер, даже когда входные данные не менялись: перерисовался родитель — снова сортировка. Плюс <code>sort</code> мутирует исходный массив: если это проп или данные из стора, вы молча портите чужое состояние, и баг проявится в другом компоненте.</p>
<p>Отдельно вредны нечистые вызовы: <code>new Date()</code> и <code>Math.random()</code> в рендере дают разный результат на сервере и на клиенте — ошибка гидратации.</p>

<h4>Решение</h4>
<p>Расставить вычисления по местам:</p>
<ul>
<li><strong>Зависит только от пропсов/состояния и дорого</strong> — <code>useMemo</code> с честными зависимостями, обязательно на копии (<code>[...items].sort()</code>).</li>
<li><strong>Не зависит от рендера вообще</strong> — вынести в модуль: константы, форматтеры (<code>Intl.NumberFormat</code> создаётся один раз, а не на каждую ячейку — это буквально порядок разницы).</li>
<li><strong>Нечистое</strong> (<code>Date.now()</code>, случайные значения, чтение DOM) — в обработчик, эффект или в состояние при инициализации.</li>
<li><strong>Действительно долгое</strong> (парсинг мегабайта, кроп изображения) — в Web Worker: <code>useMemo</code> не спасёт от блокировки главного потока, он лишь пропускает повтор.</li>
</ul>

<h4>Почему так лучше</h4>
<p>Рендер возвращается к своей задаче — описать вид. Пропадают ошибки гидратации и мутации чужих данных. Вынос форматтеров часто даёт больший эффект, чем вся мемоизация: создание <code>Intl.NumberFormat</code> стоит десятки микросекунд, а в таблице их тысячи.</p>

<h4>Когда не надо</h4>
<p><code>useMemo</code> не бесплатен: сравнение зависимостей и удержание ссылки стоят памяти, а на дешёвых вычислениях это чистый убыток. Не мемоизируйте <code>a + b</code>. И помните, что <code>useMemo</code> — не гарантия: React вправе выбросить кеш, поэтому строить на нём корректность (например, генерацию id) нельзя — для этого <code>useState</code>-инициализатор или <code>useRef</code>.</p>

<h4>Что спросят следом</h4>
<p><strong>Как понять, что мемоизация нужна?</strong> Профайлер до и после; правило большой пальца — вычисление дороже 1 мс. <strong>Почему sort в рендере опасен вдвойне?</strong> Мутирует вход и одновременно даёт новую ссылку — ломает и чужие данные, и мемоизацию потребителей.</p>`,
    code: `// было: тяжёлое и нечистое в теле компонента
function Table({ items, currency }) {
  const sorted = items.sort((a, b) => a.total - b.total);   // мутирует проп!
  const now = new Date();                                   // ломает гидратацию
  return sorted.map(i => (
    <Row key={i.id}
      // новый Intl на каждую строку — десятки тысяч объектов
      sum={new Intl.NumberFormat('ru-RU', { style: 'currency', currency }).format(i.total)}
      fresh={now - i.createdAt < 86400000}
    />
  ));
}

// стало: форматтер в модуле, вычисление мемоизировано, время — в состоянии
const formatters = new Map();
const money = cur => {
  if (!formatters.has(cur)) {
    formatters.set(cur, new Intl.NumberFormat('ru-RU', { style: 'currency', currency: cur }));
  }
  return formatters.get(cur);
};

function Table({ items, currency }) {
  const sorted = useMemo(() => [...items].sort((a, b) => a.total - b.total), [items]);
  const [now] = useState(() => Date.now());     // одинаково на сервере и клиенте
  const fmt = money(currency);
  return sorted.map(i => (
    <Row key={i.id} sum={fmt.format(i.total)} fresh={now - i.createdAt < 86400000} />
  ));
}`,
    tip: 'Не ограничивайтесь useMemo: назовите вынос форматтеров в модуль. В таблицах создание Intl.NumberFormat на ячейку — типичная скрытая причина медленного рендера, которую профайлер показывает как «медленный компонент».' },

  { id: 'pt28',
    q: 'Коллега обернул в useMemo и useCallback вообще всё, включая сложение двух чисел. Аргумент: «хуже не будет». Возрази по существу.',
    a: `<h4>В чём боль</h4>
<p>Хуже будет, просто незаметно. Каждая мемоизация — это: место в массиве хуков фибера, массив зависимостей, который создаётся заново каждый рендер, цикл сравнения через <code>Object.is</code>, удержание старого значения в памяти до размонтирования. На дешёвом вычислении накладные расходы больше самого вычисления. В компоненте с сорока <code>useCallback</code> вы гарантированно платите на каждом рендере, а выигрываете только там, где потребитель обёрнут в <code>memo</code> и все остальные его пропсы тоже стабильны.</p>
<p>Есть и цена для людей: код становится нечитаемым, массивы зависимостей ошибаются, а ошибка в них — залипшее значение, которое ищут часами. Плюс удержание ссылок продлевает жизнь замыканиям — это реальные утечки в долгоживущих экранах.</p>

<h4>Решение</h4>
<p>Мемоизировать по показаниям, а не превентивно. Показания три: 1) значение уходит пропсом в компонент под <code>memo</code>; 2) значение стоит в зависимостях эффекта, и нестабильность вызывает лишние запуски; 3) вычисление реально дорогое по профайлеру (сортировка, парсинг, тяжёлые структуры). Во всех остальных случаях — не мемоизировать.</p>
<p>Системное решение — <strong>React Compiler</strong>: он расставляет мемоизацию автоматически на основе анализа, снимая вопрос с людей. Но он требует соблюдения правил React (никаких мутаций пропсов и состояния в рендере) — то есть дисциплина всё равно нужна.</p>

<h4>Почему так лучше</h4>
<p>Код короче и честнее: мемоизация в нём становится сигналом «здесь есть причина», а не фоновым шумом. Профайлер показывает реальные горячие места, а не ровный слой накладных расходов.</p>

<h4>Когда не надо</h4>
<p>Обратная крайность тоже плоха: в дизайн-системе и в библиотечном коде стабильные ссылки — часть контракта, там <code>useCallback</code> оправдан по умолчанию, потому что вы не знаете, кто и как будет использовать компонент. То же для значений контекста и для колбэков, попадающих в подписки.</p>

<h4>Что спросят следом</h4>
<p><strong>Сколько стоит useMemo?</strong> Порядка сотни наносекунд плюс аллокация массива зависимостей — мало, но умножается на число хуков и рендеров. <strong>Что даёт React Compiler?</strong> Автоматическую мемоизацию на уровне сборки; ручные <code>useMemo</code> при этом чаще всего можно удалить, но только если компонент соответствует правилам React.</p>`,
    code: `// было: мемоизация ради мемоизации
function Cell({ a, b, label }) {
  const sum = useMemo(() => a + b, [a, b]);              // дороже, чем само сложение
  const title = useMemo(() => label.trim(), [label]);    // то же самое
  const onClick = useCallback(() => console.log(sum), [sum]); // потребитель не memo
  return <div onClick={onClick}>{title}: {sum}</div>;
}

// стало: считаем прямо
function Cell({ a, b, label }) {
  const sum = a + b;
  return <div onClick={() => console.log(sum)}>{label.trim()}: {sum}</div>;
}

// а вот здесь мемоизация обоснована — есть все три признака
function Chart({ points, onZoom }) {
  const series = useMemo(() => buildSeries(points), [points]);  // 12 мс по профайлеру
  const handleZoom = useCallback(r => onZoom(r), [onZoom]);     // уходит в memo-компонент
  useEffect(() => { subscribe(handleZoom); }, [handleZoom]);    // и в зависимости эффекта
  return <HeavyCanvas series={series} onZoom={handleZoom} />;
}
const HeavyCanvas = React.memo(Canvas);`,
    tip: 'Назовите три признака, при которых мемоизация оправдана: проп для memo-компонента, зависимость эффекта, дорогое вычисление по профайлеру. И упомяните React Compiler — он снимает спор целиком, но требует соблюдения правил React.' },

  { id: 'pt29',
    q: 'В автодополнении иногда показываются результаты для предыдущего запроса: набрал «мос», потом «москва», а в списке — результаты по «мос». Почему и как чинить?',
    a: `<h4>В чём боль</h4>
<p>Гонка запросов (race condition). Запросы уходят параллельно, но приходят в произвольном порядке: ответ на «мос» может прийти после ответа на «москва», и последний <code>setState</code> перезапишет актуальные данные устаревшими. Воспроизводится нестабильно, чаще на медленной сети — поэтому такие баги живут в проде годами и «не воспроизводятся у меня».</p>
<p>Та же природа у второго классического бага: ответ приходит после размонтирования компонента, и обновление состояния уходит в никуда, удерживая замыкание.</p>

<h4>Решение</h4>
<p>Три уровня надёжности.</p>
<ol>
<li><strong>Отмена</strong>: <code>AbortController</code> — при новом запросе рвём предыдущий, в очистке эффекта вызываем <code>abort()</code>. Это лучший вариант: экономит и сеть, и сервер.</li>
<li><strong>Проверка актуальности</strong>: флаг <code>ignore</code> в замыкании эффекта или сравнение «мой запрос всё ещё последний» по счётчику/ключу. Нужен там, где отмена невозможна (например, вычисление в воркере).</li>
<li><strong>Библиотека запросов</strong>: <code>queryKey</code> делает актуальность структурной — данные привязаны к ключу, а не к моменту прихода. Ответ на старый ключ просто попадает в свою ячейку кеша и на экран не влияет.</li>
</ol>

<h4>Почему так лучше</h4>
<p>Гонка перестаёт зависеть от порядка ответов: либо старый запрос отменён, либо его результат отбрасывается по признаку неактуальности. Отмена дополнительно снимает нагрузку с сервера при быстром вводе.</p>

<h4>Когда не надо</h4>
<p>Не отменяйте изменяющие запросы (POST/PUT): <code>abort</code> рвёт соединение на клиенте, но сервер мог уже применить операцию — получите «отменённый» платёж, который прошёл. Для мутаций нужны идемпотентные ключи и очередь, а не отмена. Ещё нюанс: <code>AbortError</code> нужно отфильтровывать в <code>catch</code>, иначе штатная отмена нарисует пользователю ошибку.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему одного дебаунса мало?</strong> Он уменьшает число запросов, но два всё равно могут разъехаться по времени ответа. <strong>Как быть с гонкой мутаций?</strong> Последовательная очередь на клиенте плюс версия сущности на сервере (<code>If-Match</code>/ETag), чтобы отклонить устаревшую запись.</p>`,
    code: `// было: побеждает тот, кто ответил последним
useEffect(() => {
  fetch('/api/search?q=' + q)
    .then(r => r.json())
    .then(setResults);        // ответ на «мос» может прийти после «москва»
}, [q]);

// стало (1): отмена — предыдущий запрос рвём
useEffect(() => {
  const ac = new AbortController();
  fetch('/api/search?q=' + encodeURIComponent(q), { signal: ac.signal })
    .then(r => r.json())
    .then(setResults)
    .catch(e => { if (e.name !== 'AbortError') setError(e); });  // отмена — не ошибка
  return () => ac.abort();
}, [q]);

// стало (2): проверка актуальности, если отменить нельзя
useEffect(() => {
  let ignore = false;
  search(q).then(res => { if (!ignore) setResults(res); });
  return () => { ignore = true; };
}, [q]);

// стало (3): актуальность структурно — данные привязаны к ключу
const { data } = useQuery({
  queryKey: ['search', q],
  queryFn: ({ signal }) => api.search(q, { signal }),
});`,
    tip: 'Обязательно скажите, что отменять можно только чтение: abort мутации рвёт соединение, но не откатывает то, что сервер уже сделал. Для мутаций — идемпотентный ключ и очередь.' },

  { id: 'pt30',
    q: 'В сетевой вкладке вижу четыре одинаковых запроса /api/me за секунду — их шлют четыре разных компонента. Как убрать дубли, не поднимая данные в корень?',
    a: `<h4>В чём боль</h4>
<p>Каждый компонент самостоятельно грузит одни и те же данные в своём <code>useEffect</code>. Последствия: лишний трафик и нагрузка на сервер; четыре независимых состояния загрузки, из-за чего экран моргает частями; четыре копии данных, которые могут разъехаться (один компонент перезапросил, остальные нет). Стандартная «оптимизация» — поднять запрос в корень и раздать пропсами — лечит дубли, но возвращает prop drilling и заставляет корень знать про нужды листьев.</p>

<h4>Решение</h4>
<p><strong>Дедупликация по ключу</strong> в общем слое кеша. Библиотеки запросов делают это из коробки: несколько компонентов вызывают <code>useQuery</code> с одинаковым <code>queryKey</code>, а сеть видит один запрос — остальные подписываются на тот же промис. Данные при этом остаются в одном экземпляре, обновление приходит всем сразу.</p>
<p>Если библиотеки нет, тот же эффект даёт <strong>кеш промисов</strong>: <code>Map</code> ключ → in-flight promise, из которого удаляем запись по завершении. В React 19 то же самое умеет <code>cache()</code> для серверных компонентов и <code>use()</code> для промисов.</p>
<p>Отдельный слой — HTTP: правильные <code>Cache-Control</code> и <code>ETag</code> убирают повторные запросы между навигациями бесплатно.</p>

<h4>Почему так лучше</h4>
<p>Компоненты остаются автономными: каждый объявляет, что ему нужно, и не знает про остальных. Дублей нет, состояние загрузки одно, обновление после мутации доходит до всех подписчиков одновременно.</p>

<h4>Когда не надо</h4>
<p>Дедупликация опасна для запросов с побочными эффектами: два «одинаковых» POST — это, скорее всего, два разных действия, их схлопывать нельзя. Осторожно с ключами: если в ключ не попал важный параметр (например, локаль или id организации), разные данные склеятся в одну ячейку — это худший вид бага, потому что пользователь увидит чужое. И следите за окном дедупликации: слишком долгое превращает её в кеш и отдаёт устаревшее.</p>

<h4>Что спросят следом</h4>
<p><strong>Что попадает в ключ?</strong> Всё, от чего зависит ответ, включая заголовки-влияющие параметры: пользователь, локаль, фильтры. <strong>Как обновить данные после мутации?</strong> Инвалидация по ключу — один вызов будит всех подписчиков.</p>`,
    code: `// было: четыре компонента — четыре запроса
function Avatar() {
  const [me, setMe] = useState(null);
  useEffect(() => { fetch('/api/me').then(r => r.json()).then(setMe); }, []);
}
// то же самое в Header, Sidebar, Settings

// стало: один ключ — один запрос, четыре подписчика
function useMe() {
  return useQuery({ queryKey: ['me'], queryFn: api.getMe, staleTime: 60_000 });
}
function Avatar()  { const { data: me } = useMe(); }
function Header()  { const { data: me } = useMe(); }   // сеть не тронута

// без библиотеки: кеш in-flight промисов
const inflight = new Map();
function dedupe(key, fn) {
  if (!inflight.has(key)) {
    inflight.set(key, fn().finally(() => inflight.delete(key)));
  }
  return inflight.get(key);
}
const getMe = () => dedupe('me', () => fetch('/api/me').then(r => r.json()));

// ключ должен включать всё, от чего зависит ответ
queryKey: ['orders', orgId, locale, { status, page }]   // иначе покажете чужие данные`,
    tip: 'Самое ценное здесь — предупреждение про ключ: если в него не попал orgId или локаль, дедупликация склеит разные данные и покажет пользователю чужое. Это худший баг из возможных в кеше.' },

  { id: 'pt31',
    q: 'Лайк ставится через полсекунды после клика — сначала спиннер, потом сердечко. Продукт просит мгновенно. Как сделать и что с ошибками?',
    a: `<h4>В чём боль</h4>
<p>Интерфейс ждёт подтверждения сервера, хотя результат действия предсказуем в 99% случаев. Пользователь получает задержку там, где её могло не быть, и это ощущается как медленное приложение. Особенно заметно на списках: поставил три лайка подряд — три спиннера.</p>

<h4>Решение</h4>
<p><strong>Оптимистичное обновление</strong>: применяем ожидаемый результат к локальному состоянию сразу, запрос отправляем в фоне, при ошибке откатываем и сообщаем. Четыре обязательные части: 1) сохранить снимок предыдущего значения; 2) применить оптимистичное; 3) при ошибке вернуть снимок и показать понятное сообщение с возможностью повтора; 4) в любом случае синхронизироваться с сервером (инвалидация), потому что сервер мог применить не то, что вы предполагали.</p>
<p>Важная деталь — <strong>отмена текущих фоновых запросов</strong> перед оптимистичным изменением: иначе прилетевший ответ старого <code>GET</code> перезапишет ваш оптимистичный результат. В TanStack Query это <code>cancelQueries</code> в <code>onMutate</code>.</p>
<p>Для локального UI React даёт <code>useOptimistic</code>: значение живёт только на время перехода и само откатывается по его завершении.</p>

<h4>Почему так лучше</h4>
<p>Действие ощущается мгновенным, интерфейс перестаёт мигать спиннерами. Латентность сети скрывается в фоне, а редкий откат воспринимается как исключение — что честно, если ошибки действительно редки.</p>

<h4>Когда не надо</h4>
<p>Только для действий с <strong>предсказуемым</strong> результатом и низкой ценой ошибки: лайк, чекбокс, переименование, добавление в избранное. Нельзя для платежей, необратимых удалений, операций с проверками на сервере (лимиты, права, остатки на складе): показать «оплачено», а потом откатить — хуже, чем честно подождать. Плохо работает, когда сервер сам генерирует часть данных (id, порядковый номер, расчётная цена) — придётся показывать заглушки и потом их подменять, что даёт скачок интерфейса. Не подходит при частых ошибках: постоянные откаты разрушают доверие.</p>

<h4>Что спросят следом</h4>
<p><strong>Как избежать двойного применения при повторе?</strong> Идемпотентный ключ мутации, чтобы сервер распознал повтор. <strong>Что с несколькими быстрыми кликами?</strong> Очередь мутаций и откат по снимку до первой из них, иначе откат одной вернёт состояние, отменив последующие.</p>`,
    code: `// было: ждём сервер, потом рисуем
async function toggleLike(id) {
  setPending(true);
  await api.like(id);
  await refetch();               // ещё один круг ожидания
  setPending(false);
}

// стало: применяем сразу, откатываем при ошибке
const likeMutation = useMutation({
  mutationFn: id => api.like(id),

  onMutate: async id => {
    await qc.cancelQueries({ queryKey: ['posts'] });   // чтобы фоновый GET не перетёр
    const prev = qc.getQueryData(['posts']);           // снимок для отката
    qc.setQueryData(['posts'], posts =>
      posts.map(p => p.id === id ? { ...p, liked: true, likes: p.likes + 1 } : p)
    );
    return { prev };
  },

  onError: (err, id, ctx) => {
    qc.setQueryData(['posts'], ctx.prev);              // откат
    toast.error('Не удалось поставить лайк', { action: { label: 'Повторить', onClick: () => likeMutation.mutate(id) } });
  },

  onSettled: () => qc.invalidateQueries({ queryKey: ['posts'] }),  // сверка с сервером
});

// локальный вариант на время перехода
const [optimisticLikes, addOptimistic] = useOptimistic(likes, (s, delta) => s + delta);`,
    tip: 'Назовите четыре обязательные части: снимок, применение, откат с сообщением, финальная сверка. И обязательно cancelQueries — без него фоновый GET перетирает оптимистичное значение, и баг выглядит как «иногда лайк слетает».' },

  { id: 'pt32',
    q: 'После создания заказа список заказов показывает старые данные, пока не нажмёшь F5. Где-то надо чистить кеш. Как это делать системно?',
    a: `<h4>В чём боль</h4>
<p>Кеш есть, а стратегии инвалидации нет. Каждый разработчик решает по-своему: кто-то вручную дописывает новый элемент в кеш, кто-то дёргает <code>refetch</code> у конкретного хука, кто-то не делает ничего. В результате часть экранов обновляется, часть — нет, и появляются рассинхроны: в списке заказ есть, в счётчике в шапке — нет.</p>

<h4>Решение</h4>
<p>Считать кеш <strong>снимком серверного состояния с известным сроком годности</strong> и договориться о трёх вещах.</p>
<ol>
<li><strong>Структура ключей.</strong> Иерархия от общего к частному: <code>['orders']</code>, <code>['orders', 'list', filters]</code>, <code>['orders', 'detail', id]</code>. Тогда инвалидация по префиксу накрывает всё семейство одной строкой. Ключи лучше строить фабрикой, а не литералами по файлам, — иначе опечатка молча ломает инвалидацию.</li>
<li><strong>Кто инвалидирует.</strong> Мутация объявляет, что она портит: в <code>onSuccess</code> — <code>invalidateQueries</code> по затронутым префиксам. Это ответственность мутации, а не экрана.</li>
<li><strong>Сроки.</strong> <code>staleTime</code> — как долго данные считаются свежими (для справочников минуты, для остатков — ноль); <code>gcTime</code> — сколько неиспользуемый кеш живёт в памяти.</li>
</ol>
<p>Плюс фоновые триггеры: обновление при возврате фокуса на вкладку и при восстановлении сети.</p>

<h4>Почему так лучше</h4>
<p>Правило одно и проверяемое на ревью: «мутация перечисляет затронутые ключи». Экраны перестают знать друг о друге. Иерархия ключей позволяет инвалидировать грубо (надёжно) там, где не жалко, и точечно там, где важна скорость.</p>

<h4>Когда не надо</h4>
<p>Не инвалидируйте корень (<code>invalidateQueries()</code> без ключа) после каждой мутации: это шторм запросов и мигание половины интерфейса. Обратная крайность — точечная правка кеша (<code>setQueryData</code>) вместо инвалидации: быстро, но вы дублируете серверную бизнес-логику на клиенте (пересчёт итогов, сортировка, права) и однажды разойдётесь с сервером. Разумный компромисс: <code>setQueryData</code> для мгновенного отклика плюс инвалидация для сверки.</p>

<h4>Что спросят следом</h4>
<p><strong>Чем invalidate отличается от refetch?</strong> Invalidate помечает данные протухшими и перезапрашивает только активные наблюдатели; refetch дёргает конкретный запрос принудительно. <strong>Как быть с данными, которые меняют другие пользователи?</strong> Короткий <code>staleTime</code>, обновление по фокусу, а для реального времени — WebSocket-события, которые точечно инвалидируют ключи.</p>`,
    code: `// было: ключи-литералы по файлам, инвалидация как получится
useQuery(['orders'], api.getOrders);
useQuery(['order-detail', id], () => api.getOrder(id));   // другой стиль ключа
await api.createOrder(data);
refetch();                                                 // обновился один экран из трёх

// стало: фабрика ключей + мутация объявляет, что портит
export const orderKeys = {
  all:    ['orders'],
  lists:  () => [...orderKeys.all, 'list'],
  list:   f => [...orderKeys.lists(), f],
  detail: id => [...orderKeys.all, 'detail', id],
};

useQuery({ queryKey: orderKeys.list(filters), queryFn: () => api.getOrders(filters),
           staleTime: 30_000 });

useMutation({
  mutationFn: api.createOrder,
  onSuccess: () => {
    qc.invalidateQueries({ queryKey: orderKeys.lists() });  // все списки, любые фильтры
    qc.invalidateQueries({ queryKey: ['stats'] });          // и счётчик в шапке
  },
});

// точечно там, где важна скорость, плюс сверка
onSuccess: (updated) => {
  qc.setQueryData(orderKeys.detail(updated.id), updated);   // мгновенно
  qc.invalidateQueries({ queryKey: orderKeys.lists() });    // и сверимся с сервером
}`,
    tip: 'Правило, которое стоит проговорить: «мутация перечисляет ключи, которые она портит». Плюс фабрика ключей — без неё инвалидация ломается от одной опечатки в литерале и никто этого не замечает.' },

  { id: 'pt33',
    q: 'Ответ API — вложенное дерево: заказ содержит клиента, клиент содержит адреса. Один и тот же клиент приходит в десяти заказах. После правки имени оно обновилось в одном месте из десяти. Что делать?',
    a: `<h4>В чём боль</h4>
<p>Данные хранятся так, как их прислал сервер, — вложенным деревом. Один и тот же клиент лежит в десяти копиях, и после правки обновляется та копия, до которой дотянулись. Дополнительно: обновление любого поля требует глубокого клонирования всей ветки, из-за чего меняется ссылка на весь массив заказов и перерисовывается весь список; поиск сущности по id превращается в перебор дерева.</p>

<h4>Решение</h4>
<p><strong>Нормализация</strong>: хранить сущности плоскими таблицами <code>id → entity</code>, а связи — по идентификаторам. По сути это реляционная схема в памяти: <code>orders.byId</code>, <code>customers.byId</code>, <code>order.customerId</code>. Собирать вложенную форму для отображения — задача селекторов (мемоизированных), а не хранилища.</p>
<p>Инструменты: <code>createEntityAdapter</code> в Redux Toolkit, <code>normalizr</code> для разбора ответов, <code>reselect</code> для сборки. В GraphQL это делает Apollo сам — по <code>__typename</code> и <code>id</code>.</p>

<h4>Почему так лучше</h4>
<p>У сущности один экземпляр, поэтому обновление доходит всюду одновременно. Обновление одного поля меняет ссылку только на одну запись — перерисовывается одна строка. Доступ по id — константный. Кеш становится предсказуемым: видно, что в нём лежит.</p>

<h4>Когда не надо</h4>
<p>Нормализация стоит дорого. Появляется слой «сборки» — селекторы, которые надо писать, мемоизировать и тестировать; данные в сторе перестают быть похожими на ответ API, и отладка усложняется. Проблема удаления: нужно чистить ссылки, иначе останутся «висячие» id. Для экранов, где данные читаются и выбрасываются (список без пересечений, отчёт, дашборд), нормализация — чистый оверхед: TanStack Query кеширует ответы по ключу «как есть», и это правильный дефолт.</p>
<p>Практический критерий: нормализуйте, когда одна сущность встречается в нескольких местах <strong>и</strong> редактируется. Если только читается — не трогайте.</p>

<h4>Что спросят следом</h4>
<p><strong>Как совместить с TanStack Query?</strong> Обычно не нормализуют вовсе, а после мутации инвалидируют ключи; нормализация — территория Redux и Apollo. <strong>Чем плохи висячие ссылки?</strong> Селектор вернёт <code>undefined</code> вместо сущности — нужны проверки или каскадная чистка при удалении.</p>`,
    code: `// было: дерево с дублями
const state = {
  orders: [
    { id: 1, total: 100, customer: { id: 7, name: 'Иванов', addresses: [...] } },
    { id: 2, total: 250, customer: { id: 7, name: 'Иванов', addresses: [...] } },
  ],
};
// правим имя -> надо найти все вхождения и обновить каждое
state.orders = state.orders.map(o =>
  o.customer.id === 7 ? { ...o, customer: { ...o.customer, name: 'Петров' } } : o
);   // новая ссылка на весь массив -> перерисовался весь список

// стало: плоские таблицы + связи по id
const state = {
  orders:    { byId: { 1: { id: 1, total: 100, customerId: 7 },
                       2: { id: 2, total: 250, customerId: 7 } }, allIds: [1, 2] },
  customers: { byId: { 7: { id: 7, name: 'Иванов', addressIds: [3] } } },
  addresses: { byId: { 3: { id: 3, city: 'Москва' } } },
};

// правка в одном месте — видна везде, меняется одна ссылка
customers.byId[7] = { ...customers.byId[7], name: 'Петров' };

// сборка для отображения — мемоизированный селектор
const selectOrderView = createSelector(
  [s => s.orders.byId[id], s => s.customers.byId],
  (order, customers) => ({ ...order, customer: customers[order.customerId] })
);`,
    tip: 'Критерий в одну фразу: нормализуем то, что встречается в нескольких местах и редактируется. Данные «прочитал и забыл» нормализовать не надо — это лишний слой сборки без выигрыша.' },

  { id: 'pt34',
    q: 'В компоненте isLoading, error, data и ещё isEmpty. В рендере лесенка из if-ов, при этом иногда мелькает «ничего не найдено» до появления данных. Как навести порядок?',
    a: `<h4>В чём боль</h4>
<p>Состояния запроса выражены независимыми флагами, поэтому порядок проверок в рендере становится негласным контрактом. Проверили <code>data.length === 0</code> раньше <code>isLoading</code> — получили вспышку «ничего не найдено» перед данными. Забыли ветку ошибки — вместо сообщения пустой экран. Добавили «обновление в фоне» — и оказалось, что <code>isLoading</code> перекрывает уже показанные данные спиннером, и таблица моргает при каждом refetch.</p>

<h4>Решение</h4>
<p>Описать <strong>состояния как перечисление</strong>, а не набор флагов, и рендерить исчерпывающим разбором. Минимальный набор для запроса: <code>idle | loading | empty | error | success</code>. Отдельно, <strong>ортогонально</strong>, — фоновое обновление (<code>isFetching</code>): это не альтернатива success, а признак поверх него.</p>
<p>Практический приём — компонент-обёртка <code>&lt;QueryState&gt;</code>, принимающая результат запроса и слоты для каждого состояния. Тогда порядок веток задан один раз в одном месте, а не переписывается в каждом экране. Плюс это место, где живут единые скелетоны, пустые состояния с призывом к действию и кнопка «Повторить».</p>

<h4>Почему так лучше</h4>
<p>Невозможно забыть ветку: TypeScript покажет незакрытый случай, а обёртка не даст перепутать порядок. Пустое состояние перестаёт мелькать. Внешний вид загрузки и ошибок становится единообразным по всему приложению — это заметная часть воспринимаемого качества.</p>

<h4>Когда не надо</h4>
<p>Не сводите к одному enum то, что ортогонально: «есть данные» и «идёт фоновое обновление» должны существовать одновременно, иначе вернётся мигание. Не делайте универсальную обёртку с двадцатью пропсами — это тот же антипаттерн, что и раньше; ограничьтесь слотами. И помните, что для критичных экранов деление сложнее: «частично загружено» (первая страница есть, остальное грузится) — отдельное состояние, которое в наивную схему не влезает.</p>

<h4>Что спросят следом</h4>
<p><strong>Чем pending отличается от fetching?</strong> Pending — данных ещё нет ни разу; fetching — запрос идёт, но данные уже могут быть на экране. <strong>Как избежать мигания скелетона на быстром ответе?</strong> Показывать его с задержкой 200–300 мс и держать минимум 300–400 мс, чтобы не было вспышки.</p>`,
    code: `// было: лесенка флагов, порядок веток — негласный контракт
function Orders() {
  const { data, isLoading, error } = useOrders();
  if (!data?.length) return <Empty />;      // сработает раньше загрузки -> вспышка
  if (isLoading) return <Spinner />;
  if (error) return <ErrorBox />;
  return <Table rows={data} />;
}

// стало: одно перечисление + ортогональный признак фонового обновления
function toState(q) {
  if (q.error) return { kind: 'error', error: q.error };
  if (q.isPending) return { kind: 'loading' };
  if (!q.data?.length) return { kind: 'empty' };
  return { kind: 'success', data: q.data };
}

function QueryState({ query, loading, empty, error, children }) {
  const s = toState(query);
  switch (s.kind) {
    case 'loading': return loading;
    case 'error':   return error(s.error, query.refetch);
    case 'empty':   return empty;
    case 'success': return children(s.data, query.isFetching);  // fetching поверх данных
  }
}

<QueryState
  query={ordersQuery}
  loading={<TableSkeleton rows={8} />}
  empty={<Empty action={<Button onClick={create}>Создать заказ</Button>} />}
  error={(e, retry) => <ErrorBox error={e} onRetry={retry} />}
>
  {(rows, isRefreshing) => <Table rows={rows} dimmed={isRefreshing} />}
</QueryState>`,
    tip: 'Разделите pending и fetching: первое — данных ещё не было, второе — обновляем уже показанное. Именно смешение этих двух даёт таблицу, которая моргает спиннером на каждом фоновом refetch.' },

  { id: 'pt35',
    q: 'На странице заказа три вложенных компонента, каждый грузит своё: заказ, потом клиента, потом его адреса. Экран собирается 1.8 секунды. Как ускорить?',
    a: `<h4>В чём боль</h4>
<p>Водопад запросов. Дочерний компонент не начинает загрузку, пока не отрендерился, а он не отрендерится, пока родитель не получил свои данные. Задержки складываются: 600 + 600 + 600 мс, хотя сами запросы быстрые. Особенно больно на мобильной сети, где RTT 200–300 мс: цена в задержках, а не в объёме данных.</p>
<p>Второй, менее заметный источник — водопад <strong>ресурсов</strong>: чанк маршрута грузится, после парсинга обнаруживается запрос, который стартует только тогда.</p>

<h4>Решение</h4>
<p>Ломать зависимость «рендер → запрос» по нескольким направлениям.</p>
<ul>
<li><strong>Параллелить независимое</strong>: если запросы не зависят друг от друга по данным, запускать их одновременно (<code>Promise.all</code>, <code>useQueries</code>), а не по цепочке.</li>
<li><strong>Поднимать старт запроса выше рендера</strong>: загрузчики маршрута (React Router <code>loader</code>, Next.js), которые стартуют одновременно с загрузкой кода.</li>
<li><strong>Схлопывать на бэкенде</strong>: один эндпоинт, отдающий заказ вместе с клиентом и адресами, или GraphQL-запрос — один RTT вместо трёх. Часто это самое эффективное решение.</li>
<li><strong>Стримить</strong>: RSC плюс <code>Suspense</code> — сервер отдаёт готовые части по мере готовности, каркас появляется сразу.</li>
<li><strong>Предзагружать</strong> данные и код по hover на ссылке.</li>
</ul>

<h4>Почему так лучше</h4>
<p>Время экрана перестаёт быть суммой задержек и становится максимумом из них. Пользователь видит каркас сразу, а не белый экран на две секунды. Префетч по hover часто убирает ожидание полностью — 200–300 мс наведения хватает.</p>

<h4>Когда не надо</h4>
<p>Параллелить нельзя то, что действительно зависимо: адреса нельзя запросить, не зная id клиента. Тогда либо схлопывать на сервере, либо возвращать нужный id раньше. Схлопывание в один эндпоинт имеет цену: ответ становится тяжелее, кешируется хуже (одно поле протухло — протух весь ответ), и растёт связанность фронта с бэком. Агрессивный префетч по hover на списке из ста ссылок сожжёт трафик — ограничивайте частоту и учитывайте <code>navigator.connection.saveData</code>.</p>

<h4>Что спросят следом</h4>
<p><strong>Как найти водопад?</strong> Вкладка Network, режим waterfall: ступеньки вместо параллельных полос. <strong>Что делать, если запросы зависимы по существу?</strong> Один серверный эндпоинт или стриминг: часть данных показываем сразу, остальное догружаем под <code>Suspense</code>.</p>`,
    code: `// было: ступеньки — каждый уровень ждёт предыдущий
function OrderPage({ id }) {
  const { data: order } = useQuery(['order', id], () => api.getOrder(id));
  if (!order) return <Spinner />;
  return <Customer id={order.customerId} />;         // старт только после заказа
}
function Customer({ id }) {
  const { data: customer } = useQuery(['customer', id], () => api.getCustomer(id));
  if (!customer) return <Spinner />;
  return <Addresses ids={customer.addressIds} />;    // и ещё один круг
}

// стало (1): независимое — параллельно
const [order, stats] = useQueries({ queries: [
  { queryKey: ['order', id],  queryFn: () => api.getOrder(id) },
  { queryKey: ['stats', id],  queryFn: () => api.getOrderStats(id) },
]});

// стало (2): старт запросов до рендера — в загрузчике маршрута
export async function loader({ params }) {
  const order = await api.getOrder(params.id);
  const [customer, addresses] = await Promise.all([   // зависимое от order, но между собой параллельное
    api.getCustomer(order.customerId),
    api.getAddresses(order.customerId),
  ]);
  return { order, customer, addresses };
}

// стало (3): один RTT вместо трёх
const data = await api.getOrderPage(id);   // order + customer + addresses

// префетч по наведению — к клику данные уже есть
<Link to={'/orders/' + id} onMouseEnter={() => qc.prefetchQuery({ queryKey: ['order', id], queryFn: () => api.getOrder(id) })} />`,
    tip: 'Отделите водопад данных от водопада ресурсов: второй лечится загрузчиками маршрута и префетчем кода, первый — параллелизацией или схлопыванием эндпоинтов. Их часто путают и лечат не тем.' },

  { id: 'pt36',
    q: 'Правило расчёта скидки живёт в трёх компонентах: в карточке товара, в корзине и в чекауте. В двух местах его поправили, в третьем забыли. Как это лечить?',
    a: `<h4>В чём боль</h4>
<p>Бизнес-правило размазано по слою представления. Формально это дублирование, но вреднее другое: правило нельзя <strong>найти</strong>. Оно не имеет имени, живёт внутри JSX, и новый разработчик узнаёт о нём, только когда ломает. Тестируется оно тоже только через рендер, поэтому проверять граничные случаи дорого и никто этого не делает.</p>

<h4>Решение</h4>
<p>Вынести правило в отдельный модуль как <strong>чистую функцию с именем из языка предметной области</strong>: <code>calcDiscount(cart, user, promo)</code>. Компоненты вызывают её и рисуют результат. Это простейшая форма разделения на слои: домен не знает про React, представление не знает про правила.</p>
<p>Дальше по мере роста: правила группируются в модуль фичи (<code>features/pricing/</code>), появляется явный тип результата (не число, а <code>{ amount, reason, appliedRules }</code>) — это сразу закрывает вопрос «почему такая цена» в поддержке. Хук (<code>usePricing</code>) остаётся тонкой обёрткой над чистой функцией, чтобы её можно было вызывать и вне React — в воркере, в тестах, на сервере.</p>

<h4>Почему так лучше</h4>
<p>Правка в одном месте доходит до всех экранов. Тесты становятся быстрыми и подробными: таблица кейсов вместо рендера. Правило получает имя, поэтому его можно обсуждать с продуктом и найти поиском. Побочный эффект — код компонентов сокращается вдвое и читается как разметка.</p>

<h4>Когда не надо</h4>
<p>Не выносите ради принципа то, что используется один раз и не является правилом: три строки форматирования в одном компоненте лучше оставить на месте — вынос в <code>utils</code> только добавит прыжок между файлами. Опасна и преждевременная унификация: два похожих правила из разных доменов, слитые в одну функцию с флагом <code>isCheckout</code>, — это не переиспользование, а связанность; они разойдутся, и флагов станет пять.</p>
<p>И следите, чтобы «вынести логику» не превратилось в свалку <code>utils/helpers.js</code> на тысячу строк: модули называются по домену, а не по признаку «это не компонент».</p>

<h4>Что спросят следом</h4>
<p><strong>Куда класть — utils или features?</strong> В модуль фичи; <code>utils</code> — только для универсального, не знающего про домен (форматирование даты, кламп числа). <strong>Как проверить, что граница держится?</strong> Доменный модуль не импортирует React — это проверяется линтером.</p>`,
    code: `// было: правило внутри разметки, в трёх местах
function ProductCard({ product, user }) {
  const price = user.isPremium ? product.price * 0.9 : product.price;   // копия 1
  return <Money value={price} />;
}
function Cart({ items, user }) {
  const total = items.reduce((s, i) =>
    s + (user.isPremium ? i.price * 0.9 : i.price), 0);                 // копия 2, уже с багом
  return <Money value={total} />;
}
// в чекауте — копия 3, где скидку поправили на 15%

// стало: правило — именованная чистая функция домена
// features/pricing/calcPrice.ts   (не импортирует React)
export function calcPrice(item, user, promo) {
  const rules = [];
  let amount = item.price;
  if (user.isPremium) { amount *= 0.85; rules.push('premium'); }
  if (promo?.isValid) { amount -= promo.discount; rules.push('promo:' + promo.code); }
  return { amount: Math.max(0, round2(amount)), appliedRules: rules };
}

// компоненты только рисуют
function ProductCard({ product, user, promo }) {
  const { amount } = calcPrice(product, user, promo);
  return <Money value={amount} />;
}

// тесты — таблицей, без рендера
test.each([
  [{ price: 100 }, { isPremium: true },  null, 85],
  [{ price: 100 }, { isPremium: false }, null, 100],
])('calcPrice', (item, user, promo, expected) => {
  expect(calcPrice(item, user, promo).amount).toBe(expected);
});`,
    tip: 'Возвращайте не голое число, а результат с причинами (appliedRules). Это превращает «почему такая цена» из расследования в чтение объекта — и на собеседовании показывает, что вы думали про поддержку.' },

  { id: 'pt37',
    q: 'Сборка иногда падает с "Cannot read properties of undefined", а иногда работает. Оказалось — циклический импорт между двумя модулями. Как такое чинить и не допускать?',
    a: `<h4>В чём боль</h4>
<p>Модуль A импортирует B, B импортирует A. В ESM это не ошибка: связывание разрешает ссылки заранее, но <strong>инициализация идёт по порядку обхода</strong>. Тот модуль, который начал выполняться первым, на момент обращения к другому видит его экспорт ещё неинициализированным — отсюда <code>undefined</code> вместо функции или ошибка временной мёртвой зоны для <code>const</code>. Поведение зависит от точки входа, поэтому тест падает, а приложение работает — или наоборот. Бандлеры добавляют своих нюансов, а после перестановки импорта проблема «уходит» и возвращается через месяц.</p>

<h4>Решение</h4>
<p>Цикл — это симптом неправильных границ, а не проблема импорта. Лечится структурно:</p>
<ol>
<li><strong>Выделить общее вниз</strong>: то, что нужно обоим (типы, константы, чистые функции), вынести в третий модуль, от которого зависят оба. Цикл исчезает.</li>
<li><strong>Развернуть зависимость</strong>: если A вызывает B, а B — колбэк из A, передавайте колбэк параметром (инверсия зависимости) вместо импорта.</li>
<li><strong>Разделить тип и значение</strong>: <code>import type</code> стирается при компиляции и цикла не создаёт.</li>
<li><strong>Проверять автоматически</strong>: <code>import/no-cycle</code> в ESLint или <code>madge --circular</code> в CI — это единственный способ не допускать новые.</li>
</ol>
<p>Отдельная частая причина — <strong>барельные файлы</strong>: <code>a.ts</code> импортирует из <code>index.ts</code> своей же папки, а тот реэкспортирует <code>a.ts</code>. Внутри модуля всегда импортируйте по прямому пути.</p>

<h4>Почему так лучше</h4>
<p>Ациклический граф зависимостей означает, что порядок инициализации детерминирован, а модули можно понимать по одному. Побочно улучшается tree-shaking и ускоряется сборка.</p>

<h4>Когда не надо</h4>
<p>Ленивый импорт внутри функции (<code>await import()</code> или <code>require</code> в теле) разрывает цикл технически, но прячет проблему проектирования: граф остаётся запутанным, а отладка усложняется. Как временная мера в чужом легаси — приемлемо, как решение — нет. Также не стоит дробить модули до атомов ради формального отсутствия циклов: сотня файлов по десять строк читается хуже.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему в CommonJS цикл ведёт себя иначе?</strong> Там <code>require</code> возвращает частично заполненный <code>module.exports</code> — получаете объект без нужных полей, а не TDZ-ошибку. <strong>Как найти цикл быстро?</strong> <code>madge --circular src</code> или граф в анализаторе бандла.</p>`,
    code: `// было: цикл через общую константу и вызов
// user.ts
import { formatOrder } from './order';
export const ROLE = 'admin';
export const describeUser = u => u.name + ' / ' + formatOrder(u.lastOrder);

// order.ts
import { ROLE } from './user';
export const formatOrder = o => (o.role === ROLE ? '[admin] ' : '') + o.number;
// в зависимости от точки входа ROLE может быть undefined на момент вызова

// стало: общее вынесено вниз, цикла нет
// shared/roles.ts
export const ROLE = 'admin';

// order.ts
import { ROLE } from '../shared/roles';
export const formatOrder = o => (o.role === ROLE ? '[admin] ' : '') + o.number;

// user.ts
import { formatOrder } from './order';
export const describeUser = u => u.name + ' / ' + formatOrder(u.lastOrder);

// цикл только из-за типа — разрывается import type
import type { Order } from './order';   // стирается компилятором

// защита в CI
// .eslintrc: 'import/no-cycle': ['error', { maxDepth: Infinity }]
// package.json: "lint:cycles": "madge --circular --extensions ts,tsx src"`,
    tip: 'Скажите, что цикл — это симптом границ, а не проблема сборщика: правильный ответ начинается с «вынести общее в третий модуль», а не с «сделать ленивый импорт».' },

  { id: 'pt38',
    q: 'В проекте компонент импортирует функцию из API-слоя, API-слой импортирует хелпер из компонентов, а стор тянет всё подряд. Как навести порядок в зависимостях?',
    a: `<h4>В чём боль</h4>
<p>Граф зависимостей — паутина. Последствия ощутимые: нельзя вынести фичу в отдельный пакет, потому что она тянет половину проекта; изменение в UI-хелпере ломает API-слой; тесты требуют поднимать всё окружение; циклы возникают сами собой. Главное — <strong>исчезает предсказуемость радиуса изменения</strong>: непонятно, что сломается от правки.</p>

<h4>Решение</h4>
<p>Ввести <strong>слои и правило зависимостей</strong>: зависимости направлены в одну сторону, обычно от периферии к ядру. Типичное разбиение (FSD и близкие подходы): <code>app → pages → widgets → features → entities → shared</code>. Правила: слой импортирует только нижележащие; внутри слоя модули не импортируют друг друга напрямую (горизонтальные связи — через слой выше); <code>shared</code> не знает про домен.</p>
<p>Если нужна связь снизу вверх (доменному сервису нужен способ показать тост), применяется <strong>инверсия зависимости</strong>: нижний слой объявляет интерфейс, верхний его реализует и внедряет.</p>
<p>Договорённость обязана быть <strong>исполняемой</strong>: <code>eslint-plugin-boundaries</code> или <code>import/no-restricted-paths</code>, проверка в CI. Правило, которое проверяет только ревьюер, перестаёт работать через два спринта.</p>

<h4>Почему так лучше</h4>
<p>Радиус изменения становится известен заранее: правка в <code>features</code> не может сломать <code>entities</code>. Фичу можно удалить или вынести целиком. Новый человек ориентируется по структуре, а не по памяти команды.</p>

<h4>Когда не надо</h4>
<p>Для проекта на двадцать компонентов слои — это бюрократия: больше папок, чем кода, и постоянные споры «это entity или feature». Не переносите чужую нарезку буквально: важна не аббревиатура методологии, а два свойства — однонаправленность и явная публичная граница модуля. Плохая нарезка хуже её отсутствия: если границы проведены не по швам домена, вы получите постоянные «протечки» и обходные импорты. И миграцию делают постепенно, запрещая новые нарушения линтером, а не переписывая всё за спринт.</p>

<h4>Что спросят следом</h4>
<p><strong>Что делать, если фиче нужна другая фича?</strong> Поднять взаимодействие на уровень выше (в <code>widget</code>/<code>page</code>) или вынести общее в <code>entities</code>. <strong>Как мигрировать легаси?</strong> Включить правило в режиме warning, зафиксировать список исключений и запрещать его рост в CI.</p>`,
    code: `// было: импорты в обе стороны
// api/orders.ts
import { formatMoney } from '../components/Money';   // API тянет UI
// components/OrderList.tsx
import { db } from '../api/db';                      // компонент лезет в БД-слой

// стало: однонаправленные слои + публичные границы
src/
  app/        // провайдеры, роутер
  pages/      // сборка экранов
  widgets/    // самостоятельные блоки
  features/   // пользовательские сценарии (создать заказ)
  entities/   // домен (order, user) + их UI-представления
  shared/     // ui-kit, lib, api-клиент — без знания о домене

// правило проверяется линтером, а не договорённостью
// .eslintrc
'import/no-restricted-paths': ['error', { zones: [
  { target: './src/shared',   from: './src/features' },   // вниз нельзя
  { target: './src/shared',   from: './src/entities' },
  { target: './src/entities', from: './src/features' },
]}],

// связь снизу вверх — через интерфейс, а не импорт
// entities/order/model.ts
export function createOrder(dto, deps: { notify: (m: string) => void }) {
  /* ... */ deps.notify('Заказ создан');
}
// features/checkout/ui.tsx
createOrder(dto, { notify: toast.success });   // реализацию внедряет верхний слой`,
    tip: 'Главное — не название методологии, а два свойства: зависимости однонаправленны и проверяются линтером в CI. Правило, которое контролирует только ревьюер, живёт два спринта.' },

  { id: 'pt39',
    q: 'Фича раскатывается постепенно, и по коду расползлись проверки if (flags.newCheckout). Их уже сорок, часть относится к фичам, выкаченным год назад. Как с этим жить?',
    a: `<h4>В чём боль</h4>
<p>Флаги — это ветвление, размазанное по кодовой базе. Каждый добавляет два пути исполнения, и они перемножаются: три флага — восемь комбинаций, из которых тестируется одна. Мёртвые флаги (фича давно раскатана на 100%) страшно удалять, потому что непонятно, какая ветка живая. Флаги читаются напрямую из глобального объекта, поэтому найти все места нельзя без грепа, а типов у них нет — опечатка в имени молча даёт <code>undefined</code> и выключенную фичу.</p>

<h4>Решение</h4>
<p>Три правила.</p>
<ol>
<li><strong>Типизированный реестр</strong>: все флаги объявлены в одном месте с типом, описанием, владельцем и датой удаления. Обращение — только через хук/функцию, не через глобальный объект. Опечатка становится ошибкой компиляции.</li>
<li><strong>Ветвление на границе, а не внутри</strong>: вместо десяти <code>if</code> по всему компоненту — одна развилка наверху, выбирающая реализацию (компонент, стратегию, хук). Тогда удаление флага — это удаление одной ветки и одного файла.</li>
<li><strong>Срок жизни</strong>: у флага раскатки есть дата истечения; после неё тест или линт-правило начинает падать. Это единственное, что реально заставляет чистить.</li>
</ol>
<p>Важно различать типы флагов: релизные (временные, удаляются), операционные (kill switch, живут долго), пермишены и эксперименты — у них разный жизненный цикл и разные правила.</p>

<h4>Почему так лучше</h4>
<p>Число живых комбинаций остаётся управляемым, а удаление флага — механическая операция. Реестр даёт ответ на вопрос «что сейчас включено», который иначе выясняется чтением кода.</p>

<h4>Когда не надо</h4>
<p>Флаг не заменяет ветку в git для больших рефакторингов: держать два несовместимых пути месяцами дороже, чем сделать миграцию. Не заворачивайте во флаг изменения схемы данных — откатить UI легко, данные нет. И помните про производительность: если под флагом лежит тяжёлый код, обе ветки попадут в бандл — там нужен динамический импорт.</p>

<h4>Что спросят следом</h4>
<p><strong>Где вычислять флаг — на сервере или клиенте?</strong> Значение должно приходить с сервера вместе с первым ответом, иначе при SSR получите рассинхрон и мигание. <strong>Как тестировать?</strong> Тесты параметризуются по флагу для критичных путей; для остальных фиксируется целевое значение.</p>`,
    code: `// было: строковые ключи и ветвление внутри компонентов
if (window.flags['newCheckout']) { /* ... */ }        // опечатка = тихо выключено
function Checkout() {
  const isNew = window.flags['newCheckout'];
  return (
    <>
      {isNew ? <NewHeader /> : <OldHeader />}
      {isNew && <PromoBanner />}
      <Total mode={isNew ? 'v2' : 'v1'} />            {/* ещё три развилки ниже */}
    </>
  );
}

// стало: реестр с типами и сроком жизни
export const FLAGS = {
  newCheckout: { kind: 'release', owner: 'payments', expiresAt: '2026-10-01' },
  killPromo:   { kind: 'ops',     owner: 'growth',   expiresAt: null },
} as const;
export type FlagName = keyof typeof FLAGS;
export const useFlag = (name: FlagName) => useContext(FlagsCtx)[name] ?? false;

// одна развилка на границе вместо десяти внутри
const CheckoutV2 = React.lazy(() => import('./CheckoutV2'));   // тяжёлое — динамически

function CheckoutRoute() {
  return useFlag('newCheckout')
    ? <Suspense fallback={<Skeleton />}><CheckoutV2 /></Suspense>
    : <CheckoutV1 />;
}

// тест, который заставит убрать протухший флаг
test('нет просроченных флагов', () => {
  const stale = Object.entries(FLAGS)
    .filter(([, f]) => f.expiresAt && new Date(f.expiresAt) < new Date());
  expect(stale.map(([n]) => n)).toEqual([]);
});`,
    tip: 'Ключевой приём — одна развилка на границе вместо десяти внутри: тогда удаление флага становится удалением файла, а не археологией. И тест на просроченные флаги — единственное, что реально заставляет чистить.' },

  { id: 'pt40',
    q: 'Импортируем одну иконку из index.ts дизайн-системы, а в бандл приезжает вся библиотека и время холодного старта dev-сервера выросло до минуты. Что происходит?',
    a: `<h4>В чём боль</h4>
<p>Барельный файл — <code>index.ts</code>, реэкспортирующий всё содержимое папки. Импорт одного символа заставляет сборщик обработать <strong>весь</strong> граф модулей, на который ссылается барель. Последствия: медленный dev-сервер и HMR (Vite прогружает сотни модулей на каждый импорт), медленные тесты (Jest не умеет tree-shaking и исполняет все реэкспорты — вместе с их побочными эффектами), раздутый бандл, если хоть один модуль в цепочке имеет побочный эффект или не помечен как <code>sideEffects: false</code>.</p>
<p>Дополнительно барели порождают циклы: файл внутри папки импортирует из собственного <code>index.ts</code>, который реэкспортирует его же.</p>

<h4>Решение</h4>
<p>Различать <strong>публичную границу</strong> и <strong>внутренние импорты</strong>. Барель уместен как узкий публичный API модуля (пакета, фичи), где он перечисляет 5–15 явных экспортов. Внутри модуля и между соседними файлами импорт идёт по прямому пути. Для крупных библиотек — точечные импорты (<code>lodash-es/debounce</code>, <code>@mui/material/Button</code>) или встроенные средства сборщика (<code>optimizePackageImports</code> в Next.js, <code>barrel-optimizer</code>-плагины).</p>
<p>Обязательное дополнение: <code>"sideEffects": false</code> в <code>package.json</code> (или список файлов с эффектами) — без этого сборщик обязан сохранять все модули на всякий случай.</p>

<h4>Почему так лучше</h4>
<p>Время старта и HMR падают кратно — это самая ощутимая для команды метрика. Тесты перестают тянуть чужие модули. Бандл содержит только использованное. Циклы через <code>index.ts</code> исчезают как класс.</p>

<h4>Когда не надо</h4>
<p>Полный отказ от барелей — тоже крайность: публичная граница модуля нужна, иначе потребители начнут импортировать внутренности и рефакторинг станет невозможным. Хороший компромисс: один барель на модуль верхнего уровня, явные экспорты вместо <code>export *</code>, никаких вложенных барелей и никаких импортов из своего барели изнутри. Правило проверяется линтером.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему tree-shaking не всегда спасает?</strong> Он работает на этапе продакшн-сборки и требует ESM без побочных эффектов; в dev-режиме и в тестах модули исполняются целиком. <strong>Чем плох export *?</strong> Он скрывает состав API, ломает автодополнение и мешает сборщику доказать, что символ не используется.</p>`,
    code: `// было: барель на всё, импорт тянет граф целиком
// ui/index.ts
export * from './Button';
export * from './Table';      // тянет react-table
export * from './Chart';      // тянет d3
export * from './icons';      // 400 иконок

import { IconCheck } from '@/ui';   // приехали d3 и react-table

// стало: точечный импорт по прямому пути
import { IconCheck } from '@/ui/icons/IconCheck';

// а барель остаётся публичной границей модуля — с явным списком
// features/checkout/index.ts
export { CheckoutPage } from './ui/CheckoutPage';
export { useCheckout } from './model/useCheckout';
export type { CheckoutState } from './model/types';
// внутри features/checkout импорты идут по прямым путям, не через этот index

// package.json — без этого сборщик не имеет права выбрасывать модули
{ "sideEffects": ["*.css", "./src/polyfills.ts"] }

// запрет импорта внутренностей чужого модуля
'import/no-internal-modules': ['error', { allow: ['@/ui/icons/*'] }]`,
    tip: 'Сформулируйте разделение: барель — это публичная граница модуля, а не способ сократить путь. Импорт из собственного index внутри модуля — источник и циклов, и медленного HMR одновременно.' },

  { id: 'pt41',
    q: 'Три приложения копируют одни и те же компоненты через copy-paste. Предлагают вынести в отдельный пакет. Когда это действительно оправдано?',
    a: `<h4>В чём боль</h4>
<p>Копипаста между приложениями расходится: исправление бага доезжает в одно из трёх, стили дрейфуют, поведение отличается. Но и поспешный вынос в пакет создаёт свою боль: чтобы поправить кнопку, нужно изменить пакет, поднять версию, опубликовать, обновить зависимость в трёх местах и прогнать три CI. Скорость разработки падает, и разработчики начинают обходить пакет копированием — круг замыкается.</p>

<h4>Решение</h4>
<p>Выносить, когда выполнены <strong>все три</strong> условия: 1) код действительно одинаков по смыслу, а не случайно похож; 2) у него есть владелец, готовый отвечать за обратную совместимость; 3) он стабилизировался — не меняется каждую неделю. Правило трёх: до третьего использования дублируйте осознанно, разница между копиями покажет настоящую границу абстракции.</p>
<p>Отдельно решается <strong>способ</strong>. Монорепо (pnpm workspaces, Turborepo, Nx) даёт переиспользование без публикации: изменение видно потребителям сразу, атомарный коммит меняет пакет и всех пользователей. Отдельный репозиторий с npm-публикацией нужен, когда потребители внешние или релизный цикл обязан быть независимым — цена в том, что появляется версионирование, changelog и окно рассинхрона.</p>

<h4>Почему так лучше</h4>
<p>Условия проверяемы, поэтому решение перестаёт быть спором о вкусах. Монорепо снимает основную издержку раннего выноса — цикл публикации, — оставляя выигрыш от единого источника.</p>

<h4>Когда не надо</h4>
<p>Не выносите ради «переиспользования в будущем» — это самый частый источник абстракций, которые никому не подошли. Не выносите то, что зависит от домена конкретного приложения: пакет обрастёт флагами <code>variant="appA"</code>. Не выносите нестабильное: каждая правка станет мажорной версией. И помните про издержки, которые редко считают: своя сборка, свои типы, своя документация, свой CI, обучение потребителей и ответственность за поломки в трёх продуктах одновременно.</p>

<h4>Что спросят следом</h4>
<p><strong>Как версионировать?</strong> Semver плюс changesets; ломающие изменения — с периодом депрекации и кодмодом, а не «обновитесь и почините». <strong>Как понять, что абстракция плохая?</strong> Если в пакет добавляют пропсы с названиями приложений-потребителей — граница проведена неверно.</p>`,
    code: `// было: копия в трёх приложениях, поправлено в одной
apps/admin/src/components/DataTable.tsx     // v1, багфикс есть
apps/portal/src/components/DataTable.tsx    // v1, багфикса нет
apps/mobile/src/components/DataTable.tsx    // разошлась по стилям

// стало: монорепо, пакет без публикации
packages/ui/src/DataTable.tsx
packages/ui/package.json     { "name": "@acme/ui", "main": "./src/index.ts" }
apps/admin/package.json      { "dependencies": { "@acme/ui": "workspace:*" } }
// правка в packages/ui видна всем сразу, один коммит меняет пакет и потребителей

// признак плохой границы — доменные пропсы просачиваются в пакет
<DataTable variant="admin" showPortalFooter hideMobileColumns />   // так нельзя

// правильно: пакет знает про механику, приложение — про домен
<DataTable
  columns={adminColumns}
  renderFooter={() => <AdminFooter />}
  onRowClick={goToUser}
/>`,
    tip: 'Три условия для выноса: одинаково по смыслу, есть владелец, код стабилизировался. И сразу отделите вопрос «выносить ли» от «публиковать ли»: монорепо даёт переиспользование без цикла публикации.' },

  { id: 'pt42',
    q: 'Один виджет на дашборде упал с TypeError — и весь экран стал белым. Пользователь потерял несохранённую работу. Как не допускать такого?',
    a: `<h4>В чём боль</h4>
<p>С React 16 необработанная ошибка при рендере размонтирует всё дерево целиком: разработчики сочли, что показывать сломанный интерфейс опаснее, чем ничего. На практике это значит, что <code>undefined.map</code> в одном виджете уносит весь дашборд, включая несохранённую форму в соседней колонке. Пользователь видит белый экран без объяснений и без пути назад.</p>

<h4>Решение</h4>
<p><strong>Error boundary</strong> — компонент со <code>static getDerivedStateFromError</code> (переключает состояние на запасной вид) и <code>componentDidCatch</code> (отправляет ошибку в мониторинг). Ключевой вопрос не «как написать», а <strong>где ставить</strong>: гранулярно, по границам независимых блоков. Корневой — чтобы не было белого экрана вообще; на уровне маршрута — чтобы шапка и навигация выжили; вокруг каждого виджета — чтобы падение одного не уносило соседей.</p>
<p>Обязательные детали: возможность восстановления (кнопка «Повторить», сбрасывающая границу через <code>key</code> или <code>resetErrorBoundary</code>), отправка в Sentry с контекстом, и понятный текст вместо стектрейса.</p>

<h4>Почему так лучше</h4>
<p>Радиус отказа ограничен блоком. Пользователь сохраняет доступ к остальному интерфейсу и своим данным. Команда узнаёт об ошибке из мониторинга, а не из тикета «всё сломалось».</p>

<h4>Когда не надо</h4>
<p>Границы <strong>не ловят</strong> ошибки в обработчиках событий, в асинхронном коде, в <code>setTimeout</code>, при SSR и ошибки самой границы — это ловится <code>try/catch</code>, состоянием запроса и глобальными слушателями <code>error</code>/<code>unhandledrejection</code>. Не превращайте границу в глушилку: молча показывать «что-то пошло не так» без логирования хуже падения, потому что проблема становится невидимой. И не ставьте границу на каждый <code>div</code> — экран, состоящий из десяти сообщений об ошибке, не лучше белого.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему нет хука useErrorBoundary?</strong> Механизм требует методов жизненного цикла класса; на практике берут <code>react-error-boundary</code>. <strong>Как показать ошибку из асинхронного кода?</strong> Пробросить её в состояние и бросить при рендере (<code>if (error) throw error</code>) — тогда её поймает ближайшая граница.</p>`,
    code: `// было: одна ошибка уносит всё приложение
<Dashboard>
  <SalesWidget />       {/* здесь undefined.map */}
  <OrdersWidget />      {/* и он тоже исчез */}
  <DraftForm />         {/* несохранённые данные потеряны */}
</Dashboard>

// стало: граница вокруг каждого независимого блока
class ErrorBoundary extends React.Component {
  state = { error: null };
  static getDerivedStateFromError(error) { return { error }; }
  componentDidCatch(error, info) {
    Sentry.captureException(error, { extra: { componentStack: info.componentStack, block: this.props.name } });
  }
  render() {
    if (this.state.error) {
      return this.props.fallback(() => this.setState({ error: null }));   // восстановление
    }
    return this.props.children;
  }
}

const Guarded = ({ name, children }) => (
  <ErrorBoundary
    name={name}
    fallback={retry => <WidgetError title={name} onRetry={retry} />}
  >{children}</ErrorBoundary>
);

<Dashboard>
  <Guarded name="Продажи"><SalesWidget /></Guarded>
  <Guarded name="Заказы"><OrdersWidget /></Guarded>
  <DraftForm />                     {/* выживет при падении соседей */}
</Dashboard>

// асинхронную ошибку граница не поймает — пробрасываем в рендер
const [asyncError, setAsyncError] = useState(null);
if (asyncError) throw asyncError;`,
    tip: 'Главное в ответе — размещение границ, а не код класса. И обязательно назовите, чего границы не ловят: обработчики событий, асинхронщину и SSR — это отсекает половину кандидатов.' },

  { id: 'pt43',
    q: 'Пользователь жалуется, что кнопка «Сохранить» не работает. В логах пусто, в Sentry ничего. В коде находим catch с пустым телом. Как выстроить работу с ошибками?',
    a: `<h4>В чём боль</h4>
<p>Тихие ошибки. Их источники однотипны: пустой <code>catch {}</code>; <code>catch</code>, который пишет <code>console.error</code> и на этом всё; <code>.catch(() =&gt; null)</code>, превращающий сбой в «пустые данные»; необработанный промис без <code>await</code> (fire-and-forget); <code>try</code>, обёрнутый вокруг слишком большого куска, где одна ошибка маскирует другую. Итог: пользователь думает, что сохранил, данных нет; в мониторинге чисто, поэтому проблема живёт месяцами и находится только по жалобам.</p>
<p>Отдельно опасен fallback «на всякий случай»: <code>const items = await load().catch(() =&gt; [])</code> — интерфейс покажет пустой список вместо ошибки, и пользователь решит, что данных нет.</p>

<h4>Решение</h4>
<p>Правило: у <strong>каждой</strong> ошибки есть адресат. Их ровно три — пользователь (понятное сообщение и путь дальше), мониторинг (запись с контекстом), разработчик (падение в тестах). Обрабатывать значит выбрать адресата, а не «поймать».</p>
<p>Практически: <code>catch</code> либо восстанавливает и логирует, либо пробрасывает выше; глушить можно только <strong>ожидаемые</strong> ошибки, названные явно (<code>AbortError</code>, 404 при опциональной загрузке). Глобальные ловушки <code>window.onerror</code> и <code>unhandledrejection</code> обязательны — они ловят то, что забыли. Мутации всегда сообщают о результате: успех — тост или изменение UI, ошибка — сообщение с повтором.</p>

<h4>Почему так лучше</h4>
<p>Проблемы становятся видимыми в момент возникновения, а не через месяц. Пользователь получает честную обратную связь и не теряет данные, думая, что всё сохранилось.</p>

<h4>Когда не надо</h4>
<p>Обратная крайность — показывать пользователю каждую ошибку: отменённый запрос, сбой отправки аналитики, ошибка префетча не должны выдавать модалку. Такие ошибки логируются, но не тревожат. Не заваливайте Sentry шумом: сетевые сбои и ошибки расширений браузера нужно фильтровать, иначе алерты перестанут читать — а это тот же тихий отказ, только на уровне процесса.</p>

<h4>Что спросят следом</h4>
<p><strong>Что писать пользователю?</strong> Что произошло, что делать дальше и идентификатор для поддержки; никаких стектрейсов и кодов вида <code>E_UNKNOWN</code>. <strong>Как ловить необработанные промисы?</strong> Слушатель <code>unhandledrejection</code>; в тестах — падать на нём, чтобы забытый <code>await</code> ловился до прода.</p>`,
    code: `// было: ошибка исчезает бесследно
async function save() {
  try {
    await api.save(form);
  } catch {}                          // пользователь думает, что сохранил
}
const items = await load().catch(() => []);   // «данных нет» вместо «сбой»
sendAnalytics(event);                 // промис без await и без catch

// стало: у каждой ошибки есть адресат
async function save() {
  try {
    await api.save(form);
    toast.success('Сохранено');                       // адресат: пользователь
  } catch (e) {
    if (e.name === 'AbortError') return;              // ожидаемое — глушим осознанно
    Sentry.captureException(e, { tags: { action: 'save-profile' }, extra: { formId } });
    toast.error('Не удалось сохранить', {
      description: 'Попробуйте ещё раз. Код обращения: ' + Sentry.lastEventId(),
      action: { label: 'Повторить', onClick: save },
    });
    throw e;                                          // выше по стеку тоже надо знать
  }
}

// нешумные ошибки: логируем, но не тревожим
void sendAnalytics(event).catch(e => Sentry.captureException(e, { level: 'warning' }));

// сеть безопасности
window.addEventListener('unhandledrejection', e => Sentry.captureException(e.reason));
window.addEventListener('error', e => Sentry.captureException(e.error));`,
    tip: 'Формулировка, которая закрывает тему: обработать ошибку — значит выбрать ей адресата (пользователь, мониторинг, тест), а не поставить catch. И назовите обратную крайность: модалка на каждый отменённый запрос — тоже плохо.' },

  { id: 'pt44',
    q: 'Раз в час бэкенд отдаёт 503 на пару секунд — и пользователи видят сломанный экран. Просто повторять запрос в цикле кажется плохой идеей. Как правильно?',
    a: `<h4>В чём боль</h4>
<p>Сеть и сервисы ненадёжны по своей природе: короткие всплески 5xx, обрывы мобильной сети, таймауты балансировщика. Клиент, который сдаётся с первой попытки, превращает секундный сбой в полноценную поломку для пользователя. Но наивный повтор в цикле — хуже: тысячи клиентов синхронно бьют по поднимающемуся сервису и роняют его снова. Это retry storm, классический способ превратить деградацию в отказ.</p>

<h4>Решение</h4>
<p>Повторы с <strong>экспоненциальной задержкой и джиттером</strong>: 1 с, 2 с, 4 с плюс случайная добавка, чтобы клиенты не синхронизировались. Ограничение по числу попыток и по общему бюджету времени.</p>
<p>Повторять можно не всё. Повторяем: сетевые ошибки, 502/503/504, 429 (уважая <code>Retry-After</code>). Не повторяем: 4xx — это ошибка запроса, повтор её не исправит. Особая осторожность с мутациями: повтор безопасен только при идемпотентности, то есть с ключом идемпотентности, который сервер распознаёт.</p>
<p>Второй уровень — <strong>деградация</strong>: если восстановиться не удалось, показать частичный результат вместо ошибки. Кешированные данные с пометкой «данные от 14:32», раздел без блока рекомендаций, режим только для чтения. Третий — <strong>circuit breaker</strong>: после серии отказов перестать долбить сервис на время.</p>

<h4>Почему так лучше</h4>
<p>Короткие сбои становятся невидимыми, а длинные — переносимыми: пользователь продолжает работать с частью функциональности. Сервер получает шанс подняться, а не добивается лавиной повторов.</p>

<h4>Когда не надо</h4>
<p>Не повторяйте неидемпотентные операции без ключа: «повторили платёж» — это списание дважды. Не растягивайте повторы там, где пользователь ждёт результата: три попытки по десять секунд ощущаются как зависание — лучше быстро показать ошибку с кнопкой «Повторить» и отдать управление человеку. Деградация тоже имеет границу: показывать устаревшие данные без явной пометки нельзя, для остатков на складе или баланса это прямой вред.</p>

<h4>Что спросят следом</h4>
<p><strong>Зачем джиттер?</strong> Без него все клиенты повторяют одновременно и создают синхронные всплески. <strong>Что такое ключ идемпотентности?</strong> Уникальный идентификатор операции в заголовке: сервер, увидев повтор, возвращает результат первой попытки, а не выполняет её заново.</p>`,
    code: `// было: одна попытка -> сломанный экран, либо цикл без пауз -> retry storm
const data = await fetch(url).then(r => r.json());
while (!ok) { await fetch(url); }

// стало: повторы с экспоненциальной задержкой и джиттером
const RETRIABLE = new Set([408, 429, 502, 503, 504]);

async function request(url, options = {}, attempt = 0) {
  try {
    const res = await fetch(url, options);
    if (RETRIABLE.has(res.status) && attempt < 3) {
      const after = Number(res.headers.get('Retry-After')) * 1000;
      const backoff = 1000 * Math.pow(2, attempt) + Math.random() * 300;  // джиттер
      await sleep(after || backoff);
      return request(url, options, attempt + 1);
    }
    if (!res.ok) throw new HttpError(res.status);      // 4xx повторять бессмысленно
    return res.json();
  } catch (e) {
    if (e instanceof TypeError && attempt < 3) {       // сетевой сбой
      await sleep(1000 * Math.pow(2, attempt) + Math.random() * 300);
      return request(url, options, attempt + 1);
    }
    throw e;
  }
}

// мутацию повторяем только с ключом идемпотентности
await request('/api/payments', {
  method: 'POST',
  headers: { 'Idempotency-Key': paymentAttemptId },    // сервер распознает повтор
  body: JSON.stringify(payload),
});

// деградация: лучше устаревшее с пометкой, чем пустой экран
const { data, isStale, dataUpdatedAt } = useQuery({ queryKey: ['rates'], queryFn: getRates, retry: 3 });
{isStale && <Notice>Данные от {formatTime(dataUpdatedAt)}, обновляем...</Notice>}`,
    tip: 'Три вещи, которые отличают сильный ответ: джиттер (иначе синхронный шторм), список кодов для повтора (4xx не повторяем) и ключ идемпотентности для мутаций. Без последнего повтор платежа списывает дважды.' },

  { id: 'pt45',
    q: 'Пользователи присылают скриншоты с вечным спиннером. Воспроизвести не получается: у нас всё грузится. Как проектировать загрузку, чтобы такого не было?',
    a: `<h4>В чём боль</h4>
<p>Состояние «загружается» включили, а гарантии выхода из него нет. Причины разные: запрос завис без ответа (мобильная сеть переключилась, соединение висит — <code>fetch</code> сам по себе таймаута <strong>не имеет</strong>); промис отвергнут, но <code>setLoading(false)</code> стоит только в ветке успеха; компонент размонтировался, а флаг остался; ошибка проглочена пустым <code>catch</code>. Внешне всё одинаково — вечный спиннер и ноль информации в логах, потому что «ничего не произошло» никто не логирует.</p>

<h4>Решение</h4>
<p>Сделать так, чтобы загрузка <strong>по построению</strong> имела конец.</p>
<ol>
<li><strong>Таймаут на каждый запрос</strong>: <code>AbortSignal.timeout(15000)</code> или гонка с таймером. Истёк — это ошибка, а не бесконечность.</li>
<li><strong>Сброс флага в finally</strong>, а не в ветке успеха. Ещё лучше — не держать флаг вручную: состояние запроса выводится из результата (<code>useQuery</code>) и не может залипнуть.</li>
<li><strong>Все ветки покрыты</strong>: <code>status</code>-автомат вместо булевых флагов не даёт остаться в <code>loading</code> без перехода.</li>
<li><strong>Наблюдаемость</strong>: метрика «доля запросов дольше N секунд» и логирование таймаутов — иначе проблема невидима.</li>
<li><strong>Выход для пользователя</strong>: через несколько секунд рядом со спиннером появляется «Долго грузится» с кнопками «Повторить» и «Отменить».</li>
</ol>

<h4>Почему так лучше</h4>
<p>Худший сценарий превращается из «вечный спиннер» в «понятная ошибка с кнопкой». Пользователь получает управление, а команда — сигнал в мониторинге. Проблема перестаёт быть невоспроизводимой, потому что таймаут логируется с контекстом.</p>

<h4>Когда не надо</h4>
<p>Единый жёсткий таймаут вреден: выгрузка большого отчёта или загрузка файла законно длятся минуты, и обрывать их на пятнадцатой секунде — вредительство. Таймаут задаётся по типу операции, а долгие задачи вообще проектируются иначе: сервер возвращает id задачи, клиент опрашивает статус и показывает прогресс. Также не показывайте спиннер на быстрых операциях: если ответ обычно приходит за 100 мс, вспышка спиннера ощущается как подтормаживание — лучше показать его с задержкой.</p>

<h4>Что спросят следом</h4>
<p><strong>Почему у fetch нет таймаута?</strong> Спецификация его не определяет; раньше делали через <code>Promise.race</code>, теперь есть <code>AbortSignal.timeout()</code>. <strong>Как отличить «долго» от «зависло»?</strong> Никак на клиенте — поэтому и нужен явный порог, после которого пользователю предлагают действие.</p>`,
    code: `// было: выход из загрузки не гарантирован
async function load() {
  setLoading(true);
  const res = await fetch(url);        // может висеть бесконечно
  setData(await res.json());
  setLoading(false);                   // не выполнится при ошибке
}

// стало: таймаут, finally и выход для пользователя
async function load() {
  setStatus('loading');
  try {
    const res = await fetch(url, { signal: AbortSignal.timeout(15_000) });
    if (!res.ok) throw new HttpError(res.status);
    setData(await res.json());
    setStatus('success');
  } catch (e) {
    const timedOut = e.name === 'TimeoutError';
    Sentry.captureException(e, { tags: { url, timedOut } });   // теперь это видно
    setStatus('error');
    setError(timedOut ? 'Сервер не ответил за 15 секунд' : 'Не удалось загрузить');
  }
  // никакого setLoading — статус выводится из перехода, залипнуть негде
}

// спиннер с задержкой + предложение действия
function Loading({ onRetry, onCancel }) {
  const slow = useDelayedFlag(4000);     // «долго грузится» через 4 с
  const visible = useDelayedFlag(200);   // не мигаем на быстрых ответах
  if (!visible) return null;
  return (
    <>
      <Spinner />
      {slow && <SlowNotice onRetry={onRetry} onCancel={onCancel} />}
    </>
  );
}`,
    tip: 'Ключевая фраза: у fetch нет таймаута по умолчанию — бесконечный спиннер чаще всего именно отсюда. И добавьте про метрику доли долгих запросов: без неё баг невоспроизводим и живёт годами.' },

];
