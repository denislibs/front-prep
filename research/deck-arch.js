const DECK_ARCH = [
  { id: 'arc1',
    q: 'Команда принесла свой компонент и хочет положить его в общую библиотеку. Как ты решаешь, что туда попадает, а что остаётся в продукте?',
    a: `<h4>Коротко</h4>
<p>В библиотеку попадает то, что уже понадобилось нескольким командам и чей контракт перестал меняться каждую неделю. Всё остальное живёт в продукте, пока не докажет право на общность. Критерий не эстетический, а экономический: сколько команд платят за отсутствие этого компонента и сколько заплатит библиотека за его поддержку.</p>

<h4>Как это работает</h4>
<p>Практическое правило — правило трёх. Первый случай пишется в продукте. Второй копируется в другой продукт осознанно, с пометкой в коде. На третьем мы уже видим, что общего между тремя реализациями, а что было случайным, и только тогда выносим. Вынести раньше — значит зафиксировать контракт по одному примеру и потом ломать его на каждом новом потребителе.</p>
<p>Дальше идёт проверка на доменность. Компонент годится в библиотеку, если у него в пропсах нет доменных типов, он не ходит в сеть и не знает про стор приложения, в нём нет продуктовых текстов и он полностью описывается токенами. Как только в пропсах появляется что-то вроде объекта заказа или счёта, компонент доменный: он принадлежит продукту, даже если внешне похож на карточку из соседнего приложения.</p>
<p>У меня в системе на 48 компонентов слоёв было три. Примитивы: кнопка, поле, чекбокс, типографика. Составные: селект, дейтпикер, таблица, модалка. Паттерны: форма с шапкой и футером, пустое состояние, лейаут страницы. Доменного слоя в библиотеке не было вообще — это отдельное правило, и оно спасало от превращения библиотеки в свалку.</p>
<p>Отдельный клапан — инкубатор. Пакет рядом с основным, куда кладут кандидатов: там разрешено ломать API без мажора, там нет гарантий поддержки, и оттуда компонент переезжает в основной пакет, когда у него появился второй-третий потребитель и стабилизировался контракт.</p>

<h4>Почему так</h4>
<p>Каждый компонент в общей библиотеке — это обязательство на годы: поддерживать, тестировать, доступность, миграции. Дублирование в двух продуктах стоит дешевле, чем абстракция, угаданная неправильно. Обратное движение почти невозможно: убрать компонент из библиотеки — мажорная версия и переговоры со всеми командами.</p>

<h4>Подводные камни</h4>
<ul>
<li>Вынос по внешнему сходству. Две карточки выглядят одинаково, но живут в разных сценариях, и через месяц у компонента девять пропов-флагов.</li>
<li>Компонент с одним потребителем в библиотеке. Он не переиспользуется, но тормозит релизы и попадает в бандл всем.</li>
<li>Отказ выносить очевидное: пять разных реализаций тултипа с пятью разными багами доступности.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Кто принимает решение?»</strong> — не библиотечная команда единолично: есть архитектурный комитет из представителей продуктов, но право вето на контракт у владельцев системы. <strong>«Что если продукт не хочет ждать?»</strong> — он пишет у себя и получает право на вынос позже; блокировать продуктовую скорость ради чистоты библиотеки нельзя.</p>`,
    code: `// Признак доменного компонента: доменный тип в пропсах и знание про сеть
// Не для библиотеки — знает про заказ и сам ходит за данными
function OrderCard({ order }) {
  const { data } = useQuery(['order', order.id], fetchOrder);
  return <Card title={order.title} status={data.status} />;
}

// Для библиотеки — не знает, что за сущность внутри, всё через пропсы и слоты
function Card({ title, status, actions, children }) {
  return (
    <Root>
      <Header>{title}{status}</Header>
      <Body>{children}</Body>
      {actions ? <Footer>{actions}</Footer> : null}
    </Root>
  );
}

// Инкубатор: отдельная точка входа с явным обещанием ломать API
// package.json -> "exports": { "./lab": "./dist/lab/index.js" }
import { DataGrid } from '@ds/components/lab'; // без гарантий semver`,
    tip: 'Сильный ответ добавляет обратную петлю: не только «что берём», но и «что выкидываем». Раз в квартал смотрим использование компонентов по коду всех продуктов и депрекейтим то, у чего меньше двух потребителей — иначе библиотека растёт только в одну сторону.' },

  { id: 'arc2',
    q: 'Как ты выкатываешь ломающее изменение в дизайн-системе и как заставляешь команды на него перейти?',
    a: `<h4>Коротко</h4>
<p>Ломающее изменение никогда не выкатывается одним мажором. Сначала новое API появляется рядом со старым в минорной версии, старое помечается устаревшим с рантайм-предупреждением и кодмодом, и только через квартал выходит мажор, который старое удаляет. Заставить перейти нельзя, можно сделать переход дешевле, чем не переходить.</p>

<h4>Как это работает</h4>
<p>Схема в четыре шага. Шаг первый: в миноре добавляем новый проп или компонент, старый продолжает работать. Шаг второй: старый путь начинает писать в консоль в дев-сборке — что устарело, чем заменить, ссылка на миграцию. Шаг третий: пишем кодмод на jscodeshift или ast-grep, который переписывает вызовы автоматически, и прогоняем его по репозиториям команд сами, отдавая пулл-реквесты. Шаг четвёртый: мажор удаляет старое, в чейнджлоге ссылка на кодмод.</p>
<p>Версионируем по semver честно: изменение дефолтного визуала — это мажор, даже если типы не поменялись. Команды ловят это глазами и на скриншотных тестах, а не компилятором, и молчаливая смена отступов ломает им вёрстку не хуже удалённого пропа.</p>
<p>Про принуждение. Работает не приказ, а три вещи. Первая — окно поддержки: предыдущий мажор чинится по багам ровно N месяцев, дальше только на новом. Вторая — видимость: дашборд, где по каждому продукту видно установленную версию и отставание, и он смотрится на общей встрече. Третья — обмен: вместе с мажором приезжает то, чего команды хотят — новая тема, доступность, размер бандла. Мажор без выгоды команды откладывают навсегда, и это рационально с их стороны.</p>

<h4>Почему так</h4>
<p>У библиотеки нет власти над чужими бэклогами. Единственный рычаг — стоимость: миграция за полчаса с автоматическим кодмодом проходит между делом, миграция на два дня ручной работы висит кварталами. Поэтому в дизайн-системе кодмод — не приятное дополнение, а часть определения готовности мажора.</p>

<h4>Подводные камни</h4>
<ul>
<li>Мажор, который меняет всё сразу. Его невозможно ни отревьюить, ни откатить частично, и он собирает саботаж.</li>
<li>Депрекейт без даты удаления. Старое API живёт годами и удваивает поверхность поддержки.</li>
<li>Предупреждения в проде: замедляют и шумят в логах, нужен флаг сборки.</li>
<li>Разные мажоры библиотеки в одном бандле через транзитивные зависимости — два набора стилей и удвоенный вес.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как публикуете?»</strong> — changesets: каждый пулл-реквест несёт файл с типом изменения, релиз собирается автоматически, чейнджлог пишется из этих файлов. <strong>«Что с канареечными релизами?»</strong> — тег next в реестре пакетов, одна пилотная команда живёт на нём и ловит проблемы за всех.</p>`,
    code: `// Депрекейт: новое рядом со старым, предупреждение один раз на компонент
const warned = new Set();

export function Button({ type, variant, ...rest }) {
  if (process.env.NODE_ENV !== 'production' && type && !warned.has('Button.type')) {
    warned.add('Button.type');
    console.warn(
      '[ds] Button: проп type устарел и будет удалён в 5.0. ' +
      'Используй variant. Кодмод: npx @ds/codemods button-type'
    );
  }
  return <Base variant={variant ?? mapLegacyType(type)} {...rest} />;
}

// Кодмод переписывает вызовы у потребителей
// npx jscodeshift -t button-type.js src/
module.exports = function (file, api) {
  const j = api.jscodeshift;
  return j(file.source)
    .findJSXElements('Button')
    .find(j.JSXAttribute, { name: { name: 'type' } })
    .forEach(function (p) { p.node.name.name = 'variant'; })
    .toSource();
};`,
    tip: 'Проговори про окно совместимости явно: два мажора одновременно поддерживать можно, три — нельзя. И назови антипаттерн — версия 0.x годами: она формально разрешает ломать что угодно в миноре, но на деле означает, что команды боятся обновляться вообще.' },

  { id: 'arc3',
    q: 'Команда просит компонент, который почти совпадает с существующим, но с другим поведением. Что ты сделаешь?',
    a: `<h4>Коротко</h4>
<p>Сначала выясняю, что именно отличается: внешний вид, поведение или сценарий целиком. Разный вид — вариант через токены. Разное наполнение — слот, а не проп. Разное поведение при том же визуале — выношу логику в хук и собираю второй компонент. Разный сценарий — это другой компонент, и попытка склеить их пропами закончится флагами.</p>

<h4>Как это работает</h4>
<p>Пошагово я задаю четыре вопроса. Первый: это про цвет, размер, плотность? Тогда добавляем вариант в закрытый список значений — не булев проп, а перечисление, потому что три булевых пропа дают восемь состояний, из которых валидны три.</p>
<p>Второй: это про то, что внутри? Тогда слот. Вместо пропа iconLeft, iconRight, badge и десятка их сочетаний компонент принимает children или именованные области, и потребитель кладёт туда что хочет. Композиция масштабируется, набор пропов — нет.</p>
<p>Третий: это про поведение — другая навигация с клавиатуры, другая логика выбора, другой момент закрытия? Тогда разделяю компонент на две части: хук с логикой и состоянием и разметку. Логика переиспользуется, разметка разная. Это же снимает большинство просьб «дайте доступ к внутренностям».</p>
<p>Четвёртый: если отличий больше двух и они не сводятся ни к одному из пунктов — это другой компонент. Общее у них выносится в базовый примитив, а два верхних слоя живут отдельно и спокойно расходятся.</p>
<p>Форк без разговора допустим ровно в одном случае: команда под дедлайном, отличие временное, форк живёт в продукте и помечен как долг с датой. Форк внутри библиотеки не допускается никогда — это два компонента, которые расходятся молча.</p>

<h4>Почему так</h4>
<p>Каждый новый проп умножает пространство состояний и число комбинаций, которые надо тестировать. Компонент с двенадцатью пропами невозможно ни задокументировать, ни отрефакторить: любое изменение потенциально ломает неизвестную комбинацию. Композиция и хуки переносят вариативность туда, где она принадлежит — в место использования.</p>

<h4>Подводные камни</h4>
<ul>
<li>Проп isSpecial или renderCustomFooter — верный признак, что нужен слот.</li>
<li>Проп-объект с переопределением стилей внутренних частей: делает всю верстку публичным API, и любой рефакторинг становится ломающим.</li>
<li>Отказ без альтернативы: команда просто форкнет молча, и через полгода у вас пять кнопок.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«А className и style наружу пробрасывать?»</strong> — да, на корень, и это единственная разрешённая точка: так команда чинит отступы в своём лейауте, не залезая внутрь. <strong>«Как понять, что пропов слишком много?»</strong> — если в сторибуке нельзя перечислить все осмысленные сочетания, компонент пора резать.</p>`,
    code: `// Плохо: булевы флаги, восемь состояний, из них валидны три
<Button primary large danger loading />

// Лучше: закрытые перечисления плюс слоты для содержимого
<Button variant="danger" size="lg" state="loading">
  <Button.Icon><TrashIcon /></Button.Icon>
  Удалить
</Button>

// Разное поведение при той же разметке: логика в хуке, вид отдельно
export function useCombobox({ items, onSelect, closeOnSelect = true }) {
  // навигация с клавиатуры, активный индекс, aria-атрибуты
  return { getInputProps, getListProps, getItemProps, isOpen, activeIndex };
}

// Два компонента поверх одного хука вместо одного с флагом multiple
function Select(props) { const api = useCombobox(props); /* ... */ }
function MultiSelect(props) { const api = useCombobox({ ...props, closeOnSelect: false }); }`,
    tip: 'Хороший ход — предложить измеримый порог заранее: если запросов на такое отличие пришло от двух и более команд, это вариант в системе; если запрос один и он про домен — это компонент в продукте. Так решение перестаёт быть вкусовым спором.' },

  { id: 'arc4',
    q: 'Расскажи про слои токенов. Почему без семантического слоя темизация разваливается?',
    a: `<h4>Коротко</h4>
<p>Токены живут в трёх слоях: примитивы (палитра и шкалы), семантика (роли — фон поверхности, цвет опасного действия, граница поля), компонентные (значения конкретного компонента). Компоненты ссылаются только на семантику. Без этого слоя тема — это подмена палитры, и любая тема, где роли распределены иначе, ломает контраст.</p>

<h4>Как это работает</h4>
<p>Первый слой — сырые значения: blue-500, gray-900, space-4, radius-2. Они ничего не значат, у них нет смысла, они не должны появляться в коде компонентов.</p>
<p>Второй слой — роли: surface-default, surface-raised, text-primary, text-muted, border-input, bg-danger, fg-on-danger. Каждая роль ссылается на примитив. Именно этот слой переопределяется темой: в тёмной surface-default указывает не на white, а на gray-950, а text-primary — не на gray-900, а на gray-50.</p>
<p>Третий слой — компонентные алиасы: button-danger-bg, input-border-focus. Нужен там, где компонент имеет право на собственное решение, отличное от общей роли. Слой опциональный, и злоупотребление им превращает систему в тысячу переменных.</p>
<p>Технически это CSS-переменные, объявленные на корне и переопределяемые на любом узле — тогда тема работает и на всю страницу, и на кусок интерфейса, и в SSR не нужен рантайм. Исходник — JSON или файлы в формате обмена токенами, из которого генерятся и CSS-переменные, и типы TypeScript, и артефакты для дизайнеров.</p>
<p>Почему разваливается без семантики: если кнопка написана как color blue-500, то в тёмной теме её надо перекрасить, а перекрасить нечего — переменная одна и общая. Появляется темизация по условию if theme dark, и она расползается по всем компонентам. Плюс исчезает главное — контрастная пара: роль знает не только цвет фона, но и цвет текста поверх, поэтому bg-danger всегда идёт с fg-on-danger, и контраст гарантирован в любой теме.</p>

<h4>Почему так</h4>
<p>Тема — это не другая палитра, а другое распределение ролей. В светлой теме приподнятая поверхность светлее фона, в тёмной — тоже светлее, но это уже другой конец шкалы. Инверсия палитры такое не выражает, а слой ролей выражает.</p>

<h4>Подводные камни</h4>
<ul>
<li>Роли, названные по внешнему виду: gray-light-bg вместо surface-muted. Через тему такое имя врёт.</li>
<li>Компонентный слой на каждый компонент — тысячи переменных, которые никто не переопределяет.</li>
<li>Тема в JS-контексте вместо CSS-переменных: на сервере тема неизвестна, и первый кадр приходит в чужих цветах.</li>
<li>Токены только для цвета. Отступы, радиусы, тени и толщина шрифта тоже роли, иначе плотный режим интерфейса не сделать.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как переключать тему без мигания?»</strong> — атрибут на html, выставляемый инлайновым скриптом до первой отрисовки, плюс media-запрос как значение по умолчанию. <strong>«Кто владеет токенами?»</strong> — дизайн, но имена ролей согласуются с фронтендом: имя роли — это API, и переименование роли равно ломающему изменению.</p>`,
    code: `/* Слой 1 — примитивы, в компонентах не используются */
:root {
  --blue-500: #2f6fed;
  --gray-50: #f7f8fa;
  --gray-950: #0e1116;
}

/* Слой 2 — роли, переопределяются темой */
:root {
  --surface-default: var(--gray-50);
  --text-primary: var(--gray-950);
  --bg-accent: var(--blue-500);
  --fg-on-accent: #fff;   /* контрастная пара едет вместе с фоном */
}
[data-theme='dark'] {
  --surface-default: var(--gray-950);
  --text-primary: var(--gray-50);
  --bg-accent: #5b8cff;
  --fg-on-accent: #0e1116;
}

/* Компонент знает только роли */
.button-accent {
  background: var(--bg-accent);
  color: var(--fg-on-accent);
}

/* Тема на куске интерфейса — то же переопределение на любом узле */
/* <section data-theme="dark"> ... </section> */`,
    tip: 'Назови проверку, которая ловит нарушение слоёв автоматически: линтер стилей запрещает примитивные токены и голые hex в коде компонентов. Без такого правила слой ролей размывается за пару спринтов, потому что срочная правка всегда быстрее делается напрямую.' },

  { id: 'arc5',
    q: 'Как принимать вклады в дизайн-систему от чужих команд и не потерять стандарт?',
    a: `<h4>Коротко</h4>
<p>Модель — открытый исходный код внутри компании: вкладывать может кто угодно, принимает решение владелец системы. Работает это, только если у вклада есть заранее описанный маршрут, чеклист готовности и понятная точка входа. Иначе получаете либо закрытую библиотеку, куда никто не пишет, либо свалку без стандарта.</p>

<h4>Как это работает</h4>
<p>Маршрут вклада начинается не с пулл-реквеста, а с обсуждения. Заводится небольшой документ: какая задача, кто ещё столкнётся, черновик API, что уже есть похожего. Половина запросов на этом шаге закрывается — оказывается, нужное собирается из существующего. Без этого шага команда пишет неделю кода, приносит пулл-реквест, получает отказ по контракту и больше не приходит.</p>
<p>Дальше — чеклист готовности, одинаковый для всех, включая саму команду системы. Типы без any в публичном API. Доступность: роли, атрибуты, навигация с клавиатуры, фокус-ловушка для оверлеев. Только семантические токены, никаких хардкодов. Поддержка серверного рендеринга: никаких обращений к window на этапе первой отрисовки. Истории в сторибуке, включая пустое, длинное и ошибочное состояния. Тесты поведения и скриншотные. Файл changeset с типом версии. Раздел документации с примерами и антипримерами.</p>
<p>Ревью — двухслойное. Владелец домена в системе смотрит контракт и наименования, дизайнер — соответствие макетам и токенам. Каждый компонент имеет владельца в CODEOWNERS, иначе ревью висит.</p>
<p>Отдельно — сопровождение. Команда, приславшая компонент, остаётся его владельцем на квартал: чинит баги, отвечает на вопросы. Это фильтрует вклады «сдал и забыл» и делает стоимость поддержки видимой для того, кто её создаёт.</p>

<h4>Почему так</h4>
<p>Стандарт держится не на вкусе ревьюера, а на явных правилах. Устный стандарт означает, что решения выглядят произвольными, и внешние контрибьюторы уходят. Записанный чеклист превращает спор о вкусе в проверку пунктов, а спорным остаётся только контракт — там и нужен человек.</p>

<h4>Подводные камни</h4>
<ul>
<li>Ревью неделями. Команда под спринтом ждать не будет, форкнет и уйдёт. Договорённость о времени ответа обязательна.</li>
<li>Приём кода без документации и историй: компонент есть, но им никто не пользуется, потому что о нём не узнают.</li>
<li>Правки прямо в чужом пулл-реквесте вместо комментариев: у автора пропадает чувство авторства и он не возвращается.</li>
<li>Отсутствие пути отказа. Отказ должен заканчиваться предложением: сделай у себя, вернёмся, когда появится второй потребитель.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Что если команда не хочет делать доступность?»</strong> — тогда компонент не попадает в общую библиотеку; чинить доступность потом дороже, а платит за неё вся компания. <strong>«Как масштабировать ревью?»</strong> — чемпионы: по человеку в каждой продуктовой команде с правом первичного ревью и прямым каналом к владельцам системы.</p>`,
    code: `// Чеклист готовности как файл в репозитории, а не устная договорённость
// .github/pull_request_template.md
// - [ ] публичное API без any, пропы описаны
// - [ ] доступность: роль, клавиатура, фокус, aria
// - [ ] только семантические токены
// - [ ] SSR: нет обращений к window/document при первом рендере
// - [ ] истории: default, loading, error, empty, длинный текст, RTL
// - [ ] тесты поведения + скриншотные
// - [ ] changeset и раздел документации

// CODEOWNERS делает ревью адресным, а не «кто-нибудь посмотрит»
// src/components/DataGrid/  @ds-core @team-orders
// src/tokens/               @ds-core @design-tokens

// Владение на время: пометка в метаданных компонента
export const meta = {
  owner: 'team-orders',
  supportedUntil: '2026-01-15',
  status: 'incubating',
};`,
    tip: 'Сильный ход — назвать, что дают взамен. Вклад принимается быстрее, если команда получает от системы обратную услугу: генератор шаблона компонента, готовый набор тестов доступности, автоматические скриншоты. Односторонние требования без поддержки заканчиваются форками.' },

  { id: 'arc6',
    q: 'Библиотека компонентов на styled-components должна работать в SSR. Где это ломается и что вы с этим делали?',
    a: `<h4>Коротко</h4>
<p>Ломается в трёх местах: стили не попадают в HTML и первый кадр приходит без оформления, идентификаторы классов и useId расходятся между сервером и клиентом, и любое обращение к window на первом рендере валит сборку страницы. Лечится сбором стилей в поток на сервере, стабильными идентификаторами через babel-плагин и переносом браузерных проверок в эффект.</p>

<h4>Как это работает</h4>
<p>styled-components генерит CSS в момент рендера компонента и вставляет его в head через CSSOM. На сервере head нет. Значит, стили надо собрать во время рендера и отдать вместе с разметкой: ServerStyleSheet собирает правила, отрендеренные конкретным деревом, и отдаёт теги стилей, которые вставляются в HTML. При потоковом рендере это делается через оборачивание потока, чтобы чанки стилей уходили вместе с чанками разметки — иначе клиент увидит кусок неоформленного контента.</p>
<p>Второй источник боли — генерация имён классов. По умолчанию имя зависит от порядка создания компонентов, и если порядок вычисления на сервере и на клиенте отличается — разъезжаются классы, React ругается на несовпадение, и часть стилей теряется. Babel-плагин добавляет каждому styled-компоненту стабильный идентификатор на основе имени файла и переменной, и порядок перестаёт иметь значение. То же с идентификаторами для aria: только useId, никаких счётчиков модуля и Math.random, иначе описания полей отвяжутся от инпутов после гидратации.</p>
<p>Третье — среда. В библиотеке нельзя читать window, matchMedia, localStorage на этапе рендера. Всё это уходит в useEffect, а первый кадр рисуется в нейтральном состоянии. Тема при этом берётся из CSS-переменных и атрибута на html, а не из JS-контекста, — тогда сервер её знать не обязан.</p>
<p>В библиотеке на 48 компонентов у нас были два обязательных теста: рендер каждого компонента через renderToString в среде без DOM и проверка гидратации без предупреждений.</p>

<h4>Почему так</h4>
<p>CSS-in-JS платит рантаймом за динамику. На клиенте это терпимо, на сервере это лишняя работа на каждый запрос: сборка правил и хеширование заново для каждого пользователя. Поэтому промышленный ответ на вопрос — либо кеш на уровне страницы, либо переезд на решения с извлечением стилей в статический файл на сборке.</p>

<h4>Подводные камни</h4>
<ul>
<li>Дублирование стилей: две копии styled-components в бандле дают два реестра и разъехавшуюся гидратацию.</li>
<li>Динамические стили от пропа: каждое новое значение — новый класс, на длинном списке это тысячи правил.</li>
<li>Проверка типа typeof window !== 'undefined' в рендере: даёт разный результат на сервере и клиенте и ломает гидратацию.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Почему не перейти на нулевой рантайм?»</strong> — переход возможен, но у библиотеки на 48 компонентов это мажор для всех потребителей; идём поэтапно, начиная с примитивов. <strong>«А серверные компоненты?»</strong> — стилевой рантайм требует клиентской границы, поэтому примитивы либо переводятся на CSS-модули, либо явно помечаются клиентскими.</p>`,
    code: `// Сбор стилей на сервере: только правила этого дерева
const sheet = new ServerStyleSheet();
try {
  const html = renderToString(sheet.collectStyles(<App />));
  const styleTags = sheet.getStyleTags();  // <style> с нужными правилами
  res.send('<!doctype html><head>' + styleTags + '</head><body><div id="root">' + html + '</div>');
} finally {
  sheet.seal();  // без этого утечка на каждый запрос
}

// Стабильные имена классов: babel-плагин на сервере и клиенте одинаково
// .babelrc: ["babel-plugin-styled-components", { "ssr": true, "displayName": true }]

// Ломается: разный результат на сервере и клиенте
const isMobile = typeof window !== 'undefined' && window.innerWidth < 768;

// Работает: первый кадр одинаковый, уточнение после гидратации
const [isMobile, setIsMobile] = useState(false);
useEffect(function () {
  const mq = window.matchMedia('(max-width: 767px)');
  const sync = function () { setIsMobile(mq.matches); };
  sync();
  mq.addEventListener('change', sync);
  return function () { mq.removeEventListener('change', sync); };
}, []);`,
    tip: 'Отдельно упомяни политику безопасности контента: инлайновые стили от CSS-in-JS требуют либо nonce, либо ослабления политики. Nonce прокидывается в провайдер стилей и в тег на сервере — это то, что забывают и обнаруживают уже на проде за корпоративным прокси.' },

  { id: 'arc7',
    q: 'Почему index.ts с реэкспортами тащит всю библиотеку в бандл, если импортируется одна кнопка?',
    a: `<h4>Коротко</h4>
<p>Потому что импорт из баррельного файла заставляет сборщик пройти весь этот файл и все его ветки. Удалить неиспользуемое он может, только доказав, что при вычислении модуля нет побочных эффектов. Одна такая ветка — импорт CSS, регистрация в глобальном реестре, вызов на верхнем уровне — и доказательство рушится, а в бандл приезжает всё.</p>

<h4>Как это работает</h4>
<p>Тряска дерева — не удаление неиспользуемого, а сохранение достижимого. Сборщик строит граф модулей, помечает используемые экспорты и выбрасывает остальное. Работает это на статических ES-модулях: импорты вычисляются до выполнения, значит граф известен на сборке.</p>
<p>Что мешает. Во-первых, CommonJS: require вычисляется в рантайме, весь модуль становится непрозрачным объектом. Во-вторых, побочные эффекты на верхнем уровне: если модуль что-то делает при загрузке — регистрирует полифил, добавляет стиль, пишет в глобальную переменную — выбросить его нельзя, поведение изменится. В-третьих, компиляция классов и декораторов, после которой невинный класс превращается в вызов функции, а вызов — это потенциальный побочный эффект.</p>
<p>Отсюда флаг sideEffects в package.json. false означает «модули этого пакета можно не выполнять, если из них ничего не берут». Массив путей нужен, когда есть CSS: файлы стилей всегда с побочным эффектом, их исключают из тряски явно.</p>
<p>Практические меры. Первая — карта экспортов в package.json, чтобы потребитель мог импортировать компонент точечно, а не через корень. Вторая — сборка библиотеки в ES-модули с сохранением файловой структуры, а не в один бандл: тогда сборщик приложения видит отдельные модули. Третья — запрет реэкспорта звёздочкой: он мешает и сборщику, и IDE. Четвёртая — никакого кода на верхнем уровне модулей: константа — можно, вызов функции, создающей объект, — уже риск.</p>
<p>Проверять надо цифрами: собрать тестовое приложение с одним импортом и посмотреть отчёт анализатора бандла. Ощущения тут всегда врут.</p>

<h4>Почему так</h4>
<p>Сборщик не может рисковать поведением. Правило «сомневаешься — оставь» единственно безопасное, иначе безобидное обновление сборщика ломало бы приложения молча.</p>

<h4>Подводные камни</h4>
<ul>
<li>Одна иконка тянет пакет иконок целиком, потому что там баррель на пять тысяч экспортов.</li>
<li>sideEffects: false при наличии импортов CSS — стили пропадают в проде и не пропадают в деве.</li>
<li>Тряска дерева не работает у потребителя в CommonJS: библиотека приезжает целиком.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Разве современные сборщики это не решают?»</strong> — частично: они умеют переписывать реэкспорты, но останавливаются на любом модуле с побочным эффектом. <strong>«Так баррели вообще нельзя?»</strong> — можно как удобный вход, но с картой экспортов и чистыми модулями; удобство импорта не должно стоить сотни килобайт.</p>`,
    code: `// package.json библиотеки: точечные входы и честный sideEffects
{
  "sideEffects": ["*.css"],
  "exports": {
    ".": "./dist/index.js",
    "./Button": "./dist/Button/index.js",
    "./tokens.css": "./dist/tokens.css"
  }
}

// Ломает тряску: работа на верхнем уровне модуля
import './global.css';
registerIcons(allIcons);              // побочный эффект, выбросить нельзя
export const theme = buildTheme(cfg); // вызов функции — тоже эффект для сборщика

// Не ломает: только объявления, работа отложена
export const tokens = { space: [0, 4, 8, 16] };
export function createTheme(cfg) { return buildTheme(cfg); }

// Проверка цифрами, а не ощущением
// npx source-map-explorer dist/assets/*.js
// или rollup-plugin-visualizer в сборке тестового приложения`,
    tip: 'Сильный ответ отделяет два разных «всё приехало»: тряска дерева могла сработать, но разделение на чанки положило библиотеку в общий чанк, который грузится на первой странице. Смотреть надо не только размер, но и то, в какой чанк попал код и когда он запрашивается.' },

  { id: 'arc8',
    q: 'Руководитель спрашивает, что дизайн-система принесла компании. Какими метриками ты ответишь?',
    a: `<h4>Коротко</h4>
<p>Три группы: принятие (какая доля интерфейса собрана из системы), скорость (время от макета до продакшена и доля кода, который команда не написала) и качество (баги вёрстки и доступности, единообразие). Метрики надо снимать автоматически с кода и продукта, а не спрашивать у команд, довольны ли они.</p>

<h4>Как это работает</h4>
<p>Принятие меряется анализом исходников всех продуктов: скрипт обходит репозитории, парсит JSX и считает, сколько элементов пришло из библиотеки, а сколько написано руками. Итог — процент покрытия по продукту и список самых частых самописных компонентов. Второй список важнее первого: он показывает, чего в системе не хватает.</p>
<p>Скорость. Честная метрика — время от готового макета до релиза для типовой задачи, снятое по трекеру на выборке однотипных тикетов до и после. Косвенная — количество строк вёрстки на фичу. Ещё одна — время сборки нового экрана в эксперименте: сажаем двух разработчиков делать один макет, один с системой, другой без, и меряем. Это не наука, но это цифра, которую можно показать.</p>
<p>Качество. Число багов с меткой «вёрстка» и «доступность» на тысячу строк продуктового кода, тренд по кварталам. Число уникальных значений цвета и отступа в собранном CSS — прямая мера расползания: было 180 оттенков серого, стало 12. Результат автоматического аудита доступности по ключевым экранам.</p>
<p>Экономика. Стоимость системы известна точно: зарплаты команды. Выгода считается через сэкономленные человеко-дни: столько-то экранов в квартал, столько-то сэкономленных дней на экран из замера. Цифра оценочная, но её надо назвать самим, иначе её назовёт кто-то другой.</p>

<h4>Почему так</h4>
<p>Дизайн-система — внутренний продукт без выручки. Её финансируют, пока видят пользу, и «команды довольны» пользой не считается. Автоматические метрики ещё и защищают от обратного эффекта: если покрытие растёт, а время до релиза не падает, значит система стала налогом, а не ускорителем, и это надо увидеть раньше руководителя.</p>

<h4>Подводные камни</h4>
<ul>
<li>Метрики активности вместо результата: число компонентов, число коммитов, число загрузок пакета. Они растут всегда и не значат ничего.</li>
<li>Покрытие как единственная цель: команды начинают заворачивать div в компонент системы ради процента.</li>
<li>Замер только на новых экранах. Легаси не мигрирует, и через год средняя по компании не меняется.</li>
<li>Опросы удовлетворённости без цифр: отвечают самые вовлечённые, картина смещена.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как понять, что систему пора сворачивать?»</strong> — если покрытие стоит на месте, а число форков растёт, значит система не покрывает реальные нужды. <strong>«Что показывать в первый квартал?»</strong> — не экономию, а базовые замеры: без точки отсчёта любая последующая цифра не доказывает ничего.</p>`,
    code: `// Покрытие считается по коду, а не по ощущениям: обход JSX всех продуктов
// npx jscodeshift -t coverage.js --dry ../products/*/src
module.exports = function (file, api, options) {
  const j = api.jscodeshift;
  const root = j(file.source);
  const fromDS = new Set();

  root.find(j.ImportDeclaration)
    .filter(function (p) { return String(p.node.source.value).startsWith('@ds/'); })
    .forEach(function (p) {
      p.node.specifiers.forEach(function (s) { fromDS.add(s.local.name); });
    });

  let ds = 0, own = 0;
  root.find(j.JSXOpeningElement).forEach(function (p) {
    const name = p.node.name.name;
    if (typeof name !== 'string') return;
    if (fromDS.has(name)) ds++;
    else if (/^[A-Z]/.test(name)) own++;   // самописный компонент
  });

  process.stdout.write(JSON.stringify({ file: file.path, ds: ds, own: own }) + '\\n');
  return null;
};

// Расползание стилей: сколько уникальных цветов осталось в собранном CSS
// grep -ohE '#[0-9a-fA-F]{3,8}' dist/**/*.css | sort -u | wc -l`,
    tip: 'Сильный ход — назвать метрику, которая может показать плохой результат. Например, доля пулл-реквестов, где команда переопределила стили компонента системы: если она растёт, система мешает. Метрики, которые физически не могут выглядеть плохо, руководитель считает рекламой.' },

  { id: 'arc9',
    q: 'Что обязательно должно быть в документации дизайн-системы, кроме списка пропов?',
    a: `<h4>Коротко</h4>
<p>Документация — это интерфейс продукта, а не приложение к нему. Обязательны: живые примеры с редактируемым кодом, антипримеры «когда не надо», правила выбора между похожими компонентами, состояния (загрузка, ошибка, пусто, длинный текст), требования доступности и путь миграции. Список пропов генерится из типов автоматически и сам по себе ничего не объясняет.</p>

<h4>Как это работает</h4>
<p>Разделы, которые реально читают. Первый — «когда использовать и когда нет», два коротких списка. Именно он снимает половину вопросов в чате поддержки: человек ищет не проп, а решение, какой компонент взять. Второй — примеры, которые можно запустить и поправить прямо на странице; статичный кусок кода без песочницы читают, но не проверяют. Третий — состояния: пустое, загрузка, ошибка, очень длинный текст, узкий экран. Их не хватает чаще всего, и именно из-за них потом ломается вёрстка в проде.</p>
<p>Четвёртый — доступность: какие роли и атрибуты компонент проставляет сам, что обязан передать потребитель, как работает клавиатура. Если этого нет, каждый потребитель придумывает своё и половина делает неправильно. Пятый — миграции: страница на каждый мажор с таблицей «было — стало» и командой кодмода.</p>
<p>Отдельно — то, что документацию удерживает от протухания. Примеры импортируются из тех же файлов, что и истории в сторибуке, а не копируются. Пропы генерятся из TypeScript при сборке. В чеклист пулл-реквеста входит пункт про документацию, а сборка падает, если у нового компонента нет страницы. Ручная синхронизация текста и кода не работает никогда — расходятся за месяц.</p>
<p>И поиск. Документация без нормального поиска по именам, синонимам и старым названиям компонентов бесполезна: люди ищут «выпадашка», а компонент называется Select.</p>

<h4>Почему так</h4>
<p>Стоимость поддержки системы прямо зависит от качества документации: каждый ненаписанный раздел превращается в вопрос в чате, а вопросы масштабируются линейно с числом команд. Документация — единственная часть системы, которая масштабируется бесплатно.</p>

<h4>Подводные камни</h4>
<ul>
<li>Документация как рекламная витрина: красивые главные экраны и ноль про ошибки и ограничения.</li>
<li>Примеры только со счастливым путём: не видно, как компонент ведёт себя с длинным текстом и в другой локали.</li>
<li>Дублирование дизайнерской документации: правила использования расходятся с кодом, и никто не знает, где истина.</li>
<li>Ссылки на внутренние решения без объяснения: «используйте новый API» без причины и без миграции.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Кто пишет документацию?»</strong> — автор компонента, ревьюит владелец системы; писать её отдельным человеком не выходит, знание теряется по дороге. <strong>«Как понять, что документация плохая?»</strong> — по повторяющимся вопросам в чате поддержки: каждый второй одинаковый вопрос — это ненаписанный раздел.</p>`,
    code: `// Пропы генерятся из типов, а не пишутся руками
export interface ButtonProps {
  /** Визуальная роль. Опасное действие всегда danger, а не красный цвет через style. */
  variant?: 'primary' | 'secondary' | 'ghost' | 'danger';
  /** Блокирует нажатия и объявляет состояние скринридеру через aria-busy. */
  loading?: boolean;
  /** Доступное имя, если внутри только иконка. Без него кнопка «безымянная». */
  'aria-label'?: string;
}

// Пример в документации импортируется из того же файла, что и история
// docs/Button.mdx
// import { Playground } from '../.storybook/Playground';
// <Playground of={ButtonStories.Danger} />

// Сборка падает, если у компонента нет страницы документации
// scripts/check-docs.js
const missing = components.filter(function (name) {
  return !fs.existsSync('docs/' + name + '.mdx');
});
if (missing.length) {
  console.error('нет документации: ' + missing.join(', '));
  process.exit(1);
}`,
    tip: 'Хороший ход — упомянуть раздел «решения»: короткие записи, почему компонент устроен именно так и какие альтернативы отвергли. Без него каждый новый человек в команде заново предлагает уже отвергнутое, а спор про пропы против слотов повторяется раз в полгода.' },

  { id: 'arc10',
    q: 'В каких случаях дизайн-систему заводить не надо?',
    a: `<h4>Коротко</h4>
<p>Не надо, когда нет второго потребителя, когда продукт ещё ищет форму, когда некому её поддерживать и когда общая библиотека решает не ту проблему. Дизайн-система — это отдельный продукт с постоянной командой; без выделенных людей она превращается в брошенный пакет, который все обходят.</p>

<h4>Как это работает</h4>
<p>Первый признак «рано» — один продукт и одна команда. Переиспользование внутри одного приложения решается обычными общими компонентами в папке, без публикации пакета, версий, чейнджлога и отдельного сторибука. Вся эта обвязка стоит времени и оправдана, только когда потребители не могут поменять код библиотеки атомарно вместе со своим.</p>
<p>Второй признак — продукт в поиске. Пока меняется сама модель интерфейса, любой зафиксированный контракт компонента устаревает за спринт. Абстракция, снятая с одного нестабильного примера, дороже дублирования.</p>
<p>Третий — нет владельца. Система без выделенных людей живёт на энтузиазме одного человека, а после его ухода команды форкают её и остаются с мёртвой зависимостью. Минимальная жизнеспособная конфигурация — хотя бы один человек, у которого это основная работа, плюс дизайнер, у которого это часть работы.</p>
<p>Четвёртый — подмена проблемы. Если жалоба звучит как «интерфейс выглядит разнобойно», причина часто не в отсутствии библиотеки, а в отсутствии макетов и правил. Тогда дешевле начать с набора токенов и правил вёрстки: они дают восемьдесят процентов единообразия и почти ничего не стоят в поддержке.</p>
<p>Разумная последовательность: токены и шрифты — общие правила вёрстки — папка общих компонентов внутри монорепозитория — отдельный пакет, когда появился второй продукт с независимым релизным циклом.</p>

<h4>Почему так</h4>
<p>Стоимость системы постоянная и растёт с числом компонентов, а выгода появляется только при повторном использовании. При одном потребителе кривая выгоды не пересекает кривую затрат никогда. Плюс есть скрытая цена: компонент общей библиотеки нельзя поменять на месте — это пулл-реквест в чужой репозиторий, релиз, обновление зависимости. Для одной команды это чистое замедление.</p>

<h4>Подводные камни</h4>
<ul>
<li>Стартовать с копии чужой системы: получаете чужие решения и полную стоимость поддержки без своей выгоды.</li>
<li>Считать взятую внешнюю библиотеку дизайн-системой: она даёт компоненты, но не даёт языка продукта и правил.</li>
<li>Начинать с редких компонентов вместо кнопки, поля и типографики — самые частые дают почти весь эффект.</li>
<li>Заводить систему ради найма и репутации: цель не совпадает с пользой, и первое сокращение бюджета её убирает.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Что если продуктов два, но команда одна?»</strong> — монорепозиторий и общий пакет без публикации: версионирование не нужно, пока релизы синхронные. <strong>«Взять готовую или писать свою?»</strong> — брать готовую основу с ненавязанным оформлением, а своим делать слой токенов и продуктовые паттерны: писать с нуля доступный селект и дейтпикер стоит месяцев.</p>`,
    code: `// Достаточный первый шаг: токены и правила, без пакета и релизов
:root {
  --space-2: 8px;  --space-3: 12px;  --space-4: 16px;
  --text-primary: #16181d;  --surface-default: #fff;
  --radius-md: 6px;
}

// Общие компоненты внутри приложения — без версий и чейнджлога
// src/shared/ui/Button.tsx — правится атомарно вместе с потребителем

// Переход к пакету оправдан, когда релизы разъехались:
// продукт A на своей ветке, продукт B на своей, общий код менять
// одним коммитом уже нельзя
{
  "name": "@company/ui",
  "version": "1.0.0",
  "peerDependencies": { "react": ">=18" }
}`,
    tip: 'Сильный ответ переворачивает вопрос: спроси, какую боль пытаются лечить. «Долго верстаем» лечится библиотекой, «дизайнеры рисуют по-разному» — макетной библиотекой и токенами, «интерфейс недоступен» — аудитом и правилами. Дизайн-система решает только первую, и подмена диагноза здесь стоит года работы.' },

  { id: 'arc11',
    q: 'Объясни, как Module Federation устроен изнутри. Что такое remoteEntry и как разрешаются общие зависимости?',
    a: `<h4>Коротко</h4>
<p>Module Federation — это подмена механизма загрузки модулей на этапе выполнения. Сборщик вместо статической ссылки на модуль вставляет асинхронную функцию, которая идёт в чужую сборку по сети, скачивает манифест remoteEntry, спрашивает у него нужный модуль и получает промис. Общие зависимости складываются в общий реестр по имени, и при первом обращении из него выбирается одна подходящая по диапазону версия.</p>

<h4>Как это работает</h4>
<p>При сборке появляются три роли. Хост — приложение, которое потребляет чужие модули. Удалённый модуль — сборка, публикующая часть себя наружу. Общие зависимости — пакеты, которые обе стороны согласны использовать совместно.</p>
<p>remoteEntry.js — маленький файл-точка входа удалённого модуля. Внутри объект с двумя методами: init принимает область общих зависимостей хоста, get по строке-ключу возвращает фабрику, которая при вызове отдаёт сам модуль. Никакого кода компонентов в remoteEntry нет, только карта и загрузчики чанков.</p>
<p>Порядок при загрузке такой. Хост создаёт область общих зависимостей и кладёт туда свои версии React и остальных общих пакетов вместе с фабриками загрузки. Тег скрипта подтягивает remoteEntry по URL. Хост вызывает init и передаёт свою область — теперь удалённый модуль видит, что уже есть. Хост вызывает get с именем модуля, получает фабрику, вызывает её, получает промис с настоящим модулем. Всё это происходит за первым await, поэтому в React такой модуль подключается через lazy и Suspense.</p>
<p>Разрешение версий. У каждой стороны для общего пакета есть требуемый диапазон и предоставляемая версия. При обращении к общей зависимости выбирается самая старшая версия, удовлетворяющая диапазонам. Если ничьей подходящей версии нет — при strictVersion падаем с ошибкой, иначе печатается предупреждение и берётся то, что есть. Флаг eager означает «положи в основной бандл синхронно» и нужен для того, что требуется до первого await, но он ломает разделение кода.</p>

<h4>Почему так</h4>
<p>Задача — независимый деплой при общем рантайме. Отдельные бандлы на каждую команду дают независимость, но дублируют библиотеки и теряют общий контекст. Федерация даёт и то и другое: код разный, рантайм один. Цена — часть графа зависимостей становится известна только в браузере, а значит ошибки версий вылезают в рантайме, а не на сборке.</p>

<h4>Подводные камни</h4>
<ul>
<li>Захардкоженный URL remoteEntry: окружения различаются, нужен манифест с адресами или динамические удалённые модули.</li>
<li>Кеширование remoteEntry браузером: файл обязан быть без хеша в имени и с коротким сроком жизни, иначе деплой не доезжает.</li>
<li>eager: true на React: удобно, но кладёт React в начальный чанк и убивает выгоду.</li>
<li>Разные версии сборщика у хоста и удалённого модуля: протокол федерации между мажорами несовместим.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Что грузится первым?»</strong> — remoteEntry (единицы килобайт), потом только те чанки, которые нужны запрошенному модулю. <strong>«Как типизировать границу?»</strong> — генерация деклараций типов из удалённого модуля в пакет и публикация её отдельно; без этого граница нетипизирована и ломается молча.</p>`,
    code: `// Хост: что берём снаружи и что делим
new ModuleFederationPlugin({
  name: 'shell',
  remotes: {
    orders: 'orders@https://cdn.example.com/orders/remoteEntry.js',
  },
  shared: {
    react: { singleton: true, requiredVersion: '^18.2.0', strictVersion: true },
    'react-dom': { singleton: true, requiredVersion: '^18.2.0', strictVersion: true },
    '@ds/components': { singleton: false, requiredVersion: '^4.0.0' },
  },
});

// Удалённый модуль: что публикуем наружу — это и есть публичный контракт
new ModuleFederationPlugin({
  name: 'orders',
  filename: 'remoteEntry.js',
  exposes: { './OrdersPage': './src/pages/Orders' },
  shared: { react: { singleton: true, requiredVersion: '^18.2.0' } },
});

// Потребление: всегда асинхронно, поэтому lazy + Suspense обязательны
const OrdersPage = React.lazy(function () { return import('orders/OrdersPage'); });

// Динамический адрес вместо захардкоженного в конфиге
async function loadRemote(url, scope, module) {
  await loadScript(url);                       // тег script с remoteEntry
  await __webpack_init_sharing__('default');   // готовим область общих зависимостей
  const container = window[scope];
  await container.init(__webpack_share_scopes__.default);
  const factory = await container.get(module);
  return factory();
}`,
    tip: 'Сильный ответ отделяет федерацию от импортных карт браузера: карты решают ту же задачу подстановки адресов, но не умеют согласовывать версии и не дают разделения кода per-remote. Федерация — это про переговоры о версиях в рантайме, и именно поэтому она тяжелее.' },

  { id: 'arc12',
    q: 'Почему две копии React в бандле ломают хуки и как singleton в shared это чинит?',
    a: `<h4>Коротко</h4>
<p>Хук не хранит состояние в себе: он читает его из внутренней переменной модуля React — текущего активного диспетчера. Компонент, отрендеренный копией номер один, выставляет диспетчер в своём экземпляре React; если хук импортирован из копии номер два, он смотрит в её переменную, а там null. Отсюда ошибка про недопустимый вызов хука. Singleton заставляет обе стороны взять один экземпляр модуля.</p>

<h4>Как это работает</h4>
<p>Внутри React есть объект общего внутреннего состояния, где живёт ссылка на текущий диспетчер. Рендерер перед вызовом функции компонента подставляет туда реализацию — свою для монтирования, свою для обновления, свою для серверного рендера. useState просто вызывает метод текущего диспетчера. Это переменная уровня модуля, то есть у каждого экземпляра модуля она своя.</p>
<p>Дальше картина простая. react-dom из копии A рендерит дерево и выставляет диспетчер в React A. Компонент из удалённого модуля собран с React B. Его useState идёт в React B, диспетчер там пустой, летит ошибка. Если ошибки нет — бывает хуже: два React работают одновременно, контекст, созданный в A, не виден потребителю из B, потому что объект контекста — это тоже просто объект, и он у них разный. Провайдер стоит, значение не доходит, потребитель молча получает значение по умолчанию.</p>
<p>Диагностика: посмотреть, сколько раз react попал в бандл — по отчёту анализатора или через сравнение внутреннего объекта React в консоли. В деве React сам подсказывает про несколько копий как одну из трёх причин ошибки.</p>
<p>Лечение в федерации: singleton: true для react, react-dom и всего, что хранит состояние в модуле — маршрутизатор, стор, провайдер стилей, клиент запросов. Плюс requiredVersion и strictVersion, чтобы несовпадение мажоров падало явно, а не деградировало в странное поведение. Плюс peerDependencies в удалённых модулях, чтобы React не приезжал транзитивно.</p>

<h4>Почему так</h4>
<p>Модульный синглтон — самый дешёвый способ хранить глобальное состояние библиотеки. Альтернатива — реестр в глобальном объекте — работает, но делает библиотеку зависимой от окружения и ломается в изолированных средах. React выбрал модульную переменную и переложил ответственность за единственность на сборку.</p>

<h4>Подводные камни</h4>
<ul>
<li>Не только React: единственность нужна маршрутизатору, стору, провайдеру стилей, i18n. Симптомы разные, причина одна.</li>
<li>singleton при несовместимых мажорах: без strictVersion загрузится один и сломается второй, но уже в рантайме и не сразу.</li>
<li>Локальная разработка через симлинки: там две копии появляются даже без федерации.</li>
<li>Проверка instanceof и сравнение объектов контекста между копиями: всегда ложь, и это выглядит как случайный баг.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«А если командам нужны разные мажоры React?»</strong> — тогда либо изоляция в отдельном дереве без общих провайдеров (веб-компонент или iframe), либо синхронное обновление; частично общий React невозможен. <strong>«Как поймать заранее?»</strong> — проверка в сборке: количество экземпляров React в собранном приложении должно быть равно одному.</p>`,
    code: `// Почему ломается: диспетчер — переменная экземпляра модуля React
// упрощённо, внутри react:
let currentDispatcher = null;              // у каждой копии своя
export function useState(initial) {
  return currentDispatcher.useState(initial);  // null -> исключение
}
// а внутри react-dom перед рендером компонента:
currentDispatcher = HooksDispatcherOnMount;    // выставляется в СВОЮ копию

// Тихий случай: контекст из копии A не виден потребителю из копии B
const Theme = React.createContext(null);   // объект-ключ у каждой копии свой
// <Theme.Provider> из A + useContext(Theme) из B -> значение по умолчанию

// Лечение: единственность в федерации
shared: {
  react: { singleton: true, requiredVersion: '^18.2.0', strictVersion: true },
  'react-dom': { singleton: true, requiredVersion: '^18.2.0', strictVersion: true },
  'react-router-dom': { singleton: true },
  'styled-components': { singleton: true },   // реестр стилей тоже один
}

// В удалённом модуле React только peer, иначе приедет транзитивно
{ "peerDependencies": { "react": "^18.2.0" }, "devDependencies": { "react": "^18.2.0" } }`,
    tip: 'Назови проверку одной строкой: если в собранном приложении объект внутренних структур React встречается дважды, копии две. И добавь, что симптом бывает не «ошибка хука», а «контекст пустой» — второй вариант ищут в разы дольше, потому что приложение не падает.' },

  { id: 'arc13',
    q: 'Легаси на старом фреймворке. Переписать с нуля или мигрировать постепенно? Как ты это выбираешь и как объясняешь бизнесу?',
    a: `<h4>Коротко</h4>
<p>По умолчанию — постепенно, паттерном «удушение»: новое приложение растёт вокруг старого, забирая по куску, старое живёт до последнего пользователя. Переписывание с нуля оправдано только на маленьком объёме или когда старую систему всё равно нельзя развивать. Бизнесу это продаётся не как «нам нужен рефакторинг», а как поставка ценности каждый спринт при неостанавливающихся релизах.</p>

<h4>Как это работает</h4>
<p>Почему переписывание проваливается. Пока пишется новая версия, старая продолжает получать фичи и правки — и на момент готовности новая уже отстала. Дата окончания неизвестна, потому что объём знания, закопанного в старом коде, неизвестен: половина логики — это накопленные краевые случаи, о которых никто не помнит. Плюс релиз одним куском означает, что весь риск сконцентрирован в одном дне.</p>
<p>Как устроено удушение. На входе ставится слой, который решает, куда направить запрос: маршрутизатор в оболочке, реверс-прокси или точка монтирования. Первая часть переносится целиком со своими данными и своим маршрутом. Пользователь ходит по старому приложению, а на одном разделе незаметно попадает в новое. Дальше переносится следующая часть. Старый код удаляется сразу после переноса — это отдельная задача в том же тикете, иначе он живёт вечно.</p>
<p>В моём случае система переезжала с Angular 5 на React через Module Federation, и релизы не останавливались ни на неделю. Оболочка держала маршрутизацию и общий каркас, старое приложение монтировалось в свой контейнер, новые разделы приезжали как удалённые модули. Ключевое условие — граница проходила по разделам с собственным маршрутом, а не по компонентам внутри одного экрана.</p>
<p>Разговор с бизнесом. Не «перепишем за полгода», а «каждый спринт один раздел переезжает и в нём становится возможным то, чего сейчас нельзя». Риск объясняется через возможность остановки: работу можно прекратить на любом шаге, и продукт останется рабочим. Плюс сразу называется цена: временно две системы, двойные стили, чуть больший бандл — это признаётся вслух, иначе на первом же вопросе разговор рассыпается.</p>

<h4>Почему так</h4>
<p>Постепенная миграция обменивает суммарную скорость на управляемость риска. Она объективно дольше и суммарно дороже, но каждый её шаг обратим и проверяем на живых пользователях. Переписывание с нуля дешевле в идеальном мире и катастрофично при первой же ошибке в оценке.</p>

<h4>Подводные камни</h4>
<ul>
<li>Заморозка фич на время миграции: не выдерживает ни один бизнес, и через два месяца новое пишут в обе системы.</li>
<li>Перенос без удаления старого: две реализации расходятся, багов становится больше.</li>
<li>Граница по компонентам внутри экрана: два фреймворка в одном дереве — самая дорогая из возможных границ.</li>
<li>Отсутствие критерия готовности раздела: без него переезд считается сделанным, когда «в основном работает».</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Когда всё-таки писать с нуля?»</strong> — когда приложение маленькое, когда меняется сама модель продукта или когда старую платформу нельзя собрать на текущем окружении. <strong>«Как мерить прогресс?»</strong> — долей трафика на новом коде, а не долей перенесённых файлов.</p>`,
    code: `// Слой принятия решения: один маршрутизатор решает, чей раздел показать
const MIGRATED = new Set(['/orders', '/reports']);

function Shell() {
  const path = useLocation().pathname;
  const isNew = [...MIGRATED].some(function (p) { return path.startsWith(p); });
  return isNew ? <NewApp /> : <LegacyMount path={path} />;
}

// Старое приложение живёт в своём контейнере и не знает про React
function LegacyMount({ path }) {
  const ref = useRef(null);
  useEffect(function () {
    const app = legacyBootstrap(ref.current, path);   // ng-приложение
    return function () { app.destroy(); };            // размонтирование обязательно
  }, [path]);
  return <div ref={ref} />;
}

// Переключение по флагу, чтобы откат был мгновенным и без деплоя
const MIGRATED_ROUTES = flags.get('migrated-routes', ['/orders']);`,
    tip: 'Сильный ход — назвать условие остановки заранее: если перенос раздела занимает больше N спринтов или ломает больше N процентов сценариев, раздел возвращается в старую систему и разбирается на части. Без такого правила застрявший раздел тянет за собой всю миграцию.' },

  { id: 'arc14',
    q: 'С какого куска легаси ты начнёшь миграцию и по каким признакам его выбираешь?',
    a: `<h4>Коротко</h4>
<p>Первый кусок выбирается не по важности, а по проверяемости: он должен быть маленьким, иметь собственный маршрут, минимум связей с общим состоянием и живой трафик, на котором видно, что всё работает. Начинать с самого сложного раздела — способ провалить миграцию на первом шаге, начинать с мёртвого — способ ничего не узнать.</p>

<h4>Как это работает</h4>
<p>Пять признаков хорошего первого острова. Первый — своя точка входа: раздел открывается по адресу и не встроен в чужой экран. Тогда граница проходит по маршруту, а не по DOM-дереву. Второй — узкий контракт с остальным приложением: он читает пользователя и токен, но не лезет в общий стор и не рассылает событий. Третий — размер: две-три недели работы, чтобы результат был виден в том же квартале. Четвёртый — реальные пользователи: без трафика вы не узнаете ни про производительность, ни про краевые случаи. Пятый — терпимость к сбою: раздел важен, но не является платёжным потоком.</p>
<p>Практически подходят справочники, настройки, отчёты, разделы администрирования. Не подходят: оформление заказа, авторизация, общие шапка и меню — они завязаны на всё сразу и ошибка в них видна всем.</p>
<p>Перед выбором полезно собрать две карты. Карта связей: какие модули читают общий стор, кто слушает глобальные события — верхние по связности берём в последнюю очередь. Карта трафика: сколько пользователей и денег проходит через раздел — она задаёт очередь риска.</p>
<p>Отдельно про первый шаг: его цель не столько перенести раздел, сколько построить дорогу. Сборка, деплой, наблюдаемость, флаг переключения, откат, авторизация на границе — всё это делается один раз и потом переиспользуется. Поэтому первый раздел закономерно занимает вдвое дольше второго, и это надо сказать заранее, иначе оценка следующих будет выглядеть подозрительной.</p>

<h4>Почему так</h4>
<p>Первый шаг проверяет гипотезу, а не выполняет план. Ошибка на маленьком разделе стоит спринт, на платёжном потоке — репутацию проекта и доверие к миграции, которое потом не восстановить.</p>

<h4>Подводные камни</h4>
<ul>
<li>Начать с общей шапки: она нужна везде и мгновенно превращает границу в общее состояние с двусторонним обменом.</li>
<li>Взять раздел без пользователей: перенос формально сделан, знания ноль.</li>
<li>Недооценить хвост: сохранение состояния фильтров, печать, экспорт, глубокие ссылки из писем.</li>
<li>Забыть про адреса: старые URL обязаны продолжать работать, редиректы делаются на слое маршрутизации.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как проверить, что перенос удался?»</strong> — сравнением метрик до и после на том же трафике: конверсия раздела, ошибки, скорость загрузки; плюс постепенный вывод по проценту пользователей. <strong>«Что если раздел оказался связан сильнее, чем казалось?»</strong> — временный адаптер на границе и явный технический долг с датой, но не расширение границы миграции на ходу.</p>`,
    code: `// Карта связности: кандидаты с наименьшим числом обращений к общему состоянию
// grep -rl "store\\.\\|rootScope\\|eventBus" src/modules | sort | uniq -c

// Постепенный вывод: сначала процент пользователей, потом все
function useMigratedRoute(path) {
  const user = useUser();
  const rollout = flags.get('orders-migration-percent', 0);
  const bucket = hashToPercent(user.id);   // стабильно для одного пользователя
  return MIGRATED.has(path) && bucket < rollout;
}

// Старые адреса обязаны жить: редиректы на слое маршрутизации
const LEGACY_URLS = {
  '/app/#/orders/list': '/orders',
  '/app/#/orders/view/:id': '/orders/:id',
};

// Критерий готовности раздела, а не «в основном работает»
// - метрика ошибок не выше базовой
// - основные сценарии проходят end-to-end
// - старый код удалён в том же релизе
// - глубокие ссылки и печать проверены`,
    tip: 'Хороший ответ разделяет две цели первого шага: доставить пользу и построить инфраструктуру. Скажи явно, что первый раздел оценивается с двойным запасом, потому что вместе с ним делается конвейер — иначе команда объясняет менеджеру просрочку задним числом.' },

  { id: 'arc15',
    q: 'Как жить с общим состоянием и роутингом, когда старое и новое приложения работают одновременно?',
    a: `<h4>Коротко</h4>
<p>Общее состояние сводится к минимуму и живёт в одном месте — обычно в оболочке. Маршрутизация одна: владелец адресной строки ровно один, остальные получают путь как входные данные. Всё остальное общается через узкий явный контракт, а не через глобальные переменные и события со строковыми именами.</p>

<h4>Как это работает</h4>
<p>Сначала честный список того, что действительно общее: текущий пользователь и права, токен и его обновление, локаль, тема, уведомления, текущий маршрут. Это почти всегда всё. Корзина, фильтры таблицы, черновик формы общими не являются, даже если очень хочется.</p>
<p>Хранится это в оболочке и раздаётся вниз как читаемое значение плюс подписка. Записывать может только владелец: приложение просит оболочку сменить локаль, а не меняет её само. Односторонний поток на границе спасает от петель, когда два приложения по очереди обновляют одно значение.</p>
<p>Маршрутизация. Владелец истории браузера — оболочка. Внутренние маршрутизаторы приложений запускаются в режиме памяти или с базовым префиксом и синхронизируются в одну сторону: оболочка передаёт путь вниз, приложение сообщает наверх о навигации через колбэк. Два маршрутизатора, одновременно пишущих в историю, дают классический баг: кнопка «назад» прыгает через раз, потому что каждое нажатие обрабатывают оба.</p>
<p>Токен и авторизация делаются один раз в оболочке: она хранит токен, обновляет его и отдаёт клиенту запросов. Иначе два приложения одновременно упираются в 401 и запускают два конкурирующих обновления, одно из которых инвалидирует второе.</p>
<p>Когда состояние действительно надо синхронизировать между независимо собранными приложениями, лучшее — события с типизированным контрактом в одном пакете: не строковые имена, а описанный набор с версиями. Тогда переименование поля ловится компилятором хотя бы на сборке.</p>

<h4>Почему так</h4>
<p>Каждый общий кусок состояния — это связь, которая отменяет независимость деплоя: изменив его форму, вы обязаны выкатить обе стороны одновременно. Поэтому общее состояние — это дефицитный ресурс, а не удобство.</p>

<h4>Подводные камни</h4>
<ul>
<li>Глобальная шина со строковыми событиями: не типизируется, не ищется по коду, ломается молча.</li>
<li>Два маршрутизатора: скачущая кнопка «назад» и двойные записи в историю.</li>
<li>Общий стор, куда пишут все: любая миграция схемы становится согласованным деплоем.</li>
<li>Прокидывание объектов классов через границу: у сторон разные копии, проверки типа не совпадают.</li>
<li>Потеря состояния при переходе между старой и новой частью — фильтры сбрасываются, и пользователь считает это багом.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«А если нужен настоящий общий стор?»</strong> — тогда это одно приложение, разделённое по коду, а не микрофронтенды; называть вещи своими именами дешевле. <strong>«Как передавать данные, чтобы не связывать?»</strong> — через URL: параметры адреса переживают перезагрузку, не требуют общего рантайма и работают в обе стороны.</p>`,
    code: `// Контракт оболочки: чтение и подписка, запись только через явные методы
export interface ShellContext {
  user: { id: string; roles: string[] } | null;
  locale: string;
  getToken(): Promise<string>;          // обновление токена — забота оболочки
  navigate(to: string): void;           // историей владеет оболочка
  subscribe(fn: (s: ShellState) => void): () => void;
}

// Внутренний маршрутизатор приложения не пишет в историю сам
function OrdersApp({ path, onNavigate }) {
  return (
    <MemoryRouter initialEntries={[path]}>
      <NavigationBridge onNavigate={onNavigate} />
      <Routes>{/* ... */}</Routes>
    </MemoryRouter>
  );
}

// События с контрактом в общем пакете вместо строк в глобальной шине
// @company/contracts
export type ShellEvent =
  | { type: 'cart.itemAdded'; v: 1; payload: { sku: string; qty: number } }
  | { type: 'auth.expired'; v: 1 };

// Плохо: строка без контракта, переименование поля никто не заметит
window.dispatchEvent(new CustomEvent('cart-add', { detail: { id: sku } }));`,
    tip: 'Сильный ход — предложить URL как основной канал состояния между частями: он не требует общего рантайма, переживает перезагрузку, работает при разных фреймворках и его невозможно сломать несовпадением версий. Всё, что можно выразить адресом, не должно жить в общем сторе.' },

  { id: 'arc16',
    q: 'Две системы стилей в одном приложении одновременно. Как изолировать и что течёт?',
    a: `<h4>Коротко</h4>
<p>Течёт всё, что глобально по природе: сброс стилей, селекторы по тегам, глобальные имена классов, шрифты и переменные на корне, а также z-index и стили печати. Изоляция строится в три слоя: убрать глобальные селекторы, дать каждому приложению префикс и область, и договориться о едином каскадном слое и шкале z-index.</p>

<h4>Как это работает</h4>
<p>Первое, что ломается — сброс. Старое приложение везёт свой normalize с правилами по тегам, новое везёт свой. Побеждает тот, чей файл загрузился позже, и вёрстка второго едет. Решение: сброс подключается ровно один раз, в оболочке, и запрещается в приложениях правилом линтера.</p>
<p>Второе — глобальные имена. Классы вроде btn или active встречаются в обеих системах. Лечится тем, что новый код использует локальные имена — CSS-модули или CSS-in-JS с хешами, а старый код прячется под общий корневой класс, добавляемый на сборке через плагин постпроцессора. Тогда правила старого приложения не достают до нового дерева.</p>
<p>Третье — каскад. Здесь помогают слои: объявляем порядок явно, и правила приложения всегда выигрывают у правил библиотеки независимо от порядка загрузки файлов. Это снимает битву специфичности между независимо собранными бандлами.</p>
<p>Четвёртое — то, что не изолируется в принципе. Шрифты подключаются один раз в оболочке, иначе грузятся дважды. Модалки и выпадашки уходят в общий портал, и им нужна общая шкала слоёв: числа задаются токенами в оболочке, приложения не имеют права ставить произвольные значения. Стили печати, обработка скролла body при открытом оверлее, стили фокуса — тоже общие.</p>
<p>Радикальная изоляция — теневой DOM. Стили не текут вообще, но платите доступностью, порталами, наследованием шрифтов и тем, что многие библиотеки вставляют стили в head и внутрь тени не попадают. Для миграции обычно избыточно.</p>

<h4>Почему так</h4>
<p>CSS по устройству глобален: селекторы применяются ко всему документу. Любая изоляция — это соглашение, добавляющее к селектору область. Поэтому дешёвая изоляция достигается не инструментом, а запретом на глобальные правила и явными общими точками.</p>

<h4>Подводные камни</h4>
<ul>
<li>Второй сброс стилей, приезжающий через удалённый модуль: страница едет только после определённой навигации, и причина ищется долго.</li>
<li>Два экземпляра провайдера стилей CSS-in-JS: удвоенные правила и разъезд при гидратации.</li>
<li>Оверлеи с произвольным z-index: модалка из одного приложения оказывается под шапкой другого.</li>
<li>Двойная загрузка шрифтов: лишние сотни килобайт и мигание текста.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Почему не iframe?»</strong> — изоляция идеальная, но ломается всё остальное: общие модалки, размеры, история, доступность, производительность. <strong>«Кто владеет z-index?»</strong> — оболочка: шкала слоёв — часть контракта, как и токены.</p>`,
    code: `/* Порядок каскада объявляется один раз в оболочке */
@layer reset, legacy, ds, app;

@layer reset  { /* единственный сброс на всё приложение */ }
@layer legacy { /* стили старого приложения, под общим корнем */ }
@layer ds     { /* дизайн-система */ }
@layer app    { /* продуктовые переопределения выигрывают всегда */ }

/* Старые глобальные правила прячутся под корень автоматически на сборке */
/* postcss-prefix-selector: prefix '.legacy-root' */
.legacy-root .btn { padding: 6px 12px; }

/* Шкала слоёв — токены оболочки, произвольные числа запрещены линтером */
:root {
  --z-dropdown: 1000;
  --z-sticky: 1100;
  --z-modal: 1300;
  --z-toast: 1400;
}

/* Ломается: правило по тегу из удалённого модуля достаёт до всей страницы */
button { font-family: inherit; margin: 0; }`,
    tip: 'Назови диагностический приём: если вёрстка едет только после захода в определённый раздел, значит стили приехали вместе с ленивым чанком этого раздела и остались в head навсегда. Порядок загрузки чанков — скрытая переменная, из-за которой баг не воспроизводится на прямом заходе по адресу.' },

  { id: 'arc17',
    q: 'Микрофронтенды деплоятся независимо. Как версионировать контракт между хостом и удалёнными модулями?',
    a: `<h4>Коротко</h4>
<p>Контракт — это то, что удалённый модуль выставляет наружу, и то, что он ожидает от оболочки. Он версионируется как публичный API: расширять можно молча, ломать — только через параллельную версию с окном перехода. Технически это делается манифестом адресов с версией и проверкой совместимости на сборке, а не на глазок.</p>

<h4>Как это работает</h4>
<p>Три вещи должны быть описаны явно. Первая — экспортируемые модули и их пропсы. Вторая — то, что нужно от оболочки: контекст, версия контракта, набор общих зависимостей. Третья — общие зависимости с диапазонами версий.</p>
<p>Типы границы генерируются из удалённого модуля и публикуются пакетом. Хост ставит их как обычную зависимость, и несовместимость обнаруживается компилятором на сборке хоста, а не в браузере пользователя. Без этого шага граница фактически нетипизирована: import из удалённого модуля даёт any.</p>
<p>Адреса живут в манифесте, а не в конфиге сборки. Оболочка при старте забирает JSON, где по каждому удалённому модулю указан URL версии и версия контракта, и грузит по нему. Это даёт откат без пересборки оболочки: поменяли адрес в манифесте — вернулись на предыдущую сборку.</p>
<p>Правила изменений. Добавить необязательный проп — минор. Изменить обязательный, убрать экспорт, поменять смысл — мажор: выкладывается новый экспорт рядом, оболочка переключается, старый удаляется после того, как метрики показали нулевое использование. Мажор общей зависимости — согласованный деплой, и это единственный случай, когда независимость деплоя честно теряется, о чём надо предупреждать заранее.</p>
<p>Проверка совместимости в конвейере: перед выкладкой удалённого модуля собирается хост с новой версией и прогоняются сквозные тесты по границе. Это дешёвая замена координации релизов.</p>

<h4>Почему так</h4>
<p>Независимый деплой означает, что в проде одновременно живут произвольные сочетания версий. Значит, совместимость должна поддерживаться не расписанием релизов, а свойствами контракта: только расширение, явные версии, окно перехода.</p>

<h4>Подводные камни</h4>
<ul>
<li>Захардкоженные адреса удалённых модулей в конфиге: откат требует пересборки оболочки.</li>
<li>Кеширование manifest.json и remoteEntry.js: без коротких заголовков кеша деплой не доезжает до части пользователей.</li>
<li>Мажор React в одном модуле: ломает всех, потому что React — singleton; это не локальное решение команды.</li>
<li>Нетипизированная граница: рефакторинг пропа проходит ревью и падает в проде.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как откатывать?»</strong> — переключением версии в манифесте, потому что сборки лежат по адресам с версией и не перезаписываются. <strong>«Как тестировать пары версий?»</strong> — контрактными тестами: удалённый модуль публикует набор ожиданий, конвейер хоста прогоняет их при каждой сборке.</p>`,
    code: `// Манифест: адреса и версии контракта, читается в рантайме
{
  "orders":  { "url": "https://cdn/orders/v2.4.1/remoteEntry.js", "contract": "2" },
  "reports": { "url": "https://cdn/reports/v1.9.0/remoteEntry.js", "contract": "1" }
}

// Оболочка проверяет совместимость до загрузки модуля
const SUPPORTED = { orders: ['2'], reports: ['1', '2'] };

async function mount(name) {
  const entry = manifest[name];
  if (!SUPPORTED[name].includes(entry.contract)) {
    reportIncident('contract-mismatch', { name: name, got: entry.contract });
    return renderFallback(name);         // не белый экран
  }
  return loadRemote(entry.url, name, './App');
}

// Типы границы публикуются пакетом и ставятся хостом как зависимость
// @company/orders-contract
declare module 'orders/OrdersPage' {
  export interface OrdersPageProps { customerId: string; compact?: boolean }
  const OrdersPage: React.ComponentType<OrdersPageProps>;
  export default OrdersPage;
}

// Сборки неизменяемы: путь содержит версию, старая версия остаётся доступной
// cdn/orders/v2.4.0/... и cdn/orders/v2.4.1/... живут одновременно`,
    tip: 'Сильный ответ добавляет, что откат должен быть операцией без участия фронтенд-разработчика: правка манифеста через панель или флаг. Если для отката нужен деплой оболочки, независимость деплоя существует только на бумаге — в инциденте это выясняется в худший момент.' },

  { id: 'arc18',
    q: 'Удалённый модуль не загрузился или упал при инициализации. Что происходит с приложением?',
    a: `<h4>Коротко</h4>
<p>Ничего не должно происходить с остальным приложением. Загрузка удалённого модуля — сетевая операция, поэтому у неё обязаны быть таймаут, повтор, запасной вариант и граница ошибок вокруг. Отсутствие раздела — это состояние интерфейса, а не сбой, и оболочка обязана остаться живой.</p>

<h4>Как это работает</h4>
<p>Сценариев отказа четыре, и они лечатся по-разному. Первый — remoteEntry не скачался: сеть, CDN, обрыв. Здесь помогают таймаут на загрузку скрипта, один-два повтора с задержкой и запасной адрес. Второй — скачался, но упал при вычислении: несовместимая версия общей зависимости, ошибка в коде. Повтор бесполезен, нужен запасной вариант. Третий — модуль загрузился, но падает при отрисовке: ловится границей ошибок. Четвёртый — модуль работает, но не отвечает: ошибка внутри его собственных запросов, и это его зона ответственности.</p>
<p>Обвязка выглядит так. Загрузка скрипта с таймаутом и очисткой обработчиков. Повтор с задержкой только для сетевых ошибок. Кеш промиса, чтобы параллельные обращения не грузили remoteEntry дважды. Граница ошибок вокруг каждого удалённого модуля отдельно, чтобы падение одного не роняло экран. Осмысленное запасное состояние: для второстепенного блока — просто скрыть, для раздела — экран с объяснением и кнопкой повтора, для критичного пути — редирект на старую реализацию, если она ещё жива.</p>
<p>Отдельно — наблюдаемость. Ошибка загрузки удалённого модуля должна приезжать в мониторинг с именем модуля, версией контракта и адресом. Иначе выглядит как случайные пустые экраны у части пользователей, которые никак не воспроизводятся у разработчиков.</p>
<p>Частая причина в проде — устаревший индекс: у пользователя открыта вкладка со старым списком чанков, деплой удалил файлы, ленивая загрузка падает. Лечится неизменяемыми путями со сборкой в имени и предложением перезагрузить страницу при ошибке загрузки чанка.</p>

<h4>Почему так</h4>
<p>Микрофронтенды переносят часть графа зависимостей в сеть, а сеть ненадёжна по определению. Значит, каждая граница между модулями — это точка отказа, и её надо проектировать так же, как обычный запрос к API.</p>

<h4>Подводные камни</h4>
<ul>
<li>Одна общая граница ошибок на всё приложение: падение виджета даёт белый экран.</li>
<li>Бесконечные повторы: при недоступном CDN клиенты устраивают шторм запросов.</li>
<li>Запасной вариант без объяснения: пустое место выглядит как потерянные данные.</li>
<li>Ошибка загрузки не логируется: инцидент виден только по падению конверсии.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как это тестировать?»</strong> — принудительным отказом: подмена адреса, троттлинг, отключение CDN в тестовой среде; иначе запасной путь не проверялся никогда. <strong>«Что показывать при отказе критичного модуля?»</strong> — понятное сообщение, повтор и путь в обход, а не бесконечный скелетон: висящий скелетон читается как зависание.</p>`,
    code: `// Загрузка remoteEntry: таймаут, кеш промиса, повтор только для сети
const cache = new Map();

function loadScript(url, timeout) {
  if (cache.has(url)) return cache.get(url);
  const p = new Promise(function (resolve, reject) {
    const el = document.createElement('script');
    const timer = setTimeout(function () {
      el.remove();
      reject(new Error('remote timeout: ' + url));
    }, timeout);
    el.src = url;
    el.onload = function () { clearTimeout(timer); resolve(); };
    el.onerror = function () { clearTimeout(timer); reject(new Error('remote failed: ' + url)); };
    document.head.appendChild(el);
  });
  cache.set(url, p);
  p.catch(function () { cache.delete(url); });   // неудачу не кешируем
  return p;
}

// Своя граница ошибок на каждый удалённый модуль
<ErrorBoundary
  key={'orders'}
  onError={function (e) { report('remote.crash', { remote: 'orders', message: e.message }); }}
  fallback={<RemoteUnavailable name="Заказы" onRetry={retry} />}
>
  <Suspense fallback={<Skeleton />}>
    <OrdersPage />
  </Suspense>
</ErrorBoundary>

// Устаревшая вкладка: чанк удалён деплоем — предложить перезагрузку, а не молчать
window.addEventListener('error', function (e) {
  if (String(e.message).includes('Loading chunk')) showReloadPrompt();
});`,
    tip: 'Хороший ответ разделяет отказ по важности блока: второстепенный виджет исчезает молча, раздел показывает ошибку с повтором, критичный путь уводит на запасную реализацию. Одинаковая заглушка на все случаи либо пугает пользователя из-за мелочи, либо прячет настоящую поломку.' },

  { id: 'arc19',
    q: 'Сколько стоят микрофронтенды и когда они не нужны?',
    a: `<h4>Коротко</h4>
<p>Микрофронтенды решают организационную задачу — независимость команд по релизам — и платят за неё технически: больше кода в бандле, сложнее отладка, ошибки версий в рантайме, отдельный конвейер на каждую часть. Если команда одна или релизы и так синхронные, платить не за что, и монолит с разделением кода лучше во всём.</p>

<h4>Как это работает</h4>
<p>Что именно дорожает. Размер: общие зависимости дублируются везде, где не удалось договориться о версии, и суммарный вес растёт. Скорость первой загрузки: появляется дополнительный сетевой раунд-трип за remoteEntry перед показом раздела. Отладка: стек трассы идёт через границы сборок, карты исходников надо собирать по нескольким источникам, а воспроизведение бага требует конкретного сочетания версий. Сборка: у каждой части свой конвейер, свои тесты, своя выкладка, и всё это надо поддерживать. Согласованность: обновление React становится проектом на всю компанию, а не задачей на день.</p>
<p>Отдельная статья — качество интерфейса. Единообразие теперь держится не кодом, а договорённостями: три команды независимо делают три разные пустые страницы и три разных обработчика ошибок.</p>
<p>Когда оправдано. Много команд с разными релизными циклами и разной готовностью, части продукта со слабой связью между собой, необходимость держать в проде разные версии разделов, миграция между технологиями с длинным периодом сосуществования. Плюс организационный признак: если для выката своей фичи команде нужно согласовать релиз с тремя другими и это происходит каждую неделю — цена уже платится, просто в другой валюте.</p>
<p>Когда не нужно. Одна команда до десяти-пятнадцати человек. Продукт с сильно связанными экранами и общим состоянием. Требование к минимальному размеру и максимальной скорости первой загрузки. Отсутствие людей на платформенную часть — микрофронтенды требуют выделенного владельца оболочки.</p>

<h4>Почему так</h4>
<p>Разделение на независимо деплоимые части — это перенос стоимости с координации людей на координацию систем. Выгодно, только когда координация людей действительно дорога, то есть при многих командах. При одной команде вы платите за проблему, которой нет.</p>

<h4>Подводные камни</h4>
<ul>
<li>Границы по техническим слоям вместо доменов: получается распределённый монолит, где любое изменение требует деплоя всех частей.</li>
<li>Слишком мелкие модули: десяток удалённых модулей на экран превращают загрузку в водопад запросов.</li>
<li>Микрофронтенды ради возможности использовать разные фреймворки: это редко настоящее требование и всегда дорогая расплата.</li>
<li>Отсутствие владельца оболочки: маршрутизация, авторизация и общие стили становятся ничьими.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Чем заменить, если независимость нужна, а цена высока?»</strong> — монорепозиторий с чёткими границами пакетов и trunk-based разработкой: независимость на уровне кода без рантайм-склейки. <strong>«Как понять, что пора?»</strong> — по времени ожидания релиза: если оно измеряется днями из-за чужих изменений, разделение окупится.</p>`,
    code: `// Распределённый монолит: границы по слоям, а не по доменам
// remotes: { ui: ..., api: ..., state: ... }
// любое изменение фичи требует деплоя трёх модулей — независимости нет

// Границы по доменам: изменение живёт внутри одного модуля
// remotes: { orders: ..., billing: ..., reports: ... }

// Дешёвая альтернатива на одну команду: то же разделение без рантайм-склейки
const Orders = React.lazy(function () { return import('./domains/orders'); });
// один репозиторий, один деплой, один React, отладка обычным способом

// Цена федерации, которую надо назвать цифрами до решения:
// - +1 сетевой запрос за remoteEntry перед первым показом раздела
// - дублирование любой общей зависимости, где не совпали диапазоны
// - N конвейеров сборки вместо одного
// - обновление singleton-зависимости = синхронный релиз всех частей`,
    tip: 'Сильный ход — назвать честный признак: микрофронтенды оправданы, когда командам нужно выкатываться независимо и они уже мешают друг другу. Если разделение обосновывают словами «так масштабируемо» без конкретной боли в релизах, это архитектура ради архитектуры и она вернётся счётом за поддержку.' },

  { id: 'arc20',
    q: 'Как понять, что миграция закончена? И что делать с последними процентами, которые никогда не переезжают?',
    a: `<h4>Коротко</h4>
<p>Миграция закончена, когда старый рантайм больше не грузится ни на одном маршруте и его код удалён из репозитория. Не когда «почти всё переехало». Последние проценты не переезжают, потому что у них нет владельца и пользы — их надо либо явно закрыть, либо перевести в поддерживаемое состояние с датой окончания.</p>

<h4>Как это работает</h4>
<p>Критерий измеряется, а не обсуждается. Метрика — доля запросов, при которых загружается бандл старого приложения. Она снимается на живом трафике, разбита по маршрутам и висит на дашборде. Пока она не ноль, миграция идёт. Дополнительно: число файлов старого кода в репозитории и число зависимостей, которые нужны только ему.</p>
<p>Почему застревает хвост. Разделы, которыми пользуются десять человек в квартал, но эти десять — бухгалтерия или поддержка. Экспорты, печатные формы, интеграции, административные экраны. Их не переносят, потому что перенос не даёт видимой пользы, а сломать их страшно.</p>
<p>Что делать с хвостом. Первое — посчитать использование по каждому маршруту за квартал. Обычно выясняется, что треть разделов не открывали ни разу: их удаляют. Второе — по оставшимся принять явное решение: перенести, заменить на другое (например, отчёт вместо экрана), либо оставить в старой системе и зафиксировать это как поддерживаемое исключение с датой пересмотра. Третье — если хвост остаётся, изолировать его: старое приложение живёт на отдельном поддомене или отдельном маршруте, не грузится на основном пути и не тянет свои зависимости в общий бандл.</p>
<p>Важное правило: пока хвост существует, миграция не считается завершённой и её стоимость продолжает начисляться — два конвейера, две системы стилей, обновления безопасности для старых зависимостей. Это надо показывать цифрой, иначе хвост остаётся навсегда, потому что каждая отдельная задача по нему всегда менее приоритетна, чем продуктовая.</p>

<h4>Почему так</h4>
<p>Основная выгода миграции — не в новом фреймворке, а в удалении старого. Пока старое живёт, вы платите за обе системы и не получаете ни одного из обещанных упрощений. Поэтому финал определяется удалением, а не долей переноса.</p>

<h4>Подводные камни</h4>
<ul>
<li>Считать прогресс по числу перенесённых компонентов: цифра растёт, старый рантайм грузится по-прежнему.</li>
<li>Оставлять старый код после переноса «на всякий случай»: он расходится с новым и попадает в бандл.</li>
<li>Тихий хвост без владельца: никто не знает, работает ли раздел, пока не приходит жалоба.</li>
<li>Забытые старые зависимости: уязвимости приходят на весь проект из мёртвой части.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как выбить время на хвост?»</strong> — через стоимость поддержки: показать, сколько часов в квартал уходит на два конвейера и обновления безопасности. <strong>«Что если раздел нельзя ни перенести, ни убрать?»</strong> — изолировать на отдельном маршруте, заморозить и назначить владельца; главное — сделать исключение явным решением, а не молчаливым состоянием.</p>`,
    code: `// Метрика финала: грузится ли старый рантайм на живом трафике
// маркер ставится в точке загрузки легаси-бандла
function bootLegacy(route) {
  metrics.increment('legacy.boot', { route: route });
  return legacyBootstrap(route);
}

// Разбор хвоста: использование маршрутов за квартал
// SELECT route, count(*) FROM pageviews
// WHERE app = 'legacy' AND ts > now() - interval '90 days'
// GROUP BY route ORDER BY 2;
// нулевые строки -> кандидаты на удаление, а не на перенос

// Изоляция хвоста: не грузится на основном пути
const LEGACY_ONLY = ['/admin/legacy-reports'];
// отдельный маршрут, отдельный чанк, свои зависимости не попадают в общий бандл

// Удаление старого кода — часть тикета переноса, а не отдельная задача «потом»
// - [ ] новый раздел на 100% трафика неделю без роста ошибок
// - [ ] старый маршрут удалён
// - [ ] модули старого приложения удалены
// - [ ] зависимости, ставшие ненужными, удалены из package.json`,
    tip: 'Сильный ответ переводит хвост в решение продукта, а не разработки: список редких разделов с числом пользователей идёт владельцу продукта, и он решает, что закрыть. Разработчик, который сам решает удалить чужой отчёт, получает инцидент; тот же список с цифрами закрывает половину хвоста за одну встречу.' },

  { id: 'arc21',
    q: 'Ты делал Server-Driven UI. Где ты проводишь границу между тем, что описывает схема, и тем, что живёт в коде?',
    a: `<h4>Коротко</h4>
<p>В схеме — состав, порядок, данные и намерения. В коде — реализация компонентов, поведение, анимации, валидация и всё, что требует ветвлений. Схема отвечает на вопрос «что показать», код — «как это выглядит и что происходит при взаимодействии». Как только в схеме появляются условия и выражения, вы изобретаете язык программирования и проигрываете.</p>

<h4>Как это работает</h4>
<p>Практический критерий: если элемент меняется чаще, чем выходит релиз клиента, он идёт в схему. Порядок блоков на экране, набор полей формы, тексты, видимость по правам, конфигурация списка — это данные. Компонент карточки, обработка жестов, поведение при потере фокуса, доступность — это код.</p>
<p>Схема ссылается на компонент по типу из закрытого реестра и передаёт ему пропсы. Движок обходит дерево узлов и вызывает фабрику по типу: неизвестный тип — не ошибка приложения, а место для запасного варианта. Действия описываются как намерения — не «вызови эту функцию», а «открой экран», «отправь форму», «покажи сообщение», — а обработчики намерений живут в коде.</p>
<p>Что не должно попадать в схему никогда: произвольные выражения и код в строках, ссылки на функции по имени, стилевые свойства напрямую (только токены и варианты), бизнес-правила, требующие ветвлений. Условная видимость решается либо на сервере — он собирает схему уже под пользователя, — либо ограниченным набором предикатов над данными формы, а не языком выражений.</p>
<p>У конструктора, который эту схему собирает, ограничения те же: он предлагает только то, что реестр умеет отрисовать, и валидирует результат той же схемой, что и рантайм. Иначе редактор позволяет собрать экран, который клиент показать не может.</p>

<h4>Почему так</h4>
<p>Смысл SDUI — менять интерфейс без релиза клиента. Всё, что для этого не нужно, добавлять в схему вредно: каждая новая возможность схемы — это код в движке, который надо поддерживать во всех версиях клиента одновременно, включая старые. Схема должна быть максимально скучной.</p>

<h4>Подводные камни</h4>
<ul>
<li>Выражения в схеме: сначала одно простое условие, через полгода — интерпретатор без отладчика и типов.</li>
<li>Стили в схеме: приходит запрос на отступ, а через год интерфейс не поддаётся редизайну, потому что значения зашиты в данных на бэке.</li>
<li>Схема как отражение внутренней модели бэка: любое изменение хранилища ломает интерфейс.</li>
<li>Отсутствие запасного варианта на неизвестный тип: старый клиент падает после появления нового блока.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Кто владеет схемой?»</strong> — продукт через конструктор, но набор допустимых типов и их контракты — фронтенд; это два разных права. <strong>«Как быть с состоянием формы?»</strong> — оно клиентское: схема описывает поля и правила, а значения, ошибки и фокус живут в коде, иначе каждое нажатие клавиши превращается в запрос.</p>`,
    code: `// Узел схемы: тип из реестра, пропсы-данные, действия как намерения
{
  "type": "Section",
  "props": { "title": "Доставка", "spacing": "md" },
  "children": [
    { "type": "TextField", "id": "address",
      "props": { "label": "Адрес", "required": true, "maxLength": 200 } },
    { "type": "Button",
      "props": { "label": "Сохранить", "variant": "primary" },
      "action": { "kind": "submitForm", "formId": "delivery" } }
  ]
}

// Реестр закрыт: рендерится только то, что в нём есть
const REGISTRY = { Section: Section, TextField: TextField, Button: Button };

function Node({ node }) {
  const Component = REGISTRY[node.type];
  if (!Component) return <UnknownBlock type={node.type} />;   // запасной вариант
  return (
    <Component {...node.props} action={node.action}>
      {(node.children || []).map(function (c) { return <Node key={c.id} node={c} />; })}
    </Component>
  );
}

// Намерения обрабатываются кодом, а не схемой
const INTENTS = {
  submitForm: function (a, ctx) { return ctx.forms.submit(a.formId); },
  navigate: function (a, ctx) { return ctx.router.push(a.to); },
};`,
    tip: 'Сильный ответ формулирует границу через частоту изменений и через право на релиз: в схему уходит то, что продукт должен менять без участия разработчика. Если для изменения всё равно нужен разработчик, схема просто добавила слой косвенности и ничего не купила.' },

  { id: 'arc22',
    q: 'Как валидируется схема экрана и что произойдёт, если бэкенд пришлёт несовместимое изменение?',
    a: `<h4>Коротко</h4>
<p>Валидация в трёх точках: в конструкторе при сохранении, на сервере перед отдачей и на клиенте перед отрисовкой. Клиентская проверка — не дубль, а последняя линия: она изолирует поломку до одного узла, а не до всего экрана. Несовместимое изменение не должно ронять приложение — оно должно превращаться в деградацию с сообщением в мониторинг.</p>

<h4>Как это работает</h4>
<p>Схема описывается формально — JSON Schema или библиотекой-валидатором в коде, из которой выводятся и типы TypeScript, и проверка в рантайме. Один источник истины: описание, типы и валидатор не должны расходиться, иначе через месяц типы врут.</p>
<p>Валидация поузловая, а не всё-или-ничего. Движок разбирает узел, проверяет по описанию его типа и при ошибке рендерит запасной вариант вместо этого узла, а остальной экран показывает. В деве запасной вариант кричит красным с текстом ошибки, в проде — либо ничего не показывает, либо показывает нейтральную заглушку, но обязательно шлёт событие с типом узла, идентификатором экрана и версией схемы.</p>
<p>Поведение при неизвестном поле — намеренно мягкое: лишние поля игнорируются. Это делает добавление полей на сервере безопасным для старых клиентов. Поведение при отсутствии обязательного поля — жёсткое: узел не рендерится. Асимметрия здесь принципиальна и является основой совместимости.</p>
<p>На сервере схема валидируется той же формой при сохранении из конструктора: невалидная схема не должна попасть в хранилище вообще. Плюс тест совместимости в конвейере бэкенда: набор реальных схем прогоняется через валидатор текущих версий клиента, и падение сборки происходит до выката.</p>
<p>Ещё одна защита — деградация экрана целиком: если сломан корень или доля битых узлов выше порога, показывается запасной экран, собранный клиентом, или предыдущая закешированная схема.</p>

<h4>Почему так</h4>
<p>В SDUI данные напрямую управляют отрисовкой, значит любая ошибка данных — это ошибка интерфейса. В обычном приложении релиз проходит тесты; в SDUI изменение схемы едет в прод мимо тестов клиента. Поэтому клиент обязан быть недоверчивым.</p>

<h4>Подводные камни</h4>
<ul>
<li>Валидация только на сервере: любая ошибка сборки схемы даёт белый экран у пользователя.</li>
<li>Строгий режим на лишние поля: сервер не может добавить ничего, не сломав старых клиентов.</li>
<li>Тихое проглатывание ошибок: экран рендерится наполовину, никто не знает.</li>
<li>Разные описания схемы на бэке и фронте: расходятся молча и обнаруживаются в проде.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Не дорого ли валидировать каждый узел в рантайме?»</strong> — на экране в сотни узлов это единицы миллисекунд; при больших списках проверяется описание элемента один раз, а не каждый экземпляр. <strong>«Как ловить регресс до прода?»</strong> — золотым набором схем: реальные схемы лежат в тестах клиента и прогоняются на каждой сборке.</p>`,
    code: `// Одно описание — из него и типы, и рантайм-проверка
const ButtonNode = z.object({
  type: z.literal('Button'),
  id: z.string(),
  props: z.object({
    label: z.string().max(60),
    variant: z.enum(['primary', 'secondary', 'ghost']).default('primary'),
  }),
  action: ActionSchema.optional(),
}).passthrough();          // лишние поля игнорируем — так безопасно добавлять новое

type ButtonNode = z.infer<typeof ButtonNode>;

// Поузловая изоляция: битый узел не роняет экран
function Node({ raw }) {
  const parsed = SCHEMAS[raw.type]?.safeParse(raw);
  if (!parsed || !parsed.success) {
    report('sdui.node.invalid', {
      type: raw.type, id: raw.id, screen: screenId,
      schemaVersion: version, issues: parsed?.error?.issues,
    });
    return <BrokenNode type={raw.type} />;    // в деве — с текстом ошибки
  }
  return renderNode(parsed.data);
}

// Порог деградации: слишком много битых узлов — показываем запасной экран
if (brokenCount / totalCount > 0.2) return <FallbackScreen cached={lastGoodSchema} />;`,
    tip: 'Назови асимметрию правил как осознанное решение: лишние поля игнорируем, отсутствующие обязательные — жёсткая ошибка узла. Это и есть механизм, который позволяет бэкенду выкатываться раньше клиента, и на интервью его формулируют редко, хотя он и делает SDUI жизнеспособным.' },

  { id: 'arc23',
    q: 'Как версионировать схему SDUI, если старые версии приложения остаются у пользователей?',
    a: `<h4>Коротко</h4>
<p>Клиент сообщает серверу, что он умеет, сервер отдаёт схему, которую этот клиент может отрисовать. Основное правило — только добавление: новые типы и поля не ломают старых, потому что неизвестное игнорируется, а обязательные поля никогда не добавляются задним числом. Мажорная версия схемы — крайняя мера с параллельной отдачей двух форм.</p>

<h4>Как это работает</h4>
<p>Клиент при запросе экрана передаёт версию схемы, которую поддерживает, и — в развитом варианте — список известных ему типов узлов. Сервер по этим данным собирает ответ: если новый блок клиенту неизвестен, он либо не включается, либо заменяется на ближайший поддерживаемый эквивалент. Это дороже в реализации, чем один общий ответ, но снимает главную боль мобильных и долгоживущих веб-клиентов.</p>
<p>Правила эволюции. Добавить новый тип узла — безопасно. Добавить необязательное поле — безопасно. Сделать поле обязательным, удалить тип, поменять смысл значения — ломающее. Ломающее делается через параллельность: новое поле появляется рядом со старым, сервер какое-то время отдаёт оба, клиент читает новое, если умеет, и старое иначе. Удаление старого — только когда доля клиентов, которые его читают, стала пренебрежимой; долю надо мерить, а не угадывать.</p>
<p>Отсюда обязательная телеметрия: распределение версий клиентов по трафику. Без него невозможно ни принять решение об удалении поля, ни объяснить, почему нельзя.</p>
<p>Отдельно — управляемая деградация. Каждый новый тип узла сопровождается указанием, чем его заменить на клиенте, который его не знает: карточка с текстом, ссылка на веб-версию, пропуск. Тогда старый клиент не показывает дыру, а даёт хоть что-то.</p>

<h4>Почему так</h4>
<p>SDUI выигрывает у обычной разработки именно на длинном хвосте клиентов: продукт меняет экран без релиза. Но тот же хвост означает, что в проде одновременно живут все версии за последний год. Совместимость только вперёд обеспечивается свойствами формата, а не расписанием.</p>

<h4>Подводные камни</h4>
<ul>
<li>Одна общая схема на всех клиентов: любое добавление ограничено самым старым живым клиентом.</li>
<li>Переиспользование имени типа с новым смыслом: старые клиенты рисуют старое поведение с новыми данными — самый неприятный класс багов.</li>
<li>Отсутствие данных о версиях: поле удаляют по ощущению и ломают часть пользователей.</li>
<li>Версия только в теле ответа, а не в ключе кеша: клиент показывает схему от предыдущей версии.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Кто отвечает за совместимость?»</strong> — сервер: он знает всех клиентов, клиент не может знать будущее. <strong>«Что если нужен принципиально новый экран?»</strong> — новый идентификатор экрана и новый тип корня, а не мутация существующего: тогда старые клиенты продолжают получать старую сборку и ничего не ломается.</p>`,
    code: `// Клиент объявляет свои возможности, сервер собирает ответ под них
GET /screens/checkout
X-Schema-Version: 3
X-Supported-Nodes: Section,TextField,Button,Banner,Carousel

// Сервер отдаёт то, что клиент может показать, плюс явную деградацию
{
  "schemaVersion": 3,
  "nodes": [
    { "type": "Carousel", "id": "promo",
      "fallback": { "type": "Banner", "props": { "text": "Смотреть акции" } } }
  ]
}

// Только добавление: новое поле рядом со старым, оба отдаются в окне перехода
{ "props": { "label": "Оплатить", "labelRich": { "text": "Оплатить", "icon": "card" } } }

// Клиент читает новое, если умеет, иначе старое
const label = node.props.labelRich && canRender('rich')
  ? renderRich(node.props.labelRich)
  : node.props.label;

// Версия схемы — часть ключа кеша, иначе приедет старая форма
const cacheKey = 'screen:' + screenId + ':v' + SCHEMA_VERSION + ':' + locale;`,
    tip: 'Сильный ход — назвать самый коварный случай: не удаление типа, а переиспользование имени с изменённым смыслом. Удаление ломается заметно и сразу, а тихая смена значения поля даёт неправильно работающий экран у части пользователей, и это выясняется через недели по жалобам.' },

  { id: 'arc24',
    q: 'Интерфейса нет в исходниках — он собран движком по схеме. Как ты его отлаживаешь?',
    a: `<h4>Коротко</h4>
<p>Отладка строится на трёх вещах: каждый узел прослеживается до места в схеме, схема экрана доступна целиком в момент показа, и есть способ подменить её локально. Без этого баг звучит как «на проде у одного клиента кнопка не там», и воспроизвести его нельзя.</p>

<h4>Как это работает</h4>
<p>Прослеживаемость. У каждого узла есть стабильный идентификатор, и движок проставляет его в разметку атрибутом данных вместе с типом и версией схемы. Тогда от элемента в инспекторе браузера есть прямой путь к узлу схемы. Имена компонентов в дереве React берутся из типа узла, а не из общего Node — иначе дерево выглядит как тысяча одинаковых узлов и профилировщик бесполезен.</p>
<p>Доступность схемы. В дев-режиме и по флагу — панель, показывающая исходный JSON экрана, разрешённые узлы, ошибки валидации и время сборки. Плюс кнопка «скопировать схему»: приложенная к тикету схема превращает невоспроизводимый баг в тест.</p>
<p>Подмена. Локальный переопределитель, который берёт схему из файла или из панели вместо сервера. Это то же самое, что запуск компонента в сторибуке, только для схемного интерфейса, и без него разработка блоков превращается в правку данных на бэке.</p>
<p>Наблюдаемость в проде. В каждое событие ошибки и в трассировку добавляются идентификатор экрана, версия схемы, идентификатор её ревизии и тип узла. Тогда всплеск ошибок связывается с конкретной публикацией схемы, а не с релизом приложения — это главная причина, почему в SDUI ошибки не коррелируют с деплоями клиента.</p>
<p>Плюс защита от «кто это изменил»: у каждой публикации схемы есть автор, время и возможность отката на предыдущую ревизию одной кнопкой. В инциденте это ценнее любых логов.</p>

<h4>Почему так</h4>
<p>Схемный интерфейс переносит часть логики из кода в данные, а данные не проходят ревью, тесты и не видны в стеке вызовов. Значит, инструменты наблюдения надо восстанавливать явно: идентификаторы, панель, история публикаций, откат.</p>

<h4>Подводные камни</h4>
<ul>
<li>Ключи React по индексу массива: перестановка блоков в схеме теряет состояние компонентов и даёт мигание.</li>
<li>Отсутствие идентификатора узла в разметке: связать элемент на экране с местом в схеме невозможно.</li>
<li>Все узлы под одним именем компонента: профилировщик и инструменты React бесполезны.</li>
<li>Схема без истории ревизий: непонятно, что изменилось между «работало» и «сломалось».</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как тестировать блоки?»</strong> — истории в сторибуке принимают узел схемы, а не пропсы: тестируется тот же путь, что в проде. <strong>«Как ловить регресс?»</strong> — скриншотные тесты на наборе реальных схем: они ловят ровно то, что не ловят юнит-тесты, — сборку экрана целиком.</p>`,
    code: `// Прослеживаемость: узел находится в инспекторе и ведёт к месту в схеме
function renderNode(node) {
  const Component = REGISTRY[node.type];
  return (
    <div data-node-id={node.id} data-node-type={node.type} data-schema-rev={rev}>
      <Component {...node.props} />
    </div>
  );
}

// Имя компонента в дереве React — из типа узла, иначе всё называется Node
Component.displayName = 'SDUI(' + node.type + ')';

// Ключи по стабильному id узла, никогда по индексу
{children.map(function (c) { return <Node key={c.id} node={c} />; })}

// Локальная подмена схемы вместо правки данных на бэке
if (import.meta.env.DEV && localStorage.getItem('sdui:override')) {
  schema = JSON.parse(localStorage.getItem('sdui:override'));
}

// Контекст в каждой ошибке: ошибки в SDUI не совпадают с деплоями клиента
Sentry.setContext('sdui', { screen: screenId, schemaVersion: version, rev: rev });`,
    tip: 'Сильный ответ упоминает, что в SDUI всплеск ошибок надо соотносить не с релизом приложения, а с публикацией схемы. Поэтому ревизия схемы обязана попадать в каждое событие мониторинга — без этого дежурный ищет причину в коде, который в тот день никто не менял.' },

  { id: 'arc25',
    q: 'Почему нельзя просто рендерить компонент по имени из схемы? Какие тут риски безопасности?',
    a: `<h4>Коротко</h4>
<p>Потому что имя из схемы — это данные, пришедшие извне, а превращение данных в исполняемый код — классическая дыра. Разрешив брать компонент по произвольному имени, вы даёте тому, кто контролирует схему, доступ ко всему, что доступно из кода: скрытым компонентам, отладочным экранам, обходу проверок прав. Правильно — закрытый реестр и запрет на любую динамику.</p>

<h4>Как это работает</h4>
<p>Опасны три конструкции. Первая — поиск компонента в объекте модуля или в глобальном пространстве по строке: любое имя из схемы становится доступным, включая то, что не предназначалось. Вторая — вычисление выражений из схемы: интерпретатор в бандле означает исполнение чужого кода в контексте страницы, то есть доступ к токенам, кукам и запросам от имени пользователя. Третья — проброс произвольных пропсов в узел: так проезжают обработчики событий, вставка сырого HTML, ссылки на javascript-схему, атрибуты стиля с внешними ресурсами.</p>
<p>Правильная конструкция. Реестр — явный объект с конечным списком типов, собранный в коде клиента. Неизвестный тип даёт запасной вариант, а не поиск где-то ещё. Пропсы не растекаются: каждый тип узла описан схемой, лишнее отбрасывается, ссылки проверяются по разрешённым схемам протоколов, строки не превращаются в HTML.</p>
<p>Отдельно — права. Видимость по правам решает сервер, а не схема: если в клиентской схеме есть узел «удалить пользователя», спрятанный флагом, то любой, кто откроет инструменты разработчика, его увидит и нажмёт. Схема, отданная клиенту, публична по определению — исходить надо из этого.</p>
<p>И источник схемы: она приходит с собственного бэкенда по защищённому каналу, а не из любого адреса, и не собирается из фрагментов, пришедших от третьих сторон. Иначе весь разбор превращается в разбор недоверенного ввода без песочницы.</p>

<h4>Почему так</h4>
<p>Схема — недоверенный ввод, даже если её пишут свои: конструктор доступен продукту, схема хранится в базе, база может быть скомпрометирована, а промежуточный сервис — подменён. Безопасность строится на том, что множество исполнимых действий конечно и задано в коде клиента.</p>

<h4>Подводные камни</h4>
<ul>
<li>Свойство для вставки сырого HTML, разрешённое в схеме «для форматирования»: прямой путь к межсайтовому скриптингу.</li>
<li>Ссылка из схемы без проверки протокола: адрес со схемой javascript выполняется по клику.</li>
<li>Разворот всех пропсов узла в элемент: проезжают обработчики и опасные атрибуты.</li>
<li>Скрытие по правам на клиенте: данные уже приехали, скрытие косметическое.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«А если очень нужен форматированный текст?»</strong> — размеченный формат с ограниченным набором узлов и своим рендерером либо очистка санитайзером с белым списком; сырой HTML из схемы — никогда. <strong>«Как ограничить ущерб?»</strong> — политика безопасности контента без inline-скриптов и с ограниченным списком источников: она превращает часть уязвимостей в заблокированный запрос.</p>`,
    code: `// Опасно: имя из данных превращается в компонент
const Component = Components[node.type] || window[node.type];   // произвольный доступ
return <Component {...node} />;                                  // все пропсы насквозь

// Опасно: интерпретатор выражений внутри схемы
if (evaluate(node.visibleIf, data)) { /* исполнение чужого кода на странице */ }

// Безопасно: закрытый реестр, разобранные пропсы, запасной вариант
const REGISTRY = Object.freeze({ Section: Section, Button: Button, Text: Text });

function Node({ raw }) {
  const Component = REGISTRY[raw.type];
  if (!Component) return <UnknownBlock type={raw.type} />;
  const props = SCHEMAS[raw.type].parse(raw).props;   // лишнее отброшено
  return <Component {...props} />;
}

// Ссылки — только разрешённые протоколы
function safeHref(url) {
  try {
    const u = new URL(url, location.origin);
    return ['http:', 'https:', 'mailto:'].includes(u.protocol) ? u.href : undefined;
  } catch (e) { return undefined; }
}

// Права решает сервер: скрытый узел в клиентской схеме виден в devtools`,
    tip: 'Сильный ответ формулирует принцип одной фразой: схема выбирает из конечного множества, заданного кодом, и никогда не расширяет это множество. Всё, что нарушает принцип — поиск по имени, выражения, сырой HTML, — это исполнение данных, и оно эквивалентно eval, как бы аккуратно ни выглядело.' },

  { id: 'arc26',
    q: 'В каких случаях Server-Driven UI обойдётся дороже, чем просто написать экран руками?',
    a: `<h4>Коротко</h4>
<p>Дороже почти всегда, если экранов мало, если они меняются редко, если у них сложное поведение или если менять их всё равно будет разработчик. SDUI окупается на большом количестве однотипных экранов, которые меняет продукт без релиза. Во всех остальных случаях вы платите движком, конструктором, схемой и потерей инструментов — а получаете обычный экран.</p>

<h4>Как это работает</h4>
<p>Что входит в стоимость, кроме движка. Конструктор с предпросмотром и правами. Хранилище схем с версиями, ревизиями и откатом. Валидация в трёх точках. Инструменты отладки: панель, идентификаторы, подмена. Отдельный процесс публикации — по сути релизный конвейер для данных, с ревью и раскаткой. Обучение продуктовой команды. И постоянный налог: любой новый блок теперь делается не как компонент, а как компонент плюс тип узла плюс правила плюс миграция старых схем.</p>
<p>Что вы теряете. Типы на границе схемы слабее, чем в обычном коде. Стек вызовов перестаёт указывать на причину. Обычные тесты покрывают движок, а не результат. Изменение интерфейса больше не проходит ревью кода. Производительность падает: сборка дерева из данных дороже статического JSX, а ленивая загрузка сложнее, потому что состав экрана известен только в рантайме.</p>
<p>Признаки, что SDUI не нужен: экранов десятки, а не сотни; экраны разные по логике; сложные формы с зависимостями между полями; тяжёлое взаимодействие — перетаскивание, холст, редакторы; частота изменений раз в квартал; менять схему всё равно будет разработчик.</p>
<p>Признаки, что нужен: витрины, ленты, промо, онбординги, анкеты; много вариантов одного экрана под сегменты; мобильные клиенты с долгим циклом обновления; продуктовая команда, которой нужны эксперименты без релиза.</p>
<p>Промежуточный вариант, который часто лучше обоих: схемой описывается только часть экрана — блок промо, набор полей анкеты, — а остальное остаётся кодом. Так вы получаете гибкость там, где она нужна, не платя за неё на всём приложении.</p>

<h4>Почему так</h4>
<p>SDUI — это построение собственной платформы. У платформы фиксированная стоимость, которая не зависит от числа экранов, и выгода, которая от него зависит линейно. Пересечение наступает не сразу, и его надо оценивать до старта.</p>

<h4>Подводные камни</h4>
<ul>
<li>SDUI ради одного экрана «на будущее»: движок остаётся, гибкость не используется.</li>
<li>Постепенное превращение схемы в язык: сначала условия, потом циклы, потом выражения.</li>
<li>Конструктор без валидации и предпросмотра: продукт собирает экраны, которые клиент не рисует.</li>
<li>Отсутствие процесса публикации: правка схемы едет в прод без ревью и без отката.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как оценить окупаемость?»</strong> — числом изменений интерфейса в квартал, которые сейчас требуют релиза, умноженным на стоимость релиза; если это меньше стоимости платформы за год, не начинайте. <strong>«Что вместо?»</strong> — конфигурация вместо схемы: набор параметров для готовых экранов покрывает большинство запросов и стоит на порядок дешевле.</p>`,
    code: `// Промежуточный вариант: схемой описан один блок, остальное — обычный код
function CheckoutPage() {
  const promo = usePromoSchema();      // управляется продуктом без релиза
  return (
    <Layout>
      <DeliveryForm />                 {/* сложное поведение — код */}
      <SduiRegion schema={promo} />    {/* витрина — схема */}
      <PaymentSection />               {/* деньги — код и ревью */}
    </Layout>
  );
}

// Конфигурация вместо схемы закрывает большинство запросов дешевле
{
  "checkout": {
    "showPromoBanner": true,
    "fieldsOrder": ["address", "date", "comment"],
    "paymentMethods": ["card", "sbp"]
  }
}

// Стоимость платформы, которую надо назвать до старта:
// движок + конструктор + хранилище ревизий + валидация в 3 точках
// + панель отладки + процесс публикации с ревью и откатом
// + миграции схем при каждом изменении контракта узла`,
    tip: 'Сильный ход — предложить обратный порядок: начать с конфигурации готовых экранов и перейти к схеме, только когда конфигурация перестанет покрывать запросы. Так вы платите за платформу тогда, когда уже видите спрос, а не строите её на предположении о будущей гибкости.' },

  { id: 'arc27',
    q: 'Feature-Sliced Design: какие проблемы он решает и чем ты за это платишь?',
    a: `<h4>Коротко</h4>
<p>FSD решает одну проблему — неконтролируемую связность в большом приложении. Он вводит слои с односторонними зависимостями и режет код по фичам, а не по техническим типам. Платите вы бюрократией: больше папок, больше файлов-реэкспортов, споры о том, куда положить модуль, и накладные расходы, которые на маленьком проекте не окупаются.</p>

<h4>Как это работает</h4>
<p>Слои сверху вниз: app, pages, widgets, features, entities, shared. Правило одно и жёсткое: слой импортирует только из слоёв ниже. Внутри слоя модули не импортируют друг друга напрямую — связь идёт через слой выше, который их складывает.</p>
<p>Второй элемент — публичный интерфейс среза. Каждый модуль экспортирует наружу ровно то, что решил экспортировать, а внутренности недоступны. Это то же самое, что делает пакет, но без публикации: границы существуют внутри одного репозитория.</p>
<p>Что это чинит на практике. Пропадает папка components на восемьсот файлов, где невозможно понять, что чем пользуется. Пропадают циклы импортов между «фича A импортирует из фичи B, а та обратно». Удаление фичи становится удалением папки, а не археологией. И появляется общий словарь: спор «куда положить» превращается в применение правила.</p>
<p>Чем платите. Односторонние зависимости иногда требуют лишнего слоя: два виджета должны обменяться, и приходится поднимать связь на страницу. Файлов больше: у каждого среза свой публичный интерфейс. Появляется постоянный вопрос, что такое сущность и что такое фича, и на него нет объективного ответа — команда договаривается. Плюс на маленьком приложении структура выглядит как оверхед, потому что она и есть оверхед: она окупается на объёме и на числе людей.</p>
<p>Держится всё это не силой воли, а линтером: правило импортов проверяет направление зависимостей и запрет на обращение к внутренностям. Без автоматической проверки структура размывается за квартал.</p>

<h4>Почему так</h4>
<p>Разделение по техническим типам — components, hooks, utils — не даёт границ: любой файл может импортировать любой. Разделение по фичам даёт границы, совпадающие с тем, как приходят задачи, поэтому изменение задевает меньше кода, а удаление возможно.</p>

<h4>Подводные камни</h4>
<ul>
<li>Слой shared превращается в свалку: туда попадает всё, что не подошло, и он тянет за собой полприложения.</li>
<li>Формальное соблюдение папок без соблюдения направления импортов — структура есть, связность прежняя.</li>
<li>Дробление на слишком мелкие срезы: на каждый чих папка с четырьмя файлами.</li>
<li>Реэкспорты в каждом срезе бьют по тряске дерева, если внутри есть побочные эффекты.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Обязательно ли именно FSD?»</strong> — нет; важны односторонние зависимости и публичные интерфейсы, а конкретные названия слоёв вторичны. <strong>«Как мигрировать существующий проект?»</strong> — по одной новой фиче: новое пишется по правилам, старое переносится при касании; массовое перекладывание файлов даёт огромный дифф и нулевую пользу.</p>`,
    code: `// Направление зависимостей проверяется линтером, а не договорённостью
// .eslintrc — import/no-restricted-paths
{
  "zones": [
    { "target": "./src/entities", "from": "./src/features" },
    { "target": "./src/entities", "from": "./src/widgets" },
    { "target": "./src/features", "from": "./src/widgets" },
    { "target": "./src/shared",   "from": "./src/entities" }
  ]
}

// Публичный интерфейс среза: наружу только то, что объявлено
// src/features/order-cancel/index.ts
export { CancelOrderButton } from './ui/CancelOrderButton';
export { useCancelOrder } from './model/useCancelOrder';
// ./model/store и ./api наружу не выходят

// Запрет на обход публичного интерфейса
// no-restricted-imports: "src/features/*/!(index)"
import { store } from 'features/order-cancel/model/store';   // ошибка линтера

// Обмен между срезами одного слоя — через слой выше, а не напрямую
// widgets/checkout собирает features/delivery и features/payment`,
    tip: 'Сильный ответ отделяет цель от буквы: ценность в односторонних зависимостях и явных публичных интерфейсах, а не в названиях папок. Команда, которая перенесла файлы по слоям, но не включила проверку направления импортов, получила только новые пути и ни одной решённой проблемы.' },

  { id: 'arc28',
    q: 'Когда монорепозиторий оправдан? Расскажи про кеширование сборок и границы пакетов.',
    a: `<h4>Коротко</h4>
<p>Монорепозиторий оправдан, когда код часто меняется вместе: дизайн-система и продукты, общие типы и контракты, несколько приложений одной команды. Он даёт атомарные изменения через границы пакетов и общий конвейер, но требует инструментов: без графа зависимостей, кеша сборок и запуска только затронутого он превращается в медленную сборку всего на каждый коммит.</p>

<h4>Как это работает</h4>
<p>Основа — граф. Инструмент читает зависимости пакетов и строит граф, из которого выводит две вещи: порядок выполнения задач и множество затронутых пакетов. Затронутость считается от изменённых файлов вверх по графу: изменили пакет токенов — пересобрать надо его и всех, кто от него зависит, но не соседние приложения.</p>
<p>Кеш. Для каждой задачи считается хеш от входов: содержимое файлов пакета, его конфигурация, версия инструментов, переменные окружения, хеши задач-зависимостей. Если такой хеш уже собирался, результат берётся из кеша — локального или общего для команды и конвейера. Общий кеш — то, ради чего это делается: сборка, уже выполненная на конвейере, не повторяется у разработчика.</p>
<p>Чтобы кеш работал, задачи обязаны быть детерминированными: никакой текущей даты в артефактах, никакого обращения к сети во время сборки, точное перечисление входов и выходов. Одна недекларированная зависимость на файл вне пакета — и кеш начинает врать: возвращает старый результат при изменившемся входе.</p>
<p>Границы пакетов. Пакет — это единица владения и версии. Правила: явные зависимости в package.json, никаких импортов по относительным путям через границу пакета, никакого доступа к внутренним файлам чужого пакета. Проверяется линтером и структурой полей exports.</p>
<p>Когда не нужен: независимые продукты разных команд без общего кода, жёсткие требования к раздельному доступу, огромная история, с которой инструменты работают медленно.</p>

<h4>Почему так</h4>
<p>Главная выгода монорепозитория — атомарность: изменить контракт и всех потребителей одним коммитом, который проходит один конвейер. В отдельных репозиториях то же самое превращается в цепочку релизов и обновлений зависимостей, растянутую на дни.</p>

<h4>Подводные камни</h4>
<ul>
<li>Сборка всего на каждый коммит: без вычисления затронутого конвейер растёт линейно с числом пакетов.</li>
<li>Недекларированные входы задач: кеш отдаёт устаревший результат, и баг выглядит мистическим.</li>
<li>Циклические зависимости между пакетами: граф перестаёт давать порядок.</li>
<li>Один общий package.json на всё: границы исчезают, и монорепозиторий превращается в большой монолит с папками.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Как версионировать пакеты внутри?»</strong> — либо общей версией для всего, либо независимо через changesets; выбор зависит от того, публикуются ли пакеты наружу. <strong>«Что с внешними потребителями?»</strong> — публикация из монорепозитория обычной командой сборки; внутренние пакеты при этом остаются неопубликованными.</p>`,
    code: `// Задача объявляет входы, выходы и зависимости — иначе кеш врёт
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],          // сначала собрать зависимости
      "inputs": ["src/**", "tsconfig.json", "package.json"],
      "outputs": ["dist/**"],
      "env": ["NODE_ENV"]               // окружение — часть ключа кеша
    },
    "test": { "dependsOn": ["build"], "inputs": ["src/**", "tests/**"] }
  }
}

// Конвейер собирает только затронутое, а не всё
// turbo run build test --filter=...[origin/main]

// Границы пакетов: явные зависимости и никаких путей насквозь
import { Button } from '@company/ui';                 // так
import { Button } from '../../../packages/ui/src/Button';  // запрещено линтером

// Недетерминированная сборка ломает кеш молча
const BUILD_TIME = new Date().toISOString();   // артефакт разный при тех же входах`,
    tip: 'Сильный ответ отделяет монорепозиторий от монолита: монорепозиторий — про то, где лежит код и как он собирается, монолит — про то, как он развёрнут. Из монорепозитория прекрасно выкатываются независимые приложения, а из нескольких репозиториев легко собирается связанный клубок.' },

  { id: 'arc29',
    q: 'Как ввести архитектурный стандарт в нескольких командах так, чтобы его действительно приняли?',
    a: `<h4>Коротко</h4>
<p>Стандарт принимают, когда он решает боль, которую команды признают своей, когда его писали вместе с ними и когда следовать ему дешевле, чем не следовать. Спущенный сверху документ не работает: его формально одобряют и продолжают писать по-старому. Порядок такой — боль, пилот, правила, автоматизация, и только потом обязательность.</p>

<h4>Как это работает</h4>
<p>Шаг первый — назвать боль цифрой. Не «у нас нет единого подхода», а «три команды написали три клиента к одному API, и на прошлом инциденте один из них не имел ретраев». Пока боли нет, стандарт выглядит как чужая работа.</p>
<p>Шаг второй — пилот. Правило обкатывается на одной команде и на реальной задаче. Половина формулировок после этого меняется, и это нормально: правило, не проверенное на практике, обязательно содержит пункт, который невозможно выполнить.</p>
<p>Шаг третий — записать в виде решений, а не свода. Короткая запись: контекст, варианты, выбор, последствия, дата. Одна страница на одно решение. Это принципиально: сводом невозможно ни спорить, ни пересматривать, а решение можно отменить, написав следующее.</p>
<p>Шаг четвёртый — автоматизировать. Правило, которое не проверяется линтером, шаблоном проекта или тестом, существует только в голове самого дисциплинированного. Всё, что можно проверить машиной, проверяется машиной; ревью остаётся для того, что машиной не проверяется.</p>
<p>Шаг пятый — дать дорожку. Генератор нового модуля по стандарту, готовая конфигурация, миграционный кодмод. Стандарт с инструментом принимают, стандарт без инструмента обсуждают.</p>
<p>И обратная связь: у правил есть владелец, у команд есть способ его оспорить, у каждого правила есть дата пересмотра. Правило, которое нельзя оспорить, обходят молча.</p>

<h4>Почему так</h4>
<p>Архитектурный стандарт — это ограничение чужой свободы, и оно принимается только в обмен на выгоду. Команда согласится на ограничение, если оно снимает её проблему или экономит её время; в остальных случаях она подчиняется формально и обходит по существу.</p>

<h4>Подводные камни</h4>
<ul>
<li>Стандарт объёмом в сорок страниц: его не читают, значит его нет.</li>
<li>Правила без причин: через полгода никто не помнит, почему нельзя, и правило нарушают из здравого смысла.</li>
<li>Введение задним числом ко всему коду: команды получают сотни ошибок линтера и отключают его.</li>
<li>Отсутствие исключений: жёсткое правило без процедуры исключения ломается на первом же особом случае и теряет силу целиком.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Что делать с командой, которая не следует?»</strong> — сначала выяснить, почему: обычно правило мешает их сценарию, и это ценная обратная связь; санкции — последнее средство и работают плохо. <strong>«Как не превратиться в полицию?»</strong> — перенести проверку в инструменты: линтер спорит без эмоций, человек — с эмоциями.</p>`,
    code: `// Решение как короткая запись, а не свод правил
// docs/decisions/012-data-fetching.md
// # Клиентские запросы — только через общий слой
// Контекст: три реализации, у одной нет ретраев, инцидент 12.03
// Варианты: свой fetch / TanStack Query / общая обёртка над fetch
// Решение: общая обёртка + TanStack Query, отдельный пакет @company/api
// Последствия: единые ретраи и таймауты; переезд трёх команд, кодмод в комплекте
// Пересмотр: через 2 квартала

// Правило, проверяемое машиной, а не памятью ревьюера
// no-restricted-imports
{ "paths": [{ "name": "axios", "message": "Только @company/api — там ретраи и трассировка" }] }

// Дорожка вместо требования: генератор нового модуля по стандарту
// npx @company/create module orders
//   -> структура, конфиг, тесты, линтер, пайплайн — уже по правилам

// Введение постепенное: новое строго, старое предупреждением
{ "overrides": [{ "files": ["src/legacy/**"], "rules": { "import/no-restricted-paths": "warn" } }] }`,
    tip: 'Хороший ход — назвать формат записи решений и то, что у каждого правила есть дата пересмотра. Это снимает главное возражение команд: они соглашаются не навсегда, а до следующего пересмотра, и спорить с временным ограничением психологически легче, чем с вечным.' },

  { id: 'arc30',
    q: 'Как удержать архитектурные границы в коде, когда команда растёт? Что реально делают линтеры и правила импортов?',
    a: `<h4>Коротко</h4>
<p>Границы держатся тем, что нарушение не проходит сборку. Основные инструменты — правила направления импортов, публичные интерфейсы модулей через поле exports, запрет на импорт внутренностей и владельцы файлов. Ревью границы не удерживает: человек пропускает, торопится и не помнит всех правил, а линтер не пропускает никогда.</p>

<h4>Как это работает</h4>
<p>Четыре уровня защиты. Первый — направление зависимостей: правило описывает зоны и запрещает импорт снизу вверх и между соседями. Второй — публичный интерфейс: пакет объявляет поле exports, и обращение к внутреннему файлу не разрешается ни сборщиком, ни компилятором; внутри одного приложения тот же эффект даёт запрет на импорт мимо файла-входа. Третий — точечные запреты: конкретный пакет только через свою обёртку, прямой доступ к хранилищу только из слоя данных. Четвёртый — владельцы файлов: изменение в общем коде требует ревью владельца.</p>
<p>Отдельно — циклы. Циклические импорты дают неопределённый порядок вычисления модулей и вылезают как undefined при загрузке, причём только в некоторых сборках. Проверка на циклы должна стоять в конвейере, а не находиться руками во время инцидента.</p>
<p>Как вводить в существующий код. Правило включается на предупреждение, снимается срез нарушений в файл исключений, новые нарушения запрещаются жёстко, исключения разбираются по мере касания кода. Резкое включение на ошибку в большом проекте заканчивается массовым отключением правила комментариями, и это хуже, чем не включать вообще.</p>
<p>И к каждому правилу — сообщение с причиной и с тем, что делать вместо. Ошибка «импорт запрещён» без объяснения вызывает поиск способа обойти; ошибка с текстом «используй общий клиент, в нём ретраи и трассировка» вызывает правильное действие.</p>

<h4>Почему так</h4>
<p>Архитектура — это набор ограничений, а неподкреплённое ограничение с ростом команды размывается: каждый новый человек не знает контекста, а срочная задача всегда важнее чистоты. Автоматическая проверка переносит правило из области памяти в область фактов и делает его одинаковым для всех, включая автора правила.</p>

<h4>Подводные камни</h4>
<ul>
<li>Правила без сообщений: команда учится обходить, а не следовать.</li>
<li>Тысяча нарушений при включении: правило отключают целиком.</li>
<li>Проверка только локально: без запуска в конвейере она необязательна.</li>
<li>Линтер вместо разговора: правило, с которым команда не согласна, будет обойдено другим способом — через реэкспорт или динамический импорт.</li>
</ul>

<h4>Что спросят следом</h4>
<p><strong>«Линтер же можно отключить комментарием?»</strong> — можно, поэтому отключения в чувствительных зонах запрещаются отдельным правилом и требуют ревью владельца. <strong>«Что делать с производительностью проверок?»</strong> — запускать только на изменённых файлах локально и полностью в конвейере с кешем.</p>`,
    code: `// Направление зависимостей: запрещаем импорт снизу вверх и между соседями
// import/no-restricted-paths
{
  "zones": [
    { "target": "./src/shared", "from": "./src/features",
      "message": "shared не знает про фичи — вынеси общее вниз или подними код выше" },
    { "target": "./src/features/a", "from": "./src/features/b",
      "message": "Фичи не импортируют друг друга — собери их в виджете выше" }
  ]
}

// Публичный интерфейс пакета: внутренности недоступны снаружи
{
  "exports": {
    ".": "./dist/index.js",
    "./Button": "./dist/Button/index.js"
  }
}
// import { internal } from '@company/ui/dist/Button/internal'  -> ошибка резолва

// Точечный запрет с объяснением, что делать вместо
{ "paths": [{ "name": "node-fetch",
  "message": "Только @company/api: там таймауты, ретраи и заголовки трассировки" }] }

// Циклы ловим в конвейере, а не в инциденте
// npx madge --circular --extensions ts,tsx src/

// Постепенное введение: срез нарушений заморожен, новые запрещены
// eslint --format json > .eslint-baseline.json`,
    tip: 'Сильный ответ добавляет, что линтер удерживает границу, но не создаёт её: если правило постоянно мешает, значит границы проведены неправильно, и чинить надо архитектуру, а не список исключений. Растущий файл исключений — это метрика ошибочной декомпозиции, и читать его надо именно так.' }
];
