const DECK_TS_EXTRA = [
  { id: 'tsx1',
    q: 'Что такое user-defined type guard и чем он опасен?',
    snippet: `function isUser(v: unknown): v is User {
  return true; // компилируется без единой жалобы
}

declare const raw: unknown;
if (isUser(raw)) raw.name.trim();`,
    a: `<h4>Коротко</h4>
    <p>Type guard — функция, у которой возвращаемый тип объявлен как предикат <code>x is T</code>. Компилятор верит предикату на слово и не проверяет, доказывает ли его тело хоть что-нибудь. По надёжности это тот же <code>as</code>, только собранный в одну функцию, которую можно покрыть тестами.</p>

    <h4>Как это работает</h4>
    <p>Сигнатура <code>function isUser(v: unknown): v is User</code> связывает результат вызова с конкретным параметром. На месте вызова control flow analysis видит <code>if (isUser(v))</code> и в true-ветке сужает <code>v</code> до <code>User</code>, в false-ветке применяет <code>Exclude</code> к объявленному типу (для <code>unknown</code> вычесть нечего, остаётся <code>unknown</code>). От тела функции компилятор требует ровно одного: чтобы оно возвращало <code>boolean</code>. Безусловный <code>return true</code> из сниппета проходит проверку, и вызов <code>raw.name.trim()</code> компилируется, а падает уже в браузере.</p>
    <p>В рантайме от предиката не остаётся ничего: в JS уезжает обычная функция, возвращающая булево. Сужение существует только в модели компилятора.</p>
    <p>С TS 5.5 работает <strong>вывод предикатов</strong>: если у функции нет явной аннотации возвращаемого типа, она состоит из одного <code>return</code> с булевым выражением над параметром и параметр нигде не переприсваивается — компилятор сам выводит <code>x is T</code>. Поэтому <code>['a', null].filter((x) =&gt; x !== null)</code> с 5.5 даёт <code>string[]</code>, а до 5.5 давало <code>(string | null)[]</code>.</p>

    <h4>Почему так</h4>
    <p>Доказать, что произвольный JS действительно устанавливает принадлежность типу, — работа верификатора, а не тайпчекера: пришлось бы понимать семантику всех операций и всех вызовов внутри. TS выбрал дешёвый обмен: разработчик берёт на себя ответственность, компилятор взамен получает право сужать. Цена — предикат молча расходится с моделью при рефакторинге.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Добавили в <code>User</code> обязательное поле — предикат, проверяющий только <code>'id' in v</code>, продолжает врать, и ошибка вылезет далеко от места проверки.</li>
    <li><code>typeof v === 'object'</code> не исключает <code>null</code> и не исключает массивы — самый частый дефект самописных предикатов.</li>
    <li>Явная аннотация <code>: boolean</code> глушит вывод предиката из 5.5: чтобы он сработал, тип возврата писать нельзя.</li>
    <li>Предикат сужает конкретный параметр, а не выражение: <code>isUser(a.b)</code> сужает путь <code>a.b</code>, но сужение слетит после любого вызова, который компилятор не может проанализировать.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем заменить на границе с сетью?</strong> Схемой валидации: у zod и valibot предикат порождается из той же декларации, что и тип (<code>z.infer</code>), поэтому разойтись они не могут. <strong>Есть ли форма для методов?</strong> Да, <code>this is T</code> — им типизируют такие проверки, как <code>isLoaded()</code> у модели.</p>`,
    code: `// предикат, который не врёт: проверяем ровно то, что обещаем
function isUser(v: unknown): v is User {
  return typeof v === 'object' && v !== null
    && 'id' in v && typeof (v as { id: unknown }).id === 'string';
}

// TS 5.5: аннотации нет — предикат выводится сам
const notNull = (x: string | null) => x !== null; // (x) => x is string
const clean = ['a', null].filter(notNull);        // string[]

const typed = (x: string | null): boolean => x !== null;
const dirty = ['a', null].filter(typed);          // (string | null)[] — вывод заглушен`,
    tip: 'Формулировка «предикат — это as с человеческим лицом» плюс уточнение, что с 5.5 явная аннотация : boolean ломает автовывод предиката.' },

  { id: 'tsx2',
    q: 'Что такое assertion function и чем она отличается от type guard?',
    snippet: `const assertDefined = <T,>(v: T): asserts v is NonNullable<T> => {
  if (v == null) throw new Error('expected value');
};

const el = document.querySelector('#root');
assertDefined(el); // почему здесь ошибка компиляции?`,
    a: `<h4>Коротко</h4>
    <p>Assertion function объявляется как <code>asserts x is T</code> или <code>asserts x</code>, ничего не возвращает и либо бросает исключение, либо сужает переменную <strong>для всего кода ниже вызова</strong>. Type guard сужает только внутри ветки <code>if</code>, assertion — до конца области видимости.</p>

    <h4>Как это работает</h4>
    <p>Компилятор трактует вызов как узел control flow с двумя исходами: либо функция выбросила и поток прервался, либо утверждение истинно. Второй исход и записывается в таблицу сужений. Тип возврата <code>asserts …</code> означает «возврата в нормальном смысле нет» — это близкий родственник <code>never</code>, и функция обязана быть объявлена без явного <code>return</code> значения.</p>
    <p>Сниппет не компилируется, и это специально: TS требует, чтобы у цели вызова было <strong>явно объявленное имя с явным типом</strong>. Стрелка, присвоенная в <code>const</code> без аннотации, этого условия не выполняет — тип выведен, а не написан, и появляется ошибка «Assertions require every name in the call target to have an explicit type annotation». Лечится двумя способами: переписать в <code>function</code>-декларацию или проставить <code>const assertDefined: &lt;T&gt;(v: T) =&gt; asserts v is NonNullable&lt;T&gt;</code> руками.</p>
    <p>В рантайме от <code>asserts</code> не остаётся ничего, кроме самой функции с её <code>throw</code>. Ни один байт проверки не генерируется компилятором — работает только то, что вы написали в теле.</p>

    <h4>Почему так</h4>
    <p>Требование явной аннотации существует потому, что вывод типов в TS ленивый и в общем случае циклический: чтобы понять, влияет ли вызов на поток управления, чекеру нужен тип вызываемого <strong>до</strong> анализа потока, а вывод типа мог бы, наоборот, зависеть от результата анализа. Явная аннотация разрывает этот цикл. Плата — эргономика: <code>invariant</code>, написанный как стрелка, внезапно не работает.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Импорт через namespace (<code>utils.assertDefined(x)</code>) требует явного типа у каждого имени в пути вызова, включая сам <code>utils</code>.</li>
    <li>Сужение не переживает деструктуризацию: <code>assertDefined(obj.a)</code> сужает <code>obj.a</code>, но не ранее вытащенную <code>const { a }</code>.</li>
    <li>Как и предикат, тело не верифицируется: <code>asserts v is string</code> с пустым телом молча пропустит что угодно.</li>
    <li>В продовой сборке соблазн вырезать <code>invariant</code> терсером: тогда типы обещают то, чего в рантайме больше не проверяется.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем отличается <code>asserts x</code> без <code>is</code>?</strong> Это утверждение истинности: сужает <code>x</code> по truthiness, годится для <code>invariant(cond)</code>. <strong>Как типизировать exhaustive switch?</strong> Через <code>function assertNever(x: never): never</code> в <code>default</code>: добавили вариант в union — получили ошибку компиляции в этой строке.</p>`,
    code: `// function-декларация — требование про explicit type выполняется само
function assertDefined<T>(v: T, msg?: string): asserts v is NonNullable<T> {
  if (v == null) throw new Error(msg ?? 'expected value');
}

const el = document.querySelector('#root'); // Element | null
assertDefined(el);
el.classList.add('ok');                     // Element — сужение живёт до конца блока

function assertNever(x: never): never { throw new Error('unhandled: ' + String(x)); }`,
    tip: 'Назовите ошибку «Assertions require every name in the call target to have an explicit type annotation» и её причину — циклическую зависимость вывода типа и анализа потока.' },

  { id: 'tsx3',
    q: 'Какими встроенными способами сужается тип и где у каждого ловушки?',
    snippet: `function f(x: string | number | null | undefined) {
  if (typeof x === 'object') { /* какой тип у x? */ }
  if (x) { /* а здесь? */ }
}`,
    a: `<h4>Коротко</h4>
    <p>Сужение — это control flow analysis: компилятор строит граф потока и на каждом узле держит текущий тип переменной. Источники сужения — <code>typeof</code>, <code>instanceof</code>, <code>in</code>, сравнение с литералом, <code>Array.isArray</code>, truthiness, дискриминант union и пользовательские предикаты. Ловушки у каждого свои и повторяют кривизну самого JS.</p>

    <h4>Как это работает</h4>
    <p>В сниппете первый <code>if</code> сузит <code>x</code> до <code>null</code>: <code>typeof null === 'object'</code>, а других объектных членов в union нет. Второй сузит до <code>string | number</code>, но <strong>не</strong> исключит пустую строку и ноль — они просто проходят по falsy-ветке вместе с <code>null</code> и <code>undefined</code>.</p>
    <p>Механически чекер для каждого условия вычисляет пару «тип в true-ветке / тип в false-ветке», раскладывая union на члены и оставляя те, что совместимы с проверкой. На слиянии ветвей (после <code>if</code>, в <code>catch</code>, на входе в цикл) типы объединяются обратно в union. Всё это существует только при компиляции: в JS уезжают ровно те <code>typeof</code>, что вы написали.</p>

    <h4>Почему так</h4>
    <p>TS обязан описывать существующий JS, а не идеальный язык: <code>typeof null</code> — баг первой реализации 1995 года, зафиксированный спецификацией навсегда. Поэтому сужение честно моделирует рантайм со всеми его странностями, а не то, что кажется логичным.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>typeof x === 'object'</code> не исключает <code>null</code> и не исключает массивы — нужна пара <code>x !== null &amp;&amp; !Array.isArray(x)</code>.</li>
    <li>Truthiness съедает <code>''</code>, <code>0</code>, <code>0n</code>, <code>NaN</code>: <code>if (count)</code> и <code>if (count !== undefined)</code> — разные проверки, и на нуле поведение расходится.</li>
    <li><code>instanceof</code> ломается через границу realm (iframe, worker, vm): объект из другого окна не пройдёт <code>instanceof Array</code>. Спасает <code>Array.isArray</code>, у которого проверка кросс-realm.</li>
    <li><code>in</code> сужает по наличию ключа и не отличает «ключа нет» от «ключ равен <code>undefined</code>». С TS 4.9 <code>in</code> ещё и добавляет ключ в тип для <code>unknown</code>-подобных значений, что делает его пригодным для валидации.</li>
    <li>Сужение слетает после любого вызова функции, если переменная — <code>let</code> или свойство объекта: компилятор не делает escape-анализ.</li>
    <li>При <code>useUnknownInCatchVariables</code> (входит в <code>strict</code>) <code>catch (e)</code> даёт <code>unknown</code>, и <code>e.message</code> требует сужения через <code>instanceof Error</code>.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что надёжнее для внешних данных?</strong> Дискриминированный union со строковым полем-тегом: сужение по литералу — единственный способ, который компилятор проверяет строго и который не ломается при добавлении полей. <strong>Почему <code>Array.isArray</code> на <code>readonly T[]</code> иногда даёт <code>any[]</code>?</strong> Из-за сигнатуры в старых lib.d.ts; в свежих версиях перегрузка для <code>ReadonlyArray</code> добавлена.</p>`,
    code: `declare const x: unknown;

if (typeof x === 'object' && x !== null && !Array.isArray(x)) {
  // x: object — null и массивы отсечены
}

type Res = { ok: true; data: string } | { ok: false; error: string };
function show(r: Res) {
  if (r.ok) r.data;   // сужение по дискриминанту — самый надёжный вариант
  else r.error;
}`,
    tip: 'Сильная формулировка: сужение — статическая модель потока управления, она консервативна везде, где не может доказать неизменность значения.' },

  { id: 'tsx4',
    q: 'Почему тип «разсуживается» внутри колбэка и как с этим жить?',
    snippet: `let v: string | null = get();
if (v !== null) {
  setTimeout(() => v.length, 0);   // Object is possibly 'null'
}

const c: string | null = get();
if (c !== null) {
  setTimeout(() => c.length, 0);   // ошибки нет
}`,
    a: `<h4>Коротко</h4>
    <p>Сужение держится на допущении «значение не изменилось с момента проверки». Для <code>let</code> и для свойств объекта компилятор доказать это через границу функции не может: колбэк выполнится когда угодно, а переприсвоить переменную может любой код. Поэтому внутри стрелки тип откатывается к объявленному.</p>

    <h4>Как это работает</h4>
    <p>При анализе тела функции-выражения чекер смотрит на <strong>вид объявления</strong> захваченной переменной. Если это <code>const</code> (или <code>readonly</code>-поле, или параметр, который нигде не переприсваивается), значение неизменно по построению — сужение из внешней области переносится внутрь. Если это <code>let</code> или <code>var</code>, чекер должен был бы доказать, что между проверкой и вызовом колбэка присваивания не произошло. Это требует межпроцедурного анализа, которого в TS нет, поэтому применяется консервативное правило: внутри функции берём объявленный тип.</p>
    <p>Отсюда рецепт из сниппета: та же проверка над <code>const c</code> работает, потому что <code>c</code> нельзя переприсвоить в принципе. Со свойствами хуже — <code>obj.value</code> разсуживается уже после <strong>любого вызова функции</strong> в том же блоке, потому что чекер не отслеживает алиасинг: другая ссылка на тот же объект могла записать туда что угодно.</p>
    <p>С TS 4.4 работает <strong>aliased condition</strong>: результат проверки, сохранённый в <code>const</code>, сам становится источником сужения — <code>const isOk = v !== null; if (isOk) { … }</code> сузит <code>v</code>, но только если <code>v</code> тоже неизменяема.</p>

    <h4>Почему так</h4>
    <p>Альтернатива — escape-анализ: доказать, что колбэк вызовется синхронно и до него нет присваиваний. Это дорого и всё равно неполно, потому что колбэк можно сохранить и вызвать из другого модуля. TS выбрал предсказуемость: правило простое, объяснимое и не зависит от того, как далеко утекла функция. Платим лишними промежуточными переменными.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Синхронный колбэк (<code>arr.forEach</code>) страдает так же, как асинхронный — компилятор не различает их и не знает про <code>Array.prototype</code>.</li>
    <li><code>await</code> внутри блока сбрасывает сужение свойств: между <code>if</code> и продолжением мог выполниться чужой код.</li>
    <li>Массовое лечение через <code>!</code> прячет реальные гонки: значение действительно могло стать <code>null</code>, пока таймер ждал.</li>
    <li>Деструктуризация в <code>const { value }</code> лечит сужение, но делает снимок: последующая запись в <code>obj.value</code> в этой копии не видна.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>А если переменная — параметр?</strong> Параметр ведёт себя как <code>let</code>: сужение внутри колбэка слетит, если в теле есть хоть одно присваивание этому параметру. <strong>Почему не помогает <code>as</code> внутри колбэка?</strong> Помогает подавить ошибку, но не решает задачу: значение к моменту вызова действительно может быть другим, и <code>as</code> просто отключает предупреждение о реальном риске.</p>`,
    code: `// плохо: ! маскирует настоящую гонку
let v: string | null = get();
if (v !== null) setTimeout(() => v!.length, 0);

// хорошо: снимок значения, неизменность доказуема
const snapshot = v;
if (snapshot !== null) setTimeout(() => snapshot.length, 0); // string

// TS 4.4 aliased condition
const hasValue = snapshot !== null;
if (hasValue) snapshot.length; // string`,
    tip: 'Ключевая фраза: TS не делает escape-анализ и не отслеживает алиасинг, поэтому сбрасывает сужение везде, где не может доказать неизменность.' },

  { id: 'tsx5',
    q: 'Как работают ограничения дженериков и вывод типов? Где обычно теряется точность?',
    snippet: `declare function a<T>(v: T): T;
declare function b<T extends string>(v: T): T;

const r1 = a('x'); // какой тип?
const r2 = b('x'); // а какой здесь?`,
    a: `<h4>Коротко</h4>
    <p><code>T extends C</code> — одновременно ограничение (что можно передать) и подсказка выводу (насколько узко выводить). Компилятор собирает кандидатов из всех позиций вывода, выбирает среди них общий супертип и только потом сверяет результат с ограничением. Точность теряется на расширении литералов, на приоритете позиций и на дефолтах.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>r1</code> получит тип <code>string</code>, а <code>r2</code> — литерал <code>'x'</code>. Причина в правиле literal widening: литеральный тип аргумента расширяется до базового примитива, если у параметра типа нет ограничения, включающего литералы. <code>T extends string</code> сигнализирует, что литеральность значима, и компилятор её сохраняет. То же с <code>T extends readonly unknown[]</code> для кортежей.</p>
    <p>Сам вывод идёт так: чекер обходит пары «параметр — аргумент», для каждого вхождения <code>T</code> кладёт кандидата в набор, помечая приоритет позиции. Позиции с более низким приоритетом (например, вывод из возвращаемого типа контекстно типизированной стрелки) учитываются только если высокоприоритетных кандидатов нет. Затем берётся общий супертип набора; если ни один кандидат не является супертипом остальных, вывод падает в union или в ошибку. Проверка <code>extends</code> выполняется в конце — она не сужает вывод, а бракует результат.</p>
    <p>Дефолт <code>T = X</code> вступает в игру только когда кандидатов нет вообще (аргумент не передан, позиция не выводима), а не когда вывод получился «плохим».</p>

    <h4>Почему так</h4>
    <p>Widening существует, чтобы <code>let s = 'a'</code> не залипал на литерале и переменную можно было переприсвоить. Ограничение — способ автора API сказать «мне литерал нужен», не заставляя вызывающего писать <code>as const</code>. С TS 5.0 у этой задачи есть более прямое решение — <code>const</code>-параметры типа.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><strong>Дженерик ради дженерика</strong>: если <code>T</code> встречается в сигнатуре один раз, он ничего не связывает и работает как замаскированный <code>any</code>. Правило: параметр типа должен появляться минимум дважды.</li>
    <li><code>T extends {}</code> и <code>T extends unknown</code> ведут себя по-разному: первое исключает <code>null</code>/<code>undefined</code> и подавляет widening, второе нет.</li>
    <li>Возвращаемый тип — не источник вывода: contextual typing идёт снаружи внутрь, поэтому <code>const x: Foo = make()</code> не «дожмёт» <code>T</code> до <code>Foo</code>, если параметр уже дал кандидата.</li>
    <li>Несколько кандидатов из разных аргументов дают супертип, а не пересечение: <code>pair(1, 'a')</code> при <code>&lt;T&gt;(a: T, b: T)</code> выведет <code>1 | 'a'</code> в новых версиях и ошибку в старых.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как заставить вывести только из одного аргумента?</strong> <code>NoInfer&lt;T&gt;</code> в остальных позициях (TS 5.4). <strong>Когда ограничение вредно?</strong> Когда оно шире реальной потребности: <code>T extends object</code> у функции, которой достаточно <code>keyof</code>, зря запрещает передать примитив с брендом.</p>`,
    code: `declare function pick<T extends string>(v: T): T;
pick('a');            // 'a' — литерал сохранён

declare function loose<T>(v: T): T;
loose('a');           // string — расширился

declare function tuple<const T extends readonly unknown[]>(v: T): T;
tuple([1, 'a']);      // readonly [1, "a"] — TS 5.0

declare function bad<T>(v: T): void;   // T встречается один раз — это any`,
    tip: 'Правило «параметр типа должен появляться в сигнатуре минимум дважды, иначе это замаскированный any» — короткий и сильный ответ про плохие сигнатуры.' },

  { id: 'tsx6',
    q: 'Что такое NoInfer и зачем управлять источниками вывода?',
    snippet: `declare function createSet<T extends string>(items: T[], initial: T): Set<T>;

createSet(['a', 'b'], 'c'); // почему это компилируется?`,
    a: `<h4>Коротко</h4>
    <p><code>NoInfer&lt;T&gt;</code> (TS 5.4) помечает позицию как <strong>не источник вывода</strong>: кандидат оттуда не берётся, а проверка присваиваемости остаётся. Нужен там, где один аргумент задаёт множество, а другой обязан быть его элементом.</p>

    <h4>Как это работает</h4>
    <p>В сниппете ошибки нет, и это логично с точки зрения алгоритма: <code>T</code> собирает кандидатов из обоих аргументов — <code>'a' | 'b'</code> из массива и <code>'c'</code> из второго параметра. Общий супертип набора — <code>'a' | 'b' | 'c'</code>, ограничение <code>extends string</code> выполнено, значит вывод успешен и проверять нечего. Второй аргумент не нарушил ограничение, он его <strong>расширил</strong>.</p>
    <p><code>NoInfer&lt;T&gt;</code> в этой позиции исключает её из сбора кандидатов. Тогда <code>T = 'a' | 'b'</code> выводится только из массива, и уже <strong>после</strong> вывода компилятор проверяет, присваивается ли <code>'c'</code> типу <code>'a' | 'b'</code>. Не присваивается — ошибка ровно там, где её ждёшь.</p>
    <p>Внутри это интринзик компилятора, а не обычный условный тип: в лице чекера <code>NoInfer&lt;T&gt;</code> — это <code>T</code> с выставленным флагом «блокировать вывод». Никакого рантайм-следа, никакого влияния на итоговый тип. До 5.4 тот же эффект имитировали хаком <code>T &amp; {}</code>, дополнительным параметром типа с дефолтом или обёрткой в <code>[T][0]</code> — все они работали через побочные эффекты алгоритма и ломались между версиями.</p>

    <h4>Почему так</h4>
    <p>Вывод в TS оптимистичен: он ищет тип, при котором вызов валиден, а не самый узкий. Это удобно в 95% случаев (не приходится писать аргументы типа руками), но ровно там, где API хочет сказать «этот параметр подчинён тому», оптимизм даёт ложноотрицательные ошибки. <code>NoInfer</code> — точечный способ отключить оптимизм в одной позиции, не выключая вывод целиком.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>NoInfer</code> не сужает и не проверяет ничего сам: если из остальных позиций <code>T</code> вывелся широко, ошибки не будет всё равно.</li>
    <li>Обернуть нужно позицию целиком: <code>NoInfer&lt;T&gt;[]</code> и <code>NoInfer&lt;T[]&gt;</code> — разные вещи по глубине блокировки.</li>
    <li>При наличии дефолта <code>T = X</code> блокировка всех позиций приводит к молчаливому применению дефолта вместо ошибки.</li>
    <li>Полифиллы <code>type NoInfer&lt;T&gt; = [T][T extends any ? 0 : never]</code> из npm ведут себя иначе на дистрибутивных типах — на 5.4+ их надо убирать.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Где это встречается в реальном коде?</strong> Типобезопасные роутеры (путь задаёт множество параметров, объект params обязан ему соответствовать), i18n (ключ задаёт набор плейсхолдеров), дизайн-система (список вариантов и дефолтный вариант), <code>useState</code>-подобные фабрики с initial value. <strong>Чем отличается от <code>const</code>-параметра?</strong> <code>const T</code> управляет <strong>насколько узко</strong> выводить, <code>NoInfer</code> — <strong>откуда</strong> выводить; их часто используют вместе.</p>`,
    code: `declare function createSet<T extends string>(
  items: T[], initial: NoInfer<T>
): Set<T>;

createSet(['a', 'b'], 'c');   // Argument of type '"c"' is not assignable to '"a" | "b"'
createSet(['a', 'b'], 'a');   // Set<"a" | "b">

// роутер: params подчинён пути
declare function go<P extends string>(
  path: P, params: Record<NoInfer<P> extends \`\${string}:\${infer K}\` ? K : never, string>
): void;`,
    tip: 'Разделите роли: const-параметр решает, насколько узко выводить, NoInfer — из каких позиций выводить вообще. Назвать оба и их связку — сильный ответ.' },

  { id: 'tsx7',
    q: 'Что делают const type parameters и когда они лучше as const на месте вызова?',
    snippet: `declare function route<const T extends readonly string[]>(p: T): T;

const r = route(['users', 'id']);   // какой тип у r?

const arr = ['users', 'id'];
const r2 = route(arr);              // а у r2?`,
    a: `<h4>Коротко</h4>
    <p><code>&lt;const T&gt;</code> (TS 5.0) заставляет выводить аргумент так, будто на месте вызова написан <code>as const</code>: литералы не расширяются, массивные литералы становятся readonly-кортежами, объектные — readonly-объектами. Ответственность за «заморозку» переезжает с потребителя API на его автора.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>r</code> получит <code>readonly ['users', 'id']</code>, а <code>r2</code> — <code>string[]</code>. Разница принципиальная: модификатор <code>const</code> влияет на то, как выводится тип <strong>литерального выражения в позиции аргумента</strong>. К моменту вызова <code>route(arr)</code> переменная <code>arr</code> уже имеет тип <code>string[]</code> — вывод не может задним числом «дозаморозить» то, что было объявлено раньше.</p>
    <p>Механически чекер при выводе кандидата из литерала обычно применяет widening: <code>'users'</code> → <code>string</code>, массивный литерал → массив, свойства объекта → изменяемые. Флаг <code>const</code> на параметре типа отключает эти три преобразования для данной позиции вывода. Результат идентичен <code>as const</code> на месте вызова, включая рекурсивность — вложенные литералы тоже застывают.</p>
    <p>Раз тип получается <code>readonly</code>, ограничение обязано его принимать: <code>const T extends string[]</code> сломает все вызовы, потому что <code>readonly ['a']</code> не присваивается <code>string[]</code>. Писать нужно <code>readonly unknown[]</code> или <code>readonly string[]</code>.</p>

    <h4>Почему так</h4>
    <p>До 5.0 автору типобезопасного API оставалось только просить пользователей писать <code>as const</code> — а они забывали, и типы молча деградировали до <code>string[]</code>, теряя всю точность роутов и токенов. Перенос модификатора в сигнатуру делает поведение частью контракта, а не документацией. Цена — раздувание типов: большой конфиг, замороженный целиком, увеличивает объём работы чекера на каждом сравнении.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Переменная, объявленная заранее, не замораживается — самый частый вопрос «почему у коллеги работает, а у меня нет».</li>
    <li>Ограничение без <code>readonly</code> ломает вызовы; сообщение об ошибке при этом невнятное и указывает на аргумент, а не на сигнатуру.</li>
    <li>Внутри реализации <code>T</code> readonly, поэтому привычные <code>push</code>/<code>sort</code> по нему не работают — придётся копировать через spread.</li>
    <li>На больших объектах (тема дизайн-системы, схема таблицы на сотни полей) заметно растёт время проверки и размер подсказок в IDE.</li>
    <li>Не действует, если аргумент проходит через spread или через промежуточный вызов.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем отличается от <code>as const</code>?</strong> Местом объявления и охватом: <code>as const</code> — решение вызывающего для одного выражения, <code>const T</code> — решение автора API для всех вызовов. <strong>Что делать, если нужны и заморозка, и изменяемость внутри?</strong> Оставить <code>const T extends readonly unknown[]</code> в публичной сигнатуре и внутри работать с копией <code>[...items]</code>.</p>`,
    code: `declare function route<const T extends readonly string[]>(p: T): T;
const r = route(['users', 'id']);        // readonly ["users", "id"]

// ограничение без readonly — вызовы перестают проходить
declare function bad<const T extends string[]>(p: T): T;
bad(['a']);   // readonly ["a"] не присваивается string[]

// объектный литерал тоже застывает рекурсивно
declare function conf<const T>(c: T): T;
const c = conf({ mode: 'dark', sizes: [1, 2] });
// { readonly mode: "dark"; readonly sizes: readonly [1, 2] }`,
    tip: 'Ключевая пара фраз: const-параметр убирает as const из пользовательского кода, но не действует на переменную, объявленную до вызова.' },

  { id: 'tsx8',
    q: 'Расскажите про вариантность в TypeScript и что меняет strictFunctionTypes.',
    snippet: `declare let handleDog: (a: Dog) => void;
declare let handleAnimal: (a: Animal) => void;

handleAnimal = handleDog;  // ошибка или нет при strictFunctionTypes?

interface Bi { on(a: Dog): void }
const bi: Bi = { on(a: Animal) {} };  // а здесь?`,
    a: `<h4>Коротко</h4>
    <p>Вариантность описывает, как отношение подтипов у <code>T</code> переносится на обёртку <code>F&lt;T&gt;</code>. В TS она нигде не объявляется — компилятор <strong>выводит её структурно</strong> из позиций, в которых параметр типа встречается. Возвращаемые значения и свойства дают ковариантность, параметры функций — контравариантность, оба сразу — инвариантность.</p>

    <h4>Как это работает</h4>
    <p>В сниппете первая строка — ошибка при <code>strictFunctionTypes</code>: <code>handleDog</code> умеет только собак, а через переменную типа <code>(a: Animal) =&gt; void</code> ему передадут кошку. Вторая строка ошибки не даёт: <code>on</code> объявлен методом, а параметры методов проверяются <strong>бивариантно</strong> даже в strict-режиме.</p>
    <p>Правило подтипирования функций: <code>(a: A) =&gt; R1</code> присваивается <code>(b: B) =&gt; R2</code>, если <code>B</code> присваивается <code>A</code> (параметры наоборот) и <code>R1</code> присваивается <code>R2</code> (возврат прямо). Это и есть контравариантность параметров, и она единственно безопасна.</p>
    <p>Для дженериков чекер вычисляет вариантность один раз при первом сравнении: обходит определение, отмечает, в каких позициях появляется параметр, и кеширует маркер (<code>Covariant</code>, <code>Contravariant</code>, <code>Invariant</code>, <code>Bivariant</code>, <code>Independent</code>). Дальше сравнение <code>F&lt;A&gt;</code> с <code>F&lt;B&gt;</code> идёт по маркеру, а не по структуре — это и есть та оптимизация, которую подкручивают аннотации <code>in</code>/<code>out</code>.</p>

    <h4>Почему так</h4>
    <p>Массивы в TS ковариантны сознательно: <code>Dog[]</code> присваивается <code>Animal[]</code>, хотя запись через вторую ссылку может положить туда кошку. Строго правильная инвариантность массивов сломала бы гигантское количество существующего JS-кода, где «список собак» естественно передают в функцию «посчитай животных». Ровно та же логика за бивариантностью методов: типы DOM, <code>Array.prototype</code>, <code>EventTarget</code> и React написаны так, что строгая проверка сделала бы их непригодными.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>strictFunctionTypes</code> входит в <code>strict</code>, но <strong>не действует на методы</strong> — это не баг конфигурации, а явное исключение в чекере.</li>
    <li>Разница «метод против свойства-функции» невидима глазом, поэтому строгость обработчиков включается переписыванием <code>on(e: E): void</code> в <code>on: (e: E) =&gt; void</code>.</li>
    <li>Ковариантность массивов даёт runtime-ошибку без единого предупреждения компилятора — классический пример unsound-места в TS.</li>
    <li>Функция с меньшим числом параметров присваивается функции с большим: <code>() =&gt; void</code> подходит везде, где ждут <code>(e: Event) =&gt; void</code>. Отсюда баг <code>arr.map(parseInt)</code>.</li>
    <li>Возврат <code>void</code> особенный: любая функция с любым возвратом присваивается типу с <code>=&gt; void</code>, поэтому <code>arr.forEach(x =&gt; set.add(x))</code> не ругается.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что такое «unsound» в этом контексте?</strong> Система типов unsound, если существует программа без ошибок компиляции, падающая на несоответствии типов в рантайме — TS unsound намеренно. <strong>Как получить инвариантность руками?</strong> Использовать параметр типа и в позиции параметра, и в позиции возврата, либо явно написать <code>in out</code>.</p>`,
    code: `type Handler = (a: Animal) => void;
let onDog: (a: Dog) => void = (d) => d.bark();
const h: Handler = onDog;      // ошибка при strictFunctionTypes

interface Loose  { on(a: Dog): void }        // бивариантно — проверки нет
interface Strict { on: (a: Dog) => void }    // контравариантно — проверка есть

const dogs: Dog[] = [new Dog()];
const animals: Animal[] = dogs;  // ковариантность массивов: ок для компилятора
animals.push(new Cat());         // и вот здесь в dogs лежит кошка`,
    tip: 'Скажите прямо: ковариантность массивов и бивариантность методов — осознанный unsound-компромисс ради совместимости с существующим JS, а не недосмотр компилятора.' },

  { id: 'tsx9',
    q: 'Зачем нужны аннотации вариантности in/out и когда их писать?',
    snippet: `interface Producer<out T> { get(): T; set(v: T): void }
// что скажет компилятор на это объявление?`,
    a: `<h4>Коротко</h4>
    <p>С TS 4.7 у параметра типа можно писать <code>in</code> (контравариантный), <code>out</code> (ковариантный), <code>in out</code> (инвариантный). Это <strong>не новая семантика</strong>, а способ сказать компилятору то, что он и так вычислил бы сам. Главный мотив — производительность проверки, вторичный — документация и защита от ошибок в объявлении.</p>

    <h4>Как это работает</h4>
    <p>Сниппет не скомпилируется: <code>T</code> объявлен как <code>out</code>, но встречается в позиции параметра метода <code>set</code>, то есть реально инвариантен. Компилятор выдаст ошибку прямо на объявлении интерфейса — «Type 'Producer&lt;T&gt;' is not assignable to type 'Producer&lt;…&gt;' as implied by variance annotation». Без аннотации он молча вывел бы инвариантность, и странное поведение всплыло бы где-нибудь через десять файлов.</p>
    <p>Обычный порядок такой: при первом сравнении <code>F&lt;A&gt;</code> с <code>F&lt;B&gt;</code> чекер обходит определение <code>F</code>, определяет вариантность каждого параметра и кеширует её. На взаимно-рекурсивных и глубоко вложенных дженериках этот обход дорог: он раскрывает типы, порождает инстанциации и тянет за собой соседние определения. Аннотация даёт маркер сразу — обход не нужен, и чекер сразу применяет быструю проверку по маркеру. Для проверки самой аннотации выполняется одно сравнение с «отмеченными» типами-маркерами, а не полный структурный обход.</p>
    <p>В рантайме, как и у всей системы типов, следа нет: аннотации стираются вместе с объявлением интерфейса.</p>

    <h4>Почему так</h4>
    <p>TS структурно типизирован, и вариантность в нём — производная от структуры, а не декларация автора (в отличие от C# и Kotlin, где <code>in</code>/<code>out</code> задают правило). Делать её обязательной было бы ломающим изменением для всей экосистемы. Поэтому аннотация — необязательная подсказка: если она совпадает с выведенной, ничего не меняется; если противоречит — ошибка.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Аннотация не «включает» ковариантность там, где её нет: она только фиксирует и проверяет, а не переопределяет правило.</li>
    <li>Выигрыш в скорости заметен на библиотечных типах уровня RxJS, Redux-Toolkit, tRPC. В прикладном коде это преждевременная оптимизация, добавляющая шум в сигнатуры.</li>
    <li>Ошибка при несовпадении указывает на объявление, но её текст про «implied by variance annotation» читается плохо и часто вводит в заблуждение новичков.</li>
    <li>Бивариантные позиции (параметры методов) под аннотацию не подпадают так, как ожидается: метод даёт бивариантность, а <code>out</code> требует ковариантности — сочетание ведёт себя неочевидно.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как понять, что тормозит именно вариантность?</strong> Через <code>tsc --generateTrace</code> и <code>analyze-trace</code>: в трейсе видны долгие <code>structuredTypeRelatedTo</code> на одних и тех же дженериках. <strong>Где это писать в первую очередь?</strong> В <code>.d.ts</code> публичных пакетов и в типах стора, которые сравниваются тысячи раз за проверку.</p>`,
    code: `interface Producer<out T> { get(): T }            // только читаем
interface Consumer<in T>  { set(v: T): void }     // только пишем
interface State<in out T> { get(): T; set(v: T): void }  // и то и другое

declare let pDog: Producer<Dog>;
declare let pAnimal: Producer<Animal>;
pAnimal = pDog;   // ок: ковариантность

declare let sDog: State<Dog>;
declare let sAnimal: State<Animal>;
sAnimal = sDog;   // ошибка: инвариантность`,
    tip: 'Правильный акцент: главный мотив фичи — время проверки (компилятор пропускает структурный вывод вариантности), а строгость идёт бонусом.' },

  { id: 'tsx10',
    q: 'Чем тип метода отличается от свойства-функции и что такое bivariance hack?',
    snippet: `interface A { handle(e: MouseEvent): void }
interface B { handle: (e: MouseEvent) => void }

const a: A = { handle(e: Event) {} };  // одна из этих строк — ошибка
const b: B = { handle: (e: Event) => {} };`,
    a: `<h4>Коротко</h4>
    <p>Синтаксис почти одинаков, проверка разная. Сокращённая форма <code>handle(e: E): void</code> — <strong>метод</strong>, его параметры сравниваются бивариантно даже при <code>strictFunctionTypes</code>. Форма <code>handle: (e: E) =&gt; void</code> — <strong>свойство функционального типа</strong>, параметры контравариантны, проверка строгая.</p>

    <h4>Как это работает</h4>
    <p>В сниппете ошибки не будет ни там, ни там — потому что <code>Event</code> шире <code>MouseEvent</code>, а это как раз безопасное (контравариантное) направление. Разница проявится на обратном: <code>{ handle(e: MouseEvent &amp; { custom: 1 }) {} }</code> пройдёт для <code>A</code> и упадёт для <code>B</code>.</p>
    <p>Внутри чекера у сигнатуры есть флаг <code>MethodDeclaration</code>. При сравнении двух сигнатур, если обе объявлены методами и включён <code>strictFunctionTypes</code>, чекер сперва пробует строгое (контравариантное) сравнение параметров, и если оно не прошло — пробует ковариантное. Успех любого из двух означает совместимость. Для свойств второй попытки нет. Это единственное место, где строгий режим сознательно оставляет дыру.</p>
    <p>Так называемый <strong>bivariance hack</strong> — объявление члена методом именно ради этой поблажки. Он используется в типах <code>Array</code> (<code>push</code>, <code>filter</code>, <code>sort</code>), в <code>EventTarget</code>, в <code>React.Component</code> и в старых типах событий React. Обратный приём — переписать метод в свойство, чтобы точечно включить строгость там, где она нужна.</p>

    <h4>Почему так</h4>
    <p>Если бы параметры методов проверялись строго, <code>Dog[]</code> перестал бы присваиваться <code>Animal[]</code> (у <code>Array&lt;T&gt;</code> есть <code>push(v: T)</code>), и вместе с ним развалилась бы половина lib.d.ts и почти любая коллекция. Команда TS выбрала оставить бивариантность для методов и включить строгость только для явных функциональных типов, где автор может сознательно выбрать форму записи.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Разницу невозможно заметить на ревью: две записи отличаются двоеточием и стрелкой, а поведение проверки — противоположное.</li>
    <li><code>declare function</code> и перегрузки интерфейса ведут себя как методы; отдельная стрелочная запись перегрузок невозможна.</li>
    <li>ESLint-правило <code>@typescript-eslint/method-signature-style</code> умеет форсировать стиль <code>property</code> — это дешёвый способ включить строгость по всему проекту разом.</li>
    <li>Переезд на свойства ломает declaration merging: перегрузки через несколько объявлений возможны только для методов.</li>
    <li>Классы: метод в классе — тоже метод, поэтому строгость обработчиков в классовых компонентах достигается только полями-стрелками.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Почему <code>arr.map(parseInt)</code> компилируется?</strong> Отдельный механизм: функция с меньшим числом параметров присваивается типу с большим, поэтому <code>parseInt(s, radix)</code> спокойно встаёт на место <code>(v, i, arr) =&gt; …</code> — и получает индекс в качестве системы счисления. <strong>Как показать разницу за 10 секунд?</strong> Присвоить объект с более узким параметром: метод пропустит, свойство — нет.</p>`,
    code: `interface Loose  { handle(e: MouseEvent): void }        // бивариантно
interface Strict { handle: (e: MouseEvent) => void }    // контравариантно

// сужение параметра — небезопасно, но метод его пропускает
const l: Loose  = { handle(e: MouseEvent & { custom: 1 }) {} };  // ок
const s: Strict = { handle: (e: MouseEvent & { custom: 1 }) => {} }; // ошибка`,
    tip: 'Готовый рецепт для ревью: правило method-signature-style в ESLint переводит интерфейсы на свойства-функции и включает строгую проверку колбэков по всему проекту.' },

  { id: 'tsx11',
    q: 'Как связаны keyof, typeof и indexed access types? Приведите нетривиальный пример.',
    snippet: `const ROLES = ['admin', 'user'];
type Role = typeof ROLES[number];   // какой тип получится?

const cfg: Record<string, number> = { port: 1 };
type Key = keyof typeof cfg;        // а здесь?`,
    a: `<h4>Коротко</h4>
    <p><code>typeof x</code> в type-позиции поднимает значение в тип, <code>keyof T</code> даёт union ключей, <code>T[K]</code> — тип значения по ключу. Вместе это мост из мира значений в мир типов: одна константа порождает и рантайм-данные, и типы, вместо того чтобы дублировать список в двух местах.</p>

    <h4>Как это работает</h4>
    <p>В сниппете обе строки дают широкий результат. <code>ROLES</code> объявлен без <code>as const</code>, поэтому его тип — <code>string[]</code>, и <code>typeof ROLES[number]</code> даёт <code>string</code>, а не <code>'admin' | 'user'</code>. У <code>cfg</code> тип объявлен как <code>Record&lt;string, number&gt;</code>, то есть с индексной сигнатурой, и <code>keyof</code> от неё — <code>string</code>, а не <code>'port'</code>. Оба случая — самая частая причина жалобы «мой Record ничего не проверяет».</p>
    <p><code>typeof</code> в type-позиции и <code>typeof</code> в выражении — два разных оператора с одинаковым написанием: первый существует только при компиляции и работает с идентификаторами, второй — рантайм-оператор, возвращающий строку. Компилятор различает их по контексту.</p>
    <p><code>T[number]</code> на кортеже даёт union элементов, потому что кортеж — это объект с числовыми ключами <code>0</code>, <code>1</code>, … плюс <code>length</code>: индексирование по <code>number</code> собирает union по всем числовым ключам. Отсюда идиома «union из массива»: <code>as const</code> превращает массив в кортеж литералов, <code>[number]</code> сплющивает его в union.</p>

    <h4>Почему так</h4>
    <p>Дублировать список в константе и в union — гарантированный источник рассинхрона: добавили роль в массив, забыли в тип, и валидация молча пропускает. Приём «значение — источник истины, тип выводится» решает это структурно. Обратное направление (тип как источник, а массив вручную) невозможно: типы стираются, породить из них рантайм-массив нельзя.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Забытый <code>as const</code> обесценивает всю конструкцию и не даёт ошибки — тип просто становится <code>string</code>.</li>
    <li>Индексная сигнатура делает <code>keyof T</code> равным <code>string | number</code>, что рушит точность любого маппера поверх такого типа.</li>
    <li><code>keyof</code> объекта с числовыми ключами вернёт <code>number</code>; <code>keyof any</code> — это <code>string | number | symbol</code>, а <code>keyof unknown</code> — <code>never</code>.</li>
    <li>Оптиональные свойства попадают в <code>keyof</code>, а их тип в <code>T[K]</code> включает <code>undefined</code>; для «только обязательные ключи» нужен отдельный маппер.</li>
    <li><code>typeof</code> берёт объявленный тип, а не сужённый в текущей точке: <code>typeof x</code> после <code>if (x !== null)</code> всё равно даст union с <code>null</code>.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем это лучше enum?</strong> Численный <code>enum</code> оставляет объект в рантайме и допускает присваивание произвольного числа; <code>as const</code>-массив даёт и итерируемый список, и точный union без рантайм-накладных расходов. <strong>Как получить только ключи с определённым типом значения?</strong> Через перемаппинг: <code>{ [K in keyof T as T[K] extends string ? K : never]: T[K] }</code> и затем <code>keyof</code>.</p>`,
    code: `const ROLES = ['admin', 'user'] as const;
type Role = typeof ROLES[number];      // "admin" | "user"

const cfg = { host: 'x', port: 1 } as const;
type Key  = keyof typeof cfg;          // "host" | "port"
type Port = typeof cfg['port'];        // 1
type Any  = typeof cfg[Key];           // "x" | 1

// индексная сигнатура убивает точность
type Wide = keyof Record<string, number>;  // string | number`,
    tip: 'Формулировка «константа рантайма порождает тип, а не наоборот» плюс уточнение, что забытый as const не даёт ошибки, а тихо расширяет тип до string.' },

  { id: 'tsx12',
    q: 'Что такое перемаппинг ключей через as в mapped types и какие задачи он решает?',
    snippet: `type Src = { a?: string; readonly b: number };

type Same   = { [K in keyof Src]: Src[K] };
type Renamed = { [K in keyof Src as \`get_\${K & string}\`]: Src[K] };
// что стало с ? и readonly в каждом из них?`,
    a: `<h4>Коротко</h4>
    <p>Синтаксис <code>{ [K in keyof T as NewKey]: … }</code> (TS 4.1) позволяет <strong>переименовывать</strong> ключи и <strong>фильтровать</strong> их: если выражение ключа даёт <code>never</code>, свойство исчезает из результата. Это идиоматический способ отобрать члены по типу значения или по имени.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>Same</code> сохранит и <code>?</code>, и <code>readonly</code>, а <code>Renamed</code> потеряет оба: получится <code>{ get_a: string | undefined; get_b: number }</code>. Причина в <strong>гомоморфности</strong>. Маппер вида <code>{ [K in keyof T]: … }</code> компилятор распознаёт как гомоморфный и копирует модификаторы каждого исходного свойства один к одному. Добавление <code>as</code> с изменением ключа разрывает связь «новый ключ ↔ исходное свойство», гомоморфность теряется, и модификаторы не переносятся.</p>
    <p>Фильтрация работает так: выражение после <code>as</code> вычисляется для каждого <code>K</code> отдельно, результаты собираются, и члены, давшие <code>never</code>, просто не попадают в итоговый тип — ключа <code>never</code> не существует. Отсюда шаблон <code>as T[K] extends Function ? K : never</code>.</p>
    <p>Модификаторы задаются здесь же: <code>-?</code> убирает опциональность (так устроен <code>Required</code>), <code>+?</code> добавляет (<code>Partial</code>), <code>-readonly</code> снимает (<code>Mutable</code>). Ключ может быть union — тогда одно исходное свойство порождает несколько (пара get/set из одного поля).</p>

    <h4>Почему так</h4>
    <p>Гомоморфное копирование модификаторов — специальная логика в чекере, введённая ради <code>Partial</code>, <code>Readonly</code>, <code>Pick</code>: без неё <code>Partial&lt;T&gt;</code> терял бы <code>readonly</code>. Распознавание идёт по синтаксической форме, а не по семантике, поэтому любое отклонение (в том числе <code>as</code>) выключает механизм. Это цена простоты правила: оно предсказуемо, но нарушается неожиданно.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Переименование съедает <code>?</code> и <code>readonly</code> — придётся выписывать <code>+?</code> и <code>+readonly</code> руками, но восстановить их <strong>поштучно</strong> (только у тех, у кого были) уже нельзя.</li>
    <li>Потерянный <code>?</code> превращается в обязательное поле типа <code>T | undefined</code>: при <code>exactOptionalPropertyTypes</code> это разные вещи, и код перестанет собираться.</li>
    <li>Два исходных ключа могут схлопнуться в один новый (например, после <code>Lowercase</code>) — компилятор не предупредит, свойство просто исчезнет.</li>
    <li>Шаблонные ключи требуют <code>K &amp; string</code>: <code>keyof T</code> включает <code>symbol</code>, а символ в template literal не подставляется.</li>
    <li>Тяжёлые мапперы поверх больших типов заметно тормозят IDE: каждый hover пересчитывает весь результат.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как сохранить опциональность при переименовании?</strong> Двухшаговый маппер: сперва гомоморфно построить промежуточный тип, потом переименовать — или собрать результат как пересечение двух мапперов (обязательные и опциональные ключи отдельно). <strong>Как реализован <code>Omit</code>?</strong> Через <code>Pick&lt;T, Exclude&lt;keyof T, K&gt;&gt;</code> — он негомоморфен по историческим причинам и потому не проверяет, что <code>K</code> вообще есть в <code>T</code>.</p>`,
    code: `type Getters<T> = {
  [K in keyof T & string as \`get\${Capitalize<K>}\`]: () => T[K]
};
// { getHost: () => string; getPort: () => number }

type OnlyFns<T> = {
  [K in keyof T as T[K] extends Function ? K : never]: T[K]
};

type Mutable<T> = { -readonly [K in keyof T]: T[K] };
type Required2<T> = { [K in keyof T]-?: T[K] };`,
    tip: 'Нюанс, отличающий практика: as-переименование выключает гомоморфность, поэтому маппер молча теряет ? и readonly, а при exactOptionalPropertyTypes это ещё и ломает сборку.' },

  { id: 'tsx13',
    q: 'Что такое template literal types и где они реально полезны?',
    snippet: `type Size  = 'sm' | 'md' | 'lg';
type Color = 'red' | 'blue';

type Token = \`--btn-\${Size}-\${Color}\`;
// сколько членов в этом union и какие?`,
    a: `<h4>Коротко</h4>
    <p>Template literal types — строковые типы, собранные из литеральных кусков и плейсхолдеров <code>\${…}</code>. При подстановке union они <strong>дистрибутивно разворачиваются в декартово произведение</strong>. К ним прилагаются четыре интринзика компилятора: <code>Uppercase</code>, <code>Lowercase</code>, <code>Capitalize</code>, <code>Uncapitalize</code>.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>Token</code> — это union из шести литералов: <code>'--btn-sm-red' | '--btn-sm-blue' | '--btn-md-red' | …</code>. Компилятор перемножает все позиции: 3 × 2. Это ключевая механика и одновременно главная опасность — размер результата мультипликативен по числу плейсхолдеров.</p>
    <p>В обратную сторону работает сопоставление: тип <code>\`get\${infer K}\`</code> в условном типе разбирает строку по образцу, отдавая в <code>K</code> хвост. Разбор жадный слева направо: <code>\`\${infer A}-\${infer B}\`</code> на строке <code>'a-b-c'</code> даст <code>A = 'a'</code>, <code>B = 'b-c'</code>. На этом строится парсинг роутов, разбор CSS-значений и типизация путей к вложенным полям.</p>
    <p>Интринзики <code>Uppercase</code> и компания реализованы прямо в чекере (в файле <code>checker.ts</code> есть таблица <code>intrinsicTypeKinds</code>), а не через типы из lib.d.ts — выразить их средствами языка невозможно.</p>
    <p>В рантайме, как обычно, ноль следа: <code>Token</code> исчезает при компиляции, а строки в коде остаются обычными строками.</p>

    <h4>Почему так</h4>
    <p>До 4.1 «ключ вида on + имя события» приходилось выписывать руками или отдавать <code>string</code>. Шаблонные типы позволили выразить строковые протоколы (CSS-переменные, ключи i18n, имена событий, пути к полям) в системе типов и получить автодополнение вместо документации. Плата — компилятор считает произведения, и большие union стоят памяти и времени.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><strong>Комбинаторный взрыв</strong>: два union по 50 членов дают 2500 литералов, три — 125 000, и компилятор упирается в жёсткий лимит в 100 000 членов union с ошибкой «Expression produces a union type that is too complex to represent».</li>
    <li>Рекурсивные пути к вложенным полям на большой модели порождают десятки тысяч литералов и в одиночку прибивают отзывчивость IDE.</li>
    <li><code>\${number}</code> и <code>\${bigint}</code> сопоставляются с числовыми литералами, но обратно тип не считает: <code>'01'</code> и <code>'1'</code> — разные строки, а <code>'1e3'</code> тоже проходит как число.</li>
    <li><code>keyof T</code> включает <code>symbol</code>, а символ в шаблон не подставляется — нужен <code>K &amp; string</code>.</li>
    <li>Порядок членов в получившемся union не специфицирован и менялся между версиями; полагаться на него в type-тестах нельзя.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как ограничить взрыв?</strong> Не разворачивать всё сразу: держать тип как проверку (<code>T extends \`--btn-\${string}\` ? T : never</code>) вместо перечисления, ограничивать глубину рекурсии и отсекать ветки на первом же несоответствии. <strong>Где это применяют в проде?</strong> Tailwind-подобные типы классов, tRPC-роуты, ключи i18n с проверкой плейсхолдеров, типизированные CSS-переменные дизайн-системы.</p>`,
    code: `type Ev<T extends string> = \`on\${Capitalize<T>}\`;
type A = Ev<'click' | 'focus'>;   // "onClick" | "onFocus"

// разбор роута на уровне типов: жадно слева направо
type Params<S extends string> =
  S extends \`\${string}:\${infer P}/\${infer R}\` ? P | Params<R>
  : S extends \`\${string}:\${infer P}\` ? P
  : never;
type P = Params<'/users/:id/posts/:postId'>;  // "id" | "postId"

// проверка вместо перечисления — не взрывается
type CssVar<T extends string> = T extends \`--\${string}\` ? T : never;`,
    tip: 'Назовите жёсткий лимит в 100 000 членов union и приём «проверять шаблоном, а не перечислять произведение» — это сразу отличает практика от читателя статей.' },

  { id: 'tsx14',
    q: 'Как писать рекурсивные типы и где у них предел?',
    snippet: `type Join<T extends string[], Acc extends string = ''> =
  T extends [infer H extends string, ...infer R extends string[]]
    ? Join<R, \`\${Acc}\${H}\`>
    : Acc;

type A = Join<['a', 'b', 'c']>;   // почему это работает на тысяче элементов,
                                  // а вариант с конкатенацией снаружи — нет?`,
    a: `<h4>Коротко</h4>
    <p>Рекурсивный тип — условный тип, ссылающийся сам на себя. Типичные задачи: <code>DeepPartial</code>, <code>DeepReadonly</code>, пути к вложенным ключам, разбор строк, кортежная арифметика. Предел один — глубина инстанцирования, и он резко разный для хвостовой и не-хвостовой рекурсии.</p>

    <h4>Как это работает</h4>
    <p>Компилятор ведёт стек инстанцирований. Для обычной рекурсии лимит — <strong>около 50 уровней</strong>, дальше выдаётся «Type instantiation is excessively deep and possibly infinite». С TS 4.5 работает <strong>tail-recursion elimination</strong>: если рекурсивная ссылка стоит непосредственно в ветке условного типа и её результат ничем не оборачивается, чекер разворачивает рекурсию в цикл, и предел поднимается до <strong>1000 шагов</strong> (жёсткая константа <code>maxTailRecursionDepth</code>).</p>
    <p>Именно поэтому сниппет работает: <code>Join&lt;R, …&gt;</code> — последнее, что вычисляется в ветке, накопленный результат передаётся аккумулятором. Вариант <code>\`\${H}\${Join&lt;R&gt;}\`</code> хвостовым не является: результат рекурсии нужно подставить в шаблон, значит кадр стека приходится сохранять — и на пятидесятом элементе всё падает.</p>
    <p>Второй важный механизм — <strong>отложенное вычисление</strong>. Условный тип с невыведенным параметром не раскрывается сразу, а откладывается. Поэтому <code>type X&lt;T&gt; = T extends … ? X&lt;…&gt; : …</code> может ссылаться на себя, а простой алиас <code>type Y&lt;T&gt; = Y&lt;T&gt;</code> даёт ошибку «circularly references itself».</p>

    <h4>Почему так</h4>
    <p>Система типов TS полна по Тьюрингу, поэтому единственная защита от зависания компилятора — жёсткие лимиты. Они намеренно консервативны: лучше отказать на пятидесятом уровне, чем повесить IDE. Tail-recursion elimination — компромисс, который снимает лимит там, где кадр стека доказуемо не нужен.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Рекурсия по объектам обязана отсекать <code>Date</code>, <code>Map</code>, <code>Set</code>, <code>RegExp</code>, функции и массивы: наивный <code>DeepPartial</code> превращает <code>Date</code> в <code>{ getTime?: … }</code>.</li>
    <li>Рекурсия по union без обёртки в кортеж дистрибутивна — тип раскрывается по каждому члену и стоимость умножается.</li>
    <li>Ошибка «excessively deep» появляется не всегда детерминированно: она зависит от порядка проверки файлов, поэтому падает в CI и не падает локально.</li>
    <li>Хвостовая форма ломается от невинного вида изменений — обернули результат в <code>Exclude</code>, и оптимизация выключилась без предупреждения.</li>
    <li>Даже в пределах лимита глубокая рекурсия дорога: подсказки IDE начинают тормозить задолго до ошибки компиляции.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как обойти лимит совсем?</strong> Разбить задачу: обрабатывать кортеж чанками по 10 через вложенный маппер, либо перенести вычисление в кодогенерацию. <strong>Как проверить, что рекурсия хвостовая?</strong> Эмпирически — прогнать тип на кортеже из 200 элементов: не-хвостовая упадёт, хвостовая пройдёт.</p>`,
    code: `// хвостовая форма с аккумулятором: до 1000 шагов
type Join<T extends readonly string[], Acc extends string = ''> =
  T extends readonly [infer H extends string, ...infer R extends string[]]
    ? Join<R, \`\${Acc}\${H}\`> : Acc;

// не хвостовая: результат оборачивается — предел ~50
type JoinBad<T extends readonly string[]> =
  T extends readonly [infer H extends string, ...infer R extends string[]]
    ? \`\${H}\${JoinBad<R>}\` : '';

// отсечение встроенных объектов обязательно
type DeepReadonly<T> =
  T extends Function | Date | RegExp ? T
  : T extends (infer U)[] ? readonly DeepReadonly<U>[]
  : T extends object ? { readonly [K in keyof T]: DeepReadonly<T[K]> }
  : T;`,
    tip: 'Конкретика, которую редко называют: обычный предел рекурсии — около 50 уровней, хвостовой (TS 4.5) — 1000, и оптимизация выключается от любой обёртки вокруг рекурсивного вызова.' },

  { id: 'tsx15',
    q: 'Что такое дистрибутивность conditional types и когда она мешает?',
    snippet: `type IsString<T> = T extends string ? true : false;

type A = IsString<string | number>;  // ?
type B = IsString<never>;            // ?
type C = IsString<any>;              // ?`,
    a: `<h4>Коротко</h4>
    <p>Если проверяемый тип — <strong>голый параметр</strong> (<code>T extends X ? A : B</code>, где <code>T</code> ничем не обёрнут), то при подстановке union условие применяется к каждому члену по отдельности, а результаты объединяются. На этом построены <code>Exclude</code>, <code>Extract</code>, <code>NonNullable</code>. Отключается обёрткой в кортеж: <code>[T] extends [X]</code>.</p>

    <h4>Как это работает</h4>
    <p>Ответы на сниппет: <code>A = boolean</code> (то есть <code>true | false</code> — условие применилось к <code>string</code> и к <code>number</code> отдельно), <code>B = never</code> (не <code>false</code>!), <code>C = boolean</code>.</p>
    <p>Случай с <code>never</code> — прямое следствие определения: <code>never</code> это union из нуля членов, дистрибуция по нулю членов даёт union из нуля результатов, то есть снова <code>never</code>. Ветка условия не выполняется ни разу. Это ломает любую наивную проверку вида «пустой ли тип» и все хелперы, которые пишут <code>T extends never ? …</code>.</p>
    <p><code>any</code> обрабатывается особым правилом: условный тип над <code>any</code> сразу возвращает union обеих веток, потому что <code>any</code> одновременно совместим и несовместим с чем угодно. <code>unknown</code>, наоборот, просто уходит в false-ветку.</p>
    <p>Отключение через кортеж работает потому, что <code>[T]</code> — уже не голый параметр, а тип-обёртка: чекер сравнивает <code>[string | number]</code> с <code>[string]</code> целиком, и получает <code>false</code>. Тот же приём применяют к правой части, когда нужно сравнить union как единое целое.</p>

    <h4>Почему так</h4>
    <p>Дистрибутивность введена ради утилит фильтрации: без неё <code>Exclude&lt;'a' | 'b', 'a'&gt;</code> пришлось бы писать через рекурсию по членам union, а такой рекурсии в языке не было. Правило «голый параметр — дистрибутивно» синтаксическое и потому предсказуемое, но оно бьёт по всем, кто пишет условный тип, не думая про union на входе.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>T = never</code> даёт <code>never</code>, а не false-ветку — самая частая ловушка на интервью и в реальных хелперах.</li>
    <li><code>boolean</code> — это <code>true | false</code>, поэтому он тоже дистрибутируется: <code>T extends true ? …</code> на <code>boolean</code> даст обе ветки.</li>
    <li>Дистрибутивность мешает при <code>UnionToIntersection</code>, при проверке «является ли T объединением» и при точном сравнении типов.</li>
    <li>Обёртка в кортеж выключает и полезную дистрибуцию: если внутри маппер, он теперь получит union целиком.</li>
    <li>Проверка на <code>any</code> должна идти первой: любое условие после неё уже даст обе ветки и результат станет бессмысленным.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как проверить, что тип — <code>never</code>?</strong> Только через невывернутую форму: <code>[T] extends [never] ? true : false</code>. <strong>Как определить, что <code>T</code> — union?</strong> Сравнить дистрибутивную версию с недистрибутивной: <code>T extends any ? ([U] extends [T] ? false : true) : never</code> с <code>U = T</code>, зафиксированным до дистрибуции.</p>`,
    code: `type IsString<T> = T extends string ? true : false;
type A = IsString<string | number>;        // boolean
type C = IsString<never>;                  // never — ветки не выполнялись
type D = IsString<any>;                    // boolean — особое правило для any

type IsStringStrict<T> = [T] extends [string] ? true : false;
type B = IsStringStrict<string | number>;  // false

type IsNever<T> = [T] extends [never] ? true : false;
type E = IsNever<never>;                   // true`,
    tip: 'Назовите случай T = never сами и объясните причину: never — union из нуля членов, поэтому дистрибуция не выполняет ни одной ветки.' },

  { id: 'tsx16',
    q: 'Как написать корректный тип «равны ли два типа» и почему это сложно?',
    snippet: `type Naive<X, Y> = [X] extends [Y] ? ([Y] extends [X] ? true : false) : false;

type A = Naive<any, string>;   // почему здесь true?
type B = Naive<{a: 1}, {a: 1}>;`,
    a: `<h4>Коротко</h4>
    <p>Наивное «взаимное <code>extends</code>» проверяет взаимную присваиваемость, а не идентичность. Оно ломается на <code>any</code> (совместим со всем в обе стороны) и на дистрибутивности union. Каноническое решение опирается на <strong>внутреннее сравнение отложенных условных типов</strong> и является деталью реализации компилятора, а не частью языка.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>A = true</code>: <code>any</code> присваивается <code>string</code>, и <code>string</code> присваивается <code>any</code>. Взаимная присваиваемость выполнена, хотя типы очевидно разные. Это принципиальный дефект подхода: присваиваемость — не отношение эквивалентности в нужном смысле.</p>
    <p>Рабочий приём выглядит так: сравниваются два обобщённых функциональных типа <code>&lt;T&gt;() =&gt; T extends X ? 1 : 2</code> и <code>&lt;T&gt;() =&gt; T extends Y ? 1 : 2</code>. Внутри них <code>T</code> не подставлен, поэтому условные типы <strong>откладываются</strong>. При сравнении двух отложенных условных типов чекер не может их вычислить и вместо этого сверяет их структурно: одинаковы ли <code>checkType</code>, <code>extendsType</code> и обе ветки. Идентичными они окажутся только если <code>X</code> и <code>Y</code> — буквально один и тот же тип, включая различие <code>any</code>/<code>unknown</code>/<code>never</code> и порядок в пересечениях.</p>
    <p>Именно поэтому такой <code>Equals</code> различает <code>any</code> и <code>unknown</code>, <code>{a: 1}</code> и <code>{a: number}</code>, но при этом даёт <code>false</code> на семантически равных, но по-разному записанных типах — например, на интерфейсе и структурно совпадающем type-алиасе в некоторых версиях.</p>

    <h4>Почему так</h4>
    <p>В TS нет оператора «типы идентичны»: система структурная, и вопрос «один ли это тип» для неё нестандартный. Функция <code>isTypeIdenticalTo</code> внутри чекера есть, но наружу не выведена. Трюк с отложенными условными типами — способ дотянуться до неё окольным путём. Отсюда и главный риск: поведение неспецифицировано и уже менялось между версиями компилятора.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Обёртка в кортеж <code>[T] extends [U]</code> убирает дистрибутивность, но не решает проблему <code>any</code> — это два разных дефекта.</li>
    <li>Результат зависит от версии TS: тесты на <code>Equals</code> ломаются при апгрейде без изменений в коде.</li>
    <li>Различает то, что различать обычно не хотят: <code>{a: 1} &amp; {b: 2}</code> и <code>{a: 1; b: 2}</code> для него разные типы.</li>
    <li>Опциональные свойства и <code>readonly</code> учитываются, поэтому <code>Equals</code> строже, чем ожидает автор теста.</li>
    <li>В проде такой тип почти всегда не нужен: если он появился в бизнес-коде, скорее всего задача решается иначе.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Где это применяют?</strong> В type-тестах: <code>expectTypeOf</code> из vitest, <code>tsd</code>, <code>@ts-expect-error</code>-фикстуры библиотек — там нужно убедиться, что утилита вывела ровно ожидаемый тип. <strong>Как проверить на <code>any</code> отдельно?</strong> <code>type IsAny&lt;T&gt; = 0 extends (1 &amp; T) ? true : false</code> — пересечение с <code>any</code> схлопывается в <code>any</code>, и <code>0 extends any</code> проходит.</p>`,
    code: `type Equals<X, Y> =
  (<T>() => T extends X ? 1 : 2) extends
  (<T>() => T extends Y ? 1 : 2) ? true : false;

type A = Equals<any, unknown>;        // false
type B = Equals<{ a: 1 }, { a: 1 }>;  // true
type C = Equals<{ a: 1 } & { b: 2 }, { a: 1; b: 2 }>;  // false — строже, чем ждут

type IsAny<T> = 0 extends (1 & T) ? true : false;
type D = IsAny<any>;    // true`,
    tip: 'Честно скажите, что трюк опирается на неспецифицированную деталь реализации чекера и ломается при апгрейде TS — такой ответ ценится выше, чем заученная формула.' },

  { id: 'tsx17',
    q: 'Где может стоять infer и что даёт infer extends?',
    snippet: `type Co<T> = T extends { a: infer U; b: infer U } ? U : never;
type Contra<T> = T extends { f: (x: infer U) => void; g: (x: infer U) => void } ? U : never;

type A = Co<{ a: string; b: number }>;                              // ?
type B = Contra<{ f: (x: string) => void; g: (x: number) => void }>; // ?`,
    a: `<h4>Коротко</h4>
    <p><code>infer</code> объявляет переменную типа прямо в правой части <code>extends</code> внутри условного типа. Позиций несколько, и от позиции зависит, как компилятор соединяет несколько кандидатов: в ковариантных — через union, в контравариантных — через пересечение.</p>

    <h4>Как это работает</h4>
    <p>Ответы на сниппет: <code>A = string | number</code>, <code>B = string &amp; number</code>, то есть <code>never</code>. Это не курьёз, а прямое следствие вариантности. Свойство — ковариантная позиция: чтобы тип подошёл обоим кандидатам «сверху», нужен супертип, то есть union. Параметр функции — контравариантная: чтобы функция приняла и <code>string</code>, и <code>number</code>, нужен подтип обоих, то есть пересечение. На этом построен классический <code>UnionToIntersection</code>.</p>
    <p>Полный список позиций: возвращаемое значение (<code>T extends () =&gt; infer R</code>), параметры, элементы кортежа с rest (<code>[infer H, ...infer R]</code> — база всей рекурсии по кортежам), аргументы дженерика (<code>Promise&lt;infer U&gt;</code>), плейсхолдеры template literal.</p>
    <p><code>infer R extends C</code> (TS 4.8) добавляет ограничение прямо в вывод. Это делает две вещи: отсекает ветку, если вывод не удовлетворяет ограничению, и — для строковых литералов — <strong>приводит тип</strong>. <code>S extends \`\${infer N extends number}\`</code> на строке <code>'42'</code> даёт числовой литерал <code>42</code>, а не строку <code>'42'</code>.</p>

    <h4>Почему так</h4>
    <p>Правило «ковариантно — union, контравариантно — intersection» не выбрано произвольно: это единственный способ получить тип, безопасный во всех местах, где параметр встречается. Компилятор здесь просто честно применяет теорию подтипов, а не эвристику.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Несколько <code>infer U</code> с одним именем — не ошибка, а сознательное объединение кандидатов; если вы этого не планировали, результат будет неожиданным.</li>
    <li>Пересечение примитивов (<code>string &amp; number</code>) схлопывается в <code>never</code> без предупреждения — типичный «почему у меня never» в самописных утилитах.</li>
    <li><code>infer</code> в rest-позиции кортежа жаден: <code>[...infer A, infer B]</code> и <code>[infer A, ...infer B]</code> дают разный разбор, и первый заметно дороже по времени проверки.</li>
    <li>Без <code>extends</code>-ограничения выведенный из шаблона тип всегда строковый — <code>\`\${infer N}\`</code> на <code>'42'</code> даст <code>'42'</code>, а не <code>42</code>.</li>
    <li>При выводе из перегруженной функции берётся <strong>последняя</strong> сигнатура, а не «подходящая» — из-за этого <code>ReturnType</code> на перегрузках часто врёт.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как устроен <code>UnionToIntersection</code>?</strong> Дистрибуция раскладывает union на члены, каждый попадает в контравариантную позицию <code>(x: T) =&gt; void</code>, затем <code>infer</code> оттуда собирает кандидатов через пересечение. <strong>Почему <code>ReturnType</code> на перегрузках даёт не то?</strong> Потому что вывод берёт последнюю сигнатуру; лечится явным списком перегрузок или условным типом с несколькими ветками.</p>`,
    code: `type Ret<T> = T extends (...a: any[]) => infer R ? R : never;
type Head<T> = T extends [infer H, ...unknown[]] ? H : never;

// контравариантная позиция даёт пересечение
type UnionToIntersection<U> =
  (U extends any ? (x: U) => void : never) extends (x: infer I) => void ? I : never;
type X = UnionToIntersection<{ a: 1 } | { b: 2 }>;  // { a: 1 } & { b: 2 }

// infer extends приводит строковый литерал к числовому
type ToNum<S> = S extends \`\${infer N extends number}\` ? N : never;
type N = ToNum<'42'>;   // 42 (число), без extends было бы "42"`,
    tip: 'Пример с двумя одноимёнными infer в свойстве и в параметре — самая короткая демонстрация того, что вы понимаете вариантность, а не заучили список позиций.' },

  { id: 'tsx18',
    q: 'Когда использовать overloads, а когда union или дженерик?',
    snippet: `function get(k: 'a'): number;
function get(k: 'b'): string;
function get(k: string): unknown { return null as never; }

declare const key: 'a' | 'b';
get(key);   // почему ошибка?`,
    a: `<h4>Коротко</h4>
    <p>Перегрузки описывают набор допустимых пар «аргументы — результат», когда связь между ними нельзя выразить одной сигнатурой. Реализационная сигнатура снаружи невидима и против перегрузок почти не проверяется — по надёжности это дыра уровня <code>as</code>. Разрешение идёт <strong>первым совпадением сверху вниз</strong>.</p>

    <h4>Как это работает</h4>
    <p>Сниппет не компилируется, и это не баг. Чекер перебирает перегрузки по порядку и ищет <strong>первую</strong>, к которой список аргументов присваивается целиком. <code>'a' | 'b'</code> не присваивается ни <code>'a'</code>, ни <code>'b'</code> по отдельности; идеи «разложить union и объединить результаты» в разрешении перегрузок нет. Ошибка: «No overload matches this call».</p>
    <p>Реализационная сигнатура проверяется слабо: она обязана быть «совместимой» с каждой перегрузкой, но компилятор проверяет это очень мягко — тело <code>get</code> может вернуть строку там, где перегрузка обещала число, и ошибки не будет. Возврат <code>unknown</code> из реализации приемлем, потому что снаружи её никто не видит.</p>
    <p>В рантайме перегрузки не существуют вовсе: в JS уезжает одна функция. Все ветвления по числу и типу аргументов вы пишете руками.</p>

    <h4>Почему так</h4>
    <p>Механизм появился для описания существующих JS-API вроде <code>document.createElement</code>, где связь «аргумент → результат» задана таблицей. Правило «первое совпадение» проще и предсказуемее выбора «наиболее подходящей», как в C++, но требует ручного упорядочивания: узкие сигнатуры ставим первыми, иначе широкая перехватит вызов и вернёт слишком общий тип.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Union на входе не подходит ни к одной перегрузке — самая частая боль; лечится дженериком с <code>keyof</code> или отдельной перегрузкой для union.</li>
    <li>Порядок значим: широкая сигнатура сверху делает все нижние недостижимыми, и компилятор об этом не предупреждает.</li>
    <li>Плохо работают со spread: <code>get(...args)</code> не разрешается, если <code>args</code> — не кортеж.</li>
    <li>Сообщения об ошибках нечитаемы: чекер печатает последнюю попытку, а не самую близкую.</li>
    <li>Опциональные параметры в разных перегрузках порождают комбинации, которые легко забыть покрыть в теле.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что вместо?</strong> Дженерик с индексированным доступом (<code>&lt;K extends keyof M&gt;(k: K) =&gt; M[K]</code>) — он дружит с union и даёт <code>M['a'] | M['b']</code> без ошибок. Второй вариант — дискриминированный union параметров-объектов: <code>f({ kind: 'a' })</code>, где результат зависит от дискриминанта. <strong>Чем платим за условный возвращаемый тип?</strong> Внутри реализации он почти не проверяется, и тело обычно требует <code>as any</code> — компромисс осознанный, но его надо назвать вслух.</p>`,
    code: `interface M { a: number; b: string }

// дженерик вместо перегрузок: union на входе работает
declare function get<K extends keyof M>(k: K): M[K];
declare const key: 'a' | 'b';
get(key);          // number | string — ошибки нет

// перегрузки: узкие сигнатуры первыми
function el(tag: 'input'): HTMLInputElement;
function el(tag: string): HTMLElement;
function el(tag: string): HTMLElement { return document.createElement(tag); }

el('input');       // HTMLInputElement — если поменять порядок, будет HTMLElement`,
    tip: 'Фраза «перегрузки не разлагают union на входе, потому что разрешение ищет первое целиком подходящее совпадение» — конкретная боль плюс объяснение причины.' },

  { id: 'tsx19',
    q: 'Что реально даёт readonly и as const, и чего они не дают?',
    snippet: `type Frozen = { readonly x: number };
declare const f: Frozen;

const mutable: { x: number } = f;   // ошибка?
mutable.x = 42;                     // а что с f.x?`,
    a: `<h4>Коротко</h4>
    <p><code>readonly</code> — <strong>исключительно compile-time</strong> модификатор. Объект не заморожен, в рантайме от него не остаётся ничего, и — что важнее — <code>readonly</code>-свойства <strong>игнорируются при проверке присваиваемости объектов</strong>. Защита протекает по построению.</p>

    <h4>Как это работает</h4>
    <p>В сниппете ошибки нет: тип <code>{ readonly x: number }</code> спокойно присваивается <code>{ x: number }</code>. После этого <code>mutable.x = 42</code> меняет <strong>тот же самый объект</strong>, на который смотрит <code>f</code>, — компилятор ничего не заметил, рантайм ничего не запретил. Это известный unsound-компромисс: сделать <code>readonly</code> значимым для присваиваемости означало бы сломать огромное количество кода, где readonly-тип передают в функцию, которая ничего не мутирует, но объявила параметр обычным.</p>
    <p>С массивами строже: <code>readonly T[]</code> и <code>T[]</code> — разные типы, и присваивание идёт только в одну сторону. <code>number[]</code> присваивается <code>readonly number[]</code>, обратно — ошибка. Причина в том, что <code>ReadonlyArray</code> просто не имеет мутирующих методов, и структурная проверка честно это ловит.</p>
    <p><code>as const</code> делает три вещи разом: не расширяет литералы (<code>'a'</code> остаётся <code>'a'</code>), помечает все свойства <code>readonly</code> рекурсивно, превращает массивные литералы в кортежи. Это чисто типовая операция — <code>Object.freeze</code> она не вызывает и в JS-выводе исчезает бесследно.</p>

    <h4>Почему так</h4>
    <p>Настоящая иммутабельность в рантайме стоит денег: <code>Object.freeze</code> в V8 переводит объект в медленный режим доступа и стоит обхода всех свойств. TS предлагает бесплатную дисциплину «не мутировать» с проверкой в 90% мест — и честно не претендует на гарантию. Понимание этой границы важнее заученных правил.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Присваивание readonly-объекта изменяемому типу — молчаливая дыра, о которой почти никто не помнит.</li>
    <li><code>as const</code> не работает на выражениях с вычислениями: <code>{ x: a + b } as const</code> даст <code>number</code>, а не литерал.</li>
    <li><code>readonly</code> поверхностный: <code>readonly items: Item[]</code> запрещает переприсвоить массив, но не запрещает <code>items.push</code>.</li>
    <li><code>Readonly&lt;T&gt;</code> тоже поверхностный — глубокая версия требует рекурсивного маппера с отсечением <code>Date</code> и функций.</li>
    <li><code>Object.freeze</code> в нестрогом режиме молча игнорирует запись; ошибка бросается только в <code>'use strict'</code>, то есть в модулях.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как получить реальную неизменяемость?</strong> Библиотекой персистентных структур (Immer с <code>freeze</code>, Immutable.js) либо <code>Object.freeze</code> в dev-сборке и полагаться на типы в prod. <strong>Зачем <code>as const</code> в реальном коде?</strong> Связка <code>as const</code> + <code>typeof</code> + <code>[number]</code> — стандартный способ иметь одно определение и для рантайма (итерируемый список), и для типов (точный union).</p>`,
    code: `const a: readonly number[] = [1, 2];
const b: number[] = a;                 // ошибка — массивы строже

type P = { readonly x: number };
const p: { x: number } = { x: 1 } as P; // ошибки нет — дыра в объектах

const ROLES = ['admin', 'user'] as const;  // readonly ["admin", "user"]
type Role = typeof ROLES[number];          // "admin" | "user"

const n = 1, m = 2;
const bad = { x: n + m } as const;         // { readonly x: number } — не литерал`,
    tip: 'Назовите то, что readonly-модификаторы свойств не влияют на присваиваемость объектов (в отличие от массивов) — этот факт почти никто не помнит.' },

  { id: 'tsx20',
    q: 'Что делает Awaited и почему он оказался нетривиальным?',
    snippet: `type Naive<T> = T extends Promise<infer U> ? U : T;

type A = Naive<Promise<Promise<string>>>;   // ?
type B = Awaited<Promise<Promise<string>>>; // ?`,
    a: `<h4>Коротко</h4>
    <p><code>Awaited&lt;T&gt;</code> (TS 4.5) моделирует то, что делает <code>await</code>: рекурсивно разворачивает вложенные промисы, понимает произвольные thenable, дистрибутируется по union и оставляет не-промисы как есть. Наивная однократная распаковка через <code>infer</code> всё это ломает.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>A = Promise&lt;string&gt;</code> — наивный тип снял ровно один слой. <code>B = string</code> — <code>Awaited</code> раскрутил рекурсивно, ровно как рантайм: <code>await</code> разрешает промис промиса до значения, потому что <code>Promise.resolve</code> ассимилирует thenable.</p>
    <p>Реализация в lib.es5.d.ts выглядит так: если <code>T</code> — <code>null</code>/<code>undefined</code>, вернуть как есть; если у <code>T</code> есть метод <code>then</code>, принимающий колбэк, — вывести аргумент этого колбэка и применить <code>Awaited</code> к нему рекурсивно; иначе вернуть <code>T</code>. Проверка идёт именно по форме <code>{ then(onfulfilled: (value: infer V) =&gt; any): any }</code>, а не по имени <code>Promise</code> — поэтому работают и пользовательские thenable, и <code>PromiseLike</code>.</p>
    <p>Через <code>Awaited</code> типизирован <code>Promise.all</code>: он применяется к каждому элементу кортежа, поэтому позиции сохраняются и <code>Promise.all([a(), b()])</code> даёт <code>[A, B]</code>, а не <code>(A | B)[]</code>. У <code>Promise.allSettled</code> результат — union <code>PromiseFulfilledResult&lt;T&gt; | PromiseRejectedResult</code>, и до <code>value</code> надо сужаться по полю <code>status</code>.</p>

    <h4>Почему так</h4>
    <p>Рекурсивная распаковка отражает решение ES2015: <code>Promise&lt;Promise&lt;T&gt;&gt;</code> в рантайме не существует как наблюдаемое состояние — цепочка резолвится до конца. Тип, который этого не учитывает, обещает то, чего никогда не будет. Отсюда и <code>Awaited</code> появился в lib: слишком многие писали наивную версию и получали неверные типы у <code>Promise.all</code>.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>Awaited&lt;any&gt;</code> даёт <code>any</code>, и вся цепочка типов после этого молча теряет проверку.</li>
    <li><code>filter((r) =&gt; r.status === 'fulfilled')</code> до TS 5.5 не сужал тип массива — нужен был явный предикат. С 5.5 предикат выводится, и <code>value</code> доступен без каста.</li>
    <li>Тип не отражает тайминг: <code>await</code> на не-промисе всё равно откладывает продолжение на микротаск, но <code>Awaited&lt;string&gt;</code> — это просто <code>string</code>.</li>
    <li><code>Promise.all</code> на массиве (а не кортеже) даёт <code>T[]</code> и теряет позиции — нужен <code>as const</code> или кортежный литерал.</li>
    <li>Тип отклонения нигде не выражен: <code>Promise&lt;T&gt;</code> не параметризован ошибкой, поэтому <code>catch</code> всегда даёт <code>unknown</code> (при <code>useUnknownInCatchVariables</code>).</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как достать тип данных загрузчика?</strong> <code>Awaited&lt;ReturnType&lt;typeof loadUser&gt;&gt;</code> — стандартная идиома для типов из API-слоя. <strong>Почему нельзя типизировать ошибку промиса?</strong> Потому что бросить можно что угодно, включая не-<code>Error</code>; поэтому библиотеки вроде neverthrow возвращают <code>Result</code>-объект вместо исключения.</p>`,
    code: `type Data = Awaited<ReturnType<typeof loadUser>>;

// кортеж сохраняет позиции
const [user, posts] = await Promise.all([loadUser(), loadPosts()]);
// user: User, posts: Post[]

// allSettled требует сужения по status
const rs = await Promise.allSettled([loadUser(), loadPosts()]);
const ok = rs.filter((r) => r.status === 'fulfilled');
// TS 5.5: PromiseFulfilledResult<...>[] — .value доступен без каста

type Weird = Awaited<{ then(cb: (v: number) => void): void }>;  // number`,
    tip: 'Пример с allSettled и тем, что сужение фильтром по status заработало только с выводом предикатов в TS 5.5, показывает работу с этим в проде.' },

  { id: 'tsx21',
    q: 'Как типизировать this в TypeScript?',
    snippet: `class Q { where(): this { return this } }
class Sub extends Q { limit(): this { return this } }

new Sub().where().limit();   // какой тип у where() и почему это компилируется?`,
    a: `<h4>Коротко</h4>
    <p>Механизмов четыре, и их путают: параметр <code>this</code> в сигнатуре, polymorphic <code>this</code> как тип, маркер <code>ThisType&lt;T&gt;</code> и флаг <code>noImplicitThis</code>. Все они compile-time; в рантайме <code>this</code> остаётся обычным динамическим <code>this</code> из JS.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>where()</code> возвращает <code>this</code> — не <code>Q</code>, а «тип текущего получателя». Для экземпляра <code>Sub</code> это <code>Sub</code>, поэтому <code>.limit()</code> доступен. Это <strong>polymorphic this</strong>, база для fluent-API: query-билдеры, чейнинг в тест-хелперах, наследуемые модели. Если бы <code>where()</code> возвращал <code>Q</code>, цепочка обрывалась бы на первом же вызове базового метода.</p>
    <p><strong>Параметр <code>this</code></strong> — фиктивный первый параметр в сигнатуре: <code>function f(this: HTMLElement, e: Event)</code>. Он не входит в число аргументов, не существует в рантайме и полностью стирается при компиляции. Зато при вызове и при присваивании обработчика компилятор проверяет, что контекст подходит; <code>strictBindCallApply</code> распространяет проверку на <code>bind</code>, <code>call</code>, <code>apply</code>.</p>
    <p><strong><code>ThisType&lt;T&gt;</code></strong> — маркер без членов, распознаваемый чекером: если он попал в тип объектного литерала, <code>this</code> внутри методов этого литерала получает тип <code>T</code>. Работает только при <code>noImplicitThis</code> и придуман под Vue Options API, где <code>this</code> в методах собирается из data, computed и props.</p>
    <p><strong><code>noImplicitThis</code></strong> — флаг из <code>strict</code>: без него <code>this</code> в свободной функции молча становится <code>any</code>.</p>

    <h4>Почему так</h4>
    <p>В JS <code>this</code> определяется способом вызова, а не местом объявления. Статически вывести его нельзя, поэтому TS даёт способ <strong>задекларировать ожидание</strong> и проверить его в местах вызова, которые видит. Всё, что уходит в динамику (<code>fn.call(x)</code> из чужого кода, обработчик, повешенный строкой в HTML), остаётся непроверенным.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Параметр <code>this</code> стирается — соблазн подумать, что он что-то гарантирует в рантайме, ошибочен.</li>
    <li>Стрелочные функции не имеют своего <code>this</code>, и объявить им параметр <code>this</code> нельзя: это и есть штатный способ избежать темы целиком.</li>
    <li>Polymorphic <code>this</code> ломает присваиваемость: класс с методом <code>(): this</code> нельзя подставить туда, где ждут конкретный тип, если наследник добавил члены.</li>
    <li>Метод, вырванный из объекта (<code>const m = obj.method</code>), теряет контекст, и параметр <code>this</code> это как раз ловит — типичная причина «почему у меня undefined».</li>
    <li><code>ThisType</code> не работает в классах и не работает без <code>noImplicitThis</code> — молча.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что делает <code>strictBindCallApply</code>?</strong> Типизирует <code>bind</code>/<code>call</code>/<code>apply</code> точно, вместо старых сигнатур с <code>any[]</code>: неправильный контекст или лишний аргумент становятся ошибкой. <strong>Как типизировать обработчик DOM?</strong> <code>(this: HTMLButtonElement, e: MouseEvent) =&gt; void</code> — тогда внутри <code>this.disabled</code> проверяется, а стрелка на этом месте вызовет ошибку по контексту.</p>`,
    code: `class Q { where(): this { return this } }
class Sub extends Q { limit(): this { return this } }
new Sub().where().limit();   // Sub — polymorphic this

// параметр this: не аргумент, стирается при компиляции
function onClick(this: HTMLButtonElement, e: MouseEvent) {
  this.disabled = true;
}
btn.addEventListener('click', onClick);       // ок
const detached = onClick;
detached(new MouseEvent('click'));            // ошибка: нет контекста

// ThisType: контекст внутри литерала
declare function make<D, M>(o: { data: D; methods: M & ThisType<D & M> }): void;`,
    tip: 'Первый вопрос-подвох после ответа: параметр this полностью стирается при компиляции и не занимает позицию в аргументах — назовите это сами.' },

  { id: 'tsx22',
    q: 'Что такое declaration merging и какие правила слияния важно помнить?',
    snippet: `interface Box { a: string }
interface Box { b: number }
interface Box { a: number }

type T = { a: string };
type T = { b: number };
// где ошибка и какая?`,
    a: `<h4>Коротко</h4>
    <p>Declaration merging — объединение нескольких объявлений с одним именем в одной области видимости. Сливаются <code>interface</code> с <code>interface</code>, <code>namespace</code> с <code>namespace</code>, а также <code>namespace</code> с <code>class</code>, <code>function</code> или <code>enum</code>. <code>type</code> не сливается никогда.</p>

    <h4>Как это работает</h4>
    <p>В сниппете две ошибки. Третье объявление <code>Box</code> конфликтует: свойство <code>a</code> уже объявлено как <code>string</code>, повторное объявление с другим типом даёт «Subsequent property declarations must have the same type». Второе объявление <code>type T</code> даёт «Duplicate identifier 'T'» — алиасы типов принципиально не мержатся. Первые два <code>interface Box</code> сливаются нормально в <code>{ a: string; b: number }</code>.</p>
    <p>Механика: компилятор строит таблицу символов; у символа может быть несколько деклараций, и при разрешении типа он собирает члены со всех. Одноимённые <strong>свойства</strong> должны совпадать по типу, а одноимённые <strong>методы</strong> превращаются в перегрузки — и порядок здесь неочевиден: объявления из позже обработанных файлов идут <strong>раньше</strong> в списке перегрузок, а внутри одного файла порядок сохраняется.</p>
    <p>Слияние namespace с функцией даёт «функция со статическими свойствами» — так типизируют jQuery-подобные API и React-компоненты с <code>Component.displayName</code>. Внутри namespace сливаются только <code>export</code>-члены; неэкспортированные остаются локальными для своего блока.</p>

    <h4>Почему так</h4>
    <p>Merging существует ради двух задач: описывать JS-библиотеки, которые дописывают свойства в существующие объекты, и давать точку расширения плагинам. Именно поэтому <code>Window</code>, <code>ProcessEnv</code>, <code>JSX.IntrinsicElements</code> — интерфейсы: любой пакет может добавить в них члены из своего <code>.d.ts</code>. Алиасы типов задумывались как обычные вычисляемые выражения над типами (они могут быть union, кортежем, условным типом), а «дописать член» в union бессмысленно — отсюда запрет.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Публичный API библиотеки на <code>type</code> лишает пользователей возможности расширить его аугментацией — необратимое решение после релиза.</li>
    <li>Слияние срабатывает <strong>молча</strong>: опечатка в имени интерфейса не создаст новый тип, а незаметно дополнит существующий.</li>
    <li>Порядок перегрузок при слиянии зависит от порядка файлов, а он зависит от порядка импортов — источник плавающих ошибок в монорепе.</li>
    <li>Класс и интерфейс с одним именем сливаются: интерфейс дописывает члены в тип экземпляра, но реализацию за вас никто не напишет — получите ложное обещание.</li>
    <li>Слияние работает только в одной области видимости: интерфейс в модуле и интерфейс в global — разные символы.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Так <code>interface</code> или <code>type</code>?</strong> Для публичных API и объектных форм — <code>interface</code> (расширяемость плюс кеширование по имени, что дешевле для чекера). Для union, кортежей, условных и мапнутых типов — <code>type</code>, потому что интерфейс их выразить не может. <strong>Где merging используется каждый день?</strong> <code>declare global { interface Window {…} }</code>, поля в <code>Express.Request</code>, темы в styled-components.</p>`,
    code: `interface Box { a: string }
interface Box { b: number }        // Box = { a: string; b: number }

// методы становятся перегрузками
interface Api { call(x: string): void }
interface Api { call(x: number): void }

// функция + namespace = функция со статикой
function greet(n: string) {}
namespace greet { export const version = '1.0' }
greet('x'); greet.version;

// type не сливается
type T = { a: string };
// type T = { b: number };         // Duplicate identifier 'T'`,
    tip: 'Сильный вывод: расширяемость через merging — причина, по которой библиотеки выставляют публичные объектные типы интерфейсами; заменить interface на type в мажорном релизе значит отобрать у пользователей аугментацию.' },

  { id: 'tsx23',
    q: 'Как работает module augmentation и когда он необходим?',
    snippet: `// types.d.ts — в файле больше ничего нет
declare module 'express-serve-static-core' {
  interface Request { user?: { id: string } }
}

// почему после этого пропали ВСЕ типы express?`,
    a: `<h4>Коротко</h4>
    <p>Module augmentation — расширение типов чужого модуля из своего кода. <code>declare module 'lib' { … }</code> внутри <strong>файла-модуля</strong> дописывает члены в существующее объявление. Тот же синтаксис в файле-скрипте создаёт <strong>новый</strong> ambient-модуль и полностью перекрывает типы библиотеки.</p>

    <h4>Как это работает</h4>
    <p>В сниппете произошло ровно это. Файл не содержит ни одного <code>import</code> или <code>export</code>, значит для компилятора это скрипт, а не модуль. В скрипте <code>declare module 'x'</code> — объявление ambient-модуля: «вот полное описание пакета x». Компилятор берёт его вместо типов из <code>node_modules</code>, и <code>Request</code> с одним полем <code>user</code> — всё, что теперь известно про express.</p>
    <p>Починка — сделать файл модулем: добавить <code>import 'express';</code> сверху или <code>export {};</code> снизу. Тогда <code>declare module</code> трактуется как аугментация, находит существующий символ модуля и мержит объявления по правилам declaration merging.</p>
    <p>Отдельная форма — <code>declare global { … }</code>: она допустима только внутри модуля и дописывает члены в глобальную область (<code>Window</code>, <code>globalThis</code>, <code>NodeJS.ProcessEnv</code>). В скрипте она не нужна и запрещена: там всё и так глобально.</p>
    <p>Важное ограничение: аугментация не создаёт новые экспорты в модуле и не может изменить существующие. Она только дополняет интерфейсы и namespace внутри него.</p>

    <h4>Почему так</h4>
    <p>Разделение «скрипт против модуля» идёт от самого ES: файл без импортов и экспортов имеет глобальную область видимости. TS наследует это правило и вешает на него разную семантику <code>declare module</code>. Правило логичное, но диагностика ужасная: вместо ошибки вы получаете тихую потерю типов, которая выглядит как баг библиотеки.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Забытый <code>export {}</code> перезатирает типы пакета молча — самая частая ошибка в теме.</li>
    <li>Расширять можно только то, что объявлено <code>interface</code> или <code>namespace</code>. Добавить поле в <code>type</code> из библиотеки невозможно в принципе.</li>
    <li>Имя модуля должно совпадать с тем, под которым типы объявлены внутри пакета: для express это <code>'express-serve-static-core'</code>, а не <code>'express'</code>.</li>
    <li>Аугментация глобальна для всей программы: в монорепе она создаёт неявную связанность между пакетами и «работает» только если файл попал в <code>include</code>.</li>
    <li>Файл с аугментацией должен быть включён в компиляцию явно; если на него никто не ссылается и он не в <code>include</code>/<code>types</code>, эффекта не будет.</li>
    <li>Две аугментации одного интерфейса из разных пакетов конфликтуют, если объявляют одно поле с разными типами — и ошибка вылезает в чужом коде.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Где это реально нужно?</strong> Поле <code>user</code> в <code>Express.Request</code> после auth-middleware, кастомная тема в styled-components/MUI, свои <code>ImportMetaEnv</code>, <code>window.__APP_STATE__</code> из SSR, расширение <code>JSX.IntrinsicElements</code> для веб-компонентов. <strong>Чем плоха аугментация как архитектурное решение?</strong> Она делает глобальное утверждение о типе, которое верно только там, где middleware отработал; безопаснее локальный тип <code>AuthedRequest</code>, проверяемый явно.</p>`,
    code: `// правильно: файл — модуль благодаря import
import 'express';
declare module 'express-serve-static-core' {
  interface Request { user?: { id: string } }
}

// глобальная аугментация допустима только в модуле
declare global {
  interface Window { __APP_STATE__?: unknown }
  namespace NodeJS { interface ProcessEnv { API_URL?: string } }
}
export {};   // без этой строки файл стал бы скриптом`,
    tip: 'Назовите export {} и объясните механизм: без импорта или экспорта файл — скрипт, и declare module в нём объявляет новый ambient-модуль, перекрывая типы пакета целиком.' },

  { id: 'tsx24',
    q: 'Нужны ли namespace в 2025 году и где они всё ещё оправданы?',
    a: `<h4>Коротко</h4>
    <p>Для организации кода — нет: ES-модули их полностью вытеснили. Но <code>namespace</code> не мёртв: в <code>.d.ts</code> он остаётся штатным инструментом описания глобальных сущностей и UMD-библиотек. Правильная формулировка — «namespace в <code>.d.ts</code> нормально, namespace в <code>.ts</code> легаси».</p>

    <h4>Как это работает</h4>
    <p>Не-<code>declare</code> namespace <strong>генерирует код</strong>: компилятор превращает его в IIFE, присваивающую свойства объекту-контейнеру. Отсюда все проблемы. Такой объект непрозрачен для бандлера: доступ идёт через <code>MyNs.helper</code>, статический анализ не может доказать, что остальные члены не нужны, и tree-shaking не срабатывает. Второе — конфликт с <code>isolatedModules</code>: однофайловый транспилятор не знает, объявляет namespace только типы (тогда его надо стереть) или ещё и значения (тогда оставить), потому что для этого нужны типы из других файлов.</p>
    <p><code>declare namespace</code> кода не генерирует — это чистое описание. Именно так устроены <code>NodeJS.ProcessEnv</code>, <code>JSX.IntrinsicElements</code>, <code>Intl</code>-типы в lib.d.ts. Слияние namespace с функцией или классом даёт «значение с типами рядом»: <code>MyLib(o)</code> вызывается, а <code>MyLib.Options</code> — тип из того же имени.</p>

    <h4>Почему так</h4>
    <p>Namespace появился в 2012 году, когда модульной системы в JS не было и код склеивали конкатенацией файлов. С приходом ES-модулей задача «избежать глобальных коллизий» решается самой модульной системой, а namespace добавляет второй, параллельный механизм областей видимости — лишнюю сложность без выгоды. Официальный TS Handbook прямо не рекомендует их в новом коде.</p>

    <h4>Где всё ещё оправданы</h4>
    <ul>
    <li>В <code>.d.ts</code> для глобальных сущностей: <code>declare namespace NodeJS { interface ProcessEnv {…} }</code>.</li>
    <li>Для слияния с функцией или классом, чтобы дать типам пространство имён рядом со значением: <code>Foo.Props</code>, <code>Foo.Ref</code>.</li>
    <li>Для описания UMD-библиотек, которые доступны и как модуль, и как глобальная переменная (<code>export as namespace jQuery</code>).</li>
    <li>Для группировки типов в одном публичном объявлении, когда десяток плоских экспортов загрязняет API пакета.</li>
    </ul>

    <h4>Подводные камни</h4>
    <ul>
    <li>Namespace внутри модуля не становится глобальным — это частая иллюзия; он просто вложенная область в этом файле.</li>
    <li><code>namespace</code> с кодом ломает сборку через esbuild/SWC при <code>isolatedModules</code> и даёт невнятную ошибку.</li>
    <li><code>/// &lt;reference path&gt;</code>-стиль с namespace несовместим с <code>moduleResolution: bundler</code> и современными пакетами.</li>
    <li>Слияние namespace с классом требует, чтобы namespace шёл <strong>после</strong> объявления класса — иначе ошибка о порядке.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем заменить в модульном коде?</strong> Именованными экспортами плюс <code>import * as X from './x'</code> на стороне потребителя — тот же синтаксис доступа, но с полноценным tree-shaking. <strong>А <code>enum</code>?</strong> Тот же класс проблем: генерирует объект, конфликтует с <code>isolatedModules</code> в форме <code>const enum</code>; в новом коде предпочтительнее <code>as const</code>-объект или union строковых литералов.</p>`,
    code: `// .d.ts — namespace уместен
declare namespace MyLib {
  interface Options { debug?: boolean }
}
declare function MyLib(o?: MyLib.Options): void;
export = MyLib;

// .ts — так больше не пишем
namespace Utils { export const f = () => {} }   // генерирует IIFE, не шейкается

// современная замена
export const f = () => {};
// у потребителя: import * as Utils from './utils'`,
    tip: 'Формулировка «namespace в .d.ts нормально, namespace в .ts легаси» плюс причина: не-declare namespace генерирует IIFE, не шейкается и ломает isolatedModules.' },

  { id: 'tsx25',
    q: 'Как типизировать внешнюю библиотеку без типов? Что важно в .d.ts?',
    a: `<h4>Коротко</h4>
    <p>Порядок такой: проверить типы в самом пакете (поля <code>types</code>/<code>typings</code>/<code>exports</code> в его package.json), затем <code>@types/*</code> из DefinitelyTyped, и только потом писать своё. Свои типы — это <code>.d.ts</code> в <code>src/types</code>, включённый в <code>include</code>, с описанием ровно того, чем вы пользуетесь.</p>

    <h4>Как это работает</h4>
    <p>Разрешение типов идёт параллельно разрешению кода и обязано давать тот же файл. Для <code>moduleResolution: node10</code> компилятор смотрит <code>types</code> в package.json и <code>index.d.ts</code> рядом с точкой входа. Для <code>node16</code>/<code>nodenext</code> и <code>bundler</code> — читает поле <code>exports</code> и идёт по условиям: <code>types</code> внутри нужной ветки (<code>import</code> или <code>require</code>), затем сам файл. Если пакет описал <code>exports</code>, но не положил в него <code>types</code>, вы получите классику: «работает в рантайме, не резолвится в типах».</p>
    <p>Быстрая заглушка <code>declare module 'legacy-lib';</code> объявляет модуль с типом <code>any</code> — компилятор перестаёт ругаться, но и проверять перестаёт. Более полезная форма — описать только используемые экспорты: остальное просто не будет доступно, и это хорошо.</p>
    <p>В <code>.d.ts</code> не должно быть исполняемого кода: только <code>declare</code>-объявления, интерфейсы, типы и <code>export</code>. <code>export =</code> описывает CommonJS-модуль, экспортирующий одно значение, и требует у потребителя <code>esModuleInterop</code> или <code>allowSyntheticDefaultImports</code>, иначе <code>import x from</code> не соберётся.</p>

    <h4>Почему так</h4>
    <p>Раздельные <code>@types</code> появились, когда библиотеки писались на JS и типов не поставляли; DefinitelyTyped решил это через сообщество. Сегодня большинство пакетов везёт типы с собой, и проблема сместилась в другую плоскость — согласование <code>exports</code>, ESM/CJS и условий разрешения. Отсюда и инструмент <code>arethetypeswrong</code>, который проверяет пакет по всем комбинациям резолюции сразу.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Заглушку кладут в <code>typeRoots</code> вместо <code>include</code> — и она не подхватывается, потому что <code>typeRoots</code> ищет папки вида <code>@types/name</code>.</li>
    <li><strong>Dual-package hazard</strong>: пакет поставляет CJS и ESM сборки с разными <code>.d.ts</code>, типы расходятся, и <code>instanceof</code> между ними ломается в рантайме.</li>
    <li>Типы, которые тянут <code>devDependencies</code> в публичные сигнатуры, ломают сборку у потребителя: он этих пакетов не ставил.</li>
    <li><code>skipLibCheck: false</code> на большом дереве зависимостей превращает любую ошибку в чужом <code>.d.ts</code> в вашу — почти всегда включают <code>true</code>.</li>
    <li>Версия <code>@types/*</code> должна соответствовать мажору библиотеки; рассинхрон даёт типы, которых в рантайме нет.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как проверить свой пакет перед публикацией?</strong> <code>attw --pack</code> (arethetypeswrong) плюс <code>publint</code>: они ловят отсутствующие <code>types</code>-условия, неверные расширения и dual-package проблемы. <strong>Что такое <code>isolatedDeclarations</code> (TS 5.5)?</strong> Режим, требующий явных аннотаций у всего экспортируемого, чтобы <code>.d.ts</code> можно было сгенерировать пофайлово и параллельно — заметно ускоряет сборку монорепы.</p>`,
    code: `// src/types/legacy-lib.d.ts — включён через include, не через typeRoots
declare module 'legacy-lib' {
  export interface Options { retries?: number }
  export default function run(o?: Options): Promise<void>;
}

// CommonJS-библиотека
declare module 'old-cjs' {
  function run(): void;
  export = run;      // требует esModuleInterop у потребителя
}

// быстрая, но слепая заглушка: всё становится any
declare module 'no-types-at-all';`,
    tip: 'Упоминание attw (arethetypeswrong), publint и dual-package hazard сразу выдаёт человека, который сам публиковал пакеты, а не только их ставил.' },

  { id: 'tsx26',
    q: 'Как правильно типизировать переменные окружения и глобальные объекты?',
    snippet: `declare global {
  namespace NodeJS { interface ProcessEnv { API_URL: string } }
}

const url = process.env.API_URL;   // тип string
fetch(url + '/users');             // что произойдёт, если переменной нет?`,
    a: `<h4>Коротко</h4>
    <p>По умолчанию <code>process.env.X</code> — это <code>string | undefined</code>, и это честно. Объявить его как <code>string</code> через аугментацию можно, но тип ничего не проверяет: переменной может не быть, и вы получите <code>undefined/users</code> в запросе. Правильный подход — один <code>parse</code> схемой на старте.</p>

    <h4>Как это работает</h4>
    <p>В сниппете компилятор доволен, а рантайм — нет: <code>process.env.API_URL</code> вернёт <code>undefined</code>, конкатенация даст строку <code>'undefined/users'</code>, и запрос уйдёт на несуществующий адрес. Ошибка проявится не в момент старта, а в момент первого запроса — возможно, у пользователя. Аугментация здесь работает как <code>as string</code>, только на весь проект сразу.</p>
    <p>Второй слой — <strong>подстановка на этапе сборки</strong>. Vite и webpack через DefinePlugin делают текстовую замену: строка <code>process.env.API_URL</code> в исходнике заменяется на литерал <code>"https://…"</code>. Замена именно текстовая, по точному совпадению выражения. Поэтому <code>process.env[key]</code> с вычисляемым ключом не заменяется, и в браузере падает с «process is not defined» — самая частая ошибка при переносе кода с Node на фронт.</p>
    <p>Схема даёт третий, правильный слой: <code>Env.parse(import.meta.env)</code> при старте либо возвращает типизированный объект, либо кидает с внятным сообщением, какой переменной не хватает. Дальше весь код читает <code>env.API_URL</code>, где <code>string</code> — не обещание, а факт.</p>

    <h4>Почему так</h4>
    <p>Переменные окружения — это внешние данные ровно в том же смысле, что и ответ сервера: их состав зависит от того, как запустили процесс, и статически он неизвестен. Все проблемы с ними — частный случай общего правила «типы стираются, доверять внешним данным нельзя». Fail fast на старте дешевле, чем отладка <code>undefined</code> в URL через три часа после деплоя.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Аугментация <code>ProcessEnv</code> с типом <code>string</code> маскирует отсутствие переменной и даёт ложное чувство безопасности.</li>
    <li>Vite подставляет только переменные с префиксом <code>VITE_</code> — остальные молча становятся <code>undefined</code> в браузерном бандле.</li>
    <li>Всё, что попало в клиентский бандл, публично: секрет в <code>VITE_API_KEY</code> уедет в исходник страницы.</li>
    <li><code>process.env[key]</code> с вычисляемым ключом не подставляется, и в браузере обращение к <code>process</code> падает.</li>
    <li>Пустая строка — валидное значение переменной, и <code>z.string()</code> её пропустит: нужен <code>.min(1)</code> или <code>.url()</code>.</li>
    <li>SSR-состояние <code>window.__STATE__</code> надо объявлять как <code>unknown</code>, а не готовой моделью: оно пришло из HTML и по сути недоверенное.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Где вызывать parse?</strong> В отдельном модуле <code>env.ts</code>, импортируемом первым из точки входа, — так падение произойдёт до старта приложения. <strong>А как быть в тестах?</strong> Отдельная схема с дефолтами или фабрика <code>createEnv(source)</code>, чтобы не тянуть реальный <code>process.env</code> и не заводить <code>.env.test</code> с секретами.</p>`,
    code: `// env.ts — импортируется первым
const Env = z.object({
  VITE_API_URL: z.string().url(),
  VITE_SENTRY_DSN: z.string().min(1).optional(),
});
export const env = Env.parse(import.meta.env);  // упадёт на старте, а не в проде

// аугментация — дополнение к валидации, не замена
declare global {
  interface ImportMetaEnv { readonly VITE_API_URL: string }
}

// это не будет подставлено бандлером и упадёт в браузере
const key = 'VITE_API_URL';
const broken = import.meta.env[key];`,
    tip: 'Мысль, которую хотят услышать: типы не проверяют наличие переменной в рантайме, поэтому нужен parse на старте — падение при запуске дешевле, чем строка «undefined/users» в URL у пользователя.' },

  { id: 'tsx27',
    q: 'Как типизировать дженерик-компонент в React, включая forwardRef и полиморфный as?',
    snippet: `const List = forwardRef(<T,>(props: { items: T[] }, ref: Ref<HTMLUListElement>) => {
  return <ul ref={ref}>{props.items.map(String)}</ul>;
});

<List items={[1, 2, 3]} />   // почему T здесь не выводится?`,
    a: `<h4>Коротко</h4>
    <p>Обычный дженерик-компонент работает без хитростей: <code>function List&lt;T&gt;(p: Props&lt;T&gt;)</code> выводит <code>T</code> из пропсов, как обычная функция. Проблемы начинаются с обёрток — <code>forwardRef</code> и <code>memo</code> «схлопывают» дженерик, потому что их собственные сигнатуры возвращают конкретный тип.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>T</code> теряется на границе <code>forwardRef</code>. Её сигнатура — <code>forwardRef&lt;T, P&gt;(render: …) =&gt; ForwardRefExoticComponent&lt;…&gt;</code>: параметры типа принадлежат самому <code>forwardRef</code>, а не результату. Компилятор вынужден зафиксировать <code>T</code> в момент вызова, кандидатов нет — и он подставляет <code>unknown</code>. Результат: компонент есть, дженерик исчез, <code>items</code> имеет тип <code>unknown[]</code>.</p>
    <p>Обходов два. Классический — привести результат: <code>const List = forwardRef(inner) as &lt;T&gt;(p: Props&lt;T&gt; &amp; { ref?: Ref&lt;HTMLUListElement&gt; }) =&gt; ReactElement</code>. Это осознанный <code>as</code>: рантайм не меняется, меняется только заявленная сигнатура. Современный — <strong>React 19</strong>, где <code>ref</code> стал обычным пропом функционального компонента, и <code>forwardRef</code> просто не нужен. Дженерик остаётся дженериком, потому что обёртки нет.</p>
    <p>С <code>memo</code> ровно та же история и то же лекарство: <code>const M = memo(C) as typeof C</code>.</p>
    <p>Отдельная тема — полиморфный <code>as</code>. Он требует <code>ElementType</code>, <code>ComponentPropsWithoutRef&lt;E&gt;</code> и вычитания собственных пропсов через <code>Omit</code>. Это работает, но дорого: каждый вызов инстанцирует большой union пропсов всех HTML-элементов, и IDE начинает подтормаживать на автодополнении.</p>

    <h4>Почему так</h4>
    <p>JS не умеет возвращать «дженерик-значение»: параметр типа существует только у функции, а не у объекта, который она вернула. <code>forwardRef</code> возвращает объект (<code>ForwardRefExoticComponent</code>), поэтому нести <code>T</code> ему негде. React 19 убрал обёртку не ради типов, а ради упрощения API — но побочный эффект для TypeScript оказался решающим.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>В <code>.tsx</code> стрелочный дженерик требует запятой — <code>&lt;T,&gt;(…)</code>, иначе парсер видит JSX-тег. С <code>extends</code> (<code>&lt;T extends unknown&gt;</code>) тоже работает.</li>
    <li>Приведение через <code>as</code> убивает проверку: если сигнатура разойдётся с реализацией, никто не заметит. Пишите его один раз в утилите, а не в каждом компоненте.</li>
    <li>Полиморфный <code>as</code> ломает <code>displayName</code> и React DevTools, а сообщения об ошибках в нём читать невозможно.</li>
    <li><code>memo</code> с дженериком и кастомным <code>areEqual</code> теряет типизацию сравнителя — его аргументы становятся <code>unknown</code>.</li>
    <li>Дефолт параметра типа (<code>&lt;T = unknown&gt;</code>) молча съедает ошибки вывода: вместо «не смог вывести» получите <code>unknown</code> без предупреждения.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как ограничить полиморфизм и не убить компилятор?</strong> Ограничить <code>as</code> союзом из 5–7 реально нужных тегов вместо всего <code>ElementType</code> — в дизайн-системах это стандартная практика. <strong>Что изменилось в типах React 19?</strong> <code>ref</code> — обычный проп, <code>ReactNode</code> больше не включает <code>{}</code>, а <code>JSX</code> переехал из глобального пространства в неймспейс <code>React.JSX</code>.</p>`,
    code: `type Props<T> = { items: T[]; render: (i: T) => React.ReactNode };

// React 19: ref — обычный проп, дженерик не теряется
export function List<T>({ items, render, ref }: Props<T> & { ref?: React.Ref<HTMLUListElement> }) {
  return <ul ref={ref}>{items.map(render)}</ul>;
}

// React 18: приведение возвращает дженерик руками
const Inner = <T,>(p: Props<T>, ref: React.Ref<HTMLUListElement>) => null;
const List18 = React.forwardRef(Inner) as <T>(
  p: Props<T> & { ref?: React.Ref<HTMLUListElement> }
) => React.ReactElement;`,
    tip: 'Скажите, что forwardRef теряет дженерик из-за возврата ForwardRefExoticComponent, и что React 19 снимает проблему целиком, сделав ref обычным пропом.' },

  { id: 'tsx28',
    q: 'Зачем zod, если есть типы? Где граница между статикой и рантаймом?',
    snippet: `const res = await fetch('/api/user');
const user = await res.json() as User;

user.profile.name.trim();   // что здесь гарантирует TypeScript?`,
    a: `<h4>Коротко</h4>
    <p>Ничего не гарантирует. <code>res.json()</code> возвращает <code>any</code> (в свежих типах — <code>Promise&lt;any&gt;</code>), <code>as User</code> — просто утверждение разработчика, а типы стираются при компиляции. Любая точка входа извне — сеть, localStorage, URL, postMessage, конфиг, форма — статически не защищена. Схема даёт <strong>одну</strong> декларацию, из которой берутся и рантайм-проверка, и тип.</p>

    <h4>Как это работает</h4>
    <p>В сниппете компилятор проверяет только то, что <code>User</code> имеет поле <code>profile</code> с полем <code>name</code>. Если бэкенд переименовал поле, вернул <code>null</code> или отдал 500 с HTML-страницей, то <code>user.profile</code> будет <code>undefined</code>, и вы получите «Cannot read properties of undefined» в компоненте — далеко от места, где данные пришли.</p>
    <p>Схема разворачивает это в обратную сторону: <code>const User = z.object({…})</code> — это <strong>значение</strong>, живущее в рантайме. <code>User.parse(data)</code> обходит объект, проверяет каждое поле и либо возвращает типизированные данные, либо кидает <code>ZodError</code> с путём до проблемного поля. Тип берётся оттуда же: <code>type User = z.infer&lt;typeof User&gt;</code> — компилятор выводит его из схемы, поэтому разойтись они не могут физически.</p>
    <p>Разница в том, где падает программа: с <code>as</code> — в случайном месте UI через несколько тиков; со схемой — на границе, с сообщением «expected string at profile.name, received null».</p>

    <h4>Почему так</h4>
    <p>TS сознательно не генерирует рантайм-проверок: это увеличило бы размер бандла для всех и сделало бы стоимость типов непредсказуемой. Позиция команды — «типы описывают то, что уже верно», а обеспечение истинности на границе — задача кода. Отсюда и всплеск schema-first библиотек: они закрывают ровно эту дыру.</p>

    <h4>Подводные камни и цена</h4>
    <ul>
    <li>Размер бандла: zod v3 добавляет заметные килобайты и плохо шейкается; valibot и zod v4 mini дают модульный API и в разы меньший вес.</li>
    <li>Стоимость валидации на больших списках реальна: <code>parse</code> тысячи объектов — это тысячи проходов; для горячих путей лучше <code>safeParse</code> выборочно или быстрые кодеки.</li>
    <li>Сложные схемы нагружают <strong>компилятор</strong>: <code>z.infer</code> над глубоко вложенной схемой считается на каждом hover.</li>
    <li><code>z.object</code> по умолчанию отбрасывает лишние поля — это тихая потеря данных, если вы прокидываете объект дальше.</li>
    <li>Схема не спасает от логических ошибок бэка: она подтвердит, что <code>price</code> — число, но не что оно в тех же копейках.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Всё ли валидировать?</strong> Нет. Полностью — критичные данные и всё, что попадает в деньги, права и хранилище. Не валидировать то, что пришло из собственного типизированного RPC (tRPC) с проверкой на своей стороне. <strong>Чем заменить, если бандл дорог?</strong> Генерацией типов из OpenAPI/GraphQL: рантайм-стоимость нулевая, но защиты от того, что бэк нарушил свою же схему, нет — это осознанный размен.</p>`,
    code: `const User = z.object({
  id: z.string().uuid(),
  age: z.number().int().nonnegative(),
  profile: z.object({ name: z.string().min(1) }),
});
type User = z.infer<typeof User>;   // тип выведен из схемы — разойтись не могут

const res = await fetch('/api/user');
if (!res.ok) throw new Error('HTTP ' + res.status);

const parsed = User.safeParse(await res.json());
if (!parsed.success) {
  report(parsed.error.issues);      // путь до поля, а не «undefined is not an object»
  return null;
}
parsed.data.profile.name.trim();    // здесь string — уже по факту, а не по обещанию`,
    tip: 'Формулировка «типы — про рефакторинг, схемы — про доверие к данным» плюс уточнение границы: RPC со своей проверкой валидировать повторно не нужно.' },

  { id: 'tsx29',
    q: 'Какие флаги tsconfig вы включаете и почему strict — это не один флаг?',
    a: `<h4>Коротко</h4>
    <p><code>strict: true</code> — зонтик над восемью отдельными флагами, а не один переключатель. Самый ценный из них — <code>strictNullChecks</code>: без него <code>null</code> и <code>undefined</code> входят в каждый тип, и вся система типов теряет смысл.</p>

    <h4>Как это работает</h4>
    <p>Сам по себе <code>strict</code> ничего не проверяет: при чтении tsconfig компилятор разворачивает его в восемь независимых флагов, каждый из которых можно переопределить рядом (<code>"strict": true, "strictPropertyInitialization": false</code> — валидная комбинация). Вот что входит в набор.</p>
    <ul>
    <li><code>strictNullChecks</code> — <code>null</code>/<code>undefined</code> перестают быть членами всех типов.</li>
    <li><code>noImplicitAny</code> — параметр без аннотации и без выводимого типа становится ошибкой.</li>
    <li><code>strictFunctionTypes</code> — контравариантная проверка параметров функциональных типов (но не методов).</li>
    <li><code>strictBindCallApply</code> — точные сигнатуры <code>bind</code>/<code>call</code>/<code>apply</code> вместо <code>any[]</code>.</li>
    <li><code>strictPropertyInitialization</code> — поле класса должно быть инициализировано или помечено <code>!</code>.</li>
    <li><code>noImplicitThis</code> — <code>this</code> в свободной функции больше не <code>any</code>.</li>
    <li><code>useUnknownInCatchVariables</code> — <code>catch (e)</code> даёт <code>unknown</code>, а не <code>any</code>.</li>
    <li><code>alwaysStrict</code> — <code>'use strict'</code> в выводе и парсинг в strict-режиме.</li>
    </ul>

    <h4>Что включают сверх strict</h4>
    <ul>
    <li><code>noUncheckedIndexedAccess</code> — <code>arr[i]</code> и <code>rec[key]</code> дают <code>T | undefined</code>.</li>
    <li><code>exactOptionalPropertyTypes</code> — различает «ключа нет» и «ключ равен <code>undefined</code>».</li>
    <li><code>noImplicitOverride</code> — <code>override</code> обязателен при переопределении метода; ловит опечатки в именах.</li>
    <li><code>noFallthroughCasesInSwitch</code>, <code>noPropertyAccessFromIndexSignature</code> (доступ через точку только к объявленным полям).</li>
    <li><code>isolatedModules</code> и <code>verbatimModuleSyntax</code> — обязательны, если собираете не через <code>tsc</code>.</li>
    <li><code>skipLibCheck: true</code> — почти всегда, ради скорости: чужие <code>.d.ts</code> не ваша забота.</li>
    </ul>

    <h4>Почему так</h4>
    <p>Флаги вводились в разные версии и по-разному ломают существующий код. <code>strictNullChecks</code> в большом легаси даёт тысячи ошибок, <code>noImplicitThis</code> — десятки. Разделение позволяет мигрировать поэтапно и держать промежуточные состояния зелёными. Зонтик <code>strict: true</code> добавлен позже, чтобы новые проекты не собирали конфиг по кусочкам — и чтобы новые флаги включались автоматически при апгрейде.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>strict: true</code> включает и будущие флаги: апгрейд минорной версии TS может сломать сборку. В CI это лечится пиннингом версии, а не отключением strict.</li>
    <li><code>strictPropertyInitialization</code> конфликтует с DI-фреймворками и ORM — там поля заполняются извне; спасает <code>declare</code> или <code>!</code>, но <code>!</code> надо ставить осознанно.</li>
    <li><code>noUnusedLocals</code>/<code>noUnusedParameters</code> лучше отдать ESLint: у компилятора нет автофикса, и он мешает при отладке, когда переменную временно закомментировали.</li>
    <li><code>skipLibCheck: false</code> на большом дереве превращает баг в чужом <code>.d.ts</code> в вашу красную сборку.</li>
    <li>Отдельный <code>tsconfig</code> для тестов часто забывают, и в тестах strict оказывается выключен.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как включить strict в легаси?</strong> Включить глобально, а старые папки вынести в отдельный проект с ослабленным конфигом через project references — так новый код сразу пишется строго, а долг виден и конечен. Откладывать «пока не почистим» значит не включить никогда. <strong>Что даёт <code>useUnknownInCatchVariables</code> на практике?</strong> Заставляет писать <code>e instanceof Error ? e.message : String(e)</code> — и ловит случаи, когда бросили строку или объект.</p>`,
    code: `{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "noEmit": true
  }
}`,
    tip: 'Стратегия миграции: включить strict глобально сразу и вынести легаси в отдельный проект с ослабленным конфигом через project references — долг становится видимым и конечным вместо «включим, когда почистим».' },

  { id: 'tsx30',
    q: 'Что делают noUncheckedIndexedAccess и exactOptionalPropertyTypes и почему их часто выключают?',
    snippet: `const list: string[] = [];
const first = list[0];
first.trim();            // ошибка при каком флаге?

type O = { a?: number };
const o: O = { a: undefined };   // а здесь при каком?`,
    a: `<h4>Коротко</h4>
    <p><code>noUncheckedIndexedAccess</code> добавляет <code>| undefined</code> к результату доступа по индексу — <code>arr[i]</code>, <code>record[key]</code>. <code>exactOptionalPropertyTypes</code> различает «ключа нет» и «ключ равен <code>undefined</code>». Оба ловят реальные баги и оба шумят, поэтому в существующих проектах их чаще всего выключают.</p>

    <h4>Как это работает</h4>
    <p>В сниппете первая ошибка — от <code>noUncheckedIndexedAccess</code>: <code>list[0]</code> получает тип <code>string | undefined</code>, потому что массив может быть пуст, и <code>.trim()</code> на нём запрещён. Вторая — от <code>exactOptionalPropertyTypes</code>: тип <code>{ a?: number }</code> при этом флаге означает «поля может не быть, но если есть — это число», и явное <code>a: undefined</code> ему не соответствует. Без флага <code>?</code> раскрывается в <code>a?: number | undefined</code>, и присваивание проходит.</p>
    <p>Оба флага меняют не рантайм, а модель типов: они делают её ближе к реальному JS. До них <code>arr[999]</code> имел тип <code>T</code> — очевидная ложь, из-за которой <code>arr[0].id</code> на пустом списке падал в проде без единого предупреждения.</p>

    <h4>Почему их не включают по умолчанию</h4>
    <p><code>noUncheckedIndexedAccess</code> нельзя сделать умным: компилятор не доказывает, что <code>i &lt; arr.length</code>, поэтому шумит даже в цикле <code>for (let i = 0; i &lt; arr.length; i++)</code>, где индекс заведомо валиден. Включение его в <code>strict</code> сломало бы почти каждый существующий проект. <code>exactOptionalPropertyTypes</code> упирается в экосистему: React-пропсы и повсеместный паттерн <code>{...props, a: undefined}</code> с ним не собираются.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Шум в индексных циклах лечится сменой стиля: <code>for...of</code>, <code>entries()</code>, <code>at()</code>, деструктуризация — они дают корректные типы без проверок.</li>
    <li>Кортежи: <code>noUncheckedIndexedAccess</code> не трогает доступ по литеральному индексу в пределах длины кортежа, но срабатывает на <code>rest</code>-элементах.</li>
    <li>Соблазн заглушить всё через <code>!</code> сводит выигрыш от флага к нулю — и делает код хуже, чем без него.</li>
    <li><code>exactOptionalPropertyTypes</code> ломает spread-паттерны в React и в билдерах опций; иногда приходится писать <code>...(v !== undefined &amp;&amp; { a: v })</code>.</li>
    <li>Флаг важен там, где <code>undefined</code> в PATCH-запросе означает «не трогать поле», а <code>null</code> — «сбросить»: без него эти два случая неотличимы в типах.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Стоит ли включать в существующем проекте?</strong> Оба дешевле включать на новом коде: ретроспективное включение даёт сотни ошибок, большинство из которых ложные. Практичный путь — включить в новом пакете монорепы и не тащить в старые. <strong>Что делать с <code>Object.keys</code> при этих флагах?</strong> Итерироваться по явному <code>as const</code>-списку ключей: тогда индексный доступ типизирован точно, а <code>undefined</code> не появляется.</p>`,
    code: `// noUncheckedIndexedAccess
const first = list[0];              // string | undefined
if (first !== undefined) first.trim();

for (const item of list) item.trim();   // for...of — undefined не появляется
const [head] = list;                    // string | undefined — тоже честно

// exactOptionalPropertyTypes
type Patch = { name?: string; avatar?: string | null };
const p1: Patch = {};                    // «не трогать»
const p2: Patch = { avatar: null };      // «сбросить»
const p3: Patch = { name: undefined };   // ошибка — намерение неясно`,
    tip: 'Честный ответ: оба флага дешевле включать на новом коде, а ретроспективно они дают сотни ложных срабатываний — и заглушённые ! делают результат хуже, чем без флага.' },

  { id: 'tsx31',
    q: 'Что такое isolatedModules и какие конструкции он запрещает?',
    snippet: `// types.ts
export interface User { id: string }

// index.ts
export { User } from './types';   // почему это ошибка при isolatedModules?`,
    a: `<h4>Коротко</h4>
    <p><code>isolatedModules</code> гарантирует, что каждый файл можно транспилировать <strong>независимо</strong>, не зная типов из других файлов. Это жёсткое требование любого однофайлового транспилятора: esbuild, SWC, Babel, Vite. Флаг ничего не меняет в выводе <code>tsc</code> — он лишь запрещает конструкции, которые такой транспилятор не смог бы обработать корректно.</p>

    <h4>Как это работает</h4>
    <p>В сниппете esbuild видит только <code>index.ts</code>. Строка <code>export { User } from './types'</code> для него — реэкспорт значения, и он оставит в выводе <code>export { User } from './types'</code>. В рантайме модуль <code>./types</code> не экспортирует ничего (интерфейс стёрся), и импорт упадёт с «does not provide an export named 'User'». Чтобы транспилятор знал, что это тип, нужно написать это явно: <code>export type { User } from './types'</code>.</p>
    <p>Ровно та же логика за остальными запретами. <code>const enum</code> требует, чтобы транспилятор подставил значение из другого файла — а он туда не заглядывает. Файл без импортов и экспортов для TS может быть модулем или скриптом в зависимости от конфигурации — нужен явный <code>export {}</code>. <code>export =</code> и часть форм declaration merging тоже требуют межфайлового контекста.</p>
    <p>С TS 5.0 есть родственный флаг <code>verbatimModuleSyntax</code>, который делает правило ещё жёстче и заодно точнее: импорты без <code>type</code> сохраняются дословно, с <code>type</code> — вырезаются целиком.</p>

    <h4>Почему так</h4>
    <p>Проверка типов принципиально требует всей программы, а транспиляция — нет. Разделив их, экосистема получила сборку в десятки раз быстрее: esbuild выкидывает аннотации по синтаксису, не строя граф типов. Цена — небольшой набор конструкций, где синтаксис неоднозначен без знания типов. <code>isolatedModules</code> просто делает эту цену явной ошибкой компиляции вместо тихого бага в сборке.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Ошибка проявляется только в рантайме продовой сборки, если флаг не включён, — <code>tsc</code> её не покажет.</li>
    <li><code>const enum</code> в библиотеке, которую вы потребляете, тоже проблема: с <code>isolatedModules</code> её значения не инлайнятся и обращение падает.</li>
    <li>Смешивание <code>export type</code> и <code>export</code> в одной строке (<code>export { type User, createUser }</code>) поддерживается только с TS 5.0.</li>
    <li>Декораторы и <code>emitDecoratorMetadata</code> с однофайловыми транспиляторами работают частично — метаданные требуют типов.</li>
    <li>Флаг не проверяет типы: <code>tsc --noEmit</code> в CI всё равно нужен, иначе ошибки типов вообще никто не увидит.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как выглядит правильный пайплайн?</strong> Сборка через Vite/esbuild с <code>isolatedModules</code>, отдельный шаг <code>tsc --noEmit</code> в CI и в pre-commit как чекер типов. <strong>Чем <code>verbatimModuleSyntax</code> отличается?</strong> <code>isolatedModules</code> запрещает неоднозначное, <code>verbatimModuleSyntax</code> задаёт детерминированное правило стирания — второй строже и заменяет старые <code>importsNotUsedAsValues</code> и <code>preserveValueImports</code>.</p>`,
    code: `import type { User } from './types';
export type { User };                    // корректно
// export { User } from './types';       // ошибка при isolatedModules

// TS 5.0: смешанный импорт
import { type Options, run } from './lib';

// файл без импортов и экспортов — нужен явный маркер модуля
export {};

// const enum вместо этого:
const Level = { Low: 0, High: 1 } as const;
type Level = typeof Level[keyof typeof Level];`,
    tip: 'Суть в одном предложении: isolatedModules нужен, потому что esbuild и SWC не видят типы других файлов и не могут отличить реэкспорт типа от реэкспорта значения.' },

  { id: 'tsx32',
    q: 'Чем moduleResolution bundler отличается от node16 и classic?',
    a: `<h4>Коротко</h4>
    <p><code>moduleResolution</code> описывает, как TS ищет файл по спецификатору импорта. Настройка должна <strong>совпадать с тем, что делает ваш рантайм или сборщик</strong> — иначе типы разрешатся из одного файла, а код возьмёт другой. Это не шкала «старое → новое», а выбор модели среды.</p>

    <h4>Четыре режима</h4>
    <ul>
    <li><code>classic</code> — доисторический алгоритм без <code>node_modules</code>. Существует только для обратной совместимости; с TS 5.0 его нельзя использовать с современными значениями <code>module</code>.</li>
    <li><code>node10</code> (раньше назывался <code>node</code>) — легаси CommonJS-алгоритм Node: подъём по <code>node_modules</code>, поле <code>main</code>, импорт без расширения. <strong>Не понимает <code>exports</code></strong> — отсюда большинство проблем с современными пакетами, которые публикуют типы только через условия.</li>
    <li><code>node16</code>/<code>nodenext</code> — реальные правила Node ESM: читает <code>exports</code> и <code>imports</code>, требует расширение <code>.js</code> в относительных ESM-импортах, различает CJS и ESM по полю <code>type</code> в package.json и по расширениям <code>.mts</code>/<code>.cts</code>. Самый строгий и единственно верный для кода, который запускает Node напрямую.</li>
    <li><code>bundler</code> (TS 5.0) — гибрид: понимает <code>exports</code>, но не требует расширений и разрешает ESM-синтаксис там, где Node потребовал бы CJS. Ровно та модель, по которой работают Vite, webpack и esbuild. Требует <code>module</code> не ниже <code>es2015</code>.</li>
    </ul>

    <h4>Как это работает</h4>
    <p>Ключевая разница — <strong>условные экспорты</strong>. Пакет объявляет <code>"exports": { ".": { "types": "./d.ts", "import": "./esm.js", "require": "./cjs.js" } }</code>. Режимы <code>node16</code> и <code>bundler</code> идут по этой карте и берут ветку в зависимости от того, как импортируют. <code>node10</code> карту игнорирует и падает на <code>main</code>, которого в современных пакетах может не быть вовсе — отсюда «Cannot find module or its corresponding type declarations» при работающем рантайме.</p>
    <p>Требование расширения <code>.js</code> в <code>node16</code> удивляет: в <code>.ts</code>-файле надо писать <code>import './a.js'</code>, хотя файл называется <code>a.ts</code>. Причина — TS не переписывает спецификаторы: что написано, то и уедет в вывод, а Node ESM расширение требует. С TS 5.7 есть <code>rewriteRelativeImportExtensions</code>, позволяющий писать <code>.ts</code>.</p>
    <p><code>module: preserve</code> (TS 5.4) подразумевает <code>moduleResolution: bundler</code> и сохраняет синтаксис импортов дословно — рекомендуемая связка для фронтенда со сборщиком.</p>

    <h4>Почему так</h4>
    <p>Единого «правильного» алгоритма нет: Node, webpack и Vite ищут файлы по-разному, и TS обязан лишь <strong>предсказать</strong> результат чужого поиска. Поэтому режимов несколько, и выбор — это описание среды, а не уровень строгости.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Тип-ошибки, которых нет в рантайме, и наоборот — почти всегда следствие рассинхрона резолюции со сборщиком.</li>
    <li><code>bundler</code> в проекте, который реально запускает Node (скрипты, SSR-сервер), скроет отсутствующие расширения — и упадёт при запуске.</li>
    <li><code>paths</code> в tsconfig влияет только на типы: сборщику нужен свой alias, иначе рантайм не найдёт модуль.</li>
    <li>Монорепа с миксом CJS и ESM пакетов требует <code>node16</code> и аккуратных <code>exports</code>; <code>bundler</code> замаскирует проблему до продакшена.</li>
    <li>Смена режима резолюции — ломающее изменение для потребителей библиотеки, если меняется и то, как публикуются типы.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что выбрать в приложении на Vite?</strong> <code>module: preserve</code> (или <code>esnext</code>) плюс <code>moduleResolution: bundler</code>, <code>noEmit: true</code>, сборка через Vite. <strong>А в библиотеке?</strong> <code>nodenext</code>: он строже всех и покажет проблемы, с которыми столкнутся ваши потребители, ещё до публикации. Проверять результат — через <code>attw</code>.</p>`,
    code: `// tsconfig приложения со сборщиком
{ "compilerOptions": {
  "module": "preserve", "moduleResolution": "bundler",
  "noEmit": true, "verbatimModuleSyntax": true
}}

// tsconfig библиотеки / Node-кода
{ "compilerOptions": {
  "module": "nodenext", "moduleResolution": "nodenext",
  "declaration": true
}}

// при nodenext расширение обязательно — TS не переписывает спецификатор
import { helper } from './helper.js';   // файл на диске helper.ts`,
    tip: 'Ключевой тезис: резолюция должна описывать вашу среду выполнения, а не «включить самое новое». bundler в проекте, который запускает Node, скрывает ошибки до продакшена.' },

  { id: 'tsx33',
    q: 'Что делает verbatimModuleSyntax и какую проблему он решает?',
    snippet: `import { Reflector } from './reflect-metadata-setup';

let r: Reflector;   // единственное использование — типовое

// что окажется в JS-выводе без verbatimModuleSyntax?`,
    a: `<h4>Коротко</h4>
    <p>Правило детерминированное: импорты и экспорты <strong>без</strong> модификатора <code>type</code> сохраняются в выводе дословно, с модификатором — удаляются целиком. Флаг убирает историческое «умное» стирание, которое смотрело на фактическое использование и порождало два зеркальных класса багов.</p>

    <h4>Как это работает</h4>
    <p>В сниппете без флага строка импорта исчезнет из вывода: <code>Reflector</code> использован только в типовой позиции, значит компилятор счёл импорт ненужным и вырезал его. Вместе с ним исчез и <strong>сайд-эффект модуля</strong> — регистрация <code>reflect-metadata</code>, установка полифилла, подписка на глобальное событие. Приложение падает в рантайме с ошибкой, не имеющей никакого отношения к типам.</p>
    <p>Зеркальный баг — в однофайловых транспиляторах: esbuild не знает, тип это или значение, и оставляет импорт. В рантайме модуль не экспортирует такого имени, и сборка ломается на этапе линковки.</p>
    <p>С <code>verbatimModuleSyntax</code> обе ситуации исключены по построению: что написано без <code>type</code>, то остаётся; что написано с <code>type</code>, того нет. Компилятор больше не принимает решений, а значит его вывод совпадает с выводом esbuild и SWC.</p>
    <p>Флаг заменяет два старых: <code>importsNotUsedAsValues</code> и <code>preserveValueImports</code>. Он же запрещает CJS-синтаксис <code>export =</code> и <code>import x = require()</code> в файлах, которые компилируются в ESM: дословное сохранение таких конструкций в ESM-выводе бессмысленно.</p>

    <h4>Почему так</h4>
    <p>«Умное» стирание было разумным в мире, где единственным транспилятором был <code>tsc</code>, видевший всю программу. С переходом на пофайловую сборку это стало источником расхождений между тем, что видит чекер, и тем, что собирает бандлер. Явность дороже в написании (приходится ставить <code>type</code> руками), но убирает целый класс несводимых багов.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Требует дисциплины: каждый типовой импорт надо помечать. Спасает ESLint-правило <code>consistent-type-imports</code> с автофиксом.</li>
    <li>Смешанная форма <code>import { type X, y }</code> идиоматична, но поддерживается только с TS 5.0.</li>
    <li>Импорт ради сайд-эффекта надо писать явно: <code>import './polyfill'</code> без имён — это единственная надёжная запись.</li>
    <li>В проектах с декораторами и <code>emitDecoratorMetadata</code> флаг ломает генерацию метаданных: они требуют, чтобы тип был доступен как значение.</li>
    <li>Апгрейд существующего проекта даёт много ошибок сразу — но все они механические и чинятся автофиксом.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем отличается от <code>isolatedModules</code>?</strong> <code>isolatedModules</code> запрещает конструкции, неоднозначные без типов; <code>verbatimModuleSyntax</code> задаёт правило стирания. Второй строже и покрывает первый по большинству случаев, но их обычно включают вместе. <strong>Зачем помечать <code>export type</code>?</strong> По той же причине с другой стороны: реэкспорт типа без модификатора превратится в рантайм-реэкспорт несуществующего значения.</p>`,
    code: `import { type User, createUser } from './user';
// в выводе: import { createUser } from './user';

import type { Config } from './config';
// в выводе: строки нет вообще

import './polyfill';
// в выводе: import './polyfill'; — сайд-эффект сохранён явно

export type { User };          // корректно
// export { User };            // ошибка: реэкспорт типа как значения`,
    tip: 'Пример с исчезающим полифиллом (импорт использован только в типовой позиции — и сайд-эффект пропал из сборки) — самая понятная иллюстрация проблемы, которую решает флаг.' },

  { id: 'tsx34',
    q: 'Проект собирается медленно, tsc ест минуты. Как диагностировать и что чинить?',
    a: `<h4>Коротко</h4>
    <p>Сначала измерить, потом чинить. <code>tsc --extendedDiagnostics</code> даёт разбивку по фазам (parse, bind, check, emit) и счётчики инстанцирований; <code>tsc --generateTrace ./trace</code> плюс <code>@typescript/analyze-trace</code> показывают конкретные файлы и типы, съевшие время. Без этого любая оптимизация — гадание.</p>

    <h4>Как это работает</h4>
    <p>В <code>--extendedDiagnostics</code> смотрят на три числа. <strong>Check time</strong> против <strong>Parse time</strong>: если парсинг доминирует, проблема в количестве файлов и <code>include</code>; если проверка — в сложности типов. <strong>Instantiations</strong> — сколько раз компилятор создавал экземпляры дженериков; счёт в десятках миллионов означает, что где-то развёрнут тяжёлый условный или мапнутый тип. <strong>Memory used</strong> под пару гигабайт — признак того же.</p>
    <p><code>analyze-trace</code> выдаёт список «hot spots» с путями до конкретных строк: чаще всего это одна утилита-тип, инстанцируемая в сотнях мест, или один <code>.d.ts</code> из зависимости.</p>

    <h4>Типовые причины и лечение</h4>
    <ul>
    <li><strong>Гигантские union и рекурсивные условные типы</strong> — самый частый источник. Ограничить глубину, заменить вычисляемый тип явным перечислением, разорвать цепочку промежуточным <code>interface</code>.</li>
    <li><strong><code>interface</code> вместо пересечений <code>type</code></strong>: интерфейс получает имя и кешируется, а <code>A &amp; B &amp; C</code> перепроверяется структурно при каждом сравнении. На больших пропсах разница измеримая.</li>
    <li><strong>Явные аннотации возвращаемых типов</strong> у экспортируемых функций: без них компилятор выводит тип заново на каждом использовании и тянет за собой всё, от чего зависит тело. Это же требование <code>isolatedDeclarations</code> (TS 5.5), который позволяет генерировать <code>.d.ts</code> пофайлово и параллельно.</li>
    <li><strong>Инфраструктура</strong>: <code>skipLibCheck: true</code>, <code>incremental: true</code> с <code>tsBuildInfoFile</code>, project references для монорепы, узкий <code>include</code> вместо всего дерева.</li>
    <li><strong>Аннотации вариантности</strong> <code>in</code>/<code>out</code> в тяжёлых библиотечных дженериках — снимают структурный вывод вариантности.</li>
    <li><strong>Зависимости</strong>: один пакет с гигантским <code>.d.ts</code> (иконки, i18n-ключи, GraphQL-схема) способен в одиночку удвоить время; лечится сужением импорта или генерацией более узкого типа.</li>
    </ul>

    <h4>Почему так</h4>
    <p>Чекер ленив: он вычисляет тип только когда его спросили, но результат кеширует не всегда — отложенные условные типы и структурные пересечения пересчитываются при каждом новом сравнении. Отсюда и правило: всё, что имеет <strong>имя</strong> (интерфейс, аннотированный возврат), дешевле того, что вычисляется на месте.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>«Медленно» бывает разным: медленный CI лечится <code>incremental</code> и references, а медленная IDE — конкретным типом в конкретном файле. Это разные задачи.</li>
    <li>tsserver в редакторе и <code>tsc</code> в CI имеют разные профили: hover пересчитывает типы, которых при полной сборке никто не спрашивает.</li>
    <li><code>skipLibCheck: false</code> заставляет проверять тысячи чужих <code>.d.ts</code> — это самая дешёвая победа, если он был выключен.</li>
    <li>Оптимизация типов ради скорости легко ухудшает читаемость ошибок; сначала докажите профилем, что этот тип действительно горячий.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Что дают project references?</strong> Инкрементальную сборку по проектам: изменение в одном пакете не заставляет перепроверять остальные, и <code>.d.ts</code> зависимостей берутся с диска. <strong>Как сократить работу до нуля в CI?</strong> Кешировать <code>.tsbuildinfo</code> между запусками и запускать <code>tsc --build</code>, а не полную проверку.</p>`,
    code: `# измерение
tsc --noEmit --extendedDiagnostics
tsc --noEmit --generateTrace ./trace
npx @typescript/analyze-trace ./trace

# дорогое: пересечение пересчитывается структурно
type Props = Base & Theme & Handlers & A11y;

# дешевле: имя кешируется
interface Props extends Base, Theme, Handlers, A11y {}

# дорогое: возврат выводится заново на каждом использовании
export const build = (o: Options) => ({ /* ... */ });

# дешевле и требуется isolatedDeclarations
export const build = (o: Options): Result => ({ /* ... */ });`,
    tip: 'Назовите --generateTrace и @typescript/analyze-trace, а также счётчик Instantiations из --extendedDiagnostics: это конкретные инструменты вместо общих слов про «упростить типы».' },

  { id: 'tsx35',
    q: 'Type-level программирование: где польза, а где вред?',
    a: `<h4>Коротко</h4>
    <p>Сложный тип оправдан, когда он написан <strong>один раз</strong> и защищает контракт, используемый сотнями мест. Внутри одной фичи — почти никогда. Главный критерий не «получилось ли выразить», а «сможет ли джун прочитать ошибку и починить её сам».</p>

    <h4>Как это работает</h4>
    <p>Механика простая и в этом ловушка: условные типы, рекурсия и <code>infer</code> дают полный по Тьюрингу вычислитель, работающий на этапе проверки. Всё, что он посчитал, стирается — в рантайм не уезжает ни байта, зато каждая инстанциация оплачивается временем чекера и памятью tsserver. То есть выразительность бесплатна для бандла и платна для команды: за неё платят скоростью IDE и читаемостью ошибок.</p>

    <h4>Где польза реальна</h4>
    <ul>
    <li>Типобезопасные роуты: путь <code>/users/:id</code> порождает тип параметров, и опечатка в имени становится ошибкой компиляции, а не 404 в проде.</li>
    <li>Ключи i18n с проверкой плейсхолдеров: перевод требует <code>{count}</code>, а вызов его не передал — ловится статически.</li>
    <li>Схемы таблиц и ORM-запросы: <code>select</code> возвращает ровно выбранные колонки, а не весь тип строки.</li>
    <li>Токены дизайн-системы: автодополнение вместо документации, невозможность опечататься в <code>--color-primry</code>.</li>
    <li>Невозможные состояния в редьюсерах: дискриминированный union вместо трёх независимых булевых флагов, из которых половина комбинаций бессмысленна.</li>
    </ul>

    <h4>Где начинается вред</h4>
    <ul>
    <li><strong>Нечитаемые ошибки.</strong> Сообщение на сорок строк с раскрытыми условными типами останавливает работу человека, который не писал этот тип. Это прямой урон скорости команды, и он не виден в метриках.</li>
    <li><strong>Время проверки.</strong> IDE начинает тормозить на автодополнении задолго до того, как упадёт CI. Разработчики замечают это как «редактор тупит», не связывая с типами.</li>
    <li><strong>Дублирование рантайма.</strong> Тип, проверяющий то, что дешевле проверить схемой на границе, — работа впустую: данные всё равно приходят извне.</li>
    <li><strong>Опора на неспецифицированное поведение.</strong> Трюки вроде <code>Equals</code> держатся на деталях реализации чекера и ломаются при апгрейде минорной версии.</li>
    <li><strong>Тип вместо теста.</strong> Инварианты, зависящие от данных, статически невыразимы; попытка приводит к монстру, который всё равно не гарантирует нужного.</li>
    </ul>

    <h4>Почему так</h4>
    <p>Система типов TS полна по Тьюрингу, поэтому вопрос «можно ли это выразить» почти всегда имеет ответ «да». Отсюда искушение: сложный тип ощущается как инженерное достижение. Но у типа есть аудитория — вся команда, включая тех, кто увидит ошибку через год. Стоимость чтения платится многократно, стоимость написания — один раз.</p>

    <h4>Как принимать решение</h4>
    <ul>
    <li>Считать отношение «мест использования» к «строкам типа». Один к ста — пишем, один к трём — не пишем.</li>
    <li>Проверить сообщение об ошибке заранее: сломать вызов специально и прочитать, что выдал компилятор.</li>
    <li>Спрятать сложность за именем: <code>type RoutePath</code> вместо инлайн-выражения на пять строк в сигнатуре.</li>
    <li>Оставить путь отхода: <code>as</code> с комментарием в одном месте лучше, чем тип, который никто не решается тронуть.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Приведите пример, где вы отказались.</strong> Хороший ответ — конкретный: «сделали типизированный билдер SQL, ошибки стали нечитаемы, откатились на генерацию типов из схемы». <strong>Как улучшить сообщение об ошибке?</strong> Ветка с «понятным» типом-маркером: <code>T extends X ? Ok : { error: 'expected X, got:'; got: T }</code> — компилятор напечатает эту структуру, и она читается.</p>`,
    code: `// хорошо: тип один, применений сотни
type Params<S extends string> =
  S extends \`\${string}:\${infer P}/\${infer R}\` ? P | Params<R>
  : S extends \`\${string}:\${infer P}\` ? P : never;

// невозможные состояния вместо трёх флагов
type State =
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'error'; error: string }
  | { status: 'done'; data: User };

// приём для читаемых ошибок
type Expect<T, Shape> =
  T extends Shape ? T : { error: 'shape mismatch'; expected: Shape; got: T };`,
    tip: 'Критерий «сможет ли джун прочитать ошибку и починить сам» — самый убедительный аргумент в этой теме, и его почти никогда не называют.' },

  { id: 'tsx36',
    q: 'Что такое excess property check и почему он иногда не срабатывает?',
    snippet: `type P = { a: number };

const x: P = { a: 1, b: 2 };   // ошибка
const t = { a: 1, b: 2 };
const y: P = t;                // ошибки нет — почему?`,
    a: `<h4>Коротко</h4>
    <p>Excess property check — дополнительная проверка при присваивании <strong>свежего объектного литерала</strong>: TS ругается на свойства, которых нет в целевом типе. Структурная типизация этого не требует, это эвристика против опечаток. Как только литерал теряет «свежесть», проверка исчезает.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>{ a: 1, b: 2 }</code> в первой строке — свежий литерал, присваиваемый напрямую типу <code>P</code>: срабатывает проверка. Во второй строке тот же объект сначала получает собственный тип <code>{ a: number; b: number }</code> через переменную <code>t</code>. Дальше идёт обычная проверка присваиваемости: <code>{ a: number; b: number }</code> структурно подходит под <code>{ a: number }</code>, потому что имеет всё требуемое. Лишнее поле по правилам структурной типизации не мешает.</p>
    <p>Внутри чекера у типа литерала есть флаг <code>FreshLiteral</code>. Он выставляется при создании и <strong>сбрасывается</strong> при первом же widening — присваивании в переменную, возврате из функции, приведении через <code>as</code>. При сравнении со свежим типом чекер дополнительно проверяет, что каждое свойство источника есть в цели.</p>
    <p>Особый случай — union в цели. Свойство считается допустимым, если оно есть <strong>хотя бы в одном</strong> члене union. Поэтому <code>{ kind: 'a', extra: 1 }</code> пройдёт, если <code>extra</code> объявлено в другом варианте дискриминированного union. Это ровно та причина, по которой лишние пропсы компонента иногда проскакивают.</p>

    <h4>Почему так</h4>
    <p>Строгая структурная типизация не может запретить лишние поля: объект с большим числом свойств — законный подтип. Но на практике лишнее поле в литерале почти всегда опечатка (<code>onClik</code> вместо <code>onClick</code>), и молча проглатывать её плохо. Команда TS выбрала компромисс: ловить очевидный случай литерала, не трогая систему типов. Отсюда и «дырявость» — она встроена в замысел.</p>

    <h4>Где проверка не срабатывает</h4>
    <ul>
    <li>Через промежуточную переменную — самый частый обход, часто случайный.</li>
    <li>При <code>as</code>: приведение сбрасывает свежесть вместе со всей проверкой.</li>
    <li>При spread: <code>{ ...obj, a: 1 }</code> в ряде позиций не считается свежим целиком.</li>
    <li>Если целевой тип — union и свойство есть хотя бы в одном члене.</li>
    <li>Если у цели есть индексная сигнатура — тогда любое свойство допустимо по определению.</li>
    <li>При возврате из функции с выводимым (не аннотированным) типом возврата.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Как запретить лишние поля по-настоящему?</strong> Тип-«exact» через перемаппинг: <code>T &amp; { [K in Exclude&lt;keyof U, keyof T&gt;]?: never }</code>. Работает, но сообщения об ошибках становятся заметно хуже — размен обычно не в пользу строгости. <strong>Почему это важно знать про пропсы?</strong> На union-пропсах (компонент с вариантами) опечатки в именах пропсов не ловятся, и единственная защита — линтер или дискриминант, сужающий union до одного члена.</p>`,
    code: `type P = { a: number };
const x: P = { a: 1, b: 2 };     // ошибка: Object literal may only specify known properties
const t = { a: 1, b: 2 };
const y: P = t;                  // ошибки нет — свежесть потеряна

// union: свойство из соседнего члена проходит
type Btn = { kind: 'icon'; icon: string } | { kind: 'text'; label: string };
const b: Btn = { kind: 'icon', icon: 'x', label: 'oops' };  // не ловится

// индексная сигнатура отключает проверку целиком
type Bag = { a: number; [k: string]: unknown };
const g: Bag = { a: 1, whatever: true };   // ок`,
    tip: 'Кейс с union — самый показательный: он объясняет, почему на пропсах компонента с вариантами опечатки в именах пропсов иногда проходят молча.' },

  { id: 'tsx37',
    q: 'Что такое typeof import и зачем он нужен?',
    snippet: `declare global {
  interface Window { api: Api }   // Api объявлен в './api'
}
// почему сюда нельзя просто написать import { Api } from './api'?`,
    a: `<h4>Коротко</h4>
    <p><code>typeof import('./m')</code> в type-позиции даёт тип пространства имён модуля целиком — все его экспорты как один объектный тип — <strong>не внося рантайм-импорта</strong>. Работает в <code>.ts</code>, в <code>.d.ts</code> и в JSDoc-комментариях в <code>.js</code>.</p>

    <h4>Как это работает</h4>
    <p>В сниппете проблема в том, что обычный <code>import</code> на верхнем уровне превращает файл в модуль, а <code>declare global</code> внутри модуля работает, но <strong>внутри самого блока</strong> <code>global</code> импортированные имена доступны не всегда, и добавление импорта меняет семантику файла целиком. <code>typeof import('./api')</code> решает это точечно: он выражается прямо в типовой позиции, ничего не добавляет в область видимости файла и не порождает <code>require</code> в выводе.</p>
    <p>Компилятор при встрече такого выражения разрешает спецификатор по обычным правилам резолюции, берёт символ модуля и строит из его экспортов анонимный объектный тип. Индексировать его можно как обычно: <code>typeof import('./api')['getUser']</code> — тип конкретного экспорта.</p>
    <p>С динамическим импортом это работает автоматически: <code>await import('./heavy')</code> имеет тип <code>typeof import('./heavy')</code>, поэтому <code>(await import('./heavy')).default</code> типизирован без единой аннотации.</p>

    <h4>Почему так</h4>
    <p>До появления этой формы «взять тип из модуля, не импортируя его» было невозможно, и в <code>.d.ts</code> приходилось дублировать определения или тащить импорт, ломающий глобальность файла. Форма <code>typeof import()</code> отделяет разрешение модуля от его загрузки: типы существуют только при компиляции, значит и импортировать ради них ничего не нужно.</p>

    <h4>Где это применяют</h4>
    <ul>
    <li>Глобальные объявления: <code>declare global { interface Window { api: typeof import('./api') } }</code> без превращения файла в лишнюю зависимость.</li>
    <li>Разрыв циклических импортов: тип берётся «по требованию», рантайм-ребро в графе модулей не появляется.</li>
    <li>Ленивая загрузка: <code>const mod: typeof import('./heavy') = await import('./heavy')</code>.</li>
    <li>Моки в тестах: <code>vi.mock('./api', (): typeof import('./api') =&gt; ({ … }))</code> — компилятор проверит, что мок покрывает все экспорты с правильными сигнатурами. Забыли функцию после рефакторинга — ошибка компиляции, а не падение теста.</li>
    <li>JSDoc в JS-файлах: <code>@type {import('./api').User}</code> — так типизируют конфиги вебпака и eslint без перехода на TS.</li>
    </ul>

    <h4>Подводные камни</h4>
    <ul>
    <li>Путь в строке не проверяется рефакторингом так же надёжно, как импорт: переименование файла в IDE иногда его не обновляет.</li>
    <li>Тип включает <strong>все</strong> экспорты, включая внутренние — это делает мок обязанным реализовать и их.</li>
    <li>С <code>moduleResolution: node16</code> строка обязана содержать расширение <code>.js</code>, как и обычный импорт.</li>
    <li>Для CJS-модуля с <code>export =</code> результат — тип самого значения, а не объекта с ключом <code>default</code>: поведение зависит от <code>esModuleInterop</code>.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем отличается от <code>import type</code>?</strong> <code>import type { X }</code> берёт один именованный тип и требует места в шапке файла; <code>typeof import()</code> берёт весь модуль и пишется прямо в типовом выражении, в том числе там, где импорт невозможен. <strong>Останется ли что-то в бандле?</strong> Нет: конструкция целиком типовая и стирается — рантайм-импорта не возникает даже при <code>verbatimModuleSyntax</code>.</p>`,
    code: `type Api = typeof import('./api');
declare global { interface Window { api: Api } }
export {};

// тип конкретного экспорта
type GetUser = typeof import('./api')['getUser'];

// динамический импорт типизирован сам
const mod = await import('./heavy');   // typeof import('./heavy')

// мок обязан совпасть с реальным модулем по сигнатурам
vi.mock('./api', (): typeof import('./api') => ({
  getUser: async () => ({ id: '1' }),
}));`,
    tip: 'Кейс с моками — самый практичный: типизировать фабрику мока как typeof import(...) значит получать ошибку компиляции, когда в модуль добавили экспорт, а мок не обновили.' },

  { id: 'tsx38',
    q: 'В чём разница между ts-ignore и ts-expect-error и что выбрать?',
    snippet: `// @ts-ignore
const a: number = 1;

// @ts-expect-error
const b: number = 1;

// одна из этих двух строк даёт ошибку — какая?`,
    a: `<h4>Коротко</h4>
    <p><code>@ts-ignore</code> подавляет ошибку на следующей строке и молчит, даже если ошибки там нет. <code>@ts-expect-error</code> подавляет ошибку, но <strong>сам становится ошибкой</strong>, когда строка стала валидной. Выбирать надо второе: оно самоочищается, первое превращается в вечный долг.</p>

    <h4>Как это работает</h4>
    <p>В сниппете ошибку даст <code>@ts-expect-error</code>: строка <code>const b: number = 1</code> совершенно корректна, значит комментарий обещал ошибку, которой нет — «Unused '@ts-expect-error' directive». <code>@ts-ignore</code> над валидной строкой не даёт ничего и остаётся в коде навсегда.</p>
    <p>Механически оба директивы работают одинаково: чекер собирает диагностики, а затем фильтрует те, что начинаются на следующей после комментария строке. Разница только в том, что для <code>expect-error</code> ведётся счётчик подавленных диагностик, и нулевое значение само порождает новую.</p>
    <p>Практическое следствие: после апгрейда библиотеки, которая наконец починила типы, <code>@ts-expect-error</code> подсветит все места, где костыль больше не нужен, — и вы удалите их одной пачкой. С <code>@ts-ignore</code> вы об этом не узнаете никогда.</p>

    <h4>Почему так</h4>
    <p><code>@ts-ignore</code> появился раньше и решал задачу «дайте собраться». Опыт показал, что подавления накапливаются и переживают причину: код рефакторят, библиотеку обновляют, а комментарий остаётся и глушит уже совсем другую, настоящую ошибку. <code>@ts-expect-error</code> (TS 3.9) добавили именно чтобы у подавления был срок годности.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li>Оба глушат <strong>все</strong> ошибки строки, а не конкретную. Если на строке два вызова, вторая ошибка исчезнет вместе с первой — сужайте строку до минимума.</li>
    <li><code>@ts-expect-error</code> над строкой, где ошибка появляется <strong>иногда</strong> (зависит от версии библиотеки или флага), делает сборку нестабильной: то ошибка, то «unused directive».</li>
    <li><code>@ts-nocheck</code> в начале файла отключает проверку целиком — крайняя мера при миграции, а не рабочий инструмент.</li>
    <li>В <code>.tsx</code> внутри JSX нужен другой синтаксис: <code>{/* @ts-expect-error */}</code>, обычный комментарий там не сработает.</li>
    <li>Подавление часто маскирует настоящий баг: тип не соответствует реальности не потому, что типы плохие, а потому что данные другие.</li>
    </ul>

    <h4>Что вместо</h4>
    <p>Точечный <code>as unknown as T</code> с комментарием часто честнее: он не глушит соседние ошибки, локализован в выражении и виден при чтении. Ещё лучше — локальная аугментация типов библиотеки: она чинит причину, а не симптом, и работает во всех местах сразу.</p>

    <h4>Что спросят следом</h4>
    <p><strong>Как это регулируется в проекте?</strong> ESLint-правило <code>@typescript-eslint/ban-ts-comment</code>: <code>'ts-ignore': true</code> (запрещён), <code>'ts-expect-error': 'allow-with-description'</code> плюс <code>descriptionFormat</code> с требованием ссылки на issue. <strong>Как найти накопленные подавления?</strong> Грепом по репозиторию плюс метрика в CI: их число не должно расти между релизами.</p>`,
    code: `// @ts-expect-error TODO: обновить типы после релиза lib@3 (issue #421)
legacy.call();

// плохо: глушит обе ошибки строки
// @ts-expect-error
foo(bad1(), bad2());

// лучше: подавление локализовано в одном выражении
const value = raw as unknown as User;   // API отдаёт snake_case, см. #421

// правило в .eslintrc
// "@typescript-eslint/ban-ts-comment": ["error", {
//   "ts-ignore": true,
//   "ts-expect-error": "allow-with-description",
//   "minimumDescriptionLength": 10
// }]`,
    tip: 'Готовый ответ про процессы: ts-ignore запрещён линтером, разрешён только ts-expect-error с описанием и ссылкой на issue — тогда подавления самоочищаются при апгрейде и их число можно мерить в CI.' },

  { id: 'tsx39',
    q: 'Почему Object.keys возвращает string[], а не keyof T, и как с этим быть?',
    snippet: `type P = { a: number };
const wide = { a: 1, b: 'oops' };
const p: P = wide;                 // структурно допустимо

Object.keys(p).forEach((k) => {
  // если бы k имел тип keyof P, что случилось бы на ключе 'b'?
});`,
    a: `<h4>Коротко</h4>
    <p>Потому что в рантайме объект может быть <strong>шире</strong> объявленного типа. Структурная типизация это прямо разрешает: переменная типа <code>{ a: number }</code> законно ссылается на объект с полями <code>a</code>, <code>b</code>, <code>c</code>. Вернуть <code>(keyof T)[]</code> значило бы дать гарантию, которая нарушается в первом же реальном коде.</p>

    <h4>Как это работает</h4>
    <p>В сниппете <code>p</code> имеет тип <code>{ a: number }</code>, но в рантайме это тот же объект <code>wide</code> с полем <code>b</code>. <code>Object.keys(p)</code> вернёт <code>['a', 'b']</code>. Если бы тип был <code>'a'[]</code>, то <code>p[k]</code> считался бы <code>number</code>, а на итерации по <code>'b'</code> там оказалась бы строка — и последующая арифметика молча дала бы <code>NaN</code>.</p>
    <p>Та же причина у <code>for...in</code> и <code>Object.entries</code>: их сигнатуры в lib.d.ts возвращают <code>string[]</code> и <code>[string, T][]</code>. Это не недосмотр, а единственный корректный вариант при структурной типизации без exact-типов.</p>
    <p>Обходов два. Безопасный — итерироваться по <strong>явному списку</strong> ключей: <code>const KEYS = ['a', 'b'] as const</code>, затем <code>for (const k of KEYS)</code>. Здесь <code>k</code> имеет точный литеральный тип, и <code>obj[k]</code> типизирован верно, а лишние поля просто не посещаются. Осознанно небезопасный — локальный хелпер <code>objectKeys</code> с <code>as (keyof T)[]</code> и комментарием: он допустим, когда объект создан здесь же и точно не шире.</p>

    <h4>Почему так</h4>
    <p>Это следствие фундаментального выбора: TS структурно типизирован и не имеет exact-типов (в отличие от Flow с его <code>{| |}</code>). Структурность даёт огромную гибкость — не нужны объявления «реализует интерфейс», подходит любой совместимый объект. Плата — невозможность знать полный набор ключей. Excess property check прикрывает только литералы; всё остальное открыто.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><code>for...in</code> дополнительно перебирает <strong>унаследованные</strong> перечислимые свойства — отсюда классический <code>hasOwnProperty</code>, а лучше <code>Object.hasOwn</code> (ES2022).</li>
    <li><code>Object.entries</code> теряет связь ключа и значения: получите <code>[string, A | B][]</code> вместо пар. Для точных пар нужен маппер по <code>keyof T</code>.</li>
    <li><code>Object.fromEntries</code> возвращает <code>{ [k: string]: T }</code> и почти всегда требует ручной аннотации результата.</li>
    <li>Хелпер <code>objectKeys</code> с приведением ломается ровно там, где объект пришёл извне, — то есть там, где он опаснее всего.</li>
    <li>При <code>noUncheckedIndexedAccess</code> доступ по ключу из <code>Object.keys</code> даёт <code>| undefined</code>, что справедливо, но неудобно.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Есть ли способ получить exact-тип?</strong> Штатного нет; имитируется через <code>T &amp; { [K in Exclude&lt;string, keyof T&gt;]?: never }</code>, но сообщения об ошибках портятся. <strong>А <code>Map</code>?</strong> <code>Map&lt;K, V&gt;</code> типизирует ключи точно, потому что это структура данных, а не структурный тип: <code>map.keys()</code> честно даёт <code>IterableIterator&lt;K&gt;</code>.</p>`,
    code: `const KEYS = ['a', 'b'] as const;
for (const k of KEYS) use(obj[k]);          // k: "a" | "b" — типобезопасно

// осознанное допущение, только для локально созданных объектов
function objectKeys<T extends object>(o: T) {
  return Object.keys(o) as (keyof T)[];     // объект может быть шире — знаем и принимаем
}

// точные пары вместо Object.entries
type Entries<T> = { [K in keyof T]: [K, T[K]] }[keyof T][];
const entries = Object.entries(obj) as Entries<typeof obj>;

for (const k in obj) if (Object.hasOwn(obj, k)) use(k);   // без унаследованных`,
    tip: 'Точная формулировка причины: объект в рантайме может быть шире объявленного типа, и структурная типизация это разрешает — а не «так решили в TS».' },

  { id: 'tsx40',
    q: 'Как типизировать миксины и абстрактные конструкторы?',
    snippet: `type Ctor<T = {}> = new (...a: any[]) => T;
abstract class Base { abstract render(): void }

function Mixin<B extends Ctor>(B_: B) { return class extends B_ {} }
Mixin(Base);   // почему ошибка и что её чинит?`,
    a: `<h4>Коротко</h4>
    <p>Миксин — функция, принимающая конструктор и возвращающая расширенный класс. Ключевых элементов два: тип конструктора <code>new (...args: any[]) =&gt; T</code> и то, что возвращаемый тип <strong>выводится компилятором</strong> — аннотировать его вручную почти невозможно без потери деталей.</p>

    <h4>Как это работает</h4>
    <p>В сниппете ошибка потому, что абстрактный класс не присваивается обычной конструкторной сигнатуре: его нельзя инстанцировать через <code>new</code>, а тип <code>Ctor</code> именно это и обещает. Чинится <strong>abstract construct signature</strong> из TS 4.2: <code>type Ctor&lt;T = {}&gt; = abstract new (...a: any[]) =&gt; T</code>. Такая сигнатура описывает «конструктор, возможно абстрактный» и принимает оба вида классов.</p>
    <p>Вывод результата устроен так: <code>class extends B_ {}</code> порождает анонимный класс, а тип его экземпляра — <code>InstanceType&lt;B&gt;</code> плюс добавленные члены. Компилятор это выводит, но <strong>назвать</strong> получившийся тип нельзя: у анонимного класса нет имени, на которое можно сослаться в <code>.d.ts</code>. Отсюда главная практическая проблема.</p>
    <p><code>extends</code> от произвольного выражения (а не от идентификатора) поддерживается языком специально ради этого паттерна — <code>class X extends Mixin(Base) {}</code> валидно и в TS, и в JS.</p>

    <h4>Почему так</h4>
    <p>JS не имеет множественного наследования, но имеет динамическое <code>extends</code> от выражения — миксины эксплуатируют именно это. TS вынужден типизировать паттерн через вывод, потому что выписать «класс со всеми членами базы плюс мои» заранее, без пересечений и <code>InstanceType</code>, невозможно.</p>

    <h4>Подводные камни</h4>
    <ul>
    <li><strong><code>declaration: true</code> ломается</strong>: компилятор не может выписать в <code>.d.ts</code> имя анонимного класса и выдаёт «has or is using private name». Лечится явным интерфейсом и слиянием — ценой дублирования.</li>
    <li><code>isolatedDeclarations</code> (TS 5.5) с миксинами несовместим по той же причине: тип возврата невыразим явно.</li>
    <li><strong>Приватные поля</strong>: <code>private</code> и <code>#x</code> проверяются номинально, поэтому классы из двух разных вызовов миксина несовместимы, даже будучи структурно идентичными.</li>
    <li>Порядок применения значим, но невыразим в типах: <code>A(B(Base))</code> и <code>B(A(Base))</code> ведут себя по-разному, а типы совпадают.</li>
    <li><code>constructor.name</code> у анонимного класса пуст — ломаются DI-контейнеры, логи и React DevTools.</li>
    <li>Цепочка из трёх-четырёх миксинов даёт нечитаемые ошибки с полностью раскрытыми пересечениями.</li>
    </ul>

    <h4>Что спросят следом</h4>
    <p><strong>Чем заменить в прикладном коде?</strong> Композицией: объект с зависимостями, хуки, отдельные сервисы. Миксины оправданы в библиотечном коде, где наследование — часть публичного API (Lit, старый Angular, часть ORM). <strong>Зачем нужен <code>InstanceType</code>?</strong> Достаёт тип экземпляра из конструктора: <code>InstanceType&lt;typeof MyClass&gt;</code> — нужен и в сигнатурах миксинов, и везде, где класс передают как значение.</p>`,
    code: `// abstract new принимает и обычные, и абстрактные классы (TS 4.2)
type Ctor<T = {}> = abstract new (...a: any[]) => T;

function Timestamped<B extends Ctor>(Base: B) {
  abstract class T extends Base { created = Date.now() }
  return T;
}

class Doc {}
class TimedDoc extends Timestamped(Doc) {}
new TimedDoc().created;     // number

// для declaration: true нужен явный именованный интерфейс
interface Timestamps { created: number }
export function withTs<B extends Ctor>(Base: B): B & Ctor<Timestamps> {
  return class extends Base { created = Date.now() };
}`,
    tip: 'Нюанс, который убеждает: у миксинов ломается declaration: true (анонимный класс нельзя назвать в .d.ts), и та же причина делает их несовместимыми с isolatedDeclarations.' }
];
