/* Разбор одной большой задачи system design: веб-клиент мессенджера уровня
   Telegram. Карточки msg1–msg14 читаются подряд как единый блок и вливаются
   в колоду System Design. */

const DECK_MESSENGER = [

  { id: 'msg1',
    q: 'Спроектируй веб-клиент мессенджера уровня Telegram. С чего начнёшь и из чего он состоит?',
    a: `<h4>Уточняющие вопросы</h4>
<p>Какие типы чатов: только личные и группы или ещё каналы на миллион подписчиков — у канала другая модель чтения (все читают, пишут единицы) и отдельный счётчик синхронизации. Нужно ли сквозное шифрование: если да, дешифровка становится обязательным этапом между сетью и хранилищем, и полнотекстовый поиск на сервере становится невозможен в принципе. Обязателен ли офлайн — читать историю без сети и отправлять в очередь, — или достаточно работы при живом соединении. Каковы объёмы: сколько диалогов у тяжёлого пользователя (реально — до нескольких тысяч), сколько сообщений в самом большом чате (сотни тысяч), какой предел на размер файла (у Telegram — 2 ГБ и 4 ГБ для премиума). Это web-страница или устанавливаемое PWA. Звонки в скоупе или нет — WebRTC это отдельная задача на сорок минут, её честно выносят за скобки.</p>

<h4>Границы</h4>
<p>Явно проговариваю, чего <strong>не</strong> проектирую: шардинг датацентров, доставку и фанаут на сервере, антиспам, ботов, звонки. Проектирую клиент: транспорт, локальное состояние, синхронизацию, рендеринг, медиа. Интервьюер почти всегда согласится — и дальше вопросы пойдут внутрь клиента, а не в сторону Kafka.</p>

<h4>Слои клиента</h4>
<ol>
<li><strong>Транспорт.</strong> Один WebSocket для событий и команд, HTTP отдельно для медиа. Реконнект с джиттером, heartbeat, определение «мёртвого» сокета.</li>
<li><strong>Сессия и протокол.</strong> Сериализация (бинарь вместо JSON: у Telegram это MTProto, у WhatsApp — protobuf), очередь исходящих запросов, повтор незавершённых после реконнекта, дедупликация по идентификатору запроса.</li>
<li><strong>Менеджер обновлений.</strong> Держит счётчики состояния, находит пропуски, дозагружает разницу. Подробно — в карточке про синхронизацию.</li>
<li><strong>Персистентное хранилище.</strong> IndexedDB для сообщений, диалогов и индексов; Cache API или OPFS для бинарных медиа.</li>
<li><strong>Store в памяти.</strong> Нормализованные сущности плюс окна лент. В памяти живёт только то, что нужно открытым экранам, остальное выгружается.</li>
<li><strong>UI.</strong> Список диалогов, виртуализированная лента, композер, панели медиа и стикеров.</li>
</ol>
<p>Правило, которое связывает слои: <strong>UI читает только из локального стора, никогда напрямую из сети</strong>. Сеть пишет в хранилище, хранилище уведомляет стор, стор перерисовывает UI. Это единственный способ, при котором офлайн, оптимистичная отправка и переподключение не превращаются в три независимые ветки кода.</p>

<h4>Транспорт: почему WebSocket</h4>
<p>Нужен двусторонний канал с задержкой в десятки миллисекунд: SSE односторонний (набор текста, подтверждения прочтения и presence уходить не смогут), long polling даёт лишний RTT и держит соединение на каждый запрос. WebSocket — один сокет, кадры по несколько байт. Медиа при этом не гоняем через сокет: файлы качаются обычным HTTP, чтобы работали Range-запросы, кеш браузера и параллелизм HTTP/2, а сокет не забивался мегабайтами и не задерживал сообщения. Это классический head-of-line: один WebSocket — это один TCP-поток, и картинка на 3 МБ в нём заблокирует текст.</p>

<h4>Компромиссы</h4>
<p>Взяли толстый клиент с полной локальной базой: получили мгновенный старт, офлайн и поиск без сети — отдали сложность миграций схемы, объём кода и риск рассинхрона локального состояния с сервером. Если бы это был виджет поддержки на чужом сайте с десятком сообщений на диалог, правильным ответом был бы тонкий клиент без IndexedDB вообще: запрос истории при открытии, никакого менеджера обновлений. Бинарный протокол экономит трафик и парсинг, но убивает отладку через devtools и требует своей схемы и кодогенерации — на маленьком продукте JSON честнее.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Сразу лезут в UI-детали (как рисовать пузырьки), не проговорив, где живёт состояние. Через десять минут выясняется, что архитектуры нет.</li>
<li>Не разделяют «получили событие» и «применили событие»: без явного менеджера обновлений порядок сообщений разъезжается при реконнекте.</li>
<li>Забывают про первый запуск: пустая база, а надо показать список диалогов за секунду. Это отдельный запрос на верхние 20 диалогов, а не «синхронизируем всё».</li>
<li>Не думают о мультивкладочности — а это первое, что ломается на проде.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Что грузим при старте?»</strong> — оболочку из Service Worker, затем список диалогов из IndexedDB (мгновенно), параллельно запрос состояния на сервер и догрузка разницы. Открытый чат рисуется из локальной базы до того, как сокет успел подняться. <strong>«Где точка отказа?»</strong> — сокет; поэтому весь UI обязан работать в режиме «данные из базы, отправка в очередь». <strong>«Сколько это весит?»</strong> — целевой первичный бандл в районе 300–500 КБ gzip, всё остальное (стикеры, редактор медиа, эмодзи-панель) — ленивыми чанками.</p>`,
    code: `// Единственная точка входа данных в приложение: сеть пишет в базу,
// UI подписан на базу и ничего не знает про сокет.
//
//   socket ──▶ updatesManager ──▶ db (IndexedDB) ──▶ store ──▶ UI
//                    ▲                                  │
//                    └────────── sendQueue ◀────────────┘

const layers = {
  transport:  'WebSocket: события и команды. Реконнект, heartbeat',
  session:    'сериализация, очередь запросов, msgId и дедупликация',
  updates:    'счётчики pts/seq, поиск пропусков, догрузка разницы',
  storage:    'IndexedDB: диалоги, сообщения, индексы. Cache API: медиа',
  memory:     'нормализованный стор: только открытые экраны',
  ui:         'список диалогов, виртуальная лента, композер',
};

// Медиа принципиально мимо сокета — иначе head-of-line blocking
fetch('/media/' + fileId, { headers: { Range: 'bytes=0-262143' } });`,
    tip: 'Первым делом закрепи границу: проектируем клиент, а не бэкенд. Второй фразой — что UI читает только из локального стора. Эти два предложения задают рамку на все сорок минут и снимают половину уточняющих вопросов.' },

  { id: 'msg2',
    q: 'Какая модель данных на клиенте? Как хранишь сообщения и в каком порядке их показываешь?',
    a: `<h4>Коротко</h4>
<p>Пять сущностей — диалог, сообщение, пользователь, медиа, реакция — хранятся нормализованно: сообщение ссылается на автора и медиа по id, а не встраивает их. Порядок ленты держится не на времени, а на монотонном серверном идентификаторе сообщения внутри чата. История хранится не как непрерывный список, а как набор загруженных диапазонов с явными «дырами» между ними.</p>

<h4>Сущности и нормализация</h4>
<p><strong>Dialog</strong>: id чата, тип (личный, группа, канал), последнее сообщение (только id), непрочитанные, id последнего прочитанного мной и последнего прочитанного собеседником, черновик, статус закрепления и уведомлений. <strong>Message</strong>: составной ключ (chatId, messageId), автор, текст, entities (форматирование как офсеты, не как HTML), id медиа, id ответа, время редактирования, флаги. <strong>User</strong> и <strong>Chat</strong> — отдельные хранилища, потому что один пользователь встречается в десятках тысяч сообщений: дублировать его имя и аватар в каждом — гарантированный рассинхрон при смене ника. <strong>Media</strong> — метаданные (размеры, длительность, blurhash, ключ файла), сами байты живут не здесь. <strong>Reaction</strong> — агрегаты на сообщении плюс отдельный список «кто именно», подгружаемый по требованию.</p>
<p>Форматирование храню как entities — массив вида «offset 5, length 7, тип bold» — а не как готовый HTML. Причина практическая: текст надо уметь индексировать для поиска, обрезать в превью диалога и рендерить в разных контекстах, а разбирать для этого HTML каждый раз — дорого и опасно.</p>

<h4>Идентификаторы: локальные и серверные</h4>
<p>Пока сообщение не подтверждено сервером, у него нет серверного id. Поэтому у каждого исходящего сообщения два ключа: <strong>clientId</strong> (uuid или счётчик, генерируется на клиенте, живёт вечно, участвует в дедупликации) и <strong>messageId</strong> (приходит от сервера). React-ключ в списке — всегда clientId, иначе при подтверждении элемент размонтируется и заново смонтируется: пропадёт анимация, слетит фокус, дёрнется скролл. Telegram решает то же самое через random_id: сервер хранит использованные random_id и на повторный вызов возвращает уже созданное сообщение вместо второго — идемпотентность отправки строится именно на этом.</p>

<h4>Почему нельзя сортировать по времени</h4>
<ul>
<li>Часы клиента врут. Сообщение с телефона с уехавшим на пять минут временем встанет в ленте не туда.</li>
<li>Два сообщения могут иметь одинаковую метку в миллисекундах.</li>
<li>Серверное время нормализуется, но при этом теряет отношение «строго после»: два сообщения из разных датацентров могут прийти с одинаковой секундой.</li>
</ul>
<p>Поэтому сортировка идёт по messageId, монотонно растущему внутри чата. Время показывается пользователю, но не участвует в порядке. Для неподтверждённых сообщений порядок держит отдельный локальный ключ: сортируем по паре (messageId или +бесконечность, localSeq) — тогда исходящие всегда внизу и в порядке отправки, а при подтверждении переезжают на своё место без прыжка.</p>

<h4>Дыры в истории</h4>
<p>Мы никогда не имеем всей истории локально. У чата есть массив загруженных диапазонов: например [1..340] и [98000..100000]. Между ними — дыра. Это критично, потому что наивная реализация «взять всё из базы и отсортировать» покажет сообщение 340 прямо над 98000, и пользователь решит, что переписка оборвалась. Правильно: лента рендерит только текущий диапазон, а на его границе стоит маркер «есть ещё», по которому идёт подгрузка. Прыжок к сообщению вне диапазона (карточка про переход к сообщению) создаёт новый диапазон, а при подгрузке диапазоны, коснувшиеся друг друга, сливаются.</p>

<h4>Курсоры</h4>
<p>Пагинация — не offset, а курсор: «дай 50 сообщений старше id X». Offset ломается ровно в тот момент, когда во время листания кто-то удалил сообщение: страница сдвинется, и одно сообщение вы увидите дважды, а другое пропустите. Курсор по id устойчив к вставкам и удалениям.</p>

<h4>Компромиссы</h4>
<p>Нормализация дала консистентность (сменили аватар — обновился один объект) и экономию места, но каждое сообщение теперь требует join-а в памяти при рендере. Для ленты на 50 видимых элементов это ничто, для экспорта чата на 200 тысяч — заметно, поэтому денормализованное имя автора всё-таки дублирую прямо в сообщение как «имя на момент отправки»: это ещё и исторически корректнее. Хранение entities вместо HTML стоит лишнего кода рендера, зато даёт поиск и превью бесплатно.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Ключ в React по индексу массива — при вставке сверху перерисовывается вся лента.</li>
<li>Один общий счётчик id на все чаты вместо счётчика внутри чата: тогда id не помогает сортировать локально.</li>
<li>Забывают про удалённые и отредактированные сообщения: нужен tombstone, иначе удалённое всплывает обратно из кеша.</li>
</ul>`,
    code: `// Сообщение: составной ключ, ссылки по id, форматирование офсетами
const message = {
  chatId: 12345,
  messageId: 98214,       // серверный, монотонный внутри чата
  clientId: 'c-8f21a0',   // локальный, живёт с момента набора
  localSeq: 17,           // порядок среди неподтверждённых
  senderId: 777,          // ссылка в users
  text: 'посмотри отчёт',
  entities: [{ offset: 9, length: 6, type: 'bold' }],
  mediaId: 'file-4412',   // ссылка в media, байты в Cache API
  replyToId: 98180,
  date: 1735689600,       // показываем, но НЕ сортируем по нему
  editDate: null,
  flags: { out: true, deleted: false },
  state: 'sending',       // sending | sent | delivered | read | failed
};

// История чата — не список, а набор диапазонов с дырами между ними
const chatRanges = {
  12345: [
    { from: 1,     to: 340,    complete: true  },  // начало переписки
    { from: 98000, to: 100000, complete: false },  // текущее окно
  ],
};

// Сортировка: неподтверждённые всегда в хвосте и в порядке отправки
const order = (a, b) =>
  (a.messageId || Infinity) - (b.messageId || Infinity) ||
  a.localSeq - b.localSeq;`,
    tip: 'Фраза, которая сразу выделяет кандидата: «порядок держит messageId, а не timestamp, потому что часы клиента врут». И следом — про дыры в истории: большинство рисует непрерывный массив и ломается на первом же прыжке к старому сообщению.' },

  { id: 'msg3',
    q: 'В чате 300 тысяч сообщений. Как рендеришь ленту, чтобы она не тормозила?',
    a: `<h4>Коротко</h4>
<p>Виртуализация с элементами переменной высоты: в DOM живёт окно из 40–80 сообщений, высоты меряются через ResizeObserver и кешируются по clientId, позиция скролла удерживается вручную при вставке сверху. Обычный virtual list здесь ломается сразу по трём причинам: высоты неизвестны заранее, лента растёт с обоих концов, и якорь у чата снизу, а не сверху.</p>

<h4>Почему обычный virtual list не подходит</h4>
<ul>
<li><strong>Высоты неизвестны.</strong> Сообщение — это от одной строки до картинки 1280×720 с подписью, цитатой и опросом. Фиксированной высоты нет, оценка «в среднем 60px» промахивается в 5 раз на медиа.</li>
<li><strong>Рост с обоих концов.</strong> Прилетело новое сообщение снизу и подгрузилась история сверху. Вставка сверху меняет scrollHeight, и браузер сохраняет scrollTop — значит контент под пальцем уезжает вниз.</li>
<li><strong>Якорь снизу.</strong> Чат открывается на последнем сообщении. Нулевая позиция скролла — это не начало списка, а его конец, и вся арифметика инвертируется.</li>
<li><strong>Асинхронное изменение высоты.</strong> Картинка догрузилась, стикер развернулся, перевод раскрылся — высота уже отрисованного элемента изменилась задним числом.</li>
</ul>

<h4>Измерение и кеш высот</h4>
<p>Каждому сообщению даём предварительную оценку по типу: текст — 24px на строку плюс паддинги, фото — считаем из известных width/height медиа (они приходят в метаданных, поэтому место резервируется <strong>до</strong> загрузки картинки), голосовое — фиксированные 56px. После монтирования ResizeObserver отдаёт реальную высоту, она пишется в Map по clientId, и если отличается от оценки больше чем на 1px, префиксные суммы пересчитываются от этого индекса. Кеш высот переживает выгрузку элемента из DOM — второй проход по тому же участку уже точный и без прыжков.</p>
<p>Смещения храню не массивом абсолютных offset-ов (пересчёт O(n) на каждую вставку сверху при n=300000 — это десятки миллисекунд на каждый скролл), а деревом Фенвика или сегментированными блоками по 1000 элементов: обновление и поиск позиции за O(log n). На практике до нескольких десятков тысяч элементов хватает и простого массива с ленивым пересчётом хвоста.</p>

<h4>Удержание позиции при вставке сверху</h4>
<p>Три работающих приёма, от простого к надёжному:</p>
<ol>
<li><strong>Инверсия контейнера</strong>: <code>flex-direction: column-reverse</code>. Браузер сам держит якорь у нижнего края, новые сообщения снизу не двигают вид. Дёшево, но ломает выделение текста в части браузеров, инвертирует направление колеса на некоторых тачпадах и путает скринридеры порядком.</li>
<li><strong>Компенсация вручную</strong>: перед вставкой запоминаем <code>scrollHeight</code>, после вставки в том же кадре ставим <code>scrollTop += newHeight - oldHeight</code>. Обязательно синхронно, в layout effect до отрисовки, иначе будет видимый рывок в один кадр.</li>
<li><strong>Якорь на элементе</strong>: запоминаем id первого видимого сообщения и его отступ от верха вьюпорта, после вставки восстанавливаем позицию через <code>getBoundingClientRect</code>. Самый устойчивый способ, переживает даже смену высот вставленных элементов.</li>
</ol>
<p>Отдельно: CSS-свойство <code>overflow-anchor</code>. Браузерное scroll anchoring само пытается компенсировать сдвиги и начинает драться с вашей ручной компенсацией — сдвиг применяется дважды. На контейнере ленты ставим <code>overflow-anchor: none</code> и управляем позицией сами.</p>

<h4>Двусторонняя подгрузка</h4>
<p>Два сентинела на IntersectionObserver с <code>rootMargin</code> порядка 800px — подгрузка стартует до того, как пользователь доскроллил. Порция — 50 сообщений вверх или вниз. Важные детали: не запускать вторую загрузку того же направления, пока первая в полёте; при быстром «броске» скролла (velocity выше порога) не грузить вообще, а дождаться остановки — иначе на одном флике улетит десяток запросов; при отмене показывать не спиннер, а skeleton нужной высоты, чтобы лента не схлопывалась.</p>

<h4>Память и открепление</h4>
<p>Даже с виртуализацией нельзя держать в JS-памяти все 300 тысяч объектов: при среднем сообщении в 400 байт это 120 МБ плюс оверхед объектов — реально ближе к 400 МБ, и вкладка падает. Держим окно в 2000–5000 сообщений вокруг видимой области; при выходе за границу дальние куски выгружаются из памяти обратно в IndexedDB, а в списке диапазонов остаётся отметка «этот кусок на диске». DOM-узлы отпускаем агрессивнее: за пределами 3 экранов вверх и вниз. Кеш высот при этом <strong>не</strong> чистим — он весит 8 байт на запись, 300 тысяч записей это 2–3 МБ, и он спасает от прыжков при возврате.</p>

<h4>Компромиссы</h4>
<p>Виртуализация с измерением даёт ровные 60 fps и постоянную память, но платим тем, что <code>Ctrl+F</code> браузера ищет только по видимому окну (нужен свой поиск — карточка msg8), а печать и выделение всего чата перестают работать штатно. Если бы чаты были короткими (техподдержка, 50 сообщений), правильным ответом был бы обычный список без виртуализации: меньше кода, работает нативный поиск и выделение. Открепление дальних кусков экономит память, но добавляет задержку в 20–50 мс при возврате к ним — сглаживаем предзагрузкой соседнего куска.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Не резервируют место под картинку: лента прыгает при каждой загрузке. Размеры медиа приходят в метаданных — используйте их.</li>
<li>Считают, что <code>column-reverse</code> решает всё. Он решает только якорь снизу, но не переменные высоты и не подгрузку сверху.</li>
<li>Забывают отключить <code>overflow-anchor</code> и потом полдня ищут, почему компенсация срабатывает дважды.</li>
<li>Меряют высоту в <code>useEffect</code> вместо <code>useLayoutEffect</code> — получают гарантированное мигание.</li>
<li>Не думают про группировку подряд идущих сообщений одного автора: она меняет высоту соседа при вставке, и кеш высот нужно инвалидировать не только для нового элемента.</li>
</ul>`,
    code: `// 1. Оценка высоты до измерения — из метаданных, а не из воздуха
function estimateHeight(msg) {
  if (msg.mediaId) {
    const m = media[msg.mediaId];
    const w = Math.min(m.width, MAX_BUBBLE);      // 480px
    return Math.round(m.height * (w / m.width)) + (msg.text ? 28 : 0) + 12;
  }
  const lines = Math.ceil(msg.text.length / CHARS_PER_LINE);
  return lines * 20 + 18;
}

// 2. Точное измерение после монтирования, кеш по стабильному ключу
const ro = new ResizeObserver((entries) => {
  let dirtyFrom = Infinity;
  for (const e of entries) {
    const id = e.target.dataset.cid;
    const h = e.borderBoxSize[0].blockSize;
    if (Math.abs((heights.get(id) || 0) - h) > 1) {
      heights.set(id, h);
      dirtyFrom = Math.min(dirtyFrom, indexOf(id));
    }
  }
  if (dirtyFrom !== Infinity) offsets.recomputeFrom(dirtyFrom);
});

// 3. Компенсация при вставке сверху — синхронно, до отрисовки
useLayoutEffect(() => {
  if (!prependedCount) return;
  const el = scrollerRef.current;
  el.scrollTop += el.scrollHeight - prevScrollHeight.current;
}, [prependedCount]);

// 4. Браузерный scroll anchoring выключаем, иначе компенсация двойная
// .messages-scroller { overflow-anchor: none; }`,
    tip: 'Сильный ответ звучит так: «место под медиа резервируем из width и height в метаданных ещё до загрузки байтов». Это показывает, что человек реально боролся с прыгающей лентой, а не читал про виртуализацию.' },

  { id: 'msg4',
    q: 'Пользователь нажал «перейти к сообщению» из поиска. Сообщение на 40 тысяч позиций выше. Что происходит?',
    a: `<h4>Коротко</h4>
<p>Не скроллим — переезжаем. Лента размонтируется и пересобирается вокруг целевого сообщения: грузим окно из ~50 сообщений вокруг него, рендерим уже на нужной позиции, подсвечиваем цель, кладём прежнюю позицию в стек возврата и показываем кнопку «вниз». Скроллить сквозь 40 тысяч элементов нельзя ни за какое время — это 800 запросов подгрузки.</p>

<h4>Механика по шагам</h4>
<ol>
<li><strong>Сохраняем откуда прыгаем.</strong> В стек кладём (chatId, id первого видимого сообщения, его offset от верха вьюпорта). Именно id, а не scrollTop: пока пользователь ходит по ссылкам, высоты могли пересчитаться, и пиксельная позиция станет враньём.</li>
<li><strong>Ищем локально.</strong> Есть ли целевой id внутри уже загруженного диапазона (см. модель дыр из карточки msg2)? Если да и он в текущем окне — просто скроллим, это дешёвый случай.</li>
<li><strong>Грузим окно.</strong> Если нет — запрос вида «дай 25 сообщений до и 25 после id X». В API Telegram это <code>messages.getHistory</code> с <code>offset_id</code> и отрицательным <code>add_offset</code>: смещение назад от якоря. Одним запросом, не двумя.</li>
<li><strong>Пересобираем ленту.</strong> Сбрасываем окно рендера и подставляем новый диапазон. Ключевой момент: <strong>первый кадр рисуется сразу на нужной позиции</strong>. Не «отрендерить сверху и потом проскроллить» — это даст вспышку чужого контента и лишний layout.</li>
<li><strong>Подсветка.</strong> Целевой пузырёк получает временный класс с фоновой вспышкой на 1–1.5 секунды. Плюс <code>prefers-reduced-motion</code>: без анимации, просто стабильный фон, который гаснет.</li>
<li><strong>Возврат.</strong> Плавающая кнопка со стрелкой вниз возвращает на сохранённую позицию. Стек, а не одна ячейка: из поиска в сообщение, оттуда в его ответ, оттуда ещё глубже — пользователь ждёт, что каждое нажатие вернёт на шаг назад.</li>
</ol>

<h4>Как встать точно на позицию</h4>
<p>Проблема: чтобы поставить scrollTop, нужно знать высоту всего, что выше цели, а мы её не знаем — эти сообщения не отрендерены. Решение — <strong>перевернуть систему координат</strong>. Рендерим только новое окно, считаем целевое сообщение точкой отсчёта: элементы до него получают отрицательные смещения, после — положительные, а виртуальные распорки сверху и снизу берут оценочную высоту из числа неподгруженных сообщений, умноженного на среднюю высоту (для чата это обычно 70–90px). Распорки заведомо врут, но пользователь их не видит: он смотрит на цель. По мере подгрузки соседних кусков распорка ужимается, а компенсация из карточки msg3 держит позицию.</p>
<p>Альтернатива для тех, кому важна честная полоса прокрутки: не рисовать полосу вообще, пока история не непрерывна. Telegram Web так и делает — скроллбар отражает загруженное окно, а не всю переписку.</p>

<h4>Если сообщения нет локально</h4>
<p>Обычный случай для глобального поиска и для ссылки вида t.me/chat/12345. Порядок: показываем скелетон окна нужной формы (не спиннер по центру — иначе видно, как всё моргает), уходим за окном на сервер, при ответе создаём <strong>новый диапазон</strong> в списке загруженных и рисуем. Диапазон не сливается с текущим — между ними дыра, и на границе стоит кнопка догрузки. Если сервер отвечает, что сообщение удалено или недоступно (нет прав, чат покинут), показываем не пустую ленту, а понятную заглушку и не портим состояние ленты: пользователь должен остаться там, где был.</p>

<h4>Частные случаи</h4>
<ul>
<li><strong>Ответ на сообщение.</strong> Клик по цитате — тот же механизм, но цель почти всегда рядом: сначала пробуем локально, и в 90% случаев это обычный скролл на 300px.</li>
<li><strong>Закреплённое.</strong> Их может быть несколько; повторные клики по плашке идут по кругу от новых к старым, индекс держим отдельно.</li>
<li><strong>Непрочитанные.</strong> Открытие чата с 500 непрочитанными — тот же прыжок, только якорь — разделитель «непрочитанные сообщения», и он должен оказаться под верхней кромкой, а не по центру.</li>
<li><strong>Быстрые повторные прыжки.</strong> Пользователь кликает по результатам поиска подряд. Каждый прыжок отменяет предыдущий запрос через AbortController, иначе поздний ответ перепишет ленту чужим окном — классическая гонка.</li>
</ul>

<h4>Компромиссы</h4>
<p>Прыжок с пересборкой ленты отдаёт непрерывность (полоса прокрутки врёт, между кусками дыры), но получает мгновенный переход за один запрос вместо сотен. Если бы чаты были короткими, можно было бы держать всю историю и просто скроллить — код проще на порядок. Стек возврата стоит памяти и логики очистки при смене чата, зато без него функция ощущается сломанной. Скелетон вместо спиннера дороже в разметке, но убирает скачок высоты при подстановке реальных сообщений.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Говорят «просто <code>scrollIntoView</code>» — а элемента в DOM нет, потому что лента виртуализирована.</li>
<li>Сохраняют scrollTop вместо id якоря, и возврат промахивается после любой подгрузки.</li>
<li>Не отменяют предыдущий запрос: два окна перетирают друг друга, лента моргает и встаёт не туда.</li>
<li>Забывают, что после прыжка «вниз к последним» — это не скролл, а ещё один переезд к концу истории.</li>
<li>Не переносят фокус: для клавиатуры и скринридера прыжок должен ставить фокус на целевое сообщение и объявлять его, иначе функция для них не существует.</li>
</ul>

<h4>Как углубят вопрос</h4>
<p><strong>«Что если id есть, но чат вообще не открыт?»</strong> — сначала открываем чат в состоянии «прыжок», не грузя последние сообщения: незачем тянуть хвост, если мы едем в середину. <strong>«Как сделать полосу прокрутки честной?»</strong> — только оценочно: общее число сообщений в чате сервер отдаёт в метаданных, умножаем на среднюю высоту, получаем приблизительную высоту холста и уточняем по мере загрузки; честной она станет только при полной истории.</p>`,
    code: `async function jumpToMessage(chatId, targetId, { highlight = true } = {}) {
  returnStack.push({
    chatId,
    anchorId: getFirstVisibleId(),          // id, а не scrollTop
    anchorOffset: getFirstVisibleOffset(),
  });

  abortCurrentJump();                        // гонка: поздний ответ не должен победить
  const signal = newJumpSignal();

  let window = readRangeFromDb(chatId, targetId, 25);
  if (!window) {
    showWindowSkeleton(targetId);
    // одним запросом: 25 до и 25 после якоря
    window = await api.getHistory({
      chatId, offsetId: targetId, addOffset: -25, limit: 51, signal,
    });
    if (signal.aborted) return;
    ranges.insert(chatId, { from: window[0].id, to: window.at(-1).id });
  }

  // лента пересобирается вокруг цели: первый кадр уже на нужной позиции
  setViewport({ items: window, anchorId: targetId, anchorAlign: 'center' });
  if (highlight) flash(targetId, 1200);
  focusMessage(targetId);                    // фокус и объявление для скринридера
}`,
    tip: 'Ключевая фраза: «первый кадр рисуем уже на нужной позиции, а не скроллим после рендера». И сразу добавь про AbortController — быстрые клики по результатам поиска ломают наивную реализацию за три секунды.' },

  { id: 'msg5',
    q: 'Где хранишь историю на клиенте? Почему IndexedDB и как выглядит схема?',
    a: `<h4>Коротко</h4>
<p>IndexedDB — единственное асинхронное хранилище браузера, которое переживает перезагрузку, держит сотни мегабайт, поддерживает составные индексы и доступно из воркера. localStorage синхронный и ограничен 5–10 МБ, Cache API не умеет запросы по диапазону ключей, OPFS быстрее на бинарях, но не даёт индексов. Схема — шесть хранилищ, ключевое из них messages с составным ключом [chatId, messageId].</p>

<h4>Почему именно IndexedDB</h4>
<ul>
<li><strong>localStorage</strong>: синхронный (блокирует главный поток на каждом чтении), 5–10 МБ на origin, только строки. На истории чатов не годится физически.</li>
<li><strong>Cache API</strong>: отличен для бинарей и ответов, но ключ — это Request. Запрос «сообщения чата 12345 старше id X, 50 штук» превращается в перебор всех ключей. Используем его для медиа, не для данных.</li>
<li><strong>OPFS</strong>: до 3–4 раз быстрее на потоковой записи бинарей и с синхронным доступом внутри воркера, но это файловая система без индексов — свой движок или SQLite в WebAssembly (плюс ~1 МБ к бандлу). Разумный гибрид: текст в IndexedDB, крупные файлы в OPFS.</li>
<li><strong>IndexedDB</strong>: транзакции, составные ключи и индексы, курсоры по диапазону, работа из Worker и Service Worker, квота в районе 60% свободного места диска в Chromium и WebKit. Для 300 тысяч сообщений по ~500 байт это 150 МБ — укладываемся с запасом.</li>
</ul>

<h4>Схема</h4>
<p>Шесть хранилищ. <strong>messages</strong> с ключом [chatId, messageId] — составной ключ сам по себе даёт сортировку внутри чата и диапазонные запросы курсором. <strong>dialogs</strong> с индексом по времени последнего сообщения и по флагу закрепления — список диалогов рисуется одним курсором. <strong>users</strong> и <strong>chats</strong> — справочники. <strong>media</strong> — метаданные файлов. <strong>outbox</strong> — очередь неотправленного, ключ clientId. <strong>searchIndex</strong> — обратный индекс, см. карточку msg8. Плюс маленькое <strong>kv</strong> для состояния синхронизации и настроек.</p>
<p>Что <strong>не</strong> кладём в IndexedDB: байты картинок и видео. Blob в IndexedDB проходит через structured clone, что на файле в 20 МБ означает лишнюю копию в памяти и ощутимую паузу; и его нельзя отдать в <code>&lt;video&gt;</code> потоком. Медиа — в Cache API (см. msg9).</p>

<h4>Что в памяти, что на диске</h4>
<p>В памяти: открытые чаты (окно 2–5 тысяч сообщений), весь список диалогов (3000 диалогов по 300 байт — меньше 1 МБ, дешевле держать), пользователи текущих чатов, состояние синхронизации. Остальное на диске. Чтение 50 сообщений из базы — 20–40 мс, это внутри одного кадра восприятия, промежуточный спиннер не нужен.</p>

<h4>Версионирование и миграции</h4>
<p>Схема меняется через <code>onupgradeneeded</code> и растущий номер версии. Три правила, о которых узнают больно:</p>
<ol>
<li><strong>Миграция синхронная внутри своей транзакции.</strong> Сходить в сеть нельзя: транзакция закроется на первом же await, не относящемся к IndexedDB.</li>
<li><strong>База, открытая в другой вкладке, блокирует апгрейд.</strong> Событие <code>blocked</code> — это «старая вкладка держит соединение». Протокол: в старой вкладке слушаем <code>versionchange</code>, закрываем соединение, показываем «обновите страницу». Иначе новая вкладка зависнет навсегда.</li>
<li><strong>Даунгрейд невозможен.</strong> После отката релиза старый код увидит базу более высокой версии и упадёт. Ловим ошибку и стираем базу — история восстановится с сервера, это лучше белого экрана.</li>
</ol>
<p>Тяжёлые миграции (перестроить индекс поиска на 300 тысячах сообщений) — не в <code>onupgradeneeded</code>, а фоновой задачей: подняли версию с пустым хранилищем, приложение работает, воркер досыпает по 500 записей с yield между порциями.</p>

<h4>Квоты и вытеснение</h4>
<p><code>navigator.storage.estimate()</code> даёт usage и quota. Квота в Chromium — до 60% свободного места диска на origin; при нехватке места браузер вытесняет данные origin-ов по принципу «давно не использовался», и вытесняет <strong>целиком</strong>: IndexedDB и Cache API исчезают вместе. В Safari при включённой защите от трекинга данные скриптового происхождения удаляются после 7 дней без взаимодействия пользователя с сайтом. Отсюда два вывода: (1) просить <code>navigator.storage.persist()</code> — в установленном PWA его выдают почти всегда; (2) проектировать так, чтобы <strong>потеря локальной базы была штатным сценарием</strong>, а не катастрофой. Единственное, что нельзя терять, — исходящая очередь; её дублируем в отдельную маленькую базу и никогда не чистим по квоте.</p>

<h4>Восстановление после сбоя</h4>
<p>Транзакция IndexedDB атомарна, «половина сообщения» не запишется. Реальные сбои другие: битая база после падения браузера (запросы виснут или бросают UnknownError) и рассинхрон счётчиков с содержимым. Лечение: при старте пишем и читаем пробную запись в kv, не вышло — <code>deleteDatabase</code> и полная пересинхронизация. Счётчик pts храним в той же транзакции, что и сообщения: иначе после падения состояние уйдёт вперёд без сообщений, и получится тихая дыра в истории.</p>

<h4>Чтение из воркера</h4>
<p>IndexedDB доступна из Worker и Service Worker — выносим туда чтение истории, запись пачек, индексацию поиска, дешифровку. Главный поток говорит с воркером сообщениями и не делает ни одной операции с базой. Выигрыш не в самом IO (он и так асинхронный), а в structured clone и десериализации: разбор пачки в 500 сообщений на главном потоке — 30–60 мс заблокированного кадра, в воркере ноль. Обратная передача тоже стоит клонирования, поэтому отдаём подготовленные к рендеру срезы, а не всю базу.</p>

<h4>Компромиссы</h4>
<p>IndexedDB дала индексы и запросы, но отдала скорость на бинарях и удобство API. Гибрид с OPFS ускоряет медиа ценой второго механизма вытеснения и второго источника расхождений. Хранить всю историю офлайн — платить местом и рисковать вытеснением; хранить последние 30 дней на чат экономно, но убивает локальный поиск по старому.</p>`,
    code: `// Схема: составной ключ [chatId, messageId] даёт и уникальность, и порядок
function upgrade(db, oldVersion) {
  if (oldVersion < 1) {
    const messages = db.createObjectStore('messages', {
      keyPath: ['chatId', 'messageId'],
    });
    messages.createIndex('byChatDate', ['chatId', 'date']);
    messages.createIndex('byMedia', 'mediaId');
    messages.createIndex('byReplyTo', ['chatId', 'replyToId']);

    const dialogs = db.createObjectStore('dialogs', { keyPath: 'chatId' });
    dialogs.createIndex('byOrder', ['pinned', 'lastMessageDate']);

    db.createObjectStore('users',  { keyPath: 'userId' });
    db.createObjectStore('media',  { keyPath: 'mediaId' });
    db.createObjectStore('outbox', { keyPath: 'clientId' });  // очередь отправки
    db.createObjectStore('kv');                               // pts, seq, настройки
  }
  if (oldVersion < 2) {
    // обратный индекс поиска: term -> постинг-лист. Заполняется фоном, не здесь
    db.createObjectStore('searchIndex', { keyPath: ['term', 'chatId'] });
  }
}

// Диапазонный запрос: 50 сообщений старше beforeId, курсором назад
const range = IDBKeyRange.bound([chatId, 0], [chatId, beforeId], false, true);
store.openCursor(range, 'prev');   // читаем 50 и останавливаемся

// Состояние синхронизации пишем В ТОЙ ЖЕ транзакции, что и сообщения,
// иначе после падения браузера pts уйдёт вперёд, а сообщений не будет
const tx = db.transaction(['messages', 'kv'], 'readwrite');
for (const m of batch) tx.objectStore('messages').put(m);
tx.objectStore('kv').put(newPts, 'pts');`,
    tip: 'Скажи вслух, что состояние синхронизации пишется в одной транзакции с сообщениями. Это ровно тот баг, который на проде проявляется как «пропали сообщения за вчера» и который почти никто не проговаривает.' },

  { id: 'msg6',
    q: 'Клиент был офлайн два дня. Как синхронизируешь состояние и как понимаешь, что что-то пропустил?',
    a: `<h4>Коротко</h4>
<p>Синхронизация не по времени и не по «дай всё новое», а по монотонному счётчику состояния. Клиент хранит последний применённый номер, сервер присылает событие с его номером и количеством шагов; если <code>локальный + count</code> не равен номеру события — это пропуск, событие откладывается в буфер и запускается догрузка разницы. После долгого офлайна разница может оказаться слишком большой — тогда сервер честно говорит «слишком много» и клиент пересобирает состояние с нуля.</p>

<h4>Как устроены счётчики</h4>
<p>Это продолжение модели данных из msg2. Telegram использует три счётчика: <strong>pts</strong> — позиция в «ящике сообщений» (у каждого канала свой, у личных чатов и обычных групп общий), <strong>qts</strong> — для секретных чатов и части служебных событий, <strong>seq</strong> — порядок самих контейнеров обновлений. Каждое событие несёт свой pts и pts_count (сколько позиций оно занимает: одно сообщение — 1, удаление десяти — 10). Правило применения одно: событие применяется, если <code>local_pts + pts_count === pts</code>. Меньше — событие уже применено, выбрасываем. Больше — пропуск.</p>
<p>Раздельные счётчики на каналы — не прихоть: канал на миллион подписчиков генерирует события с бешеной скоростью, и один общий счётчик заставил бы каждого клиента догонять чужой поток. Обратная сторона: менеджер обновлений обязан вести таблицу счётчиков по чатам и не пытаться догонять больше 10 каналов одновременно — иначе на старте улетает сотня параллельных запросов.</p>

<h4>Обнаружение пропуска</h4>
<ol>
<li>Пришло событие с pts выше ожидаемого. Не применяем, кладём в буфер.</li>
<li><strong>Ждём до 0.5 секунды.</strong> События могли прийти не по порядку из-за конкурентных запросов — недостающее часто приезжает само. Это дословная рекомендация протокола Telegram и она снимает большую часть ложных срабатываний.</li>
<li>Не приехало — запрашиваем разницу: «моё состояние такое, отдай, что изменилось». Ответ содержит новые сообщения, изменённые чаты, удаления и новое состояние.</li>
<li><strong>Разницу применяем перед буфером.</strong> Пока мы её тянули, из сокета накапали ещё события: их обрабатываем после, иначе получим неверный порядок.</li>
<li>Пока идёт догрузка, поток обновлений не применяется вообще — только копится. Иначе состояние разъедется необратимо.</li>
</ol>

<h4>После долгого офлайна</h4>
<p>Ящик сообщений на сервере конечен — порядка 100 тысяч событий для канала и порядка 5 миллионов для общего ящика. Если клиентский pts вывалился за это окно, сервер отвечает «разница слишком велика» и не может рассказать историю по шагам. Тогда: берём текущее состояние, перезагружаем список диалогов (топ-100 по свежести), для каждого открытого чата дозагружаем хвост, помечаем всё остальное как «возможно устарело» и досинхронизируем лениво при открытии. Локальную базу при этом <strong>не стираем</strong> — старые сообщения по-прежнему валидны, невалидны только счётчики непрочитанного и факты удаления.</p>
<p>Отдельная неприятность — удаления: если сообщение удалили, пока мы были офлайн, и событие не поместилось в разницу, оно останется у нас навсегда. Лечится сверкой при загрузке окна: сервер отдаёт актуальные id диапазона, лишние локальные вычищаем.</p>

<h4>Переподключение</h4>
<p>Сокет умирает молча чаще, чем с ошибкой: туннель, мобильная сеть, спящий ноутбук. Поэтому свой heartbeat — ping раз в 20–30 секунд, отсутствие pong за 10 секунд считаем разрывом, не дожидаясь TCP-таймаута в две минуты. Реконнект — экспоненциальный backoff с джиттером: 1, 2, 4, 8, 16, максимум 30 секунд, добавка до 30% (без джиттера тысячи клиентов ломятся синхронно после сбоя). Мгновенный реконнект без задержки — по событиям <code>online</code> и <code>visibilitychange</code>. После подъёма сокета первым делом проверка состояния.</p>

<h4>Идемпотентность и дедупликация</h4>
<p>Одно и то же событие приходит дважды регулярно: пришло по сокету и попало в разницу, или сервер переслал после реконнекта. Три уровня защиты: (1) сравнение pts — событие ниже текущего просто игнорируется; (2) запись сообщения через <code>put</code> по составному ключу [chatId, messageId], а не <code>add</code> — повтор перезаписывает тем же; (3) для исходящих — clientId (random_id), сервер хранит их и на повтор возвращает уже созданное сообщение вместо второго. Все три нужны: pts не спасает от повторов внутри разницы, put не спасает от неподтверждённых сообщений без серверного id.</p>

<h4>Порядок применения</h4>
<p>Внутри пачки события применяются строго по возрастанию pts, и вся пачка — одной транзакцией IndexedDB вместе с новым значением счётчика. Это тот же принцип, что в msg5: состояние и данные обязаны обновляться атомарно. Уведомление UI — одно на всю пачку, а не по событию, иначе на разнице в 2000 сообщений будет 2000 перерисовок.</p>

<h4>Компромиссы</h4>
<p>Модель со счётчиком гарантирует отсутствие дыр и даёт дешёвую догрузку, но требует, чтобы сервер вёл упорядоченный лог событий на пользователя, — серьёзное серверное обязательство. Альтернатива «дай всё после timestamp T» пишется за час и ломается на рассинхроне часов, одинаковых метках и событиях без сообщений (прочитано, отредактировано). Для read-only дашборда, где потеря события некритична, timestamp был бы разумным выбором. Пауза в 0.5 секунды добавляет задержку в редком случае, но экономит массу ненужных запросов разницы.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Применяют события сразу по мере поступления, без буфера, — и при любом переупорядочивании получают дыру, о которой не узнают.</li>
<li>Тянут разницу и параллельно применяют новые события из сокета: порядок перемешивается.</li>
<li>Реконнект без джиттера — при массовом обрыве клиенты укладывают сервер повторно.</li>
<li>Не сбрасывают presence и «печатает» при разрыве: собеседник вечно «печатает».</li>
<li>Не различают «нет сети» и «сервер недоступен»: <code>navigator.onLine</code> говорит только о сетевом интерфейсе, реальный признак — успешный обмен по сокету.</li>
</ul>`,
    code: `// Событие с сервера несёт своё положение в логе и вес
// { type:'newMessage', pts: 1043, ptsCount: 1, message: {...} }

function applyUpdate(u) {
  const local = state.pts;

  if (local + u.ptsCount === u.pts) { commit(u); return; }   // ровно следующее
  if (u.pts <= local)               { return; }              // уже применяли
  bufferGap(u);                                              // пропуск
}

function bufferGap(u) {
  pending.push(u);
  clearTimeout(gapTimer);
  // ждём 0.5 с: недостающее событие часто приезжает само
  gapTimer = setTimeout(fillGap, 500);
}

async function fillGap() {
  stopApplying();                     // поток обновлений только копится
  let diff = await api.getDifference({ pts: state.pts, date: state.date });

  if (diff.type === 'tooLong') {      // офлайн дольше, чем окно лога сервера
    await resyncFromScratch();        // диалоги заново, историю не трём
  } else {
    await commitBatch(diff.messages, diff.newPts);   // одна транзакция
  }

  // разницу применили — теперь добираем то, что накопилось в буфере
  const rest = pending.splice(0).sort((a, b) => a.pts - b.pts);
  resumeApplying();
  rest.forEach(applyUpdate);
}`,
    tip: 'Назови паузу в полсекунды перед объявлением пропуска — это деталь из реального протокола, и она сразу показывает, что человек читал спецификацию, а не пересказывает общую идею «синхронизации по курсору».' },

  { id: 'msg7',
    q: 'Пользователь набрал сообщение и нажал Enter в метро без сети. Опиши весь путь сообщения.',
    a: `<h4>Коротко</h4>
<p>Сообщение рисуется в ленте мгновенно с локальным clientId и статусом «отправляется», попадает в персистентную очередь outbox в IndexedDB, и оттуда его забирает единственный отправщик — строго по одному на чат, чтобы не сломать порядок. Идемпотентность держится на clientId: сервер помнит его и на повтор возвращает уже созданное сообщение, а не создаёт второе.</p>

<h4>Путь по шагам</h4>
<ol>
<li><strong>Композер.</strong> Генерируем clientId и localSeq (см. msg2), собираем entities из разметки, очищаем черновик.</li>
<li><strong>Запись в outbox.</strong> В <strong>одной</strong> транзакции: put в outbox и put в messages со статусом sending. Не в память — в базу: пользователь закроет вкладку через две секунды, и сообщение обязано пережить это.</li>
<li><strong>Рендер.</strong> Пузырёк появляется сразу, с часиками. Лента скроллится вниз. Задержка от Enter до пикселя — целевые 50 мс, реально достижимо 15–25.</li>
<li><strong>Отправка.</strong> Отправщик берёт из очереди первое по этому чату, шлёт по сокету с clientId. Если сокета нет — ничего не делает, ждёт события подключения.</li>
<li><strong>Подтверждение.</strong> Сервер отвечает messageId и датой. Обновляем запись: клеим серверный id, статус sent, удаляем из outbox — снова одной транзакцией.</li>
<li><strong>Доставка и прочтение.</strong> Приходят отдельными событиями обновлений, меняют только флаг. Отрисовка галочек — это смена одного класса, не перемонтирование пузырька.</li>
</ol>

<h4>Порядок при нескольких сообщениях подряд</h4>
<p>Главная ловушка. Пользователь отправил три сообщения за секунду. Если запустить три запроса параллельно, сервер присвоит им id в порядке прихода — и «раз», «два», «три» станут «два», «раз», «три». Решение: <strong>очередь с параллелизмом 1 в пределах чата</strong>. Между чатами параллелить можно и нужно — они независимы. Второй нюанс: если первое сообщение упало насовсем, остальные из этого чата нельзя отправлять молча вперёд — иначе разговор станет бессвязным. Правильно: блокируем очередь чата, показываем ошибку на первом, остальные держим в статусе «ждёт», и пользователь решает — повторить или удалить.</p>
<p>Медиа выбивается из этой схемы: файл в 50 МБ грузится минуту, и держать за ним текстовые сообщения нельзя. Компромисс, который делают все: загрузка байтов (msg9) идёт параллельно и заранее, а <strong>публикация</strong> сообщения с уже загруженным файлом встаёт в общую очередь чата.</p>

<h4>Ретраи и backoff</h4>
<p>Различаем три класса ошибок. <strong>Сетевые и 5xx</strong> — повторяем: 1, 2, 4, 8, 16, 30 секунд, дальше каждые 30 с джиттером ±30%. <strong>Явные отказы</strong> (нет прав писать в чат, пользователь заблокировал, сообщение слишком длинное) — не повторяем никогда, сразу показываем ошибку: бесконечный ретрай на 403 греет батарею и ничего не чинит. <strong>Флуд-контроль</strong> — сервер присылает «подожди N секунд», уважаем именно это число, а не свою формулу. Отдельно: <code>navigator.onLine === false</code> — не ретраим вообще, спим до события <code>online</code>. Ретраи по таймеру в закрытой вкладке бесполезны, поэтому серьёзные клиенты дублируют отправку через Background Sync в Service Worker — там, где он есть (в Safari его нет, см. msg12).</p>

<h4>Статусы и что показывать при неудаче</h4>
<p>Пять состояний: <strong>sending</strong> (часики), <strong>sent</strong> (одна галочка — сервер принял), <strong>delivered</strong> (две — доставлено на устройство), <strong>read</strong> (две цветные), <strong>failed</strong> (красный восклицательный знак и меню «повторить / изменить / удалить»). Тонкость: часики не должны появляться мгновенно. Если ответ пришёл за 200 мс, мигнувшая иконка выглядит как глюк — вводим задержку показа индикатора в 300–500 мс. Пузырёк при этом видно сразу, а вот индикатор проблемы — только когда проблема реальна.</p>
<p>Ещё одна тонкость, которую замечают только на проде: сообщение в статусе failed нельзя молча удалять при перезагрузке. Оно живёт в outbox и должно быть видно в ленте после перезапуска — иначе пользователь уверен, что отправил.</p>

<h4>Идемпотентность</h4>
<p>Сценарий, который случается постоянно: запрос ушёл, сервер создал сообщение, ответ потерялся при обрыве. Клиент повторяет. Без защиты — дубль. С clientId сервер видит уже использованный идентификатор и возвращает ранее созданное сообщение. Именно так работает random_id в Telegram: сервер хранит использованные значения и на повторный вызов отдаёт результат первого. На клиенте нужна зеркальная защита: когда подтверждение пришло дважды, вторая обработка не должна создавать вторую запись — спасает <code>put</code> по составному ключу и удаление из outbox по clientId (повторное удаление безвредно).</p>

<h4>Компромиссы</h4>
<p>Оптимистичный UI даёт ощущение мгновенности, но платит откатами: сообщение может исчезнуть или покраснеть через минуту. Альтернатива — блокировать композер до подтверждения — честнее, но в мессенджере неприемлема: пользователи набирают быстрее, чем летает пакет. Очередь с параллелизмом 1 гарантирует порядок ценой пропускной способности: при 10 сообщениях подряд и RTT 300 мс последнее уйдёт через 3 секунды. Можно оптимизировать батчингом (одна отправка с массивом), но это усложняет и API, и обработку частичных отказов.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Хранят очередь только в памяти — при закрытии вкладки сообщение пропадает бесследно.</li>
<li>Ретраят всё подряд, включая 403 и «текст слишком длинный».</li>
<li>Параллельная отправка ломает порядок, и это замечают только в проде на быстрых печатающих.</li>
<li>Меняют ключ элемента с clientId на серверный id при подтверждении — React размонтирует пузырёк, слетает анимация и дёргается скролл.</li>
<li>Не различают «сервер принял» и «доставлено получателю» — рисуют галочку по ответу сервера и врут пользователю.</li>
</ul>`,
    code: `// Очередь: строго по одному в пределах чата, чаты параллельно
const inFlight = new Map();   // chatId -> Promise

async function enqueue(msg) {
  const tx = db.transaction(['outbox', 'messages'], 'readwrite');
  tx.objectStore('outbox').put(msg);                    // переживёт закрытие вкладки
  tx.objectStore('messages').put({ ...msg, state: 'sending' });
  await tx.done;
  render(msg);                                          // пузырёк уже в ленте

  const prev = inFlight.get(msg.chatId) || Promise.resolve();
  const next = prev.then(() => pump(msg)).catch(() => {});
  inFlight.set(msg.chatId, next);
}

async function pump(msg, attempt = 0) {
  if (!navigator.onLine) return waitFor('online').then(() => pump(msg, attempt));

  try {
    // clientId = random_id: повтор вернёт то же сообщение, а не создаст второе
    const res = await api.sendMessage({ ...msg, randomId: msg.clientId });
    await confirm(msg.clientId, res.messageId, res.date);
  } catch (e) {
    if (e.permanent) return markFailed(msg, e);         // 403, слишком длинно
    if (e.floodWait) return sleep(e.floodWait * 1000).then(() => pump(msg, attempt));

    const delay = Math.min(30000, 1000 * 2 ** attempt);
    await sleep(delay * (0.7 + Math.random() * 0.6));   // джиттер +-30%
    return pump(msg, attempt + 1);
  }
}`,
    tip: 'Проговори задержку показа часиков в 300–500 мс: пузырёк рисуем сразу, а индикатор проблемы — только если проблема успела стать реальной. Это уровень детали, до которого доходят только те, кто такое делал.' },

  { id: 'msg8',
    q: 'Как сделать поиск по сообщениям? Пользователь ищет «отчёт» и ждёт результат мгновенно.',
    a: `<h4>Коротко</h4>
<p>Гибрид: локальный обратный индекс в IndexedDB отвечает за 10–30 мс по тому, что уже скачано, и рисуется сразу; параллельно уходит серверный запрос, и его результаты дописываются к списку с дедупликацией по id. Индекс строится в воркере инкрементально при каждом новом сообщении, ключ индекса — нормализованный токен, значение — постинг-лист id сообщений.</p>

<h4>Токенизация</h4>
<p>Режем по границам слов через <code>Intl.Segmenter</code> с гранулярностью word — он справляется и с кириллицей, и с китайским, где пробелов нет, в отличие от регулярки по <code>\\s+</code>. Дальше: нижний регистр, <code>normalize('NFKD')</code>, схлопывание ё в е (иначе «ёлка» и «елка» — разные слова), выброс токенов короче двух символов и стоп-слов («и», «в», «на» дают постинг-листы в десятки тысяч записей и ничего не различают). Упоминания и хештеги токенизируем отдельно — по ним ищут постоянно.</p>

<h4>Русская морфология</h4>
<p>Главная специфика: «отчёт», «отчёта», «отчётом», «отчёты» — для пользователя одно слово. Три варианта:</p>
<ul>
<li><strong>Стеммер Портера (Snowball) для русского</strong> — отсекает окончания по правилам. Реализация на JS около 6 КБ, работает за микросекунды; ошибается на исключениях, но для поиска по своим сообщениям точности хватает.</li>
<li><strong>Морфоанализатор со словарём</strong> — точнее, но это 5–15 МБ словаря, неприемлемо без ленивой загрузки.</li>
<li><strong>Обрезка до 5–6 символов</strong> — грубо, 30 строк кода, даёт ложные срабатывания, зато не требует ничего.</li>
</ul>
<p>Практичный выбор: Snowball, в индекс кладём <strong>основу</strong>, запрос стеммим тем же алгоритмом. Версию алгоритма храним в схеме: смена стеммера означает полную переиндексацию.</p>

<h4>Префиксный поиск</h4>
<p>Пользователь набирает «отч» и ждёт подсказку. Составной ключ [term, chatId] в IndexedDB даёт это бесплатно: <code>IDBKeyRange.bound(['отч'], ['отч\\uffff'])</code> — курсор по всем терминам с этим префиксом. Ограничиваем сотней терминов, иначе на «а» вычитаем полбазы. Именно поэтому обратный индекс лежит в IndexedDB как отдельное хранилище, а не как Map в памяти: диапазонный курсор по ключу — то, чего Map не умеет.</p>

<h4>Структура и размер индекса</h4>
<p>Постинг-лист — не запись на пару (термин, сообщение), а массив id на пару (термин, чат). Разница принципиальна: 300 тысяч сообщений по 15 значимых слов дают 4.5 миллиона пар — отдельными записями это гигабайты оверхеда IndexedDB, массивами 30–40 МБ. Ориентир: индекс весит 25–40% от объёма текста. Слишком длинные постинг-листы режем по 5000 id — такое слово всё равно не сужает поиск.</p>

<h4>Инкрементальное обновление</h4>
<p>Новое сообщение токенизируем и дописываем id в постинг-листы, но не по одному (транзакция на каждое слово дорога) — копим в памяти и сбрасываем пачкой раз в 2 секунды или на 50 сообщений. Первичная индексация — фоновая задача в воркере: пачками по 500, между пачками yield, прогресс в kv, чтобы перезагрузка не начинала сначала. Удаление не переписывает постинг-листы (пришлось бы знать все термины сообщения) — ведём tombstone-множество и фильтруем на выдаче.</p>

<h4>Когда идти на сервер</h4>
<p>Всегда параллельно локальному: локально нет истории старше загруженного окна и нет чатов, которые пользователь не открывал. Выдача в три шага: локальные результаты рисуем через 10–30 мс, экран уже не пустой; серверный запрос уходит с дебаунсом 250–300 мс; ответ мержится — уже показанные id не дублируются, новые дописываются снизу, без перестроения списка сверху, иначе пользователь промахнётся по результату, в который целился. Устаревшие ответы отбрасываем по номеру запроса (гонка: «отч» ответил позже «отчёт»).</p>

<h4>Ранжирование</h4>
<p>Простое и предсказуемое лучше умного: точная фраза выше совпадения по основам; свежие выше старых через логарифмическое затухание по возрасту, а не жёсткую сортировку по дате (иначе релевантность не работает вовсе); текущий чат выше остальных. BM25 в браузере посчитать можно, но он требует хранить длины документов и частоты — обычно избыточно.</p>

<h4>Внутри чата против глобального</h4>
<p>Внутри чата — фильтр по chatId в составном ключе, диапазон сужается сразу, отвечает за единицы миллисекунд, и результат показывается лентой с прыжком по клику (msg4). Глобальный — обход всех чатов, группировка по диалогам, показ по 2–3 совпадения на диалог с кнопкой «показать все». Это разные UI и разные запросы, их нельзя делать одним компонентом.</p>

<h4>Подсветка</h4>
<p>Подсвечиваем не по подстроке запроса (стемминг же — «отчёт» найдёт «отчётами», а подстроки там нет), а по позициям токенов: при токенизации сниппета помечаем токены, чья основа совпала с основой запроса. Резать HTML регуляркой нельзя — сломается на entities. Правильный путь: <code>&lt;mark&gt;</code> поверх уже разобранного дерева, или CSS Custom Highlight API, где подсветка вообще не трогает DOM.</p>

<h4>Компромиссы</h4>
<p>Локальный индекс даёт мгновенность и офлайн, но стоит 25–40% дополнительного места и постоянной фоновой работы, а при вытеснении квоты исчезает вместе с базой. При сквозном шифровании серверного поиска нет вообще, и локальный индекс становится единственным вариантом — тогда его придётся держать полным любой ценой. Стемминг повышает полноту и снижает точность. Отказ от частых слов ускоряет всё, но ломает поиск точных фраз с ними — лечится постфильтрацией кандидатов по исходному тексту.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Предлагают перебирать все сообщения курсором и проверять <code>includes</code>. На 300 тысячах это 3–8 секунд и заблокированный воркер.</li>
<li>Игнорируют морфологию: русский поиск без стемминга не находит ничего по естественному запросу.</li>
<li>Не дедуплицируют локальную и серверную выдачу — результаты двоятся.</li>
<li>Не отменяют устаревшие запросы, и список моргает назад к предыдущему запросу.</li>
<li>Индексируют на главном потоке — при загрузке истории интерфейс встаёт колом.</li>
</ul>`,
    code: `// Обратный индекс: ключ [основа, чат], значение — постинг-лист
// { term: 'отчёт', chatId: 12345, ids: [98214, 98180, 97002, ...] }

const STOP = new Set(['и','в','на','с','по','не','что','а','это','как']);
const seg = new Intl.Segmenter('ru', { granularity: 'word' });

function tokenize(text) {
  const out = new Set();
  for (const { segment, isWordLike } of seg.segment(text)) {
    if (!isWordLike) continue;
    const w = segment.toLowerCase().normalize('NFKD').replace(/ё/g, 'е');
    if (w.length < 2 || STOP.has(w)) continue;
    out.add(stemRu(w));            // Snowball: 'отчётами' -> 'отчёт'
  }
  return out;
}

// Префиксный поиск даром — диапазонный курсор по составному ключу
function prefixTerms(prefix, chatId) {
  const lo = chatId ? [prefix, chatId] : [prefix];
  const hi = chatId ? [prefix + '\\uffff', chatId] : [prefix + '\\uffff'];
  return store.getAll(IDBKeyRange.bound(lo, hi), 100);
}

// Гибридная выдача: локально сразу, сервер догоняет, дубли не показываем
let queryId = 0;
async function search(q) {
  const my = ++queryId;
  render(await localSearch(q));                    // 10-30 мс
  const remote = await api.search(q, { debounce: 280 });
  if (my !== queryId) return;                      // ответ устарел
  appendUnique(remote.filter(r => !shownIds.has(r.messageId)));
}`,
    tip: 'Про русскую морфологию скажи конкретно: Snowball-стеммер весом около 6 КБ, основа в индекс, тот же стеммер на запрос. Без этого поиск по русскому не находит ничего, и это ровно тот пункт, который отличает продуманный ответ от книжного.' },

  { id: 'msg9',
    q: 'Как устроена работа с медиа: загрузка файла на 500 МБ, показ картинок в ленте, воспроизведение видео?',
    a: `<h4>Коротко</h4>
<p>Загрузка — чанками по 512 КБ с возобновлением по сохранённому в IndexedDB прогрессу. Показ — трёхступенчато: blurhash из метаданных мгновенно, миниатюра, полный файл. Хранение байтов — Cache API, потому что он умеет отдавать Response потоком и переживать Range-запросы, в отличие от Blob в IndexedDB. Видео — через Service Worker, который склеивает 206-ответы из кеша.</p>

<h4>Загрузка с возобновлением</h4>
<p>Файл режется на чанки по 512 КБ: меньше — слишком много round-trip-ов, больше — дорогой повтор при обрыве на мобильной сети. Каждый чанк уходит со своим индексом, сервер собирает их по id сессии. Прогресс (id сессии, размер чанка, подтверждённые индексы) пишем в IndexedDB после каждого чанка — иначе перезагрузка на 480-м мегабайте начнёт всё сначала. Возобновление: спрашиваем сервер, какие чанки уже есть (в tus это HEAD с Upload-Offset), и досылаем недостающие.</p>
<p>Параллелизм — 3–4 чанка: один поток не насыщает канал, десять забивают его так, что текстовые сообщения опаздывают. Отмена — <code>AbortController</code> на каждый чанк плюс удаление сессии на сервере, иначе на бэкенде копятся мусорные полузагрузки. Файл держим по ссылке: <code>file.slice(offset, offset + CHUNK)</code> даёт ленивый срез, и 500 МБ никогда не оказываются в куче.</p>

<h4>Превью и прогрессивная подгрузка</h4>
<p>Три ступени: <strong>blurhash</strong> — 20–50 байт прямо в метаданных, декодируется в canvas за доли миллисекунды, показывается до любого сетевого запроса (ThumbHash даёт то же за ~28 байт и вдобавок кодирует пропорции и альфу); <strong>миниатюра</strong> — JPEG на 320px, 10–30 КБ, грузится при появлении сообщения в окне; <strong>полный файл</strong> — когда элемент реально видно (IntersectionObserver с запасом в экран) или по клику. Место резервируется по width и height из метаданных ещё до первого байта — то самое условие из msg3, без которого лента прыгает. Переход между ступенями — кроссфейд 150–200 мс, иначе смена blurhash на фото выглядит как моргание.</p>

<h4>Cache API против IndexedDB</h4>
<p>Байты медиа кладём в Cache API: значение — это <code>Response</code>, его отдаёт в <code>&lt;img&gt;</code> или <code>&lt;video&gt;</code> Service Worker без материализации в память; Blob в IndexedDB проходит через structured clone, то есть лишняя копия и заметная пауза на десятках мегабайт. Метаданные (какой файл, когда трогали, сколько весит) при этом всё равно в IndexedDB — Cache API не умеет запросы «отдай самое старое». Альтернатива — OPFS: до 3–4 раз быстрее на потоковой записи и синхронный доступ в воркере, но нет интеграции с Service Worker, всё чтение придётся проксировать самому.</p>

<h4>Вытеснение по объёму</h4>
<p>Свой бюджет, скажем 1 ГБ, независимо от браузерной квоты (60% свободного диска в Chromium — это может быть и 200 ГБ, забивать их нельзя). Учёт в таблице media: размер, время последнего доступа, признак «закреплено» (аватары, стикеры активных наборов, играющий сейчас файл). Вытеснение — LRU, запускается при превышении бюджета и в простое через <code>requestIdleCallback</code>, пачками до 20% запаса, иначе будем чистить на каждой картинке. Перед вытеснением сверяемся с <code>navigator.storage.estimate()</code>: если браузер сам близок к пределу, режем бюджет вдвое, не дожидаясь, пока он вытеснит origin целиком вместе с историей.</p>

<h4>Стриминг видео и голосовых</h4>
<p><code>&lt;video&gt;</code> запрашивает файл Range-запросами: сначала пара сотен килобайт с начала (moov-атом с индексом кадров), потом куски по мере воспроизведения. Cache API этого не умеет: <code>cache.match()</code> на запрос с Range отдаст полный 200-ответ, а Safari требует именно 206 и без него не начнёт играть. Решение — Service Worker: перехватываем запрос, достаём полный ответ из кеша, режем по диапазону и возвращаем рукотворный 206 с корректным Content-Range (это и делает workbox-range-requests). Второй камень: 206-ответы <strong>нельзя класть в Cache API</strong> — <code>cache.put</code> на частичный ответ бросает исключение.</p>
<p>Голосовые короткие — десятки килобайт в opus, качаем целиком и держим LRU на 50–100 последних, чтобы перемотка была мгновенной. Волновая форма приходит в метаданных массивом уровней и рисуется до загрузки звука.</p>

<h4>Экономия трафика</h4>
<p>Режим «не грузить медиа автоматически»: <code>navigator.connection.effectiveType</code> и <code>saveData</code> дают подсказку (не гарантию — в Safari их нет). При saveData только blurhash и миниатюры, полные файлы по тапу. Формат — AVIF или WebP через <code>&lt;picture&gt;</code>: WebP экономит 25–35% против JPEG, AVIF до 50%. Превью видео — не картинка, а беззвучный VP9 на 1–2 секунды весом 30–60 КБ.</p>

<h4>Компромиссы</h4>
<p>512 КБ — компромисс между накладными расходами и ценой повтора: на проводном канале 4–8 МБ быстрее, на 3G 256 КБ надёжнее, поэтому размер стоит адаптировать по измеренной скорости. Cache API дал стриминг ценой запросов по метаданным — держим вторую таблицу и рискуем рассинхроном (лечится сверкой при старте). Blurhash стоит 20–50 байт на сообщение, зато убирает серые прямоугольники полностью. Свой бюджет в 1 ГБ защищает от вытеснения всего origin, но означает, что старое медиа придётся качать заново — для мессенджера приемлемо, для офлайн-архива нет.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Читают весь файл через FileReader перед отправкой — вкладка падает на 500 МБ. Нужен <code>slice</code>.</li>
<li>Не сохраняют прогресс на диск: перезагрузка обнуляет часовую загрузку.</li>
<li>Кладут в Cache API 206-ответы и получают исключение, а видео не играет в Safari.</li>
<li>Не резервируют место под картинку — лента прыгает на каждой загрузке.</li>
<li>Забывают отзывать <code>URL.createObjectURL</code> — сотни мегабайт удержанных blob-ов за сессию.</li>
</ul>`,
    code: `// Загрузка: ленивые срезы файла, прогресс на диске, 4 чанка параллельно
const CHUNK = 512 * 1024;

async function upload(file, sessionId) {
  const total = Math.ceil(file.size / CHUNK);
  const done = await db.get('uploads', sessionId) || new Set();

  await pool(4, range(total), async (i) => {
    if (done.has(i)) return;                       // возобновление
    const part = file.slice(i * CHUNK, (i + 1) * CHUNK);   // без чтения в память
    await api.saveFilePart({ sessionId, index: i, part, signal });
    done.add(i);
    await db.put('uploads', done, sessionId);      // переживёт перезагрузку
  });
}

// Отдача видео: Cache API не режет Range сам, режем в Service Worker
self.addEventListener('fetch', (e) => {
  const range = e.request.headers.get('range');
  if (!range || !isMedia(e.request.url)) return;

  e.respondWith((async () => {
    const full = await caches.match(e.request.url, { ignoreSearch: true });
    if (!full) return fetch(e.request);

    const buf = await full.arrayBuffer();
    const start = Number(/bytes=(\\d+)-/.exec(range)[1]);
    const end = Math.min(start + 1024 * 1024, buf.byteLength) - 1;

    return new Response(buf.slice(start, end + 1), {
      status: 206,                                  // Safari без 206 не играет
      headers: {
        'Content-Range': 'bytes ' + start + '-' + end + '/' + buf.byteLength,
        'Accept-Ranges': 'bytes',
        'Content-Type': full.headers.get('content-type'),
      },
    });
  })());
});
// ВАЖНО: cache.put() с 206-ответом бросает исключение — кешируем только 200`,
    tip: 'Скажи, что 206-ответ нельзя положить в Cache API и что Safari без 206 не начнёт воспроизведение. Эти два факта вместе объясняют, зачем вообще Service Worker в схеме медиа, — и почти никто их не называет.' },

  { id: 'msg10',
    q: 'Как сделать реакции на сообщения? В канале на миллион подписчиков лайки летят сотнями в секунду.',
    a: `<h4>Коротко</h4>
<p>Храним не список реакций, а агрегаты — «эмодзи, счётчик, поставил ли я» — плюс отдельно подгружаемый список авторов. Своё нажатие применяем оптимистично и мгновенно, чужие обновления схлопываем в буфер и вливаем пачкой раз в 300–500 мс. Анимация счётчика делается так, чтобы не менялась высота пузырька, иначе лента дёргается на каждом чужом лайке.</p>

<h4>Модель данных</h4>
<p>На сообщении лежит короткий массив агрегатов: <code>[{ emoji: '👍', count: 1240, chosen: true }, ...]</code>. Никаких «кто поставил» здесь нет: в канале это миллион записей, и держать их на клиенте бессмысленно. Список авторов — отдельный запрос по клику на счётчик, страницами по 50, с курсором; кешируется на минуту и выбрасывается при закрытии поповера. Флаг <code>chosen</code> — единственное, что связывает агрегат со мной, и он приходит с сервера вместе с сообщением.</p>
<p>Порядок реакций в пузырьке фиксирован (по убыванию счётчика), но <strong>не пересортировывается на лету</strong>: иначе кнопки прыгают под пальцем и пользователь промахивается. Пересортировка — только когда сообщение ушло из вьюпорта. Кастомные эмодзи-реакции хранят documentId вместо символа и рендерятся механизмом из msg11, со всеми его ограничениями по числу анимаций.</p>

<h4>Оптимистичное применение</h4>
<p>Нажатие: сразу меняем <code>chosen</code> и <code>count</code> на ±1 локально, ставим в очередь, рисуем анимацию. Отличие от отправки сообщения (msg7): реакции <strong>не нужна очередь с порядком</strong>, нужна дедупликация по последнему состоянию. Пользователь может ткнуть в лайк десять раз за секунду — уйти должен один запрос с финальным состоянием. Реализация: троттлинг с trailing на 400 мс на пару (messageId, emoji), в полёте только один запрос, новое намерение перезаписывает то, что уйдёт следующим.</p>
<p>Откат при ошибке — <strong>к серверному значению из ответа</strong>, а не к запомненному до нажатия: пока запрос летел, счётчик мог измениться от чужих действий, и наивный откат покажет устаревшее число.</p>

<h4>Схлопывание частых обновлений</h4>
<p>Сервер в канале обычно и сам не шлёт каждое нажатие: события приходят агрегированно раз в несколько секунд, с итоговым счётчиком. Но даже так на экране 30 видимых сообщений и по каждому обновление — 30 перерисовок в секунду. Схема: события падают в буфер Map по messageId (последнее побеждает, промежуточные не нужны), буфер сливается в стор по <code>requestAnimationFrame</code> или таймеру на 300–500 мс. Обновления по сообщениям <strong>вне вьюпорта</strong> до UI не доходят вовсе — пишутся в базу и применятся при рендере.</p>
<p>Ещё экономия: счётчик выше тысячи показываем округлённо («1,2К»). Обновление с 1240 на 1241 не меняет отрисованный текст, и перерисовку можно пропустить, сравнив форматированные строки, а не числа.</p>

<h4>Конфликты при одновременных реакциях</h4>
<p>Классическая гонка: я ставлю лайк, одновременно приходит серверное событие со счётчиком, посчитанным <strong>до</strong> моего нажатия. Наивная реализация запишет серверное значение, лайк «отвалится» на глазах и через секунду вернётся. Лечение — версионирование: у агрегата есть номер версии, событие с версией ниже применённой игнорируется. Плюс период «мой оптимистичный ответ главнее»: 2–3 секунды после локального действия входящие события меняют только чужие счётчики, не трогая мой <code>chosen</code>. Второй конфликт — две мои вкладки; решается BroadcastChannel из msg14: локальное изменение сразу транслируется соседям, чтобы лайк не мигал.</p>

<h4>Анимация без дёрганья ленты</h4>
<p>Правило: <strong>панель реакций не меняет высоту пузырька во время анимации</strong>. Первая реакция высоту меняет неизбежно — единственный допустимый случай, и там работает компенсация скролла из msg3. Дальше всё внутри зарезервированной строки: счётчик — вертикальный сдвиг цифр (два спана, <code>transform: translateY</code>), эмодзи — <code>scale</code>, частицы — абсолютно позиционированный слой вне потока. Только <code>transform</code> и <code>opacity</code>: они анимируются на композиторе и не вызывают ни layout, ни paint. Полноэкранные эффекты (конфетти на весь чат) рисуем в canvas, не больше одного одновременно, и отключаем при <code>prefers-reduced-motion</code>.</p>

<h4>Доступность</h4>
<ul>
<li>Кнопка реакции — настоящий <code>button</code> с <code>aria-pressed</code>, отражающим <code>chosen</code>, и с внятным именем: «Нравится, 1240 реакций, вы отреагировали». Голый эмодзи скринридер прочитает как «эмодзи большой палец вверх» без счётчика и без состояния.</li>
<li>Обновление счётчика от чужих действий <strong>не</strong> объявляется — это шум. Объявляется только результат собственного действия, через <code>aria-live="polite"</code> на невидимом статус-регионе.</li>
<li>Панель выбора эмодзи — фокус-ловушка, закрытие по Escape, возврат фокуса на кнопку, стрелки для навигации по сетке.</li>
<li>Цель нажатия минимум 24×24 CSS-пикселя (требование WCAG 2.2), а реально 32×32 — эмодзи в 16px промахивается пальцем.</li>
</ul>

<h4>Компромиссы</h4>
<p>Агрегаты дали постоянный размер данных на сообщение независимо от аудитории, но отобрали локальный ответ на «кто поставил» — всегда нужен запрос. В личной переписке можно было бы хранить список целиком и не ходить на сервер; модель выбрана по худшему случаю. Троттлинг в 400 мс экономит запросы, но открывает окно расхождения — за это платим версионированием. Батчинг в 300–500 мс делает счётчики «ленивыми», зато держит 60 fps при скролле по каналу.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Хранят список пользователей на каждой реакции — на канале это разносит память.</li>
<li>Пересортировывают реакции при каждом обновлении, и кнопки прыгают под курсором.</li>
<li>Откатывают ошибку к запомненному до нажатия значению, а не к серверному, и показывают устаревший счётчик.</li>
<li>Анимируют высоту или margin вместо transform — получают layout thrashing на каждом лайке.</li>
<li>Не ставят <code>aria-pressed</code>: для скринридера реакция и «нереакция» выглядят одинаково.</li>
</ul>`,
    code: `// Агрегаты, а не список авторов: размер не зависит от аудитории
// message.reactions = [{ emoji:'👍', count:1240, chosen:true, v: 88123 }]

// 1. Своё нажатие: мгновенно локально, на сервер — троттлингом
const pending = new Map();          // key -> желаемое состояние
function toggle(messageId, emoji) {
  applyLocal(messageId, emoji);     // count +-1, chosen переключён
  markOptimistic(messageId, 3000);  // 3 с чужие события не трогают мой chosen
  pending.set(messageId + ':' + emoji, currentChosen(messageId, emoji));
  scheduleFlush(400);               // trailing: десять тапов -> один запрос
}

// 2. Чужие обновления: буфер, последнее побеждает, слив пачкой
const inbox = new Map();
function onReactionUpdate(u) {
  const cur = getReactions(u.messageId);
  if (cur && u.version <= cur.version) return;      // устаревшее событие
  if (!isInViewport(u.messageId)) { writeToDb(u); return; }  // без рендера
  inbox.set(u.messageId, u);
  scheduleRaf(flushInbox);          // одна перерисовка на кадр, не 30
}

// 3. Округление даёт бесплатный фильтр перерисовок
const fmt = (n) => n < 1000 ? String(n) : (n / 1000).toFixed(1) + 'К';
if (fmt(next) === fmt(prev)) return;   // 1240 -> 1241 не меняет пиксели`,
    tip: 'Назови окно «мой оптимистичный ответ главнее» на 2–3 секунды. Без него собственный лайк мигает при каждом входящем агрегате, и это самый заметный баг реакций — его видно на первом же демо.' },

  { id: 'msg11',
    q: 'Стикеры и кастомные эмодзи. На экране двадцать анимированных стикеров — как не уронить кадры?',
    a: `<h4>Коротко</h4>
<p>Форматы разные по цене: векторный Lottie считается на CPU и стоит дорого, WebM VP9 декодируется аппаратно и почти бесплатен, статический WebP не стоит ничего. Отсюда стратегия: анимации играют только во вьюпорте и только ограниченное число одновременно (обычно 4–8), остальное показывается первым кадром; рендер Lottie уходит в воркеры, панель стикеров инициализируется лениво.</p>

<h4>Форматы и их цена</h4>
<ul>
<li><strong>TGS (Lottie)</strong> — gzip-нутый JSON от Bodymovin, проигрывается библиотекой rlottie. Ограничения у Telegram: холст 512×512, до 3 секунд, 60 fps, до 64 КБ. Векторный, масштабируется без потерь и весит копейки в трафике, но <strong>каждый кадр растеризуется на CPU</strong> — самый дорогой вариант в рендере, плюс rlottie в WebAssembly добавляет 300–600 КБ к бандлу.</li>
<li><strong>WebM с VP9</strong> — видеостикеры, без аудиодорожки, одна из сторон 512px, ограничение около 256 КБ. Декодируются аппаратно средствами браузера: 20 таких на экране стоят кратно дешевле пяти Lottie. Минус — растр, на ретине с масштабированием видно мыло.</li>
<li><strong>WebP</strong> — статические стикеры, лимит 512 КБ. Нулевая цена рендера.</li>
<li><strong>GIF</strong> — только для совместимости, до 2 МБ; кадры не сжимаются нормально, декодер медленный. В новом коде не используем.</li>
</ul>
<p>Практический вывод для интервью: если продукт вправе диктовать формат — берём WebM VP9 и живём спокойно. Lottie нужен там, где важен вес трафика и произвольный масштаб (кастомные эмодзи в тексте, где размер зависит от кегля).</p>

<h4>Как не просадить кадры</h4>
<p>Арифметика: растеризация одного кадра Lottie 512×512 — порядка 1–4 мс на CPU. Двадцать стикеров по 60 fps дают 1200 растеризаций в секунду, то есть от 1.2 до 5 секунд работы на секунду времени. Кадр не соберётся никогда. Четыре приёма, применяемые вместе:</p>
<ol>
<li><strong>Играет только видимое.</strong> Ушёл из вьюпорта — <code>pause()</code>, а не <code>display: none</code>. Свернули вкладку — стоп всему.</li>
<li><strong>Квота одновременных анимаций.</strong> Планировщик держит список активных, максимум 4–8 (для эмодзи в тексте — 2–4). Остальные показывают первый кадр как статику; при скролле квота передаётся тем, кто ближе к центру экрана.</li>
<li><strong>Понижение частоты.</strong> Стикеры не нуждаются в 60 fps: 30 достаточно, а для мелких кастомных эмодзи хватает 24. Это сразу двукратная экономия. Ограничение честное — пропускаем кадры, а не замедляем анимацию.</li>
<li><strong>Растеризация в воркерах.</strong> rlottie в WebAssembly внутри 2–4 воркеров, результат — через OffscreenCanvas или ImageBitmap с передачей владения. Главный поток только рисует готовые битмапы: иначе просадка рендера стикеров становится подвисанием ввода.</li>
</ol>
<p>Пятое, менее очевидное: <strong>кеш растеризованных кадров</strong>. Стикер зациклен и короток — 3 секунды при 30 fps это 90 кадров: растеризовали один раз в ImageBitmap, дальше перекладываем битмапы. В полном разрешении кадр 512×512 весит 1 МБ, что запретительно, поэтому растеризуем в реальный размер отображения (128–160px, то есть 64–100 КБ на кадр) и держим кеш на 2–3 недавних стикера.</p>

<h4>Предзагрузка набора и вытеснение</h4>
<p>Набор — 50–120 стикеров, то есть 5–15 МБ, если тянуть всё при открытии панели. Схема из трёх ступеней: метаданные набора (id, эмодзи-соответствия, миниатюра) — маленький JSON сразу; статические превью первого кадра — пачкой при открытии вкладки; полные анимации — только для видимых ячеек через IntersectionObserver с запасом в строку. Недавние и избранные предзагружаются в фоне при простое.</p>
<p>Файлы стикеров лежат в том же Cache API, что и медиа (msg9), но с отдельным бюджетом в 100–150 МБ и другим приоритетом: установленные наборы «закреплены» и вытесняются последними, случайно просмотренные чужие — первыми. Отсутствующий стикер в чате выглядит как поломка, а чужой набор пользователь всё равно откроет заново.</p>

<h4>Ленивая инициализация панели</h4>
<p>Панель — это сетка на тысячи ячеек, движок анимаций, поиск и вкладки: отдельный чанк, который грузится при первом открытии, а не при старте, — экономия 400–800 КБ на первичной загрузке. Предзагружаем его на <code>mouseenter</code> по кнопке или через <code>requestIdleCallback</code> после первой отрисовки: к моменту клика он уже в кеше. Сама сетка виртуализирована — тот же механизм, что в msg3, но проще: ячейки фиксированной высоты.</p>

<h4>Кастомные эмодзи в тексте</h4>
<p>Самый тяжёлый случай: 20 инлайновых эмодзи в одном сообщении. Решения: рендерить как <code>&lt;span&gt;</code> фиксированного размера по кеглю строки (место резервируется до загрузки, иначе текст переливается — принцип из msg3); анимировать максимум 2–4 на весь экран; в списке диалогов, уведомлениях и заголовках — всегда статикой. Копирование текста должно давать исходный юникод-символ, поэтому в спане лежит настоящий символ, визуально скрытый, а картинка накладывается сверху.</p>

<h4>Компромиссы</h4>
<p>WebM даёт аппаратное декодирование и снимает проблему кадров, но лишает векторности и требует файлов под каждое разрешение. Lottie экономит трафик и масштабируется, но приносит WebAssembly в бандл и вечную борьбу за CPU. Квота анимаций спасает fps ценой того, что часть стикеров стоит статикой — заметно, но меньше, чем рывки скролла. Кеш растровых кадров ускоряет повторы ценой десятков мегабайт памяти.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Считают, что «Lottie лёгкий, потому что файл маленький», и путают вес трафика с ценой рендера.</li>
<li>Ставят <code>display: none</code> вместо <code>pause()</code> — таймеры и растеризация продолжают работать.</li>
<li>Не резервируют место под кастомные эмодзи — текст переливается при загрузке.</li>
<li>Грузят весь набор при открытии панели и получают секунду фриза.</li>
<li>Забывают про <code>prefers-reduced-motion</code>: для части пользователей двадцать дёргающихся картинок — физический дискомфорт, по флагу всё должно замереть.</li>
</ul>`,
    code: `// Планировщик: играет только видимое и только ограниченное число
class StickerScheduler {
  constructor(limit = 6) { this.limit = limit; this.active = new Set(); }

  request(player, distanceFromCenter) {
    if (this.active.size < this.limit) return this.start(player);
    // отбираем квоту у самого дальнего от центра экрана
    const worst = [...this.active].sort((a, b) => b.dist - a.dist)[0];
    if (worst.dist > distanceFromCenter) { this.stop(worst); this.start(player); }
    else player.showPoster();          // первый кадр статикой
  }

  start(p) { this.active.add(p); p.play(); }
  stop(p)  { this.active.delete(p); p.pause(); p.showPoster(); }
}

// Растеризация Lottie вне главного потока, кадры отдаём без копирования
const worker = new Worker('rlottie-worker.js');
worker.postMessage({ id, json, size: 160, fps: 30 });
worker.onmessage = ({ data }) => {
  frameCache.set(data.id, data.frames);   // ImageBitmap[], transferable
};

// Остановить всё, что не нужно прямо сейчас
document.addEventListener('visibilitychange', () => {
  if (document.hidden) scheduler.pauseAll();
});
if (matchMedia('(prefers-reduced-motion: reduce)').matches) scheduler.limit = 0;`,
    tip: 'Посчитай вслух: 20 Lottie по 60 fps — это больше тысячи растеризаций в секунду по 1–4 мс каждая. Арифметика убеждает интервьюера мгновенно и сама объясняет, зачем нужны квота, понижение fps и воркеры.' },

  { id: 'msg12',
    q: 'Нужен ли здесь Service Worker и PWA? Что он даёт и где ломается?',
    a: `<h4>Коротко</h4>
<p>Service Worker нужен ради трёх вещей: мгновенный старт оболочки офлайн, отдача медиа с Range-запросами из кеша (msg9) и push-уведомления. Стратегии кеширования разные для трёх типов ресурсов: оболочка — stale-while-revalidate с версионированием, медиа — cache-first с LRU, данные — вообще мимо кеша, через IndexedDB. Приложение обязано работать и без воркера: в приватном окне, при отключённых куки и на части корпоративных политик его просто нет.</p>

<h4>Стратегии по типам ресурсов</h4>
<ul>
<li><strong>Оболочка</strong> (HTML, JS, CSS, шрифты, иконки). Файлы с хешем в имени — <code>cache-first</code> навсегда, они иммутабельны. Входной HTML — <code>network-first</code> с таймаутом 2–3 секунды и откатом в кеш: иначе пользователь навсегда застрянет на старой версии. Кеш именуем по версии сборки, при активации нового воркера старые кеши удаляем.</li>
<li><strong>Медиа</strong> — <code>cache-first</code>, потому что файл по id иммутабелен: если он есть в кеше, он правильный. Плюс обработка Range из msg9. Вытеснение по своему LRU-бюджету, а не по браузерной квоте.</li>
<li><strong>Данные</strong> (сообщения, диалоги) — <strong>не кешируем в Service Worker вообще</strong>. Это частая ошибка: данные уже лежат в IndexedDB, и второй слой кеша даст только рассинхрон и «сообщение из прошлой недели поверх свежего». Запросы к API проходят насквозь.</li>
<li><strong>Аватары</strong> — <code>stale-while-revalidate</code>: показать старый мгновенно, тихо обновить. Аватар меняется редко, и секунда устаревания никого не волнует.</li>
</ul>

<h4>Обновление без потери состояния</h4>
<p>Мессенджер держат открытым неделями. Классическая ошибка — вызвать <code>skipWaiting()</code> сразу при установке: новый воркер захватит страницу, чанки старой сборки исчезнут из кеша, и любая ленивая подгрузка (та же панель стикеров) упадёт с ошибкой загрузки чанка прямо посреди сессии.</p>
<p>Правильный протокол: новый воркер устанавливается и <strong>ждёт</strong>. Приложение получает событие <code>updatefound</code>, показывает ненавязчивую плашку «доступна новая версия». Пользователь нажимает — шлём воркеру <code>skipWaiting</code>, ловим <code>controllerchange</code>, перезагружаем страницу. Состояние при этом не теряется, потому что оно всё в IndexedDB: черновики, очередь отправки, история. Если пользователь плашку игнорирует — обновляемся автоматически при следующем открытии вкладки, когда неотправленных сообщений нет. Старые чанки при этом держим ещё одну версию, чтобы уже загруженная страница дожила до перезагрузки.</p>

<h4>Push-уведомления</h4>
<p>Путь: разрешение через <code>Notification.requestPermission()</code> — обязательно по явному действию, а не при загрузке, иначе браузер заблокирует и второго шанса не будет; подписка через <code>pushManager.subscribe</code> с VAPID-ключом; endpoint на сервер. Дальше сервер шлёт push, браузер будит Service Worker, и тот в обработчике <code>push</code> обязан показать уведомление — иначе браузер покажет своё «сайт обновился в фоне» и при повторении отзовёт разрешение.</p>
<p>Тонкости: payload ограничен примерно 4 КБ, а при сквозном шифровании текста там нет вовсе — показываем «новое сообщение»; схлопываем по <code>tag</code>, равному chatId, иначе 30 сообщений из группы дадут 30 всплывашек; при клике через <code>clients.matchAll</code> ищем открытую вкладку и фокусируем её вместо новой; если вкладка видима, уведомление не показываем вообще — только звук и счётчик; подписка протухает, поэтому обрабатываем <code>pushsubscriptionchange</code>, иначе уведомления тихо исчезнут.</p>

<h4>Установка приложения</h4>
<p>Манифест с иконками 192 и 512 плюс maskable, <code>display: standalone</code>, <code>start_url</code>. В Chromium <code>beforeinstallprompt</code> перехватывается, чтобы показать свою кнопку в подходящий момент, а не при первом заходе. Установленное PWA почти всегда получает <code>navigator.storage.persist()</code> — защита базы от вытеснения из msg5 и весомый аргумент предлагать установку.</p>

<h4>Ограничения на iOS</h4>
<ul>
<li>Web Push работает <strong>только из установленного на домашний экран</strong> приложения, с iOS 16.4 (март 2023). Из вкладки Safari — не работает вообще.</li>
<li>Никакого <code>beforeinstallprompt</code>: пользователя приходится вести текстом «Поделиться → На экран Домой». Конверсия соответствующая.</li>
<li>Нет Background Sync и Periodic Background Sync — отложенная отправка из msg7 держится только на живой вкладке.</li>
<li>Нет silent push: любой push обязан показать видимое уведомление, фоново досинхронизироваться нельзя.</li>
<li>Badging API с 16.4 есть — счётчик на иконке работает.</li>
<li>Хранилище агрессивно вытесняется: без взаимодействия с сайтом семь дней данные скриптового происхождения удаляются.</li>
</ul>
<p>Вывод для продукта: на iOS честно говорим, что уведомления требуют установки, а полноценный фон недостижим. Это архитектурное ограничение, а не то, что чинится кодом.</p>

<h4>Когда воркера нет</h4>
<p>Приватное окно, отключённое хранилище, корпоративная политика, первый визит до активации. Приложение обязано работать: оболочка грузится по сети, вместо push — внутристраничные уведомления, медиа обычным <code>fetch</code>, видео прямо с сервера с его Range-запросами. Всё это <strong>ветвление, а не падение</strong>: проверяем <code>'serviceWorker' in navigator</code> и наличие контроллера.</p>

<h4>Компромиссы</h4>
<p>Service Worker даёт мгновенный старт и офлайн, но приносит класс багов «пользователь на старой версии» и требует дисциплины версионирования кешей. Отказ от кеширования данных стоит слоя, который ускорил бы холодный старт, зато исключает целый класс рассинхронов. Ожидание вместо <code>skipWaiting</code> означает, что часть пользователей неделями сидит на старой сборке, — компенсируем принудительным обновлением по флагу с сервера при несовместимом протоколе.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Кешируют API-ответы в Service Worker и получают показ старых сообщений поверх новых.</li>
<li>Вызывают <code>skipWaiting</code> безусловно и ломают ленивые чанки открытым сессиям.</li>
<li>Просят разрешение на уведомления при загрузке страницы — блокировка навсегда.</li>
<li>Не схлопывают уведомления по tag — шквал системных всплывашек из одной группы.</li>
<li>Считают, что PWA на iOS «почти как везде», и не знают про требование установки для push.</li>
</ul>`,
    code: `// Роутинг по типам: оболочка, медиа, данные — три разные стратегии
self.addEventListener('fetch', (e) => {
  const url = new URL(e.request.url);

  if (url.pathname.startsWith('/api/')) return;              // данные — насквозь
  if (url.pathname.startsWith('/media/')) return e.respondWith(cacheFirst(e));
  if (e.request.mode === 'navigate') return e.respondWith(networkFirst(e, 2500));
  e.respondWith(cacheFirst(e));                              // хешированные ассеты
});

// Обновление: НЕ skipWaiting сразу, а по решению пользователя
self.addEventListener('message', (e) => {
  if (e.data === 'skipWaiting') self.skipWaiting();
});

// в приложении:
reg.addEventListener('updatefound', () => {
  const sw = reg.installing;
  sw.addEventListener('statechange', () => {
    if (sw.state === 'installed' && navigator.serviceWorker.controller) {
      showUpdateBanner(() => sw.postMessage('skipWaiting'));
    }
  });
});
navigator.serviceWorker.addEventListener('controllerchange', () => location.reload());

// Push: схлопываем по чату, иначе 30 сообщений = 30 всплывашек
self.addEventListener('push', (e) => {
  const d = e.data.json();
  e.waitUntil((async () => {
    const wins = await self.clients.matchAll({ type: 'window' });
    if (wins.some(w => w.visibilityState === 'visible' && w.focused)) return;
    await self.registration.showNotification(d.title, {
      body: d.preview, tag: 'chat-' + d.chatId, renotify: true,
      data: { chatId: d.chatId, messageId: d.messageId },
    });
  })());
});`,
    tip: 'Про iOS назови конкретику: push только из установленного на домашний экран приложения начиная с 16.4, нет beforeinstallprompt, нет Background Sync. Общая фраза «на iOS PWA хуже» ничего не стоит, три факта — стоят.' },

  { id: 'msg13',
    q: 'Пользователь жалуется: печатаю — буквы появляются с задержкой, скролл дёргается. Как ищешь и чем лечишь?',
    a: `<h4>Коротко</h4>
<p>Симптом один — главный поток занят дольше, чем длится кадр. Ищу через Long Animation Frames и профиль Performance, а не гаданием. Типовые виновники в мессенджере: перерисовка всей ленты на каждое нажатие, парсинг и запись входящих пачек, индексация поиска и растеризация стикеров. Лечение — вынести тяжёлое в воркеры, батчить обновления и разорвать связь «composer state» с рендером ленты.</p>

<h4>Что держит поток при печати</h4>
<p>Композер с состоянием на верхнем уровне — самая частая причина. Каждое нажатие обновляет стейт, стейт живёт в компоненте, который рендерит и ленту, и на каждую букву перерисовывается 60 пузырьков. При 25 мс на рендер и печати в 8 знаков в секунду поток занят на 20% — это уже видно. Лечение: композер — изолированный компонент с локальным состоянием, лента подписана на стор, а не получает пропсы сверху. Плюс: «печатает» шлём троттлингом раз в 3–5 секунд, черновик — дебаунсом 500 мс в IndexedDB.</p>
<p>Второй виновник — разбор ввода: превью ссылок, распознавание разметки, кастомные эмодзи. Это регулярки по всему тексту на каждый keystroke. Разбирать надо на дебаунсе и только изменившийся фрагмент.</p>

<h4>Что держит поток при скролле</h4>
<ul>
<li><strong>Синхронное измерение в обработчике скролла</strong>: чтение <code>offsetHeight</code> после записи стилей — принудительный reflow. Чтения в одну фазу, записи в другую.</li>
<li><strong>Неpassive-слушатели</strong>: <code>touchmove</code> без <code>{ passive: true }</code> заставляет браузер ждать возможного <code>preventDefault</code> перед прокруткой.</li>
<li><strong>Входящие обновления во время скролла</strong>: пачка в 200 сообщений, парсинг и запись на 80 мс — пять пропущенных кадров подряд.</li>
<li><strong>Тяжёлые эффекты</strong>: <code>backdrop-filter</code> на шапке и тени на каждом пузырьке — это не JS, а paint и композитинг; в профиле зелёные полосы, а не жёлтые.</li>
</ul>

<h4>INP и как мерить</h4>
<p>Целевая метрика — <strong>INP (Interaction to Next Paint): 200 мс и меньше на 75-м перцентиле</strong>; 200–500 требует улучшения, выше 500 плохо. Меряем в проде через <code>PerformanceObserver</code> на тип <code>event</code> с <code>durationThreshold</code> и обязательно с атрибуцией: какой элемент и какая фаза — задержка до обработчика, обработчик или отрисовка. Без атрибуции цифра бесполезна.</p>
<p>Второй инструмент — <strong>Long Animation Frames</strong> (тип <code>long-animation-frame</code>): в отличие от старого longtask, он показывает не просто «был длинный таск», а какой скрипт его вызвал, сколько ушло на стили и layout. Это то, с чем реально можно идти чинить. Локально — профиль Performance в devtools с 4–6-кратным замедлением CPU: на мощном ноутбуке проблема не воспроизводится никогда.</p>

<h4>Что выносим в воркеры</h4>
<p>Правило: в воркер уходит всё, что не трогает DOM. В мессенджере это разбор входящих пачек и запись в IndexedDB (500 сообщений на главном потоке — 30–60 мс, в воркере ноль), индексация поиска (msg8), дешифровка, растеризация Lottie (msg11), декодирование blurhash. Цена — structured clone на границе: передача 500 объектов туда-обратно сама стоит миллисекунды, поэтому из воркера отдаём готовые к рендеру срезы, а бинари передаём как transferable (ArrayBuffer, ImageBitmap) — по владению, без копирования.</p>

<h4>Батчинг и приоритизация</h4>
<p>Обновления не применяем по одному: события копятся в буфере и сливаются в стор раз в кадр через <code>requestAnimationFrame</code>, а второстепенное (счётчики, presence, реакции из msg10) — раз в 300–500 мс. Приоритеты явные: ввод и скролл синхронно; новые сообщения в открытом чате — на ближайший кадр; список диалогов и счётчики — на idle; индексация и вытеснение кеша — через <code>requestIdleCallback</code> с таймаутом. Длинные циклы рвём через <code>await scheduler.yield()</code> с откатом на <code>setTimeout(0)</code>, держа куски под 50 мс.</p>

<h4>Память при долгих сессиях</h4>
<p>Вкладка мессенджера живёт неделями: утечка в 2 МБ в час — это 300 МБ за неделю и падение. Источники:</p>
<ul>
<li><strong>Подписки.</strong> Каждый компонент сообщения подписан на стор, IntersectionObserver, ResizeObserver. Забытая отписка держит весь компонент с данными. Один общий observer на ленту вместо одного на элемент снимает проблему радикально.</li>
<li><strong>Обработчики на window и document</strong> — их никто не размонтирует за вас.</li>
<li><strong>Object URL.</strong> <code>createObjectURL</code> без <code>revokeObjectURL</code> держит Blob вечно — на медиа это сотни мегабайт.</li>
<li><strong>Кеши без границ</strong>: высоты, кадры стикеров, результаты поиска. Любой Map, куда только пишут, — утечка по определению. Нужен LRU с потолком.</li>
<li><strong>Замыкания на старые сообщения</strong>: обработчик клика держит объект после выгрузки из стора.</li>
</ul>
<p>Диагностика: heap snapshot, 10 минут работы, второй snapshot, сравнение по Delta. Растущее число Detached HTMLElement — прямое указание на утечку через подписки. В проде — <code>performance.measureUserAgentSpecificMemory()</code> раз в час и тренд по нему.</p>

<h4>Компромиссы</h4>
<p>Воркеры убирают фризы, но добавляют асинхронность там, где её не было, а на маленьких порциях structured clone дороже самой работы — воркер имеет смысл от сотен элементов, не от десятков. Батчинг в 300–500 мс делает счётчики «ленивыми»: за плавность платим свежестью. Агрессивная выгрузка из памяти экономит сотни мегабайт, но возврат к выгруженному куску стоит 20–50 мс чтения из базы.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Говорят «мемоизируем компоненты» вместо того, чтобы разорвать связь композера и ленты. <code>React.memo</code> не спасёт, если пропсы меняются на каждую букву.</li>
<li>Меряют на своём ноутбуке без throttling и не воспроизводят проблему.</li>
<li>Называют FID вместо INP: метрика заменена, и FID не ловит те задержки, на которые жалуются.</li>
<li>Оптимизируют рендер, когда виноват paint: тени и фильтры не чинятся мемоизацией.</li>
<li>Не думают про утечки — а вкладку мессенджера не перезагружают неделями.</li>
</ul>`,
    code: `// 1. Атрибуция INP в проде: без неё цифра ни о чём не говорит
new PerformanceObserver((list) => {
  for (const e of list.getEntries()) {
    report('inp', {
      target: e.target && e.target.tagName,
      inputDelay: e.processingStart - e.startTime,   // ждали свободный поток
      handler:    e.processingEnd - e.processingStart,
      presentation: e.startTime + e.duration - e.processingEnd, // отрисовка
    });
  }
}).observe({ type: 'event', durationThreshold: 40, buffered: true });

// 2. Long Animation Frames показывают ВИНОВНИКА, а не только факт
new PerformanceObserver((list) => {
  for (const f of list.getEntries()) {
    if (f.duration < 100) continue;
    report('loaf', { blocking: f.blockingDuration, scripts: f.scripts.map(s => s.name) });
  }
}).observe({ type: 'long-animation-frame', buffered: true });

// 3. Батчинг входящих: одна перерисовка на кадр вместо N на событие
let queued = null;
function onUpdates(batch) {
  (queued ||= []).push(...batch);
  if (queued.length === batch.length) requestAnimationFrame(flush);
}

// 4. Длинную работу режем, чтобы ввод успевал вклиниться
async function reindex(all) {
  for (let i = 0; i < all.length; i += 500) {
    indexChunk(all.slice(i, i + 500));
    await (scheduler.yield ? scheduler.yield() : new Promise(r => setTimeout(r)));
  }
}`,
    tip: 'Начни с диагностики, а не с рецептов: «беру Long Animation Frames с атрибуцией и профиль с четырёхкратным throttling». Кандидат, который сразу сыплет оптимизациями, не показав, как нашёл узкое место, выглядит слабее.' },

  { id: 'msg14',
    q: 'У пользователя открыто три вкладки мессенджера и телефон. Как всё это не рассинхронить?',
    a: `<h4>Коротко</h4>
<p>Один сокет на все вкладки: лидер выбирается через Web Locks, остальные вкладки — клиенты, общающиеся с ним по BroadcastChannel. Общее состояние — в IndexedDB, она и есть источник правды; вкладки обмениваются только сигналами «перечитай вот это», а не самими данными. Между устройствами конфликты разрешает сервер: клиент никогда не считает своё локальное состояние авторитетным.</p>

<h4>Единственный владелец сокета</h4>
<p>Три вкладки — это три сокета, три потока обновлений, тройная работа по разбору и записи. Хуже того, три независимых менеджера обновлений (msg6) наперегонки пишут pts в одну базу и гарантированно его ломают.</p>
<p>Выбор лидера: <code>navigator.locks.request('socket-owner', { mode: 'exclusive' }, ...)</code> с промисом, который никогда не резолвится. Первая вкладка берёт лок и держит, пока жива, остальные висят в очереди. Когда лидер закрывается или падает, браузер освобождает лок <strong>автоматически</strong> — следующий становится лидером за миллисекунды. В этом преимущество перед самодельной схемой на localStorage с heartbeat: там смерть лидера ловится по таймауту в несколько секунд, и всё это время приложение мертво. SharedWorker архитектурно красивее, но не поддержан в Safari на iOS; Service Worker не годится вовсе — засыпает через десятки секунд бездействия.</p>

<h4>Обмен между вкладками</h4>
<p><code>BroadcastChannel</code> — канал по origin, все вкладки слышат всех, сообщения проходят через structured clone. Что по нему шлём:</p>
<ul>
<li>От лидера: «применена пачка обновлений для чата X, диапазон такой-то» — <strong>сигнал, а не данные</strong>. Вкладки перечитывают нужное из IndexedDB сами; гонять сообщения через канал значит копировать их N раз и рисковать расхождением с базой.</li>
<li>От вкладки лидеру: команды на отправку, подписка на присутствие в чате.</li>
<li>Между всеми: локальные оптимистичные действия — отправка, реакция (msg10), прочтение. Их нужно видеть во всех вкладках до подтверждения сервером.</li>
<li>Служебное: logout, «база обновилась до версии N, перезагрузись».</li>
</ul>
<p>Фолбэк для старых движков — событие <code>storage</code> на localStorage: пишем ключ со случайным значением, соседи ловят. Работает везде, но синхронно и только со строками.</p>

<h4>Разграничение обязанностей</h4>
<p>Лидер держит сокет, применяет обновления, пишет в базу, качает разницу и гонит очередь отправки. Остальные читают базу, рисуют и отдают команды лидеру. Тонкость: <strong>очередь отправки только у лидера</strong> — иначе одно сообщение уйдёт трижды, и спасёт лишь идемпотентность по clientId (msg7), которую лучше не проверять на прочность. Presence и «печатает» шлёт лидер, но от имени активной вкладки, иначе, печатая в одной, вы «печатаете» в чате из другой.</p>

<h4>Блокировки для критических секций</h4>
<p>Web Locks нужны не только для лидерства. Апгрейд схемы IndexedDB (msg5) — под именованным локом, иначе две вкладки начнут миграцию одновременно. Обновление токена — тоже: три вкладки, увидевшие 401, пойдут за токеном параллельно, две получат «refresh-токен уже использован», и пользователя выкинет.</p>

<h4>Счётчики непрочитанного</h4>
<p>Самое заметное место рассинхрона. Счётчик — <strong>производная</strong> от <code>maxReadIncomingId</code> в диалоге, а не самостоятельное число: инкрементируемый счётчик копит ошибку от каждого пропущенного или продублированного события и гарантированно разъезжается. Прочтение — это «поднять maxReadIncomingId до X», операция монотонная и идемпотентная: применение дважды или не по порядку ничего не ломает, побеждает максимум. Применяем оптимистично, транслируем в BroadcastChannel, шлём на сервер; при расхождении побеждает серверное. Общий бейдж (msg12) ставит лидер, иначе три вкладки наперебой перезапишут иконку.</p>

<h4>Черновики</h4>
<p>Черновик — тоже данные аккаунта, Telegram синхронизирует его между устройствами. Локально: дебаунс 500 мс в IndexedDB плюс трансляция соседям. На сервер реже — раз в 3–5 секунд, при уходе с чата и на <code>visibilitychange</code> в hidden (не на <code>beforeunload</code>: на мобильных он часто не срабатывает). Конфликт разрешаем по времени изменения, но <strong>не затираем непустой локальный черновик пустым удалённым</strong>, если пользователь прямо сейчас в этом поле.</p>

<h4>Выход из аккаунта</h4>
<p>Должен быть атомарным для всех вкладок: вкладка шлёт серверу «убить сессию» и кидает в BroadcastChannel сигнал logout; все вкладки немедленно переходят на экран входа, <strong>не дожидаясь</strong> очистки; лидер закрывает сокет, отписывает push (<code>subscription.unsubscribe()</code> — иначе на чужом компьютере продолжат приходить уведомления), удаляет базы и чистит Cache API; соседи на <code>versionchange</code> обязаны закрыть соединения, иначе <code>deleteDatabase</code> заблокируется навсегда. Если в очереди есть неотправленное — спрашиваем перед выходом, молча стирать нельзя.</p>

<h4>Между устройствами</h4>
<p>Клиентской магии здесь нет: арбитр — сервер. Каждое устройство ведёт свой pts и догоняет разницу независимо (msg6); прочтения, черновики и настройки — часть состояния аккаунта. При сквозном шифровании сложнее: у WhatsApp каждое устройство имеет свои ключи, сервер хранит зашифрованную копию состояния приложения, а сообщение шифруется отдельно под каждое устройство получателя. Отсюда лимит на связанные устройства (четыре): фанаут растёт линейно.</p>

<h4>Компромиссы</h4>
<p>Один сокет на лидере исключает гонки, но добавляет окно переизбрания — десятки миллисекунд с Web Locks против секунд с самодельным heartbeat. Если бы вкладка была заведомо одна, весь слой был бы лишним кодом. Сигналы вместо данных исключают расхождение ценой лишнего чтения из базы в каждой вкладке. BroadcastChannel не работает между браузерами и профилями — там только сервер.</p>

<h4>Что упускают кандидаты</h4>
<ul>
<li>Открывают сокет в каждой вкладке — и не понимают, откуда дубли и почему pts скачет.</li>
<li>Инкрементируют счётчик непрочитанного вместо вычисления из maxReadIncomingId.</li>
<li>Гоняют по BroadcastChannel сами сообщения вместо сигналов — база и вкладки расходятся.</li>
<li>Не обрабатывают <code>versionchange</code> — <code>deleteDatabase</code> при выходе висит вечно.</li>
<li>Забывают отписать push при выходе: чужой компьютер продолжает получать уведомления — это уже безопасность, а не удобство.</li>
</ul>`,
    code: `// Лидер: лок держится, пока жива вкладка. Умерла — браузер отдаст следующему
async function becomeLeaderWhenPossible() {
  await navigator.locks.request('socket-owner', { mode: 'exclusive' }, () =>
    new Promise(() => {           // никогда не резолвится = держим лок
      openSocket();
      startSendQueue();           // очередь отправки ТОЛЬКО у лидера
      startUpdatesManager();
    })
  );
}

// Обмен: шлём сигналы, а не данные. Источник правды — IndexedDB
const bus = new BroadcastChannel('app');

bus.onmessage = async ({ data }) => {
  switch (data.type) {
    case 'updates':  await store.reloadRange(data.chatId, data.from, data.to); break;
    case 'read':     store.setMaxRead(data.chatId, data.maxId); break;   // монотонно
    case 'draft':    store.setDraft(data.chatId, data.text, data.at); break;
    case 'logout':   hardResetUi(); break;
  }
};

// Непрочитанные — производная, а не инкремент: расходиться нечему
const unread = (d) => d.totalIncoming - countUpTo(d.chatId, d.maxReadIncomingId);

// Обновление токена — под локом, иначе три вкладки сожгут refresh-токен
const token = await navigator.locks.request('auth-refresh', async () => {
  const fresh = await db.get('kv', 'token');
  return isValid(fresh) ? fresh : refreshAndStore();
});`,
    tip: 'Главная фраза: «по BroadcastChannel идут сигналы, а данные читаются из IndexedDB — она источник правды». И добавь, что счётчик непрочитанного вычисляется из maxReadIncomingId, а не инкрементируется: это ровно тот баг, который в проде живёт годами.' },

];
