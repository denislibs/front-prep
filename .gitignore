node_modules/
.DS_Store

# Локальные настройки инструментов
.claude/

# Временный файл проверки синтаксиса из build.js
.build-check.js

# Копия собранной страницы под именем для артефакта.
# В репозитории публикуется index.html — его отдаёт GitHub Pages.
# Восстанавливается командой: node build.js
app.html

# Генерируется export-notes.js из research/ и шаблона
notes-src/

# Сборка сайта: делается в CI, в репозитории не нужна
dist/

# Тяжёлые библиотеки, вынесенные из index.html — генерируются build.js
assets/

# Личное: рассказ о себе, истории, заметки — в публичный репозиторий не идёт
personal/
